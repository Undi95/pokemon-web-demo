// AUTO-GENERATED from src/link.c by extract-engine-helpers.mjs
// Do not edit — re-run `node scripts/extract-engine-helpers.mjs` to refresh.
//
// Generated: 2026-04-27
// Functions: 117

export const ENGINE_FUNCTIONS = {
  "AreAnyLinkPlayersUsingVersions": {
    returnType: "static int",
    params: "u32 version1, u32 version2",
    callsTo: ["GetLinkPlayerCount"],
    lineCount: 10,
    bodyC: "int i;\n    u8 nPlayers;\n\n    nPlayers = GetLinkPlayerCount();\n    for (i = 0; i < nPlayers; i++)\n    {\n        if ((gLinkPlayers[i].version & 0xFF) == version1\n         || (gLinkPlayers[i].version & 0xFF) == version2)\n            return 1;\n    }\n    return -1;",
  },
  "BitmaskAllOtherLinkPlayers": {
    returnType: "u8",
    params: "void",
    callsTo: ["GetMultiplayerId"],
    lineCount: 3,
    bodyC: "u8 mpId;\n\n    mpId = GetMultiplayerId();\n    return ((1 << MAX_LINK_PLAYERS) - 1) ^ (1 << mpId);",
  },
  "BuildSendCmd": {
    returnType: "static void",
    params: "u16 command",
    lineCount: 58,
    bodyC: "switch (command)\n    {\n        case LINKCMD_SEND_LINK_TYPE:\n            gSendCmd[0] = LINKCMD_SEND_LINK_TYPE;\n            gSendCmd[1] = gLinkType;\n            break;\n        case LINKCMD_READY_EXIT_STANDBY:\n            gSendCmd[0] = LINKCMD_READY_EXIT_STANDBY;\n            break;\n        case LINKCMD_BLENDER_SEND_KEYS:\n            gSendCmd[0] = LINKCMD_BLENDER_SEND_KEYS;\n            gSendCmd[1] = gMain.heldKeys;\n            break;\n        case LINKCMD_DUMMY_1:\n            gSendCmd[0] = LINKCMD_DUMMY_1;\n            break;\n        case LINKCMD_SEND_EMPTY:\n            gSendCmd[0] = LINKCMD_SEND_EMPTY;\n            gSendCmd[1] = 0;\n            break;\n        case LINKCMD_SEND_0xEE:\n        {\n            u8 i;\n            gSendCmd[0] = LINKCMD_SEND_0xEE;\n            for (i = 0; i < 5; i++)\n                gSendCmd[i + 1] = 0xEE;\n            break;\n        }\n        case LINKCMD_INIT_BLOCK:\n            gSendCmd[0] = LINKCMD_INIT_BLOCK;\n            gSendCmd[1] = sBlockSend.size;\n            gSendCmd[2] = sBlockSend.multiplayerId + 0x80;\n            break;\n        case LINKCMD_BLENDER_NO_PBLOCK_SPACE:\n            gSendCmd[0] = LINKCMD_BLENDER_NO_PBLOCK_SPACE;\n            break;\n        case LINKCMD_SEND_ITEM:\n            gSendCmd[0] = LINKCMD_SEND_ITEM;\n            gSendCmd[1] = gSpecialVar_ItemId;\n            break;\n        case LINKCMD_SEND_BLOCK_REQ:\n            gSendCmd[0] = LINKCMD_SEND_BLOCK_REQ;\n            gSendCmd[1] = gBlockRequestType;\n            break;\n        case LINKCMD_READY_CLOSE_LINK:\n            gSendCmd[0] = LINKCMD_READY_CLOSE_LINK;\n            gSendCmd[1] = gReadyCloseLinkType;\n            break;\n        case LINKCMD_DUMMY_2:\n            gSendCmd[0] = LINKCMD_DUMMY_2;\n            break;\n        case LINKCMD_SEND_HELD_KEYS:\n            if (gHeldKeyCodeToSend == 0 || gLinkTransferringData)\n                break;\n\n            gSendCmd[0] = LINKCMD_SEND_HELD_KEYS;\n            gSendCmd[1] = gHeldKeyCodeToSend;\n            break;\n    }",
  },
  "CB2_LinkError": {
    returnType: "void",
    params: "void",
    callsTo: ["ARRAY_COUNT","Alloc","AnimateSprites","BG_PLTT_ID","BuildOamBuffer","ClearGpuRegBits","CreateTask","DeactivateAllTextPrinters","FreeAllSpritePalettes","InitBgsFromTemplates","InitHeap","InitWindows","LoadPalette","ResetBgsAndClearDma3BusyFlags","ResetLinkRfuGFLayer","ResetPaletteFadeControl","ResetSpriteData","ResetTasks","ResetTempTileDataBuffers","RunTasks","ScanlineEffect_Stop","SetBackdropFromColor","SetBgTilemapBuffer","SetGpuReg","SetMainCallback2","SetVBlankCallback","StopMapMusic","UpdatePaletteFade","m4aMPlayStop"],
    lineCount: 45,
    bodyC: "u8 *tilemapBuffer;\n\n    SetGpuReg(REG_OFFSET_DISPCNT, 0);\n    m4aMPlayStop(&gMPlayInfo_SE1);\n    m4aMPlayStop(&gMPlayInfo_SE2);\n    m4aMPlayStop(&gMPlayInfo_SE3);\n    InitHeap(gHeap, HEAP_SIZE);\n    ResetSpriteData();\n    FreeAllSpritePalettes();\n    ResetPaletteFadeControl();\n    SetBackdropFromColor(RGB_BLACK);\n    ResetTasks();\n    ScanlineEffect_Stop();\n    if (gWirelessCommType)\n    {\n        if (!sLinkErrorBuffer.disconnected)\n            gWirelessCommType = 3;\n\n        ResetLinkRfuGFLayer();\n    }\n    SetVBlankCallback(VBlankCB_LinkError);\n    ResetBgsAndClearDma3BusyFlags(0);\n    InitBgsFromTemplates(0, sLinkErrorBgTemplates, ARRAY_COUNT(sLinkErrorBgTemplates));\n    sLinkErrorBgTilemapBuffer = tilemapBuffer = Alloc(BG_SCREEN_SIZE);\n    SetBgTilemapBuffer(1, tilemapBuffer);\n    if (InitWindows(sLinkErrorWindowTemplates))\n    {\n        DeactivateAllTextPrinters();\n        ResetTempTileDataBuffers();\n        SetGpuReg(REG_OFFSET_BLDCNT, 0);\n        SetGpuReg(REG_OFFSET_BLDALPHA, 0);\n        SetGpuReg(REG_OFFSET_BG0HOFS, 0);\n        SetGpuReg(REG_OFFSET_BG0VOFS, 0);\n        SetGpuReg(REG_OFFSET_BG1HOFS, 0);\n        SetGpuReg(REG_OFFSET_BG1VOFS, 0);\n        ClearGpuRegBits(REG_OFFSET_DISPCNT, DISPCNT_WIN0_ON | DISPCNT_WIN1_ON | DISPCNT_OBJWIN_ON);\n        LoadPalette(gStandardMenuPalette, BG_PLTT_ID(15), PLTT_SIZE_4BPP);\n        gSoftResetDisabled = FALSE;\n        CreateTask(Task_DestroySelf, 0);\n        StopMapMusic();\n        gMain.callback1 = NULL;\n        RunTasks();\n        AnimateSprites();\n        BuildOamBuffer();\n        UpdatePaletteFade();\n        SetMainCallback2(CB2_PrintErrorMessage);\n    }",
  },
  "CB2_LinkTest": {
    returnType: "static void",
    params: "void",
    callsTo: ["AnimateSprites","BuildOamBuffer","LinkTestProcessKeyInput","RunTasks","TestBlockTransfer","UpdatePaletteFade"],
    lineCount: 6,
    bodyC: "LinkTestProcessKeyInput();\n    TestBlockTransfer(1, 1, 0);\n    RunTasks();\n    AnimateSprites();\n    BuildOamBuffer();\n    UpdatePaletteFade();",
  },
  "CB2_PrintErrorMessage": {
    returnType: "static void",
    params: "void",
    callsTo: ["AddTextPrinterParameterized3","DoSoftReset","ErrorMsg_CheckConnections","ErrorMsg_MoveCloserToPartner","JOY_NEW","PlaySE","ReloadSave","ShowBg","rfu_REQ_stopMode","rfu_waitREQComplete"],
    lineCount: 53,
    bodyC: "switch (gMain.state)\n    {\n        case  00:\n             \n             \n            if (sLinkErrorBuffer.disconnected)\n                ErrorMsg_MoveCloserToPartner();\n            else\n                ErrorMsg_CheckConnections();\n            break;\n        case  02:\n            ShowBg(0);\n            if (sLinkErrorBuffer.disconnected)\n                ShowBg(1);\n            break;\n        case  30:\n            PlaySE(SE_BOO);\n            break;\n        case  60:\n            PlaySE(SE_BOO);\n            break;\n        case  90:\n            PlaySE(SE_BOO);\n            break;\n        case 130:\n            if (gWirelessCommType == 2)\n                AddTextPrinterParameterized3(WIN_LINK_ERROR_TOP, FONT_SHORT_COPY_1, 2, 20, sTextColors, 0, gText_ABtnTitleScreen);\n            else if (gWirelessCommType == 1)\n                AddTextPrinterParameterized3(WIN_LINK_ERROR_TOP, FONT_SHORT_COPY_1, 2, 20, sTextColors, 0, gText_ABtnRegistrationCounter);\n            break;\n    }\n    if (gMain.state == 160)\n    {\n        if (gWirelessCommType == 1)\n        {\n            if (JOY_NEW(A_BUTTON))\n            {\n                PlaySE(SE_PIN);\n                gWirelessCommType = 0;\n                sLinkErrorBuffer.disconnected = FALSE;\n                ReloadSave();\n            }\n        }\n        else if (gWirelessCommType == 2)\n        {\n            if (JOY_NEW(A_BUTTON))\n            {\n                rfu_REQ_stopMode();\n                rfu_waitREQComplete();\n                DoSoftReset();\n            }\n        }\n    }\n\n    if (gMain.state != 160)\n        gMain.state++;",
  },
  "CheckLinkPlayersMatchSaved": {
    returnType: "void",
    params: "void",
    callsTo: ["CloseLink","SetMainCallback2","StringCompare"],
    lineCount: 11,
    bodyC: "u8 i;\n\n    for (i = 0; i < gSavedLinkPlayerCount; i++)\n    {\n        if (sSavedLinkPlayers[i].trainerId != gLinkPlayers[i].trainerId\n         || StringCompare(sSavedLinkPlayers[i].name, gLinkPlayers[i].name) != 0)\n        {\n            gLinkErrorOccurred = TRUE;\n            CloseLink();\n            SetMainCallback2(CB2_LinkError);\n        }\n    }",
  },
  "CheckMasterOrSlave": {
    returnType: "static void",
    params: "void",
    lineCount: 10,
    bodyC: "u32 terminals;\n\n    terminals = *(vu32 *)REG_ADDR_SIOCNT & (SIO_MULTI_SD | SIO_MULTI_SI);\n    if (terminals == SIO_MULTI_SD && gLink.localId == 0)\n    {\n        gLink.isMaster = LINK_MASTER;\n    }\n    else\n    {\n        gLink.isMaster = LINK_SLAVE;\n    }",
  },
  "CheckShouldAdvanceLinkState": {
    returnType: "void",
    params: "void",
    callsTo: ["EXTRACT_PLAYER_COUNT"],
    lineCount: 2,
    bodyC: "if ((gLinkStatus & LINK_STAT_MASTER) && EXTRACT_PLAYER_COUNT(gLinkStatus) > 1)\n        gShouldAdvanceLinkState = 1;",
  },
  "ClearLinkCallback": {
    returnType: "void",
    params: "void",
    callsTo: ["ClearLinkRfuCallback"],
    lineCount: 4,
    bodyC: "if (gWirelessCommType)\n        ClearLinkRfuCallback();\n    else\n        gLinkCallback = NULL;",
  },
  "ClearLinkCallback_2": {
    returnType: "void",
    params: "void",
    callsTo: ["ClearLinkRfuCallback"],
    lineCount: 4,
    bodyC: "if (gWirelessCommType)\n        ClearLinkRfuCallback();\n    else\n        gLinkCallback = NULL;",
  },
  "ClearSavedLinkPlayers": {
    returnType: "void",
    params: "void",
    callsTo: ["CpuSet","memset"],
    lineCount: 7,
    bodyC: "#ifdef UBFIX\n    memset(sSavedLinkPlayers, 0, sizeof(sSavedLinkPlayers));\n#else\n    int i;\n    for (i = 0; i < MAX_LINK_PLAYERS; i++)\n        CpuSet(&sSavedLinkPlayers[i], NULL, sizeof(struct LinkPlayer));\n#endif",
  },
  "CloseLink": {
    returnType: "void",
    params: "void",
    callsTo: ["DisableSerial","LinkRfu_Shutdown"],
    lineCount: 5,
    bodyC: "gReceivedRemoteLinkPlayers = FALSE;\n    if (gWirelessCommType)\n        LinkRfu_Shutdown();\n    sLinkOpen = FALSE;\n    DisableSerial();",
  },
  "ConvertLinkPlayerName": {
    returnType: "void",
    params: "struct LinkPlayer *player",
    callsTo: ["ConvertInternationalString"],
    lineCount: 2,
    bodyC: "player->progressFlagsCopy = player->progressFlags;  \n    ConvertInternationalString(player->name, player->language);",
  },
  "DequeueRecvCmds": {
    returnType: "static void",
    params: "u16 (*recvCmds)[CMD_LENGTH]",
    lineCount: 33,
    bodyC: "u8 i;\n    u8 j;\n\n    gLinkSavedIme = REG_IME;\n    REG_IME = 0;\n    if (gLink.recvQueue.count == 0)\n    {\n        for (i = 0; i < gLink.playerCount; i++)\n        {\n            for (j = 0; j < CMD_LENGTH; j++)\n            {\n                recvCmds[i][j] = 0;\n            }\n        }\n\n        gLink.receivedNothing = TRUE;\n    }\n    else\n    {\n        for (i = 0; i < gLink.playerCount; i++)\n        {\n            for (j = 0; j < CMD_LENGTH; j++)\n            {\n                recvCmds[i][j] = gLink.recvQueue.data[i][j][gLink.recvQueue.pos];\n            }\n        }\n        gLink.recvQueue.count--;\n        gLink.recvQueue.pos++;\n        if (gLink.recvQueue.pos >= QUEUE_CAPACITY)\n        {\n            gLink.recvQueue.pos = 0;\n        }\n        gLink.receivedNothing = FALSE;\n    }\n    REG_IME = gLinkSavedIme;",
  },
  "DisableSerial": {
    returnType: "static void",
    params: "void",
    callsTo: ["CpuFill32","DisableInterrupts","REG_TMCNT_H"],
    lineCount: 7,
    bodyC: "DisableInterrupts(INTR_FLAG_TIMER3 | INTR_FLAG_SERIAL);\n    REG_SIOCNT = SIO_MULTI_MODE;\n    REG_TMCNT_H(3) = 0;\n    REG_IF = INTR_FLAG_TIMER3 | INTR_FLAG_SERIAL;\n    REG_SIOMLT_SEND = 0;\n    REG_SIOMLT_RECV = 0;\n    CpuFill32(0, &gLink, sizeof(gLink));",
  },
  "DoHandshake": {
    returnType: "static bool8",
    params: "void",
    lineCount: 46,
    bodyC: "u8 i;\n    u8 playerCount;\n    u16 minRecv;\n\n    playerCount = 0;\n    minRecv = 0xFFFF;\n    if (gLink.handshakeAsMaster == TRUE)\n    {\n        REG_SIOMLT_SEND = MASTER_HANDSHAKE;\n    }\n    else\n    {\n        REG_SIOMLT_SEND = SLAVE_HANDSHAKE;\n    }\n    *(u64 *)gLink.handshakeBuffer = REG_SIOMLT_RECV;\n    REG_SIOMLT_RECV = 0;\n    gLink.handshakeAsMaster = FALSE;\n    for (i = 0; i < MAX_LINK_PLAYERS; i++)\n    {\n        if ((gLink.handshakeBuffer[i] & ~0x3) == SLAVE_HANDSHAKE || gLink.handshakeBuffer[i] == MASTER_HANDSHAKE)\n        {\n            playerCount++;\n            if (minRecv > gLink.handshakeBuffer[i] && gLink.handshakeBuffer[i] != 0)\n                minRecv = gLink.handshakeBuffer[i];\n        }\n        else\n        {\n            if (gLink.handshakeBuffer[i] != 0xFFFF)\n                playerCount = 0;\n            break;\n        }\n    }\n    gLink.playerCount = playerCount;\n    if (gLink.playerCount > 1 && gLink.playerCount == sHandshakePlayerCount && gLink.handshakeBuffer[0] == MASTER_HANDSHAKE)\n    {\n        return TRUE;\n    }\n    if (gLink.playerCount > 1)\n    {\n        gLink.link_field_F = (minRecv & 3) + 1;\n    }\n    else\n    {\n        gLink.link_field_F = 0;\n    }\n    sHandshakePlayerCount = gLink.playerCount;\n    return FALSE;",
  },
  "DoRecv": {
    returnType: "static void",
    params: "void",
    lineCount: 43,
    bodyC: "u16 recv[4];\n    u8 i;\n    u8 index;\n\n    *(u64 *)recv = REG_SIOMLT_RECV;\n    if (gLink.sendCmdIndex == 0)\n    {\n        for (i = 0; i < gLink.playerCount; i++)\n        {\n            if (gLink.checksum != recv[i] && sChecksumAvailable)\n            {\n                gLink.badChecksum = TRUE;\n            }\n        }\n        gLink.checksum = 0;\n        sChecksumAvailable = TRUE;\n    }\n    else\n    {\n        index = gLink.recvQueue.pos + gLink.recvQueue.count;\n        if (index >= QUEUE_CAPACITY)\n        {\n            index -= QUEUE_CAPACITY;\n        }\n        if (gLink.recvQueue.count < QUEUE_CAPACITY)\n        {\n            for (i = 0; i < gLink.playerCount; i++)\n            {\n                gLink.checksum += recv[i];\n                sRecvNonzeroCheck |= recv[i];\n                gLink.recvQueue.data[i][gLink.recvCmdIndex][index] = recv[i];\n            }\n        }\n        else\n        {\n            gLink.queueFull = QUEUE_FULL_RECV;\n        }\n        gLink.recvCmdIndex++;\n        if (gLink.recvCmdIndex == CMD_LENGTH && sRecvNonzeroCheck)\n        {\n            gLink.recvQueue.count++;\n            sRecvNonzeroCheck = 0;\n        }\n    }",
  },
  "DoSend": {
    returnType: "static void",
    params: "void",
    lineCount: 33,
    bodyC: "if (gLink.sendCmdIndex == CMD_LENGTH)\n    {\n        REG_SIOMLT_SEND = gLink.checksum;\n        if (!sSendBufferEmpty)\n        {\n            gLink.sendQueue.count--;\n            gLink.sendQueue.pos++;\n            if (gLink.sendQueue.pos >= QUEUE_CAPACITY)\n            {\n                gLink.sendQueue.pos = 0;\n            }\n        }\n        else\n        {\n            sSendBufferEmpty = FALSE;\n        }\n    }\n    else\n    {\n        if (!sSendBufferEmpty && gLink.sendQueue.count == 0)\n        {\n            sSendBufferEmpty = TRUE;\n        }\n        if (sSendBufferEmpty)\n        {\n            REG_SIOMLT_SEND = 0;\n        }\n        else\n        {\n            REG_SIOMLT_SEND = gLink.sendQueue.data[gLink.sendCmdIndex][gLink.sendQueue.pos];\n        }\n        gLink.sendCmdIndex++;\n    }",
  },
  "DoesLinkPlayerCountMatchSaved": {
    returnType: "bool8",
    params: "void",
    callsTo: ["GetLinkPlayerCount_2"],
    lineCount: 25,
    bodyC: "int i;\n    u32 count = 0;\n\n    for (i = 0; i < gSavedLinkPlayerCount; i++)\n    {\n        if (gLinkPlayers[i].trainerId == sSavedLinkPlayers[i].trainerId)\n        {\n            if (gLinkType == LINKTYPE_BATTLE_TOWER)\n            {\n                if (gLinkType == gLinkPlayers[i].linkType)\n                    count++;\n            }\n            else\n            {\n                count++;\n            }\n        }\n    }\n    if (count == gSavedLinkPlayerCount)\n    {\n        if (GetLinkPlayerCount_2() == gSavedLinkPlayerCount)\n        {\n            return TRUE;\n        }\n    }\n    return FALSE;",
  },
  "EnableSerial": {
    returnType: "static void",
    params: "void",
    callsTo: ["CpuFill32","DisableInterrupts","EnableInterrupts"],
    lineCount: 14,
    bodyC: "DisableInterrupts(INTR_FLAG_TIMER3 | INTR_FLAG_SERIAL);\n    REG_RCNT = 0;\n    REG_SIOCNT = SIO_MULTI_MODE;\n    REG_SIOCNT |= SIO_115200_BPS | SIO_INTR_ENABLE;\n    EnableInterrupts(INTR_FLAG_SERIAL);\n    REG_SIOMLT_SEND = 0;\n    CpuFill32(0, &gLink, sizeof(gLink));\n    sNumVBlanksWithoutSerialIntr = 0;\n    sSendNonzeroCheck = 0;\n    sRecvNonzeroCheck = 0;\n    sChecksumAvailable = 0;\n    sHandshakePlayerCount = 0;\n    gLastSendQueueCount = 0;\n    gLastRecvQueueCount = 0;",
  },
  "EnqueueSendCmd": {
    returnType: "static void",
    params: "u16 *sendCmd",
    lineCount: 30,
    bodyC: "u8 i;\n    u8 offset;\n\n    gLinkSavedIme = REG_IME;\n    REG_IME = 0;\n    if (gLink.sendQueue.count < QUEUE_CAPACITY)\n    {\n        offset = gLink.sendQueue.pos + gLink.sendQueue.count;\n        if (offset >= QUEUE_CAPACITY)\n        {\n            offset -= QUEUE_CAPACITY;\n        }\n        for (i = 0; i < CMD_LENGTH; i++)\n        {\n            sSendNonzeroCheck |= *sendCmd;\n            gLink.sendQueue.data[i][offset] = *sendCmd;\n            *sendCmd = 0;\n            sendCmd++;\n        }\n    }\n    else\n    {\n        gLink.queueFull = QUEUE_FULL_SEND;\n    }\n    if (sSendNonzeroCheck)\n    {\n        gLink.sendQueue.count++;\n        sSendNonzeroCheck = 0;\n    }\n    REG_IME = gLinkSavedIme;\n    gLastSendQueueCount = gLink.sendQueue.count;",
  },
  "ErrorMsg_CheckConnections": {
    returnType: "static void",
    params: "void",
    callsTo: ["AddTextPrinterParameterized3","CopyWindowToVram","FillWindowPixelBuffer","LoadBgTiles","PIXEL_FILL","PutWindowTilemap"],
    lineCount: 8,
    bodyC: "LoadBgTiles(0, sCommErrorBg_Gfx, 0x20, 0);\n    FillWindowPixelBuffer(WIN_LINK_ERROR_MID, PIXEL_FILL(0));\n    FillWindowPixelBuffer(WIN_LINK_ERROR_BOTTOM, PIXEL_FILL(0));\n    AddTextPrinterParameterized3(WIN_LINK_ERROR_MID, FONT_SHORT_COPY_1, 2, 0, sTextColors, 0, gText_CommErrorCheckConnections);\n    PutWindowTilemap(WIN_LINK_ERROR_MID);\n    PutWindowTilemap(WIN_LINK_ERROR_BOTTOM);\n    CopyWindowToVram(WIN_LINK_ERROR_MID, COPYWIN_NONE);  \n    CopyWindowToVram(WIN_LINK_ERROR_BOTTOM, COPYWIN_FULL);",
  },
  "ErrorMsg_MoveCloserToPartner": {
    returnType: "static void",
    params: "void",
    callsTo: ["AddTextPrinterParameterized3","BG_PLTT_ID","CopyBgTilemapBufferToVram","CopyToBgTilemapBuffer","CopyWindowToVram","DecompressAndLoadBgGfxUsingHeap","FillWindowPixelBuffer","LoadBgTiles","LoadPalette","PIXEL_FILL","PutWindowTilemap"],
    lineCount: 13,
    bodyC: "LoadBgTiles(0, sCommErrorBg_Gfx, 0x20, 0);\n    DecompressAndLoadBgGfxUsingHeap(1, sWirelessLinkDisplayGfx, FALSE, 0, 0);\n    CopyToBgTilemapBuffer(1, sWirelessLinkDisplayTilemap, 0, 0);\n    CopyBgTilemapBufferToVram(1);\n    LoadPalette(sWirelessLinkDisplayPal, BG_PLTT_ID(0), sizeof(sWirelessLinkDisplayPal));\n    FillWindowPixelBuffer(WIN_LINK_ERROR_TOP, PIXEL_FILL(0));\n    FillWindowPixelBuffer(WIN_LINK_ERROR_BOTTOM, PIXEL_FILL(0));\n    AddTextPrinterParameterized3(WIN_LINK_ERROR_TOP, FONT_SHORT_COPY_1, 2, 6, sTextColors, 0, gText_CommErrorEllipsis);\n    AddTextPrinterParameterized3(WIN_LINK_ERROR_BOTTOM, FONT_SHORT_COPY_1, 2, 1, sTextColors, 0, gText_MoveCloserToLinkPartner);\n    PutWindowTilemap(WIN_LINK_ERROR_TOP);\n    PutWindowTilemap(WIN_LINK_ERROR_BOTTOM);\n    CopyWindowToVram(WIN_LINK_ERROR_TOP, COPYWIN_NONE);  \n    CopyWindowToVram(WIN_LINK_ERROR_BOTTOM, COPYWIN_FULL);",
  },
  "GetBlockReceivedStatus": {
    returnType: "u8",
    params: "void",
    callsTo: ["Rfu_GetBlockReceivedStatus"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType == TRUE)\n        return Rfu_GetBlockReceivedStatus();\n\n    return (gBlockReceivedStatus[3] << 3) | (gBlockReceivedStatus[2] << 2) | (gBlockReceivedStatus[1] << 1) | (gBlockReceivedStatus[0] << 0);",
  },
  "GetLinkPlayerCount": {
    returnType: "u8",
    params: "void",
    callsTo: ["EXTRACT_PLAYER_COUNT","Rfu_GetLinkPlayerCount"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType)\n        return Rfu_GetLinkPlayerCount();\n\n    return EXTRACT_PLAYER_COUNT(gLinkStatus);",
  },
  "GetLinkPlayerCountAsBitFlags": {
    returnType: "u8",
    params: "void",
    callsTo: ["GetLinkPlayerCount"],
    lineCount: 6,
    bodyC: "int i;\n    u8 flags;\n\n    flags = 0;\n    for (i = 0; i < GetLinkPlayerCount(); i++)\n        flags |= (1 << i);\n\n    return flags;",
  },
  "GetLinkPlayerCount_2": {
    returnType: "u8",
    params: "void",
    callsTo: ["EXTRACT_PLAYER_COUNT"],
    lineCount: 1,
    bodyC: "return EXTRACT_PLAYER_COUNT(gLinkStatus);",
  },
  "GetLinkPlayerDataExchangeStatusTimed": {
    returnType: "u8",
    params: "int minPlayers, int maxPlayers",
    callsTo: ["CloseLink","GetGameProgressForLinkTrade","GetLinkPlayerCount","GetLinkPlayerCount_2","GetMultiplayerId"],
    lineCount: 69,
    bodyC: "int i;\n    int count;\n    u32 index;\n    u8 numPlayers;\n    u32 linkType1;\n    u32 linkType2;\n\n    count = 0;\n    if (gReceivedRemoteLinkPlayers == TRUE)\n    {\n        numPlayers = GetLinkPlayerCount_2();\n        if (minPlayers > numPlayers || numPlayers > maxPlayers)\n        {\n            sPlayerDataExchangeStatus = EXCHANGE_WRONG_NUM_PLAYERS;\n            return sPlayerDataExchangeStatus;\n        }\n        else\n        {\n            if (GetLinkPlayerCount() == 0)\n            {\n                gLinkErrorOccurred = TRUE;\n                CloseLink();\n            }\n            for (i = 0, index = 0; i < GetLinkPlayerCount(); index++, i++)\n            {\n                if (gLinkPlayers[index].linkType == gLinkPlayers[0].linkType)\n                {\n                    count++;\n                }\n            }\n            if (count == GetLinkPlayerCount())\n            {\n                if (gLinkPlayers[0].linkType == LINKTYPE_TRADE_SETUP)\n                {\n                    switch (GetGameProgressForLinkTrade())\n                    {\n                    case TRADE_PLAYER_NOT_READY:\n                        sPlayerDataExchangeStatus = EXCHANGE_PLAYER_NOT_READY;\n                        break;\n                    case TRADE_PARTNER_NOT_READY:\n                        sPlayerDataExchangeStatus = EXCHANGE_PARTNER_NOT_READY;\n                        break;\n                    case TRADE_BOTH_PLAYERS_READY:\n                        sPlayerDataExchangeStatus = EXCHANGE_COMPLETE;\n                        break;\n                    }\n                }\n                else\n                {\n                    sPlayerDataExchangeStatus = EXCHANGE_COMPLETE;\n                }\n            }\n            else\n            {\n                sPlayerDataExchangeStatus = EXCHANGE_DIFF_SELECTIONS;\n                linkType1 = gLinkPlayers[GetMultiplayerId()].linkType;\n                linkType2 = gLinkPlayers[GetMultiplayerId() ^ 1].linkType;\n                if ((linkType1 == LINKTYPE_BATTLE_TOWER_50 && linkType2 == LINKTYPE_BATTLE_TOWER_OPEN)\n                 || (linkType1 == LINKTYPE_BATTLE_TOWER_OPEN && linkType2 == LINKTYPE_BATTLE_TOWER_50))\n                {\n                     \n                     \n                    gSpecialVar_0x8005 = 3;\n                }\n            }\n        }\n    }\n    else if (++sTimeOutCounter > 600)\n    {\n        sPlayerDataExchangeStatus = EXCHANGE_TIMED_OUT;\n    }\n    return sPlayerDataExchangeStatus;",
  },
  "GetLinkPlayerTrainerId": {
    returnType: "u32",
    params: "u8 who",
    lineCount: 1,
    bodyC: "return gLinkPlayers[who].trainerId;",
  },
  "GetLinkRecvQueueLength": {
    returnType: "u32",
    params: "void",
    callsTo: ["GetRfuRecvQueueLength"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType != 0)\n        return GetRfuRecvQueueLength();\n\n    return gLink.recvQueue.count;",
  },
  "GetMultiplayerId": {
    returnType: "u8",
    params: "void",
    callsTo: ["Rfu_GetMultiplayerId"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType == TRUE)\n        return Rfu_GetMultiplayerId();\n\n    return SIO_MULTI_CNT->id;",
  },
  "GetSavedLinkPlayerCountAsBitFlags": {
    returnType: "u8",
    params: "void",
    lineCount: 6,
    bodyC: "int i;\n    u8 flags;\n\n    flags = 0;\n    for (i = 0; i < gSavedLinkPlayerCount; i++)\n        flags |= (1 << i);\n\n    return flags;",
  },
  "GetSavedPlayerCount": {
    returnType: "u8",
    params: "void",
    lineCount: 1,
    bodyC: "return gSavedLinkPlayerCount;",
  },
  "GetSioMultiSI": {
    returnType: "bool8",
    params: "void",
    lineCount: 1,
    bodyC: "return (REG_SIOCNT & SIO_MULTI_SI) != 0;",
  },
  "GetWirelessCommType": {
    returnType: "u8",
    params: "void",
    lineCount: 1,
    bodyC: "return gWirelessCommType;",
  },
  "HandleLinkConnection": {
    returnType: "bool8",
    params: "void",
    callsTo: ["IsRfuRecvQueueEmpty","IsSendingKeysOverCable","LinkMain1","LinkMain2","RfuMain1","RfuMain2"],
    lineCount: 19,
    bodyC: "bool32 main1Failed, main2Failed;\n\n    if (gWirelessCommType == 0)\n    {\n        gLinkStatus = LinkMain1(&gShouldAdvanceLinkState, gSendCmd, gRecvCmds);\n        LinkMain2(&gMain.heldKeys);\n        if ((gLinkStatus & LINK_STAT_RECEIVED_NOTHING) && IsSendingKeysOverCable() == TRUE)\n            return TRUE;\n    }\n    else\n    {\n        main1Failed = RfuMain1();  \n        main2Failed = RfuMain2();\n        if (IsSendingKeysOverCable() == TRUE)\n        {\n             \n             \n            if (main1Failed == TRUE || IsRfuRecvQueueEmpty() || main2Failed)\n                return TRUE;\n        }\n    }\n    return FALSE;",
  },
  "HandleReceiveRemoteLinkPlayer": {
    returnType: "static void",
    params: "u8 who",
    callsTo: ["GetLinkPlayerCount_2"],
    lineCount: 12,
    bodyC: "int i;\n    int count;\n\n    count = 0;\n    gRemoteLinkPlayersNotReceived[who] = FALSE;\n    for (i = 0; i < GetLinkPlayerCount_2(); i++)\n    {\n        count += gRemoteLinkPlayersNotReceived[i];\n    }\n    if (count == 0 && gReceivedRemoteLinkPlayers == 0)\n    {\n        gReceivedRemoteLinkPlayers = 1;\n    }",
  },
  "HasLinkErrorOccurred": {
    returnType: "bool8",
    params: "void",
    lineCount: 1,
    bodyC: "return gLinkErrorOccurred;",
  },
  "InitBlockSend": {
    returnType: "static bool32",
    params: "const void *src, size_t size",
    callsTo: ["BuildSendCmd","GetMultiplayerId","memcpy"],
    lineCount: 22,
    bodyC: "if (sBlockSend.active)\n    {\n        return FALSE;\n    }\n    sBlockSend.multiplayerId = GetMultiplayerId();\n    sBlockSend.active = TRUE;\n    sBlockSend.size = size;\n    sBlockSend.pos = 0;\n    if (size > BLOCK_BUFFER_SIZE)\n    {\n        sBlockSend.src = src;\n    }\n    else\n    {\n        if (src != gBlockSendBuffer)\n            memcpy(gBlockSendBuffer, src, size);\n\n        sBlockSend.src = gBlockSendBuffer;\n    }\n    BuildSendCmd(LINKCMD_INIT_BLOCK);\n    gLinkCallback = LinkCB_BlockSendBegin;\n    sBlockSendDelayCounter = 0;\n    return TRUE;",
  },
  "InitLink": {
    returnType: "static void",
    params: "void",
    callsTo: ["EnableSerial"],
    lineCount: 5,
    bodyC: "int i;\n\n    for (i = 0; i < CMD_LENGTH; i++)\n        gSendCmd[i] = LINKCMD_NONE;\n\n    sLinkOpen = TRUE;\n    EnableSerial();",
  },
  "InitLinkTestBG": {
    returnType: "static void",
    params: "u8 paletteNum, u8 bgNum, u8 screenBaseBlock, u8 charBaseBlock, u16 baseChar",
    callsTo: ["BGCNT_CHARBASE","BGCNT_PRIORITY","BGCNT_SCREENBASE","BG_CHAR_ADDR","BG_PLTT_ID","DmaCopy16","LoadPalette","SetGpuReg"],
    lineCount: 19,
    bodyC: "LoadPalette(sLinkTestDigitsPal, BG_PLTT_ID(paletteNum), PLTT_SIZE_4BPP);\n    DmaCopy16(3, sLinkTestDigitsGfx, (u16 *)BG_CHAR_ADDR(charBaseBlock) + (16 * baseChar), sizeof sLinkTestDigitsGfx);\n    gLinkTestBGInfo.screenBaseBlock = screenBaseBlock;\n    gLinkTestBGInfo.paletteNum = paletteNum;\n    gLinkTestBGInfo.baseChar = baseChar;\n    switch (bgNum)\n    {\n        case 1:\n            SetGpuReg(REG_OFFSET_BG1CNT, BGCNT_SCREENBASE(screenBaseBlock) | BGCNT_PRIORITY(1) | BGCNT_CHARBASE(charBaseBlock));\n            break;\n        case 2:\n            SetGpuReg(REG_OFFSET_BG2CNT, BGCNT_SCREENBASE(screenBaseBlock) | BGCNT_PRIORITY(1) | BGCNT_CHARBASE(charBaseBlock));\n            break;\n        case 3:\n            SetGpuReg(REG_OFFSET_BG3CNT, BGCNT_SCREENBASE(screenBaseBlock) | BGCNT_PRIORITY(1) | BGCNT_CHARBASE(charBaseBlock));\n            break;\n    }\n    SetGpuReg(REG_OFFSET_BG0HOFS + bgNum * 4, 0);\n    SetGpuReg(REG_OFFSET_BG0VOFS + bgNum * 4, 0);",
  },
  "InitLocalLinkPlayer": {
    returnType: "static void",
    params: "void",
    callsTo: ["FlagGet","IsNationalPokedexEnabled","StringCopy"],
    lineCount: 12,
    bodyC: "gLocalLinkPlayer.trainerId = gSaveBlock2Ptr->playerTrainerId[0] | (gSaveBlock2Ptr->playerTrainerId[1] << 8) | (gSaveBlock2Ptr->playerTrainerId[2] << 16) | (gSaveBlock2Ptr->playerTrainerId[3] << 24);\n    StringCopy(gLocalLinkPlayer.name, gSaveBlock2Ptr->playerName);\n    gLocalLinkPlayer.gender = gSaveBlock2Ptr->playerGender;\n    gLocalLinkPlayer.linkType = gLinkType;\n    gLocalLinkPlayer.language = gGameLanguage;\n    gLocalLinkPlayer.version = gGameVersion + 0x4000;\n    gLocalLinkPlayer.lp_field_2 = 0x8000;\n    gLocalLinkPlayer.progressFlags = IsNationalPokedexEnabled();\n    if (FlagGet(FLAG_IS_CHAMPION))\n    {\n        gLocalLinkPlayer.progressFlags |= 0x10;\n    }",
  },
  "InitTimer": {
    returnType: "static void",
    params: "void",
    callsTo: ["EnableInterrupts"],
    lineCount: 6,
    bodyC: "if (gLink.isMaster)\n    {\n        REG_TM3CNT_L = -197;\n        REG_TM3CNT_H = TIMER_64CLK | TIMER_INTR_ENABLE;\n        EnableInterrupts(INTR_FLAG_TIMER3);\n    }",
  },
  "IsLinkConnectionEstablished": {
    returnType: "bool8",
    params: "void",
    callsTo: ["EXTRACT_CONN_ESTABLISHED"],
    lineCount: 1,
    bodyC: "return EXTRACT_CONN_ESTABLISHED(gLinkStatus);",
  },
  "IsLinkMaster": {
    returnType: "bool8",
    params: "void",
    callsTo: ["EXTRACT_MASTER","Rfu_IsMaster"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType)\n        return Rfu_IsMaster();\n\n    return EXTRACT_MASTER(gLinkStatus);",
  },
  "IsLinkPlayerDataExchangeComplete": {
    returnType: "bool8",
    params: "void",
    callsTo: ["GetLinkPlayerCount"],
    lineCount: 20,
    bodyC: "u8 i;\n    u8 count;\n    bool8 retval;\n\n    count = 0;\n    for (i = 0; i < GetLinkPlayerCount(); i++)\n    {\n        if (gLinkPlayers[i].linkType == gLinkPlayers[0].linkType)\n            count++;\n    }\n    if (count == GetLinkPlayerCount())\n    {\n        retval = TRUE;\n        sPlayerDataExchangeStatus = EXCHANGE_COMPLETE;\n    }\n    else\n    {\n        retval = FALSE;\n        sPlayerDataExchangeStatus = EXCHANGE_DIFF_SELECTIONS;\n    }\n    return retval;",
  },
  "IsLinkRecvQueueAtOverworldMax": {
    returnType: "bool32",
    params: "void",
    callsTo: ["GetLinkRecvQueueLength"],
    lineCount: 3,
    bodyC: "if (GetLinkRecvQueueLength() >= OVERWORLD_RECV_QUEUE_MAX)\n        return TRUE;\n\n    return FALSE;",
  },
  "IsLinkTaskFinished": {
    returnType: "bool8",
    params: "void",
    callsTo: ["IsLinkRfuTaskFinished"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType == TRUE)\n        return IsLinkRfuTaskFinished();\n\n    return gLinkCallback == NULL;",
  },
  "IsSendingKeysToLink": {
    returnType: "bool32",
    params: "void",
    callsTo: ["IsSendingKeysToRfu"],
    lineCount: 5,
    bodyC: "if (gWirelessCommType)\n        return IsSendingKeysToRfu();\n\n    if (gLinkCallback == LinkCB_SendHeldKeys)\n        return TRUE;\n\n    return FALSE;",
  },
  "IsSioMultiMaster": {
    returnType: "static bool8",
    params: "void",
    lineCount: 1,
    bodyC: "return (REG_SIOCNT & SIO_MULTI_SD) && (REG_SIOCNT & SIO_MULTI_SI) == 0;",
  },
  "IsWirelessAdapterConnected": {
    returnType: "bool8",
    params: "void",
    callsTo: ["CloseLink","InitRFUAPI","RestoreSerialTimer3IntrHandlers","SetWirelessCommType0_Internal","SetWirelessCommType1","rfu_LMAN_REQBN_softReset_and_checkID","rfu_REQ_stopMode","rfu_waitREQComplete"],
    lineCount: 12,
    bodyC: "SetWirelessCommType1();\n    InitRFUAPI();\n    if (rfu_LMAN_REQBN_softReset_and_checkID() == RFU_ID)\n    {\n        rfu_REQ_stopMode();\n        rfu_waitREQComplete();\n        return TRUE;\n    }\n    SetWirelessCommType0_Internal();\n    CloseLink();\n    RestoreSerialTimer3IntrHandlers();\n    return FALSE;",
  },
  "LinkCB_BerryBlenderSendHeldKeys": {
    returnType: "static void",
    params: "void",
    callsTo: ["BuildSendCmd","GetMultiplayerId"],
    lineCount: 3,
    bodyC: "GetMultiplayerId();\n    BuildSendCmd(LINKCMD_BLENDER_SEND_KEYS);\n    gBerryBlenderKeySendAttempts++;",
  },
  "LinkCB_BlockSend": {
    returnType: "static void",
    params: "void",
    lineCount: 14,
    bodyC: "int i;\n    const u8 *src;\n\n    src = sBlockSend.src;\n    gSendCmd[0] = LINKCMD_CONT_BLOCK;\n    for (i = 0; i < CMD_LENGTH - 1; i++)\n    {\n        gSendCmd[i + 1] = (src[sBlockSend.pos + i * 2 + 1] << 8) | src[sBlockSend.pos + i * 2];\n    }\n    sBlockSend.pos += 14;\n    if (sBlockSend.size <= sBlockSend.pos)\n    {\n        sBlockSend.active = FALSE;\n        gLinkCallback = LinkCB_BlockSendEnd;\n    }",
  },
  "LinkCB_BlockSendBegin": {
    returnType: "static void",
    params: "void",
    lineCount: 2,
    bodyC: "if (++sBlockSendDelayCounter > 2)\n        gLinkCallback = LinkCB_BlockSend;",
  },
  "LinkCB_BlockSendEnd": {
    returnType: "static void",
    params: "void",
    lineCount: 1,
    bodyC: "gLinkCallback = NULL;",
  },
  "LinkCB_ReadyCloseLink": {
    returnType: "static void",
    params: "void",
    callsTo: ["BuildSendCmd"],
    lineCount: 5,
    bodyC: "if (gLastRecvQueueCount == 0)\n    {\n        BuildSendCmd(LINKCMD_READY_CLOSE_LINK);\n        gLinkCallback = LinkCB_WaitCloseLink;\n    }",
  },
  "LinkCB_ReadyCloseLinkWithJP": {
    returnType: "static void",
    params: "void",
    callsTo: ["BuildSendCmd"],
    lineCount: 5,
    bodyC: "if (gLastRecvQueueCount == 0)\n    {\n        BuildSendCmd(LINKCMD_READY_CLOSE_LINK);\n        gLinkCallback = LinkCB_WaitCloseLinkWithJP;\n    }",
  },
  "LinkCB_RequestPlayerDataExchange": {
    returnType: "static void",
    params: "void",
    callsTo: ["BuildSendCmd"],
    lineCount: 5,
    bodyC: "if (gLinkStatus & LINK_STAT_MASTER)\n    {\n        BuildSendCmd(LINKCMD_SEND_LINK_TYPE);\n    }\n    gLinkCallback = NULL;",
  },
  "LinkCB_SendHeldKeys": {
    returnType: "static void",
    params: "void",
    callsTo: ["BuildSendCmd"],
    lineCount: 2,
    bodyC: "if (gReceivedRemoteLinkPlayers == TRUE)\n        BuildSendCmd(LINKCMD_SEND_HELD_KEYS);",
  },
  "LinkCB_Standby": {
    returnType: "static void",
    params: "void",
    callsTo: ["BuildSendCmd"],
    lineCount: 5,
    bodyC: "if (gLastRecvQueueCount == 0)\n    {\n        BuildSendCmd(LINKCMD_READY_EXIT_STANDBY);\n        gLinkCallback = LinkCB_StandbyForAll;\n    }",
  },
  "LinkCB_StandbyForAll": {
    returnType: "static void",
    params: "void",
    callsTo: ["GetLinkPlayerCount"],
    lineCount: 13,
    bodyC: "u8 i;\n    u8 linkPlayerCount = GetLinkPlayerCount();\n    for (i = 0; i < linkPlayerCount; i++)\n    {\n        if (!gReadyToExitStandby[i])\n            break;\n    }\n\n     \n    if (i == linkPlayerCount)\n    {\n        for (i = 0; i < MAX_LINK_PLAYERS; i++)\n            gReadyToExitStandby[i] = FALSE;\n\n        gLinkCallback = NULL;\n    }",
  },
  "LinkCB_WaitCloseLink": {
    returnType: "static void",
    params: "void",
    callsTo: ["CloseLink","GetLinkPlayerCount"],
    lineCount: 17,
    bodyC: "int i;\n    unsigned count;\n\n     \n    u8 linkPlayerCount = GetLinkPlayerCount();\n    count = 0;\n    for (i = 0; i < linkPlayerCount; i++)\n    {\n        if (gReadyToCloseLink[i])\n            count++;\n    }\n\n    if (count == linkPlayerCount)\n    {\n         \n        gBattleTypeFlags &= ~BATTLE_TYPE_LINK_IN_BATTLE;\n        gLinkVSyncDisabled = TRUE;\n        CloseLink();\n        gLinkCallback = NULL;\n        gLinkDummy1 = TRUE;\n    }",
  },
  "LinkCB_WaitCloseLinkWithJP": {
    returnType: "static void",
    params: "void",
    callsTo: ["CloseLink","GetLinkPlayerCount"],
    lineCount: 20,
    bodyC: "int i;\n    unsigned count;\n    u8 linkPlayerCount;\n\n    linkPlayerCount = GetLinkPlayerCount();\n    count = 0;\n\n     \n    for (i = 0; i < linkPlayerCount; i++)\n    {\n         \n         \n        if (gLinkPlayers[i].language == LANGUAGE_JAPANESE)\n            count++;\n        else if (gReadyToCloseLink[i])\n            count++;\n    }\n\n    if (count == linkPlayerCount)\n    {\n         \n        gBattleTypeFlags &= ~BATTLE_TYPE_LINK_IN_BATTLE;\n        gLinkVSyncDisabled = TRUE;\n        CloseLink();\n        gLinkCallback = NULL;\n        gLinkDummy1 = TRUE;\n    }",
  },
  "LinkDummy_Return2": {
    returnType: "u32",
    params: "void",
    lineCount: 1,
    bodyC: "return 2;",
  },
  "LinkMain1": {
    returnType: "u32",
    params: "u8 *shouldAdvanceLinkState, u16 *sendCmd, u16 (*recvCmds)[CMD_LENGTH]",
    callsTo: ["CheckMasterOrSlave","DequeueRecvCmds","DisableSerial","EnableSerial","EnqueueSendCmd","InitTimer"],
    lineCount: 82,
    bodyC: "u32 retVal;\n    u32 retVal2;\n\n    switch (gLink.state)\n    {\n        case LINK_STATE_START0:\n            DisableSerial();\n            gLink.state = 1;\n            break;\n        case LINK_STATE_START1:\n            if (*shouldAdvanceLinkState == 1)\n            {\n                EnableSerial();\n                gLink.state = 2;\n            }\n            break;\n        case LINK_STATE_HANDSHAKE:\n            switch (*shouldAdvanceLinkState)\n            {\n                default:\n                    CheckMasterOrSlave();\n                    break;\n                case 1:\n                    if (gLink.isMaster == LINK_MASTER && gLink.playerCount > 1)\n                        gLink.handshakeAsMaster = TRUE;\n                    break;\n                case 2:\n                    gLink.state = LINK_STATE_START0;\n                    REG_SIOMLT_SEND = 0;\n                    break;\n            }\n            break;\n        case LINK_STATE_INIT_TIMER:\n            InitTimer();\n            gLink.state = LINK_STATE_CONN_ESTABLISHED;\n             \n        case LINK_STATE_CONN_ESTABLISHED:\n            EnqueueSendCmd(sendCmd);\n            DequeueRecvCmds(recvCmds);\n            break;\n    }\n    *shouldAdvanceLinkState = 0;\n    retVal = gLink.localId;\n    retVal |= (gLink.playerCount << LINK_STAT_PLAYER_COUNT_SHIFT);\n    if (gLink.isMaster == LINK_MASTER)\n    {\n        retVal |= LINK_STAT_MASTER;\n    }\n    {\n        u32 receivedNothing = gLink.receivedNothing << LINK_STAT_RECEIVED_NOTHING_SHIFT;\n        u32 link_field_F = gLink.link_field_F << LINK_STAT_UNK_FLAG_9_SHIFT;\n        u32 hardwareError = gLink.hardwareError << LINK_STAT_ERROR_HARDWARE_SHIFT;\n        u32 badChecksum = gLink.badChecksum << LINK_STAT_ERROR_CHECKSUM_SHIFT;\n        u32 queueFull = gLink.queueFull << LINK_STAT_ERROR_QUEUE_FULL_SHIFT;\n        u32 val;\n\n        if (gLink.state == LINK_STATE_CONN_ESTABLISHED)\n        {\n            val = LINK_STAT_CONN_ESTABLISHED;\n            val |= receivedNothing;\n            val |= retVal;\n            val |= link_field_F;\n            val |= hardwareError;\n            val |= badChecksum;\n            val |= queueFull;\n        }\n        else\n        {\n            val = retVal;\n            val |= receivedNothing;\n            val |= link_field_F;\n            val |= hardwareError;\n            val |= badChecksum;\n            val |= queueFull;\n        }\n\n        retVal = val;\n    }\n\n    if (gLink.lag == LAG_MASTER)\n        retVal |= LINK_STAT_ERROR_LAG_MASTER;\n\n    if (gLink.localId >= MAX_LINK_PLAYERS)\n        retVal |= LINK_STAT_ERROR_INVALID_ID;\n\n    retVal2 = retVal;\n    if (gLink.lag == LAG_SLAVE)\n        retVal2 |= LINK_STAT_ERROR_LAG_SLAVE;\n\n    return retVal2;",
  },
  "LinkMain2": {
    returnType: "u16",
    params: "const u16 *heldKeys",
    callsTo: ["ProcessRecvCmds","TrySetLinkErrorBuffer","gLinkCallback"],
    lineCount: 14,
    bodyC: "u8 i;\n\n    if (!sLinkOpen)\n        return 0;\n\n    for (i = 0; i < CMD_LENGTH; i++)\n        gSendCmd[i] = 0;\n\n    gLinkHeldKeys = *heldKeys;\n    if (gLinkStatus & LINK_STAT_CONN_ESTABLISHED)\n    {\n        ProcessRecvCmds(SIO_MULTI_CNT->id);\n        if (gLinkCallback != NULL)\n            gLinkCallback();\n        TrySetLinkErrorBuffer();\n    }\n    return gLinkStatus;",
  },
  "LinkPlayerFromBlock": {
    returnType: "void",
    params: "u32 who",
    callsTo: ["ConvertLinkPlayerName","SetMainCallback2","strcmp"],
    lineCount: 10,
    bodyC: "u8 who_ = who;\n    struct LinkPlayerBlock *block;\n    struct LinkPlayer *player;\n\n    block = (struct LinkPlayerBlock *)gBlockRecvBuffer[who_];\n    player = &gLinkPlayers[who_];\n    *player = block->linkPlayer;\n    ConvertLinkPlayerName(player);\n\n    if (strcmp(block->magic1, sASCIIGameFreakInc) != 0\n     || strcmp(block->magic2, sASCIIGameFreakInc) != 0)\n        SetMainCallback2(CB2_LinkError);",
  },
  "LinkTestCalcBlockChecksum": {
    returnType: "static u16",
    params: "const u16 *src, u16 size",
    lineCount: 6,
    bodyC: "u16 chksum;\n    u16 i;\n\n    chksum = 0;\n    for (i = 0; i < size / 2; i++)\n        chksum += src[i];\n\n    return chksum;",
  },
  "LinkTestProcessKeyInput": {
    returnType: "static void",
    params: "void",
    callsTo: ["BeginNormalPaletteFade","InitBlockSend","JOY_HELD","JOY_NEW","RGB","SetCloseLinkCallback","SetLinkDebugValues","SetSuppressLinkErrorMessage","TrySavingData"],
    lineCount: 28,
    bodyC: "if (JOY_NEW(A_BUTTON))\n    {\n        gShouldAdvanceLinkState = 1;\n    }\n    if (JOY_HELD(B_BUTTON))\n    {\n        InitBlockSend(gHeap + 0x4000, 0x00002004);\n    }\n    if (JOY_NEW(L_BUTTON))\n    {\n        BeginNormalPaletteFade(PALETTES_ALL, 0, 16, 0, RGB(2, 0, 0));\n    }\n    if (JOY_NEW(START_BUTTON))\n    {\n        SetSuppressLinkErrorMessage(TRUE);\n    }\n    if (JOY_NEW(R_BUTTON))\n    {\n        TrySavingData(SAVE_LINK);\n    }\n    if (JOY_NEW(SELECT_BUTTON))\n    {\n        SetCloseLinkCallback();\n    }\n    if (sLinkTestDebugValuesEnabled)\n    {\n        SetLinkDebugValues(gMain.vblankCounter2, gLinkCallback ? gLinkVSyncDisabled : gLinkVSyncDisabled | 0x10);\n    }",
  },
  "LinkTest_PrintChar": {
    returnType: "static void",
    params: "char val, u8 x, u8 y",
    callsTo: ["BG_SCREEN_ADDR"],
    lineCount: 3,
    bodyC: "u16 *vAddr;\n\n    vAddr = (u16 *)BG_SCREEN_ADDR(gLinkTestBGInfo.screenBaseBlock);\n    vAddr[y * 32 + x] = (gLinkTestBGInfo.paletteNum << 12) | (val + gLinkTestBGInfo.baseChar);",
  },
  "LinkTest_PrintHex": {
    returnType: "static void",
    params: "u32 num, u8 x, u8 y, u8 length",
    callsTo: ["LinkTest_PrintNumChar"],
    lineCount: 12,
    bodyC: "char buff[16];\n    int i;\n\n    for (i = 0; i < length; i++)\n    {\n        buff[i] = num & 0xF;\n        num >>= 4;\n    }\n    for (i = length - 1; i >= 0; i--)\n    {\n        LinkTest_PrintNumChar(buff[i], x, y);\n        x++;\n    }",
  },
  "LinkTest_PrintNumChar": {
    returnType: "static void",
    params: "char val, u8 x, u8 y",
    callsTo: ["BG_SCREEN_ADDR"],
    lineCount: 3,
    bodyC: "u16 *vAddr;\n\n    vAddr = (u16 *)BG_SCREEN_ADDR(gLinkTestBGInfo.screenBaseBlock);\n    vAddr[y * 32 + x] = (gLinkTestBGInfo.paletteNum << 12) | (val + 1 + gLinkTestBGInfo.baseChar);",
  },
  "LinkTest_PrintString": {
    returnType: "static void",
    params: "const char *str, u8 x, u8 y",
    callsTo: ["LinkTest_PrintChar"],
    lineCount: 18,
    bodyC: "int xOffset;\n    int i;\n    int yOffset;\n\n    yOffset = 0;\n    xOffset = 0;\n    for (i = 0; str[i] != 0; str++)\n    {\n        if (str[i] == *\"\\n\")\n        {\n            yOffset++;\n            xOffset = 0;\n        }\n        else\n        {\n            LinkTest_PrintChar(str[i], x + xOffset, y + yOffset);\n            xOffset++;\n        }\n    }",
  },
  "LinkVSync": {
    returnType: "void",
    params: "void",
    callsTo: ["StartTransfer"],
    lineCount: 42,
    bodyC: "if (gLink.isMaster)\n    {\n        switch (gLink.state)\n        {\n            case LINK_STATE_CONN_ESTABLISHED:\n                if (gLink.serialIntrCounter < 9)\n                {\n                    if (gLink.hardwareError != TRUE)\n                    {\n                        gLink.lag = LAG_MASTER;\n                    }\n                    else\n                    {\n                        StartTransfer();\n                    }\n                }\n                else if (gLink.lag != LAG_MASTER)\n                {\n                    gLink.serialIntrCounter = 0;\n                    StartTransfer();\n                }\n                break;\n            case LINK_STATE_HANDSHAKE:\n                StartTransfer();\n                break;\n        }\n    }\n    else if (gLink.state == LINK_STATE_CONN_ESTABLISHED || gLink.state == LINK_STATE_HANDSHAKE)\n    {\n        if (++sNumVBlanksWithoutSerialIntr > 10)\n        {\n            if (gLink.state == LINK_STATE_CONN_ESTABLISHED)\n            {\n                gLink.lag = LAG_SLAVE;\n            }\n            if (gLink.state == LINK_STATE_HANDSHAKE)\n            {\n                gLink.playerCount = 0;\n                gLink.link_field_F = FALSE;\n            }\n        }\n    }",
  },
  "Link_AnyPartnersPlayingFRLG_JP": {
    returnType: "bool32",
    params: "void",
    callsTo: ["AreAnyLinkPlayersUsingVersions"],
    lineCount: 7,
    bodyC: "int i;\n\n    i = AreAnyLinkPlayersUsingVersions(VERSION_FIRE_RED, VERSION_LEAF_GREEN);\n    if (i >= 0 && gLinkPlayers[i].language == LANGUAGE_JAPANESE)\n    {\n        return TRUE;\n    }\n    return FALSE;",
  },
  "Link_AnyPartnersPlayingRubyOrSapphire": {
    returnType: "bool32",
    params: "void",
    callsTo: ["AreAnyLinkPlayersUsingVersions"],
    lineCount: 5,
    bodyC: "if (AreAnyLinkPlayersUsingVersions(VERSION_RUBY, VERSION_SAPPHIRE) >= 0)\n    {\n        return TRUE;\n    }\n    return FALSE;",
  },
  "LocalLinkPlayerToBlock": {
    returnType: "void",
    params: "void",
    callsTo: ["InitLocalLinkPlayer","memcpy"],
    lineCount: 7,
    bodyC: "struct LinkPlayerBlock *block;\n\n    InitLocalLinkPlayer();\n    block = &gLocalLinkPlayerBlock;\n    block->linkPlayer = gLocalLinkPlayer;\n    memcpy(block->magic1, sASCIIGameFreakInc, sizeof(block->magic1) - 1);\n    memcpy(block->magic2, sASCIIGameFreakInc, sizeof(block->magic2) - 1);\n    memcpy(gBlockSendBuffer, block, sizeof(*block));",
  },
  "OpenLink": {
    returnType: "void",
    params: "void",
    callsTo: ["CreateTask","InitLink","InitRFUAPI","ResetBlockReceivedFlags","ResetBlockSend","ResetSerial"],
    lineCount: 28,
    bodyC: "int i;\n\n    if (!gWirelessCommType)\n    {\n        ResetSerial();\n        InitLink();\n        gLinkCallback = LinkCB_RequestPlayerDataExchange;\n        gLinkVSyncDisabled = FALSE;\n        gLinkErrorOccurred = FALSE;\n        gSuppressLinkErrorMessage = FALSE;\n        ResetBlockReceivedFlags();\n        ResetBlockSend();\n        sDummy1 = FALSE;\n        gLinkDummy2 = FALSE;\n        gLinkDummy1 = FALSE;\n        gReadyCloseLinkType = 0;\n        CreateTask(Task_TriggerHandshake, 2);\n    }\n    else\n    {\n        InitRFUAPI();\n    }\n    gReceivedRemoteLinkPlayers = 0;\n    for (i = 0; i < MAX_LINK_PLAYERS; i++)\n    {\n        gRemoteLinkPlayersNotReceived[i] = TRUE;\n        gReadyToCloseLink[i] = FALSE;\n        gReadyToExitStandby[i] = FALSE;\n    }",
  },
  "OpenLinkTimed": {
    returnType: "void",
    params: "void",
    callsTo: ["OpenLink"],
    lineCount: 3,
    bodyC: "sPlayerDataExchangeStatus = EXCHANGE_NOT_STARTED;\n    sTimeOutCounter = 0;\n    OpenLink();",
  },
  "ProcessRecvCmds": {
    returnType: "static void",
    params: "u8 unused",
    callsTo: ["ConvertLinkPlayerName","HandleReceiveRemoteLinkPlayer","InitBlockSend","InitLocalLinkPlayer","SendBlock","SetBerryBlenderLinkCallback","SetBlockReceivedFlag","SetMainCallback2","memcpy","strcmp"],
    lineCount: 110,
    bodyC: "u16 i;\n\n    for (i = 0; i < MAX_LINK_PLAYERS; i++)\n    {\n        gLinkPartnersHeldKeys[i] = 0;\n        if (gRecvCmds[i][0] == 0)\n        {\n            continue;\n        }\n        switch (gRecvCmds[i][0])\n        {\n            case LINKCMD_SEND_LINK_TYPE:\n            {\n                struct LinkPlayerBlock *block;\n\n                InitLocalLinkPlayer();\n                block = &gLocalLinkPlayerBlock;\n                block->linkPlayer = gLocalLinkPlayer;\n                memcpy(block->magic1, sASCIIGameFreakInc, sizeof(block->magic1) - 1);\n                memcpy(block->magic2, sASCIIGameFreakInc, sizeof(block->magic2) - 1);\n                InitBlockSend(block, sizeof(*block));\n                break;\n            }\n            case LINKCMD_BLENDER_SEND_KEYS:\n                gLinkPartnersHeldKeys[i] = gRecvCmds[i][1];\n                break;\n            case LINKCMD_DUMMY_1:\n                gLinkDummy2 = TRUE;\n                break;\n            case LINKCMD_DUMMY_2:\n                gLinkDummy2 = TRUE;\n                break;\n            case LINKCMD_INIT_BLOCK:\n            {\n                struct BlockTransfer *blockRecv;\n\n                blockRecv = &sBlockRecv[i];\n                blockRecv->pos = 0;\n                blockRecv->size = gRecvCmds[i][1];\n                blockRecv->multiplayerId = gRecvCmds[i][2];\n                break;\n            }\n            case LINKCMD_CONT_BLOCK:\n            {\n                if (sBlockRecv[i].size > BLOCK_BUFFER_SIZE)\n                {\n                    u16 *buffer;\n                    u16 j;\n\n                    buffer = (u16 *)gDecompressionBuffer;\n                    for (j = 0; j < CMD_LENGTH - 1; j++)\n                    {\n                        buffer[(sBlockRecv[i].pos / 2) + j] = gRecvCmds[i][j + 1];\n                    }\n                }\n                else\n                {\n                    u16 j;\n\n                    for (j = 0; j < CMD_LENGTH - 1; j++)\n                    {\n                        gBlockRecvBuffer[i][(sBlockRecv[i].pos / 2) + j] = gRecvCmds[i][j + 1];\n                    }\n                }\n\n                sBlockRecv[i].pos += (CMD_LENGTH - 1) * 2;\n\n                if (sBlockRecv[i].pos >= sBlockRecv[i].size)\n                {\n                    if (gRemoteLinkPlayersNotReceived[i] == TRUE)\n                    {\n                        struct LinkPlayerBlock *block;\n                        struct LinkPlayer *linkPlayer;\n\n                        block = (struct LinkPlayerBlock *)&gBlockRecvBuffer[i];\n                        linkPlayer = &gLinkPlayers[i];\n                        *linkPlayer = block->linkPlayer;\n                        if ((linkPlayer->version & 0xFF) == VERSION_RUBY || (linkPlayer->version & 0xFF) == VERSION_SAPPHIRE)\n                        {\n                            linkPlayer->progressFlagsCopy = 0;\n                            linkPlayer->neverRead = 0;\n                            linkPlayer->progressFlags = 0;\n                        }\n                        ConvertLinkPlayerName(linkPlayer);\n                        if (strcmp(block->magic1, sASCIIGameFreakInc) != 0\n                            || strcmp(block->magic2, sASCIIGameFreakInc) != 0)\n                        {\n                            SetMainCallback2(CB2_LinkError);\n                        }\n                        else\n                        {\n                            HandleReceiveRemoteLinkPlayer(i);\n                        }\n                    }\n                    else\n                    {\n                        SetBlockReceivedFlag(i);\n                    }\n                }\n            }\n                break;\n            case LINKCMD_READY_CLOSE_LINK:\n                gReadyToCloseLink[i] = TRUE;\n                break;\n            case LINKCMD_READY_EXIT_STANDBY:\n                gReadyToExitStandby[i] = TRUE;\n                break;\n            case LINKCMD_BLENDER_NO_PBLOCK_SPACE:\n                SetBerryBlenderLinkCallback();\n                break;\n            case LINKCMD_SEND_BLOCK_REQ:\n                SendBlock(0, sBlockRequests[gRecvCmds[i][1]].address, sBlockRequests[gRecvCmds[i][1]].size);\n                break;\n            case LINKCMD_SEND_HELD_KEYS:\n                gLinkPartnersHeldKeys[i] = gRecvCmds[i][1];\n                break;\n        }\n    }",
  },
  "ResetBlockReceivedFlag": {
    returnType: "void",
    params: "u8 who",
    callsTo: ["Rfu_ResetBlockReceivedFlag"],
    lineCount: 8,
    bodyC: "if (gWirelessCommType == TRUE)\n    {\n        Rfu_ResetBlockReceivedFlag(who);\n    }\n    else if (gBlockReceivedStatus[who])\n    {\n        gBlockReceivedStatus[who] = FALSE;\n    }",
  },
  "ResetBlockReceivedFlags": {
    returnType: "void",
    params: "void",
    callsTo: ["Rfu_ResetBlockReceivedFlag"],
    lineCount: 11,
    bodyC: "int i;\n\n    if (gWirelessCommType == TRUE)\n    {\n        for (i = 0; i < MAX_RFU_PLAYERS; i++)\n            Rfu_ResetBlockReceivedFlag(i);\n    }\n    else\n    {\n        for (i = 0; i < MAX_LINK_PLAYERS; i++)\n            gBlockReceivedStatus[i] = FALSE;\n    }",
  },
  "ResetBlockSend": {
    returnType: "static void",
    params: "void",
    lineCount: 4,
    bodyC: "sBlockSend.active = FALSE;\n    sBlockSend.pos = 0;\n    sBlockSend.size = 0;\n    sBlockSend.src = NULL;",
  },
  "ResetLinkPlayerCount": {
    returnType: "void",
    params: "void",
    lineCount: 2,
    bodyC: "gSavedLinkPlayerCount = 0;\n    gSavedMultiplayerId = 0;",
  },
  "ResetLinkPlayers": {
    returnType: "void",
    params: "void",
    lineCount: 3,
    bodyC: "int i;\n\n    for (i = 0; i <= MAX_LINK_PLAYERS; i++)\n        gLinkPlayers[i] = (struct LinkPlayer){};",
  },
  "ResetRecvBuffer": {
    returnType: "void",
    params: "void",
    lineCount: 13,
    bodyC: "u8 i;\n    u8 j;\n    u8 k;\n\n    gLink.recvQueue.count = 0;\n    gLink.recvQueue.pos = 0;\n    for (i = 0; i < MAX_LINK_PLAYERS; i++)\n    {\n        for (j = 0; j < CMD_LENGTH; j++)\n        {\n            for (k = 0; k < QUEUE_CAPACITY; k++)\n                gLink.recvQueue.data[i][j][k] = LINKCMD_NONE;\n        }\n    }",
  },
  "ResetSendBuffer": {
    returnType: "void",
    params: "void",
    lineCount: 9,
    bodyC: "u8 i;\n    u8 j;\n\n    gLink.sendQueue.count = 0;\n    gLink.sendQueue.pos = 0;\n    for (i = 0; i < CMD_LENGTH; i++)\n    {\n        for (j = 0; j < QUEUE_CAPACITY; j++)\n            gLink.sendQueue.data[i][j] = LINKCMD_NONE;\n    }",
  },
  "ResetSerial": {
    returnType: "void",
    params: "void",
    callsTo: ["DisableSerial","EnableSerial"],
    lineCount: 2,
    bodyC: "EnableSerial();\n    DisableSerial();",
  },
  "SaveLinkPlayers": {
    returnType: "void",
    params: "u8 playerCount",
    callsTo: ["GetMultiplayerId"],
    lineCount: 5,
    bodyC: "int i;\n\n    gSavedLinkPlayerCount = playerCount;\n    gSavedMultiplayerId = GetMultiplayerId();\n    for (i = 0; i < MAX_RFU_PLAYERS; i++)\n        sSavedLinkPlayers[i] = gLinkPlayers[i];",
  },
  "SendBlock": {
    returnType: "bool8",
    params: "u8 unused, const void *src, u16 size",
    callsTo: ["InitBlockSend","Rfu_InitBlockSend"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType == TRUE)\n        return Rfu_InitBlockSend(src, size);\n\n    return InitBlockSend(src, size);",
  },
  "SendBlockRequest": {
    returnType: "bool8",
    params: "u8 blockReqType",
    callsTo: ["BuildSendCmd","Rfu_SendBlockRequest"],
    lineCount: 9,
    bodyC: "if (gWirelessCommType == TRUE)\n        return Rfu_SendBlockRequest(blockReqType);\n\n    if (gLinkCallback == NULL)\n    {\n        gBlockRequestType = blockReqType;\n        BuildSendCmd(LINKCMD_SEND_BLOCK_REQ);\n        return TRUE;\n    }\n    return FALSE;",
  },
  "SendRecvDone": {
    returnType: "static void",
    params: "void",
    lineCount: 9,
    bodyC: "if (gLink.recvCmdIndex == CMD_LENGTH)\n    {\n        gLink.sendCmdIndex = 0;\n        gLink.recvCmdIndex = 0;\n    }\n    else if (gLink.isMaster)\n    {\n        REG_TM3CNT_H |= TIMER_ENABLE;\n    }",
  },
  "SerialCB": {
    returnType: "void",
    params: "void",
    callsTo: ["DoHandshake","DoRecv","DoSend","SendRecvDone"],
    lineCount: 30,
    bodyC: "gLink.localId = SIO_MULTI_CNT->id;\n    switch (gLink.state)\n    {\n        case LINK_STATE_CONN_ESTABLISHED:\n            gLink.hardwareError = SIO_MULTI_CNT->error;\n            DoRecv();\n            DoSend();\n            SendRecvDone();\n            break;\n        case LINK_STATE_HANDSHAKE:\n            if (DoHandshake())\n            {\n                if (gLink.isMaster)\n                {\n                    gLink.state = LINK_STATE_INIT_TIMER;\n                    gLink.serialIntrCounter = 8;\n                }\n                else\n                {\n                    gLink.state = LINK_STATE_CONN_ESTABLISHED;\n                }\n            }\n            break;\n    }\n    gLink.serialIntrCounter++;\n    sNumVBlanksWithoutSerialIntr = 0;\n    if (gLink.serialIntrCounter == 8)\n    {\n        gLastRecvQueueCount = gLink.recvQueue.count;\n    }",
  },
  "SetBerryBlenderLinkCallback": {
    returnType: "void",
    params: "void",
    callsTo: ["Rfu_SetBerryBlenderLinkCallback"],
    lineCount: 5,
    bodyC: "gBerryBlenderKeySendAttempts = 0;\n    if (gWirelessCommType)\n        Rfu_SetBerryBlenderLinkCallback();\n    else\n        gLinkCallback = LinkCB_BerryBlenderSendHeldKeys;",
  },
  "SetBlockReceivedFlag": {
    returnType: "static void",
    params: "u8 who",
    callsTo: ["Rfu_SetBlockReceivedFlag"],
    lineCount: 4,
    bodyC: "if (gWirelessCommType == TRUE)\n        Rfu_SetBlockReceivedFlag(who);\n    else\n        gBlockReceivedStatus[who] = TRUE;",
  },
  "SetCloseLinkCallback": {
    returnType: "void",
    params: "void",
    callsTo: ["Rfu_SetCloseLinkCallback"],
    lineCount: 17,
    bodyC: "if (gWirelessCommType == TRUE)\n    {\n        Rfu_SetCloseLinkCallback();\n    }\n    else\n    {\n        if (gLinkCallback != NULL)\n        {\n            sReadyCloseLinkAttempts++;\n        }\n        else\n        {\n            gLinkCallback = LinkCB_ReadyCloseLink;\n            gLinkDummy1 = FALSE;\n            gReadyCloseLinkType = 0;\n        }\n    }",
  },
  "SetCloseLinkCallbackAndType": {
    returnType: "void",
    params: "u16 type",
    callsTo: ["Rfu_SetCloseLinkCallback"],
    lineCount: 13,
    bodyC: "if (gWirelessCommType == TRUE)\n    {\n        Rfu_SetCloseLinkCallback();\n    }\n    else\n    {\n        if (gLinkCallback == NULL)\n        {\n            gLinkCallback = LinkCB_ReadyCloseLink;\n            gLinkDummy1 = FALSE;\n            gReadyCloseLinkType = type;\n        }\n    }",
  },
  "SetCloseLinkCallbackHandleJP": {
    returnType: "void",
    params: "void",
    callsTo: ["Rfu_SetCloseLinkCallback"],
    lineCount: 17,
    bodyC: "if (gWirelessCommType == TRUE)\n    {\n        Rfu_SetCloseLinkCallback();\n    }\n    else\n    {\n        if (gLinkCallback != NULL)\n        {\n            sReadyCloseLinkAttempts++;\n        }\n        else\n        {\n            gLinkCallback = LinkCB_ReadyCloseLinkWithJP;\n            gLinkDummy1 = FALSE;\n            gReadyCloseLinkType = 0;\n        }\n    }",
  },
  "SetLinkDebugValues": {
    returnType: "void",
    params: "u32 seed, u32 flags",
    lineCount: 2,
    bodyC: "gLinkDebugSeed = seed;\n    gLinkDebugFlags = flags;",
  },
  "SetLinkErrorBuffer": {
    returnType: "void",
    params: "u32 status, u8 lastSendQueueCount, u8 lastRecvQueueCount, bool8 disconnected",
    lineCount: 4,
    bodyC: "sLinkErrorBuffer.status = status;\n    sLinkErrorBuffer.lastSendQueueCount = lastSendQueueCount;\n    sLinkErrorBuffer.lastRecvQueueCount = lastRecvQueueCount;\n    sLinkErrorBuffer.disconnected = disconnected;",
  },
  "SetLinkStandbyCallback": {
    returnType: "void",
    params: "void",
    callsTo: ["Rfu_SetLinkStandbyCallback"],
    lineCount: 10,
    bodyC: "if (gWirelessCommType == TRUE)\n    {\n        Rfu_SetLinkStandbyCallback();\n    }\n    else\n    {\n        if (gLinkCallback == NULL)\n            gLinkCallback = LinkCB_Standby;\n\n        gLinkDummy1 = FALSE;\n    }",
  },
  "SetLocalLinkPlayerId": {
    returnType: "void",
    params: "u8 playerId",
    lineCount: 1,
    bodyC: "gLocalLinkPlayer.id = playerId;",
  },
  "SetSuppressLinkErrorMessage": {
    returnType: "void",
    params: "bool8 flag",
    lineCount: 1,
    bodyC: "gSuppressLinkErrorMessage = flag;",
  },
  "SetWirelessCommType0": {
    returnType: "void",
    params: "void",
    lineCount: 2,
    bodyC: "if (gReceivedRemoteLinkPlayers == 0)\n        gWirelessCommType = 0;",
  },
  "SetWirelessCommType0_Internal": {
    returnType: "static void",
    params: "void",
    lineCount: 2,
    bodyC: "if (gReceivedRemoteLinkPlayers == 0)\n        gWirelessCommType = 0;",
  },
  "SetWirelessCommType1": {
    returnType: "void",
    params: "void",
    lineCount: 2,
    bodyC: "if (gReceivedRemoteLinkPlayers == 0)\n        gWirelessCommType = 1;",
  },
  "StartSendingKeysToLink": {
    returnType: "void",
    params: "void",
    callsTo: ["StartSendingKeysToRfu"],
    lineCount: 3,
    bodyC: "if (gWirelessCommType)\n        StartSendingKeysToRfu();\n\n    gLinkCallback = LinkCB_SendHeldKeys;",
  },
  "StartTransfer": {
    returnType: "static void",
    params: "void",
    lineCount: 1,
    bodyC: "REG_SIOCNT |= SIO_START;",
  },
  "StopTimer": {
    returnType: "static void",
    params: "void",
    lineCount: 5,
    bodyC: "if (gLink.isMaster)\n    {\n        REG_TM3CNT_H &= ~TIMER_ENABLE;\n        REG_TM3CNT_L = -197;\n    }",
  },
  "Task_DestroySelf": {
    returnType: "void",
    params: "u8 taskId",
    callsTo: ["DestroyTask"],
    lineCount: 1,
    bodyC: "DestroyTask(taskId);",
  },
  "Task_PrintTestData": {
    returnType: "static void",
    params: "u8 taskId",
    callsTo: ["EXTRACT_PLAYER_COUNT","GetBlockReceivedStatus","GetMultiplayerId","GetSioMultiSI","HasLinkErrorOccurred","IsLinkConnectionEstablished","IsSioMultiMaster","LinkTest_PrintHex","LinkTest_PrintString","strcpy"],
    lineCount: 20,
    bodyC: "char testTitle[32];\n    int i;\n\n    strcpy(testTitle, sASCIITestPrint);\n    LinkTest_PrintString(testTitle, 5, 2);\n    LinkTest_PrintHex(gShouldAdvanceLinkState, 2, 1, 2);\n    LinkTest_PrintHex(gLinkStatus, 15, 1, 8);\n    LinkTest_PrintHex(gLink.state, 2, 10, 2);\n    LinkTest_PrintHex(EXTRACT_PLAYER_COUNT(gLinkStatus), 15, 10, 2);\n    LinkTest_PrintHex(GetMultiplayerId(), 15, 12, 2);\n    LinkTest_PrintHex(gLastSendQueueCount, 25, 1, 2);\n    LinkTest_PrintHex(gLastRecvQueueCount, 25, 2, 2);\n    LinkTest_PrintHex(GetBlockReceivedStatus(), 15, 5, 2);\n    LinkTest_PrintHex(gLinkDebugSeed, 2, 12, 8);\n    LinkTest_PrintHex(gLinkDebugFlags, 2, 13, 8);\n    LinkTest_PrintHex(GetSioMultiSI(), 25, 5, 1);\n    LinkTest_PrintHex(IsSioMultiMaster(), 25, 6, 1);\n    LinkTest_PrintHex(IsLinkConnectionEstablished(), 25, 7, 1);\n    LinkTest_PrintHex(HasLinkErrorOccurred(), 25, 8, 1);\n\n    for (i = 0; i < MAX_LINK_PLAYERS; i++)\n        LinkTest_PrintHex(gLinkTestBlockChecksums[i], 10, 4 + i, 4);",
  },
  "Task_TriggerHandshake": {
    returnType: "static void",
    params: "u8 taskId",
    callsTo: ["DestroyTask"],
    lineCount: 5,
    bodyC: "if (++gTasks[taskId].data[0] == 5)\n    {\n        gShouldAdvanceLinkState = 1;\n        DestroyTask(taskId);\n    }",
  },
  "TestBlockTransfer": {
    returnType: "static void",
    params: "u8 nothing, u8 is, u8 used",
    callsTo: ["GetBlockReceivedStatus","LinkTestCalcBlockChecksum","LinkTest_PrintHex","ResetBlockReceivedFlag"],
    lineCount: 32,
    bodyC: "u8 i;\n    u8 status;\n\n    if (sLinkTestLastBlockSendPos != sBlockSend.pos)\n    {\n        LinkTest_PrintHex(sBlockSend.pos, 2, 3, 2);\n        sLinkTestLastBlockSendPos = sBlockSend.pos;\n    }\n    for (i = 0; i < MAX_LINK_PLAYERS; i++)\n    {\n        if (sLinkTestLastBlockRecvPos[i] != sBlockRecv[i].pos)\n        {\n            LinkTest_PrintHex(sBlockRecv[i].pos, 2, i + 4, 2);\n            sLinkTestLastBlockRecvPos[i] = sBlockRecv[i].pos;\n        }\n    }\n    status = GetBlockReceivedStatus();\n    if (status == 0xF)  \n    {\n        for (i = 0; i < MAX_LINK_PLAYERS; i++)\n        {\n            if ((status >> i) & 1)\n            {\n                gLinkTestBlockChecksums[i] = LinkTestCalcBlockChecksum(gBlockRecvBuffer[i], sBlockRecv[i].size);\n                ResetBlockReceivedFlag(i);\n                if (gLinkTestBlockChecksums[i] != 0x0342)\n                {\n                    sLinkTestDebugValuesEnabled = FALSE;\n                    sDummyFlag = FALSE;\n                }\n            }\n        }\n    }",
  },
  "Timer3Intr": {
    returnType: "void",
    params: "void",
    callsTo: ["StartTransfer","StopTimer"],
    lineCount: 2,
    bodyC: "StopTimer();\n    StartTransfer();",
  },
  "TrySetLinkErrorBuffer": {
    returnType: "static void",
    params: "void",
    callsTo: ["CloseLink","EXTRACT_LINK_ERRORS","SetMainCallback2"],
    lineCount: 12,
    bodyC: "if (sLinkOpen && EXTRACT_LINK_ERRORS(gLinkStatus))\n    {\n         \n         \n        if (!gSuppressLinkErrorMessage)\n        {\n            sLinkErrorBuffer.status = gLinkStatus;\n            sLinkErrorBuffer.lastRecvQueueCount = gLastRecvQueueCount;\n            sLinkErrorBuffer.lastSendQueueCount = gLastSendQueueCount;\n            SetMainCallback2(CB2_LinkError);\n        }\n        gLinkErrorOccurred = TRUE;\n        CloseLink();\n    }",
  },
  "VBlankCB_LinkError": {
    returnType: "static void",
    params: "void",
    callsTo: ["LoadOam","ProcessSpriteCopyRequests","TransferPlttBuffer"],
    lineCount: 3,
    bodyC: "LoadOam();\n    ProcessSpriteCopyRequests();\n    TransferPlttBuffer();",
  },
} as const;
