// AUTO-GENERATED from src/menu_helpers.c by extract-engine-helpers.mjs
// Do not edit — re-run `node scripts/extract-engine-helpers.mjs` to refresh.
//
// Generated: 2026-04-27
// Functions: 25

export const ENGINE_FUNCTIONS = {
  "AdjustQuantityAccordingToDPadInput": {
    returnType: "bool8",
    params: "s16 *quantity, u16 max",
    callsTo: ["JOY_REPEAT","PlaySE"],
    lineCount: 62,
    bodyC: "s16 valBefore = *quantity;\n\n    if (JOY_REPEAT(DPAD_ANY) == DPAD_UP)\n    {\n        (*quantity)++;\n        if (*quantity > max)\n            *quantity = 1;\n\n        if (*quantity == valBefore)\n        {\n            return FALSE;\n        }\n        else\n        {\n            PlaySE(SE_SELECT);\n            return TRUE;\n        }\n    }\n    else if (JOY_REPEAT(DPAD_ANY) == DPAD_DOWN)\n    {\n        (*quantity)--;\n        if (*quantity <= 0)\n            *quantity = max;\n\n        if (*quantity == valBefore)\n        {\n            return FALSE;\n        }\n        else\n        {\n            PlaySE(SE_SELECT);\n            return TRUE;\n        }\n    }\n    else if (JOY_REPEAT(DPAD_ANY) == DPAD_RIGHT)\n    {\n        *quantity += 10;\n        if (*quantity > max)\n            *quantity = max;\n\n        if (*quantity == valBefore)\n        {\n            return FALSE;\n        }\n        else\n        {\n            PlaySE(SE_SELECT);\n            return TRUE;\n        }\n    }\n    else if (JOY_REPEAT(DPAD_ANY) == DPAD_LEFT)\n    {\n        *quantity -= 10;\n        if (*quantity <= 0)\n            *quantity = 1;\n\n        if (*quantity == valBefore)\n        {\n            return FALSE;\n        }\n        else\n        {\n            PlaySE(SE_SELECT);\n            return TRUE;\n        }\n    }\n\n    return FALSE;",
  },
  "CreateSwapLineSprites": {
    returnType: "void",
    params: "u8 *spriteIds, u8 count",
    callsTo: ["CreateSprite","StartSpriteAnim"],
    lineCount: 8,
    bodyC: "u8 i;\n\n    for (i = 0; i < count; i++)\n    {\n        spriteIds[i] = CreateSprite(&sSpriteTemplate_SwapLine, i * 16, 0, 0);\n        if (i != 0)\n            StartSpriteAnim(&gSprites[spriteIds[i]], 1);\n\n        gSprites[spriteIds[i]].invisible = TRUE;\n    }",
  },
  "CreateYesNoMenuWithCallbacks": {
    returnType: "void",
    params: "u8 taskId, const struct WindowTemplate *template, u8 unused1, u8 unused2, u8 unused3, u16 tileStart, u8 palette, const struct YesNoFuncTable *yesNo",
    callsTo: ["CreateYesNoMenu"],
    lineCount: 3,
    bodyC: "CreateYesNoMenu(template, tileStart, palette, 0);\n    sYesNo = *yesNo;\n    gTasks[taskId].func = Task_CallYesOrNoCallback;",
  },
  "DestroySwapLineSprites": {
    returnType: "void",
    params: "u8 *spriteIds, u8 count",
    callsTo: ["DestroySprite","DestroySpriteAndFreeResources"],
    lineCount: 8,
    bodyC: "u8 i;\n\n    for (i = 0; i < count; i++)\n    {\n        if (i == count - 1)\n            DestroySpriteAndFreeResources(&gSprites[spriteIds[i]]);\n        else\n            DestroySprite(&gSprites[spriteIds[i]]);\n    }",
  },
  "DisplayMessageAndContinueTask": {
    returnType: "void",
    params: "u8 taskId, u8 windowId, u16 tileNum, u8 paletteNum, u8 fontId, u8 textSpeed, const u8 *string, void *taskFunc",
    callsTo: ["AddTextPrinterParameterized2","DrawDialogFrameWithCustomTileAndPalette","StringExpandPlaceholders"],
    lineCount: 8,
    bodyC: "sMessageWindowId = windowId;\n    DrawDialogFrameWithCustomTileAndPalette(windowId, TRUE, tileNum, paletteNum);\n\n    if (string != gStringVar4)\n        StringExpandPlaceholders(gStringVar4, string);\n\n    gTextFlags.canABSpeedUpPrint = 1;\n    AddTextPrinterParameterized2(windowId, fontId, gStringVar4, textSpeed, NULL, TEXT_COLOR_DARK_GRAY, TEXT_COLOR_WHITE, TEXT_COLOR_LIGHT_GRAY);\n    sMessageNextTask = taskFunc;\n    gTasks[taskId].func = Task_ContinueTaskAfterMessagePrints;",
  },
  "DoYesNoFuncWithChoice": {
    returnType: "void",
    params: "u8 taskId, const struct YesNoFuncTable *data",
    lineCount: 2,
    bodyC: "sYesNo = *data;\n    gTasks[taskId].func = Task_CallYesOrNoCallback;",
  },
  "GetLRKeysPressed": {
    returnType: "u8",
    params: "void",
    callsTo: ["JOY_NEW"],
    lineCount: 8,
    bodyC: "if (gSaveBlock2Ptr->optionsButtonMode == OPTIONS_BUTTON_MODE_LR)\n    {\n        if (JOY_NEW(L_BUTTON))\n            return MENU_L_PRESSED;\n        if (JOY_NEW(R_BUTTON))\n            return MENU_R_PRESSED;\n    }\n\n    return 0;",
  },
  "GetLRKeysPressedAndHeld": {
    returnType: "u8",
    params: "void",
    callsTo: ["JOY_REPEAT"],
    lineCount: 8,
    bodyC: "if (gSaveBlock2Ptr->optionsButtonMode == OPTIONS_BUTTON_MODE_LR)\n    {\n        if (JOY_REPEAT(L_BUTTON))\n            return MENU_L_PRESSED;\n        if (JOY_REPEAT(R_BUTTON))\n            return MENU_R_PRESSED;\n    }\n\n    return 0;",
  },
  "IsActiveOverworldLinkBusy": {
    returnType: "static bool8",
    params: "void",
    callsTo: ["MenuHelpers_IsLinkActive","Overworld_IsRecvQueueAtMax"],
    lineCount: 4,
    bodyC: "if (!MenuHelpers_IsLinkActive())\n        return FALSE;\n    else\n        return Overworld_IsRecvQueueAtMax();",
  },
  "IsHoldingItemAllowed": {
    returnType: "bool8",
    params: "u16 itemId",
    callsTo: ["InUnionRoom","MAP_GROUP","MAP_NUM"],
    lineCount: 7,
    bodyC: "if (itemId == ITEM_ENIGMA_BERRY\n     && ((gSaveBlock1Ptr->location.mapGroup == MAP_GROUP(MAP_TRADE_CENTER)\n       && gSaveBlock1Ptr->location.mapNum == MAP_NUM(MAP_TRADE_CENTER))\n       || InUnionRoom() == TRUE))\n        return FALSE;\n    else\n        return TRUE;",
  },
  "IsWritingMailAllowed": {
    returnType: "bool8",
    params: "u16 itemId",
    callsTo: ["InUnionRoom","IsOverworldLinkActive","ItemIsMail"],
    lineCount: 4,
    bodyC: "if ((IsOverworldLinkActive() == TRUE || InUnionRoom() == TRUE) && ItemIsMail(itemId) == TRUE)\n        return FALSE;\n    else\n        return TRUE;",
  },
  "LoadListMenuSwapLineGfx": {
    returnType: "void",
    params: "void",
    callsTo: ["LoadCompressedSpritePalette","LoadCompressedSpriteSheet"],
    lineCount: 2,
    bodyC: "LoadCompressedSpriteSheet(&sSpriteSheet_SwapLine);\n    LoadCompressedSpritePalette(&sSpritePalette_SwapLine);",
  },
  "MenuHelpers_IsLinkActive": {
    returnType: "bool8",
    params: "void",
    callsTo: ["IsOverworldLinkActive"],
    lineCount: 4,
    bodyC: "if (IsOverworldLinkActive() == TRUE || gReceivedRemoteLinkPlayers == 1)\n        return TRUE;\n    else\n        return FALSE;",
  },
  "MenuHelpers_ShouldWaitForLinkRecv": {
    returnType: "bool8",
    params: "void",
    callsTo: ["IsActiveOverworldLinkBusy","IsLinkRecvQueueAtOverworldMax"],
    lineCount: 4,
    bodyC: "if (IsActiveOverworldLinkBusy() == TRUE || IsLinkRecvQueueAtOverworldMax() == TRUE )\n        return TRUE;\n    else\n        return FALSE;",
  },
  "ResetAllBgsCoordinates": {
    returnType: "void",
    params: "void",
    callsTo: ["ChangeBgX","ChangeBgY"],
    lineCount: 8,
    bodyC: "ChangeBgX(0, 0, BG_COORD_SET);\n    ChangeBgY(0, 0, BG_COORD_SET);\n    ChangeBgX(1, 0, BG_COORD_SET);\n    ChangeBgY(1, 0, BG_COORD_SET);\n    ChangeBgX(2, 0, BG_COORD_SET);\n    ChangeBgY(2, 0, BG_COORD_SET);\n    ChangeBgX(3, 0, BG_COORD_SET);\n    ChangeBgY(3, 0, BG_COORD_SET);",
  },
  "ResetVramOamAndBgCntRegs": {
    returnType: "void",
    params: "void",
    callsTo: ["CpuFill16","CpuFill32","SetGpuReg"],
    lineCount: 8,
    bodyC: "SetGpuReg(REG_OFFSET_DISPCNT, 0);\n    SetGpuReg(REG_OFFSET_BG3CNT, 0);\n    SetGpuReg(REG_OFFSET_BG2CNT, 0);\n    SetGpuReg(REG_OFFSET_BG1CNT, 0);\n    SetGpuReg(REG_OFFSET_BG0CNT, 0);\n    CpuFill16(0, (void *) VRAM, VRAM_SIZE);\n    CpuFill32(0, (void *) OAM, OAM_SIZE);\n    CpuFill16(0, (void *) PLTT, PLTT_SIZE);",
  },
  "RunTextPrintersRetIsActive": {
    returnType: "bool16",
    params: "u8 textPrinterId",
    callsTo: ["IsTextPrinterActive","RunTextPrinters"],
    lineCount: 2,
    bodyC: "RunTextPrinters();\n    return IsTextPrinterActive(textPrinterId);",
  },
  "SetCursorScrollWithinListBounds": {
    returnType: "void",
    params: "u16 *scrollOffset, u16 *cursorPos, u8 shownItems, u8 totalItems, u8 maxShownItems",
    lineCount: 27,
    bodyC: "u8 i;\n\n    if (maxShownItems % 2 != 0)\n    {\n         \n        if (*cursorPos >= maxShownItems / 2)\n        {\n            for (i = 0; i < *cursorPos - (maxShownItems / 2); i++)\n            {\n                 \n                if (*scrollOffset + shownItems == totalItems)\n                    break;\n                (*cursorPos)--;\n                (*scrollOffset)++;\n            }\n        }\n    }\n    else\n    {\n         \n        if (*cursorPos >= (maxShownItems / 2) + 1)\n        {\n            for (i = 0; i <= *cursorPos - (maxShownItems / 2); i++)\n            {\n                 \n                if (*scrollOffset + shownItems == totalItems)\n                    break;\n                (*cursorPos)--;\n                (*scrollOffset)++;\n            }\n        }\n    }",
  },
  "SetCursorWithinListBounds": {
    returnType: "void",
    params: "u16 *scrollOffset, u16 *cursorPos, u8 maxShownItems, u8 totalItems",
    lineCount: 9,
    bodyC: "if (*scrollOffset != 0 && *scrollOffset + maxShownItems > totalItems)\n        *scrollOffset = totalItems - maxShownItems;\n\n    if (*scrollOffset + *cursorPos >= totalItems)\n    {\n        if (totalItems == 0)\n            *cursorPos = 0;\n        else\n            *cursorPos = totalItems - 1;\n    }",
  },
  "SetItemListPerPageCount": {
    returnType: "void",
    params: "struct ItemSlot *slots, u8 slotsCount, u8 *pageItems, u8 *totalItems, u8 maxPerPage",
    lineCount: 13,
    bodyC: "u16 i;\n    struct ItemSlot *slots_ = slots;\n\n     \n    *totalItems = 0;\n    for (i = 0; i < slotsCount; i++)\n    {\n        if (slots_[i].itemId != ITEM_NONE)\n            (*totalItems)++;\n    }\n    (*totalItems)++;  \n\n     \n    if (*totalItems > maxPerPage)\n        *pageItems = maxPerPage;\n    else\n        *pageItems = *totalItems;",
  },
  "SetSwapLineSpritesInvisibility": {
    returnType: "void",
    params: "u8 *spriteIds, u8 count, bool8 invisible",
    lineCount: 3,
    bodyC: "u8 i;\n\n    for (i = 0; i < count; i++)\n        gSprites[spriteIds[i]].invisible = invisible;",
  },
  "SetVBlankHBlankCallbacksToNull": {
    returnType: "void",
    params: "void",
    callsTo: ["SetHBlankCallback","SetVBlankCallback"],
    lineCount: 2,
    bodyC: "SetVBlankCallback(NULL);\n    SetHBlankCallback(NULL);",
  },
  "Task_CallYesOrNoCallback": {
    returnType: "static void",
    params: "u8 taskId",
    callsTo: ["Menu_ProcessInputNoWrapClearOnChoose","PlaySE","noFunc","yesFunc"],
    lineCount: 12,
    bodyC: "switch (Menu_ProcessInputNoWrapClearOnChoose())\n    {\n    case 0:\n        PlaySE(SE_SELECT);\n        sYesNo.yesFunc(taskId);\n        break;\n    case 1:\n    case MENU_B_PRESSED:\n        PlaySE(SE_SELECT);\n        sYesNo.noFunc(taskId);\n        break;\n    }",
  },
  "Task_ContinueTaskAfterMessagePrints": {
    returnType: "static void",
    params: "u8 taskId",
    callsTo: ["RunTextPrintersRetIsActive","sMessageNextTask"],
    lineCount: 2,
    bodyC: "if (!RunTextPrintersRetIsActive(sMessageWindowId))\n        sMessageNextTask(taskId);",
  },
  "UpdateSwapLineSpritesPos": {
    returnType: "void",
    params: "u8 *spriteIds, u8 count, s16 x, u16 y",
    lineCount: 11,
    bodyC: "u8 i;\n    bool8 hasMargin = count & SWAP_LINE_HAS_MARGIN;\n    count &= ~SWAP_LINE_HAS_MARGIN;\n\n    for (i = 0; i < count; i++)\n    {\n         \n         \n         \n         \n        if (i == count - 1 && hasMargin)\n            gSprites[spriteIds[i]].x2 = x - 8;\n        else\n            gSprites[spriteIds[i]].x2 = x;\n\n        gSprites[spriteIds[i]].y = 1 + y;\n    }",
  },
} as const;
