// AUTO-GENERATED from src/union_room_player_avatar.c by extract-engine-helpers.mjs
// Do not edit — re-run `node scripts/extract-engine-helpers.mjs` to refresh.
//
// Generated: 2026-04-27
// Functions: 38

export const ENGINE_FUNCTIONS = {
  "AnimateUnionRoomPlayer": {
    returnType: "static void",
    params: "u32 leaderId, struct UnionRoomObject *object",
    callsTo: ["AnimateUnionRoomPlayerDespawn","AnimateUnionRoomPlayerSpawn","HideUnionRoomPlayer","IsUnionRoomPlayerInvisible","RemoveUnionRoomPlayerObjectEvent"],
    lineCount: 41,
    bodyC: "switch (object->state)\n    {\n    case 0:\n        if (object->schedAnim == UNION_ROOM_SPAWN_IN)\n        {\n            object->state = 2;\n            object->animState = 0;\n        }\n        else\n        {\n            break;\n        }\n         \n    case 2:\n        if (!IsUnionRoomPlayerInvisible(leaderId, 0) && object->schedAnim == UNION_ROOM_SPAWN_OUT)\n        {\n            object->state = 0;\n            object->animState = 0;\n            RemoveUnionRoomPlayerObjectEvent(leaderId);\n            HideUnionRoomPlayer(leaderId);\n        }\n        else if (AnimateUnionRoomPlayerSpawn(&object->animState, leaderId, object) == TRUE)\n        {\n            object->state = 1;\n        }\n        break;\n    case 1:\n         \n        if (object->schedAnim == UNION_ROOM_SPAWN_OUT)\n        {\n            object->state = 3;\n            object->animState = 0;\n        }\n        else\n        {\n            break;\n        }\n         \n    case 3:\n        if (AnimateUnionRoomPlayerDespawn(&object->animState, leaderId, object) == 1)\n            object->state = 0;\n        break;\n    }\n    object->schedAnim = UNION_ROOM_SPAWN_NONE;",
  },
  "AnimateUnionRoomPlayerDespawn": {
    returnType: "static bool32",
    params: "s8 *state, u32 leaderId, struct UnionRoomObject *object",
    callsTo: ["HideUnionRoomPlayer","RemoveUnionRoomPlayerObjectEvent","SetUnionRoomPlayerEnterExitMovement","TryReleaseUnionRoomPlayerObjectEvent"],
    lineCount: 20,
    bodyC: "switch (*state)\n    {\n    case 0:\n        if (SetUnionRoomPlayerEnterExitMovement(leaderId, sMovement_UnionPlayerExit) == TRUE)\n        {\n            HideUnionRoomPlayer(leaderId);\n            (*state)++;\n        }\n        break;\n    case 1:\n        if (TryReleaseUnionRoomPlayerObjectEvent(leaderId))\n        {\n            RemoveUnionRoomPlayerObjectEvent(leaderId);\n            HideUnionRoomPlayer(leaderId);\n            *state = 0;\n            return TRUE;\n        }\n        break;\n    }\n    return FALSE;",
  },
  "AnimateUnionRoomPlayerSpawn": {
    returnType: "static bool32",
    params: "s8 *state, u32 leaderId, struct UnionRoomObject *object",
    callsTo: ["CreateUnionRoomPlayerObjectEvent","IsPlayerStandingStill","IsUnionRoomPlayerAt","PlayerGetDestCoords","SetUnionRoomPlayerEnterExitMovement","SetUnionRoomPlayerGfx","ShowUnionRoomPlayer","TryReleaseUnionRoomPlayerObjectEvent","player_get_pos_including_state_based_drift"],
    lineCount: 29,
    bodyC: "s16 x, y;\n\n    switch (*state)\n    {\n    case 0:\n        if (!IsPlayerStandingStill())\n            break;\n        PlayerGetDestCoords(&x, &y);\n        if (IsUnionRoomPlayerAt(leaderId, 0, x, y) == TRUE)\n            break;\n        player_get_pos_including_state_based_drift(&x, &y);\n        if (IsUnionRoomPlayerAt(leaderId, 0, x, y) == TRUE)\n            break;\n        SetUnionRoomPlayerGfx(leaderId, object->gfxId);\n        CreateUnionRoomPlayerObjectEvent(leaderId);\n        ShowUnionRoomPlayer(leaderId);\n        (*state)++;\n         \n    case 3:  \n        if (SetUnionRoomPlayerEnterExitMovement(leaderId, sMovement_UnionPlayerEnter) == TRUE)\n            (*state)++;\n        break;\n    case 2:\n        if (TryReleaseUnionRoomPlayerObjectEvent(leaderId))\n        {\n            *state = 0;\n            return TRUE;\n        }\n        break;\n    }\n    return FALSE;",
  },
  "AssembleGroup": {
    returnType: "static void",
    params: "u32 leaderId, struct RfuGameData *gameData",
    callsTo: ["DespawnGroupMember","GetUnionRoomPlayerGraphicsId","IsUnionRoomPlayerAt","IsVirtualObjectInvisible","PlayerGetDestCoords","SpawnGroupMember","UR_PLAYER_SPRITE_ID","player_get_pos_including_state_based_drift"],
    lineCount: 23,
    bodyC: "s16 x, y, x2, y2;\n    s32 i;\n\n    PlayerGetDestCoords(&x, &y);\n    player_get_pos_including_state_based_drift(&x2, &y2);\n    if (IsVirtualObjectInvisible(UR_PLAYER_SPRITE_ID(leaderId, 0) - UR_SPRITE_START_ID) == TRUE)\n    {\n        if (IsUnionRoomPlayerAt(leaderId, 0, x, y) == TRUE || IsUnionRoomPlayerAt(leaderId, 0, x2, y2) == TRUE)\n            return;\n        SpawnGroupMember(leaderId, 0, GetUnionRoomPlayerGraphicsId(gameData->playerGender, gameData->compatibility.playerTrainerId[0]), gameData);\n    }\n    for (i = 1; i < MAX_RFU_PLAYERS; i++)\n    {\n        if (gameData->partnerInfo[i - 1] == 0)\n        {\n            DespawnGroupMember(leaderId, i);\n        }\n        else if (IsUnionRoomPlayerAt(leaderId, i, x, y) == FALSE && IsUnionRoomPlayerAt(leaderId, i, x2, y2) == FALSE)\n        {\n            SpawnGroupMember(leaderId, i, GetUnionRoomPlayerGraphicsId((gameData->partnerInfo[i - 1] >> PINFO_GENDER_SHIFT) & 1,\n                                                                        gameData->partnerInfo[i - 1] & PINFO_TID_MASK),\n                                                                        gameData);\n        }\n    }",
  },
  "CreateTask_AnimateUnionRoomPlayers": {
    returnType: "static u8",
    params: "void",
    callsTo: ["CreateTask","FuncIsActiveTask"],
    lineCount: 4,
    bodyC: "if (FuncIsActiveTask(Task_AnimateUnionRoomPlayers) == TRUE)\n        return NUM_TASKS;\n    else\n        return CreateTask(Task_AnimateUnionRoomPlayers, 5);",
  },
  "CreateUnionRoomPlayerObjectEvent": {
    returnType: "static void",
    params: "u32 leaderId",
    callsTo: ["TrySpawnObjectEvent"],
    lineCount: 1,
    bodyC: "TrySpawnObjectEvent(sUnionRoomLocalIds[leaderId], gSaveBlock1Ptr->location.mapNum, gSaveBlock1Ptr->location.mapGroup);",
  },
  "CreateUnionRoomPlayerSprites": {
    returnType: "void",
    params: "u8 *spriteIds, s32 leaderId",
    callsTo: ["CreateVirtualObject","SetVirtualObjectInvisibility","UR_PLAYER_SPRITE_ID"],
    lineCount: 11,
    bodyC: "s32 memberId;\n    for (memberId = 0; memberId < MAX_RFU_PLAYERS; memberId++)\n    {\n        s32 id = UR_PLAYER_SPRITE_ID(leaderId, memberId);\n        spriteIds[id] = CreateVirtualObject(OBJ_EVENT_GFX_MAN_4,\n                                           id - UR_SPRITE_START_ID,\n                                           sUnionRoomPlayerCoords[leaderId][0] + sUnionRoomGroupOffsets[memberId][0],\n                                           sUnionRoomPlayerCoords[leaderId][1] + sUnionRoomGroupOffsets[memberId][1],\n                                           3, 1);\n        SetVirtualObjectInvisibility(id - UR_SPRITE_START_ID, TRUE);\n    }",
  },
  "DespawnGroupLeader": {
    returnType: "static bool32",
    params: "u32 leaderId",
    lineCount: 6,
    bodyC: "struct UnionRoomObject *object = &sUnionObjWork[leaderId];\n    object->schedAnim = UNION_ROOM_SPAWN_OUT;\n\n    if (object->state == 1)\n        return TRUE;\n    else\n        return FALSE;",
  },
  "DespawnGroupLeaderAndMembers": {
    returnType: "static void",
    params: "u32 leaderId, struct RfuGameData *gameData",
    callsTo: ["DespawnGroupLeader","DespawnGroupMember"],
    lineCount: 4,
    bodyC: "s32 i;\n    DespawnGroupLeader(leaderId);\n    for (i = 0; i < MAX_RFU_PLAYERS; i++)\n        DespawnGroupMember(leaderId, i);",
  },
  "DespawnGroupMember": {
    returnType: "static void",
    params: "u32 leaderId, u32 memberId",
    callsTo: ["GetUnionRoomPlayerCoords","MapGridSetMetatileImpassabilityAt","SetVirtualObjectSpriteAnim","UR_PLAYER_SPRITE_ID"],
    lineCount: 4,
    bodyC: "s32 x, y;\n    SetVirtualObjectSpriteAnim(UR_PLAYER_SPRITE_ID(leaderId, memberId) - UR_SPRITE_START_ID, UNION_ROOM_SPAWN_OUT);\n    GetUnionRoomPlayerCoords(leaderId, memberId, &x, &y);\n    MapGridSetMetatileImpassabilityAt(x, y, FALSE);",
  },
  "DestroyTask_AnimateUnionRoomPlayers": {
    returnType: "static void",
    params: "void",
    callsTo: ["DestroyTask","FindTaskIdByFunc"],
    lineCount: 3,
    bodyC: "u8 taskId = FindTaskIdByFunc(Task_AnimateUnionRoomPlayers);\n    if (taskId < NUM_TASKS)\n        DestroyTask(taskId);",
  },
  "DestroyUnionRoomPlayerObjects": {
    returnType: "void",
    params: "void",
    callsTo: ["DestroyTask_AnimateUnionRoomPlayers","HideUnionRoomPlayer","IsUnionRoomPlayerHidden","RemoveUnionRoomPlayerObjectEvent"],
    lineCount: 11,
    bodyC: "s32 i;\n    for (i = 0; i < MAX_UNION_ROOM_LEADERS; i++)\n    {\n        if (!IsUnionRoomPlayerHidden(i))\n        {\n            RemoveUnionRoomPlayerObjectEvent(i);\n            HideUnionRoomPlayer(i);\n        }\n    }\n    sUnionObjWork = NULL;\n    DestroyTask_AnimateUnionRoomPlayers();",
  },
  "DestroyUnionRoomPlayerSprites": {
    returnType: "void",
    params: "u8 *spriteIds",
    callsTo: ["DestroySprite"],
    lineCount: 3,
    bodyC: "s32 i;\n    for (i = 0; i < NUM_UNION_ROOM_SPRITES; i++)\n        DestroySprite(&gSprites[spriteIds[i]]);",
  },
  "GetNewFacingDirectionForUnionRoomPlayer": {
    returnType: "static u8",
    params: "u32 memberId, u32 leaderId, struct RfuGameData *gameData",
    lineCount: 6,
    bodyC: "if (memberId)  \n        return sMemberFacingDirections[memberId];\n    else if (gameData->activity == (ACTIVITY_CHAT | IN_UNION_ROOM))\n        return DIR_SOUTH;\n    else\n        return DIR_EAST;",
  },
  "GetUnionRoomPlayerCoords": {
    returnType: "static void",
    params: "u32 leaderId, u32 memberId, s32 *x, s32 *y",
    lineCount: 2,
    bodyC: "*x = sUnionRoomPlayerCoords[leaderId][0] + sUnionRoomGroupOffsets[memberId][0] + MAP_OFFSET;\n    *y = sUnionRoomPlayerCoords[leaderId][1] + sUnionRoomGroupOffsets[memberId][1] + MAP_OFFSET;",
  },
  "GetUnionRoomPlayerGraphicsId": {
    returnType: "static u8",
    params: "u32 gender, u32 id",
    lineCount: 1,
    bodyC: "return sUnionRoomObjGfxIds[gender][id % NUM_UNION_ROOM_CLASSES];",
  },
  "HandleUnionRoomPlayerRefresh": {
    returnType: "void",
    params: "struct WirelessLink_URoom *uroom",
    callsTo: ["UpdateUnionRoomPlayerSprites"],
    lineCount: 2,
    bodyC: "if (++sUnionObjRefreshTimer > 300)\n        UpdateUnionRoomPlayerSprites(uroom);",
  },
  "HideUnionRoomPlayer": {
    returnType: "static void",
    params: "u32 player_idx",
    callsTo: ["FlagSet"],
    lineCount: 1,
    bodyC: "FlagSet(FLAG_HIDE_UNION_ROOM_PLAYER_1 + player_idx);",
  },
  "InitUnionRoomPlayerObjects": {
    returnType: "u8",
    params: "struct UnionRoomObject *players",
    callsTo: ["CreateTask_AnimateUnionRoomPlayers"],
    lineCount: 11,
    bodyC: "s32 i;\n\n    sUnionObjRefreshTimer = 0;\n    sUnionObjWork = players;\n    for (i = 0; i < MAX_UNION_ROOM_LEADERS; i++)\n    {\n        players[i].state = 0;\n        players[i].gfxId = 0;\n        players[i].animState = 0;\n        players[i].schedAnim = UNION_ROOM_SPAWN_NONE;\n    }\n    return CreateTask_AnimateUnionRoomPlayers();",
  },
  "IsPlayerStandingStill": {
    returnType: "static bool32",
    params: "void",
    lineCount: 4,
    bodyC: "if (gPlayerAvatar.tileTransitionState == T_TILE_CENTER || gPlayerAvatar.tileTransitionState == T_NOT_MOVING)\n        return TRUE;\n    else\n        return FALSE;",
  },
  "IsUnionRoomPlayerAt": {
    returnType: "static bool32",
    params: "u32 leaderId, u32 memberId, s32 x, s32 y",
    lineCount: 5,
    bodyC: "if ((sUnionRoomPlayerCoords[leaderId][0] + sUnionRoomGroupOffsets[memberId][0] + MAP_OFFSET == x)\n    &&  (sUnionRoomPlayerCoords[leaderId][1] + sUnionRoomGroupOffsets[memberId][1] + MAP_OFFSET == y))\n        return TRUE;\n    else\n        return FALSE;",
  },
  "IsUnionRoomPlayerHidden": {
    returnType: "static bool32",
    params: "u32 player_idx",
    callsTo: ["FlagGet"],
    lineCount: 1,
    bodyC: "return FlagGet(FLAG_HIDE_UNION_ROOM_PLAYER_1 + player_idx);",
  },
  "IsUnionRoomPlayerInvisible": {
    returnType: "static bool32",
    params: "u32 leaderId, u32 memberId",
    callsTo: ["IsVirtualObjectInvisible","UR_PLAYER_SPRITE_ID"],
    lineCount: 1,
    bodyC: "return IsVirtualObjectInvisible(UR_PLAYER_SPRITE_ID(leaderId, memberId) - UR_SPRITE_START_ID);",
  },
  "RemoveUnionRoomPlayerObjectEvent": {
    returnType: "static void",
    params: "u32 leaderId",
    callsTo: ["RemoveObjectEventByLocalIdAndMap"],
    lineCount: 1,
    bodyC: "RemoveObjectEventByLocalIdAndMap(sUnionRoomLocalIds[leaderId], gSaveBlock1Ptr->location.mapNum, gSaveBlock1Ptr->location.mapGroup);",
  },
  "ScheduleUnionRoomPlayerRefresh": {
    returnType: "void",
    params: "struct WirelessLink_URoom *uroom",
    lineCount: 1,
    bodyC: "sUnionObjRefreshTimer = 300;",
  },
  "SetTilesAroundUnionRoomPlayersPassable": {
    returnType: "void",
    params: "void",
    callsTo: ["GetUnionRoomPlayerCoords","MapGridSetMetatileImpassabilityAt"],
    lineCount: 9,
    bodyC: "s32 i, memberId, x, y;\n    for (i = 0; i < MAX_UNION_ROOM_LEADERS; i++)\n    {\n        for (memberId = 0; memberId < MAX_RFU_PLAYERS; memberId++)\n        {\n            GetUnionRoomPlayerCoords(i, memberId, &x, &y);\n            MapGridSetMetatileImpassabilityAt(x, y, FALSE);\n        }\n    }",
  },
  "SetUnionRoomObjectFacingDirection": {
    returnType: "static void",
    params: "s32 memberId, s32 leaderId, u8 newDirection",
    callsTo: ["TurnVirtualObject"],
    lineCount: 1,
    bodyC: "TurnVirtualObject(MAX_RFU_PLAYERS * leaderId - UR_SPRITE_START_ID + memberId, newDirection);",
  },
  "SetUnionRoomPlayerEnterExitMovement": {
    returnType: "static bool32",
    params: "u32 leaderId, const u8 *movement",
    callsTo: ["ObjectEventIsMovementOverridden","ObjectEventSetHeldMovement","TryGetObjectEventIdByLocalIdAndMap"],
    lineCount: 10,
    bodyC: "u8 objectId;\n    struct ObjectEvent *object;\n    if (TryGetObjectEventIdByLocalIdAndMap(sUnionRoomLocalIds[leaderId], gSaveBlock1Ptr->location.mapNum, gSaveBlock1Ptr->location.mapGroup, &objectId))\n        return FALSE;\n    object = &gObjectEvents[objectId];\n    if (ObjectEventIsMovementOverridden(object))\n        return FALSE;\n    if (ObjectEventSetHeldMovement(object, *movement))\n        return FALSE;\n    return TRUE;",
  },
  "SetUnionRoomPlayerGfx": {
    returnType: "static void",
    params: "u32 leaderId, u32 gfxId",
    callsTo: ["VarSet"],
    lineCount: 1,
    bodyC: "VarSet(VAR_OBJ_GFX_ID_0 + leaderId, gfxId);",
  },
  "ShowUnionRoomPlayer": {
    returnType: "static void",
    params: "u32 player_idx",
    callsTo: ["FlagClear"],
    lineCount: 1,
    bodyC: "FlagClear(FLAG_HIDE_UNION_ROOM_PLAYER_1 + player_idx);",
  },
  "SpawnGroupLeader": {
    returnType: "static bool32",
    params: "u32 leaderId, u32 gender, u32 id",
    callsTo: ["GetUnionRoomPlayerGraphicsId"],
    lineCount: 7,
    bodyC: "struct UnionRoomObject *object = &sUnionObjWork[leaderId];\n    object->schedAnim = UNION_ROOM_SPAWN_IN;\n    object->gfxId = GetUnionRoomPlayerGraphicsId(gender, id);\n\n    if (object->state == 0)\n        return TRUE;\n    else\n        return FALSE;",
  },
  "SpawnGroupLeaderAndMembers": {
    returnType: "static void",
    params: "u32 leaderId, struct RfuGameData *gameData",
    callsTo: ["AssembleGroup","DespawnGroupLeader","DespawnGroupMember","SpawnGroupLeader"],
    lineCount: 20,
    bodyC: "u32 i;\n    switch (gameData->activity)\n    {\n    case ACTIVITY_NONE | IN_UNION_ROOM:\n    case ACTIVITY_PLYRTALK | IN_UNION_ROOM:\n        SpawnGroupLeader(leaderId, gameData->playerGender, gameData->compatibility.playerTrainerId[0]);\n        for (i = 0; i < MAX_RFU_PLAYERS; i++)\n            DespawnGroupMember(leaderId, i);\n        break;\n    case ACTIVITY_BATTLE_SINGLE | IN_UNION_ROOM:\n    case ACTIVITY_TRADE | IN_UNION_ROOM:\n    case ACTIVITY_CHAT | IN_UNION_ROOM:\n    case ACTIVITY_CARD | IN_UNION_ROOM:\n    case ACTIVITY_ACCEPT | IN_UNION_ROOM:\n    case ACTIVITY_DECLINE | IN_UNION_ROOM:\n    case ACTIVITY_NPCTALK | IN_UNION_ROOM:\n        DespawnGroupLeader(leaderId);\n        AssembleGroup(leaderId, gameData);\n        break;\n    }",
  },
  "SpawnGroupMember": {
    returnType: "static void",
    params: "u32 leaderId, u32 memberId, u8 graphicsId, struct RfuGameData *gameData",
    callsTo: ["GetNewFacingDirectionForUnionRoomPlayer","GetUnionRoomPlayerCoords","IsUnionRoomPlayerInvisible","MapGridSetMetatileImpassabilityAt","SetUnionRoomObjectFacingDirection","SetVirtualObjectGraphics","SetVirtualObjectInvisibility","SetVirtualObjectSpriteAnim","UR_PLAYER_SPRITE_ID"],
    lineCount: 11,
    bodyC: "s32 x, y;\n    s32 id = UR_PLAYER_SPRITE_ID(leaderId, memberId);\n    if (IsUnionRoomPlayerInvisible(leaderId, memberId) == TRUE)\n    {\n        SetVirtualObjectInvisibility(id - UR_SPRITE_START_ID, FALSE);\n        SetVirtualObjectSpriteAnim(id - UR_SPRITE_START_ID, UNION_ROOM_SPAWN_IN);\n    }\n    SetVirtualObjectGraphics(id - UR_SPRITE_START_ID, graphicsId);\n    SetUnionRoomObjectFacingDirection(memberId, leaderId, GetNewFacingDirectionForUnionRoomPlayer(memberId, leaderId, gameData));\n    GetUnionRoomPlayerCoords(leaderId, memberId, &x, &y);\n    MapGridSetMetatileImpassabilityAt(x, y, TRUE);",
  },
  "Task_AnimateUnionRoomPlayers": {
    returnType: "static void",
    params: "u8 taskId",
    callsTo: ["AnimateUnionRoomPlayer"],
    lineCount: 3,
    bodyC: "s32 i;\n    for (i = 0; i < MAX_UNION_ROOM_LEADERS; i++)\n        AnimateUnionRoomPlayer(i, &sUnionObjWork[i]);",
  },
  "TryInteractWithUnionRoomMember": {
    returnType: "bool32",
    params: "struct RfuPlayerList *list, s16 *memberIdPtr, s16 *leaderIdPtr, u8 *spriteIds",
    callsTo: ["GetPlayerFacingDirection","GetXYCoordsOneStepInFrontOfPlayer","IsPlayerStandingStill","IsVirtualObjectAnimating","IsVirtualObjectInvisible","SetUnionRoomObjectFacingDirection","UR_PLAYER_SPRITE_ID"],
    lineCount: 28,
    bodyC: "s16 x, y;\n    s32 i, memberId;\n    struct RfuPlayer *leaders;\n    if (!IsPlayerStandingStill())\n        return FALSE;\n\n    GetXYCoordsOneStepInFrontOfPlayer(&x, &y);\n    for (i = 0, leaders = list->players; i < MAX_UNION_ROOM_LEADERS; i++)\n    {\n        for (memberId = 0; memberId < MAX_RFU_PLAYERS; memberId++)\n        {\n            s32 id = UR_PLAYER_SPRITE_ID(i, memberId);\n\n             \n            if (x != sUnionRoomPlayerCoords[i][0] + sUnionRoomGroupOffsets[memberId][0] + 7)\n                continue;\n            if (y != sUnionRoomPlayerCoords[i][1] + sUnionRoomGroupOffsets[memberId][1] + 7)\n                continue;\n\n             \n            if (IsVirtualObjectInvisible(id - UR_SPRITE_START_ID))\n                continue;\n            if (IsVirtualObjectAnimating(id - UR_SPRITE_START_ID))\n                continue;\n            if (leaders[i].groupScheduledAnim != UNION_ROOM_SPAWN_IN)\n                continue;\n\n             \n            SetUnionRoomObjectFacingDirection(memberId, i, sOppositeFacingDirection[GetPlayerFacingDirection()]);\n            *memberIdPtr = memberId;\n            *leaderIdPtr = i;\n            return TRUE;\n        }\n    }\n    return FALSE;",
  },
  "TryReleaseUnionRoomPlayerObjectEvent": {
    returnType: "static bool32",
    params: "u32 leaderId",
    callsTo: ["ArePlayerFieldControlsLocked","FreezeObjectEvent","ObjectEventClearHeldMovementIfFinished","TryGetObjectEventIdByLocalIdAndMap","UnfreezeObjectEvent"],
    lineCount: 12,
    bodyC: "u8 objectId;\n    struct ObjectEvent *object;\n    if (TryGetObjectEventIdByLocalIdAndMap(sUnionRoomLocalIds[leaderId], gSaveBlock1Ptr->location.mapNum, gSaveBlock1Ptr->location.mapGroup, &objectId))\n        return TRUE;\n\n    object = &gObjectEvents[objectId];\n    if (!ObjectEventClearHeldMovementIfFinished(object))\n        return FALSE;\n\n    if (!ArePlayerFieldControlsLocked())\n        UnfreezeObjectEvent(object);\n    else\n        FreezeObjectEvent(object);\n    return TRUE;",
  },
  "UpdateUnionRoomMemberFacing": {
    returnType: "void",
    params: "u32 memberId, u32 leaderId, struct RfuPlayerList *list",
    callsTo: ["GetNewFacingDirectionForUnionRoomPlayer","SetUnionRoomObjectFacingDirection"],
    lineCount: 1,
    bodyC: "return SetUnionRoomObjectFacingDirection(memberId, leaderId, GetNewFacingDirectionForUnionRoomPlayer(memberId, leaderId, &list->players[leaderId].rfu.data));",
  },
  "UpdateUnionRoomPlayerSprites": {
    returnType: "static void",
    params: "struct WirelessLink_URoom *uroom",
    callsTo: ["DespawnGroupLeaderAndMembers","SpawnGroupLeaderAndMembers"],
    lineCount: 10,
    bodyC: "s32 i;\n    struct RfuPlayer *leaders;\n    sUnionObjRefreshTimer = 0;\n    for (i = 0, leaders = uroom->playerList->players; i < MAX_UNION_ROOM_LEADERS; i++)\n    {\n        if (leaders[i].groupScheduledAnim == UNION_ROOM_SPAWN_IN)\n            SpawnGroupLeaderAndMembers(i, &leaders[i].rfu.data);\n        else if (leaders[i].groupScheduledAnim == UNION_ROOM_SPAWN_OUT)\n            DespawnGroupLeaderAndMembers(i, &leaders[i].rfu.data);\n    }",
  },
} as const;
