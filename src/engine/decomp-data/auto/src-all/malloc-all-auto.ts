// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/malloc.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 8 functions extracted from src/malloc.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let sHeapSize: any = null;
let sHeapStart: any = null;
/** void PutMemBlockHeader(void *block, struct MemBlock *prev, struct MemBlock *next, u32 size) */
export function PutMemBlockHeader(block: any, prev: any, next: any, size: any): any {
  let header: any = block;

      header.flag = FALSE;
      header.magic = (0xA3A3);
      header.size = size;
      header.prev = prev;
      header.next = next;
}

/** void PutFirstMemBlockHeader(void *block, u32 size) */
export function PutFirstMemBlockHeader(block: any, size: any): any {
  PutMemBlockHeader(block, block, block, size - 0);
}

/** void FreeInternal(void *heapStart, void *pointer) */
export function FreeInternal(heapStart: any, pointer: any): any {
  if (pointer)
      {
          let head: any = heapStart;
          let block: any = (pointer - 0);
          block.flag = FALSE;

           
           
          if (block.next != head)
          {
              if (!block.next.flag)
              {
                  block.size += 0 + block.next.size;
                  block.next.magic = 0;
                  block.next = block.next.next;
                  if (block.next != head)
                      block.next.prev = block;
              }
          }

           
           
          if (block != head)
          {
              if (!block.prev.flag)
              {
                  block.prev.next = block.next;

                  if (block.next != head)
                      block.next.prev = block.prev;

                  block.magic = 0;
                  block.prev.size += 0 + block.size;
              }
          }
      }
}

/** bool32 CheckMemBlockInternal(void *heapStart, void *pointer) */
export function CheckMemBlockInternal(heapStart: any, pointer: any): any {
  let head: any = heapStart;
      let block: any = (pointer - 0);

      if (block.magic != (0xA3A3))
          return FALSE;

      if (block.next.magic != (0xA3A3))
          return FALSE;

      if (block.next != head && block.next.prev != block)
          return FALSE;

      if (block.prev.magic != (0xA3A3))
          return FALSE;

      if (block.prev != head && block.prev.next != block)
          return FALSE;

      if (block.next != head && block.next != (block.data + block.size))
          return FALSE;

      return TRUE;
}

/** void InitHeap(void *heapStart, u32 heapSize) */
export function InitHeap(heapStart: any, heapSize: any): any {
  sHeapStart = heapStart;
      sHeapSize = heapSize;
      PutFirstMemBlockHeader(heapStart, heapSize);
}

/** void Free(void *pointer) */
export function Free(pointer: any): any {
  FreeInternal(sHeapStart, pointer);
}

/** bool32 CheckMemBlock(void *pointer) */
export function CheckMemBlock(pointer: any): any {
  return CheckMemBlockInternal(sHeapStart, pointer);
}

/** bool32 CheckHeap() */
export function CheckHeap(): any {
  let pos: any = sHeapStart;

      do {
          if (!CheckMemBlockInternal(sHeapStart, pos.data))
              return FALSE;
          pos = pos.next;
      } while (pos != sHeapStart);

      return TRUE;
}

// ─── callsTo manifest (= 4 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["CheckMemBlockInternal","FreeInternal","PutFirstMemBlockHeader","PutMemBlockHeader"];
