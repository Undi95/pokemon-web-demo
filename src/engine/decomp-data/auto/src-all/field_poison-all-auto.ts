// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/field_poison.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 7 functions extracted from src/field_poison.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  AdjustFriendship, AllMonsFainted, CreateTask, CurrentBattlePyramidLocation,
  DestroyTask, FaintFromFieldPoison, FldEffPoison_Start, GetAilmentFromStatus,
  GetMonData, InBattlePike, InTrainerHillChallenge, IsFieldMessageBoxHidden,
  IsMonValidSpecies, MonFaintedFromPoison, ScriptContext_Enable, ScriptContext_Stop,
  SetMonData, ShowFieldMessage, StringGet_Nickname,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static bool32 IsMonValidSpecies(struct Pokemon *pokemon) */
export function IsMonValidSpecies(pokemon: any): any {
  let species: any = GetMonData(pokemon, MON_DATA_SPECIES_OR_EGG);
      if (species == SPECIES_NONE || species == SPECIES_EGG)
          return FALSE;

      return TRUE;
}

/** static bool32 AllMonsFainted(void) */
export function AllMonsFainted(): any {
  let i: any = null;
      let pokemon: any = gPlayerParty;

      for (i = 0; i < PARTY_SIZE; i++, pokemon++)
      {
          if (IsMonValidSpecies(pokemon) && GetMonData(pokemon, MON_DATA_HP) != 0)
              return FALSE;
      }
      return TRUE;
}

/** static void FaintFromFieldPoison(u8 partyIdx) */
export function FaintFromFieldPoison(partyIdx: any): any {
  let pokemon: any =gPlayerParty[partyIdx];
      let status: any = STATUS1_NONE;

      AdjustFriendship(pokemon, FRIENDSHIP_EVENT_FAINT_FIELD_PSN);
      SetMonData(pokemon, MON_DATA_STATUS,status);
      GetMonData(pokemon, MON_DATA_NICKNAME, gStringVar1);
      StringGet_Nickname(gStringVar1);
}

/** static bool32 MonFaintedFromPoison(u8 partyIdx) */
export function MonFaintedFromPoison(partyIdx: any): any {
  let pokemon: any =gPlayerParty[partyIdx];
      if (IsMonValidSpecies(pokemon) && GetMonData(pokemon, MON_DATA_HP) == 0 && GetAilmentFromStatus(GetMonData(pokemon, MON_DATA_STATUS)) == AILMENT_PSN)
          return TRUE;

      return FALSE;
}

/** static void Task_TryFieldPoisonWhiteOut(u8 taskId) */
export function Task_TryFieldPoisonWhiteOut(taskId: any): any {
  let data: any = gTasks[taskId].data;
      switch (tState)
      {
      case 0:
          for (; tPartyIdx < PARTY_SIZE; tPartyIdx++)
          {
              if (MonFaintedFromPoison(tPartyIdx))
              {
                  FaintFromFieldPoison(tPartyIdx);
                  ShowFieldMessage(gText_PkmnFainted_FldPsn);
                  tState++;
                  return;
              }
          }
          tState = 2;  
          break;
      case 1:
           
          if (IsFieldMessageBoxHidden())
              tState--;
          break;
      case 2:
          if (AllMonsFainted())
          {
              if (CurrentBattlePyramidLocation() || InBattlePike() || InTrainerHillChallenge())
                  gSpecialVar_Result = FLDPSN_FRONTIER_WHITEOUT;
              else
                  gSpecialVar_Result = FLDPSN_WHITEOUT;
          }
          else
          {
              gSpecialVar_Result = FLDPSN_NO_WHITEOUT;
          }
          ScriptContext_Enable();
          DestroyTask(taskId);
          break;
      }
}

/** void TryFieldPoisonWhiteOut(void) */
export function TryFieldPoisonWhiteOut(): any {
  CreateTask(Task_TryFieldPoisonWhiteOut, 80);
      ScriptContext_Stop();
}

/** s32 DoPoisonFieldEffect(void) */
export function DoPoisonFieldEffect(): any {
  let i: any = null;
      let hp: any = null;
      let pokemon: any = gPlayerParty;
      let numPoisoned: any = 0;
      let numFainted: any = 0;

      for (i = 0; i < PARTY_SIZE; i++)
      {
          if (GetMonData(pokemon, MON_DATA_SANITY_HAS_SPECIES) && GetAilmentFromStatus(GetMonData(pokemon, MON_DATA_STATUS)) == AILMENT_PSN)
          {
               
              hp = GetMonData(pokemon, MON_DATA_HP);
              if (hp == 0 || --hp == 0)
                  numFainted++;

              SetMonData(pokemon, MON_DATA_HP,hp);
              numPoisoned++;
          }
          pokemon++;
      }

       
      if (numFainted != 0 || numPoisoned != 0)
          FldEffPoison_Start();

      if (numFainted != 0)
          return FLDPSN_FNT;

      if (numPoisoned != 0)
          return FLDPSN_PSN;

      return FLDPSN_NONE;
}

// ─── callsTo manifest (= 19 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AdjustFriendship","AllMonsFainted","CreateTask","CurrentBattlePyramidLocation","DestroyTask","FaintFromFieldPoison","FldEffPoison_Start","GetAilmentFromStatus","GetMonData","InBattlePike","InTrainerHillChallenge","IsFieldMessageBoxHidden","IsMonValidSpecies","MonFaintedFromPoison","ScriptContext_Enable","ScriptContext_Stop","SetMonData","ShowFieldMessage","StringGet_Nickname"];
