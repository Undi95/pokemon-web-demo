// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/load_save.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 21 functions extracted from src/load_save.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  ApplyNewEncryptionKeyToBagItems, ApplyNewEncryptionKeyToBagItems_, ApplyNewEncryptionKeyToBerryPowder, ApplyNewEncryptionKeyToGameStats,
  CpuFill16, IdentifyFlash, InitFlashTimer, InitHeap,
  Random, SetBagItemsPointers, SetContinueGameWarpToDynamicWarp, SetDecorationInventoriesPointers,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void CheckForFlashMemory(void) */
export function CheckForFlashMemory(): any {
  if (!IdentifyFlash())
      {
          gFlashMemoryPresent = TRUE;
          InitFlashTimer();
      }
      else
      {
          gFlashMemoryPresent = FALSE;
      }
}

/** void ClearSav2(void) */
export function ClearSav2(): any {
  CpuFill16(0,gSaveblock2, 0);
}

/** void ClearSav1(void) */
export function ClearSav1(): any {
  CpuFill16(0,gSaveblock1, 0);
}

/** void SetSaveBlocksPointers(u16 offset) */
export function SetSaveBlocksPointers(offset: any): any {
  let sav1_LocalVar: any =gSaveBlock1Ptr;

      offset = (offset + Random()) & (SAVEBLOCK_MOVE_RANGE - 4);

      gSaveBlock2Ptr = (gSaveblock2) + offset;
      sav1_LocalVar = (gSaveblock1) + offset;
      gPokemonStoragePtr = (gPokemonStorage) + offset;

      SetBagItemsPointers();
      SetDecorationInventoriesPointers();
}

/** void MoveSaveBlocks_ResetHeap(void) */
export function MoveSaveBlocks_ResetHeap(): any {
  void vblankCB, hblankCB;
      let encryptionKey: any = null;
      let saveBlock2Copy: any = null;
      let saveBlock1Copy: any = null;
      let pokemonStorageCopy: any = null;

       
      vblankCB = gMain.vblankCallback;
      hblankCB = gMain.hblankCallback;
      gMain.vblankCallback = NULL;
      gMain.hblankCallback = NULL;
      gTrainerHillVBlankCounter = NULL;

      saveBlock2Copy = (gHeap);
      saveBlock1Copy = (gHeap + 0);
      pokemonStorageCopy = (gHeap + 0 + 0);

       
      saveBlock2Copy = gSaveBlock2Ptr;
      saveBlock1Copy = gSaveBlock1Ptr;
      pokemonStorageCopy = gPokemonStoragePtr;

       
       
      SetSaveBlocksPointers(
        saveBlock2Copy.playerTrainerId[0] +
        saveBlock2Copy.playerTrainerId[1] +
        saveBlock2Copy.playerTrainerId[2] +
        saveBlock2Copy.playerTrainerId[3]);

       
      gSaveBlock2Ptr = saveBlock2Copy;
      gSaveBlock1Ptr = saveBlock1Copy;
      gPokemonStoragePtr = pokemonStorageCopy;

       
      InitHeap(gHeap, HEAP_SIZE);

       
      gMain.hblankCallback = hblankCB;
      gMain.vblankCallback = vblankCB;

       
      encryptionKey = (Random() << 16) + (Random());
      ApplyNewEncryptionKeyToAllEncryptedData(encryptionKey);
      gSaveBlock2Ptr.encryptionKey = encryptionKey;
}

/** u32 UseContinueGameWarp(void) */
export function UseContinueGameWarp(): any {
  return gSaveBlock2Ptr.specialSaveWarpFlags & CONTINUE_GAME_WARP;
}

/** void ClearContinueGameWarpStatus(void) */
export function ClearContinueGameWarpStatus(): any {
  gSaveBlock2Ptr.specialSaveWarpFlags &= ~CONTINUE_GAME_WARP;
}

/** void SetContinueGameWarpStatus(void) */
export function SetContinueGameWarpStatus(): any {
  gSaveBlock2Ptr.specialSaveWarpFlags |= CONTINUE_GAME_WARP;
}

/** void SetContinueGameWarpStatusToDynamicWarp(void) */
export function SetContinueGameWarpStatusToDynamicWarp(): any {
  SetContinueGameWarpToDynamicWarp(0);
      gSaveBlock2Ptr.specialSaveWarpFlags |= CONTINUE_GAME_WARP;
}

/** void ClearContinueGameWarpStatus2(void) */
export function ClearContinueGameWarpStatus2(): any {
  gSaveBlock2Ptr.specialSaveWarpFlags &= ~CONTINUE_GAME_WARP;
}

/** void SavePlayerParty(void) */
export function SavePlayerParty(): any {
  let i: any = null;

      gSaveBlock1Ptr.playerPartyCount = gPlayerPartyCount;

      for (i = 0; i < PARTY_SIZE; i++)
          gSaveBlock1Ptr.playerParty[i] = gPlayerParty[i];
}

/** void LoadPlayerParty(void) */
export function LoadPlayerParty(): any {
  let i: any = null;

      gPlayerPartyCount = gSaveBlock1Ptr.playerPartyCount;

      for (i = 0; i < PARTY_SIZE; i++)
          gPlayerParty[i] = gSaveBlock1Ptr.playerParty[i];
}

/** void SaveObjectEvents(void) */
export function SaveObjectEvents(): any {
  let i: any = null;

      for (i = 0; i < OBJECT_EVENTS_COUNT; i++)
          gSaveBlock1Ptr.objectEvents[i] = gObjectEvents[i];
}

/** void LoadObjectEvents(void) */
export function LoadObjectEvents(): any {
  let i: any = null;

      for (i = 0; i < OBJECT_EVENTS_COUNT; i++)
          gObjectEvents[i] = gSaveBlock1Ptr.objectEvents[i];
}

/** void CopyPartyAndObjectsToSave(void) */
export function CopyPartyAndObjectsToSave(): any {
  SavePlayerParty();
      SaveObjectEvents();
}

/** void CopyPartyAndObjectsFromSave(void) */
export function CopyPartyAndObjectsFromSave(): any {
  LoadPlayerParty();
      LoadObjectEvents();
}

/** void LoadPlayerBag(void) */
export function LoadPlayerBag(): any {
  let i: any = null;

       
      for (i = 0; i < BAG_ITEMS_COUNT; i++)
          gLoadedSaveData.items[i] = gSaveBlock1Ptr.bagPocket_Items[i];

       
      for (i = 0; i < BAG_KEYITEMS_COUNT; i++)
          gLoadedSaveData.keyItems[i] = gSaveBlock1Ptr.bagPocket_KeyItems[i];

       
      for (i = 0; i < BAG_POKEBALLS_COUNT; i++)
          gLoadedSaveData.pokeBalls[i] = gSaveBlock1Ptr.bagPocket_PokeBalls[i];

       
      for (i = 0; i < BAG_TMHM_COUNT; i++)
          gLoadedSaveData.TMsHMs[i] = gSaveBlock1Ptr.bagPocket_TMHM[i];

       
      for (i = 0; i < BAG_BERRIES_COUNT; i++)
          gLoadedSaveData.berries[i] = gSaveBlock1Ptr.bagPocket_Berries[i];

       
      for (i = 0; i < MAIL_COUNT; i++)
          gLoadedSaveData.mail[i] = gSaveBlock1Ptr.mail[i];

      gLastEncryptionKey = gSaveBlock2Ptr.encryptionKey;
}

/** void SavePlayerBag(void) */
export function SavePlayerBag(): any {
  let i: any = null;
      let encryptionKeyBackup: any = null;

       
      for (i = 0; i < BAG_ITEMS_COUNT; i++)
          gSaveBlock1Ptr.bagPocket_Items[i] = gLoadedSaveData.items[i];

       
      for (i = 0; i < BAG_KEYITEMS_COUNT; i++)
          gSaveBlock1Ptr.bagPocket_KeyItems[i] = gLoadedSaveData.keyItems[i];

       
      for (i = 0; i < BAG_POKEBALLS_COUNT; i++)
          gSaveBlock1Ptr.bagPocket_PokeBalls[i] = gLoadedSaveData.pokeBalls[i];

       
      for (i = 0; i < BAG_TMHM_COUNT; i++)
          gSaveBlock1Ptr.bagPocket_TMHM[i] = gLoadedSaveData.TMsHMs[i];

       
      for (i = 0; i < BAG_BERRIES_COUNT; i++)
          gSaveBlock1Ptr.bagPocket_Berries[i] = gLoadedSaveData.berries[i];

       
      for (i = 0; i < MAIL_COUNT; i++)
          gSaveBlock1Ptr.mail[i] = gLoadedSaveData.mail[i];

      encryptionKeyBackup = gSaveBlock2Ptr.encryptionKey;
      gSaveBlock2Ptr.encryptionKey = gLastEncryptionKey;
      ApplyNewEncryptionKeyToBagItems(encryptionKeyBackup);
      gSaveBlock2Ptr.encryptionKey = encryptionKeyBackup;
}

/** void ApplyNewEncryptionKeyToHword(u16 *hWord, u32 newKey) */
export function ApplyNewEncryptionKeyToHword(hWord: any, newKey: any): any {
  *hWord ^= gSaveBlock2Ptr.encryptionKey;
      hWord ^= newKey;
}

/** void ApplyNewEncryptionKeyToWord(u32 *word, u32 newKey) */
export function ApplyNewEncryptionKeyToWord(word: any, newKey: any): any {
  *word ^= gSaveBlock2Ptr.encryptionKey;
      word ^= newKey;
}

/** static void ApplyNewEncryptionKeyToAllEncryptedData(u32 encryptionKey) */
export function ApplyNewEncryptionKeyToAllEncryptedData(encryptionKey: any): any {
  ApplyNewEncryptionKeyToGameStats(encryptionKey);
      ApplyNewEncryptionKeyToBagItems_(encryptionKey);
      ApplyNewEncryptionKeyToBerryPowder(encryptionKey);
      ApplyNewEncryptionKeyToWord(gSaveBlock1Ptr.money, encryptionKey);
      ApplyNewEncryptionKeyToHword(gSaveBlock1Ptr.coins, encryptionKey);
}

// ─── callsTo manifest (= 20 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ApplyNewEncryptionKeyToAllEncryptedData","ApplyNewEncryptionKeyToBagItems","ApplyNewEncryptionKeyToBagItems_","ApplyNewEncryptionKeyToBerryPowder","ApplyNewEncryptionKeyToGameStats","ApplyNewEncryptionKeyToHword","ApplyNewEncryptionKeyToWord","CpuFill16","IdentifyFlash","InitFlashTimer","InitHeap","LoadObjectEvents","LoadPlayerParty","Random","SaveObjectEvents","SavePlayerParty","SetBagItemsPointers","SetContinueGameWarpToDynamicWarp","SetDecorationInventoriesPointers","SetSaveBlocksPointers"];
