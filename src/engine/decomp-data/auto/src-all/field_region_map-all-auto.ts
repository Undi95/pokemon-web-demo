// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/field_region_map.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 6 functions extracted from src/field_region_map.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, AddTextPrinterParameterized, Alloc, AnimateSprites,
  BG_PLTT_ID, BeginNormalPaletteFade, BuildOamBuffer, ClearScheduledBgCopiesToVram,
  CopyWindowToVram, CreateRegionMapCursor, CreateRegionMapPlayerIcon, DISPCNT_OBJ_1D_MAP,
  DISPCNT_OBJ_ON, DeactivateAllTextPrinters, DoRegionMapInputCallback, DoScheduledBgTilemapCopiesToVram,
  DrawStdFrameWithCustomTileAndPalette, FALSE, FillWindowPixelBuffer, FreeAllSpritePalettes,
  FreeAllWindowBuffers, FreeRegionMapIconResources, GetStringCenterAlignXOffset, InitBgsFromTemplates,
  InitRegionMap, InitWindows, LoadOam, LoadUserWindowBorderGfx,
  NULL, PALETTES_ALL, PIXEL_FILL, ProcessSpriteCopyRequests,
  REG_OFFSET_BG0HOFS, REG_OFFSET_BG0VOFS, REG_OFFSET_BG1HOFS, REG_OFFSET_BG1VOFS,
  REG_OFFSET_BG2HOFS, REG_OFFSET_BG2VOFS, REG_OFFSET_BG3HOFS, REG_OFFSET_BG3VOFS,
  REG_OFFSET_DISPCNT, ResetBgsAndClearDma3BusyFlags, ResetSpriteData, ScheduleBgCopyTilemapToVram,
  SetGpuReg, SetGpuRegBits, SetMainCallback2, SetVBlankCallback,
  ShowBg, TRY_FREE_AND_SET_NULL, TransferPlttBuffer, UpdatePaletteFade,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void FieldInitRegionMap(MainCallback callback) */
export function FieldInitRegionMap(callback: any): any {
  SetVBlankCallback(NULL);
      sFieldRegionMapHandler = Alloc(0);
      sFieldRegionMapHandler.state = 0;
      sFieldRegionMapHandler.callback = callback;
      SetMainCallback2(MCB2_InitRegionMapRegisters);
}

/** static void MCB2_InitRegionMapRegisters(void) */
export function MCB2_InitRegionMapRegisters(): any {
  SetGpuReg(REG_OFFSET_DISPCNT, 0);
      SetGpuReg(REG_OFFSET_BG0HOFS, 0);
      SetGpuReg(REG_OFFSET_BG0VOFS, 0);
      SetGpuReg(REG_OFFSET_BG1HOFS, 0);
      SetGpuReg(REG_OFFSET_BG1VOFS, 0);
      SetGpuReg(REG_OFFSET_BG2HOFS, 0);
      SetGpuReg(REG_OFFSET_BG2VOFS, 0);
      SetGpuReg(REG_OFFSET_BG3HOFS, 0);
      SetGpuReg(REG_OFFSET_BG3VOFS, 0);
      ResetSpriteData();
      FreeAllSpritePalettes();
      ResetBgsAndClearDma3BusyFlags(0);
      InitBgsFromTemplates(1, sFieldRegionMapBgTemplates, ARRAY_COUNT(sFieldRegionMapBgTemplates));
      InitWindows(sFieldRegionMapWindowTemplates);
      DeactivateAllTextPrinters();
      LoadUserWindowBorderGfx(0, _0x27, BG_PLTT_ID(13));
      ClearScheduledBgCopiesToVram();
      SetMainCallback2(MCB2_FieldUpdateRegionMap);
      SetVBlankCallback(VBCB_FieldUpdateRegionMap);
}

/** static void VBCB_FieldUpdateRegionMap(void) */
export function VBCB_FieldUpdateRegionMap(): any {
  LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
}

/** static void MCB2_FieldUpdateRegionMap(void) */
export function MCB2_FieldUpdateRegionMap(): any {
  FieldUpdateRegionMap();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
      DoScheduledBgTilemapCopiesToVram();
}

/** static void FieldUpdateRegionMap(void) */
export function FieldUpdateRegionMap(): any {
  let offset: any = null;

      switch (sFieldRegionMapHandler.state)
      {
          case 0:
              InitRegionMap(sFieldRegionMapHandler.regionMap, FALSE);
              CreateRegionMapPlayerIcon(TAG_PLAYER_ICON, TAG_PLAYER_ICON);
              CreateRegionMapCursor(TAG_CURSOR, TAG_CURSOR);
              sFieldRegionMapHandler.state++;
              break;
          case 1:
              DrawStdFrameWithCustomTileAndPalette(WIN_TITLE, FALSE, _0x27, 0xd);
              offset = GetStringCenterAlignXOffset(FONT_NORMAL, gText_Hoenn, _0x38);
              AddTextPrinterParameterized(WIN_TITLE, FONT_NORMAL, gText_Hoenn, offset, 1, 0, NULL);
              ScheduleBgCopyTilemapToVram(0);
              DrawStdFrameWithCustomTileAndPalette(WIN_MAPSEC_NAME, FALSE, _0x27, 0xd);
              PrintRegionMapSecName();
              BeginNormalPaletteFade(PALETTES_ALL, 0, 16, 0, RGB_BLACK);
              sFieldRegionMapHandler.state++;
              break;
          case 2:
              SetGpuRegBits(REG_OFFSET_DISPCNT, DISPCNT_OBJ_1D_MAP | DISPCNT_OBJ_ON);
              ShowBg(0);
              ShowBg(2);
              sFieldRegionMapHandler.state++;
              break;
          case 3:
              if (!gPaletteFade.active)
              {
                  sFieldRegionMapHandler.state++;
              }
              break;
          case 4:
              switch (DoRegionMapInputCallback())
              {
                  case MAP_INPUT_MOVE_END:
                      PrintRegionMapSecName();
                      break;
                  case MAP_INPUT_A_BUTTON:
                  case MAP_INPUT_B_BUTTON:
                      sFieldRegionMapHandler.state++;
                      break;
              }
              break;
          case 5:
              BeginNormalPaletteFade(PALETTES_ALL, 0, 0, 16, RGB_BLACK);
              sFieldRegionMapHandler.state++;
              break;
          case 6:
              if (!gPaletteFade.active)
              {
                  FreeRegionMapIconResources();
                  SetMainCallback2(sFieldRegionMapHandler.callback);
                  TRY_FREE_AND_SET_NULL(sFieldRegionMapHandler);
                  FreeAllWindowBuffers();
              }
              break;
      }
}

/** static void PrintRegionMapSecName(void) */
export function PrintRegionMapSecName(): any {
  if (sFieldRegionMapHandler.regionMap.mapSecType != MAPSECTYPE_NONE)
      {
          FillWindowPixelBuffer(WIN_MAPSEC_NAME, PIXEL_FILL(1));
          AddTextPrinterParameterized(WIN_MAPSEC_NAME, FONT_NORMAL, sFieldRegionMapHandler.regionMap.mapSecName, 0, 1, 0, NULL);
          ScheduleBgCopyTilemapToVram(WIN_MAPSEC_NAME);
      }
      else
      {
          FillWindowPixelBuffer(WIN_MAPSEC_NAME, PIXEL_FILL(1));
          CopyWindowToVram(WIN_MAPSEC_NAME, COPYWIN_FULL);
      }
}

// ─── callsTo manifest (= 40 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AddTextPrinterParameterized","Alloc","AnimateSprites","BG_PLTT_ID","BeginNormalPaletteFade","BuildOamBuffer","ClearScheduledBgCopiesToVram","CopyWindowToVram","CreateRegionMapCursor","CreateRegionMapPlayerIcon","DeactivateAllTextPrinters","DoRegionMapInputCallback","DoScheduledBgTilemapCopiesToVram","DrawStdFrameWithCustomTileAndPalette","FieldUpdateRegionMap","FillWindowPixelBuffer","FreeAllSpritePalettes","FreeAllWindowBuffers","FreeRegionMapIconResources","GetStringCenterAlignXOffset","InitBgsFromTemplates","InitRegionMap","InitWindows","LoadOam","LoadUserWindowBorderGfx","PIXEL_FILL","PrintRegionMapSecName","ProcessSpriteCopyRequests","ResetBgsAndClearDma3BusyFlags","ResetSpriteData","ScheduleBgCopyTilemapToVram","SetGpuReg","SetGpuRegBits","SetMainCallback2","SetVBlankCallback","ShowBg","TRY_FREE_AND_SET_NULL","TransferPlttBuffer","UpdatePaletteFade"];
