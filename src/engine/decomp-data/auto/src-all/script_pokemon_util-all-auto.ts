// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/script_pokemon_util.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 13 functions extracted from src/script_pokemon_util.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** void HealPlayerParty(void) */
export function HealPlayerParty(): any {
  let i, j;
      let ppBonuses: any = null;
      let arg: any = [];

       
      for(i = 0; i < gPlayerPartyCount; i++)
      {
          let maxHP: any = GetMonData(gPlayerParty[i], MON_DATA_MAX_HP);
          arg[0] = maxHP;
          arg[1] = maxHP >> 8;
          SetMonData(gPlayerParty[i], MON_DATA_HP, arg);
          ppBonuses = GetMonData(gPlayerParty[i], MON_DATA_PP_BONUSES);

           
          for(j = 0; j < (4); j++)
          {
              arg[0] = CalculatePPWithBonus(GetMonData(gPlayerParty[i], MON_DATA_MOVE1 + j), ppBonuses, j);
              SetMonData(gPlayerParty[i], MON_DATA_PP1 + j, arg);
          }

           
          arg[0] = 0;
          arg[1] = 0;
          arg[2] = 0;
          arg[3] = 0;
          SetMonData(gPlayerParty[i], MON_DATA_STATUS, arg);
      }
}

/** u8 ScriptGiveMon(u16 species, u8 level, u16 item, u32 unused1, u32 unused2, u8 unused3) */
export function ScriptGiveMon(species: any, level: any, item: any, unused1: any, unused2: any, unused3: any): any {
  let nationalDexNum: any = null;
      let sentToPc: any = null;
      let heldItem: any = [];
      let mon: any = null;

      CreateMon(mon, species, level, (((31) + 1)), FALSE, 0, (0), 0);
      heldItem[0] = item;
      heldItem[1] = item >> 8;
      SetMonData(mon, MON_DATA_HELD_ITEM, heldItem);
      sentToPc = GiveMonToPlayer(mon);
      nationalDexNum = SpeciesToNationalPokedexNum(species);

       
      switch(sentToPc)
      {
      case (0):
      case (1):
          GetSetPokedexFlag(nationalDexNum, FLAG_SET_SEEN);
          GetSetPokedexFlag(nationalDexNum, FLAG_SET_CAUGHT);
          break;
      }
      return sentToPc;
}

/** u8 ScriptGiveEgg(u16 species) */
export function ScriptGiveEgg(species: any): any {
  let mon: any = null;
      let isEgg: any = null;

      CreateEgg(mon, species, TRUE);
      isEgg = TRUE;
      SetMonData(mon, MON_DATA_IS_EGG,isEgg);

      return GiveMonToPlayer(mon);
}

/** void HasEnoughMonsForDoubleBattle(void) */
export function HasEnoughMonsForDoubleBattle(): any {
  switch (GetMonsStateToDoubles())
      {
      case (0):
          gSpecialVar_Result = (0);
          break;
      case (1):
          gSpecialVar_Result = (1);
          break;
      case (2):
          gSpecialVar_Result = (2);
          break;
      }
}

/** static bool8 CheckPartyMonHasHeldItem(u16 item) */
export function CheckPartyMonHasHeldItem(item: any): any {
  let i: any = null;

      for(i = 0; i < (6); i++)
      {
          let species: any = GetMonData(gPlayerParty[i], MON_DATA_SPECIES_OR_EGG);
          if (species != (0) && species != (412) && GetMonData(gPlayerParty[i], MON_DATA_HELD_ITEM) == item)
              return TRUE;
      }
      return FALSE;
}

/** bool8 DoesPartyHaveEnigmaBerry(void) */
export function DoesPartyHaveEnigmaBerry(): any {
  let hasItem: any = CheckPartyMonHasHeldItem((175));
      if (hasItem == TRUE)
          GetBerryNameByBerryType(ItemIdToBerryType((175)), gStringVar1);

      return hasItem;
}

/** void CreateScriptedWildMon(u16 species, u8 level, u16 item) */
export function CreateScriptedWildMon(species: any, level: any, item: any): any {
  let heldItem: any = [];

      ZeroEnemyPartyMons();
      CreateMon(gEnemyParty[0], species, level, (((31) + 1)), 0, 0, (0), 0);
      if (item)
      {
          heldItem[0] = item;
          heldItem[1] = item >> 8;
          SetMonData(gEnemyParty[0], MON_DATA_HELD_ITEM, heldItem);
      }
}

/** void ScriptSetMonMoveSlot(u8 monIndex, u16 move, u8 slot) */
export function ScriptSetMonMoveSlot(monIndex: any, move: any, slot: any): any {
      if (monIndex >= (6))
          monIndex = gPlayerPartyCount - 1;

      SetMonMoveSlot(gPlayerParty[monIndex], move, slot);
}

/** void ChooseHalfPartyForBattle(void) */
export function ChooseHalfPartyForBattle(): any {
  gMain.savedCallback = CB2_ReturnFromChooseHalfParty;
      VarSet((0x40CF), (9));
      InitChooseHalfPartyForBattle(0);
}

/** static void CB2_ReturnFromChooseHalfParty(void) */
export function CB2_ReturnFromChooseHalfParty(): any {
  switch (gSelectedOrderFromParty[0])
      {
      case 0:
          gSpecialVar_Result = FALSE;
          break;
      default:
          gSpecialVar_Result = TRUE;
          break;
      }

      SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic);
}

/** void ChoosePartyForBattleFrontier(void) */
export function ChoosePartyForBattleFrontier(): any {
  gMain.savedCallback = CB2_ReturnFromChooseBattleFrontierParty;
      InitChooseHalfPartyForBattle(gSpecialVar_0x8004 + 1);
}

/** static void CB2_ReturnFromChooseBattleFrontierParty(void) */
export function CB2_ReturnFromChooseBattleFrontierParty(): any {
  switch (gSelectedOrderFromParty[0])
      {
      case 0:
          gSpecialVar_Result = FALSE;
          break;
      default:
          gSpecialVar_Result = TRUE;
          break;
      }

      SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic);
}

/** void ReducePlayerPartyToSelectedMons(void) */
export function ReducePlayerPartyToSelectedMons(): any {
  let party: any[] = [];
      let i: any = null;

      CpuFill32(0, party, 0);

       
      for (i = 0; i < MAX_FRONTIER_PARTY_SIZE; i++)
          if (gSelectedOrderFromParty[i])  
              party[i] = gPlayerParty[gSelectedOrderFromParty[i] - 1];  

      CpuFill32(0, gPlayerParty, 0);

       
      for (i = 0; i < MAX_FRONTIER_PARTY_SIZE; i++)
          gPlayerParty[i] = party[i];

      CalculatePlayerPartyCount();
}

// ─── callsTo manifest (= 19 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["CalculatePPWithBonus","CalculatePlayerPartyCount","CheckPartyMonHasHeldItem","CpuFill32","CreateEgg","CreateMon","GetBerryNameByBerryType","GetMonData","GetMonsStateToDoubles","GetSetPokedexFlag","GiveMonToPlayer","InitChooseHalfPartyForBattle","ItemIdToBerryType","SetMainCallback2","SetMonData","SetMonMoveSlot","SpeciesToNationalPokedexNum","VarSet","ZeroEnemyPartyMons"];
