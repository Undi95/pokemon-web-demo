// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/give_gift_ribbon_to_party.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 1 functions extracted from src/give_gift_ribbon_to_party.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** void GiveGiftRibbonToParty(u8 index, u8 ribbonId) */
export function GiveGiftRibbonToParty(index: any, ribbonId: any): any {
  let i: any = null;
      let gotRibbon: any = FALSE;
      let data: any = 1;
      let array: any = [];
      memcpy(array, sGiftRibbonsMonDataIds, 0);

      if (index < GIFT_RIBBONS_COUNT && ribbonId <= MAX_GIFT_RIBBON)
      {
          gSaveBlock1Ptr.giftRibbons[index] = ribbonId;
          for (i = 0; i < PARTY_SIZE; i++)
          {
              let mon: any =gPlayerParty[i];

              if (GetMonData(mon, MON_DATA_SPECIES) != 0 && GetMonData(mon, MON_DATA_SANITY_IS_EGG) == 0)
              {
                  SetMonData(mon, array[index],data);
                  gotRibbon = TRUE;
              }
          }
          if (gotRibbon)
              FlagSet(FLAG_SYS_RIBBON_GET);
      }
}

// ─── callsTo manifest (= 4 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","FlagSet","GetMonData","SetMonData"];
