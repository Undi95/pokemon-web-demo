// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/confetti_util.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 8 functions extracted from src/confetti_util.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, AllocZeroed, FREE_AND_SET_NULL, GetSpriteTileStartByTag,
  GetTilesPerImage, IndexOfSpritePaletteTag,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** bool32 ConfettiUtil_Init(u8 count) */
export function ConfettiUtil_Init(count: any): any {
  let i: any = 0;

      if (count == 0)
          return FALSE;
      if (count > 64)
          count = 64;

      sWork = AllocZeroed(0);
      if (sWork == NULL)
          return FALSE;
      sWork.array = AllocZeroed(count * 0);
      if (sWork.array == NULL)
      {
          FREE_AND_SET_NULL(sWork);
          return FALSE;
      }

      sWork.count = count;
      for (i = 0; i < count; i++)
      {
          memcpy(sWork.array[i].oam,gDummyOamData, 0);
          sWork.array[i].dummied = TRUE;
      }

      return TRUE;
}

/** bool32 ConfettiUtil_Free(void) */
export function ConfettiUtil_Free(): any {
  let i: any = 0;

      if (sWork == NULL)
          return FALSE;

      for (i = 0; i < sWork.count; i++)
          memcpy(gMain.oamBuffer[i + 64],gDummyOamData, 0);

      memset(sWork.array, 0, sWork.count * 0);
      FREE_AND_SET_NULL(sWork.array);
      memset(sWork, 0, 0);
      FREE_AND_SET_NULL(sWork);

      return TRUE;
}

/** bool32 ConfettiUtil_Update(void) */
export function ConfettiUtil_Update(): any {
  let i: any = 0;

      if (sWork == NULL || sWork.array == NULL)
          return FALSE;

      for (i = 0; i < sWork.count; i++)
      {
          if (sWork.array[i].active && sWork.array[i].allowUpdates)
          {
              if (sWork.array[i].callback != NULL)
                  sWork.array[i].callback(sWork.array[i]);

              if (sWork.array[i].dummied)
              {
                  memcpy(gMain.oamBuffer[i + 64],gDummyOamData, 0);
              }
              else
              {
                  sWork.array[i].oam.y = sWork.array[i].y + sWork.array[i].yDelta;
                  sWork.array[i].oam.x = sWork.array[i].x + sWork.array[i].xDelta;
                  sWork.array[i].oam.priority = sWork.array[i].priority;
                  sWork.array[i].oam.tileNum = sWork.array[i].tileNum;
                  memcpy(gMain.oamBuffer[i + 64],sWork.array[i], 0);
              }
          }
      }

      return TRUE;
}

/** static bool32 SetAnimAndTileNum(struct ConfettiUtil *structPtr, u8 animNum) */
export function SetAnimAndTileNum(structPtr: any, animNum: any): any {
  let tileStart: any = null;

      if (structPtr == NULL)
          return FALSE;

      tileStart = GetSpriteTileStartByTag(structPtr.tileTag);
      if (tileStart == 0xFFFF)
          return FALSE;

      structPtr.animNum = animNum;
      structPtr.tileNum = (GetTilesPerImage(structPtr.oam.shape, structPtr.oam.size) * animNum) + tileStart;
      return TRUE;
}

/** u8 ConfettiUtil_SetCallback(u8 id, void (*func)(struct ConfettiUtil *)) */
export function ConfettiUtil_SetCallback(id: any, func: any): any {
  if (sWork == NULL || id >= sWork.count)
          return 0xFF;
      else if (!sWork.array[id].active)
          return 0xFF;

      sWork.array[id].callback = func;
      return id;
}

/** u8 ConfettiUtil_SetData(u8 id, u8 dataArrayId, s16 dataValue) */
export function ConfettiUtil_SetData(id: any, dataArrayId: any, dataValue: any): any {
  if (sWork == NULL || id >= sWork.count)
          return 0xFF;
      else if (!sWork.array[id].active || dataArrayId > ARRAY_COUNT(sWork.array[id].data) - 1)  
          return 0xFF;

      sWork.array[id].data[dataArrayId] = dataValue;
      return id;
}

/** u8 ConfettiUtil_AddNew(const struct OamData *oam, u16 tileTag, u16 palTag, s16 x, s16 y, u8 animNum, u8 priority) */
export function ConfettiUtil_AddNew(oam: any, tileTag: any, palTag: any, x: any, y: any, animNum: any, priority: any): any {
  let structPtr: any = NULL;
      let i: any = null;

      if (sWork == NULL || oam == NULL)
          return 0xFF;

      for (i = 0; i < sWork.count; i++)
      {
          if (!sWork.array[i].active)
          {
              structPtr =sWork.array[i];
              memset(structPtr, 0, 0);
              structPtr.id = i;
              structPtr.active = TRUE;
              structPtr.allowUpdates = TRUE;
              break;
          }
      }

      if (structPtr == NULL)
          return 0xFF;

      memcpy(structPtr.oam, oam, 0);
      structPtr.tileTag = tileTag;
      structPtr.palTag = palTag;
      structPtr.x = x;
      structPtr.y = y;
      structPtr.oam.paletteNum = IndexOfSpritePaletteTag(palTag);
      if (priority < 4)
      {
          structPtr.priority = priority;
          structPtr.oam.priority = priority;
      }
      SetAnimAndTileNum(structPtr, animNum);

      return structPtr.id;
}

/** u8 ConfettiUtil_Remove(u8 id) */
export function ConfettiUtil_Remove(id: any): any {
  if (sWork == NULL || !sWork.array[id].active)
          return 0xFF;

      memset(sWork.array[id], 0, 0);
      sWork.array[id].oam.y = DISPLAY_HEIGHT;
      sWork.array[id].oam.x = DISPLAY_WIDTH;
      sWork.array[id].dummied = TRUE;
      memcpy(gMain.oamBuffer[id + 64],gDummyOamData, 0);
      return id;
}

// ─── callsTo manifest (= 7 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AllocZeroed","FREE_AND_SET_NULL","GetSpriteTileStartByTag","GetTilesPerImage","IndexOfSpritePaletteTag","SetAnimAndTileNum"];
