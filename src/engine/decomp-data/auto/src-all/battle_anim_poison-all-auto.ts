// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_anim_poison.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 9 functions extracted from src/battle_anim_poison.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  DestroyAnimSprite, GetBattlerSide, GetBattlerSpriteCoord, InitAnimArcTranslation,
  InitSpriteDataForLinearTranslation, InitSpritePosToAnimAttacker, InitSpritePosToAnimTarget, SetAverageBattlerPositions,
  Sin, StartSpriteAnim, StoreSpriteCallbackInData6, TranslateAnimHorizontalArc,
  TranslateSpriteLinearFixedPoint,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static void AnimSludgeProjectile(struct Sprite *sprite) */
export function AnimSludgeProjectile(sprite: any): any {
  if (!gBattleAnimArgs[3])
          StartSpriteAnim(sprite, 2);

      InitSpritePosToAnimAttacker(sprite, TRUE);

      sprite.data[0] = gBattleAnimArgs[2];
      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET);
      sprite.data[5] = -30;

      InitAnimArcTranslation(sprite);

      sprite.callback = AnimSludgeProjectile_Step;
}

/** static void AnimSludgeProjectile_Step(struct Sprite *sprite) */
export function AnimSludgeProjectile_Step(sprite: any): any {
  if (TranslateAnimHorizontalArc(sprite))
          DestroyAnimSprite(sprite);
}

/** static void AnimAcidPoisonBubble(struct Sprite *sprite) */
export function AnimAcidPoisonBubble(sprite: any): any {
  let l1, l2;
      if (!gBattleAnimArgs[3])
          StartSpriteAnim(sprite, 2);

      InitSpritePosToAnimAttacker(sprite, TRUE);
      SetAverageBattlerPositions(gBattleAnimTarget, TRUE,l1,l2);

      if (GetBattlerSide(gBattleAnimAttacker))
          gBattleAnimArgs[4] = -gBattleAnimArgs[4];

      sprite.data[0] = gBattleAnimArgs[2];
      sprite.data[2] = l1 + gBattleAnimArgs[4];
      sprite.data[4] = l2 + gBattleAnimArgs[5];
      sprite.data[5] = -30;

      InitAnimArcTranslation(sprite);

      sprite.callback = AnimAcidPoisonBubble_Step;
}

/** static void AnimAcidPoisonBubble_Step(struct Sprite *sprite) */
export function AnimAcidPoisonBubble_Step(sprite: any): any {
  if (TranslateAnimHorizontalArc(sprite))
          DestroyAnimSprite(sprite);
}

/** static void AnimSludgeBombHitParticle(struct Sprite *sprite) */
export function AnimSludgeBombHitParticle(sprite: any): any {
  sprite.data[0] = gBattleAnimArgs[2];
      sprite.data[1] = sprite.x;
      sprite.data[2] = sprite.x + gBattleAnimArgs[0];
      sprite.data[3] = sprite.y;
      sprite.data[4] = sprite.y + gBattleAnimArgs[1];

      InitSpriteDataForLinearTranslation(sprite);

      sprite.data[5] = sprite.data[1] / gBattleAnimArgs[2];
      sprite.data[6] = sprite.data[2] / gBattleAnimArgs[2];

      sprite.callback = AnimSludgeBombHitParticle_Step;
}

/** static void AnimSludgeBombHitParticle_Step(struct Sprite *sprite) */
export function AnimSludgeBombHitParticle_Step(sprite: any): any {
  TranslateSpriteLinearFixedPoint(sprite);

      sprite.data[1] -= sprite.data[5];
      sprite.data[2] -= sprite.data[6];

      if (!sprite.data[0])
          DestroyAnimSprite(sprite);
}

/** static void AnimAcidPoisonDroplet(struct Sprite *sprite) */
export function AnimAcidPoisonDroplet(sprite: any): any {
  SetAverageBattlerPositions(gBattleAnimTarget, TRUE,sprite.x,sprite.y);

      if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
          gBattleAnimArgs[0] = -gBattleAnimArgs[0];

      sprite.x += gBattleAnimArgs[0];
      sprite.y += gBattleAnimArgs[1];

      sprite.data[0] = gBattleAnimArgs[4];
      sprite.data[2] = sprite.x + gBattleAnimArgs[2];
      sprite.data[4] = sprite.y + sprite.data[0];

      sprite.callback = StartAnimLinearTranslation;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
}

/** static void AnimBubbleEffect(struct Sprite *sprite) */
export function AnimBubbleEffect(sprite: any): any {
  if (!gBattleAnimArgs[2])
      {
          InitSpritePosToAnimTarget(sprite, TRUE);
      }
      else
      {
          SetAverageBattlerPositions(gBattleAnimTarget, TRUE,sprite.x,sprite.y);

          if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
              gBattleAnimArgs[0] = -gBattleAnimArgs[0];

          sprite.x += gBattleAnimArgs[0];
          sprite.y += gBattleAnimArgs[1];
      }

      sprite.callback = AnimBubbleEffect_Step;
}

/** static void AnimBubbleEffect_Step(struct Sprite *sprite) */
export function AnimBubbleEffect_Step(sprite: any): any {
  sprite.data[0] = (sprite.data[0] + 0xB) & 0xFF;
      sprite.x2 = Sin(sprite.data[0], 4);
      sprite.data[1] += _0x30;
      sprite.y2 = -(sprite.data[1] >> 8);

      if (sprite.affineAnimEnded)
          DestroyAnimSprite(sprite);
}

// ─── callsTo manifest (= 13 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["DestroyAnimSprite","GetBattlerSide","GetBattlerSpriteCoord","InitAnimArcTranslation","InitSpriteDataForLinearTranslation","InitSpritePosToAnimAttacker","InitSpritePosToAnimTarget","SetAverageBattlerPositions","Sin","StartSpriteAnim","StoreSpriteCallbackInData6","TranslateAnimHorizontalArc","TranslateSpriteLinearFixedPoint"];
