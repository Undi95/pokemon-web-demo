// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/mini_printf.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 9 functions extracted from src/mini_printf.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  StringLength,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static inline char mini_pchar_decode(char encoded) */
export function mini_pchar_decode(encoded: any): any {
  let ret: any = '?';
      if (encoded >= CHAR_a && encoded <= CHAR_z)
          ret = encoded-(CHAR_a-'a');  
      else if (encoded >= CHAR_A && encoded <= CHAR_Z)
          ret = encoded-(CHAR_A-'A');  
      else if (encoded >= CHAR_0 && encoded <= CHAR_9)
          ret = encoded-(CHAR_0-'0');  
      else if (encoded == CHAR_SPACE)
          ret = ' ';  
      else if (encoded == CHAR_EXCL_MARK)
          ret = '!';  
      else if (encoded == CHAR_QUESTION_MARK)
          ret = '?';  
      else if (encoded == CHAR_PERIOD)
          ret = '.';  
      else if (encoded == CHAR_DBL_QUOTE_LEFT || encoded == CHAR_DBL_QUOTE_RIGHT)
          ret = '"';  
      else if (encoded == CHAR_SGL_QUOTE_LEFT || encoded == CHAR_SGL_QUOTE_RIGHT)
          ret = '\'';  
      else if (encoded == CHAR_CURRENCY)
          ret = '$';  
      else if (encoded == CHAR_COMMA)
          ret = ',';  
      else if (encoded == CHAR_MULT_SIGN)
          ret = '#';  
      else if (encoded == CHAR_SLASH)
          ret = '/';  
      else if (encoded == CHAR_LESS_THAN)
          ret = '<';  
      else if (encoded == CHAR_GREATER_THAN)
          ret = '>';  
      else if (encoded == CHAR_PERCENT)
          ret = '%';  
      else if (encoded == CHAR_LEFT_PAREN)
          ret = '(';  
      else if (encoded == CHAR_RIGHT_PAREN)
          ret = ')';  
      else if (encoded == CHAR_HYPHEN)
          ret = '-';  
      return ret;
}

/** static s32 _putsAscii(char *s, s32 len, void *buf) */
export function _putsAscii(s: any, len: any, buf: any): any {
  let p0: any = null;
      let i: any = null;
      let b: any = null;

      if (!buf)
          return len;

      b = buf;
      p0 = b.buffer;

       
      for (i = 0; i < len; i++)
      {
          if(b.pbuffer == b.buffer + b.buffer_len - 1)
          {
              break;
          }
          b.pbuffer =  s[i];
      }
      MEM_WRITE((b.pbuffer), 0);
      return b.pbuffer - p0;
}

/** static s32 _putsEncoded(char *s, s32 len, void *buf) */
export function _putsEncoded(s: any, len: any, buf: any): any {
  let p0: any = null;
      let i: any = null;
      let b: any = null;

      if (!buf)
          return len;

      b = buf;
      p0 = b.buffer;

       
      for (i = 0; i < len; i++)
      {
          if(b.pbuffer == b.buffer + b.buffer_len - 1)
          {
              break;
          }
          if (s[i] == CHAR_NEWLINE)
          {
              b.pbuffer =  '\\';
              b.pbuffer =  'n';
          }
          else if (s[i] == CHAR_PROMPT_SCROLL)
          {
              b.pbuffer =  '\\';
              b.pbuffer =  'l';
          }
          else if (s[i] == CHAR_PROMPT_CLEAR)
          {
              b.pbuffer =  '\\';
              b.pbuffer =  'p';
          }
          else if (s[i] == CHAR_ELLIPSIS)
          {
              b.pbuffer =  '.';
              b.pbuffer =  '.';
              b.pbuffer =  '.';
          }
          else
          {
              b.pbuffer =  mini_pchar_decode(s[i]);
          }
      }
      MEM_WRITE((b.pbuffer), 0);
      return b.pbuffer - p0;
}

/** static s32 mini_strlen(const char *s) */
export function mini_strlen(s: any): any {
  let len: any = 0;
      while (s[len] != '\0') len++;
      return len;
}

/** static s32 mini_itoa(s32 value, u32 radix, s32 uppercase, bool32 unsig, char *buffer) */
export function mini_itoa(value: any, radix: any, uppercase: any, unsig: any, buffer: any): any {
  let pbuffer: any = buffer;
      let negative: any = 0;
      let i, len;

       
      if (radix > 16)
          return 0;

      if (value < 0 && !unsig)
      {
          negative = 1;
          value = -value;
      }

       
      do
      {
          let digit: any = value % radix;
          pbuffer =  (digit < 10 ? '0' + digit : (uppercase ? 'A' : 'a') + digit - 10);
          value /= radix;
      } while (value > 0);

      if (negative)
          pbuffer =  '-';

      MEM_WRITE((pbuffer), '\0');

       
      len = (pbuffer - buffer);
      for (i = 0; i < len / 2; i++)
      {
          let j: any = buffer[i];
          buffer[i] = buffer[len-i-1];
          buffer[len-i-1] = j;
      }

      return len;
}

/** static s32 mini_itoa_bin(u32 value, char *buffer) */
export function mini_itoa_bin(value: any, buffer: any): any {
  let pbuffer: any = buffer;
      let i, len;

       
      do
      {
          pbuffer =  '0' + (value & 1);
          value /= 2;   
      } while (value > 0);

      MEM_WRITE((pbuffer), '\0');

       
      len = (pbuffer - buffer);
      for (i = 0; i < len / 2; i++)
      {
          let j: any = buffer[i];
          buffer[i] = buffer[len-i-1];
          buffer[len-i-1] = j;
      }

      return len;
}

/** static s32 mini_pad(char *ptr, s32 len, char pad_char, s32 pad_to, char *buffer) */
export function mini_pad(ptr: any, len: any, pad_char: any, pad_to: any, buffer: any): any {
  let i: any = null;
      let overflow: any = FALSE;
      let pbuffer: any = buffer;
      if(pad_to == 0)
          pad_to = len;
      if (len > pad_to)
      {
          len = pad_to;
          overflow = TRUE;
      }
      for(i = pad_to - len; i > 0; i --)
      {
          pbuffer =  pad_char;
      }
      for(i = len; i > 0; i --)
      {
          pbuffer =  (ptr++);
      }
      len = pbuffer - buffer;
      if(overflow)
      {
          for (i = 0; i < 3 && pbuffer > buffer; i ++)
          {
              MEM_WRITE((pbuffer-- - 1), '*');
          }
      }
      return len;
}

/** s32 mini_vsnprintf(char *buffer, u32 buffer_len, const char *fmt, va_list va) */
export function mini_vsnprintf(buffer: any, buffer_len: any, fmt: any, va: any): any {
  let b: any = null;
      let n: any = null;
      b.buffer = buffer;
      b.pbuffer = buffer;
      b.buffer_len = buffer_len;
      if (buffer_len == 0)
          buffer = NULL;
      n = mini_vpprintf((buffer != NULL) ?b : NULL, fmt, va);
      if (buffer == NULL)
          return n;
      return b.pbuffer - b.buffer;
}

/** s32 mini_vpprintf(void *buf, const char *fmt, va_list va) */
export function mini_vpprintf(buf: any, fmt: any, va: any): any {
  const bf: any[] = [];
      const bf2: any[] = [];
      let ch: any = null;
      let n: any = null;
      n = 0;
      while ((ch=(fmt++)))
      {
          let len: any = null;
          if (ch != '%')
          {
              len = 1;
              len = _putsAscii(ch, len, buf);
          }
          else
          {
              let pad_char: any = ' ';
              let pad_to: any = 0;
              let l: any = 0;
              let ptr: any = null;

              ch=(fmt++);

               
              if (ch == '0')
                  pad_char = '0';
              while (ch >= '0' && ch <= '9')
              {
                  pad_to = pad_to * 10 + (ch - '0');
                  ch= (fmt++);
              }
              if(pad_to > 0)
              {
                  pad_to = 0;
              }
              if (ch == 'l')
              {
                  l = 1;
                  ch=(fmt++);
              }

              switch (ch)
              {
                  case 0:
  // goto end;
                  case 'u':
                  case 'd':
                      if(l)
                      {
                          len = mini_itoa(va_arg(va, "u32"), 10, 0, (ch=='u'), bf2);
                      }
                      else
                      {
                          if(ch == 'u')
                          {
                              len = mini_itoa(va_arg(va, "u32"), 10, 0, 1, bf2);
                          }
                          else
                          {
                              len = mini_itoa(va_arg(va, "s32"), 10, 0, 0, bf2);
                          }
                      }
                      len = mini_pad(bf2, len, pad_char, pad_to, bf);
                      len = _putsAscii(bf, len, buf);
                      break;

                  case 'x':
                  case 'X':
                      if(l)
                      {
                          len = mini_itoa(va_arg(va, "u32"), 16, (ch=='X'), 1, bf2);
                      }
                      else
                      {
                          len = mini_itoa(va_arg(va, "u32"), 16, (ch=='X'), 1, bf2);
                      }
                      len = mini_pad(bf2, len, pad_char, pad_to, bf);
                      len = _putsAscii(bf, len, buf);
                      break;
                  case 'b':
                      len = mini_itoa_bin(va_arg(va, "u32"), bf2);
                      len = mini_pad(bf2, len, pad_char, pad_to, bf);
                      len = _putsAscii(bf, len, buf);
                      break;
                  case 'c' :
                      ch = (va_arg(va, "s32"));
                      len = mini_pad(ch, 1, pad_char, pad_to, bf);
                      len = _putsAscii(bf, len, buf);
                      break;

                  case 's' :
                      ptr = va_arg(va, "char*");
                      len = mini_strlen(ptr);
                      if (pad_to > 0)
                      {
                          len = mini_pad(ptr, len, pad_char, pad_to, bf);
                          len = _putsAscii(bf, len, buf);
                      }
                      else
                      {
                          len = _putsAscii(ptr, len, buf);
                      }
                      break;
                  case 'S' :  
                      ptr = va_arg(va, "char*");
                      len = StringLength(ptr);
                      if (pad_to > 0)
                      {
                          len = mini_pad(ptr, len, pad_char, pad_to, bf);
                          len = _putsEncoded(bf, len, buf);
                      }
                      else
                      {
                          len = _putsEncoded(ptr, len, buf);
                      }
                      break;
                  default:
                      len = 1;
                      len = _putsAscii(ch, len, buf);
                      break;
              }
          }
          n = n + len;
      }
  // LABEL: end:
      return n;
}

// ─── callsTo manifest (= 3 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["StringLength","_putsAscii","_putsEncoded"];
