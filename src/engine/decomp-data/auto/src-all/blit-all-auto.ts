// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/blit.c
// Generated : 2026-05-08
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 5 functions extracted from src/blit.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** void BlitBitmapRect4BitWithoutColorKey(const struct Bitmap *src, struct Bitmap *dst, u16 srcX, u16 srcY, u16 dstX, u16 dstY, u16 width, u16 height) */
export function BlitBitmapRect4BitWithoutColorKey(...args: any[]): any {
  BlitBitmapRect4Bit(src, dst, srcX, srcY, dstX, dstY, width, height, 0xFF);
}

/** void BlitBitmapRect4Bit(const struct Bitmap *src, struct Bitmap *dst, u16 srcX, u16 srcY, u16 dstX, u16 dstY, u16 width, u16 height, u8 colorKey) */
export function BlitBitmapRect4Bit(...args: any[]): any {
  let xEnd: any = null;
      let yEnd: any = null;
      let multiplierSrcY: any = null;
      let multiplierDstY: any = null;
      let loopSrcY, loopDstY;
      let loopSrcX, loopDstX;
      let pixelsSrc: any = null;
      let pixelsDst: any = null;
      let toOrr: any = null;
      let toAnd: any = null;
      let toShift: any = null;

      if (dst.width - dstX < width)
          xEnd = (dst.width - dstX) + srcX;
      else
          xEnd = srcX + width;

      if (dst.height - dstY < height)
          yEnd = (dst.height - dstY) + srcY;
      else
          yEnd = height + srcY;

      multiplierSrcY = (src.width + (src.width & 7)) >> 3;
      multiplierDstY = (dst.width + (dst.width & 7)) >> 3;

      if (colorKey == 0xFF)
      {
          for (loopSrcY = srcY, loopDstY = dstY; loopSrcY < yEnd; loopSrcY++, loopDstY++)
          {
              for (loopSrcX = srcX, loopDstX = dstX; loopSrcX < xEnd; loopSrcX++, loopDstX++)
              {
                  pixelsSrc = src.pixels + ((loopSrcX >> 1) & 3) + ((loopSrcX >> 3) << 5) + (((loopSrcY >> 3) * multiplierSrcY) << 5) + ((u32)(loopSrcY << 0x1d) >> 0x1B);
                  pixelsDst = dst.pixels + ((loopDstX >> 1) & 3) + ((loopDstX >> 3) << 5) + (((loopDstY >> 3) * multiplierDstY) << 5) + ((u32)(loopDstY << 0x1d) >> 0x1B);
                  toOrr = ((*pixelsSrc >> ((loopSrcX & 1) << 2)) & 0xF);
                  toShift = ((loopDstX & 1) << 2);
                  toOrr <<= toShift;
                  toAnd = 0xF0 >> (toShift);
                  *pixelsDst = toOrr | (*pixelsDst & toAnd);
              }
          }
      }
      else
      {
          for (loopSrcY = srcY, loopDstY = dstY; loopSrcY < yEnd; loopSrcY++, loopDstY++)
          {
              for (loopSrcX = srcX, loopDstX = dstX; loopSrcX < xEnd; loopSrcX++, loopDstX++)
              {
                  pixelsSrc = src.pixels + ((loopSrcX >> 1) & 3) + ((loopSrcX >> 3) << 5) + (((loopSrcY >> 3) * multiplierSrcY) << 5) + ((u32)(loopSrcY << 0x1d) >> 0x1B);
                  pixelsDst = dst.pixels + ((loopDstX >> 1) & 3) + ((loopDstX >> 3) << 5) + (((loopDstY >> 3) * multiplierDstY) << 5) + ((u32)(loopDstY << 0x1d) >> 0x1B);
                  toOrr = ((*pixelsSrc >> ((loopSrcX & 1) << 2)) & 0xF);
                  if (toOrr != colorKey)
                  {
                      toShift = ((loopDstX & 1) << 2);
                      toOrr <<= toShift;
                      toAnd = 0xF0 >> (toShift);
                      *pixelsDst = toOrr | (*pixelsDst & toAnd);
                  }
              }
          }
      }
}

/** void FillBitmapRect4Bit(struct Bitmap *surface, u16 x, u16 y, u16 width, u16 height, u8 fillValue) */
export function FillBitmapRect4Bit(...args: any[]): any {
  let xEnd: any = null;
      let yEnd: any = null;
      let multiplierY: any = null;
      let loopX, loopY;
      let toOrr1, toOrr2;

      xEnd = x + width;
      if (xEnd > surface.width)
          xEnd = surface.width;

      yEnd = y + height;
      if (yEnd > surface.height)
          yEnd = surface.height;

      multiplierY = (surface.width + (surface.width & 7)) >> 3;
      toOrr1 = fillValue << 4;
      toOrr2 = fillValue & 0xF;

      for (loopY = y; loopY < yEnd; loopY++)
      {
          for (loopX = x; loopX < xEnd; loopX++)
          {
              let pixels = surface.pixels + ((loopX >> 1) & 3) + ((loopX >> 3) << 5) + (((loopY >> 3) * multiplierY) << 5) + ((u32)(loopY << 0x1d) >> 0x1B);
              if ((loopX << 0x1F) != 0)
                  *pixels = toOrr1 | (*pixels & 0xF);
              else
                  *pixels = toOrr2 | (*pixels & 0xF0);
          }
      }
}

/** void BlitBitmapRect4BitTo8Bit(const struct Bitmap *src, struct Bitmap *dst, u16 srcX, u16 srcY, u16 dstX, u16 dstY, u16 width, u16 height, u8 colorKey, u8 paletteOffset) */
export function BlitBitmapRect4BitTo8Bit(...args: any[]): any {
  let palOffsetBits: any = null;
      let xEnd: any = null;
      let yEnd: any = null;
      let multiplierSrcY: any = null;
      let multiplierDstY: any = null;
      let loopSrcY, loopDstY;
      let loopSrcX, loopDstX;
      let pixelsSrc: any = null;
      let pixelsDst: any = null;
      let colorKeyBits: any = null;

      palOffsetBits = (u32)(paletteOffset << 0x1C) >> 0x18;
      colorKeyBits = (u32)(colorKey << 0x1C) >> 0x18;

      if (dst.width - dstX < width)
          xEnd = (dst.width - dstX) + srcX;
      else
          xEnd = width + srcX;

      if (dst.height - dstY < height)
          yEnd = (srcY + dst.height) - dstY;
      else
          yEnd = srcY + height;

      multiplierSrcY = (src.width + (src.width & 7)) >> 3;
      multiplierDstY = (dst.width + (dst.width & 7)) >> 3;

      if (colorKey == 0xFF)
      {
          for (loopSrcY = srcY, loopDstY = dstY; loopSrcY < yEnd; loopSrcY++, loopDstY++)
          {
              pixelsSrc = src.pixels + ((srcX >> 1) & 3) + ((srcX >> 3) << 5) + (((loopSrcY >> 3) * multiplierSrcY) << 5) + ((u32)(loopSrcY << 0x1d) >> 0x1b);
              for (loopSrcX = srcX, loopDstX = dstX; loopSrcX < xEnd; loopSrcX++, loopDstX++)
              {
                  pixelsDst = dst.pixels + (loopDstX & 7) + ((loopDstX >> 3) << 6) + (((loopDstY >> 3) * multiplierDstY) << 6) + ((u32)(loopDstY << 0x1d) >> 0x1a);
                  if (loopSrcX & 1)
                  {
                      *pixelsDst = palOffsetBits + (*pixelsSrc >> 4);
                  }
                  else
                  {
                      pixelsSrc = src.pixels + ((loopSrcX >> 1) & 3) + ((loopSrcX >> 3) << 5) + (((loopSrcY >> 3) * multiplierSrcY) << 5) + ((u32)(loopSrcY << 0x1d) >> 0x1b);
                      *pixelsDst = palOffsetBits + (*pixelsSrc & 0xF);
                  }
              }
          }
      }
      else
      {
          for (loopSrcY = srcY, loopDstY = dstY; loopSrcY < yEnd; loopSrcY++, loopDstY++)
          {
              pixelsSrc = src.pixels + ((srcX >> 1) & 3) + ((srcX >> 3) << 5) + (((loopSrcY >> 3) * multiplierSrcY) << 5) + ((u32)(loopSrcY << 0x1d) >> 0x1b);
              for (loopSrcX = srcX, loopDstX = dstX; loopSrcX < xEnd; loopSrcX++, loopDstX++)
              {
                  if (loopSrcX & 1)
                  {
                      if ((*pixelsSrc & 0xF0) != colorKeyBits)
                      {
                          pixelsDst = dst.pixels + (loopDstX & 7) + ((loopDstX >> 3) << 6) + (((loopDstY >> 3) * multiplierDstY) << 6) + ((u32)(loopDstY << 0x1d) >> 0x1a);
                          *pixelsDst = palOffsetBits + (*pixelsSrc >> 4);
                      }
                  }
                  else
                  {
                      pixelsSrc = src.pixels + ((loopSrcX >> 1) & 3) + ((loopSrcX >> 3) << 5) + (((loopSrcY >> 3) * multiplierSrcY) << 5) + ((u32)(loopSrcY << 0x1d) >> 0x1b);
                      if ((*pixelsSrc & 0xF) != colorKey)
                      {
                          pixelsDst = dst.pixels + (loopDstX & 7) + ((loopDstX >> 3) << 6) + (((loopDstY >> 3) * multiplierDstY) << 6) + ((u32)(loopDstY << 0x1d) >> 0x1a);
                          *pixelsDst = palOffsetBits + (*pixelsSrc & 0xF);
                      }
                  }
              }
          }
      }
}

/** void FillBitmapRect8Bit(struct Bitmap *surface, u16 x, u16 y, u16 width, u16 height, u8 fillValue) */
export function FillBitmapRect8Bit(...args: any[]): any {
  let xEnd: any = null;
      let yEnd: any = null;
      let multiplierY: any = null;
      let loopX, loopY;

      xEnd = x + width;
      if (xEnd > surface.width)
          xEnd = surface.width;

      yEnd = y + height;
      if (yEnd > surface.height)
          yEnd = surface.height;

      multiplierY = (surface.width + (surface.width & 7)) >> 3;

      for (loopY = y; loopY < yEnd; loopY++)
      {
          for (loopX = x; loopX < xEnd; loopX++)
          {
              let pixels = surface.pixels + (loopX & 7) + ((loopX >> 3) << 6) + (((loopY >> 3) * multiplierY) << 6) + ((u32)(loopY << 0x1d) >> 0x1a);
              *pixels = fillValue;
          }
      }
}

// ─── callsTo manifest (= 1 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["BlitBitmapRect4Bit"];
