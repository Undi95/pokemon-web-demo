// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/title_screen.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 20 functions extracted from src/title_screen.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let gBattle_BG1_X: any = null;
let gBattle_BG1_Y: any = null;
let gReservedSpritePaletteCount: any = null;
/** static void SpriteCB_VersionBannerLeft(struct Sprite *sprite) */
export function SpriteCB_VersionBannerLeft(sprite: any): any {
  if (gTasks[sprite.sParentTaskId].tSkipToNext)
      {
          sprite.oam.objMode = ST_OAM_OBJ_NORMAL;
          sprite.y = (68);
      }
      else
      {
          if (sprite.y != (68))
              sprite.y++;
          if (sprite.sAlphaBlendIdx != 0)
              sprite.sAlphaBlendIdx--;
          SetGpuReg(REG_OFFSET_BLDALPHA, gTitleScreenAlphaBlend[sprite.sAlphaBlendIdx]);
      }
}

/** static void SpriteCB_VersionBannerRight(struct Sprite *sprite) */
export function SpriteCB_VersionBannerRight(sprite: any): any {
  if (gTasks[sprite.sParentTaskId].tSkipToNext)
      {
          sprite.oam.objMode = ST_OAM_OBJ_NORMAL;
          sprite.y = (68);
      }
      else
      {
          if (sprite.y != (68))
              sprite.y++;
      }
}

/** static void SpriteCB_PressStartCopyrightBanner(struct Sprite *sprite) */
export function SpriteCB_PressStartCopyrightBanner(sprite: any): any {
  if (sprite.sAnimate == TRUE)
      {
           
          if (++sprite.sTimer & 16)
              sprite.invisible = FALSE;
          else
              sprite.invisible = TRUE;
      }
      else
      {
          sprite.invisible = FALSE;
      }
}

/** static void CreatePressStartBanner(s16 x, s16 y) */
export function CreatePressStartBanner(x: any, y: any): any {
  let i: any = null;
      let spriteId: any = null;

      x -= 64;
      for (i = 0; i < (5); i++, x += 32)
      {
          spriteId = CreateSprite(sStartCopyrightBannerSpriteTemplate, x, y, 0);
          StartSpriteAnim(gSprites[spriteId], i);
          gSprites[spriteId].sAnimate = TRUE;
      }
}

/** static void CreateCopyrightBanner(s16 x, s16 y) */
export function CreateCopyrightBanner(x: any, y: any): any {
  let i: any = null;
      let spriteId: any = null;

      x -= 64;
      for (i = 0; i < (5); i++, x += 32)
      {
          spriteId = CreateSprite(sStartCopyrightBannerSpriteTemplate, x, y, 0);
          StartSpriteAnim(gSprites[spriteId], i + (5));
      }
}

/** static void SpriteCB_PokemonLogoShine(struct Sprite *sprite) */
export function SpriteCB_PokemonLogoShine(sprite: any): any {
  if (sprite.x < DISPLAY_WIDTH + 32)
      {
           
           
          if (sprite.sMode != SHINE_MODE_SINGLE_NO_BG_COLOR)
          {
              let backgroundColor: any = null;

              if (sprite.x < DISPLAY_WIDTH / 2)
              {
                   
                  if (sprite.sBgColor < 31)
                      sprite.sBgColor++;
                  if (sprite.sBgColor < 31)
                      sprite.sBgColor++;
              }
              else
              {
                   
                  if (sprite.sBgColor != 0)
                      sprite.sBgColor--;
                  if (sprite.sBgColor != 0)
                      sprite.sBgColor--;
              }

              backgroundColor = _RGB(sprite.sBgColor, sprite.sBgColor, sprite.sBgColor);

               
               
              if (sprite.x == DISPLAY_WIDTH / 2 + (3 * (4))
               || sprite.x == DISPLAY_WIDTH / 2 + (4 * (4))
               || sprite.x == DISPLAY_WIDTH / 2 + (5 * (4))
               || sprite.x == DISPLAY_WIDTH / 2 + (6 * (4)))
                  gPlttBufferFaded[0] = RGB(24, 31, 12);
              else
                  gPlttBufferFaded[0] = backgroundColor;
          }

          sprite.x += (4);
      }
      else
      {
           
          gPlttBufferFaded[0] = (RGB(0, 0, 0));
          DestroySprite(sprite);
      }
}

/** static void SpriteCB_PokemonLogoShine_Fast(struct Sprite *sprite) */
export function SpriteCB_PokemonLogoShine_Fast(sprite: any): any {
  if (sprite.x < DISPLAY_WIDTH + 32)
          sprite.x += (4) * 2;
      else
          DestroySprite(sprite);
}

/** static void StartPokemonLogoShine(u8 mode) */
export function StartPokemonLogoShine(mode: any): any {
  let spriteId: any = null;

      switch (mode)
      {
      case SHINE_MODE_SINGLE_NO_BG_COLOR:
      case SHINE_MODE_SINGLE:
           
           
          spriteId = CreateSprite(sPokemonLogoShineSpriteTemplate, 0, 68, 0);
          gSprites[spriteId].oam.objMode = ST_OAM_OBJ_WINDOW;
          gSprites[spriteId].sMode = mode;
          break;
      case SHINE_MODE_DOUBLE:
           
          spriteId = CreateSprite(sPokemonLogoShineSpriteTemplate, 0, 68, 0);
          gSprites[spriteId].oam.objMode = ST_OAM_OBJ_WINDOW;
          gSprites[spriteId].sMode = mode;
          gSprites[spriteId].invisible = TRUE;

           
          spriteId = CreateSprite(sPokemonLogoShineSpriteTemplate, 0, 68, 0);
          gSprites[spriteId].callback = SpriteCB_PokemonLogoShine_Fast;
          gSprites[spriteId].oam.objMode = ST_OAM_OBJ_WINDOW;

          spriteId = CreateSprite(sPokemonLogoShineSpriteTemplate, -80, 68, 0);
          gSprites[spriteId].callback = SpriteCB_PokemonLogoShine_Fast;
          gSprites[spriteId].oam.objMode = ST_OAM_OBJ_WINDOW;
          break;
      }
}

/** static void VBlankCB(void) */
export function VBlankCB(): any {
  ScanlineEffect_InitHBlankDmaTransfer();
      LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
      SetGpuReg(REG_OFFSET_BG1VOFS, gBattle_BG1_Y);
}

/** void CB2_InitTitleScreen(void) */
export function CB2_InitTitleScreen(): any {
  switch (gMain.state)
      {
      default:
      case 0:
          SetVBlankCallback(NULL);
          SetGpuReg(REG_OFFSET_BLDCNT, 0);
          SetGpuReg(REG_OFFSET_BLDALPHA, 0);
          SetGpuReg(REG_OFFSET_BLDY, 0);
          MEM_WRITE((PLTT), (RGB(31, 31, 31)));
          SetGpuReg(REG_OFFSET_DISPCNT, 0);
          SetGpuReg(REG_OFFSET_BG2CNT, 0);
          SetGpuReg(REG_OFFSET_BG1CNT, 0);
          SetGpuReg(REG_OFFSET_BG0CNT, 0);
          SetGpuReg(REG_OFFSET_BG2HOFS, 0);
          SetGpuReg(REG_OFFSET_BG2VOFS, 0);
          SetGpuReg(REG_OFFSET_BG1HOFS, 0);
          SetGpuReg(REG_OFFSET_BG1VOFS, 0);
          SetGpuReg(REG_OFFSET_BG0HOFS, 0);
          SetGpuReg(REG_OFFSET_BG0VOFS, 0);
          DmaFill16(3, 0, VRAM, VRAM_SIZE);
          DmaFill32(3, 0, OAM, OAM_SIZE);
          DmaFill16(3, 0, (PLTT + 2), PLTT_SIZE - 2);
          ResetPaletteFade();
          gMain.state = 1;
          break;
      case 1:
           
          LZ77UnCompVram(gTitleScreenPokemonLogoGfx, (BG_CHAR_ADDR(0)));
          LZ77UnCompVram(gTitleScreenPokemonLogoTilemap, (BG_SCREEN_ADDR(9)));
          LoadPalette(gTitleScreenBgPalettes, BG_PLTT_ID(0), 15 * PLTT_SIZE_4BPP);
           
          LZ77UnCompVram(sTitleScreenRayquazaGfx, (BG_CHAR_ADDR(2)));
          LZ77UnCompVram(sTitleScreenRayquazaTilemap, (BG_SCREEN_ADDR(26)));
           
          LZ77UnCompVram(sTitleScreenCloudsGfx, (BG_CHAR_ADDR(3)));
          LZ77UnCompVram(gTitleScreenCloudsTilemap, (BG_SCREEN_ADDR(27)));
          ScanlineEffect_Stop();
          ResetTasks();
          ResetSpriteData();
          FreeAllSpritePalettes();
          gReservedSpritePaletteCount = 9;
          LoadCompressedSpriteSheet(sSpriteSheet_EmeraldVersion[0]);
          LoadCompressedSpriteSheet(sSpriteSheet_PressStart[0]);
          LoadCompressedSpriteSheet(sPokemonLogoShineSpriteSheet[0]);
          LoadPalette(gTitleScreenEmeraldVersionPal, OBJ_PLTT_ID(0), PLTT_SIZE_4BPP);
          LoadSpritePalette(sSpritePalette_PressStart[0]);
          gMain.state = 2;
          break;
      case 2:
      {
          let taskId: any = CreateTask(Task_TitleScreenPhase1, 0);

          gTasks[taskId].tCounter = 256;
          gTasks[taskId].tSkipToNext = FALSE;
          gTasks[taskId].tPointless = -16;
          gTasks[taskId].tBg2Y = -32;
          gMain.state = 3;
          break;
      }
      case 3:
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 1, 16, 0, (((RGB(31, 31, 31)) | ((1 << 15)))));
          SetVBlankCallback(VBlankCB);
          gMain.state = 4;
          break;
      case 4:
          PanFadeAndZoomScreen(DISPLAY_WIDTH / 2, DISPLAY_HEIGHT / 2, 0x100, 0);
          SetGpuReg(REG_OFFSET_BG2X_L, -29 * 256);
          SetGpuReg(REG_OFFSET_BG2X_H, -1);
          SetGpuReg(REG_OFFSET_BG2Y_L, -32 * 256);
          SetGpuReg(REG_OFFSET_BG2Y_H, -1);
          SetGpuReg(REG_OFFSET_WIN0H, 0);
          SetGpuReg(REG_OFFSET_WIN0V, 0);
          SetGpuReg(REG_OFFSET_WIN1H, 0);
          SetGpuReg(REG_OFFSET_WIN1V, 0);
          SetGpuReg(REG_OFFSET_WININ, WININ_WIN0_BG_ALL | WININ_WIN0_OBJ | WININ_WIN1_BG_ALL | WININ_WIN1_OBJ);
          SetGpuReg(REG_OFFSET_WINOUT, WINOUT_WIN01_BG_ALL | WINOUT_WIN01_OBJ | WINOUT_WINOBJ_ALL);
          SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG2 | BLDCNT_EFFECT_LIGHTEN);
          SetGpuReg(REG_OFFSET_BLDALPHA, 0);
          SetGpuReg(REG_OFFSET_BLDY, 12);
          SetGpuReg(REG_OFFSET_BG0CNT, BGCNT_PRIORITY(3) | BGCNT_CHARBASE(2) | BGCNT_SCREENBASE(26) | BGCNT_16COLOR | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG1CNT, BGCNT_PRIORITY(2) | BGCNT_CHARBASE(3) | BGCNT_SCREENBASE(27) | BGCNT_16COLOR | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG2CNT, BGCNT_PRIORITY(1) | BGCNT_CHARBASE(0) | BGCNT_SCREENBASE(9) | BGCNT_256COLOR | BGCNT_AFF256x256);
          EnableInterrupts(INTR_FLAG_VBLANK);
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_1
                                      | DISPCNT_OBJ_1D_MAP
                                      | DISPCNT_BG2_ON
                                      | DISPCNT_OBJ_ON
                                      | DISPCNT_WIN0_ON
                                      | DISPCNT_OBJWIN_ON);
          m4aSongNumStart((413));
          gMain.state = 5;
          break;
      case 5:
          if (!UpdatePaletteFade())
          {
              StartPokemonLogoShine(SHINE_MODE_SINGLE_NO_BG_COLOR);
              ScanlineEffect_InitWave(0, DISPLAY_HEIGHT, 4, 4, 0, ((REG_ADDR_BG1HOFS - REG_ADDR_BG0HOFS)), TRUE);
              SetMainCallback2(MainCB2);
          }
          break;
      }
}

/** static void MainCB2(void) */
export function MainCB2(): any {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
}

/** static void Task_TitleScreenPhase1(u8 taskId) */
export function Task_TitleScreenPhase1(taskId: any): any {
  if (JOY_NEW(((A_BUTTON | B_BUTTON | START_BUTTON | SELECT_BUTTON))) || gTasks[taskId].tSkipToNext)
      {
          gTasks[taskId].tSkipToNext = TRUE;
          gTasks[taskId].tCounter = 0;
      }

      if (gTasks[taskId].tCounter != 0)
      {
          let frameNum: any = gTasks[taskId].tCounter;
          if (frameNum == 176)
              StartPokemonLogoShine(SHINE_MODE_DOUBLE);
          else if (frameNum == 64)
              StartPokemonLogoShine(SHINE_MODE_SINGLE);

          gTasks[taskId].tCounter--;
      }
      else
      {
          let spriteId: any = null;

          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_1 | DISPCNT_OBJ_1D_MAP | DISPCNT_BG2_ON | DISPCNT_OBJ_ON);
          SetGpuReg(REG_OFFSET_WININ, 0);
          SetGpuReg(REG_OFFSET_WINOUT, 0);
          SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_OBJ | BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_ALL);
          SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 0));
          SetGpuReg(REG_OFFSET_BLDY, 0);

           
          spriteId = CreateSprite(sVersionBannerLeftSpriteTemplate, (98), (4), 0);
          gSprites[spriteId].sAlphaBlendIdx = ARRAY_COUNT(gTitleScreenAlphaBlend);
          gSprites[spriteId].sParentTaskId = taskId;

           
          spriteId = CreateSprite(sVersionBannerRightSpriteTemplate, (162), (4), 0);
          gSprites[spriteId].sParentTaskId = taskId;

          gTasks[taskId].tCounter = 144;
          gTasks[taskId].func = Task_TitleScreenPhase2;
      }
}

/** static void Task_TitleScreenPhase2(u8 taskId) */
export function Task_TitleScreenPhase2(taskId: any): any {
  let yPos: any = null;

       
      if (JOY_NEW(((A_BUTTON | B_BUTTON | START_BUTTON | SELECT_BUTTON))) || gTasks[taskId].tSkipToNext)
      {
          gTasks[taskId].tSkipToNext = TRUE;
          gTasks[taskId].tCounter = 0;
      }

      if (gTasks[taskId].tCounter != 0)
      {
          gTasks[taskId].tCounter--;
      }
      else
      {
          gTasks[taskId].tSkipToNext = TRUE;
          SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG1 | BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_BG0 | BLDCNT_TGT2_BD);
          SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(6, 15));
          SetGpuReg(REG_OFFSET_BLDY, 0);
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_1
                                      | DISPCNT_OBJ_1D_MAP
                                      | DISPCNT_BG0_ON
                                      | DISPCNT_BG1_ON
                                      | DISPCNT_BG2_ON
                                      | DISPCNT_OBJ_ON);
          CreatePressStartBanner((128), 108);
          CreateCopyrightBanner((128), 148);
          gTasks[taskId].tBg1Y = 0;
          gTasks[taskId].func = Task_TitleScreenPhase3;
      }

      if (!(gTasks[taskId].tCounter & 3) && gTasks[taskId].tPointless != 0)
          gTasks[taskId].tPointless++;
      if (!(gTasks[taskId].tCounter & 1) && gTasks[taskId].tBg2Y != 0)
          gTasks[taskId].tBg2Y++;

       
      yPos = gTasks[taskId].tBg2Y * 256;
      SetGpuReg(REG_OFFSET_BG2Y_L, yPos);
      SetGpuReg(REG_OFFSET_BG2Y_H, yPos / 0x10000);

      gTasks[taskId].data[5] = 15;  
      gTasks[taskId].data[6] = 6;
}

/** static void Task_TitleScreenPhase3(u8 taskId) */
export function Task_TitleScreenPhase3(taskId: any): any {
  if ((JOY_NEW(A_BUTTON)) || (JOY_NEW(START_BUTTON)))
      {
          FadeOutBGM(4);
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 0, 16, (((RGB(31, 31, 31)) | ((1 << 15)))));
          SetMainCallback2(CB2_GoToMainMenu);
      }
      else if (JOY_HELD(((B_BUTTON | SELECT_BUTTON | DPAD_UP))) == ((B_BUTTON | SELECT_BUTTON | DPAD_UP)))
      {
          SetMainCallback2(CB2_GoToClearSaveDataScreen);
      }
      else if (JOY_HELD(((B_BUTTON | SELECT_BUTTON | DPAD_LEFT))) == ((B_BUTTON | SELECT_BUTTON | DPAD_LEFT))
        && CanResetRTC() == TRUE)
      {
          FadeOutBGM(4);
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 0, 16, (RGB(0, 0, 0)));
          SetMainCallback2(CB2_GoToResetRtcScreen);
      }
      else if (JOY_HELD(((B_BUTTON | SELECT_BUTTON))) == ((B_BUTTON | SELECT_BUTTON)))
      {
          FadeOutBGM(4);
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 0, 16, (RGB(0, 0, 0)));
          SetMainCallback2(CB2_GoToBerryFixScreen);
      }
      else
      {
          SetGpuReg(REG_OFFSET_BG2Y_L, 0);
          SetGpuReg(REG_OFFSET_BG2Y_H, 0);
          if (++gTasks[taskId].tCounter & 1)
          {
              gTasks[taskId].tBg1Y++;
              gBattle_BG1_Y = gTasks[taskId].tBg1Y / 2;
              gBattle_BG1_X = 0;
          }
          UpdateLegendaryMarkingColor(gTasks[taskId].tCounter);
          if ((gMPlayInfo_BGM.status & 0xFFFF) == 0)
          {
              BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 0, 16, (((RGB(31, 31, 31)) | ((1 << 15)))));
              SetMainCallback2(CB2_GoToCopyrightScreen);
          }
      }
}

/** static void CB2_GoToMainMenu(void) */
export function CB2_GoToMainMenu(): any {
  if (!UpdatePaletteFade())
          SetMainCallback2(CB2_InitMainMenu);
}

/** static void CB2_GoToCopyrightScreen(void) */
export function CB2_GoToCopyrightScreen(): any {
  if (!UpdatePaletteFade())
          SetMainCallback2(CB2_InitCopyrightScreenAfterTitleScreen);
}

/** static void CB2_GoToClearSaveDataScreen(void) */
export function CB2_GoToClearSaveDataScreen(): any {
  if (!UpdatePaletteFade())
          SetMainCallback2(CB2_InitClearSaveDataScreen);
}

/** static void CB2_GoToResetRtcScreen(void) */
export function CB2_GoToResetRtcScreen(): any {
  if (!UpdatePaletteFade())
          SetMainCallback2(CB2_InitResetRtcScreen);
}

/** static void CB2_GoToBerryFixScreen(void) */
export function CB2_GoToBerryFixScreen(): any {
  if (!UpdatePaletteFade())
      {
          m4aMPlayAllStop();
          SetMainCallback2(CB2_InitBerryFixProgram);
      }
}

/** static void UpdateLegendaryMarkingColor(u8 frameNum) */
export function UpdateLegendaryMarkingColor(frameNum: any): any {
  if ((frameNum % 4) == 0)  
      {
          let intensity: any = Cos(frameNum, Q_8_8(0.5)) + Q_8_8(0.5);
          let r: any = 31 - Q_8_8_TO_INT(intensity * 31);
          let g: any = 31 - Q_8_8_TO_INT(intensity * 22);
          let b: any = 12;

          let color: any = RGB(r, g, b);
          LoadPalette(color, BG_PLTT_ID(14) + 15, 0);
     }
}

// ─── callsTo manifest (= 51 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AnimateSprites","BGCNT_CHARBASE","BGCNT_PRIORITY","BGCNT_SCREENBASE","BG_CHAR_ADDR","BG_PLTT_ID","BG_SCREEN_ADDR","BLDALPHA_BLEND","BeginNormalPaletteFade","BuildOamBuffer","CanResetRTC","Cos","CreateCopyrightBanner","CreatePressStartBanner","CreateSprite","CreateTask","DestroySprite","DmaFill16","DmaFill32","EnableInterrupts","FadeOutBGM","FreeAllSpritePalettes","JOY_HELD","JOY_NEW","LZ77UnCompVram","LoadCompressedSpriteSheet","LoadOam","LoadPalette","LoadSpritePalette","OBJ_PLTT_ID","PanFadeAndZoomScreen","ProcessSpriteCopyRequests","Q_8_8","Q_8_8_TO_INT","ResetPaletteFade","ResetSpriteData","ResetTasks","RunTasks","ScanlineEffect_InitHBlankDmaTransfer","ScanlineEffect_InitWave","ScanlineEffect_Stop","SetGpuReg","SetMainCallback2","SetVBlankCallback","StartPokemonLogoShine","StartSpriteAnim","TransferPlttBuffer","UpdateLegendaryMarkingColor","UpdatePaletteFade","_RGB"];
