// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_anim_ice.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 32 functions extracted from src/battle_anim_ice.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, AnimFastTranslateLinear, AnimLoadCompressedBgTilemapHandleContest, AnimTranslateLinear,
  BG_PLTT_ID, BLDALPHA_BLEND, ClearBattleAnimBg, ConvertPosDataToTranslateLinearData,
  Cos, CreateSprite, DestroyAnimSprite, DestroyAnimVisualTask,
  DestroySprite, DestroySpriteAndMatrix, FreeOamMatrix, GET_BATTLER_SIDE2,
  GenerateHailParticle, GetAnimBattlerSpriteId, GetBattleAnimBg1Data, GetBattlerAtPosition,
  GetBattlerSide, GetBattlerSpriteBGPriority, GetBattlerSpriteCoord, GetBattlerSpriteCoordAttr,
  InitAnimArcTranslation, InitAnimFastLinearTranslationWithSpeed, InitAnimLinearTranslation, InitAnimLinearTranslationWithSpeed,
  InitSpritePosToAnimAttacker, InitSpritePosToAnimTarget, IsBattlerSpriteVisible, IsContest,
  IsDoubleBattle, LoadBgTiles, LoadPalette, Random2,
  SetAnimBgAttribute, SetAverageBattlerPositions, SetGpuReg, Sin,
  StartSpriteAffineAnim, StartSpriteAnim, StoreSpriteCallbackInData6, TranslateAnimHorizontalArc,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static void AnimUnusedIceCrystalThrow(struct Sprite *sprite) */
export function AnimUnusedIceCrystalThrow(sprite: any): any {
  let targetX, targetY, attackerX, attackerY;

      sprite.oam.tileNum += 7;
      targetX = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
      targetY = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET);
      attackerX = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2);
      attackerY = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_Y_PIC_OFFSET);
      sprite.data[0] = gBattleAnimArgs[4];
      sprite.data[1] = gBattleAnimArgs[0] + attackerX;
      sprite.data[2] = gBattleAnimArgs[2] + targetX;
      sprite.data[3] = gBattleAnimArgs[1] + attackerY;
      sprite.data[4] = gBattleAnimArgs[3] + targetY;
      ConvertPosDataToTranslateLinearData(sprite);

      for (;(targetX >= -32 && targetX <= DISPLAY_WIDTH + 32) && (targetY >= -32 && targetY <= DISPLAY_HEIGHT + 32);
             targetX += sprite.data[1], targetY += sprite.data[2])
          ;

      sprite.data[1] = -sprite.data[1];
      sprite.data[2] = -sprite.data[2];
      for (;(attackerX >= -32 && attackerX <= DISPLAY_WIDTH + 32) && (attackerY >= -32 && attackerY <= DISPLAY_HEIGHT + 32);
             attackerX += sprite.data[1], attackerY += sprite.data[2])
          ;

      sprite.x = attackerX;
      sprite.y = attackerY;
      sprite.data[0] = gBattleAnimArgs[4];
      sprite.data[1] = attackerX;
      sprite.data[2] = targetX;
      sprite.data[3] = attackerY;
      sprite.data[4] = targetY;
      ConvertPosDataToTranslateLinearData(sprite);
      sprite.data[3] = gBattleAnimArgs[5];
      sprite.data[4] = gBattleAnimArgs[6];
      sprite.callback = AnimUnusedIceCrystalThrow_Step;
}

/** static void AnimUnusedIceCrystalThrow_Step(struct Sprite *sprite) */
export function AnimUnusedIceCrystalThrow_Step(sprite: any): any {
  if (sprite.data[0] != 0)
      {
          sprite.data[5] += sprite.data[1];
          sprite.data[6] += sprite.data[2];
          sprite.x2 = sprite.data[5];
          sprite.y2 = sprite.data[6];
          sprite.x2 += Sin(sprite.data[7], sprite.data[3]);
          sprite.y2 += Sin(sprite.data[7], sprite.data[3]);
          sprite.data[7] = (sprite.data[7] + sprite.data[4]) & 0xFF;
          sprite.data[0]--;
      }
      else
      {
          DestroyAnimSprite(sprite);
      }
}

/** static void AnimIcePunchSwirlingParticle(struct Sprite *sprite) */
export function AnimIcePunchSwirlingParticle(sprite: any): any {
  sprite.data[0] = gBattleAnimArgs[0];
      sprite.data[1] = 60;
      sprite.data[2] = 9;
      sprite.data[3] = 30;
      sprite.data[4] = -512;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
      sprite.callback = TranslateSpriteInGrowingCircle;
      sprite.callback(sprite);
}

/** static void AnimIceBeamParticle(struct Sprite *sprite) */
export function AnimIceBeamParticle(sprite: any): any {
  InitSpritePosToAnimAttacker(sprite, TRUE);
      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);

      if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
          sprite.data[2] -= gBattleAnimArgs[2];
      else
          sprite.data[2] += gBattleAnimArgs[2];

      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + gBattleAnimArgs[3];
      sprite.data[0] = gBattleAnimArgs[4];
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
      sprite.callback = StartAnimLinearTranslation;
}

/** static void AnimIceEffectParticle(struct Sprite *sprite) */
export function AnimIceEffectParticle(sprite: any): any {
  if (gBattleAnimArgs[2] == 0)
      {
          InitSpritePosToAnimTarget(sprite, TRUE);
      }
      else
      {
          SetAverageBattlerPositions(gBattleAnimTarget, TRUE,sprite.x,sprite.y);
          if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
              gBattleAnimArgs[0] = -gBattleAnimArgs[0];

          sprite.x += gBattleAnimArgs[0];
          sprite.y += gBattleAnimArgs[1];
      }

      StoreSpriteCallbackInData6(sprite, AnimFlickerIceEffectParticle);
      sprite.callback = RunStoredCallbackWhenAffineAnimEnds;
}

/** static void AnimFlickerIceEffectParticle(struct Sprite *sprite) */
export function AnimFlickerIceEffectParticle(sprite: any): any {
  sprite.invisible ^= 1;
      sprite.data[0] += 1;
      if (sprite.data[0] == 20)
          DestroySpriteAndMatrix(sprite);
}

/** static void AnimSwirlingSnowball(struct Sprite *sprite) */
export function AnimSwirlingSnowball(sprite: any): any {
  let i: any = null;
      const tempDataHolder: any[] = [];

      InitSpritePosToAnimAttacker(sprite, TRUE);

      sprite.data[0] = gBattleAnimArgs[4];
      sprite.data[1] = sprite.x;
      sprite.data[3] = sprite.y;

      if (!gBattleAnimArgs[5])
      {
          sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
          sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + gBattleAnimArgs[3];
      }
      else
      {
          SetAverageBattlerPositions(gBattleAnimTarget, TRUE,sprite.data[2],sprite.data[4]);
      }

      if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
          sprite.data[2] -= gBattleAnimArgs[2];
      else
          sprite.data[2] += gBattleAnimArgs[2];

      for (i = 0; i < 8; i++)
          tempDataHolder[i] = sprite.data[i];

      InitAnimFastLinearTranslationWithSpeed(sprite);
      sprite.data[1] ^= 1;
      sprite.data[2] ^= 1;

      while (1)
      {
          sprite.data[0] = 1;
          AnimFastTranslateLinear(sprite);

          if (sprite.x + sprite.x2 > DISPLAY_WIDTH + 16
           || sprite.x + sprite.x2 < -16
           || sprite.y + sprite.y2 > DISPLAY_HEIGHT
           || sprite.y + sprite.y2 < -16)
              break;
      }

      sprite.x += sprite.x2;
      sprite.y += sprite.y2;
      sprite.y2 = 0;
      sprite.x2 = 0;

      for (i = 0; i < 8; i++)
          sprite.data[i] = tempDataHolder[i];

      sprite.callback = InitAnimFastLinearTranslationWithSpeedAndPos;
      StoreSpriteCallbackInData6(sprite, AnimSwirlingSnowball_Step1);
}

/** static void AnimSwirlingSnowball_Step1(struct Sprite *sprite) */
export function AnimSwirlingSnowball_Step1(sprite: any): any {
  let tempVar: any = null;

      sprite.x += sprite.x2;
      sprite.y += sprite.y2;
      sprite.y2 = 0;
      sprite.x2 = 0;
      sprite.data[0] = 128;

      tempVar = GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER ? 20 : -20;

      sprite.data[3] = Sin(sprite.data[0], tempVar);
      sprite.data[4] = Cos(sprite.data[0], 0xF);
      sprite.data[5] = 0;
      sprite.callback = AnimSwirlingSnowball_Step2;
      sprite.callback(sprite);
}

/** static void AnimSwirlingSnowball_Step2(struct Sprite *sprite) */
export function AnimSwirlingSnowball_Step2(sprite: any): any {
  let tempVar: any = null;
      tempVar = GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER ? 20 : -20;

      if (sprite.data[5] <= 31)
      {
          sprite.x2 = Sin(sprite.data[0], tempVar) - sprite.data[3];
          sprite.y2 = Cos(sprite.data[0], 15)      - sprite.data[4];
          sprite.data[0] = (sprite.data[0] + 16) & 0xFF;
          sprite.data[5] += 1;
      }
      else
      {
          sprite.x += sprite.x2;
          sprite.y += sprite.y2;
          sprite.y2 = 0;
          sprite.x2 = 0;
          sprite.data[4] = 0;
          sprite.data[3] = 0;
          sprite.callback = AnimSwirlingSnowball_End;
      }
}

/** static void AnimSwirlingSnowball_End(struct Sprite *sprite) */
export function AnimSwirlingSnowball_End(sprite: any): any {
  sprite.data[0] = 1;
      AnimFastTranslateLinear(sprite);

      if (sprite.x + sprite.x2 > 256
       || sprite.x + sprite.x2 < -16
       || sprite.y + sprite.y2 > 256
       || sprite.y + sprite.y2 < -16)
          DestroyAnimSprite(sprite);
}

/** static void AnimMoveParticleBeyondTarget(struct Sprite *sprite) */
export function AnimMoveParticleBeyondTarget(sprite: any): any {
  let i: any = null;
      const tempDataHolder: any[] = [];

      InitSpritePosToAnimAttacker(sprite, TRUE);

      sprite.data[0] = gBattleAnimArgs[4];
      sprite.data[1] = sprite.x;
      sprite.data[3] = sprite.y;

      if (!gBattleAnimArgs[7])
      {
          sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
          sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET);
      }
      else
      {
          SetAverageBattlerPositions(gBattleAnimTarget, TRUE,sprite.data[2],sprite.data[4]);
      }

      if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
          sprite.data[2] -= gBattleAnimArgs[2];
      else
          sprite.data[2] += gBattleAnimArgs[2];

      sprite.data[4] += gBattleAnimArgs[3];
      InitAnimFastLinearTranslationWithSpeed(sprite);
      for (i = 0; i < 8; i++)
          tempDataHolder[i] = sprite.data[i];

      sprite.data[1] ^= 1;
      sprite.data[2] ^= 1;

      while (1)
      {
          sprite.data[0] = 1;
          AnimFastTranslateLinear(sprite);
          if (sprite.x + sprite.x2 > DISPLAY_WIDTH + 16
           || sprite.x + sprite.x2 < -16
           || sprite.y + sprite.y2 > DISPLAY_HEIGHT
           || sprite.y + sprite.y2 < -16)
              break;
      }

      sprite.x += sprite.x2;
      sprite.y += sprite.y2;
      sprite.y2 = 0;
      sprite.x2 = 0;

      for (i = 0; i < 8; i++)
          sprite.data[i] = tempDataHolder[i];

      sprite.data[5] = gBattleAnimArgs[5];
      sprite.data[6] = gBattleAnimArgs[6];
      sprite.callback = AnimWiggleParticleTowardsTarget;
}

/** static void AnimWiggleParticleTowardsTarget(struct Sprite *sprite) */
export function AnimWiggleParticleTowardsTarget(sprite: any): any {
  AnimFastTranslateLinear(sprite);
      if (sprite.data[0] == 0)
          sprite.data[0] = 1;

      sprite.y2 += Sin(sprite.data[7], sprite.data[5]);
      sprite.data[7] = (sprite.data[7] + sprite.data[6]) & 0xFF;
      if (sprite.data[0] == 1)
      {
          if (sprite.x + sprite.x2 > DISPLAY_WIDTH + 16
           || sprite.x + sprite.x2 < -16
           || sprite.y + sprite.y2 > DISPLAY_HEIGHT
           || sprite.y + sprite.y2 < -16)
              DestroyAnimSprite(sprite);
      }
}

/** static void AnimWaveFromCenterOfTarget(struct Sprite *sprite) */
export function AnimWaveFromCenterOfTarget(sprite: any): any {
  if (sprite.data[0] == 0)
      {
          if (gBattleAnimArgs[2] == 0)
          {
              InitSpritePosToAnimTarget(sprite, FALSE);
          }
          else
          {
              SetAverageBattlerPositions(gBattleAnimTarget, FALSE,sprite.x,sprite.y);

              if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
                  gBattleAnimArgs[0] = -gBattleAnimArgs[0];

              sprite.x += gBattleAnimArgs[0];
              sprite.y += gBattleAnimArgs[1];
          }

          sprite.data[0]++;
      }
      else
      {
          if (sprite.animEnded)
              DestroyAnimSprite(sprite);
      }
}

/** static void InitSwirlingFogAnim(struct Sprite *sprite) */
export function InitSwirlingFogAnim(sprite: any): any {
  let tempVar: any = null;
      let battler: any = null;

      if (gBattleAnimArgs[4] == 0)
      {
          if (gBattleAnimArgs[5] == 0)
          {
              InitSpritePosToAnimAttacker(sprite, FALSE);
          }
          else
          {
              SetAverageBattlerPositions(gBattleAnimAttacker, FALSE,sprite.x,sprite.y);
              if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
                  sprite.x -= gBattleAnimArgs[0];
              else
                  sprite.x += gBattleAnimArgs[0];

              sprite.y += gBattleAnimArgs[1];
          }

          battler = gBattleAnimAttacker;
      }
      else
      {
          if (gBattleAnimArgs[5] == 0)
          {
              InitSpritePosToAnimTarget(sprite, FALSE);
          }
          else
          {
              SetAverageBattlerPositions(gBattleAnimTarget, FALSE,sprite.x,sprite.y);
              if (GetBattlerSide(gBattleAnimTarget) != B_SIDE_PLAYER)
                  sprite.x -= gBattleAnimArgs[0];
              else
                  sprite.x += gBattleAnimArgs[0];

              sprite.y += gBattleAnimArgs[1];
          }

          battler = gBattleAnimTarget;
      }

      sprite.data[7] = battler;
      if (gBattleAnimArgs[5] == 0 || !IsDoubleBattle())
          tempVar = _0x20;
      else
          tempVar = _0x40;

      sprite.data[6] = tempVar;
      if (GetBattlerSide(gBattleAnimTarget) == B_SIDE_PLAYER)
          sprite.y += 8;

      sprite.data[0] = gBattleAnimArgs[3];
      sprite.data[1] = sprite.x;
      sprite.data[2] = sprite.x;
      sprite.data[3] = sprite.y;
      sprite.data[4] = sprite.y + gBattleAnimArgs[2];

      InitAnimLinearTranslation(sprite);

      sprite.data[5] = 64;
      sprite.callback = AnimSwirlingFogAnim;
      sprite.callback(sprite);
}

/** static void AnimSwirlingFogAnim(struct Sprite *sprite) */
export function AnimSwirlingFogAnim(sprite: any): any {
  if (!AnimTranslateLinear(sprite))
      {
          sprite.x2 += Sin(sprite.data[5], sprite.data[6]);
          sprite.y2 += Cos(sprite.data[5], -6);

          if ((sprite.data[5] - 64) <= 0x7F)
              sprite.oam.priority = GetBattlerSpriteBGPriority(sprite.data[7]);
          else
              sprite.oam.priority = GetBattlerSpriteBGPriority(sprite.data[7]) + 1;

          sprite.data[5] = (sprite.data[5] + 3) & 0xFF;
      }
      else
      {
          DestroyAnimSprite(sprite);
      }
}

/** void AnimTask_HazeScrollingFog(u8 taskId) */
export function AnimTask_HazeScrollingFog(taskId: any): any {
  let animBg: any = null;

      SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG1 | BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_ALL);
      SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(0, 16));
      SetAnimBgAttribute(1, BG_ANIM_PRIORITY, 1);
      SetAnimBgAttribute(1, BG_ANIM_SCREEN_SIZE, 0);

      if (!IsContest())
          SetAnimBgAttribute(1, BG_ANIM_CHAR_BASE_BLOCK, 1);

      gBattle_BG1_X = 0;
      gBattle_BG1_Y = 0;
      SetGpuReg(REG_OFFSET_BG1HOFS, gBattle_BG1_X);
      SetGpuReg(REG_OFFSET_BG1VOFS, gBattle_BG1_Y);

      GetBattleAnimBg1Data(animBg);
      LoadBgTiles(animBg.bgId, gWeatherFogHorizontalTiles, _0x800, animBg.tilesOffset);
      AnimLoadCompressedBgTilemapHandleContest(animBg, gBattleAnimFogTilemap, FALSE);
      LoadPalette(gFogPalette, BG_PLTT_ID(animBg.paletteId), PLTT_SIZE_4BPP);

      gTasks[taskId].func = AnimTask_HazeScrollingFog_Step;
}

/** static void AnimTask_HazeScrollingFog_Step(u8 taskId) */
export function AnimTask_HazeScrollingFog_Step(taskId: any): any {
  let animBg: any = null;

      gBattle_BG1_X += -1;
      gBattle_BG1_Y += 0;

      switch (gTasks[taskId].data[12])
      {
      case 0:
          if (++gTasks[taskId].data[10] == 4)
          {
              gTasks[taskId].data[10] = 0;
              gTasks[taskId].data[9]++;
              gTasks[taskId].data[11] = sHazeBlendAmounts[gTasks[taskId].data[9]];

              SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(gTasks[taskId].data[11], 16 - gTasks[taskId].data[11]));
              if (gTasks[taskId].data[11] == 9)
              {
                  gTasks[taskId].data[12]++;
                  gTasks[taskId].data[11] = 0;
              }
          }
          break;
      case 1:
          if (++gTasks[taskId].data[11] == _0x51)
          {
              gTasks[taskId].data[11] = 9;
              gTasks[taskId].data[12]++;
          }
          break;
      case 2:
          if (++gTasks[taskId].data[10] == 4)
          {
              gTasks[taskId].data[10] = 0;
              gTasks[taskId].data[11]--;

              SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(gTasks[taskId].data[11], 16 - gTasks[taskId].data[11]));
              if (gTasks[taskId].data[11] == 0)
              {
                  gTasks[taskId].data[12]++;
                  gTasks[taskId].data[11] = 0;
              }
          }
          break;
      case 3:
          GetBattleAnimBg1Data(animBg);
          ClearBattleAnimBg(1);
          ClearBattleAnimBg(2);
          gTasks[taskId].data[12]++;
           
      case 4:
          if (!IsContest())
              SetAnimBgAttribute(1, BG_ANIM_CHAR_BASE_BLOCK, 0);

          gBattle_BG1_X = 0;
          gBattle_BG1_Y = 0;
          SetGpuReg(REG_OFFSET_BLDCNT, 0);
          SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(0, 0));
          SetAnimBgAttribute(1, BG_ANIM_PRIORITY, 1);
          DestroyAnimVisualTask(taskId);
          break;
      }
}

/** static void AnimThrowMistBall(struct Sprite *sprite) */
export function AnimThrowMistBall(sprite: any): any {
  sprite.x = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2);
      sprite.y = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_Y_PIC_OFFSET);
      sprite.callback = TranslateAnimSpriteToTargetMonLocation;
}

/** void AnimTask_MistBallFog(u8 taskId) */
export function AnimTask_MistBallFog(taskId: any): any {
  let animBg: any = null;

      SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG1 | BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_ALL);
      SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(0, 16));
      SetAnimBgAttribute(1, BG_ANIM_PRIORITY, 1);
      SetAnimBgAttribute(1, BG_ANIM_SCREEN_SIZE, 0);

      if (!IsContest())
          SetAnimBgAttribute(1, BG_ANIM_CHAR_BASE_BLOCK, 1);

      gBattle_BG1_X = 0;
      gBattle_BG1_Y = 0;
      SetGpuReg(REG_OFFSET_BG1HOFS, gBattle_BG1_X);
      SetGpuReg(REG_OFFSET_BG1VOFS, gBattle_BG1_Y);

      GetBattleAnimBg1Data(animBg);
      LoadBgTiles(animBg.bgId, gWeatherFogHorizontalTiles, _0x800, animBg.tilesOffset);
      AnimLoadCompressedBgTilemapHandleContest(animBg, gBattleAnimFogTilemap, FALSE);
      LoadPalette(gFogPalette, BG_PLTT_ID(animBg.paletteId), PLTT_SIZE_4BPP);

      gTasks[taskId].data[15] = -1;
      gTasks[taskId].func = AnimTask_MistBallFog_Step;
}

/** static void AnimTask_MistBallFog_Step(u8 taskId) */
export function AnimTask_MistBallFog_Step(taskId: any): any {
  let animBg: any = null;

      gBattle_BG1_X += gTasks[taskId].data[15];
      gBattle_BG1_Y += 0;

      switch (gTasks[taskId].data[12])
      {
      case 0:
          gTasks[taskId].data[9] += 1;
          gTasks[taskId].data[11] = sMistBlendAmounts[gTasks[taskId].data[9]];
          SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(gTasks[taskId].data[11], 17 - gTasks[taskId].data[11]));
          if (gTasks[taskId].data[11] == 5)
          {
              gTasks[taskId].data[12]++;
              gTasks[taskId].data[11] = 0;
          }
          break;
      case 1:
          if (++gTasks[taskId].data[11] == _0x51)
          {
              gTasks[taskId].data[11] = 5;
              gTasks[taskId].data[12]++;
          }
          break;
      case 2:
          if (++gTasks[taskId].data[10] == 4)
          {
              gTasks[taskId].data[10] = 0;
              gTasks[taskId].data[11] -= 1;
              SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(gTasks[taskId].data[11], 16 - gTasks[taskId].data[11]));
              if (gTasks[taskId].data[11] == 0)
              {
                  gTasks[taskId].data[12]++;
                  gTasks[taskId].data[11] = 0;
              }
          }
          break;
      case 3:
          GetBattleAnimBg1Data(animBg);
          ClearBattleAnimBg(1);
          ClearBattleAnimBg(2);

          gTasks[taskId].data[12]++;

           
      case 4:
          if (!IsContest())
              SetAnimBgAttribute(1, BG_ANIM_CHAR_BASE_BLOCK, 0);

          gBattle_BG1_X = 0;
          gBattle_BG1_Y = 0;
          SetGpuReg(REG_OFFSET_BLDCNT, 0);
          SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(0, 0));
          SetAnimBgAttribute(1, BG_ANIM_PRIORITY, 1);
          DestroyAnimVisualTask(taskId);
          break;
      }
}

/** static void InitPoisonGasCloudAnim(struct Sprite *sprite) */
export function InitPoisonGasCloudAnim(sprite: any): any {
  sprite.data[0] = gBattleAnimArgs[0];

      if (GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2) < GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2))
          sprite.data[7] = _0x8000;

      if (GET_BATTLER_SIDE2(gBattleAnimTarget) == B_SIDE_PLAYER)
      {
          gBattleAnimArgs[1] = -gBattleAnimArgs[1];
          gBattleAnimArgs[3] = -gBattleAnimArgs[3];

          if ((sprite.data[7] & _0x8000) && GET_BATTLER_SIDE2(gBattleAnimAttacker) == B_SIDE_PLAYER)
              sprite.subpriority = gSprites[GetAnimBattlerSpriteId(ANIM_TARGET)].subpriority + 1;

          sprite.data[6] = 1;
      }

      sprite.x = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2);
      sprite.y = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_Y_PIC_OFFSET);
      if (gBattleAnimArgs[7])
      {
          sprite.data[1] = sprite.x + gBattleAnimArgs[1];
          sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2) + gBattleAnimArgs[3];
          sprite.data[3] = sprite.y + gBattleAnimArgs[2];
          sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + gBattleAnimArgs[4];
          sprite.data[7] |= GetBattlerSpriteBGPriority(gBattleAnimTarget) << 8;
      }
      else
      {
          sprite.data[1] = sprite.x + gBattleAnimArgs[1];
          sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X) + gBattleAnimArgs[3];
          sprite.data[3] = sprite.y + gBattleAnimArgs[2];
          sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y) + gBattleAnimArgs[4];
          sprite.data[7] |= GetBattlerSpriteBGPriority(gBattleAnimTarget) << 8;
      }

      if (IsContest())
      {
          sprite.data[6] = 1;
          sprite.subpriority = _0x80;
      }

      InitAnimLinearTranslation(sprite);
      sprite.callback = MovePoisonGasCloud;
}

/** static void MovePoisonGasCloud(struct Sprite *sprite) */
export function MovePoisonGasCloud(sprite: any): any {
  let value: any = null;

      switch (sprite.data[7] & 0xFF)
      {
      case 0:
          AnimTranslateLinear(sprite);
          value = gSineTable[sprite.data[5]];
          sprite.x2 += value >> 4;
          if (sprite.data[6])
              sprite.data[5] = (sprite.data[5] - 8) & 0xFF;
          else
              sprite.data[5] = (sprite.data[5] + 8) & 0xFF;

          if (sprite.data[0] <= 0)
          {
              sprite.data[0] = 80;
              sprite.x = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X);
              sprite.data[1] = sprite.x;
              sprite.data[2] = sprite.x;
              sprite.y += sprite.y2;
              sprite.data[3] = sprite.y;
              sprite.data[4] = sprite.y + 29;
              sprite.data[7]++;
              if (IsContest())
                  sprite.data[5] = 80;
              else if (GET_BATTLER_SIDE2(gBattleAnimTarget) != B_SIDE_PLAYER)
                  sprite.data[5] = 204;
              else
                  sprite.data[5] = 80;

              sprite.y2 = 0;
              value = gSineTable[sprite.data[5]];
              sprite.x2 = value >> 3;
              sprite.data[5] = (sprite.data[5] + 2) & 0xFF;
              InitAnimLinearTranslation(sprite);
          }
          break;
      case 1:
          AnimTranslateLinear(sprite);
          value = gSineTable[sprite.data[5]];
          sprite.x2 += value >> 3;
          sprite.y2 += (gSineTable[sprite.data[5] + _0x40] * -3) >> 8;
          if (!IsContest())
          {
              let var0: any = sprite.data[5] - _0x40;
              if (var0 <= 0x7F)
                  sprite.oam.priority = sprite.data[7] >> 8;
              else
                  sprite.oam.priority = (sprite.data[7] >> 8) + 1;

              sprite.data[5] = (sprite.data[5] + 4) & 0xFF;
          }
          else
          {
              let var0: any = sprite.data[5] - _0x40;
              if (var0 <= 0x7F)
                  sprite.subpriority = 128;
              else
                  sprite.subpriority = 140;

              sprite.data[5] = (sprite.data[5] - 4) & 0xFF;
          }

          if (sprite.data[0] <= 0)
          {
              sprite.data[0] = _0x300;
              sprite.data[1] = sprite.x += sprite.x2;
              sprite.data[3] = sprite.y += sprite.y2;
              sprite.data[4] = sprite.y + 4;
              if (IsContest())
                  sprite.data[2] = -16;
              else if (GET_BATTLER_SIDE2(gBattleAnimTarget) != B_SIDE_PLAYER)
                  sprite.data[2] = DISPLAY_WIDTH + 16;
              else
                  sprite.data[2] = -16;

              sprite.data[7]++;
              sprite.x2 = sprite.y2 = 0;
              InitAnimLinearTranslationWithSpeed(sprite);
          }
          break;
      case 2:
          if (AnimTranslateLinear(sprite))
          {
              if (sprite.oam.affineMode & 1)
              {
                  FreeOamMatrix(sprite.oam.matrixNum);
                  sprite.oam.affineMode = ST_OAM_AFFINE_OFF;
              }

              DestroySprite(sprite);
              gAnimVisualTaskCount--;
          }
          break;
      }
}

/** void AnimTask_Hail(u8 taskId) */
export function AnimTask_Hail(taskId: any): any {
  let task: any =gTasks[taskId];

      task.func = AnimTask_Hail2;
}

/** static void AnimTask_Hail2(u8 taskId) */
export function AnimTask_Hail2(taskId: any): any {
  let task: any =gTasks[taskId];
      switch (task.tState)
      {
      case 0:
          if (++task.tInitialDelayTimer > 2)
          {
              task.tInitialDelayTimer = 0;
              task.tHailSpawnTimer = 0;
              task.tHailAffineAnimNum = 0;
              task.tState++;
          }
          break;
      case 1:
          if (task.tHailSpawnTimer == 0)
          {
              if (GenerateHailParticle(task.tHailStructId, task.tHailAffineAnimNum, taskId, 1))
                  task.tSpriteCount++;

              if (++task.tHailAffineAnimNum == ARRAY_COUNT(sAffineAnims_HailParticle))
              {
                  if (++task.tHailStructId == ARRAY_COUNT(sHailCoordData))
                      task.tState++;
                  else
                      task.tState--;
              }
              else
              {
                  task.tHailSpawnTimer = 1;
              }

          }
          else
          {
              task.tHailSpawnTimer--;
          }
          break;
      case 2:
          if (task.tSpriteCount == 0)
              DestroyAnimVisualTask(taskId);
          break;
      }
}

/** static bool8 GenerateHailParticle(u8 hailStructId, u8 affineAnimNum, u8 taskId, u8 c) */
export function GenerateHailParticle(hailStructId: any, affineAnimNum: any, taskId: any, c: any): any {
  let id: any = null;
      let battlerX, battlerY;
      let spriteX: any = null;
      let shouldSpawnImpactEffect: any = FALSE;
      let type: any = sHailCoordData[hailStructId].type;

      if (type != HAILSTRUCTTYPE_FIXED_POSITION)
      {
          id = GetBattlerAtPosition(sHailCoordData[hailStructId].bPosition);
          if (IsBattlerSpriteVisible(id))
          {
              shouldSpawnImpactEffect = TRUE;
              battlerX = GetBattlerSpriteCoord(id, BATTLER_COORD_X_2);
              battlerY = GetBattlerSpriteCoord(id, BATTLER_COORD_Y_PIC_OFFSET);
              switch (type)
              {
              case HAILSTRUCTTYPE_NEGATIVE_POS_MOD:
                  battlerX -= GetBattlerSpriteCoordAttr(id, BATTLER_COORD_ATTR_WIDTH) / 6;
                  battlerY -= GetBattlerSpriteCoordAttr(id, BATTLER_COORD_ATTR_HEIGHT) / 6;
                  break;
              case HAILSTRUCTTYPE_POSITIVE_POS_MOD:
                  battlerX += GetBattlerSpriteCoordAttr(id, BATTLER_COORD_ATTR_WIDTH) / 6;
                  battlerY += GetBattlerSpriteCoordAttr(id, BATTLER_COORD_ATTR_HEIGHT) / 6;
                  break;
              }
          }
          else
          {
              battlerX = sHailCoordData[hailStructId].x;
              battlerY = sHailCoordData[hailStructId].y;
          }
      }
      else
      {
          battlerX = sHailCoordData[hailStructId].x;
          battlerY = sHailCoordData[hailStructId].y;
      }
      spriteX = battlerX - ((battlerY + 8) / 2);
      id = CreateSprite(gHailParticleSpriteTemplate, spriteX, -8, 18);
      if (id == MAX_SPRITES)
      {
          return FALSE;
      }
      else
      {
          StartSpriteAffineAnim(gSprites[id], affineAnimNum);
          gSprites[id].sSpawnImpactEffect = shouldSpawnImpactEffect;
          gSprites[id].sTargetX           = battlerX;
          gSprites[id].sTargetY           = battlerY;
          gSprites[id].sAffineAnimNum     = affineAnimNum;
          gSprites[id].sOwnerTaskId       = taskId;
          gSprites[id].sOwnerTaskSpriteCountField = c;
          return TRUE;
      }
}

/** static void AnimHailBegin(struct Sprite *sprite) */
export function AnimHailBegin(sprite: any): any {
  let spriteId: any = null;

      sprite.x += 4;
      sprite.y += 8;

      if (sprite.x < sprite.sTargetX && sprite.y < sprite.sTargetY)
          return;

      if (sprite.sSpawnImpactEffect == 1 && sprite.sAffineAnimNum == 0)
      {
          spriteId = CreateSprite(gIceCrystalHitLargeSpriteTemplate,
                                  sprite.sTargetX, sprite.sTargetY, sprite.subpriority);

          sprite.data[0] = spriteId;
          if (spriteId != MAX_SPRITES)
          {
               
               
               
              gSprites[sprite.data[0]].callback = AnimHailContinue;
              gSprites[sprite.data[0]].sOwnerTaskId = sprite.sOwnerTaskId;
              gSprites[sprite.data[0]].sOwnerTaskSpriteCountField = sprite.sOwnerTaskSpriteCountField;
          }

          FreeOamMatrix(sprite.oam.matrixNum);
          DestroySprite(sprite);
      }
      else
      {
          gTasks[sprite.sOwnerTaskId].data[sprite.sOwnerTaskSpriteCountField]--;
          FreeOamMatrix(sprite.oam.matrixNum);
          DestroySprite(sprite);
      }
}

/** static void AnimHailContinue(struct Sprite *sprite) */
export function AnimHailContinue(sprite: any): any {
  if (++sprite.sTimer == 20)
      {
          gTasks[sprite.sOwnerTaskId].data[sprite.sOwnerTaskSpriteCountField]--;
          FreeOamMatrix(sprite.oam.matrixNum);
          DestroySprite(sprite);
      }
}

/** static void InitIceBallAnim(struct Sprite *sprite) */
export function InitIceBallAnim(sprite: any): any {
  let animNum: any = gAnimDisableStructPtr.rolloutTimerStartValue - gAnimDisableStructPtr.rolloutTimer - 1;

      if (animNum > 4)
          animNum = 4;

      StartSpriteAffineAnim(sprite, animNum);
      InitSpritePosToAnimAttacker(sprite, TRUE);

      sprite.data[0] = gBattleAnimArgs[4];

      if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
          gBattleAnimArgs[2] = -gBattleAnimArgs[2];

      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2) + gBattleAnimArgs[2];
      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + gBattleAnimArgs[3];
      sprite.data[5] = gBattleAnimArgs[5];

      InitAnimArcTranslation(sprite);

      sprite.callback = AnimThrowIceBall;
}

/** static void AnimThrowIceBall(struct Sprite *sprite) */
export function AnimThrowIceBall(sprite: any): any {
  if (!TranslateAnimHorizontalArc(sprite))
          return;

      StartSpriteAnim(sprite, 1);
      sprite.callback = RunStoredCallbackWhenAnimEnds;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
}

/** static void InitIceBallParticle(struct Sprite *sprite) */
export function InitIceBallParticle(sprite: any): any {
  let randA, randB;

      sprite.oam.tileNum += 8;
      InitSpritePosToAnimTarget(sprite, TRUE);

      randA = (Random2() & 0xFF) + 256;
      randB = Random2() & 0x1FF;

      if (randB > 0xFF)
          randB = 256 - randB;

      sprite.data[1] = randA;
      sprite.data[2] = randB;
      sprite.callback = AnimIceBallParticle;
}

/** static void AnimIceBallParticle(struct Sprite *sprite) */
export function AnimIceBallParticle(sprite: any): any {
  sprite.data[3] += sprite.data[1];
      sprite.data[4] += sprite.data[2];

      if (sprite.data[1] & 1)
          sprite.x2 = -(sprite.data[3] >> 8);
      else
          sprite.x2 = sprite.data[3] >> 8;

      sprite.y2 = sprite.data[4] >> 8;

      if (++sprite.data[0] == 21)
          DestroyAnimSprite(sprite);
}

/** void AnimTask_GetIceBallCounter(u8 taskId) */
export function AnimTask_GetIceBallCounter(taskId: any): any {
  let arg: any = gBattleAnimArgs[0];

      gBattleAnimArgs[arg] = gAnimDisableStructPtr.rolloutTimerStartValue - gAnimDisableStructPtr.rolloutTimer - 1;
      DestroyAnimVisualTask(taskId);
}

// ─── callsTo manifest (= 44 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AnimFastTranslateLinear","AnimLoadCompressedBgTilemapHandleContest","AnimTranslateLinear","BG_PLTT_ID","BLDALPHA_BLEND","ClearBattleAnimBg","ConvertPosDataToTranslateLinearData","Cos","CreateSprite","DestroyAnimSprite","DestroyAnimVisualTask","DestroySprite","DestroySpriteAndMatrix","FreeOamMatrix","GET_BATTLER_SIDE2","GenerateHailParticle","GetAnimBattlerSpriteId","GetBattleAnimBg1Data","GetBattlerAtPosition","GetBattlerSide","GetBattlerSpriteBGPriority","GetBattlerSpriteCoord","GetBattlerSpriteCoordAttr","InitAnimArcTranslation","InitAnimFastLinearTranslationWithSpeed","InitAnimLinearTranslation","InitAnimLinearTranslationWithSpeed","InitSpritePosToAnimAttacker","InitSpritePosToAnimTarget","IsBattlerSpriteVisible","IsContest","IsDoubleBattle","LoadBgTiles","LoadPalette","Random2","SetAnimBgAttribute","SetAverageBattlerPositions","SetGpuReg","Sin","StartSpriteAffineAnim","StartSpriteAnim","StoreSpriteCallbackInData6","TranslateAnimHorizontalArc"];
