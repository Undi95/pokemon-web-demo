// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/agb_flash_mx.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 5 functions extracted from src/agb_flash_mx.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** u16 EraseFlashChip_MX(void) */
export function EraseFlashChip_MX(): any {
  let result: any = null;
      let readFlash1Buffer: any = [];

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | gFlash.wait[0];

      FLASH_WRITE(0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, 0x55);
      FLASH_WRITE(0x5555, 0x80);
      FLASH_WRITE(0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, 0x55);
      FLASH_WRITE(0x5555, 0x10);

      SetReadFlash1(readFlash1Buffer);

      result = WaitForFlashWrite(3, FLASH_BASE, 0xFF);

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | WAITCNT_SRAM_8;

      return result;
}

/** u16 EraseFlashSector_MX(u16 sectorNum) */
export function EraseFlashSector_MX(sectorNum: any): any {
  let numTries: any = null;
      let result: any = null;
      let addr: any = null;
      let readFlash1Buffer: any = [];

      if (sectorNum >= gFlash.sector.count)
          return 0x80FF;

      SwitchFlashBank(sectorNum / SECTORS_PER_BANK);
      sectorNum %= SECTORS_PER_BANK;

      numTries = 0;

  // LABEL: try_erase:
      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | gFlash.wait[0];

      addr = FLASH_BASE + (sectorNum << gFlash.sector.shift);

      FLASH_WRITE(0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, 0x55);
      FLASH_WRITE(0x5555, 0x80);
      FLASH_WRITE(0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, 0x55);
      addr = 0x30;

      SetReadFlash1(readFlash1Buffer);

      result = WaitForFlashWrite(2, addr, 0xFF);

      if (!(result & 0xA000) || numTries > 3)
  // goto done;

      numTries++;
  // goto try_erase;

  // LABEL: done:
      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | WAITCNT_SRAM_8;

      return result;
}

/** u16 ProgramFlashByte_MX(u16 sectorNum, u32 offset, u8 data) */
export function ProgramFlashByte_MX(sectorNum: any, offset: any, data: any): any {
  let addr: any = null;
      let readFlash1Buffer: any = [];

      if (offset >= gFlash.sector.size)
          return 0x8000;

      SwitchFlashBank(sectorNum / SECTORS_PER_BANK);
      sectorNum %= SECTORS_PER_BANK;

      addr = FLASH_BASE + (sectorNum << gFlash.sector.shift) + offset;

      SetReadFlash1(readFlash1Buffer);

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | gFlash.wait[0];

      FLASH_WRITE(0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, 0x55);
      FLASH_WRITE(0x5555, 0xA0);
      addr = data;

      return WaitForFlashWrite(1, addr, data);
}

/** static u16 ProgramByte(u8 *src, u8 *dest) */
export function ProgramByte(src: any, dest: any): any {
  FLASH_WRITE(0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, 0x55);
      FLASH_WRITE(0x5555, 0xA0);
      dest = src;

      return WaitForFlashWrite(1, dest, src);
}

/** u16 ProgramFlashSector_MX(u16 sectorNum, u8 *src) */
export function ProgramFlashSector_MX(sectorNum: any, src: any): any {
  let result: any = null;
      let dest: any = null;
      let readFlash1Buffer: any = [];

      if (sectorNum >= gFlash.sector.count)
          return 0x80FF;

      result = EraseFlashSector_MX(sectorNum);

      if (result != 0)
          return result;

      SwitchFlashBank(sectorNum / SECTORS_PER_BANK);
      sectorNum %= SECTORS_PER_BANK;

      SetReadFlash1(readFlash1Buffer);

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | gFlash.wait[0];

      gFlashNumRemainingBytes = gFlash.sector.size;
      dest = FLASH_BASE + (sectorNum << gFlash.sector.shift);

      while (gFlashNumRemainingBytes > 0)
      {
          result = ProgramByte(src, dest);

          if (result != 0)
              break;

          gFlashNumRemainingBytes--;
          src++;
          dest++;
      }

      return result;
}

// ─── callsTo manifest (= 6 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["EraseFlashSector_MX","FLASH_WRITE","ProgramByte","SetReadFlash1","SwitchFlashBank","WaitForFlashWrite"];
