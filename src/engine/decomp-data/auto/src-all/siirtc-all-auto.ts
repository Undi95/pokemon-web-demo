// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/siirtc.c
// Generated : 2026-05-08
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 15 functions extracted from src/siirtc.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** void SiiRtcUnprotect(void) */
export function SiiRtcUnprotect(): any {
  EnableGpioPortRead();
      sLocked = FALSE;
}

/** void SiiRtcProtect(void) */
export function SiiRtcProtect(): any {
  DisableGpioPortRead();
      sLocked = TRUE;
}

/** u8 SiiRtcProbe(void) */
export function SiiRtcProbe(): any {
  let errorCode: any = null;
      let rtc: any = null;

      if (!SiiRtcGetStatus(rtc))
          return 0;

      errorCode = 0;
      if (!(rtc.status & SIIRTCINFO_24HOUR) || (rtc.status & SIIRTCINFO_POWER))
      if ((rtc.status & (SIIRTCINFO_POWER | SIIRTCINFO_24HOUR)) == SIIRTCINFO_POWER
       || (rtc.status & (SIIRTCINFO_POWER | SIIRTCINFO_24HOUR)) == 0)
      {
           

           
           
           
           

          if (!SiiRtcReset())
              return 0;

          errorCode++;
      }

      SiiRtcGetTime(rtc);

      if (rtc.second & TEST_MODE)
      {
           

          if (!SiiRtcReset())
              return (errorCode << 4) & 0xF0;

          errorCode++;
      }

      return (errorCode << 4) | 1;
}

/** bool8 SiiRtcReset(void) */
export function SiiRtcReset(): any {
  let result: any = null;
      let rtc: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI | CS_HI;

      GPIO_PORT_DIRECTION = DIR_ALL_OUT;

      WriteCommand(CMD_RESET | WR);

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI;

      sLocked = FALSE;

      rtc.status = SIIRTCINFO_24HOUR;

      result = SiiRtcSetStatus(rtc);

      return result;
}

/** bool8 SiiRtcGetStatus(struct SiiRtcInfo *rtc) */
export function SiiRtcGetStatus(...args: any[]): any {
  let statusData: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI | CS_HI;

      GPIO_PORT_DIRECTION = DIR_ALL_OUT;

      WriteCommand(CMD_STATUS | RD);

      GPIO_PORT_DIRECTION = DIR_0_OUT | DIR_1_IN | DIR_2_OUT;

      statusData = ReadData();

      rtc.status = (statusData & (STATUS_POWER | STATUS_24HOUR))
                  | ((statusData & STATUS_INTAE) >> 3)
                  | ((statusData & STATUS_INTME) >> 2)
                  | ((statusData & STATUS_INTFE) >> 1);

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI;

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcSetStatus(struct SiiRtcInfo *rtc) */
export function SiiRtcSetStatus(...args: any[]): any {
  let statusData: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI | CS_HI;

      statusData = STATUS_24HOUR
                 | ((rtc.status & SIIRTCINFO_INTAE) << 3)
                 | ((rtc.status & SIIRTCINFO_INTME) << 2)
                 | ((rtc.status & SIIRTCINFO_INTFE) << 1);

      GPIO_PORT_DIRECTION = DIR_ALL_OUT;

      WriteCommand(CMD_STATUS | WR);

      WriteData(statusData);

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI;

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcGetDateTime(struct SiiRtcInfo *rtc) */
export function SiiRtcGetDateTime(...args: any[]): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI | CS_HI;

      GPIO_PORT_DIRECTION = DIR_ALL_OUT;

      WriteCommand(CMD_DATETIME | RD);

      GPIO_PORT_DIRECTION = DIR_0_OUT | DIR_1_IN | DIR_2_OUT;

      for (i = 0; i < DATETIME_BUF_LEN; i++)
          DATETIME_BUF(rtc, i) = ReadData();

      INFO_BUF(rtc, OFFSET_HOUR) &= 0x7F;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI;

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcSetDateTime(struct SiiRtcInfo *rtc) */
export function SiiRtcSetDateTime(...args: any[]): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI | CS_HI;

      GPIO_PORT_DIRECTION = DIR_ALL_OUT;

      WriteCommand(CMD_DATETIME | WR);

      for (i = 0; i < DATETIME_BUF_LEN; i++)
          WriteData(DATETIME_BUF(rtc, i));

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI;

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcGetTime(struct SiiRtcInfo *rtc) */
export function SiiRtcGetTime(...args: any[]): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI | CS_HI;

      GPIO_PORT_DIRECTION = DIR_ALL_OUT;

      WriteCommand(CMD_TIME | RD);

      GPIO_PORT_DIRECTION = DIR_0_OUT | DIR_1_IN | DIR_2_OUT;

      for (i = 0; i < TIME_BUF_LEN; i++)
          TIME_BUF(rtc, i) = ReadData();

      INFO_BUF(rtc, OFFSET_HOUR) &= 0x7F;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI;

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcSetTime(struct SiiRtcInfo *rtc) */
export function SiiRtcSetTime(...args: any[]): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI | CS_HI;

      GPIO_PORT_DIRECTION = DIR_ALL_OUT;

      WriteCommand(CMD_TIME | WR);

      for (i = 0; i < TIME_BUF_LEN; i++)
          WriteData(TIME_BUF(rtc, i));

      GPIO_PORT_DATA = SCK_HI;
      GPIO_PORT_DATA = SCK_HI;

      sLocked = FALSE;

      return TRUE;
}

/** static int WriteCommand(u8 value) */
export function WriteCommand(...args: any[]): any {
  let i: any = null;
      let temp: any = null;

      for (i = 0; i < 8; i++)
      {
          temp = ((value >> (7 - i)) & 1);
          GPIO_PORT_DATA = (temp << 1) | CS_HI;
          GPIO_PORT_DATA = (temp << 1) | CS_HI;
          GPIO_PORT_DATA = (temp << 1) | CS_HI;
          GPIO_PORT_DATA = (temp << 1) | SCK_HI | CS_HI;
      }
      return 0;

}

/** static int WriteData(u8 value) */
export function WriteData(...args: any[]): any {
  let i: any = null;
      let temp: any = null;

      for (i = 0; i < 8; i++)
      {
          temp = ((value >> i) & 1);
          GPIO_PORT_DATA = (temp << 1) | CS_HI;
          GPIO_PORT_DATA = (temp << 1) | CS_HI;
          GPIO_PORT_DATA = (temp << 1) | CS_HI;
          GPIO_PORT_DATA = (temp << 1) | SCK_HI | CS_HI;
      }
      return 0;

}

/** static u8 ReadData() */
export function ReadData(): any {
  let i: any = null;
      let temp: any = null;
      let value: any = null;
      value = 0;

      for (i = 0; i < 8; i++)
      {
          GPIO_PORT_DATA = CS_HI;
          GPIO_PORT_DATA = CS_HI;
          GPIO_PORT_DATA = CS_HI;
          GPIO_PORT_DATA = CS_HI;
          GPIO_PORT_DATA = CS_HI;
          GPIO_PORT_DATA = SCK_HI | CS_HI;

          temp = ((GPIO_PORT_DATA & SIO_HI) >> 1);
          value = (value >> 1) | (temp << 7);
      }

      return value;
}

/** static void EnableGpioPortRead() */
export function EnableGpioPortRead(): any {
  GPIO_PORT_READ_ENABLE = TRUE;
}

/** static void DisableGpioPortRead() */
export function DisableGpioPortRead(): any {
  GPIO_PORT_READ_ENABLE = FALSE;
}

// ─── callsTo manifest (= 12 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["DATETIME_BUF","DisableGpioPortRead","EnableGpioPortRead","INFO_BUF","ReadData","SiiRtcGetStatus","SiiRtcGetTime","SiiRtcReset","SiiRtcSetStatus","TIME_BUF","WriteCommand","WriteData"];
