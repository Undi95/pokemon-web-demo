// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/siirtc.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 15 functions extracted from src/siirtc.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let sLocked: any = null;
/** void SiiRtcUnprotect(void) */
export function SiiRtcUnprotect(): any {
  EnableGpioPortRead();
      sLocked = FALSE;
}

/** void SiiRtcProtect(void) */
export function SiiRtcProtect(): any {
  DisableGpioPortRead();
      sLocked = TRUE;
}

/** u8 SiiRtcProbe(void) */
export function SiiRtcProbe(): any {
  let errorCode: any = null;
      let rtc: any = null;

      if (!SiiRtcGetStatus(rtc))
          return 0;

      errorCode = 0;
      if (!(rtc.status & SIIRTCINFO_24HOUR) || (rtc.status & SIIRTCINFO_POWER))
      {
           

           
           
           
           

          if (!SiiRtcReset())
              return 0;

          errorCode++;
      }

      SiiRtcGetTime(rtc);

      if (rtc.second & (0x80))
      {
           

          if (!SiiRtcReset())
              return (errorCode << 4) & 0xF0;

          errorCode++;
      }

      return (errorCode << 4) | 1;
}

/** bool8 SiiRtcReset(void) */
export function SiiRtcReset(): any {
  let result: any = null;
      let rtc: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1) | (4);

      GPIO_PORT_DIRECTION = (((1) | (2) | (4)));

      WriteCommand((CMD(0)) | (0));

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1);

      sLocked = FALSE;

      rtc.status = SIIRTCINFO_24HOUR;

      result = SiiRtcSetStatus(rtc);

      return result;
}

/** bool8 SiiRtcGetStatus(struct SiiRtcInfo *rtc) */
export function SiiRtcGetStatus(rtc: any): any {
  let statusData: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1) | (4);

      GPIO_PORT_DIRECTION = (((1) | (2) | (4)));

      WriteCommand((CMD(1)) | (1));

      GPIO_PORT_DIRECTION = (1) | (0) | (4);

      statusData = ReadData();

      rtc.status = (statusData & ((0x80) | (0x40)))
                  | ((statusData & (0x20)) >> 3)
                  | ((statusData & (0x08)) >> 2)
                  | ((statusData & (0x02)) >> 1);

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1);

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcSetStatus(struct SiiRtcInfo *rtc) */
export function SiiRtcSetStatus(rtc: any): any {
  let statusData: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1) | (4);

      statusData = (0x40)
                 | ((rtc.status & SIIRTCINFO_INTAE) << 3)
                 | ((rtc.status & SIIRTCINFO_INTME) << 2)
                 | ((rtc.status & SIIRTCINFO_INTFE) << 1);

      GPIO_PORT_DIRECTION = (((1) | (2) | (4)));

      WriteCommand((CMD(1)) | (0));

      WriteData(statusData);

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1);

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcGetDateTime(struct SiiRtcInfo *rtc) */
export function SiiRtcGetDateTime(rtc: any): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1) | (4);

      GPIO_PORT_DIRECTION = (((1) | (2) | (4)));

      WriteCommand((CMD(2)) | (1));

      GPIO_PORT_DIRECTION = (1) | (0) | (4);

      for (i = 0; i < (((0) - (0) + 1)); i++)
          /* transpiler bug : DATETIME_BUF(rtc, i) = ReadData(); */ ;

      /* transpiler bug : INFO_BUF(rtc, (0)) &= 0x7F; */ ;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1);

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcSetDateTime(struct SiiRtcInfo *rtc) */
export function SiiRtcSetDateTime(rtc: any): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1) | (4);

      GPIO_PORT_DIRECTION = (((1) | (2) | (4)));

      WriteCommand((CMD(2)) | (0));

      for (i = 0; i < (((0) - (0) + 1)); i++)
          WriteData(DATETIME_BUF(rtc, i));

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1);

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcGetTime(struct SiiRtcInfo *rtc) */
export function SiiRtcGetTime(rtc: any): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1) | (4);

      GPIO_PORT_DIRECTION = (((1) | (2) | (4)));

      WriteCommand((CMD(3)) | (1));

      GPIO_PORT_DIRECTION = (1) | (0) | (4);

      for (i = 0; i < ((OFFSET_SECOND - (0) + 1)); i++)
          /* transpiler bug : TIME_BUF(rtc, i) = ReadData(); */ ;

      /* transpiler bug : INFO_BUF(rtc, (0)) &= 0x7F; */ ;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1);

      sLocked = FALSE;

      return TRUE;
}

/** bool8 SiiRtcSetTime(struct SiiRtcInfo *rtc) */
export function SiiRtcSetTime(rtc: any): any {
  let i: any = null;

      if (sLocked == TRUE)
          return FALSE;

      sLocked = TRUE;

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1) | (4);

      GPIO_PORT_DIRECTION = (((1) | (2) | (4)));

      WriteCommand((CMD(3)) | (0));

      for (i = 0; i < ((OFFSET_SECOND - (0) + 1)); i++)
          WriteData(TIME_BUF(rtc, i));

      GPIO_PORT_DATA = (1);
      GPIO_PORT_DATA = (1);

      sLocked = FALSE;

      return TRUE;
}

/** static int WriteCommand(u8 value) */
export function WriteCommand(value: any): any {
  let i: any = null;
      let temp: any = null;

      for (i = 0; i < 8; i++)
      {
          temp = ((value >> (7 - i)) & 1);
          GPIO_PORT_DATA = (temp << 1) | (4);
          GPIO_PORT_DATA = (temp << 1) | (4);
          GPIO_PORT_DATA = (temp << 1) | (4);
          GPIO_PORT_DATA = (temp << 1) | (1) | (4);
      }
      return 0;

}

/** static int WriteData(u8 value) */
export function WriteData(value: any): any {
  let i: any = null;
      let temp: any = null;

      for (i = 0; i < 8; i++)
      {
          temp = ((value >> i) & 1);
          GPIO_PORT_DATA = (temp << 1) | (4);
          GPIO_PORT_DATA = (temp << 1) | (4);
          GPIO_PORT_DATA = (temp << 1) | (4);
          GPIO_PORT_DATA = (temp << 1) | (1) | (4);
      }
      return 0;

}

/** static u8 ReadData() */
export function ReadData(): any {
  let i: any = null;
      let temp: any = null;
      let value: any = null;
      value = 0;

      for (i = 0; i < 8; i++)
      {
          GPIO_PORT_DATA = (4);
          GPIO_PORT_DATA = (4);
          GPIO_PORT_DATA = (4);
          GPIO_PORT_DATA = (4);
          GPIO_PORT_DATA = (4);
          GPIO_PORT_DATA = (1) | (4);

          temp = ((GPIO_PORT_DATA & (2)) >> 1);
          value = (value >> 1) | (temp << 7);
      }

      return value;
}

/** static void EnableGpioPortRead() */
export function EnableGpioPortRead(): any {
  GPIO_PORT_READ_ENABLE = TRUE;
}

/** static void DisableGpioPortRead() */
export function DisableGpioPortRead(): any {
  GPIO_PORT_READ_ENABLE = FALSE;
}

// ─── callsTo manifest (= 12 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["DATETIME_BUF","DisableGpioPortRead","EnableGpioPortRead","INFO_BUF","ReadData","SiiRtcGetStatus","SiiRtcGetTime","SiiRtcReset","SiiRtcSetStatus","TIME_BUF","WriteCommand","WriteData"];
