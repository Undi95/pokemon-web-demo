// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/fldeff_flash.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 20 functions extracted from src/fldeff_flash.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** bool8 SetUpFieldMove_Flash(void) */
export function SetUpFieldMove_Flash(): any {
  if (ShouldDoBrailleRegisteelEffect())
      {
          gSpecialVar_Result = GetCursorSelectionMonId();
          gFieldCallback2 = FieldCallback_PrepareFadeInFromMenu;
          gPostMenuFieldCallback = SetUpPuzzleEffectRegisteel;
          return TRUE;
      }
      else if (gMapHeader.cave == TRUE && !FlagGet(FLAG_SYS_USE_FLASH))
      {
          gFieldCallback2 = FieldCallback_PrepareFadeInFromMenu;
          gPostMenuFieldCallback = FieldCallback_Flash;
          return TRUE;
      }

      return FALSE;
}

/** static void FieldCallback_Flash(void) */
export function FieldCallback_Flash(): any {
  let taskId: any = CreateFieldMoveTask();
      gFieldEffectArguments[0] = GetCursorSelectionMonId();
      gTasks[taskId].data[8] = FldEff_UseFlash >> 16;
      gTasks[taskId].data[9] = FldEff_UseFlash;
}

/** static void FldEff_UseFlash(void) */
export function FldEff_UseFlash(): any {
  PlaySE(SE_M_REFLECT);
      FlagSet(FLAG_SYS_USE_FLASH);
      ScriptContext_SetupScript(EventScript_UseFlash);
}

/** static void CB2_ChangeMapMain(void) */
export function CB2_ChangeMapMain(): any {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
}

/** static void VBC_ChangeMapVBlank(void) */
export function VBC_ChangeMapVBlank(): any {
  LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
}

/** void CB2_DoChangeMap(void) */
export function CB2_DoChangeMap(): any {
  let ime: any = null;

      SetVBlankCallback(NULL);
      SetGpuReg(REG_OFFSET_DISPCNT, 0);
      SetGpuReg(REG_OFFSET_BG2CNT, 0);
      SetGpuReg(REG_OFFSET_BG1CNT, 0);
      SetGpuReg(REG_OFFSET_BG0CNT, 0);
      SetGpuReg(REG_OFFSET_BG2HOFS, 0);
      SetGpuReg(REG_OFFSET_BG2VOFS, 0);
      SetGpuReg(REG_OFFSET_BG1HOFS, 0);
      SetGpuReg(REG_OFFSET_BG1VOFS, 0);
      SetGpuReg(REG_OFFSET_BG0HOFS, 0);
      SetGpuReg(REG_OFFSET_BG0VOFS, 0);
      DmaFill16(3, 0, VRAM, VRAM_SIZE);
      DmaFill32(3, 0, OAM, OAM_SIZE);
      DmaFill16(3, 0, (PLTT + 2), PLTT_SIZE - 2);
      ResetPaletteFade();
      ResetTasks();
      ResetSpriteData();
      ime = REG_IME;
      REG_IME = 0;
      REG_IE |= INTR_FLAG_VBLANK;
      REG_IME = ime;
      SetVBlankCallback(VBC_ChangeMapVBlank);
      SetMainCallback2(CB2_ChangeMapMain);
      if (!TryDoMapTransition())
          SetMainCallback2(gMain.savedCallback);
}

/** static bool8 TryDoMapTransition(void) */
export function TryDoMapTransition(): any {
  let i: any = null;
      let fromType: any = GetLastUsedWarpMapType();
      let toType: any = GetCurrentMapType();

      for (i = 0; sTransitionTypes[i].fromType; i++)
      {
          if (sTransitionTypes[i].fromType == fromType && sTransitionTypes[i].toType == toType)
          {
              sTransitionTypes[i].func();
              return TRUE;
          }
      }

      return FALSE;
}

/** bool8 GetMapPairFadeToType(u8 _fromType, u8 _toType) */
export function GetMapPairFadeToType(_fromType: any, _toType: any): any {
  let i: any = null;
      let fromType: any = _fromType;
      let toType: any = _toType;

      for (i = 0; sTransitionTypes[i].fromType; i++)
      {
          if (sTransitionTypes[i].fromType == fromType && sTransitionTypes[i].toType == toType)
          {
              return sTransitionTypes[i].isEnter;
          }
      }

      return FALSE;
}

/** bool8 GetMapPairFadeFromType(u8 _fromType, u8 _toType) */
export function GetMapPairFadeFromType(_fromType: any, _toType: any): any {
  let i: any = null;
      let fromType: any = _fromType;
      let toType: any = _toType;

      for (i = 0; sTransitionTypes[i].fromType; i++)
      {
          if (sTransitionTypes[i].fromType == fromType && sTransitionTypes[i].toType == toType)
          {
              return sTransitionTypes[i].isExit;
          }
      }

      return FALSE;
}

/** static void DoExitCaveTransition(void) */
export function DoExitCaveTransition(): any {
  CreateTask(Task_ExitCaveTransition1, 0);
}

/** static void Task_ExitCaveTransition1(u8 taskId) */
export function Task_ExitCaveTransition1(taskId: any): any {
  gTasks[taskId].func = Task_ExitCaveTransition2;
}

/** static void Task_ExitCaveTransition2(u8 taskId) */
export function Task_ExitCaveTransition2(taskId: any): any {
  SetGpuReg(REG_OFFSET_DISPCNT, 0);
      LZ77UnCompVram(sCaveTransitionTiles, (VRAM + 0xC000));
      LZ77UnCompVram(sCaveTransitionTilemap, (VRAM + 0xF800));
      LoadPalette(sCaveTransitionPalette_White, BG_PLTT_ID(14), PLTT_SIZE_4BPP);
       
      LoadPalette(sCaveTransitionPalette_Exit, BG_PLTT_ID(14), PLTT_SIZEOF(8));
      SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG0
                                  | BLDCNT_EFFECT_BLEND
                                  | BLDCNT_TGT2_BG1
                                  | BLDCNT_TGT2_BG2
                                  | BLDCNT_TGT2_BG3
                                  | BLDCNT_TGT2_OBJ
                                  | BLDCNT_TGT2_BD);
      SetGpuReg(REG_OFFSET_BLDALPHA, 0);
      SetGpuReg(REG_OFFSET_BLDY, 0);
      SetGpuReg(REG_OFFSET_BG0CNT, BGCNT_PRIORITY(0)
                                 | BGCNT_CHARBASE(3)
                                 | BGCNT_SCREENBASE(31)
                                 | BGCNT_16COLOR
                                 | BGCNT_TXT256x256);
      SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                  | DISPCNT_OBJ_1D_MAP
                                  | DISPCNT_BG0_ON
                                  | DISPCNT_OBJ_ON);
      gTasks[taskId].func = Task_ExitCaveTransition3;
      gTasks[taskId].data[0] = 16;
      gTasks[taskId].data[1] = 0;
}

/** static void Task_ExitCaveTransition3(u8 taskId) */
export function Task_ExitCaveTransition3(taskId: any): any {
  let count: any = gTasks[taskId].data[1];
      let blend: any = count + 0x1000;

      SetGpuReg(REG_OFFSET_BLDALPHA, blend);
      if (count <= 16)
      {
          gTasks[taskId].data[1]++;
      }
      else
      {
          gTasks[taskId].data[2] = 0;
          gTasks[taskId].func = Task_ExitCaveTransition4;
      }
}

/** static void Task_ExitCaveTransition4(u8 taskId) */
export function Task_ExitCaveTransition4(taskId: any): any {
  let count: any = null;

      SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 16));
      count = gTasks[taskId].data[2];

      if (count < 8)
      {
          gTasks[taskId].data[2]++;
           
          LoadPalette(sCaveTransitionPalette_Exit[count], BG_PLTT_ID(14), 0 - PLTT_SIZEOF(count));
      }
      else
      {
          LoadPalette(sCaveTransitionPalette_White, BG_PLTT_ID(0), PLTT_SIZE_4BPP);
          gTasks[taskId].func = Task_ExitCaveTransition5;
          gTasks[taskId].data[2] = 8;
      }
}

/** static void Task_ExitCaveTransition5(u8 taskId) */
export function Task_ExitCaveTransition5(taskId: any): any {
  if (gTasks[taskId].data[2])
          gTasks[taskId].data[2]--;
      else
          SetMainCallback2(gMain.savedCallback);
}

/** static void DoEnterCaveTransition(void) */
export function DoEnterCaveTransition(): any {
  CreateTask(Task_EnterCaveTransition1, 0);
}

/** static void Task_EnterCaveTransition1(u8 taskId) */
export function Task_EnterCaveTransition1(taskId: any): any {
  gTasks[taskId].func = Task_EnterCaveTransition2;
}

/** static void Task_EnterCaveTransition2(u8 taskId) */
export function Task_EnterCaveTransition2(taskId: any): any {
  SetGpuReg(REG_OFFSET_DISPCNT, 0);
      LZ77UnCompVram(sCaveTransitionTiles, (VRAM + 0xC000));
      LZ77UnCompVram(sCaveTransitionTilemap, (VRAM + 0xF800));
      SetGpuReg(REG_OFFSET_BLDCNT, 0);
      SetGpuReg(REG_OFFSET_BLDALPHA, 0);
      SetGpuReg(REG_OFFSET_BLDY, 0);
      SetGpuReg(REG_OFFSET_BG0CNT, BGCNT_PRIORITY(0)
                                 | BGCNT_CHARBASE(3)
                                 | BGCNT_SCREENBASE(31)
                                 | BGCNT_16COLOR
                                 | BGCNT_TXT256x256);
      SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                  | DISPCNT_OBJ_1D_MAP
                                  | DISPCNT_BG0_ON
                                  | DISPCNT_OBJ_ON);
      LoadPalette(sCaveTransitionPalette_White, BG_PLTT_ID(14), PLTT_SIZE_4BPP);
      LoadPalette(sCaveTransitionPalette_Black, BG_PLTT_ID(0), PLTT_SIZE_4BPP);
      gTasks[taskId].func = Task_EnterCaveTransition3;
      gTasks[taskId].data[0] = 16;
      gTasks[taskId].data[1] = 0;
      gTasks[taskId].data[2] = 0;
}

/** static void Task_EnterCaveTransition3(u8 taskId) */
export function Task_EnterCaveTransition3(taskId: any): any {
  let count: any = gTasks[taskId].data[2];

      if (count < 16)
      {
          gTasks[taskId].data[2]++;
          gTasks[taskId].data[2]++;
          LoadPalette(sCaveTransitionPalette_Enter[15 - count], BG_PLTT_ID(14), PLTT_SIZEOF(count + 1));
      }
      else
      {
          SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 16));
          SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG0
                                      | BLDCNT_EFFECT_BLEND
                                      | BLDCNT_TGT2_BG1
                                      | BLDCNT_TGT2_BG2
                                      | BLDCNT_TGT2_BG3
                                      | BLDCNT_TGT2_OBJ
                                      | BLDCNT_TGT2_BD);
          gTasks[taskId].func = Task_EnterCaveTransition4;
      }
}

/** static void Task_EnterCaveTransition4(u8 taskId) */
export function Task_EnterCaveTransition4(taskId: any): any {
  let count: any = 16 - gTasks[taskId].data[1];
      let blend: any = count + 0x1000;

      SetGpuReg(REG_OFFSET_BLDALPHA, blend);
      if (count)
      {
          gTasks[taskId].data[1]++;
      }
      else
      {
          LoadPalette(sCaveTransitionPalette_Black, BG_PLTT_ID(0), PLTT_SIZE_4BPP);
          SetMainCallback2(gMain.savedCallback);
      }
}

// ─── callsTo manifest (= 34 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AnimateSprites","BGCNT_CHARBASE","BGCNT_PRIORITY","BGCNT_SCREENBASE","BG_PLTT_ID","BLDALPHA_BLEND","BuildOamBuffer","CreateFieldMoveTask","CreateTask","DmaFill16","DmaFill32","FlagGet","FlagSet","GetCurrentMapType","GetCursorSelectionMonId","GetLastUsedWarpMapType","LZ77UnCompVram","LoadOam","LoadPalette","PLTT_SIZEOF","PlaySE","ProcessSpriteCopyRequests","ResetPaletteFade","ResetSpriteData","ResetTasks","RunTasks","ScriptContext_SetupScript","SetGpuReg","SetMainCallback2","SetVBlankCallback","ShouldDoBrailleRegisteelEffect","TransferPlttBuffer","TryDoMapTransition","UpdatePaletteFade"];
