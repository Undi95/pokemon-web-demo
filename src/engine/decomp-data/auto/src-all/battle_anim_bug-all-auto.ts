// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_anim_bug.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 13 functions extracted from src/battle_anim_bug.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  AnimTranslateLinear, ArcTan2Neg, BLDALPHA_BLEND, CMD_ARGS,
  DestroyAnimSprite, GetBattlerPosition, GetBattlerSide, GetBattlerSpriteCoord,
  GetBattlerSpriteCoord2, InitAnimArcTranslation, InitAnimLinearTranslationWithSpeed, InitSpritePosToAnimAttacker,
  IsContest, SetAverageBattlerPositions, SetGpuReg, Sin,
  StartSpriteAffineAnim, StoreSpriteCallbackInData6, TranslateAnimHorizontalArc, TrySetSpriteRotScale,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static void AnimMegahornHorn(struct Sprite *sprite) */
export function AnimMegahornHorn(sprite: any): any {
  CMD_ARGS(x1, y1, x2, y2, duration);

      if (IsContest())
      {
          StartSpriteAffineAnim(sprite, 2);
          cmd.x2 = -cmd.x2;
          cmd.x1 = -cmd.x1;
      }
      else if (GetBattlerSide(gBattleAnimTarget) == B_SIDE_PLAYER)
      {
          StartSpriteAffineAnim(sprite, 1);
          cmd.y1 = -cmd.y1;
          cmd.x2 = -cmd.x2;
          cmd.y2 = -cmd.y2;
          cmd.x1 = -cmd.x1;
      }

      sprite.x = GetBattlerSpriteCoord2(gBattleAnimTarget, BATTLER_COORD_X_2) + cmd.x1;
      sprite.y = GetBattlerSpriteCoord2(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + cmd.y1;
      sprite.data[0] = cmd.duration;

      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2) + cmd.x2;
      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + cmd.y2;

      sprite.callback = StartAnimLinearTranslation;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
}

/** static void AnimLeechLifeNeedle(struct Sprite *sprite) */
export function AnimLeechLifeNeedle(sprite: any): any {
  CMD_ARGS(x, y, duration);

      if (IsContest())
      {
          cmd.x = -cmd.x;
          StartSpriteAffineAnim(sprite, 2);
      }
      else if (GetBattlerSide(gBattleAnimTarget) == B_SIDE_PLAYER)
      {
          cmd.y = -cmd.y;
          cmd.x = -cmd.x;
      }

      sprite.x = GetBattlerSpriteCoord2(gBattleAnimTarget, BATTLER_COORD_X_2) + cmd.x;
      sprite.y = GetBattlerSpriteCoord2(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + cmd.y;
      sprite.data[0] = cmd.duration;

      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET);

      sprite.callback = StartAnimLinearTranslation;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
}

/** static void AnimTranslateWebThread(struct Sprite *sprite) */
export function AnimTranslateWebThread(sprite: any): any {
  CMD_ARGS(x, y, unk2, amplitude, targetsBoth);

      if (IsContest())
          cmd.unk2 /= 2;

      InitSpritePosToAnimAttacker(sprite, TRUE);
      sprite.data[0] = cmd.unk2;
      sprite.data[1] = sprite.x;
      sprite.data[3] = sprite.y;

      if (!cmd.targetsBoth)
      {
          sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
          sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET);
      }
      else
      {
          SetAverageBattlerPositions(gBattleAnimTarget, TRUE,sprite.data[2],sprite.data[4]);
      }

      InitAnimLinearTranslationWithSpeed(sprite);
      sprite.data[5] = cmd.amplitude;
      sprite.callback = AnimTranslateWebThread_Step;
}

/** static void AnimTranslateWebThread_Step(struct Sprite *sprite) */
export function AnimTranslateWebThread_Step(sprite: any): any {
  if (AnimTranslateLinear(sprite))
      {
          DestroyAnimSprite(sprite);
          return;
      }

      sprite.x2 += Sin(sprite.data[6], sprite.data[5]);
      sprite.data[6] = (sprite.data[6] + 13) & 0xFF;
}

/** static void AnimStringWrap(struct Sprite *sprite) */
export function AnimStringWrap(sprite: any): any {
  CMD_ARGS(x, y);

      SetAverageBattlerPositions(gBattleAnimTarget, FALSE,sprite.x,sprite.y);
      if (GetBattlerSide(gBattleAnimAttacker))
          sprite.x -= cmd.x;
      else
          sprite.x += cmd.x;

      sprite.y += cmd.y;
      if (GetBattlerSide(gBattleAnimTarget) == B_SIDE_PLAYER)
          sprite.y += 8;

      sprite.callback = AnimStringWrap_Step;
}

/** static void AnimStringWrap_Step(struct Sprite *sprite) */
export function AnimStringWrap_Step(sprite: any): any {
  if (++sprite.data[0] == 3)
      {
          sprite.data[0] = 0;
          sprite.invisible ^= 1;
      }

      if (++sprite.data[1] == 51)
      {
          DestroyAnimSprite(sprite);
      }
}

/** static void AnimSpiderWeb(struct Sprite *sprite) */
export function AnimSpiderWeb(sprite: any): any {
  SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT2_ALL | BLDCNT_EFFECT_BLEND);
      SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 0));

      sprite.data[0] = 16;
      sprite.callback = AnimSpiderWeb_Step;
}

/** static void AnimSpiderWeb_Step(struct Sprite *sprite) */
export function AnimSpiderWeb_Step(sprite: any): any {
  if (sprite.data[2] < 20)
      {
          sprite.data[2]++;
      }
      else if (sprite.data[1]++ & 1)
      {
          sprite.data[0]--;
          SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(sprite.data[0], 16 - sprite.data[0]));

          if (sprite.data[0] == 0)
          {
              sprite.invisible = TRUE;
              sprite.callback = AnimSpiderWeb_End;
          }
      }
}

/** static void AnimSpiderWeb_End(struct Sprite *sprite) */
export function AnimSpiderWeb_End(sprite: any): any {
  SetGpuReg(REG_OFFSET_BLDCNT, 0);
      SetGpuReg(REG_OFFSET_BLDALPHA, 0);
      DestroyAnimSprite(sprite);
}

/** static void AnimTranslateStinger(struct Sprite *sprite) */
export function AnimTranslateStinger(sprite: any): any {
  CMD_ARGS(initialX, intialY, targetX, targetY, duration);

      let lVarX, lVarY;
      let rot: any = null;

      if (IsContest())
      {
          cmd.targetX = -cmd.targetX;
      }
      else
      {
          if (GetBattlerSide(gBattleAnimAttacker))
          {
              cmd.targetX = -cmd.targetX;
              cmd.intialY = -cmd.intialY;
              cmd.targetY = -cmd.targetY;
          }
      }

      if (!IsContest() && GetBattlerSide(gBattleAnimAttacker) == GetBattlerSide(gBattleAnimTarget))
      {
          if (GetBattlerPosition(gBattleAnimTarget) == B_POSITION_PLAYER_LEFT
           || GetBattlerPosition(gBattleAnimTarget) == B_POSITION_OPPONENT_LEFT)
          {
              cmd.targetX *= -1;
              cmd.initialX *= -1;
          }
      }

      InitSpritePosToAnimAttacker(sprite, TRUE);

      lVarX = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2) + cmd.targetX;
      lVarY = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + cmd.targetY;
      rot = ArcTan2Neg(lVarX - sprite.x, lVarY - sprite.y);
      rot += 0xC000;
      TrySetSpriteRotScale(sprite, FALSE, _0x100, _0x100, rot);

      sprite.data[0] = cmd.duration;
      sprite.data[2] = lVarX;
      sprite.data[4] = lVarY;

      sprite.callback = StartAnimLinearTranslation;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
}

/** static void AnimMissileArc(struct Sprite *sprite) */
export function AnimMissileArc(sprite: any): any {
  CMD_ARGS(initialX, intialY, targetX, targetY, duration, waveAmplitude);

      InitSpritePosToAnimAttacker(sprite, TRUE);

      if (GetBattlerSide(gBattleAnimAttacker))
          cmd.targetX = -cmd.targetX;

      sprite.data[0] = cmd.duration;
      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2) + cmd.targetX;
      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + cmd.targetY;
      sprite.data[5] = cmd.waveAmplitude;
      InitAnimArcTranslation(sprite);

      sprite.callback = AnimMissileArc_Step;
      sprite.invisible = TRUE;
}

/** static void AnimMissileArc_Step(struct Sprite *sprite) */
export function AnimMissileArc_Step(sprite: any): any {
  sprite.invisible = FALSE;

      if (TranslateAnimHorizontalArc(sprite))
      {
          DestroyAnimSprite(sprite);
      }
      else
      {
          const tempData: any[] = [];
          let data: any = sprite.data;
          let x1: any = sprite.x;
          let x2: any = sprite.x2;
          let y1: any = sprite.y;
          let y2: any = sprite.y2;
          let i: any = null;

          for (i = 0; i < 8; i++)
              tempData[i] = data[i];

          x2 += x1;
          y2 += y1;

          if (!TranslateAnimHorizontalArc(sprite))
          {
              let rotation: any = ArcTan2Neg(sprite.x + sprite.x2 - x2,
                                    sprite.y + sprite.y2 - y2);
              rotation += 0xC000;
              TrySetSpriteRotScale(sprite, FALSE, _0x100, _0x100, rotation);

              for (i = 0; i < 8; i++)
                  data[i] = tempData[i];
          }
      }
}

/** static void AnimTailGlowOrb(struct Sprite *sprite) */
export function AnimTailGlowOrb(sprite: any): any {
  CMD_ARGS(relativeTo);

      if (cmd.relativeTo == ANIM_ATTACKER)
      {
          sprite.x = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2);
          sprite.y = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_Y_PIC_OFFSET) + 18;
      }
      else
      {
          sprite.x = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
          sprite.y = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + 18;
      }

      StoreSpriteCallbackInData6(sprite, DestroySpriteAndMatrix);
      sprite.callback = RunStoredCallbackWhenAffineAnimEnds;
}

// ─── callsTo manifest (= 20 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AnimTranslateLinear","ArcTan2Neg","BLDALPHA_BLEND","CMD_ARGS","DestroyAnimSprite","GetBattlerPosition","GetBattlerSide","GetBattlerSpriteCoord","GetBattlerSpriteCoord2","InitAnimArcTranslation","InitAnimLinearTranslationWithSpeed","InitSpritePosToAnimAttacker","IsContest","SetAverageBattlerPositions","SetGpuReg","Sin","StartSpriteAffineAnim","StoreSpriteCallbackInData6","TranslateAnimHorizontalArc","TrySetSpriteRotScale"];
