// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_ai_switch_items.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 13 functions extracted from src/battle_ai_switch_items.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  ABILITY_ON_FIELD2, ABILITY_ON_OPPOSING_FIELD, AI_CalcDmg, AI_TypeCalc,
  AreStatsRaised, BATTLE_OPPOSITE, BATTLE_PARTNER, BtlController_EmitTwoReturnValues,
  FindMonThatAbsorbsOpponentsMove, FindMonWithFlagsAndSuperEffective, GetAI_ItemType, GetBattlerAtPosition,
  GetBattlerPosition, GetBattlerSide, GetItemEffectParamOffset, GetMonData,
  GetMostSuitableMonToSwitchInto, HasSuperEffectiveMoveAgainstOpponents, IS_BATTLER_OF_TYPE, ModulateByTypeEffectiveness,
  Random, ShouldSwitch, ShouldSwitchIfNaturalCure, ShouldSwitchIfPerishSong,
  ShouldSwitchIfWonderGuard, ShouldUseItem, TYPE_EFFECT_ATK_TYPE, TYPE_EFFECT_DEF_TYPE,
  TYPE_EFFECT_MULTIPLIER, TypeCalc,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static bool8 ShouldSwitchIfPerishSong(void) */
export function ShouldSwitchIfPerishSong(): any {
  if (gStatuses3[gActiveBattler] & STATUS3_PERISH_SONG
          && gDisableStructs[gActiveBattler].perishSongTimer == 0)
      {
          MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), PARTY_SIZE);
          BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_SWITCH, 0);
          return TRUE;
      }
      else
      {
          return FALSE;
      }
}

/** static bool8 ShouldSwitchIfWonderGuard(void) */
export function ShouldSwitchIfWonderGuard(): any {
  let opposingPosition: any = null;
      let opposingBattler: any = null;
      let moveFlags: any = null;
      let i, j;
      let firstId: any = null;
      let lastId: any = null;  
      let party: any = NULL;
      let move: any = null;

      if (gBattleTypeFlags & BATTLE_TYPE_DOUBLE)
          return FALSE;

      opposingPosition = BATTLE_OPPOSITE(GetBattlerPosition(gActiveBattler));

      if (gBattleMons[GetBattlerAtPosition(opposingPosition)].ability != ABILITY_WONDER_GUARD)
          return FALSE;

       
      for (opposingBattler = GetBattlerAtPosition(opposingPosition), i = 0; i < MAX_MON_MOVES; i++)
      {
          move = gBattleMons[gActiveBattler].moves[i];
          if (move == MOVE_NONE)
              continue;

          moveFlags = AI_TypeCalc(move, gBattleMons[opposingBattler].species, gBattleMons[opposingBattler].ability);
          if (moveFlags & MOVE_RESULT_SUPER_EFFECTIVE)
              return FALSE;
      }

       
      if (gBattleTypeFlags & (BATTLE_TYPE_TWO_OPPONENTS | BATTLE_TYPE_TOWER_LINK_MULTI))
      {
          if ((gActiveBattler & BIT_FLANK) == B_FLANK_LEFT)
              firstId = 0, lastId = PARTY_SIZE / 2;
          else
              firstId = PARTY_SIZE / 2, lastId = PARTY_SIZE;
      }
      else
      {
          firstId = 0, lastId = PARTY_SIZE;
      }

      if (GetBattlerSide(gActiveBattler) == B_SIDE_PLAYER)
          party = gPlayerParty;
      else
          party = gEnemyParty;

       
      for (i = firstId; i < lastId; i++)
      {
          if (GetMonData(party[i], MON_DATA_HP) == 0)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_NONE)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_EGG)
              continue;
          if (i == gBattlerPartyIndexes[gActiveBattler])
              continue;

          GetMonData(party[i], MON_DATA_SPECIES);  
          GetMonData(party[i], MON_DATA_ABILITY_NUM);  

          for (opposingBattler = GetBattlerAtPosition(opposingPosition), j = 0; j < MAX_MON_MOVES; j++)
          {
              move = GetMonData(party[i], MON_DATA_MOVE1 + j);
              if (move == MOVE_NONE)
                  continue;

              moveFlags = AI_TypeCalc(move, gBattleMons[opposingBattler].species, gBattleMons[opposingBattler].ability);
              if (moveFlags & MOVE_RESULT_SUPER_EFFECTIVE && Random() % 3 < 2)
              {
                   
                  MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), i);
                  BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_SWITCH, 0);
                  return TRUE;
              }
          }
      }

      return FALSE;
}

/** static bool8 FindMonThatAbsorbsOpponentsMove(void) */
export function FindMonThatAbsorbsOpponentsMove(): any {
  let battlerIn1, battlerIn2;
      let absorbingTypeAbility: any = null;
      let firstId: any = null;
      let lastId: any = null;  
      let party: any = null;
      let i: any = null;

      if (HasSuperEffectiveMoveAgainstOpponents(TRUE) && Random() % 3 != 0)
          return FALSE;
      if (gLastLandedMoves[gActiveBattler] == MOVE_NONE)
          return FALSE;
      if (gLastLandedMoves[gActiveBattler] == MOVE_UNAVAILABLE)
          return FALSE;
      if (gBattleMoves[gLastLandedMoves[gActiveBattler]].power == 0)
          return FALSE;

      if (gBattleTypeFlags & BATTLE_TYPE_DOUBLE)
      {
          battlerIn1 = gActiveBattler;
          if (gAbsentBattlerFlags & gBitTable[GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(gActiveBattler)))])
              battlerIn2 = gActiveBattler;
          else
              battlerIn2 = GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(gActiveBattler)));
      }
      else
      {
          battlerIn1 = gActiveBattler;
          battlerIn2 = gActiveBattler;
      }

      if (gBattleMoves[gLastLandedMoves[gActiveBattler]].type == TYPE_FIRE)
          absorbingTypeAbility = ABILITY_FLASH_FIRE;
      else if (gBattleMoves[gLastLandedMoves[gActiveBattler]].type == TYPE_WATER)
          absorbingTypeAbility = ABILITY_WATER_ABSORB;
      else if (gBattleMoves[gLastLandedMoves[gActiveBattler]].type == TYPE_ELECTRIC)
          absorbingTypeAbility = ABILITY_VOLT_ABSORB;
      else
          return FALSE;

      if (gBattleMons[gActiveBattler].ability == absorbingTypeAbility)
          return FALSE;

      if (gBattleTypeFlags & (BATTLE_TYPE_TWO_OPPONENTS | BATTLE_TYPE_TOWER_LINK_MULTI))
      {
          if ((gActiveBattler & BIT_FLANK) == B_FLANK_LEFT)
              firstId = 0, lastId = PARTY_SIZE / 2;
          else
              firstId = PARTY_SIZE / 2, lastId = PARTY_SIZE;
      }
      else
      {
          firstId = 0, lastId = PARTY_SIZE;
      }

      if (GetBattlerSide(gActiveBattler) == B_SIDE_PLAYER)
          party = gPlayerParty;
      else
          party = gEnemyParty;

      for (i = firstId; i < lastId; i++)
      {
          let species: any = null;
          let monAbility: any = null;

          if (GetMonData(party[i], MON_DATA_HP) == 0)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_NONE)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_EGG)
              continue;
          if (i == gBattlerPartyIndexes[battlerIn1])
              continue;
          if (i == gBattlerPartyIndexes[battlerIn2])
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn1))
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn2))
              continue;

          species = GetMonData(party[i], MON_DATA_SPECIES);
          if (GetMonData(party[i], MON_DATA_ABILITY_NUM) != 0)
              monAbility = gSpeciesInfo[species].abilities[1];
          else
              monAbility = gSpeciesInfo[species].abilities[0];

          if (absorbingTypeAbility == monAbility && Random() & 1)
          {
               
              MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), i);
              BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_SWITCH, 0);
              return TRUE;
          }
      }

      return FALSE;
}

/** static bool8 ShouldSwitchIfNaturalCure(void) */
export function ShouldSwitchIfNaturalCure(): any {
  if (!(gBattleMons[gActiveBattler].status1 & STATUS1_SLEEP))
          return FALSE;
      if (gBattleMons[gActiveBattler].ability != ABILITY_NATURAL_CURE)
          return FALSE;
      if (gBattleMons[gActiveBattler].hp < gBattleMons[gActiveBattler].maxHP / 2)
          return FALSE;

      if ((gLastLandedMoves[gActiveBattler] == MOVE_NONE
        || gLastLandedMoves[gActiveBattler] == MOVE_UNAVAILABLE)
       && Random() & 1)
      {
          MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), PARTY_SIZE);
          BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_SWITCH, 0);
          return TRUE;
      }
      else if (gBattleMoves[gLastLandedMoves[gActiveBattler]].power == 0
            && Random() & 1)
      {
          MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), PARTY_SIZE);
          BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_SWITCH, 0);
          return TRUE;
      }

      if (FindMonWithFlagsAndSuperEffective(MOVE_RESULT_DOESNT_AFFECT_FOE, 1))
          return TRUE;
      if (FindMonWithFlagsAndSuperEffective(MOVE_RESULT_NOT_VERY_EFFECTIVE, 1))
          return TRUE;

      if (Random() & 1)
      {
          MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), PARTY_SIZE);
          BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_SWITCH, 0);
          return TRUE;
      }

      return FALSE;
}

/** static bool8 HasSuperEffectiveMoveAgainstOpponents(bool8 noRng) */
export function HasSuperEffectiveMoveAgainstOpponents(noRng: any): any {
  let opposingPosition: any = null;
      let opposingBattler: any = null;
      let i: any = null;
      let moveFlags: any = null;
      let move: any = null;

      opposingPosition = BATTLE_OPPOSITE(GetBattlerPosition(gActiveBattler));
      opposingBattler = GetBattlerAtPosition(opposingPosition);

      if (!(gAbsentBattlerFlags & gBitTable[opposingBattler]))
      {
          for (i = 0; i < MAX_MON_MOVES; i++)
          {
              move = gBattleMons[gActiveBattler].moves[i];
              if (move == MOVE_NONE)
                  continue;

              moveFlags = AI_TypeCalc(move, gBattleMons[opposingBattler].species, gBattleMons[opposingBattler].ability);
              if (moveFlags & MOVE_RESULT_SUPER_EFFECTIVE)
              {
                  if (noRng)
                      return TRUE;
                  if (Random() % 10 != 0)
                      return TRUE;
              }
          }
      }
      if (!(gBattleTypeFlags & BATTLE_TYPE_DOUBLE))
          return FALSE;

      opposingBattler = GetBattlerAtPosition(BATTLE_PARTNER(opposingPosition));

      if (!(gAbsentBattlerFlags & gBitTable[opposingBattler]))
      {
          for (i = 0; i < MAX_MON_MOVES; i++)
          {
              move = gBattleMons[gActiveBattler].moves[i];
              if (move == MOVE_NONE)
                  continue;

              moveFlags = AI_TypeCalc(move, gBattleMons[opposingBattler].species, gBattleMons[opposingBattler].ability);
              if (moveFlags & MOVE_RESULT_SUPER_EFFECTIVE)
              {
                  if (noRng)
                      return TRUE;
                  if (Random() % 10 != 0)
                      return TRUE;
              }
          }
      }

      return FALSE;
}

/** static bool8 AreStatsRaised(void) */
export function AreStatsRaised(): any {
  let buffedStatsValue: any = 0;
      let i: any = null;

      for (i = 0; i < NUM_BATTLE_STATS; i++)
      {
          if (gBattleMons[gActiveBattler].statStages[i] > DEFAULT_STAT_STAGE)
              buffedStatsValue += gBattleMons[gActiveBattler].statStages[i] - DEFAULT_STAT_STAGE;
      }

      return (buffedStatsValue > 3);
}

/** static bool8 FindMonWithFlagsAndSuperEffective(u8 flags, u8 moduloPercent) */
export function FindMonWithFlagsAndSuperEffective(flags: any, moduloPercent: any): any {
  let battlerIn1, battlerIn2;
      let firstId: any = null;
      let lastId: any = null;  
      let party: any = null;
      let i, j;
      let move: any = null;
      let moveFlags: any = null;

      if (gLastLandedMoves[gActiveBattler] == MOVE_NONE)
          return FALSE;
      if (gLastLandedMoves[gActiveBattler] == MOVE_UNAVAILABLE)
          return FALSE;
      if (gLastHitBy[gActiveBattler] == 0xFF)
          return FALSE;
      if (gBattleMoves[gLastLandedMoves[gActiveBattler]].power == 0)
          return FALSE;

      if (gBattleTypeFlags & BATTLE_TYPE_DOUBLE)
      {
          battlerIn1 = gActiveBattler;
          if (gAbsentBattlerFlags & gBitTable[GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(gActiveBattler)))])
              battlerIn2 = gActiveBattler;
          else
              battlerIn2 = GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(gActiveBattler)));
      }
      else
      {
          battlerIn1 = gActiveBattler;
          battlerIn2 = gActiveBattler;
      }

      if (gBattleTypeFlags & (BATTLE_TYPE_TWO_OPPONENTS | BATTLE_TYPE_TOWER_LINK_MULTI))
      {
          if ((gActiveBattler & BIT_FLANK) == 0)
              firstId = 0, lastId = PARTY_SIZE / 2;
          else
              firstId = PARTY_SIZE / 2, lastId = PARTY_SIZE;
      }
      else
      {
          firstId = 0, lastId = PARTY_SIZE;
      }

      if (GetBattlerSide(gActiveBattler) == B_SIDE_PLAYER)
          party = gPlayerParty;
      else
          party = gEnemyParty;

      for (i = firstId; i < lastId; i++)
      {
          let species: any = null;
          let monAbility: any = null;

          if (GetMonData(party[i], MON_DATA_HP) == 0)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_NONE)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_EGG)
              continue;
          if (i == gBattlerPartyIndexes[battlerIn1])
              continue;
          if (i == gBattlerPartyIndexes[battlerIn2])
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn1))
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn2))
              continue;

          species = GetMonData(party[i], MON_DATA_SPECIES);
          if (GetMonData(party[i], MON_DATA_ABILITY_NUM) != 0)
              monAbility = gSpeciesInfo[species].abilities[1];
          else
              monAbility = gSpeciesInfo[species].abilities[0];

          moveFlags = AI_TypeCalc(gLastLandedMoves[gActiveBattler], species, monAbility);
          if (moveFlags & flags)
          {
              battlerIn1 = gLastHitBy[gActiveBattler];

              for (j = 0; j < MAX_MON_MOVES; j++)
              {
                  move = GetMonData(party[i], MON_DATA_MOVE1 + j);
                  if (move == 0)
                      continue;

                  moveFlags = AI_TypeCalc(move, gBattleMons[battlerIn1].species, gBattleMons[battlerIn1].ability);
                  if (moveFlags & MOVE_RESULT_SUPER_EFFECTIVE && Random() % moduloPercent == 0)
                  {
                      MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), i);
                      BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_SWITCH, 0);
                      return TRUE;
                  }
              }
          }
      }

      return FALSE;
}

/** static bool8 ShouldSwitch(void) */
export function ShouldSwitch(): any {
  let battlerIn1, battlerIn2;
      let activeBattlerPtr: any = null;  
      let firstId: any = null;
      let lastId: any = null;  
      let party: any = null;
      let i: any = null;
      let availableToSwitch: any = null;

      if (gBattleMons[(activeBattlerPtr =gActiveBattler)].status2 & (STATUS2_WRAPPED | STATUS2_ESCAPE_PREVENTION))
          return FALSE;
      if (gStatuses3[gActiveBattler] & STATUS3_ROOTED)
          return FALSE;
      if (ABILITY_ON_OPPOSING_FIELD(gActiveBattler, ABILITY_SHADOW_TAG))
          return FALSE;
      if (ABILITY_ON_OPPOSING_FIELD(gActiveBattler, ABILITY_ARENA_TRAP))  
          return FALSE;
      if (ABILITY_ON_FIELD2(ABILITY_MAGNET_PULL))
      {
          if (IS_BATTLER_OF_TYPE(gActiveBattler, TYPE_STEEL))
              return FALSE;
      }
      if (gBattleTypeFlags & BATTLE_TYPE_ARENA)
          return FALSE;

      availableToSwitch = 0;
      if (gBattleTypeFlags & BATTLE_TYPE_DOUBLE)
      {
          battlerIn1 = activeBattlerPtr;
          if (gAbsentBattlerFlags & gBitTable[GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(activeBattlerPtr)))])
              battlerIn2 = activeBattlerPtr;
          else
              battlerIn2 = GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(activeBattlerPtr)));
      }
      else
      {
          battlerIn1 = activeBattlerPtr;
          battlerIn2 = activeBattlerPtr;
      }

      if (gBattleTypeFlags & (BATTLE_TYPE_TWO_OPPONENTS | BATTLE_TYPE_TOWER_LINK_MULTI))
      {
          if ((gActiveBattler & BIT_FLANK) == B_FLANK_LEFT)
              firstId = 0, lastId = PARTY_SIZE / 2;
          else
              firstId = PARTY_SIZE / 2, lastId = PARTY_SIZE;
      }
      else
      {
          firstId = 0, lastId = PARTY_SIZE;
      }

      if (GetBattlerSide(gActiveBattler) == B_SIDE_PLAYER)
          party = gPlayerParty;
      else
          party = gEnemyParty;

      for (i = firstId; i < lastId; i++)
      {
          if (GetMonData(party[i], MON_DATA_HP) == 0)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_NONE)
              continue;
          if (GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) == SPECIES_EGG)
              continue;
          if (i == gBattlerPartyIndexes[battlerIn1])
              continue;
          if (i == gBattlerPartyIndexes[battlerIn2])
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn1))
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn2))
              continue;

          availableToSwitch++;
      }

      if (availableToSwitch == 0)
          return FALSE;
      if (ShouldSwitchIfPerishSong())
          return TRUE;
      if (ShouldSwitchIfWonderGuard())
          return TRUE;
      if (FindMonThatAbsorbsOpponentsMove())
          return TRUE;
      if (ShouldSwitchIfNaturalCure())
          return TRUE;
      if (HasSuperEffectiveMoveAgainstOpponents(FALSE))
          return FALSE;
      if (AreStatsRaised())
          return FALSE;
      if (FindMonWithFlagsAndSuperEffective(MOVE_RESULT_DOESNT_AFFECT_FOE, 2)
          || FindMonWithFlagsAndSuperEffective(MOVE_RESULT_NOT_VERY_EFFECTIVE, 3))
          return TRUE;

      return FALSE;
}

/** void AI_TrySwitchOrUseItem(void) */
export function AI_TrySwitchOrUseItem(): any {
  let party: any = null;
      let battlerIn1, battlerIn2;
      let firstId: any = null;
      let lastId: any = null;  
      let battlerIdentity: any = GetBattlerPosition(gActiveBattler);

      if (GetBattlerSide(gActiveBattler) == B_SIDE_PLAYER)
          party = gPlayerParty;
      else
          party = gEnemyParty;

      if (gBattleTypeFlags & BATTLE_TYPE_TRAINER)
      {
          if (ShouldSwitch())
          {
              if ((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler) == PARTY_SIZE)
              {
                  let monToSwitchId: any = GetMostSuitableMonToSwitchInto();
                  if (monToSwitchId == PARTY_SIZE)
                  {
                      if (!(gBattleTypeFlags & BATTLE_TYPE_DOUBLE))
                      {
                          battlerIn1 = GetBattlerAtPosition(battlerIdentity);
                          battlerIn2 = battlerIn1;
                      }
                      else
                      {
                          battlerIn1 = GetBattlerAtPosition(battlerIdentity);
                          battlerIn2 = GetBattlerAtPosition(BATTLE_PARTNER(battlerIdentity));
                      }

                      if (gBattleTypeFlags & (BATTLE_TYPE_TWO_OPPONENTS | BATTLE_TYPE_TOWER_LINK_MULTI))
                      {
                          if ((gActiveBattler & BIT_FLANK) == B_FLANK_LEFT)
                              firstId = 0, lastId = PARTY_SIZE / 2;
                          else
                              firstId = PARTY_SIZE / 2, lastId = PARTY_SIZE;
                      }
                      else
                      {
                          firstId = 0, lastId = PARTY_SIZE;
                      }

                      for (monToSwitchId = firstId; monToSwitchId < lastId; monToSwitchId++)
                      {
                          if (GetMonData(party[monToSwitchId], MON_DATA_HP) == 0)
                              continue;
                          if (monToSwitchId == gBattlerPartyIndexes[battlerIn1])
                              continue;
                          if (monToSwitchId == gBattlerPartyIndexes[battlerIn2])
                              continue;
                          if (monToSwitchId == (gBattleStruct.monToSwitchIntoId + battlerIn1))
                              continue;
                          if (monToSwitchId == (gBattleStruct.monToSwitchIntoId + battlerIn2))
                              continue;

                          break;
                      }
                  }

                  MEM_WRITE((gBattleStruct.AI_monToSwitchIntoId + gActiveBattler), monToSwitchId);
              }

              MEM_WRITE((gBattleStruct.monToSwitchIntoId + gActiveBattler), (gBattleStruct.AI_monToSwitchIntoId + gActiveBattler));
              return;
          }
          else if (ShouldUseItem())
          {
              return;
          }
      }

      BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_USE_MOVE, BATTLE_OPPOSITE(gActiveBattler) << 8);
}

/** static void ModulateByTypeEffectiveness(u8 atkType, u8 defType1, u8 defType2, u8 *var) */
export function ModulateByTypeEffectiveness(atkType: any, defType1: any, defType2: any, _var: any): any {
  let i: any = 0;

      while (TYPE_EFFECT_ATK_TYPE(i) != TYPE_ENDTABLE)
      {
          if (TYPE_EFFECT_ATK_TYPE(i) == TYPE_FORESIGHT)
          {
              i += 3;
              continue;
          }
          else if (TYPE_EFFECT_ATK_TYPE(i) == atkType)
          {
               
              if (TYPE_EFFECT_DEF_TYPE(i) == defType1)
                  _var = (_var * TYPE_EFFECT_MULTIPLIER(i)) / TYPE_MUL_NORMAL;
               
              if (TYPE_EFFECT_DEF_TYPE(i) == defType2 && defType1 != defType2)
                  _var = (_var * TYPE_EFFECT_MULTIPLIER(i)) / TYPE_MUL_NORMAL;
          }
          i += 3;
      }
}

/** u8 GetMostSuitableMonToSwitchInto(void) */
export function GetMostSuitableMonToSwitchInto(): any {
  let opposingBattler: any = null;
      let bestDmg: any = null;
      let bestMonId: any = null;
      let battlerIn1, battlerIn2;
      let firstId: any = null;
      let lastId: any = null;  
      let party: any = null;
      let i, j;
      let invalidMons: any = null;
      let move: any = null;

      if ((gBattleStruct.monToSwitchIntoId + gActiveBattler) != PARTY_SIZE)
          return (gBattleStruct.monToSwitchIntoId + gActiveBattler);
      if (gBattleTypeFlags & BATTLE_TYPE_ARENA)
          return gBattlerPartyIndexes[gActiveBattler] + 1;

      if (gBattleTypeFlags & BATTLE_TYPE_DOUBLE)
      {
          battlerIn1 = gActiveBattler;
          if (gAbsentBattlerFlags & gBitTable[GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(gActiveBattler)))])
              battlerIn2 = gActiveBattler;
          else
              battlerIn2 = GetBattlerAtPosition(BATTLE_PARTNER(GetBattlerPosition(gActiveBattler)));

           
          opposingBattler = Random() & BIT_FLANK;
          if (gAbsentBattlerFlags & gBitTable[opposingBattler])
              opposingBattler ^= BIT_FLANK;
      }
      else
      {
          opposingBattler = GetBattlerAtPosition(BATTLE_OPPOSITE(GetBattlerPosition(gActiveBattler)));
          battlerIn1 = gActiveBattler;
          battlerIn2 = gActiveBattler;
      }

      if (gBattleTypeFlags & (BATTLE_TYPE_TWO_OPPONENTS | BATTLE_TYPE_TOWER_LINK_MULTI))
      {
          if ((gActiveBattler & BIT_FLANK) == B_FLANK_LEFT)
              firstId = 0, lastId = PARTY_SIZE / 2;
          else
              firstId = PARTY_SIZE / 2, lastId = PARTY_SIZE;
      }
      else
      {
          firstId = 0, lastId = PARTY_SIZE;
      }

      if (GetBattlerSide(gActiveBattler) == B_SIDE_PLAYER)
          party = gPlayerParty;
      else
          party = gEnemyParty;

      invalidMons = 0;

      while (invalidMons != (1 << PARTY_SIZE) - 1)  
      {
          bestDmg = TYPE_MUL_NO_EFFECT;
          bestMonId = PARTY_SIZE;
           
          for (i = firstId; i < lastId; i++)
          {
              let species: any = GetMonData(party[i], MON_DATA_SPECIES);
              if (species != SPECIES_NONE
                  && GetMonData(party[i], MON_DATA_HP) != 0
                  && !(gBitTable[i] & invalidMons)
                  && gBattlerPartyIndexes[battlerIn1] != i
                  && gBattlerPartyIndexes[battlerIn2] != i
                  && i != (gBattleStruct.monToSwitchIntoId + battlerIn1)
                  && i != (gBattleStruct.monToSwitchIntoId + battlerIn2))
              {
                  let type1: any = gSpeciesInfo[species].types[0];
                  let type2: any = gSpeciesInfo[species].types[1];
                  let typeDmg: any = TYPE_MUL_NORMAL;
                  ModulateByTypeEffectiveness(gBattleMons[opposingBattler].types[0], type1, type2,typeDmg);
                  ModulateByTypeEffectiveness(gBattleMons[opposingBattler].types[1], type1, type2,typeDmg);

                   
                  if (bestDmg < typeDmg)
                  {
                      bestDmg = typeDmg;
                      bestMonId = i;
                  }
              }
              else
              {
                  invalidMons |= gBitTable[i];
              }
          }

           
          if (bestMonId != PARTY_SIZE)
          {
              for (i = 0; i < MAX_MON_MOVES; i++)
              {
                  move = GetMonData(party[bestMonId], MON_DATA_MOVE1 + i);
                  if (move != MOVE_NONE && TypeCalc(move, gActiveBattler, opposingBattler) & MOVE_RESULT_SUPER_EFFECTIVE)
                      break;
              }

              if (i != MAX_MON_MOVES)
                  return bestMonId;  

              invalidMons |= gBitTable[bestMonId];  
          }
          else
          {
              invalidMons = (1 << PARTY_SIZE) - 1;  
          }
      }

      gDynamicBasePower = 0;
      gBattleStruct.dynamicMoveType = 0;
      gBattleScripting.dmgMultiplier = 1;
      gMoveResultFlags = 0;
      gCritMultiplier = 1;
      bestDmg = 0;
      bestMonId = PARTY_SIZE;

       
      for (i = firstId; i < lastId; i++)
      {
          if ((GetMonData(party[i], MON_DATA_SPECIES)) == SPECIES_NONE)
              continue;
          if (GetMonData(party[i], MON_DATA_HP) == 0)
              continue;
          if (gBattlerPartyIndexes[battlerIn1] == i)
              continue;
          if (gBattlerPartyIndexes[battlerIn2] == i)
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn1))
              continue;
          if (i == (gBattleStruct.monToSwitchIntoId + battlerIn2))
              continue;

          for (j = 0; j < MAX_MON_MOVES; j++)
          {
              move = GetMonData(party[i], MON_DATA_MOVE1 + j);
              gBattleMoveDamage = 0;
              if (move != MOVE_NONE && gBattleMoves[move].power != 1)
              {
                  AI_CalcDmg(gActiveBattler, opposingBattler);
                  TypeCalc(move, gActiveBattler, opposingBattler);
              }
              if (bestDmg < gBattleMoveDamage)
              {
                  bestDmg = gBattleMoveDamage;
                  bestMonId = i;
              }
          }
      }

      return bestMonId;
}

/** static u8 GetAI_ItemType(u8 itemId, const u8 *itemEffect) */
export function GetAI_ItemType(itemId: any, itemEffect: any): any {
  if (itemId == ITEM_FULL_RESTORE)
          return AI_ITEM_FULL_RESTORE;
      else if (itemEffect[4] & ITEM4_HEAL_HP)
          return AI_ITEM_HEAL_HP;
      else if (itemEffect[3] & ITEM3_STATUS_ALL)
          return AI_ITEM_CURE_CONDITION;
      else if (itemEffect[0] & (ITEM0_DIRE_HIT | ITEM0_X_ATTACK) || itemEffect[1] != 0 || itemEffect[2] != 0)
          return AI_ITEM_X_STAT;
      else if (itemEffect[3] & ITEM3_GUARD_SPEC)
          return AI_ITEM_GUARD_SPEC;
      else
          return AI_ITEM_NOT_RECOGNIZABLE;
}

/** static bool8 ShouldUseItem(void) */
export function ShouldUseItem(): any {
  let party: any = null;
      let i: any = null;
      let validMons: any = 0;
      let shouldUse: any = FALSE;

      if (gBattleTypeFlags & BATTLE_TYPE_INGAME_PARTNER && GetBattlerPosition(gActiveBattler) == B_POSITION_PLAYER_RIGHT)
          return FALSE;

      if (GetBattlerSide(gActiveBattler) == B_SIDE_PLAYER)
          party = gPlayerParty;
      else
          party = gEnemyParty;

      for (i = 0; i < PARTY_SIZE; i++)
      {
          if (GetMonData(party[i], MON_DATA_HP) != 0
              && GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) != SPECIES_NONE
              && GetMonData(party[i], MON_DATA_SPECIES_OR_EGG) != SPECIES_EGG)
          {
              validMons++;
          }
      }

      for (i = 0; i < MAX_TRAINER_ITEMS; i++)
      {
          let item: any = null;
          let itemEffects: any = null;
          let paramOffset: any = null;
          let battlerSide: any = null;

          if (i != 0 && validMons > (gBattleResources.battleHistory.itemsNo - i) + 1)
              continue;
          item = gBattleResources.battleHistory.trainerItems[i];
          if (item == ITEM_NONE)
              continue;
          if (gItemEffectTable[item - ITEM_POTION] == NULL)
              continue;

          if (item == ITEM_ENIGMA_BERRY)
              itemEffects = gSaveBlock1Ptr.enigmaBerry.itemEffect;
          else
              itemEffects = gItemEffectTable[item - ITEM_POTION];

          MEM_WRITE((gBattleStruct.AI_itemType + gActiveBattler / 2), GetAI_ItemType(item, itemEffects));

          switch ((gBattleStruct.AI_itemType + gActiveBattler / 2))
          {
          case AI_ITEM_FULL_RESTORE:
              if (gBattleMons[gActiveBattler].hp >= gBattleMons[gActiveBattler].maxHP / 4)
                  break;
              if (gBattleMons[gActiveBattler].hp == 0)
                  break;
              shouldUse = TRUE;
              break;
          case AI_ITEM_HEAL_HP:
              paramOffset = GetItemEffectParamOffset(item, 4, ITEM4_HEAL_HP);
              if (paramOffset == 0)
                  break;
              if (gBattleMons[gActiveBattler].hp == 0)
                  break;
              if (gBattleMons[gActiveBattler].hp < gBattleMons[gActiveBattler].maxHP / 4 || gBattleMons[gActiveBattler].maxHP - gBattleMons[gActiveBattler].hp > itemEffects[paramOffset])
                  shouldUse = TRUE;
              break;
          case AI_ITEM_CURE_CONDITION:
              MEM_WRITE((gBattleStruct.AI_itemFlags + gActiveBattler / 2), 0);
              if (itemEffects[3] & ITEM3_SLEEP && gBattleMons[gActiveBattler].status1 & STATUS1_SLEEP)
              {
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_HEAL_SLEEP);
                  shouldUse = TRUE;
              }
              if (itemEffects[3] & ITEM3_POISON && (gBattleMons[gActiveBattler].status1 & STATUS1_POISON
                                                 || gBattleMons[gActiveBattler].status1 & STATUS1_TOXIC_POISON))
              {
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_HEAL_POISON);
                  shouldUse = TRUE;
              }
              if (itemEffects[3] & ITEM3_BURN && gBattleMons[gActiveBattler].status1 & STATUS1_BURN)
              {
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_HEAL_BURN);
                  shouldUse = TRUE;
              }
              if (itemEffects[3] & ITEM3_FREEZE && gBattleMons[gActiveBattler].status1 & STATUS1_FREEZE)
              {
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_HEAL_FREEZE);
                  shouldUse = TRUE;
              }
              if (itemEffects[3] & ITEM3_PARALYSIS && gBattleMons[gActiveBattler].status1 & STATUS1_PARALYSIS)
              {
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_HEAL_PARALYSIS);
                  shouldUse = TRUE;
              }
              if (itemEffects[3] & ITEM3_CONFUSION && gBattleMons[gActiveBattler].status2 & STATUS2_CONFUSION)
              {
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_HEAL_CONFUSION);
                  shouldUse = TRUE;
              }
              break;
          case AI_ITEM_X_STAT:
              MEM_WRITE((gBattleStruct.AI_itemFlags + gActiveBattler / 2), 0);
              if (gDisableStructs[gActiveBattler].isFirstTurn == 0)
                  break;
              if (itemEffects[0] & ITEM0_X_ATTACK)
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_X_ATTACK);
              if (itemEffects[1] & ITEM1_X_DEFEND)
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_X_DEFEND);
              if (itemEffects[1] & ITEM1_X_SPEED)
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_X_SPEED);
              if (itemEffects[2] & ITEM2_X_SPATK)
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_X_SPATK);
              if (itemEffects[2] & ITEM2_X_ACCURACY)
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_X_ACCURACY);
              if (itemEffects[0] & ITEM0_DIRE_HIT)
                  (gBattleStruct.AI_itemFlags + gActiveBattler / 2) |= (1 << AI_DIRE_HIT);
              shouldUse = TRUE;
              break;
          case AI_ITEM_GUARD_SPEC:
              battlerSide = GetBattlerSide(gActiveBattler);
              if (gDisableStructs[gActiveBattler].isFirstTurn != 0 && gSideTimers[battlerSide].mistTimer == 0)
                  shouldUse = TRUE;
              break;
          case AI_ITEM_NOT_RECOGNIZABLE:
              return FALSE;
          }

          if (shouldUse)
          {
              BtlController_EmitTwoReturnValues(B_COMM_TO_ENGINE, B_ACTION_USE_ITEM, 0);
              MEM_WRITE((gBattleStruct.chosenItem + (gActiveBattler / 2) * 2), item);
              gBattleResources.battleHistory.trainerItems[i] = ITEM_NONE;
              return shouldUse;
          }
      }

      return FALSE;
}

// ─── callsTo manifest (= 30 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ABILITY_ON_FIELD2","ABILITY_ON_OPPOSING_FIELD","AI_CalcDmg","AI_TypeCalc","AreStatsRaised","BATTLE_OPPOSITE","BATTLE_PARTNER","BtlController_EmitTwoReturnValues","FindMonThatAbsorbsOpponentsMove","FindMonWithFlagsAndSuperEffective","GetAI_ItemType","GetBattlerAtPosition","GetBattlerPosition","GetBattlerSide","GetItemEffectParamOffset","GetMonData","GetMostSuitableMonToSwitchInto","HasSuperEffectiveMoveAgainstOpponents","IS_BATTLER_OF_TYPE","ModulateByTypeEffectiveness","Random","ShouldSwitch","ShouldSwitchIfNaturalCure","ShouldSwitchIfPerishSong","ShouldSwitchIfWonderGuard","ShouldUseItem","TYPE_EFFECT_ATK_TYPE","TYPE_EFFECT_DEF_TYPE","TYPE_EFFECT_MULTIPLIER","TypeCalc"];
