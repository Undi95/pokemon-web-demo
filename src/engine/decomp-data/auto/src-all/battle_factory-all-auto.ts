// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_factory.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 26 functions extracted from src/battle_factory.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, CalculateMonStats, CreateMon, CreateMonWithEVSpreadNatureOTID,
  DoBattleFactorySelectScreen, DoBattleFactorySwapScreen, GetBoxMonData, GetMonData,
  GetRandomScaledFrontierTrainerId, Random, SaveGameFrontier, SetBattleFacilityTrainerGfxId,
  SetDynamicWarp, SetFacilityPtrsGetLevel, SetMonData, SetMonMoveSlot,
  T1_READ_32, VarGet, VarSet, ZeroPlayerPartyMons,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void CallBattleFactoryFunction(void) */
export function CallBattleFactoryFunction(): any {
  sBattleFactoryFunctions[gSpecialVar_0x8004]();
}

/** static void InitFactoryChallenge(void) */
export function InitFactoryChallenge(): any {
  let i: any = null;
      let lvlMode: any = gSaveBlock2Ptr.frontier.lvlMode;
      let battleMode: any = VarGet(VAR_FRONTIER_BATTLE_MODE);

      gSaveBlock2Ptr.frontier.challengeStatus = 0;
      gSaveBlock2Ptr.frontier.curChallengeBattleNum = 0;
      gSaveBlock2Ptr.frontier.challengePaused = FALSE;
      gSaveBlock2Ptr.frontier.disableRecordBattle = FALSE;
      if (!(gSaveBlock2Ptr.frontier.winStreakActiveFlags & sWinStreakFlags[battleMode][lvlMode]))
      {
          gSaveBlock2Ptr.frontier.factoryWinStreaks[battleMode][lvlMode] = 0;
          gSaveBlock2Ptr.frontier.factoryRentsCount[battleMode][lvlMode] = 0;
      }

      sPerformedRentalSwap = FALSE;
      for (i = 0; i < ARRAY_COUNT(gSaveBlock2Ptr.frontier.rentalMons); i++)
          gSaveBlock2Ptr.frontier.rentalMons[i].monId = 0xFFFF;
      for (i = 0; i < FRONTIER_PARTY_SIZE; i++)
          gFrontierTempParty[i] = 0xFFFF;

      SetDynamicWarp(0, gSaveBlock1Ptr.location.mapGroup, gSaveBlock1Ptr.location.mapNum, WARP_ID_NONE);
      gTrainerBattleOpponent_A = 0;
}

/** static void GetBattleFactoryData(void) */
export function GetBattleFactoryData(): any {
  let lvlMode: any = gSaveBlock2Ptr.frontier.lvlMode;
      let battleMode: any = VarGet(VAR_FRONTIER_BATTLE_MODE);

      switch (gSpecialVar_0x8005)
      {
      case FACTORY_DATA_WIN_STREAK:
          gSpecialVar_Result = gSaveBlock2Ptr.frontier.factoryWinStreaks[battleMode][lvlMode];
          break;
      case FACTORY_DATA_WIN_STREAK_ACTIVE:
          gSpecialVar_Result = ((gSaveBlock2Ptr.frontier.winStreakActiveFlags & sWinStreakFlags[battleMode][lvlMode]) != 0);
          break;
      case FACTORY_DATA_WIN_STREAK_SWAPS:
          gSpecialVar_Result = gSaveBlock2Ptr.frontier.factoryRentsCount[battleMode][lvlMode];
          break;
      }
}

/** static void SetBattleFactoryData(void) */
export function SetBattleFactoryData(): any {
  let lvlMode: any = gSaveBlock2Ptr.frontier.lvlMode;
      let battleMode: any = VarGet(VAR_FRONTIER_BATTLE_MODE);

      switch (gSpecialVar_0x8005)
      {
      case FACTORY_DATA_WIN_STREAK:
          gSaveBlock2Ptr.frontier.factoryWinStreaks[battleMode][lvlMode] = gSpecialVar_0x8006;
          break;
      case FACTORY_DATA_WIN_STREAK_ACTIVE:
          if (gSpecialVar_0x8006)
              gSaveBlock2Ptr.frontier.winStreakActiveFlags |= sWinStreakFlags[battleMode][lvlMode];
          else
              gSaveBlock2Ptr.frontier.winStreakActiveFlags &= sWinStreakMasks[battleMode][lvlMode];
          break;
      case FACTORY_DATA_WIN_STREAK_SWAPS:
          if (sPerformedRentalSwap == TRUE)
          {
              gSaveBlock2Ptr.frontier.factoryRentsCount[battleMode][lvlMode] = gSpecialVar_0x8006;
              sPerformedRentalSwap = FALSE;
          }
          break;
      }
}

/** static void SaveFactoryChallenge(void) */
export function SaveFactoryChallenge(): any {
  gSaveBlock2Ptr.frontier.challengeStatus = gSpecialVar_0x8005;
      VarSet(VAR_TEMP_CHALLENGE_STATUS, 0);
      gSaveBlock2Ptr.frontier.challengePaused = TRUE;
      SaveGameFrontier();
}

/** static void SelectInitialRentalMons(void) */
export function SelectInitialRentalMons(): any {
  ZeroPlayerPartyMons();
      DoBattleFactorySelectScreen();
}

/** static void SwapRentalMons(void) */
export function SwapRentalMons(): any {
  DoBattleFactorySwapScreen();
}

/** static void SetPerformedRentalSwap(void) */
export function SetPerformedRentalSwap(): any {
  sPerformedRentalSwap = TRUE;
}

/** static void GenerateOpponentMons(void) */
export function GenerateOpponentMons(): any {
  let i, j, k;
      const species: any[] = [];
      const heldItems: any[] = [];
      let firstMonId: any = 0;
      let trainerId: any = 0;
      let lvlMode: any = gSaveBlock2Ptr.frontier.lvlMode;
      let battleMode: any = VarGet(VAR_FRONTIER_BATTLE_MODE);
      let winStreak: any = gSaveBlock2Ptr.frontier.factoryWinStreaks[battleMode][lvlMode];
      let challengeNum: any = winStreak / FRONTIER_STAGES_PER_CHALLENGE;
      gFacilityTrainers = gBattleFrontierTrainers;

      do
      {
           
          trainerId = GetRandomScaledFrontierTrainerId(challengeNum, gSaveBlock2Ptr.frontier.curChallengeBattleNum);
          for (i = 0; i < gSaveBlock2Ptr.frontier.curChallengeBattleNum; i++)
          {
              if (gSaveBlock2Ptr.frontier.trainerIds[i] == trainerId)
                  break;
          }
      } while (i != gSaveBlock2Ptr.frontier.curChallengeBattleNum);

      gTrainerBattleOpponent_A = trainerId;
      if (gSaveBlock2Ptr.frontier.curChallengeBattleNum < FRONTIER_STAGES_PER_CHALLENGE - 1)
          gSaveBlock2Ptr.frontier.trainerIds[gSaveBlock2Ptr.frontier.curChallengeBattleNum] = trainerId;

      i = 0;
      while (i != FRONTIER_PARTY_SIZE)
      {
          let monId: any = GetFactoryMonId(lvlMode, challengeNum, FALSE);

           
          if (gFacilityTrainerMons[monId].species == SPECIES_UNOWN)
              continue;

           
          for (j = 0; j < ARRAY_COUNT(gSaveBlock2Ptr.frontier.rentalMons); j++)
          {
              if (gFacilityTrainerMons[monId].species == gFacilityTrainerMons[gSaveBlock2Ptr.frontier.rentalMons[j].monId].species)
                  break;
          }
          if (j != ARRAY_COUNT(gSaveBlock2Ptr.frontier.rentalMons))
              continue;

           
          if (lvlMode == FRONTIER_LVL_50 && monId > FRONTIER_MONS_HIGH_TIER)
              continue;

           
          for (k = firstMonId; k < firstMonId + i; k++)
          {
              if (species[k] == gFacilityTrainerMons[monId].species)
                  break;
          }
          if (k != firstMonId + i)
              continue;

           
          for (k = firstMonId; k < firstMonId + i; k++)
          {
              if (heldItems[k] != ITEM_NONE && heldItems[k] == gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId])
                  break;
          }
          if (k != firstMonId + i)
              continue;

           
          species[i] = gFacilityTrainerMons[monId].species;
          heldItems[i] = gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId];
          gFrontierTempParty[i] = monId;
          i++;
      }
}

/** static void SetOpponentGfxVar(void) */
export function SetOpponentGfxVar(): any {
  SetBattleFacilityTrainerGfxId(gTrainerBattleOpponent_A, 0);
}

/** static void SetRentalsToOpponentParty(void) */
export function SetRentalsToOpponentParty(): any {
  let i: any = null;

      if (gSaveBlock2Ptr.frontier.lvlMode != FRONTIER_LVL_TENT)
          gFacilityTrainerMons = gBattleFrontierMons;
      else
          gFacilityTrainerMons = gSlateportBattleTentMons;

      for (i = 0; i < FRONTIER_PARTY_SIZE; i++)
      {
          gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].monId = gFrontierTempParty[i];
          gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].ivs = GetBoxMonData(gEnemyParty[i].box, MON_DATA_ATK_IV, NULL);
          gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].personality = GetMonData(gEnemyParty[i], MON_DATA_PERSONALITY, NULL);
          gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].abilityNum = GetBoxMonData(gEnemyParty[i].box, MON_DATA_ABILITY_NUM, NULL);
          SetMonData(gEnemyParty[i], MON_DATA_HELD_ITEM,gBattleFrontierHeldItems[gFacilityTrainerMons[gFrontierTempParty[i]].itemTableId]);
      }
}

/** static void SetPlayerAndOpponentParties(void) */
export function SetPlayerAndOpponentParties(): any {
  let i, j, k;
      let count: any = 0;
      let bits: any = 0;
      let monLevel: any = null;
      let monId: any = null;
      let evs: any = null;
      let ivs: any = null;
      let friendship: any = null;

      if (gSaveBlock2Ptr.frontier.lvlMode == FRONTIER_LVL_TENT)
      {
          gFacilityTrainerMons = gSlateportBattleTentMons;
          monLevel = TENT_MIN_LEVEL;
      }
      else
      {
          gFacilityTrainerMons = gBattleFrontierMons;
          if (gSaveBlock2Ptr.frontier.lvlMode != FRONTIER_LVL_50)
              monLevel = FRONTIER_MAX_LEVEL_OPEN;
          else
              monLevel = FRONTIER_MAX_LEVEL_50;
      }

      if (gSpecialVar_0x8005 < 2)
      {
          ZeroPlayerPartyMons();
          for (i = 0; i < FRONTIER_PARTY_SIZE; i++)
          {
              monId = gSaveBlock2Ptr.frontier.rentalMons[i].monId;
              ivs = gSaveBlock2Ptr.frontier.rentalMons[i].ivs;
              CreateMon(gPlayerParty[i],
                        gFacilityTrainerMons[monId].species,
                        monLevel,
                        ivs,
                        TRUE, gSaveBlock2Ptr.frontier.rentalMons[i].personality,
                        OT_ID_PLAYER_ID, 0);

              count = 0;
              bits = gFacilityTrainerMons[monId].evSpread;
              for (j = 0; j < NUM_STATS; bits >>= 1, j++)
              {
                  if (bits & 1)
                      count++;
              }

              evs = MAX_TOTAL_EVS / count;
              bits = 1;
              for (j = 0; j < NUM_STATS; bits <<= 1, j++)
              {
                  if (gFacilityTrainerMons[monId].evSpread & bits)
                      SetMonData(gPlayerParty[i], MON_DATA_HP_EV + j,evs);
              }

              CalculateMonStats(gPlayerParty[i]);
              friendship = 0;
              for (k = 0; k < MAX_MON_MOVES; k++)
                  SetMonMoveAvoidReturn(gPlayerParty[i], gFacilityTrainerMons[monId].moves[k], k);
              SetMonData(gPlayerParty[i], MON_DATA_FRIENDSHIP,friendship);
              SetMonData(gPlayerParty[i], MON_DATA_HELD_ITEM,gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId]);
              SetMonData(gPlayerParty[i], MON_DATA_ABILITY_NUM,gSaveBlock2Ptr.frontier.rentalMons[i].abilityNum);
          }
      }

      switch (gSpecialVar_0x8005)
      {
      case 0:
      case 2:
          for (i = 0; i < FRONTIER_PARTY_SIZE; i++)
          {
              monId = gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].monId;
              ivs = gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].ivs;
              CreateMon(gEnemyParty[i],
                        gFacilityTrainerMons[monId].species,
                        monLevel,
                        ivs,
                        TRUE, gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].personality,
                        OT_ID_PLAYER_ID, 0);

              count = 0;
              bits = gFacilityTrainerMons[monId].evSpread;
              for (j = 0; j < NUM_STATS; bits >>= 1, j++)
              {
                  if (bits & 1)
                      count++;
              }

              evs = MAX_TOTAL_EVS / count;
              bits = 1;
              for (j = 0; j < NUM_STATS; bits <<= 1, j++)
              {
                  if (gFacilityTrainerMons[monId].evSpread & bits)
                      SetMonData(gEnemyParty[i], MON_DATA_HP_EV + j,evs);
              }

              CalculateMonStats(gEnemyParty[i]);
              for (k = 0; k < MAX_MON_MOVES; k++)
                  SetMonMoveAvoidReturn(gEnemyParty[i], gFacilityTrainerMons[monId].moves[k], k);
              SetMonData(gEnemyParty[i], MON_DATA_HELD_ITEM,gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId]);
              SetMonData(gEnemyParty[i], MON_DATA_ABILITY_NUM,gSaveBlock2Ptr.frontier.rentalMons[i + FRONTIER_PARTY_SIZE].abilityNum);
          }
          break;
      }
}

/** static void GenerateInitialRentalMons(void) */
export function GenerateInitialRentalMons(): any {
  let i, j;
      let firstMonId: any = null;
      let battleMode: any = null;
      let lvlMode: any = null;
      let challengeNum: any = null;
      let factoryLvlMode: any = null;
      let factoryBattleMode: any = null;
      let rentalRank: any = null;
      let monId: any = null;
      let currSpecies: any = null;
      const species: any[] = [];
      const monIds: any[] = [];
      const heldItems: any[] = [];

      gFacilityTrainers = gBattleFrontierTrainers;
      for (i = 0; i < PARTY_SIZE; i++)
      {
          species[i] = SPECIES_NONE;
          monIds[i] = 0;
          heldItems[i] = ITEM_NONE;
      }
      lvlMode = gSaveBlock2Ptr.frontier.lvlMode;
      battleMode = VarGet(VAR_FRONTIER_BATTLE_MODE);
      challengeNum = gSaveBlock2Ptr.frontier.factoryWinStreaks[battleMode][lvlMode] / FRONTIER_STAGES_PER_CHALLENGE;
      if (VarGet(VAR_FRONTIER_BATTLE_MODE) == FRONTIER_MODE_DOUBLES)
          factoryBattleMode = FRONTIER_MODE_DOUBLES;
      else
          factoryBattleMode = FRONTIER_MODE_SINGLES;

      gFacilityTrainerMons = gBattleFrontierMons;
      if (gSaveBlock2Ptr.frontier.lvlMode != FRONTIER_LVL_50)
      {
          factoryLvlMode = FRONTIER_LVL_OPEN;
          firstMonId = 0;
      }
      else
      {
          factoryLvlMode = FRONTIER_LVL_50;
          firstMonId = 0;
      }
      rentalRank = GetNumPastRentalsRank(factoryBattleMode, factoryLvlMode);

      currSpecies = SPECIES_NONE;
      i = 0;
      while (i != PARTY_SIZE)
      {
          if (i < rentalRank)  
              monId = GetFactoryMonId(factoryLvlMode, challengeNum, TRUE);
          else
              monId = GetFactoryMonId(factoryLvlMode, challengeNum, FALSE);

          if (gFacilityTrainerMons[monId].species == SPECIES_UNOWN)
              continue;

           
          for (j = firstMonId; j < firstMonId + i; j++)
          {
              let existingMonId: any = monIds[j];
              if (existingMonId == monId)
                  break;
              if (species[j] == gFacilityTrainerMons[monId].species)
              {
                  if (currSpecies == SPECIES_NONE)
                      currSpecies = gFacilityTrainerMons[monId].species;
                  else
                      break;
              }
          }
          if (j != firstMonId + i)
              continue;

           
          for (j = firstMonId; j < firstMonId + i; j++)
          {
              if (heldItems[j] != ITEM_NONE && heldItems[j] == gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId])
              {
                  if (gFacilityTrainerMons[monId].species == currSpecies)
                      currSpecies = SPECIES_NONE;
                  break;
              }
          }
          if (j != firstMonId + i)
              continue;

          gSaveBlock2Ptr.frontier.rentalMons[i].monId = monId;
          species[i] = gFacilityTrainerMons[monId].species;
          heldItems[i] = gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId];
          monIds[i] = monId;
          i++;
      }
}

/** static void GetOpponentMostCommonMonType(void) */
export function GetOpponentMostCommonMonType(): any {
  let i: any = null;
      const typeCounts: any[] = [];
      const mostCommonTypes: any[] = [];

      gFacilityTrainerMons = gBattleFrontierMons;

       
      for (i = TYPE_NORMAL; i < NUMBER_OF_MON_TYPES; i++)
          typeCounts[i] = 0;
      for (i = 0; i < FRONTIER_PARTY_SIZE; i++)
      {
          let species: any = gFacilityTrainerMons[gFrontierTempParty[i]].species;
          typeCounts[gSpeciesInfo[species].types[0]]++;
          if (gSpeciesInfo[species].types[0] != gSpeciesInfo[species].types[1])
              typeCounts[gSpeciesInfo[species].types[1]]++;
      }

       
       
       
      mostCommonTypes[0] = 0;
      mostCommonTypes[1] = 0;
      for (i = 1; i < NUMBER_OF_MON_TYPES; i++)
      {
          if (typeCounts[mostCommonTypes[0]] < typeCounts[i])
              mostCommonTypes[0] = i;
          else if (typeCounts[mostCommonTypes[0]] == typeCounts[i])
              mostCommonTypes[1] = i;
      }

      if (typeCounts[mostCommonTypes[0]] != 0)
      {
           
           
           
          if (typeCounts[mostCommonTypes[0]] > typeCounts[mostCommonTypes[1]])
              gSpecialVar_Result = mostCommonTypes[0];
          else if (mostCommonTypes[0] == mostCommonTypes[1])
              gSpecialVar_Result = mostCommonTypes[0];
          else
              gSpecialVar_Result = NUMBER_OF_MON_TYPES;
      }
      else
      {
          gSpecialVar_Result = NUMBER_OF_MON_TYPES;
      }
}

/** static void GetOpponentBattleStyle(void) */
export function GetOpponentBattleStyle(): any {
  let i, j, count;
      const stylePoints: any[] = [];

      count = 0;
      gFacilityTrainerMons = gBattleFrontierMons;
      for (i = 0; i < FACTORY_NUM_STYLES; i++)
          stylePoints[i] = 0;

      for (i = 0; i < FRONTIER_PARTY_SIZE; i++)
      {
          let monId: any = gFrontierTempParty[i];
          for (j = 0; j < MAX_MON_MOVES; j++)
          {
              let battleStyle: any = GetMoveBattleStyle(gFacilityTrainerMons[monId].moves[j]);
              stylePoints[battleStyle]++;
          }
      }

      gSpecialVar_Result = FACTORY_STYLE_NONE;
      for (i = 1; i < FACTORY_NUM_STYLES; i++)
      {
          if (stylePoints[i] >= sRequiredMoveCounts[i - 1])
          {
              gSpecialVar_Result = i;
              count++;
          }
      }

       
      if (count > 2)
          gSpecialVar_Result = FACTORY_NUM_STYLES;
}

/** static u8 GetMoveBattleStyle(u16 move) */
export function GetMoveBattleStyle(move: any): any {
  let moves: any = null;
      let i, j;

      for (i = 0; i < ARRAY_COUNT(sMoveStyles); i++)
      {
          for (j = 0, moves = sMoveStyles[i]; moves[j] != MOVE_NONE; j++)
          {
              if (moves[j] == move)
                  return i + 1;
          }
      }
      return FACTORY_STYLE_NONE;
}

/** bool8 InBattleFactory(void) */
export function InBattleFactory(): any {
  return gMapHeader.mapLayoutId == LAYOUT_BATTLE_FRONTIER_BATTLE_FACTORY_PRE_BATTLE_ROOM
          || gMapHeader.mapLayoutId == LAYOUT_BATTLE_FRONTIER_BATTLE_FACTORY_BATTLE_ROOM;
}

/** static void RestorePlayerPartyHeldItems(void) */
export function RestorePlayerPartyHeldItems(): any {
  let i: any = null;

      if (gSaveBlock2Ptr.frontier.lvlMode != FRONTIER_LVL_TENT)
          gFacilityTrainerMons = gBattleFrontierMons;
      else
          gFacilityTrainerMons = gSlateportBattleTentMons;

      for (i = 0; i < FRONTIER_PARTY_SIZE; i++)
      {
          SetMonData(gPlayerParty[i],
                     MON_DATA_HELD_ITEM,gBattleFrontierHeldItems[gFacilityTrainerMons[gSaveBlock2Ptr.frontier.rentalMons[i].monId].itemTableId]);
      }
}

/** u8 GetFactoryMonFixedIV(u8 challengeNum, bool8 isLastBattle) */
export function GetFactoryMonFixedIV(challengeNum: any, isLastBattle: any): any {
  let ivSet: any = null;
      let useHigherIV: any = isLastBattle ? TRUE : FALSE;
      if (challengeNum >= ARRAY_COUNT(sFixedIVTable))
          ivSet = ARRAY_COUNT(sFixedIVTable) - 1;
      else
          ivSet = challengeNum;

      return sFixedIVTable[ivSet][useHigherIV];
}

/** void FillFactoryBrainParty(void) */
export function FillFactoryBrainParty(): any {
  let i, j, k;
      const species: any[] = [];
      const heldItems: any[] = [];
      let friendship: any = null;
      let monLevel: any = null;
      let fixedIV: any = null;
      let otId: any = null;

      let lvlMode: any = gSaveBlock2Ptr.frontier.lvlMode;
      let battleMode: any = VarGet(VAR_FRONTIER_BATTLE_MODE);
      let challengeNum: any = gSaveBlock2Ptr.frontier.factoryWinStreaks[battleMode][lvlMode] / FRONTIER_STAGES_PER_CHALLENGE;
      fixedIV = GetFactoryMonFixedIV(challengeNum + 2, FALSE);
      monLevel = SetFacilityPtrsGetLevel();
      i = 0;
      otId = T1_READ_32(gSaveBlock2Ptr.playerTrainerId);

      while (i != FRONTIER_PARTY_SIZE)
      {
          let monId: any = GetFactoryMonId(lvlMode, challengeNum, FALSE);

          if (gFacilityTrainerMons[monId].species == SPECIES_UNOWN)
              continue;
          if (monLevel == FRONTIER_MAX_LEVEL_50 && monId > FRONTIER_MONS_HIGH_TIER)
              continue;

          for (j = 0; j < ARRAY_COUNT(gSaveBlock2Ptr.frontier.rentalMons); j++)
          {
              if (monId == gSaveBlock2Ptr.frontier.rentalMons[j].monId)
                  break;
          }
          if (j != ARRAY_COUNT(gSaveBlock2Ptr.frontier.rentalMons))
              continue;

          for (k = 0; k < i; k++)
          {
              if (species[k] == gFacilityTrainerMons[monId].species)
                  break;
          }
          if (k != i)
              continue;

          for (k = 0; k < i; k++)
          {
              if (heldItems[k] != ITEM_NONE && heldItems[k] == gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId])
                  break;
          }
          if (k != i)
              continue;

          species[i] = gFacilityTrainerMons[monId].species;
          heldItems[i] = gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId];
          CreateMonWithEVSpreadNatureOTID(gEnemyParty[i],
                                               gFacilityTrainerMons[monId].species,
                                               monLevel,
                                               gFacilityTrainerMons[monId].nature,
                                               fixedIV,
                                               gFacilityTrainerMons[monId].evSpread,
                                               otId);

          friendship = 0;
          for (k = 0; k < MAX_MON_MOVES; k++)
              SetMonMoveAvoidReturn(gEnemyParty[i], gFacilityTrainerMons[monId].moves[k], k);
          SetMonData(gEnemyParty[i], MON_DATA_FRIENDSHIP,friendship);
          SetMonData(gEnemyParty[i], MON_DATA_HELD_ITEM,gBattleFrontierHeldItems[gFacilityTrainerMons[monId].itemTableId]);
          i++;
      }
}

/** static u16 GetFactoryMonId(u8 lvlMode, u8 challengeNum, bool8 useBetterRange) */
export function GetFactoryMonId(lvlMode: any, challengeNum: any, useBetterRange: any): any {
  let numMons, monId;
      let adder: any = null;  

      if (lvlMode == FRONTIER_LVL_50)
          adder = 0;
      else
          adder = 8;

      if (challengeNum < 7)
      {
          if (useBetterRange)
          {
              numMons = (sInitialRentalMonRanges[adder + challengeNum + 1][1] - sInitialRentalMonRanges[adder + challengeNum + 1][0]) + 1;
              monId = Random() % numMons;
              monId += sInitialRentalMonRanges[adder + challengeNum + 1][0];
          }
          else
          {
              numMons = (sInitialRentalMonRanges[adder + challengeNum][1] - sInitialRentalMonRanges[adder + challengeNum][0]) + 1;
              monId = Random() % numMons;
              monId += sInitialRentalMonRanges[adder + challengeNum][0];
          }
      }
      else
      {
          let challenge: any = challengeNum;
          if (challenge != 7)
              challenge = 7;  

          numMons = (sInitialRentalMonRanges[adder + challenge][1] - sInitialRentalMonRanges[adder + challenge][0]) + 1;
          monId = Random() % numMons;
          monId += sInitialRentalMonRanges[adder + challenge][0];
      }

      return monId;
}

/** u8 GetNumPastRentalsRank(u8 battleMode, u8 lvlMode) */
export function GetNumPastRentalsRank(battleMode: any, lvlMode: any): any {
  let ret: any = null;
      let rents: any = gSaveBlock2Ptr.frontier.factoryRentsCount[battleMode][lvlMode];

      if (rents < 15)
          ret = 0;
      else if (rents < 22)
          ret = 1;
      else if (rents < 29)
          ret = 2;
      else if (rents < 36)
          ret = 3;
      else if (rents < 43)
          ret = 4;
      else
          ret = 5;

      return ret;
}

/** u32 GetAiScriptsInBattleFactory(void) */
export function GetAiScriptsInBattleFactory(): any {
  let lvlMode: any = gSaveBlock2Ptr.frontier.lvlMode;

      if (lvlMode == FRONTIER_LVL_TENT)
      {
          return 0;
      }
      else
      {
          let battleMode: any = VarGet(VAR_FRONTIER_BATTLE_MODE);
          let challengeNum: any = gSaveBlock2Ptr.frontier.factoryWinStreaks[battleMode][lvlMode] / FRONTIER_STAGES_PER_CHALLENGE;

          if (gTrainerBattleOpponent_A == TRAINER_FRONTIER_BRAIN)
              return AI_SCRIPT_CHECK_BAD_MOVE | AI_SCRIPT_TRY_TO_FAINT | AI_SCRIPT_CHECK_VIABILITY;
          else if (challengeNum < 2)
              return 0;
          else if (challengeNum < 4)
              return AI_SCRIPT_CHECK_BAD_MOVE;
          else
              return AI_SCRIPT_CHECK_BAD_MOVE | AI_SCRIPT_TRY_TO_FAINT | AI_SCRIPT_CHECK_VIABILITY;
      }
}

/** void SetMonMoveAvoidReturn(struct Pokemon *mon, u16 moveArg, u8 moveSlot) */
export function SetMonMoveAvoidReturn(mon: any, moveArg: any, moveSlot: any): any {
  let move: any = moveArg;
      if (moveArg == MOVE_RETURN)
          move = MOVE_FRUSTRATION;
      SetMonMoveSlot(mon, move, moveSlot);
}

// ─── callsTo manifest (= 25 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","CalculateMonStats","CreateMon","CreateMonWithEVSpreadNatureOTID","DoBattleFactorySelectScreen","DoBattleFactorySwapScreen","GetBoxMonData","GetFactoryMonFixedIV","GetFactoryMonId","GetMonData","GetMoveBattleStyle","GetNumPastRentalsRank","GetRandomScaledFrontierTrainerId","Random","SaveGameFrontier","SetBattleFacilityTrainerGfxId","SetDynamicWarp","SetFacilityPtrsGetLevel","SetMonData","SetMonMoveAvoidReturn","SetMonMoveSlot","T1_READ_32","VarGet","VarSet","ZeroPlayerPartyMons"];

// ─── Failed transpiles (2) ───────────────────────────────
// FactoryDummy1 : empty body
// FactoryDummy2 : empty body