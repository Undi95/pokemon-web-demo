// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/fldeff_escalator.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 6 functions extracted from src/fldeff_escalator.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let sEscalatorAnim_TaskId: any = null;
let tDrawingEscalator: any = null;
let tGoingUp: any = null;
let tState: any = null;
let tTransitionStage: any = null;
/** static void SetEscalatorMetatile(u8 taskId, const s16 *metatileIds, u16 metatileMasks) */
export function SetEscalatorMetatile(taskId: any, metatileIds: any, metatileMasks: any): any {
  let x: any = gTasks[taskId].tPlayerX - 1;
      let y: any = gTasks[taskId].tPlayerY - 1;
      let transitionStage: any = gTasks[taskId].tTransitionStage;
      let i: any = null;
      let j: any = null;

       
      if (!gTasks[taskId].tGoingUp)
      {
          for (i = 0; i < 3; i++)
          {
              for (j = 0; j < 3; j++)
              {
                  let metatileId: any = MapGridGetMetatileIdAt(x + j, y + i);

                  if (metatileIds[transitionStage] == metatileId)
                  {
                      if (transitionStage != (((3) - 1)))
                          MapGridSetMetatileIdAt(x + j, y + i, metatileMasks | metatileIds[transitionStage + 1]);
                      else
                          MapGridSetMetatileIdAt(x + j, y + i, metatileMasks | metatileIds[0]);
                  }
              }
          }
      }
      else
      {
          for (i = 0; i < 3; i++)
          {
              for (j = 0; j < 3; j++)
              {
                  let metatileId: any = MapGridGetMetatileIdAt(x + j, y + i);

                  if (metatileIds[(((3) - 1)) - transitionStage] == metatileId)
                  {
                      if (transitionStage != (((3) - 1)))
                          MapGridSetMetatileIdAt(x + j, y + i, metatileMasks | metatileIds[1 - transitionStage]);
                      else
                          MapGridSetMetatileIdAt(x + j, y + i, metatileMasks | metatileIds[(((3) - 1))]);
                  }
              }
          }
      }
}

/** static void Task_DrawEscalator(u8 taskId) */
export function Task_DrawEscalator(taskId: any): any {
  let data: any = gTasks[taskId].data;

      tDrawingEscalator = TRUE;

       
      switch (tState)
      {
          case 0:
              SetEscalatorMetatile(taskId, sEscalatorMetatiles_1F_0, 0);
              break;
          case 1:
              SetEscalatorMetatile(taskId, sEscalatorMetatiles_1F_1, 0);
              break;
          case 2:
              SetEscalatorMetatile(taskId, sEscalatorMetatiles_1F_2, ((0x0C00)));
              break;
          case 3:
              SetEscalatorMetatile(taskId, sEscalatorMetatiles_1F_3, 0);
              break;
          case 4:
              SetEscalatorMetatile(taskId, sEscalatorMetatiles_2F_0, ((0x0C00)));
              break;
          case 5:
              SetEscalatorMetatile(taskId, sEscalatorMetatiles_2F_1, 0);
              break;
          case 6:
              SetEscalatorMetatile(taskId, sEscalatorMetatiles_2F_2, 0);
              break;
      }

      tState = (tState + 1) & 7;

       
      if (tState == 0)
      {
          DrawWholeMapView();
          tTransitionStage = (tTransitionStage + 1) % (3);
          tDrawingEscalator = FALSE;
      }
}

/** static u8 CreateEscalatorTask(bool16 goingUp) */
export function CreateEscalatorTask(goingUp: any): any {
  let taskId: any = CreateTask(Task_DrawEscalator, 0);
      let data: any = gTasks[taskId].data;

      PlayerGetDestCoords(tPlayerX,tPlayerY);
      tState = 0;
      tTransitionStage = 0;
      tGoingUp = goingUp;
      Task_DrawEscalator(taskId);
      return taskId;
}

/** void StartEscalator(bool8 goingUp) */
export function StartEscalator(goingUp: any): any {
  sEscalatorAnim_TaskId = CreateEscalatorTask(goingUp);
}

/** void StopEscalator(void) */
export function StopEscalator(): any {
  DestroyTask(sEscalatorAnim_TaskId);
}

/** bool8 IsEscalatorMoving(void) */
export function IsEscalatorMoving(): any {
  if (gTasks[sEscalatorAnim_TaskId].tDrawingEscalator == FALSE
       && gTasks[sEscalatorAnim_TaskId].tTransitionStage == (((3) - 1)))
          return FALSE;
      else
          return TRUE;
}

// ─── callsTo manifest (= 9 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["CreateEscalatorTask","CreateTask","DestroyTask","DrawWholeMapView","MapGridGetMetatileIdAt","MapGridSetMetatileIdAt","PlayerGetDestCoords","SetEscalatorMetatile","Task_DrawEscalator"];
