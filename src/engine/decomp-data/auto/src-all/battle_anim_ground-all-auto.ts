// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_anim_ground.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 25 functions extracted from src/battle_anim_ground.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  BATTLE_PARTNER, CreateTask, DISPLAY_HEIGHT, DestroyAnimSprite,
  DestroyAnimVisualTask, DestroyTask, FALSE, GetAnimBattlerSpriteId,
  GetBattlerSide, GetBattlerSpriteBGPriorityRank, GetBattlerSpriteCoord, GetBattlerSpriteCoord2,
  GetBattlerYCoordWithElevation, InitAnimArcTranslation, InitSpritePosToAnimAttacker, InitSpritePosToAnimTarget,
  IsBattlerSpriteVisible, Random2, ScanlineEffect_SetParams, StoreSpriteCallbackInData6,
  TRUE, TranslateAnimHorizontalArc, gSineTable,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static void AnimBonemerangProjectile(struct Sprite *sprite) */
export function AnimBonemerangProjectile(sprite: any): any {
  sprite.x = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2);
      sprite.y = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_Y_PIC_OFFSET);
      sprite.data[0] = 20;
      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2);
      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET);
      sprite.data[5] = -40;
      InitAnimArcTranslation(sprite);
      sprite.callback = AnimBonemerangProjectile_Step;
}

/** static void AnimBonemerangProjectile_Step(struct Sprite *sprite) */
export function AnimBonemerangProjectile_Step(sprite: any): any {
  if (TranslateAnimHorizontalArc(sprite))
      {
          sprite.x += sprite.x2;
          sprite.y += sprite.y2;
          sprite.y2 = 0;
          sprite.x2 = 0;
          sprite.data[0] = 20;
          sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2);
          sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_Y_PIC_OFFSET);
          sprite.data[5] = 40;
          InitAnimArcTranslation(sprite);
          sprite.callback = AnimBonemerangProjectile_End;
      }
}

/** static void AnimBonemerangProjectile_End(struct Sprite *sprite) */
export function AnimBonemerangProjectile_End(sprite: any): any {
  if (TranslateAnimHorizontalArc(sprite))
          DestroyAnimSprite(sprite);
}

/** static void AnimBoneHitProjectile(struct Sprite *sprite) */
export function AnimBoneHitProjectile(sprite: any): any {
  InitSpritePosToAnimTarget(sprite, TRUE);
      if (GetBattlerSide(gBattleAnimAttacker) != B_SIDE_PLAYER)
          gBattleAnimArgs[2] = -gBattleAnimArgs[2];

      sprite.data[0] = gBattleAnimArgs[4];
      sprite.data[2] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_X_2) + gBattleAnimArgs[2];
      sprite.data[4] = GetBattlerSpriteCoord(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET) + gBattleAnimArgs[3];
      sprite.callback = StartAnimLinearTranslation;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
}

/** static void AnimDirtScatter(struct Sprite *sprite) */
export function AnimDirtScatter(sprite: any): any {
  let targetXPos, targetYPos;
      let xOffset, yOffset;

      InitSpritePosToAnimAttacker(sprite, TRUE);

      targetXPos = GetBattlerSpriteCoord2(gBattleAnimTarget, BATTLER_COORD_X_2);
      targetYPos = GetBattlerSpriteCoord2(gBattleAnimTarget, BATTLER_COORD_Y_PIC_OFFSET);

      xOffset = Random2() & 0x1F;
      yOffset = Random2() & 0x1F;
      if (xOffset > 16)
          xOffset = 16 - xOffset;
      if (yOffset > 16)
          yOffset = 16 - yOffset;

      sprite.data[0] = gBattleAnimArgs[2];
      sprite.data[2] = targetXPos + xOffset;
      sprite.data[4] = targetYPos + yOffset;
      sprite.callback = StartAnimLinearTranslation;
      StoreSpriteCallbackInData6(sprite, DestroySpriteAndMatrix);
}

/** static void AnimMudSportDirt(struct Sprite *sprite) */
export function AnimMudSportDirt(sprite: any): any {
  sprite.oam.tileNum++;
      if (gBattleAnimArgs[0] == 0)
      {
          sprite.x = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_X_2) + gBattleAnimArgs[1];
          sprite.y = GetBattlerSpriteCoord(gBattleAnimAttacker, BATTLER_COORD_Y_PIC_OFFSET) + gBattleAnimArgs[2];
          sprite.data[0] = gBattleAnimArgs[1] > 0 ? 1 : -1;
          sprite.callback = AnimMudSportDirtRising;
      }
      else
      {
          sprite.x = gBattleAnimArgs[1];
          sprite.y = gBattleAnimArgs[2];
          sprite.y2 = -gBattleAnimArgs[2];
          sprite.callback = AnimMudSportDirtFalling;
      }
}

/** static void AnimMudSportDirtRising(struct Sprite *sprite) */
export function AnimMudSportDirtRising(sprite: any): any {
  if (++sprite.data[1] > 1)
      {
          sprite.data[1] = 0;
          sprite.x += sprite.data[0];
      }

      sprite.y -= 4;
      if (sprite.y < -4)
          DestroyAnimSprite(sprite);
}

/** static void AnimMudSportDirtFalling(struct Sprite *sprite) */
export function AnimMudSportDirtFalling(sprite: any): any {
  switch (sprite.data[0])
      {
      case 0:
          sprite.y2 += 4;
          if (sprite.y2 >= 0)
          {
              sprite.y2 = 0;
              sprite.data[0]++;
          }
          break;
      case 1:
          if (++sprite.data[1] > 0)
          {
              sprite.data[1] = 0;
              sprite.invisible ^= 1;
              if (++sprite.data[2] == 10)
                  DestroyAnimSprite(sprite);
          }
          break;
      }
}

/** void AnimTask_DigDownMovement(u8 taskId) */
export function AnimTask_DigDownMovement(taskId: any): any {
  let task: any =gTasks[taskId];

      if (gBattleAnimArgs[0] == FALSE)
          task.func = AnimTask_DigBounceMovement;
      else
          task.func = AnimTask_DigEndBounceMovementSetInvisible;

      task.func(taskId);
}

/** static void AnimTask_DigBounceMovement(u8 taskId) */
export function AnimTask_DigBounceMovement(taskId: any): any {
  let y: any = null;
      let task: any =gTasks[taskId];

      switch (task.data[0])
      {
      case 0:
          task.data[10] = GetAnimBattlerSpriteId(ANIM_ATTACKER);
          task.data[11] = GetBattlerSpriteBGPriorityRank(gBattleAnimAttacker);
          if (task.data[11] == 1)
          {
              task.data[12] = gBattle_BG1_X;
              task.data[13] = gBattle_BG1_Y;
          }
          else
          {
              task.data[12] = gBattle_BG2_X;
              task.data[13] = gBattle_BG2_Y;
          }

          y = GetBattlerYCoordWithElevation(gBattleAnimAttacker);
          task.data[14] = y - 32;
          task.data[15] = y + 32;
          if (task.data[14] < 0)
              task.data[14] = 0;

          gSprites[task.data[10]].invisible = TRUE;
          task.data[0]++;
          break;
      case 1:
          SetDigScanlineEffect(task.data[11], task.data[14], task.data[15]);
          task.data[0]++;
          break;
      case 2:
          task.data[2] = (task.data[2] + 6) & 0x7F;
          if (++task.data[4] > 2)
          {
              task.data[4] = 0;
              task.data[3]++;
          }

          task.data[5] = task.data[3] + (gSineTable[task.data[2]] >> 4);
          if (task.data[11] == 1)
              gBattle_BG1_Y = task.data[13] - task.data[5];
          else
              gBattle_BG2_Y = task.data[13] - task.data[5];

          if (task.data[5] > 63)
          {
              task.data[5] = 120 - task.data[14];
              if (task.data[11] == 1)
                  gBattle_BG1_Y = task.data[13] - task.data[5];
              else
                  gBattle_BG2_Y = task.data[13] - task.data[5];

              gSprites[task.data[10]].x2 = DISPLAY_WIDTH + 32 - gSprites[task.data[10]].x;
              task.data[0]++;
          }
          break;
      case 3:
          gScanlineEffect.state = 3;
          task.data[0]++;
          break;
      case 4:
          DestroyAnimVisualTask(taskId);
          gSprites[task.data[10]].invisible = TRUE;
          break;
      }
}

/** static void AnimTask_DigEndBounceMovementSetInvisible(u8 taskId) */
export function AnimTask_DigEndBounceMovementSetInvisible(taskId: any): any {
  let spriteId: any = GetAnimBattlerSpriteId(ANIM_ATTACKER);
      gSprites[spriteId].invisible = TRUE;
      gSprites[spriteId].x2 = 0;
      gSprites[spriteId].y2 = 0;

      if (GetBattlerSpriteBGPriorityRank(gBattleAnimAttacker) == 1)
          gBattle_BG1_Y = 0;
      else
          gBattle_BG2_Y = 0;

      DestroyAnimVisualTask(taskId);
}

/** void AnimTask_DigUpMovement(u8 taskId) */
export function AnimTask_DigUpMovement(taskId: any): any {
  let task: any =gTasks[taskId];

      if (gBattleAnimArgs[0] == FALSE)
          task.func = AnimTask_DigSetVisibleUnderground;
      else
          task.func = AnimTask_DigRiseUpFromHole;

      task.func(taskId);
}

/** static void AnimTask_DigSetVisibleUnderground(u8 taskId) */
export function AnimTask_DigSetVisibleUnderground(taskId: any): any {
  let task: any =gTasks[taskId];

      switch (task.data[0])
      {
      case 0:
          task.data[10] = GetAnimBattlerSpriteId(ANIM_ATTACKER);
          gSprites[task.data[10]].invisible = FALSE;
          gSprites[task.data[10]].x2 = 0;
          gSprites[task.data[10]].y2 = DISPLAY_HEIGHT - gSprites[task.data[10]].y;
          task.data[0]++;
          break;
      case 1:
          DestroyAnimVisualTask(taskId);
      }
}

/** static void AnimTask_DigRiseUpFromHole(u8 taskId) */
export function AnimTask_DigRiseUpFromHole(taskId: any): any {
  let var0: any = null;
      let task: any =gTasks[taskId];

      switch (task.data[0])
      {
      case 0:
          task.data[10] = GetAnimBattlerSpriteId(ANIM_ATTACKER);
          task.data[11] = GetBattlerSpriteBGPriorityRank(gBattleAnimAttacker);
          if (task.data[11] == 1)
              task.data[12] = gBattle_BG1_X;
          else
              task.data[12] = gBattle_BG2_X;

          var0 =  GetBattlerYCoordWithElevation(gBattleAnimAttacker);
          task.data[14] = var0 - 32;
          task.data[15] = var0 + 32;
          task.data[0]++;
          break;
      case 1:
          SetDigScanlineEffect(task.data[11], 0, task.data[15]);
          task.data[0]++;
          break;
      case 2:
          gSprites[task.data[10]].y2 = 96;
          task.data[0]++;
          break;
      case 3:
          gSprites[task.data[10]].y2 -= 8;
          if (gSprites[task.data[10]].y2 == 0)
          {
              gScanlineEffect.state = 3;
              task.data[0]++;
          }
          break;
      case 4:
          DestroyAnimVisualTask(taskId);
          break;
      }
}

/** static void SetDigScanlineEffect(u8 useBG1, s16 y, s16 endY) */
export function SetDigScanlineEffect(useBG1: any, y: any, endY: any): any {
  let bgX: any = null;
      let scanlineParams: any = null;

      if (useBG1 == 1)
      {
          bgX = gBattle_BG1_X;
          scanlineParams.dmaDest =REG_BG1HOFS;
      }
      else
      {
          bgX = gBattle_BG2_X;
          scanlineParams.dmaDest =REG_BG2HOFS;
      }

      if (y < 0)
          y = 0;

      while (y < endY)
      {
          gScanlineEffectRegBuffers[0][y] = bgX;
          gScanlineEffectRegBuffers[1][y] = bgX;
          y++;
      }

      while (y < DISPLAY_HEIGHT)
      {
          gScanlineEffectRegBuffers[0][y] = bgX + DISPLAY_WIDTH;
          gScanlineEffectRegBuffers[1][y] = bgX + DISPLAY_WIDTH;
          y++;
      }

      scanlineParams.dmaControl = SCANLINE_EFFECT_DMACNT_16BIT;
      scanlineParams.initState = 1;
      scanlineParams.unused9 = 0;
      ScanlineEffect_SetParams(scanlineParams);
}

/** void AnimDirtPlumeParticle(struct Sprite *sprite) */
export function AnimDirtPlumeParticle(sprite: any): any {
  let battler: any = null;
      let xOffset: any = null;

      if (gBattleAnimArgs[0] == 0)
          battler = gBattleAnimAttacker;
      else
          battler = gBattleAnimTarget;

      xOffset = 24;
      if (gBattleAnimArgs[1] == 1)
      {
          xOffset *= -1;
          gBattleAnimArgs[2] *= -1;
      }

      sprite.x = GetBattlerSpriteCoord(battler, BATTLER_COORD_X_2) + xOffset;
      sprite.y = GetBattlerYCoordWithElevation(battler) + 30;
      sprite.data[0] = gBattleAnimArgs[5];
      sprite.data[2] = sprite.x + gBattleAnimArgs[2];
      sprite.data[4] = sprite.y + gBattleAnimArgs[3];
      sprite.data[5] = gBattleAnimArgs[4];
      InitAnimArcTranslation(sprite);
      sprite.callback = AnimDirtPlumeParticle_Step;
}

/** static void AnimDirtPlumeParticle_Step(struct Sprite *sprite) */
export function AnimDirtPlumeParticle_Step(sprite: any): any {
  if (TranslateAnimHorizontalArc(sprite))
          DestroyAnimSprite(sprite);
}

/** static void AnimDigDirtMound(struct Sprite *sprite) */
export function AnimDigDirtMound(sprite: any): any {
  let battler: any = null;

      if (gBattleAnimArgs[0] == 0)
          battler = gBattleAnimAttacker;
      else
          battler = gBattleAnimTarget;

      sprite.x = GetBattlerSpriteCoord(battler, BATTLER_COORD_X) - 16 + (gBattleAnimArgs[1] * 32);
      sprite.y = GetBattlerYCoordWithElevation(battler) + 32;
      sprite.oam.tileNum += gBattleAnimArgs[1] * 8;
      StoreSpriteCallbackInData6(sprite, DestroyAnimSprite);
      sprite.data[0] = gBattleAnimArgs[2];
      sprite.callback = WaitAnimForDuration;
}

/** void AnimTask_HorizontalShake(u8 taskId) */
export function AnimTask_HorizontalShake(taskId: any): any {
  let i: any = null;
      let task: any =gTasks[taskId];

      if (gBattleAnimArgs[1] != 0)
          task.tHorizOffset = task.tInitHorizOffset = gBattleAnimArgs[1] + 3;
      else
          task.tHorizOffset = task.tInitHorizOffset = (gAnimMovePower / 10) + 3;

      task.tMaxTime = gBattleAnimArgs[2];
      switch (gBattleAnimArgs[0])
      {
      case MAX_BATTLERS_COUNT + 1:  
          task.tInitialX = gBattle_BG3_X;
          task.func = AnimTask_ShakePlatforms;
          break;
      case MAX_BATTLERS_COUNT:  
          task.tNumBattlers = 0;
          for (i = 0; i < MAX_BATTLERS_COUNT; i++)
          {
              if (IsBattlerSpriteVisible(i))
              {
                  task.tbattlerSpriteIds(task.tNumBattlers) = gBattlerSpriteIds[i];
                  task.tNumBattlers++;
              }
          }
          task.func = AnimTask_ShakeBattlers;
          break;
      default:  
          task.tbattlerSpriteIds(0) = GetAnimBattlerSpriteId(gBattleAnimArgs[0]);
          if (task.tbattlerSpriteIds(0) == SPRITE_NONE)
          {
              DestroyAnimVisualTask(taskId);
          }
          else
          {
              task.tNumBattlers = 1;
              task.func = AnimTask_ShakeBattlers;
          }
          break;
      }
}

/** static void AnimTask_ShakePlatforms(u8 taskId) */
export function AnimTask_ShakePlatforms(taskId: any): any {
  let task: any =gTasks[taskId];

      switch (task.tState)
      {
      case 0:
          if (++task.tDelay > 1)
          {
              task.tDelay = 0;
              if ((task.tTimer & 1) == 0)
                  gBattle_BG3_X = task.tInitialX + task.tInitHorizOffset;
              else
                  gBattle_BG3_X = task.tInitialX - task.tInitHorizOffset;

              if (++task.tTimer == task.tMaxTime)
              {
                  task.tTimer = 0;
                  task.tHorizOffset--;
                  task.tState++;
              }
          }
          break;
      case 1:
          if (++task.tDelay > 1)
          {
              task.tDelay = 0;
              if ((task.tTimer & 1) == 0)
                  gBattle_BG3_X = task.tInitialX + task.tHorizOffset;
              else
                  gBattle_BG3_X = task.tInitialX - task.tHorizOffset;

              if (++task.tTimer == 4)
              {
                  task.tTimer = 0;
                  if (--task.tHorizOffset == 0)
                      task.tState++;
              }
          }
          break;
      case 2:
          gBattle_BG3_X = task.tInitialX;
          DestroyAnimVisualTask(taskId);
          break;
      }
}

/** static void AnimTask_ShakeBattlers(u8 taskId) */
export function AnimTask_ShakeBattlers(taskId: any): any {
  let i: any = null;
      let task: any =gTasks[taskId];

      switch (task.tState)
      {
      case 0:
          if (++task.tDelay > 1)
          {
              task.tDelay = 0;
              SetBattlersXOffsetForShake(task);
              if (++task.tTimer == task.tMaxTime)
              {
                  task.tTimer = 0;
                  task.tHorizOffset--;
                  task.tState++;
              }
          }
          break;
      case 1:
          if (++task.tDelay > 1)
          {
              task.tDelay = 0;
              SetBattlersXOffsetForShake(task);
              if (++task.tTimer == 4)
              {
                  task.tTimer = 0;
                  if (--task.tHorizOffset == 0)
                      task.tState++;
              }
          }
          break;
      case 2:
          for (i = 0; i < task.tNumBattlers; i++)
              gSprites[task.tbattlerSpriteIds(i)].x2 = 0;

          DestroyAnimVisualTask(taskId);
          break;
      }
}

/** static void SetBattlersXOffsetForShake(struct Task *task) */
export function SetBattlersXOffsetForShake(task: any): any {
  let i: any = null;
      let xOffset: any = null;

      if ((task.tTimer & 1) == 0)
          xOffset = (task.tHorizOffset / 2) + (task.tHorizOffset & 1);
      else
          xOffset = -(task.tHorizOffset / 2);

      for (i = 0; i < task.tNumBattlers; i++)
      {
          gSprites[task.tbattlerSpriteIds(i)].x2 = xOffset;
      }
}

/** void AnimTask_IsPowerOver99(u8 taskId) */
export function AnimTask_IsPowerOver99(taskId: any): any {
  gBattleAnimArgs[15] = gAnimMovePower > 99;
      DestroyAnimVisualTask(taskId);
}

/** void AnimTask_PositionFissureBgOnBattler(u8 taskId) */
export function AnimTask_PositionFissureBgOnBattler(taskId: any): any {
  let newTask: any = null;
      let battler: any = (gBattleAnimArgs[0] & ANIM_TARGET) ? gBattleAnimTarget : gBattleAnimAttacker;

      if (gBattleAnimArgs[0] > ANIM_TARGET)
          battler = BATTLE_PARTNER(battler);

      newTask =gTasks[CreateTask(WaitForFissureCompletion, gBattleAnimArgs[1])];
      newTask.data[1] = (32 - GetBattlerSpriteCoord(battler, BATTLER_COORD_X_2)) & 0x1FF;
      newTask.data[2] = (64 - GetBattlerSpriteCoord(battler, BATTLER_COORD_Y_PIC_OFFSET)) & 0xFF;
      gBattle_BG3_X = newTask.data[1];
      gBattle_BG3_Y = newTask.data[2];
      newTask.data[3] = gBattleAnimArgs[2];
      DestroyAnimVisualTask(taskId);
}

/** static void WaitForFissureCompletion(u8 taskId) */
export function WaitForFissureCompletion(taskId: any): any {
  let task: any =gTasks[taskId];

       
       
      if (gBattleAnimArgs[7] == task.data[3])
      {
          gBattle_BG3_X = 0;
          gBattle_BG3_Y = 0;
          DestroyTask(taskId);
      }
      else
      {
          gBattle_BG3_X = task.data[1];
          gBattle_BG3_Y = task.data[2];
      }
}

// ─── callsTo manifest (= 21 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["BATTLE_PARTNER","CreateTask","DestroyAnimSprite","DestroyAnimVisualTask","DestroyTask","GetAnimBattlerSpriteId","GetBattlerSide","GetBattlerSpriteBGPriorityRank","GetBattlerSpriteCoord","GetBattlerSpriteCoord2","GetBattlerYCoordWithElevation","InitAnimArcTranslation","InitSpritePosToAnimAttacker","InitSpritePosToAnimTarget","IsBattlerSpriteVisible","Random2","ScanlineEffect_SetParams","SetBattlersXOffsetForShake","SetDigScanlineEffect","StoreSpriteCallbackInData6","TranslateAnimHorizontalArc"];
