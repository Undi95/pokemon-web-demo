// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/diploma.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 10 functions extracted from src/diploma.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, AddTextPrinterParameterized4, Alloc, AnimateSprites,
  BG_PLTT_ID, BeginNormalPaletteFade, BlendPalettes, BuildOamBuffer,
  CopyBgTilemapBufferToVram, CopyWindowToVram, CreateTask, DeactivateAllTextPrinters,
  DecompressAndCopyTileDataToVram, DestroyTask, DisplayDiplomaText, DmaFill16,
  DmaFill32, EnableInterrupts, FillWindowPixelBuffer, Free,
  FreeAllSpritePalettes, FreeAllWindowBuffers, FreeTempTileDataBuffersIfPossible, HasAllMons,
  InitBgsFromTemplates, InitDiplomaBg, InitDiplomaWindow, InitWindows,
  JOY_NEW, LZDecompressWram, LoadOam, LoadPalette,
  PIXEL_FILL, PrintDiplomaText, ProcessSpriteCopyRequests, PutWindowTilemap,
  ResetBgsAndClearDma3BusyFlags, ResetPaletteFade, ResetSpriteData, ResetTasks,
  ResetTempTileDataBuffers, RunTasks, ScanlineEffect_Stop, SetBgTilemapBuffer,
  SetGpuReg, SetMainCallback2, SetVBlankCallback, ShowBg,
  StringCopy, StringExpandPlaceholders, TransferPlttBuffer, UpdatePaletteFade,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static void VBlankCB(void) */
export function VBlankCB(): any {
  LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
}

/** void CB2_ShowDiploma(void) */
export function CB2_ShowDiploma(): any {
  SetVBlankCallback(NULL);
      SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0);
      SetGpuReg(REG_OFFSET_BG3CNT, 0);
      SetGpuReg(REG_OFFSET_BG2CNT, 0);
      SetGpuReg(REG_OFFSET_BG1CNT, 0);
      SetGpuReg(REG_OFFSET_BG0CNT, 0);
      SetGpuReg(REG_OFFSET_BG3HOFS, 0);
      SetGpuReg(REG_OFFSET_BG3VOFS, 0);
      SetGpuReg(REG_OFFSET_BG2HOFS, 0);
      SetGpuReg(REG_OFFSET_BG2VOFS, 0);
      SetGpuReg(REG_OFFSET_BG1HOFS, 0);
      SetGpuReg(REG_OFFSET_BG1VOFS, 0);
      SetGpuReg(REG_OFFSET_BG0HOFS, 0);
      SetGpuReg(REG_OFFSET_BG0VOFS, 0);
       
      DmaFill16(3, 0, VRAM, VRAM_SIZE);
      DmaFill32(3, 0, OAM, OAM_SIZE);
      DmaFill16(3, 0, PLTT, PLTT_SIZE);
      ScanlineEffect_Stop();
      ResetTasks();
      ResetSpriteData();
      ResetPaletteFade();
      FreeAllSpritePalettes();
      LoadPalette(sDiplomaPalettes, BG_PLTT_ID(0), 0);
      sDiplomaTilemapPtr = Alloc(_0x1000);
      InitDiplomaBg();
      InitDiplomaWindow();
      ResetTempTileDataBuffers();
      DecompressAndCopyTileDataToVram(1,sDiplomaTiles, 0, 0, 0);
      while (FreeTempTileDataBuffersIfPossible())
          ;
      LZDecompressWram(sDiplomaTilemap, sDiplomaTilemapPtr);
      CopyBgTilemapBufferToVram(1);
      DisplayDiplomaText();
      BlendPalettes(PALETTES_ALL, 16, RGB_BLACK);
      BeginNormalPaletteFade(PALETTES_ALL, 0, 16, 0, RGB_BLACK);
      EnableInterrupts(1);
      SetVBlankCallback(VBlankCB);
      SetMainCallback2(MainCB2);
      CreateTask(Task_DiplomaFadeIn, 0);
}

/** static void MainCB2(void) */
export function MainCB2(): any {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
}

/** static void Task_DiplomaFadeIn(u8 taskId) */
export function Task_DiplomaFadeIn(taskId: any): any {
  if (!gPaletteFade.active)
          gTasks[taskId].func = Task_DiplomaWaitForKeyPress;
}

/** static void Task_DiplomaWaitForKeyPress(u8 taskId) */
export function Task_DiplomaWaitForKeyPress(taskId: any): any {
  if (JOY_NEW(A_BUTTON | B_BUTTON))
      {
          BeginNormalPaletteFade(PALETTES_ALL, 0, 0, 16, RGB_BLACK);
          gTasks[taskId].func = Task_DiplomaFadeOut;
      }
}

/** static void Task_DiplomaFadeOut(u8 taskId) */
export function Task_DiplomaFadeOut(taskId: any): any {
  if (!gPaletteFade.active)
      {
          Free(sDiplomaTilemapPtr);
          FreeAllWindowBuffers();
          DestroyTask(taskId);
          SetMainCallback2(CB2_ReturnToFieldFadeFromBlack);
      }
}

/** static void DisplayDiplomaText(void) */
export function DisplayDiplomaText(): any {
  if (HasAllMons())
      {
          SetGpuReg(REG_OFFSET_BG1HOFS, DISPLAY_WIDTH + 16);
          StringCopy(gStringVar1, gText_DexNational);
      }
      else
      {
          SetGpuReg(REG_OFFSET_BG1HOFS, 0);
          StringCopy(gStringVar1, gText_DexHoenn);
      }
      StringExpandPlaceholders(gStringVar4, gText_PokedexDiploma);
      PrintDiplomaText(gStringVar4, 0, 1);
      PutWindowTilemap(0);
      CopyWindowToVram(0, COPYWIN_FULL);
}

/** static void InitDiplomaBg(void) */
export function InitDiplomaBg(): any {
  ResetBgsAndClearDma3BusyFlags(0);
      InitBgsFromTemplates(0, sDiplomaBgTemplates, ARRAY_COUNT(sDiplomaBgTemplates));
      SetBgTilemapBuffer(1, sDiplomaTilemapPtr);
      SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_OBJ_ON | DISPCNT_OBJ_1D_MAP);
      ShowBg(0);
      ShowBg(1);
      SetGpuReg(REG_OFFSET_BLDCNT, 0);
      SetGpuReg(REG_OFFSET_BLDALPHA, 0);
      SetGpuReg(REG_OFFSET_BLDY, 0);
}

/** static void InitDiplomaWindow(void) */
export function InitDiplomaWindow(): any {
  InitWindows(sDiplomaWinTemplates);
      DeactivateAllTextPrinters();
      LoadPalette(gStandardMenuPalette, BG_PLTT_ID(15), PLTT_SIZE_4BPP);
      FillWindowPixelBuffer(0, PIXEL_FILL(0));
      PutWindowTilemap(0);
}

/** static void PrintDiplomaText(u8 *text, u8 var1, u8 var2) */
export function PrintDiplomaText(text: any, var1: any, var2: any): any {
  const color: any = [0, 2, 3];

      AddTextPrinterParameterized4(0, FONT_NORMAL, var1, var2, 0, 0, color, TEXT_SKIP_DRAW, text);
}

// ─── callsTo manifest (= 52 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AddTextPrinterParameterized4","Alloc","AnimateSprites","BG_PLTT_ID","BeginNormalPaletteFade","BlendPalettes","BuildOamBuffer","CopyBgTilemapBufferToVram","CopyWindowToVram","CreateTask","DeactivateAllTextPrinters","DecompressAndCopyTileDataToVram","DestroyTask","DisplayDiplomaText","DmaFill16","DmaFill32","EnableInterrupts","FillWindowPixelBuffer","Free","FreeAllSpritePalettes","FreeAllWindowBuffers","FreeTempTileDataBuffersIfPossible","HasAllMons","InitBgsFromTemplates","InitDiplomaBg","InitDiplomaWindow","InitWindows","JOY_NEW","LZDecompressWram","LoadOam","LoadPalette","PIXEL_FILL","PrintDiplomaText","ProcessSpriteCopyRequests","PutWindowTilemap","ResetBgsAndClearDma3BusyFlags","ResetPaletteFade","ResetSpriteData","ResetTasks","ResetTempTileDataBuffers","RunTasks","ScanlineEffect_Stop","SetBgTilemapBuffer","SetGpuReg","SetMainCallback2","SetVBlankCallback","ShowBg","StringCopy","StringExpandPlaceholders","TransferPlttBuffer","UpdatePaletteFade"];
