// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/mystery_event_menu.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 6 functions extracted from src/mystery_event_menu.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let gLinkType: any = null;
let sUnused: any = null;
/** static void VBlankCB(void) */
export function VBlankCB(): any {
  LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
}

/** static bool8 CheckLanguageMatch(void) */
export function CheckLanguageMatch(): any {
  return (gLinkPlayers[0].language == gLinkPlayers[1].language);
}

/** void CB2_InitMysteryEventMenu(void) */
export function CB2_InitMysteryEventMenu(): any {
  ResetSpriteData();
      FreeAllSpritePalettes();
      ResetTasks();
      SetVBlankCallback(VBlankCB);
      ResetBgsAndClearDma3BusyFlags(0);
      InitBgsFromTemplates(0, sBgTemplates, ARRAY_COUNT(sBgTemplates));
      if (InitWindows(sWindowTemplates))
      {
          let i: any = null;

          DeactivateAllTextPrinters();
          for (i = 0; i < ARRAY_COUNT(sWindowTemplates) - 1; i++)
              FillWindowPixelBuffer(i, PIXEL_FILL(0));

          FillBgTilemapBufferRect_Palette0(0, 0, 0, 0, DISPLAY_TILE_WIDTH, DISPLAY_TILE_HEIGHT);
          LoadUserWindowBorderGfx(0, 1, BG_PLTT_ID(13));
          Menu_LoadStdPalAt(BG_PLTT_ID(14));
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0 | DISPCNT_OBJ_1D_MAP | DISPCNT_BG0_ON);
          SetGpuReg(REG_OFFSET_BLDCNT, 0);
          CreateTask(Task_DestroySelf, 0);
          StopMapMusic();
          RunTasks();
          AnimateSprites();
          BuildOamBuffer();
          RunTextPrinters();
          UpdatePaletteFade();
          SetBackdropFromColor((RGB(0, 0, 0)));
          SetMainCallback2(CB2_MysteryEventMenu);
      }
}

/** static bool8 GetEventLoadMessage(u8 *dest, u32 status) */
export function GetEventLoadMessage(dest: any, status: any): any {
  let retVal: any = TRUE;

      if (status == MEVENT_STATUS_LOAD_OK)
      {
          StringCopy(dest, gText_EventSafelyLoaded);
          retVal = FALSE;
      }

      if (status == MEVENT_STATUS_SUCCESS)
          retVal = FALSE;

      if (status == MEVENT_STATUS_LOAD_ERROR)
          StringCopy(dest, gText_LoadErrorEndingSession);

      return retVal;
}

/** static void CB2_MysteryEventMenu(void) */
export function CB2_MysteryEventMenu(): any {
  switch (gMain.state)
      {
      case 0:
          DrawStdFrameWithCustomTileAndPalette(WIN_MSG, TRUE, 1, 0xD);
          PutWindowTilemap(WIN_MSG);
          CopyWindowToVram(WIN_MSG, COPYWIN_FULL);
          ShowBg(0);
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 0x10, 0, (RGB(0, 0, 0)));
          gMain.state++;
          break;
      case 1:
          if (!gPaletteFade.active)
          {
              PrintMysteryMenuText(WIN_MSG, gText_LinkStandby2, 1, 2, 1);
              gMain.state++;
          }
          break;
      case 2:
          if (!IsTextPrinterActive(WIN_MSG))
          {
              gMain.state++;
              gLinkType = (0x5501);
              OpenLink();
          }
          break;
      case 3:
          if ((gLinkStatus & (0x00000020)) && (gLinkStatus & (0x0000001C)) > 4)
          {
              PlaySE((21));
              PrintMysteryMenuText(WIN_MSG, gText_PressAToLoadEvent, 1, 2, 1);
              gMain.state++;
          }
          if (JOY_NEW(B_BUTTON))
          {
              PlaySE((5));
              CloseLink();
              gMain.state = 15;
          }
          break;
      case 4:
          if (!IsTextPrinterActive(WIN_MSG))
              gMain.state++;
          break;
      case 5:
          if (GetLinkPlayerCount_2() == 2)
          {
              if (JOY_NEW(A_BUTTON))
              {
                  PlaySE((5));
                  CheckShouldAdvanceLinkState();
                  DrawStdFrameWithCustomTileAndPalette(WIN_LOADING, TRUE, 1, 0xD);
                  PrintMysteryMenuText(WIN_LOADING, gText_LoadingEvent, 1, 2, 0);
                  PutWindowTilemap(WIN_LOADING);
                  CopyWindowToVram(WIN_LOADING, COPYWIN_FULL);
                  gMain.state++;
              }
              else if (JOY_NEW(B_BUTTON))
              {
                  PlaySE((5));
                  CloseLink();
                  gMain.state = 15;
              }
          }
          else
          {
              GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
              PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
              gMain.state = 13;
          }
          break;
      case 6:
          if (IsLinkConnectionEstablished())
          {
              if (gReceivedRemoteLinkPlayers)
              {
                  if (GetLinkPlayerDataExchangeStatusTimed(2, 2) == EXCHANGE_DIFF_SELECTIONS)
                  {
                      SetCloseLinkCallback();
                      GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
                      PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
                      gMain.state = 13;
                  }
                  else if (CheckLanguageMatch())
                  {
                      PrintMysteryMenuText(WIN_MSG, gText_DontRemoveCableTurnOff, 1, 2, 1);
                      gMain.state++;
                  }
                  else
                  {
                      CloseLink();
                      GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
                      PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
                      gMain.state = 13;
                  }
              }
          }
          else if (JOY_NEW(B_BUTTON))
          {
              PlaySE((5));
              CloseLink();
              gMain.state = 15;
          }
          break;
      case 7:
          if (!IsTextPrinterActive(WIN_MSG))
              gMain.state++;
          break;
      case 8:
          if (GetBlockReceivedStatus())
          {
              ResetBlockReceivedFlags();
              gMain.state++;
          }
          break;
      case 9:
          gMain.state++;
          break;
      case 10:
          SetCloseLinkCallback();
          gMain.state++;
          break;
      case 11:
          if (gReceivedRemoteLinkPlayers == 0)
          {
              let status: any = RunMysteryEventScript(gDecompressionBuffer);
              CpuFill32(0, gDecompressionBuffer, 0x7D4);
              if (!GetEventLoadMessage(gStringVar4, status))
                  TrySavingData(SAVE_NORMAL);
              gMain.state++;
          }
          break;
      case 12:
          PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
          gMain.state++;
          break;
      case 13:
          if (!IsTextPrinterActive(WIN_MSG))
          {
              gMain.state++;
              sUnused = 0;
          }
          break;
      case 14:
          if (JOY_NEW(A_BUTTON))
          {
              PlaySE((5));
              gMain.state++;
          }
          break;
      case 15:
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 0, 0x10, (RGB(0, 0, 0)));
          gMain.state++;
          break;
      case 16:
          if (!gPaletteFade.active)
              DoSoftReset();
          break;
      }

      if (gLinkStatus & (0x00000040) && !IsLinkMaster())
      {
          CloseLink();
          GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
          PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
          gMain.state = 13;
      }

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      RunTextPrinters();
      UpdatePaletteFade();
}

/** static void PrintMysteryMenuText(u8 windowId, const u8 *text, u8 x, u8 y, s32 speed) */
export function PrintMysteryMenuText(windowId: any, text: any, x: any, y: any, speed: any): any {
  let textColor: any = [];
      let letterSpacing: any = 0;
      let lineSpacing: any = 1;
      textColor[0] = 1;
      textColor[1] = 2;
      textColor[2] = 3;

      FillWindowPixelBuffer(windowId, PIXEL_FILL(textColor[0]));
      AddTextPrinterParameterized4(windowId, FONT_NORMAL, x, y, letterSpacing, lineSpacing, textColor, speed, text);
}

// ─── callsTo manifest (= 55 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AddTextPrinterParameterized4","AnimateSprites","BG_PLTT_ID","BeginNormalPaletteFade","BuildOamBuffer","CheckLanguageMatch","CheckShouldAdvanceLinkState","CloseLink","CopyWindowToVram","CpuFill32","CreateTask","DeactivateAllTextPrinters","DoSoftReset","DrawStdFrameWithCustomTileAndPalette","FillBgTilemapBufferRect_Palette0","FillWindowPixelBuffer","FreeAllSpritePalettes","GetBlockReceivedStatus","GetEventLoadMessage","GetLinkPlayerCount_2","GetLinkPlayerDataExchangeStatusTimed","InitBgsFromTemplates","InitWindows","IsLinkConnectionEstablished","IsLinkMaster","IsTextPrinterActive","JOY_NEW","LoadOam","LoadUserWindowBorderGfx","Menu_LoadStdPalAt","OpenLink","PIXEL_FILL","PlaySE","PrintMysteryMenuText","ProcessSpriteCopyRequests","PutWindowTilemap","ResetBgsAndClearDma3BusyFlags","ResetBlockReceivedFlags","ResetSpriteData","ResetTasks","RunMysteryEventScript","RunTasks","RunTextPrinters","SetBackdropFromColor","SetCloseLinkCallback","SetGpuReg","SetMainCallback2","SetVBlankCallback","ShowBg","StopMapMusic","StringCopy","TransferPlttBuffer","TrySavingData","UpdatePaletteFade"];
