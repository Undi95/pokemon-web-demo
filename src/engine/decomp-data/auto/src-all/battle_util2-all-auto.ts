// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_util2.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 5 functions extracted from src/battle_util2.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** void AllocateBattleResources(void) */
export function AllocateBattleResources(): any {
  if (gBattleResources != NULL) {
           
           
      }

      if (gBattleTypeFlags & BATTLE_TYPE_TRAINER_HILL)
          InitTrainerHillBattleStruct();

      gBattleStruct = AllocZeroed(0);

      gBattleResources = AllocZeroed(0);
      gBattleResources.secretBase = AllocZeroed(sizeof(gBattleResources.secretBase));
      gBattleResources.flags = AllocZeroed(sizeof(gBattleResources.flags));
      gBattleResources.battleScriptsStack = AllocZeroed(sizeof(gBattleResources.battleScriptsStack));
      gBattleResources.battleCallbackStack = AllocZeroed(sizeof(gBattleResources.battleCallbackStack));
      gBattleResources.beforeLvlUp = AllocZeroed(sizeof(gBattleResources.beforeLvlUp));
      gBattleResources.ai = AllocZeroed(sizeof(gBattleResources.ai));
      gBattleResources.battleHistory = AllocZeroed(sizeof(gBattleResources.battleHistory));
      gBattleResources.AI_ScriptsStack = AllocZeroed(sizeof(gBattleResources.AI_ScriptsStack));

      gLinkBattleSendBuffer = AllocZeroed(BATTLE_BUFFER_LINK_SIZE);
      gLinkBattleRecvBuffer = AllocZeroed(BATTLE_BUFFER_LINK_SIZE);

      gBattleAnimBgTileBuffer = AllocZeroed(0x2000);
      gBattleAnimBgTilemapBuffer = AllocZeroed(0x1000);

      if (gBattleTypeFlags & BATTLE_TYPE_SECRET_BASE)
      {
          let currSecretBaseId: any = VarGet(VAR_CURRENT_SECRET_BASE);
          CreateSecretBaseEnemyParty(gSaveBlock1Ptr.secretBases[currSecretBaseId]);
      }
}

/** void FreeBattleResources(void) */
export function FreeBattleResources(): any {
  if (gBattleTypeFlags & BATTLE_TYPE_TRAINER_HILL)
          FreeTrainerHillBattleStruct();

      if (gBattleResources != NULL)
      {
          FREE_AND_SET_NULL(gBattleStruct);

          FREE_AND_SET_NULL(gBattleResources.secretBase);
          FREE_AND_SET_NULL(gBattleResources.flags);
          FREE_AND_SET_NULL(gBattleResources.battleScriptsStack);
          FREE_AND_SET_NULL(gBattleResources.battleCallbackStack);
          FREE_AND_SET_NULL(gBattleResources.beforeLvlUp);
          FREE_AND_SET_NULL(gBattleResources.ai);
          FREE_AND_SET_NULL(gBattleResources.battleHistory);
          FREE_AND_SET_NULL(gBattleResources.AI_ScriptsStack);
          FREE_AND_SET_NULL(gBattleResources);

          FREE_AND_SET_NULL(gLinkBattleSendBuffer);
          FREE_AND_SET_NULL(gLinkBattleRecvBuffer);

          FREE_AND_SET_NULL(gBattleAnimBgTileBuffer);
          FREE_AND_SET_NULL(gBattleAnimBgTilemapBuffer);
      }
}

/** void AdjustFriendshipOnBattleFaint(u8 battler) */
export function AdjustFriendshipOnBattleFaint(battler: any): any {
  let opposingBattlerId: any = null;

      if (gBattleTypeFlags & BATTLE_TYPE_DOUBLE)
      {
          let opposingBattlerId2: any = null;

          opposingBattlerId = GetBattlerAtPosition(B_POSITION_OPPONENT_LEFT);
          opposingBattlerId2 = GetBattlerAtPosition(B_POSITION_OPPONENT_RIGHT);

          if (gBattleMons[opposingBattlerId2].level > gBattleMons[opposingBattlerId].level)
              opposingBattlerId = opposingBattlerId2;
      }
      else
      {
          opposingBattlerId = GetBattlerAtPosition(B_POSITION_OPPONENT_LEFT);
      }

      if (gBattleMons[opposingBattlerId].level > gBattleMons[battler].level)
      {
          if (gBattleMons[opposingBattlerId].level - gBattleMons[battler].level > 29)
              AdjustFriendship(gPlayerParty[gBattlerPartyIndexes[battler]], FRIENDSHIP_EVENT_FAINT_LARGE);
          else
              AdjustFriendship(gPlayerParty[gBattlerPartyIndexes[battler]], FRIENDSHIP_EVENT_FAINT_SMALL);
      }
      else
      {
          AdjustFriendship(gPlayerParty[gBattlerPartyIndexes[battler]], FRIENDSHIP_EVENT_FAINT_SMALL);
      }
}

/** void SwitchPartyOrderInGameMulti(u8 battler, u8 arg1) */
export function SwitchPartyOrderInGameMulti(battler: any, arg1: any): any {
  if (GetBattlerSide(battler) != B_SIDE_OPPONENT)
      {
          let i: any = null;
          for (i = 0; i < ARRAY_COUNT(gBattlePartyCurrentOrder); i++)
              gBattlePartyCurrentOrder[i] = (i + (gBattleStruct.battlerPartyOrders));

          SwitchPartyMonSlots(GetPartyIdFromBattlePartyId(gBattlerPartyIndexes[battler]), GetPartyIdFromBattlePartyId(arg1));

          for (i = 0; i < ARRAY_COUNT(gBattlePartyCurrentOrder); i++)
              MEM_WRITE((i + (gBattleStruct.battlerPartyOrders)), gBattlePartyCurrentOrder[i]);
      }
}

/** u32 BattlePalace_TryEscapeStatus(u8 battler) */
export function BattlePalace_TryEscapeStatus(battler: any): any {
  let effect: any = 0;

      do
      {
          switch (gBattleCommunication[MULTIUSE_STATE])
          {
          case 0:
              if (gBattleMons[battler].status1 & STATUS1_SLEEP)
              {
                  if (UproarWakeUpCheck(battler))
                  {
                       
                      gBattleMons[battler].status1 &= ~(STATUS1_SLEEP);
                      gBattleMons[battler].status2 &= ~(STATUS2_NIGHTMARE);
                      BattleScriptPushCursor();
                      gBattleCommunication[MULTISTRING_CHOOSER] = B_MSG_WOKE_UP_UPROAR;
                      gBattlescriptCurrInstr = BattleScript_MoveUsedWokeUp;
                      effect = 2;
                  }
                  else
                  {
                      let toSub: any = null;

                      if (gBattleMons[battler].ability == ABILITY_EARLY_BIRD)
                          toSub = 2;
                      else
                          toSub = 1;

                       
                      if ((gBattleMons[battler].status1 & STATUS1_SLEEP) < toSub)
                          gBattleMons[battler].status1 &= ~(STATUS1_SLEEP);
                      else
                          gBattleMons[battler].status1 -= toSub;

                      if (gBattleMons[battler].status1 & STATUS1_SLEEP)
                      {
                           
                          gBattlescriptCurrInstr = BattleScript_MoveUsedIsAsleep;
                          effect = 2;
                      }
                      else
                      {
                           
                          gBattleMons[battler].status2 &= ~(STATUS2_NIGHTMARE);
                          BattleScriptPushCursor();
                          gBattleCommunication[MULTISTRING_CHOOSER] = B_MSG_WOKE_UP;
                          gBattlescriptCurrInstr = BattleScript_MoveUsedWokeUp;
                          effect = 2;
                      }
                  }
              }
              gBattleCommunication[MULTIUSE_STATE]++;
              break;
          case 1:
              if (gBattleMons[battler].status1 & STATUS1_FREEZE)
              {
                  if (Random() % 5 != 0)
                  {
                       
                      gBattlescriptCurrInstr = BattleScript_MoveUsedIsFrozen;
                  }
                  else
                  {
                       
                      gBattleMons[battler].status1 &= ~(STATUS1_FREEZE);
                      BattleScriptPushCursor();
                      gBattlescriptCurrInstr = BattleScript_MoveUsedUnfroze;
                      gBattleCommunication[MULTISTRING_CHOOSER] = B_MSG_DEFROSTED;
                  }
                  effect = 2;
              }
              gBattleCommunication[MULTIUSE_STATE]++;
              break;
          case 2:
              break;
          }
           
      } while (gBattleCommunication[MULTIUSE_STATE] != 2 && effect == 0);

      if (effect == 2)
      {
          gActiveBattler = battler;
          BtlController_EmitSetMonData(B_COMM_TO_CONTROLLER, REQUEST_STATUS_BATTLE, 0, 4,gBattleMons[gActiveBattler].status1);
          MarkBattlerForControllerExec(gActiveBattler);
      }

      return effect;
}

// ─── callsTo manifest (= 17 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AdjustFriendship","AllocZeroed","BattleScriptPushCursor","BtlController_EmitSetMonData","CreateSecretBaseEnemyParty","FREE_AND_SET_NULL","FreeTrainerHillBattleStruct","GetBattlerAtPosition","GetBattlerSide","GetPartyIdFromBattlePartyId","InitTrainerHillBattleStruct","MarkBattlerForControllerExec","Random","SwitchPartyMonSlots","UproarWakeUpCheck","VarGet"];
