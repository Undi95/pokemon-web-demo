// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/battle_records.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 31 functions extracted from src/battle_records.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** static void ClearLinkBattleRecord(struct LinkBattleRecord *record) */
export function ClearLinkBattleRecord(record: any): any {
  CpuFill16(0, record, 0);
      record.name[0] = EOS;
      record.trainerId = 0;
      record.wins = 0;
      record.losses = 0;
      record.draws = 0;
}

/** static void ClearLinkBattleRecords(struct LinkBattleRecord *records) */
export function ClearLinkBattleRecords(records: any): any {
  let i: any = null;
      for (i = 0; i < LINK_B_RECORDS_COUNT; i++)
      {
          ClearLinkBattleRecord(records + i);
      }
      SetGameStat(GAME_STAT_LINK_BATTLE_WINS, 0);
      SetGameStat(GAME_STAT_LINK_BATTLE_LOSSES, 0);
      SetGameStat(GAME_STAT_LINK_BATTLE_DRAWS, 0);
}

/** static s32 GetLinkBattleRecordTotalBattles(struct LinkBattleRecord *record) */
export function GetLinkBattleRecordTotalBattles(record: any): any {
  return record.wins + record.losses + record.draws;
}

/** static s32 FindLinkBattleRecord(struct LinkBattleRecord *records, const u8 *name, u16 trainerId) */
export function FindLinkBattleRecord(records: any, name: any, trainerId: any): any {
  let i: any = null;

      for (i = 0; i < LINK_B_RECORDS_COUNT; i++)
      {
          if (!StringCompareN(records[i].name, name, PLAYER_NAME_LENGTH) && records[i].trainerId == trainerId)
              return i;
      }

      return LINK_B_RECORDS_COUNT;
}

/** static void SortLinkBattleRecords(struct LinkBattleRecords *records) */
export function SortLinkBattleRecords(records: any): any {
  let i, j;

      for (i = LINK_B_RECORDS_COUNT - 1; i > 0; i--)
      {
          for (j = i - 1; j >= 0; j--)
          {
              let totalBattlesI: any = GetLinkBattleRecordTotalBattles(records.entries[i]);
              let totalBattlesJ: any = GetLinkBattleRecordTotalBattles(records.entries[j]);

              if (totalBattlesI > totalBattlesJ)
              {
                  let temp1: any = null;
                  let temp2: any = null;

                  temp1 = records.entries[i];
                  records.entries[i] = records.entries[j];
                  records.entries[j] = temp1;

                  temp2 = records.languages[i];
                  records.languages[i] = records.languages[j];
                  records.languages[j] = temp2;
              }
          }
      }
}

/** static void UpdateLinkBattleRecord(struct LinkBattleRecord *record, s32 battleOutcome) */
export function UpdateLinkBattleRecord(record: any, battleOutcome: any): any {
  switch (battleOutcome)
      {
      case B_OUTCOME_WON:
          record.wins++;
          if (record.wins > 9999)
              record.wins = 9999;
          break;
      case B_OUTCOME_LOST:
          record.losses++;
          if (record.losses > 9999)
              record.losses = 9999;
          break;
      case B_OUTCOME_DREW:
          record.draws++;
          if (record.draws > 9999)
              record.draws = 9999;
          break;
      }
}

/** static void UpdateLinkBattleGameStats(s32 battleOutcome) */
export function UpdateLinkBattleGameStats(battleOutcome: any): any {
  let stat: any = null;

      switch (battleOutcome)
      {
      case B_OUTCOME_WON:
          stat = GAME_STAT_LINK_BATTLE_WINS;
          break;
      case B_OUTCOME_LOST:
          stat = GAME_STAT_LINK_BATTLE_LOSSES;
          break;
      case B_OUTCOME_DREW:
          stat = GAME_STAT_LINK_BATTLE_DRAWS;
          break;
      default:
          return;
      }

      if (GetGameStat(stat) < 9999)
          IncrementGameStat(stat);
}

/** static void UpdateLinkBattleRecords(struct LinkBattleRecords *records, const u8 *name, u16 trainerId, s32 battleOutcome, u8 battler) */
export function UpdateLinkBattleRecords(records: any, name: any, trainerId: any, battleOutcome: any, battler: any): any {
  let index: any = null;

      UpdateLinkBattleGameStats(battleOutcome);
      SortLinkBattleRecords(records);
      index = FindLinkBattleRecord(records.entries, name, trainerId);
      if (index == LINK_B_RECORDS_COUNT)
      {
          index = LINK_B_RECORDS_COUNT - 1;
          ClearLinkBattleRecord(records.entries[index]);
          StringCopyN(records.entries[index].name, name, PLAYER_NAME_LENGTH);
          records.entries[index].trainerId = trainerId;
          records.languages[index] = gLinkPlayers[battler].language;
      }
      UpdateLinkBattleRecord(records.entries[index], battleOutcome);
      SortLinkBattleRecords(records);
}

/** void ClearPlayerLinkBattleRecords(void) */
export function ClearPlayerLinkBattleRecords(): any {
  ClearLinkBattleRecords(gSaveBlock1Ptr.linkBattleRecords.entries);
}

/** static void IncTrainerCardWins(s32 battler) */
export function IncTrainerCardWins(battler: any): any {
  let wins: any =gTrainerCards[battler].linkBattleWins;
      wins++;
      if (wins > 9999)
          wins = 9999;
}

/** static void IncTrainerCardLosses(s32 battler) */
export function IncTrainerCardLosses(battler: any): any {
  let losses: any =gTrainerCards[battler].linkBattleLosses;
      losses++;
      if (losses > 9999)
          losses = 9999;
}

/** static void UpdateTrainerCardWinsLosses(s32 battler) */
export function UpdateTrainerCardWinsLosses(battler: any): any {
  switch (gBattleOutcome)
      {
      case B_OUTCOME_WON:
          IncTrainerCardWins(BATTLE_OPPOSITE(battler));
          IncTrainerCardLosses(battler);
          break;
      case B_OUTCOME_LOST:
          IncTrainerCardLosses(BATTLE_OPPOSITE(battler));
          IncTrainerCardWins(battler);
          break;
      }
}

/** void UpdatePlayerLinkBattleRecords(s32 battler) */
export function UpdatePlayerLinkBattleRecords(battler: any): any {
  if (InUnionRoom() != TRUE)
      {
          UpdateTrainerCardWinsLosses(battler);
          UpdateLinkBattleRecords(gSaveBlock1Ptr.linkBattleRecords,
              gTrainerCards[battler].playerName,
              gTrainerCards[battler].trainerId,
              gBattleOutcome,
              battler);
      }
}

/** static void PrintLinkBattleWinsLossesDraws(struct LinkBattleRecord *records) */
export function PrintLinkBattleWinsLossesDraws(records: any): any {
  let x: any = null;

      ConvertIntToDecimalStringN(gStringVar1, GetGameStat(GAME_STAT_LINK_BATTLE_WINS), STR_CONV_MODE_LEFT_ALIGN, 4);
      ConvertIntToDecimalStringN(gStringVar2, GetGameStat(GAME_STAT_LINK_BATTLE_LOSSES), STR_CONV_MODE_LEFT_ALIGN, 4);
      ConvertIntToDecimalStringN(gStringVar3, GetGameStat(GAME_STAT_LINK_BATTLE_DRAWS), STR_CONV_MODE_LEFT_ALIGN, 4);
      StringExpandPlaceholders(gStringVar4, gText_TotalRecordWLD);

      x = GetStringCenterAlignXOffset(FONT_NORMAL, gStringVar4, 0xD0);
      AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, gStringVar4, x, 0x11, 0, NULL);
}

/** static void PrintLinkBattleRecord(struct LinkBattleRecord *record, u8 y, s32 language) */
export function PrintLinkBattleRecord(record: any, y: any, language: any): any {
  if (record.wins == 0 && record.losses == 0 && record.draws == 0)
      {
           
          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, sText_DashesNoPlayer,   8, (y * 8) + 1, 0, NULL);
          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, sText_DashesNoScore,  80, (y * 8) + 1, 0, NULL);
          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, sText_DashesNoScore, 128, (y * 8) + 1, 0, NULL);
          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, sText_DashesNoScore, 176, (y * 8) + 1, 0, NULL);
      }
      else
      {
          StringFillWithTerminator(gStringVar1, 8);
          StringCopyN(gStringVar1, record.name, 7);
          ConvertInternationalString(gStringVar1, language);

          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, gStringVar1, 8, (y * 8) + 1, 0, NULL);

          ConvertIntToDecimalStringN(gStringVar1, record.wins, STR_CONV_MODE_RIGHT_ALIGN, 4);
          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, gStringVar1,  80, (y * 8) + 1, 0, NULL);

          ConvertIntToDecimalStringN(gStringVar1, record.losses, STR_CONV_MODE_RIGHT_ALIGN, 4);
          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, gStringVar1, 128, (y * 8) + 1, 0, NULL);

          ConvertIntToDecimalStringN(gStringVar1, record.draws, STR_CONV_MODE_RIGHT_ALIGN, 4);
          AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, gStringVar1, 176, (y * 8) + 1, 0, NULL);
      }
}

/** void ShowLinkBattleRecords(void) */
export function ShowLinkBattleRecords(): any {
  let i, x;

      gRecordsWindowId = AddWindow(sLinkBattleRecordsWindow);
      DrawStdWindowFrame(gRecordsWindowId, FALSE);
      FillWindowPixelBuffer(gRecordsWindowId, PIXEL_FILL(1));
      StringExpandPlaceholders(gStringVar4, gText_PlayersBattleResults);

      x = GetStringCenterAlignXOffset(FONT_NORMAL, gStringVar4, 208);
      AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, gStringVar4, x, 1, 0, NULL);
      PrintLinkBattleWinsLossesDraws(gSaveBlock1Ptr.linkBattleRecords.entries);

      StringExpandPlaceholders(gStringVar4, gText_WinLoseDraw);
      AddTextPrinterParameterized(gRecordsWindowId, FONT_NORMAL, gStringVar4, 0, 41, 0, NULL);

      for (i = 0; i < LINK_B_RECORDS_COUNT; i++)
      {
          PrintLinkBattleRecord(gSaveBlock1Ptr.linkBattleRecords.entries[i], 7 + (i * 2), gSaveBlock1Ptr.linkBattleRecords.languages[i]);
      }

      PutWindowTilemap(gRecordsWindowId);
      CopyWindowToVram(gRecordsWindowId, COPYWIN_FULL);
}

/** void RemoveRecordsWindow(void) */
export function RemoveRecordsWindow(): any {
  ClearStdWindowAndFrame(gRecordsWindowId, FALSE);
      RemoveWindow(gRecordsWindowId);
}

/** static void Task_TrainerHillWaitForPaletteFade(u8 taskId) */
export function Task_TrainerHillWaitForPaletteFade(taskId: any): any {
  if (!gPaletteFade.active)
          gTasks[taskId].func = Task_CloseTrainerHillRecordsOnButton;
}

/** static void Task_CloseTrainerHillRecordsOnButton(u8 taskId) */
export function Task_CloseTrainerHillRecordsOnButton(taskId: any): any {
  let task: any =gTasks[taskId];

      if (JOY_NEW(A_BUTTON) || JOY_NEW(B_BUTTON))
      {
          PlaySE(SE_SELECT);
          task.func = Task_BeginPaletteFade;
      }
}

/** static void Task_BeginPaletteFade(u8 taskId) */
export function Task_BeginPaletteFade(taskId: any): any {
  BeginNormalPaletteFade(PALETTES_ALL, 0, 0, 0x10, RGB_BLACK);
      gTasks[taskId].func = Task_ExitTrainerHillRecords;
}

/** static void Task_ExitTrainerHillRecords(u8 taskId) */
export function Task_ExitTrainerHillRecords(taskId: any): any {
  if (!gPaletteFade.active)
      {
          SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic);
          Free(sTilemapBuffer);
          RemoveTrainerHillRecordsWindow(0);
          FreeAllWindowBuffers();
          DestroyTask(taskId);
      }
}

/** static void RemoveTrainerHillRecordsWindow(u8 windowId) */
export function RemoveTrainerHillRecordsWindow(windowId: any): any {
  FillWindowPixelBuffer(windowId, PIXEL_FILL(0));
      ClearWindowTilemap(windowId);
      CopyWindowToVram(windowId, COPYWIN_GFX);
      RemoveWindow(windowId);
}

/** static void ClearVramOamPlttRegs(void) */
export function ClearVramOamPlttRegs(): any {
  DmaClearLarge16(3, (VRAM), VRAM_SIZE, 0x1000);
      DmaClear32(3, OAM, OAM_SIZE);
      DmaClear16(3, PLTT, PLTT_SIZE);

      SetGpuReg(REG_OFFSET_DISPCNT, 0);
      SetGpuReg(REG_OFFSET_BG0CNT, 0);
      SetGpuReg(REG_OFFSET_BG0HOFS, 0);
      SetGpuReg(REG_OFFSET_BG0VOFS, 0);
      SetGpuReg(REG_OFFSET_BG1CNT, 0);
      SetGpuReg(REG_OFFSET_BG1HOFS, 0);
      SetGpuReg(REG_OFFSET_BG1VOFS, 0);
      SetGpuReg(REG_OFFSET_BG2CNT, 0);
      SetGpuReg(REG_OFFSET_BG2HOFS, 0);
      SetGpuReg(REG_OFFSET_BG2VOFS, 0);
      SetGpuReg(REG_OFFSET_BG3CNT, 0);
      SetGpuReg(REG_OFFSET_BG3HOFS, 0);
      SetGpuReg(REG_OFFSET_BG3VOFS, 0);
      SetGpuReg(REG_OFFSET_WIN0H, 0);
      SetGpuReg(REG_OFFSET_WIN0V, 0);
      SetGpuReg(REG_OFFSET_WININ, 0);
      SetGpuReg(REG_OFFSET_WINOUT, 0);
      SetGpuReg(REG_OFFSET_BLDCNT, 0);
      SetGpuReg(REG_OFFSET_BLDALPHA, 0);
      SetGpuReg(REG_OFFSET_BLDY, 0);
}

/** static void ClearTasksAndGraphicalStructs(void) */
export function ClearTasksAndGraphicalStructs(): any {
  ScanlineEffect_Stop();
      ResetTasks();
      ResetSpriteData();
      ResetPaletteFade();
      FreeAllSpritePalettes();
}

/** static void ResetBgCoordinates(void) */
export function ResetBgCoordinates(): any {
  ChangeBgX(0, 0, BG_COORD_SET);
      ChangeBgY(0, 0, BG_COORD_SET);
      ChangeBgX(1, 0, BG_COORD_SET);
      ChangeBgY(1, 0, BG_COORD_SET);
      ChangeBgX(2, 0, BG_COORD_SET);
      ChangeBgY(2, 0, BG_COORD_SET);
      ChangeBgX(3, 0, BG_COORD_SET);
      ChangeBgY(3, 0, BG_COORD_SET);
}

/** static void SetDispcntReg(void) */
export function SetDispcntReg(): any {
  SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_BG0_ON | DISPCNT_BG3_ON | DISPCNT_OBJ_1D_MAP);
}

/** static void LoadTrainerHillRecordsWindowGfx(u8 bgId) */
export function LoadTrainerHillRecordsWindowGfx(bgId: any): any {
  LoadBgTiles(bgId, sTrainerHillWindowTileset, 0, 0);
      CopyToBgTilemapBufferRect(bgId, sTrainerHillWindowTilemap, 0, 0, 0x20, 0x20);
      LoadPalette(sTrainerHillWindowPalette, BG_PLTT_ID(0), 0);
}

/** static void VblankCB_TrainerHillRecords(void) */
export function VblankCB_TrainerHillRecords(): any {
  LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
}

/** static void MainCB2_TrainerHillRecords(void) */
export function MainCB2_TrainerHillRecords(): any {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
}

/** void ShowTrainerHillRecords(void) */
export function ShowTrainerHillRecords(): any {
  SetVBlankCallback(NULL);
      SetMainCallback2(CB2_ShowTrainerHillRecords);
}

/** static void CB2_ShowTrainerHillRecords(void) */
export function CB2_ShowTrainerHillRecords(): any {
  switch (gMain.state)
      {
      case 0:
          SetVBlankCallback(NULL);
          ClearVramOamPlttRegs();
          gMain.state++;
          break;
      case 1:
          ClearTasksAndGraphicalStructs();
          gMain.state++;
          break;
      case 2:
          sTilemapBuffer = AllocZeroed(BG_SCREEN_SIZE);
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sTrainerHillRecordsBgTemplates, ARRAY_COUNT(sTrainerHillRecordsBgTemplates));
          SetBgTilemapBuffer(3, sTilemapBuffer);
          ResetBgCoordinates();
          gMain.state++;
          break;
      case 3:
          LoadTrainerHillRecordsWindowGfx(3);
          LoadPalette(GetTextWindowPalette(0), BG_PLTT_ID(15), PLTT_SIZE_4BPP);
          gMain.state++;
          break;
      case 4:
          if (IsDma3ManagerBusyWithBgCopy() != TRUE)
          {
              ShowBg(0);
              ShowBg(3);
              CopyBgTilemapBufferToVram(3);
              gMain.state++;
          }
          break;
      case 5:
          InitWindows(sTrainerHillRecordsWindowTemplates);
          DeactivateAllTextPrinters();
          gMain.state++;
          break;
      case 6:
          BeginNormalPaletteFade(PALETTES_ALL, 0, 0x10, 0, RGB_BLACK);
          gMain.state++;
          break;
      case 7:
          SetDispcntReg();
          SetVBlankCallback(VblankCB_TrainerHillRecords);
          PrintOnTrainerHillRecordsWindow();
          CreateTask(Task_TrainerHillWaitForPaletteFade, 8);
          SetMainCallback2(MainCB2_TrainerHillRecords);
          gMain.state = 0;
          break;
      }
}

// ─── callsTo manifest (= 85 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AddTextPrinterParameterized","AddWindow","AllocZeroed","AnimateSprites","BATTLE_OPPOSITE","BG_PLTT_ID","BeginNormalPaletteFade","BuildOamBuffer","ChangeBgX","ChangeBgY","ClearLinkBattleRecord","ClearLinkBattleRecords","ClearStdWindowAndFrame","ClearTasksAndGraphicalStructs","ClearVramOamPlttRegs","ClearWindowTilemap","ConvertIntToDecimalStringN","ConvertInternationalString","CopyBgTilemapBufferToVram","CopyToBgTilemapBufferRect","CopyWindowToVram","CpuFill16","CreateTask","DeactivateAllTextPrinters","DestroyTask","DmaClear16","DmaClear32","DmaClearLarge16","DrawStdWindowFrame","FillWindowPixelBuffer","FindLinkBattleRecord","Free","FreeAllSpritePalettes","FreeAllWindowBuffers","GetGameStat","GetLinkBattleRecordTotalBattles","GetStringCenterAlignXOffset","GetTextWindowPalette","InUnionRoom","IncTrainerCardLosses","IncTrainerCardWins","IncrementGameStat","InitBgsFromTemplates","InitWindows","IsDma3ManagerBusyWithBgCopy","JOY_NEW","LoadBgTiles","LoadOam","LoadPalette","LoadTrainerHillRecordsWindowGfx","PIXEL_FILL","PlaySE","PrintLinkBattleRecord","PrintLinkBattleWinsLossesDraws","PrintOnTrainerHillRecordsWindow","ProcessSpriteCopyRequests","PutWindowTilemap","RemoveTrainerHillRecordsWindow","RemoveWindow","ResetBgCoordinates","ResetBgsAndClearDma3BusyFlags","ResetPaletteFade","ResetSpriteData","ResetTasks","RunTasks","ScanlineEffect_Stop","SetBgTilemapBuffer","SetDispcntReg","SetGameStat","SetGpuReg","SetMainCallback2","SetVBlankCallback","ShowBg","SortLinkBattleRecords","StringCompareN","StringCopyN","StringExpandPlaceholders","StringFillWithTerminator","TransferPlttBuffer","UpdateLinkBattleGameStats","UpdateLinkBattleRecord","UpdateLinkBattleRecords","UpdatePaletteFade","UpdateTrainerCardWinsLosses"];
