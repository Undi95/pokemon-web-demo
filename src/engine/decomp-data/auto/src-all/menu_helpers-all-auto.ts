// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/menu_helpers.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 25 functions extracted from src/menu_helpers.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  AddTextPrinterParameterized2, ChangeBgX, ChangeBgY, CpuFill16,
  CpuFill32, CreateSprite, CreateYesNoMenu, DestroySprite,
  DestroySpriteAndFreeResources, DrawDialogFrameWithCustomTileAndPalette, InUnionRoom, IsLinkRecvQueueAtOverworldMax,
  IsOverworldLinkActive, IsTextPrinterActive, ItemIsMail, JOY_NEW,
  JOY_REPEAT, LoadCompressedSpritePalette, LoadCompressedSpriteSheet, MAP_GROUP,
  MAP_NUM, Menu_ProcessInputNoWrapClearOnChoose, Overworld_IsRecvQueueAtMax, PlaySE,
  RunTextPrinters, SetGpuReg, SetHBlankCallback, SetVBlankCallback,
  StartSpriteAnim, StringExpandPlaceholders,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void ResetVramOamAndBgCntRegs(void) */
export function ResetVramOamAndBgCntRegs(): any {
  SetGpuReg(REG_OFFSET_DISPCNT, 0);
      SetGpuReg(REG_OFFSET_BG3CNT, 0);
      SetGpuReg(REG_OFFSET_BG2CNT, 0);
      SetGpuReg(REG_OFFSET_BG1CNT, 0);
      SetGpuReg(REG_OFFSET_BG0CNT, 0);
      CpuFill16(0, VRAM, VRAM_SIZE);
      CpuFill32(0, OAM, OAM_SIZE);
      CpuFill16(0, PLTT, PLTT_SIZE);
}

/** void ResetAllBgsCoordinates(void) */
export function ResetAllBgsCoordinates(): any {
  ChangeBgX(0, 0, BG_COORD_SET);
      ChangeBgY(0, 0, BG_COORD_SET);
      ChangeBgX(1, 0, BG_COORD_SET);
      ChangeBgY(1, 0, BG_COORD_SET);
      ChangeBgX(2, 0, BG_COORD_SET);
      ChangeBgY(2, 0, BG_COORD_SET);
      ChangeBgX(3, 0, BG_COORD_SET);
      ChangeBgY(3, 0, BG_COORD_SET);
}

/** void SetVBlankHBlankCallbacksToNull(void) */
export function SetVBlankHBlankCallbacksToNull(): any {
  SetVBlankCallback(NULL);
      SetHBlankCallback(NULL);
}

/** void DisplayMessageAndContinueTask(u8 taskId, u8 windowId, u16 tileNum, u8 paletteNum, u8 fontId, u8 textSpeed, const u8 *string, void *taskFunc) */
export function DisplayMessageAndContinueTask(taskId: any, windowId: any, tileNum: any, paletteNum: any, fontId: any, textSpeed: any, string: any, taskFunc: any): any {
  sMessageWindowId = windowId;
      DrawDialogFrameWithCustomTileAndPalette(windowId, TRUE, tileNum, paletteNum);

      if (string != gStringVar4)
          StringExpandPlaceholders(gStringVar4, string);

      gTextFlags.canABSpeedUpPrint = 1;
      AddTextPrinterParameterized2(windowId, fontId, gStringVar4, textSpeed, NULL, TEXT_COLOR_DARK_GRAY, TEXT_COLOR_WHITE, TEXT_COLOR_LIGHT_GRAY);
      sMessageNextTask = taskFunc;
      gTasks[taskId].func = Task_ContinueTaskAfterMessagePrints;
}

/** bool16 RunTextPrintersRetIsActive(u8 textPrinterId) */
export function RunTextPrintersRetIsActive(textPrinterId: any): any {
  RunTextPrinters();
      return IsTextPrinterActive(textPrinterId);
}

/** static void Task_ContinueTaskAfterMessagePrints(u8 taskId) */
export function Task_ContinueTaskAfterMessagePrints(taskId: any): any {
  if (!RunTextPrintersRetIsActive(sMessageWindowId))
          sMessageNextTask(taskId);
}

/** void DoYesNoFuncWithChoice(u8 taskId, const struct YesNoFuncTable *data) */
export function DoYesNoFuncWithChoice(taskId: any, data: any): any {
  sYesNo = data;
      gTasks[taskId].func = Task_CallYesOrNoCallback;
}

/** void CreateYesNoMenuWithCallbacks(u8 taskId, const struct WindowTemplate *template, u8 unused1, u8 unused2, u8 unused3, u16 tileStart, u8 palette, const struct YesNoFuncTable *yesNo) */
export function CreateYesNoMenuWithCallbacks(taskId: any, template: any, unused1: any, unused2: any, unused3: any, tileStart: any, palette: any, yesNo: any): any {
  CreateYesNoMenu(template, tileStart, palette, 0);
      sYesNo = yesNo;
      gTasks[taskId].func = Task_CallYesOrNoCallback;
}

/** static void Task_CallYesOrNoCallback(u8 taskId) */
export function Task_CallYesOrNoCallback(taskId: any): any {
  switch (Menu_ProcessInputNoWrapClearOnChoose())
      {
      case 0:
          PlaySE(SE_SELECT);
          sYesNo.yesFunc(taskId);
          break;
      case 1:
      case MENU_B_PRESSED:
          PlaySE(SE_SELECT);
          sYesNo.noFunc(taskId);
          break;
      }
}

/** bool8 AdjustQuantityAccordingToDPadInput(s16 *quantity, u16 max) */
export function AdjustQuantityAccordingToDPadInput(quantity: any, max: any): any {
  let valBefore: any = quantity;

      if (JOY_REPEAT(DPAD_ANY) == DPAD_UP)
      {
          quantity++;
          if (quantity > max)
              quantity = 1;

          if (quantity == valBefore)
          {
              return FALSE;
          }
          else
          {
              PlaySE(SE_SELECT);
              return TRUE;
          }
      }
      else if (JOY_REPEAT(DPAD_ANY) == DPAD_DOWN)
      {
          quantity--;
          if (quantity <= 0)
              quantity = max;

          if (quantity == valBefore)
          {
              return FALSE;
          }
          else
          {
              PlaySE(SE_SELECT);
              return TRUE;
          }
      }
      else if (JOY_REPEAT(DPAD_ANY) == DPAD_RIGHT)
      {
          quantity += 10;
          if (quantity > max)
              quantity = max;

          if (quantity == valBefore)
          {
              return FALSE;
          }
          else
          {
              PlaySE(SE_SELECT);
              return TRUE;
          }
      }
      else if (JOY_REPEAT(DPAD_ANY) == DPAD_LEFT)
      {
          quantity -= 10;
          if (quantity <= 0)
              quantity = 1;

          if (quantity == valBefore)
          {
              return FALSE;
          }
          else
          {
              PlaySE(SE_SELECT);
              return TRUE;
          }
      }

      return FALSE;
}

/** u8 GetLRKeysPressed(void) */
export function GetLRKeysPressed(): any {
  if (gSaveBlock2Ptr.optionsButtonMode == OPTIONS_BUTTON_MODE_LR)
      {
          if (JOY_NEW(L_BUTTON))
              return MENU_L_PRESSED;
          if (JOY_NEW(R_BUTTON))
              return MENU_R_PRESSED;
      }

      return 0;
}

/** u8 GetLRKeysPressedAndHeld(void) */
export function GetLRKeysPressedAndHeld(): any {
  if (gSaveBlock2Ptr.optionsButtonMode == OPTIONS_BUTTON_MODE_LR)
      {
          if (JOY_REPEAT(L_BUTTON))
              return MENU_L_PRESSED;
          if (JOY_REPEAT(R_BUTTON))
              return MENU_R_PRESSED;
      }

      return 0;
}

/** bool8 IsHoldingItemAllowed(u16 itemId) */
export function IsHoldingItemAllowed(itemId: any): any {
  if (itemId == ITEM_ENIGMA_BERRY
       && ((gSaveBlock1Ptr.location.mapGroup == MAP_GROUP(MAP_TRADE_CENTER)
         && gSaveBlock1Ptr.location.mapNum == MAP_NUM(MAP_TRADE_CENTER))
         || InUnionRoom() == TRUE))
          return FALSE;
      else
          return TRUE;
}

/** bool8 IsWritingMailAllowed(u16 itemId) */
export function IsWritingMailAllowed(itemId: any): any {
  if ((IsOverworldLinkActive() == TRUE || InUnionRoom() == TRUE) && ItemIsMail(itemId) == TRUE)
          return FALSE;
      else
          return TRUE;
}

/** bool8 MenuHelpers_IsLinkActive(void) */
export function MenuHelpers_IsLinkActive(): any {
  if (IsOverworldLinkActive() == TRUE || gReceivedRemoteLinkPlayers == 1)
          return TRUE;
      else
          return FALSE;
}

/** static bool8 IsActiveOverworldLinkBusy(void) */
export function IsActiveOverworldLinkBusy(): any {
  if (!MenuHelpers_IsLinkActive())
          return FALSE;
      else
          return Overworld_IsRecvQueueAtMax();
}

/** bool8 MenuHelpers_ShouldWaitForLinkRecv(void) */
export function MenuHelpers_ShouldWaitForLinkRecv(): any {
  if (IsActiveOverworldLinkBusy() == TRUE || IsLinkRecvQueueAtOverworldMax() == TRUE )
          return TRUE;
      else
          return FALSE;
}

/** void SetItemListPerPageCount(struct ItemSlot *slots, u8 slotsCount, u8 *pageItems, u8 *totalItems, u8 maxPerPage) */
export function SetItemListPerPageCount(slots: any, slotsCount: any, pageItems: any, totalItems: any, maxPerPage: any): any {
  let i: any = null;
      let slots_: any = slots;

       
      totalItems = 0;
      for (i = 0; i < slotsCount; i++)
      {
          if (slots_[i].itemId != ITEM_NONE)
              totalItems++;
      }
      totalItems++;  

       
      if (totalItems > maxPerPage)
          pageItems = maxPerPage;
      else
          pageItems = totalItems;
}

/** void SetCursorWithinListBounds(u16 *scrollOffset, u16 *cursorPos, u8 maxShownItems, u8 totalItems) */
export function SetCursorWithinListBounds(scrollOffset: any, cursorPos: any, maxShownItems: any, totalItems: any): any {
  if (scrollOffset != 0 && scrollOffset + maxShownItems > totalItems)
          scrollOffset = totalItems - maxShownItems;

      if (scrollOffset + cursorPos >= totalItems)
      {
          if (totalItems == 0)
              cursorPos = 0;
          else
              cursorPos = totalItems - 1;
      }
}

/** void SetCursorScrollWithinListBounds(u16 *scrollOffset, u16 *cursorPos, u8 shownItems, u8 totalItems, u8 maxShownItems) */
export function SetCursorScrollWithinListBounds(scrollOffset: any, cursorPos: any, shownItems: any, totalItems: any, maxShownItems: any): any {
  let i: any = null;

      if (maxShownItems % 2 != 0)
      {
           
          if (cursorPos >= maxShownItems / 2)
          {
              for (i = 0; i < cursorPos - (maxShownItems / 2); i++)
              {
                   
                  if (scrollOffset + shownItems == totalItems)
                      break;
                  cursorPos--;
                  scrollOffset++;
              }
          }
      }
      else
      {
           
          if (cursorPos >= (maxShownItems / 2) + 1)
          {
              for (i = 0; i <= cursorPos - (maxShownItems / 2); i++)
              {
                   
                  if (scrollOffset + shownItems == totalItems)
                      break;
                  cursorPos--;
                  scrollOffset++;
              }
          }
      }
}

/** void LoadListMenuSwapLineGfx(void) */
export function LoadListMenuSwapLineGfx(): any {
  LoadCompressedSpriteSheet(sSpriteSheet_SwapLine);
      LoadCompressedSpritePalette(sSpritePalette_SwapLine);
}

/** void CreateSwapLineSprites(u8 *spriteIds, u8 count) */
export function CreateSwapLineSprites(spriteIds: any, count: any): any {
  let i: any = null;

      for (i = 0; i < count; i++)
      {
          spriteIds[i] = CreateSprite(sSpriteTemplate_SwapLine, i * 16, 0, 0);
          if (i != 0)
              StartSpriteAnim(gSprites[spriteIds[i]], 1);

          gSprites[spriteIds[i]].invisible = TRUE;
      }
}

/** void DestroySwapLineSprites(u8 *spriteIds, u8 count) */
export function DestroySwapLineSprites(spriteIds: any, count: any): any {
  let i: any = null;

      for (i = 0; i < count; i++)
      {
          if (i == count - 1)
              DestroySpriteAndFreeResources(gSprites[spriteIds[i]]);
          else
              DestroySprite(gSprites[spriteIds[i]]);
      }
}

/** void SetSwapLineSpritesInvisibility(u8 *spriteIds, u8 count, bool8 invisible) */
export function SetSwapLineSpritesInvisibility(spriteIds: any, count: any, invisible: any): any {
  let i: any = null;

      for (i = 0; i < count; i++)
          gSprites[spriteIds[i]].invisible = invisible;
}

/** void UpdateSwapLineSpritesPos(u8 *spriteIds, u8 count, s16 x, u16 y) */
export function UpdateSwapLineSpritesPos(spriteIds: any, count: any, x: any, y: any): any {
  let i: any = null;
      let hasMargin: any = count & SWAP_LINE_HAS_MARGIN;
      count &= ~SWAP_LINE_HAS_MARGIN;

      for (i = 0; i < count; i++)
      {
           
           
           
           
          if (i == count - 1 && hasMargin)
              gSprites[spriteIds[i]].x2 = x - 8;
          else
              gSprites[spriteIds[i]].x2 = x;

          gSprites[spriteIds[i]].y = 1 + y;
      }
}

// ─── callsTo manifest (= 33 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AddTextPrinterParameterized2","ChangeBgX","ChangeBgY","CpuFill16","CpuFill32","CreateSprite","CreateYesNoMenu","DestroySprite","DestroySpriteAndFreeResources","DrawDialogFrameWithCustomTileAndPalette","InUnionRoom","IsActiveOverworldLinkBusy","IsLinkRecvQueueAtOverworldMax","IsOverworldLinkActive","IsTextPrinterActive","ItemIsMail","JOY_NEW","JOY_REPEAT","LoadCompressedSpritePalette","LoadCompressedSpriteSheet","MAP_GROUP","MAP_NUM","MenuHelpers_IsLinkActive","Menu_ProcessInputNoWrapClearOnChoose","Overworld_IsRecvQueueAtMax","PlaySE","RunTextPrinters","RunTextPrintersRetIsActive","SetGpuReg","SetHBlankCallback","SetVBlankCallback","StartSpriteAnim","StringExpandPlaceholders"];
