// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/money.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 15 functions extracted from src/money.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  AddTextPrinterParameterized, AddWindow, ClearStdWindowAndFrameToTransparent, ConvertIntToDecimalStringN,
  CopyWindowToVram, CreateSprite, DestroySpriteAndFreeResources, DrawStdFrameWithCustomTileAndPalette,
  FALSE, FillWindowPixelBuffer, LoadCompressedSpritePalette, LoadCompressedSpriteSheet,
  NULL, PIXEL_FILL, PutWindowTilemap, RemoveWindow,
  STR_CONV_MODE_LEFT_ALIGN, SetWindowTemplateFields, StringExpandPlaceholders, StringLength,
  TRUE,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** u32 GetMoney(u32 *moneyPtr) */
export function GetMoney(moneyPtr: any): any {
  return moneyPtr ^ gSaveBlock2Ptr.encryptionKey;
}

/** void SetMoney(u32 *moneyPtr, u32 newValue) */
export function SetMoney(moneyPtr: any, newValue: any): any {
  moneyPtr = gSaveBlock2Ptr.encryptionKey ^ newValue;
}

/** bool8 IsEnoughMoney(u32 *moneyPtr, u32 cost) */
export function IsEnoughMoney(moneyPtr: any, cost: any): any {
  if (GetMoney(moneyPtr) >= cost)
          return TRUE;
      else
          return FALSE;
}

/** void AddMoney(u32 *moneyPtr, u32 toAdd) */
export function AddMoney(moneyPtr: any, toAdd: any): any {
  let toSet: any = GetMoney(moneyPtr);

       
      if (toSet + toAdd > MAX_MONEY)
      {
          toSet = MAX_MONEY;
      }
      else
      {
          toSet += toAdd;
           
          if (toSet < GetMoney(moneyPtr))
              toSet = MAX_MONEY;
      }

      SetMoney(moneyPtr, toSet);
}

/** void RemoveMoney(u32 *moneyPtr, u32 toSub) */
export function RemoveMoney(moneyPtr: any, toSub: any): any {
  let toSet: any = GetMoney(moneyPtr);

       
      if (toSet < toSub)
          toSet = 0;
      else
          toSet -= toSub;

      SetMoney(moneyPtr, toSet);
}

/** bool8 IsEnoughForCostInVar0x8005(void) */
export function IsEnoughForCostInVar0x8005(): any {
  return IsEnoughMoney(gSaveBlock1Ptr.money, gSpecialVar_0x8005);
}

/** void SubtractMoneyFromVar0x8005(void) */
export function SubtractMoneyFromVar0x8005(): any {
  RemoveMoney(gSaveBlock1Ptr.money, gSpecialVar_0x8005);
}

/** void PrintMoneyAmountInMoneyBox(u8 windowId, int amount, u8 speed) */
export function PrintMoneyAmountInMoneyBox(windowId: any, amount: any, speed: any): any {
  PrintMoneyAmount(windowId, 38, 1, amount, speed);
}

/** void PrintMoneyAmount(u8 windowId, u8 x, u8 y, int amount, u8 speed) */
export function PrintMoneyAmount(windowId: any, x: any, y: any, amount: any, speed: any): any {
  let txtPtr: any = null;
      let strLength: any = null;

      ConvertIntToDecimalStringN(gStringVar1, amount, STR_CONV_MODE_LEFT_ALIGN, 6);

      strLength = 6 - StringLength(gStringVar1);
      txtPtr = gStringVar4;

      while (strLength-- > 0)
          txtPtr =  CHAR_SPACER;

      StringExpandPlaceholders(txtPtr, gText_PokedollarVar1);
      AddTextPrinterParameterized(windowId, FONT_NORMAL, gStringVar4, x, y, speed, NULL);
}

/** void PrintMoneyAmountInMoneyBoxWithBorder(u8 windowId, u16 tileStart, u8 pallete, int amount) */
export function PrintMoneyAmountInMoneyBoxWithBorder(windowId: any, tileStart: any, pallete: any, amount: any): any {
  DrawStdFrameWithCustomTileAndPalette(windowId, FALSE, tileStart, pallete);
      PrintMoneyAmountInMoneyBox(windowId, amount, 0);
}

/** void ChangeAmountInMoneyBox(int amount) */
export function ChangeAmountInMoneyBox(amount: any): any {
  PrintMoneyAmountInMoneyBox(sMoneyBoxWindowId, amount, 0);
}

/** void DrawMoneyBox(int amount, u8 x, u8 y) */
export function DrawMoneyBox(amount: any, x: any, y: any): any {
  let template: any = null;

      SetWindowTemplateFields(template, 0, x + 1, y + 1, 10, 2, 15, 8);
      sMoneyBoxWindowId = AddWindow(template);
      FillWindowPixelBuffer(sMoneyBoxWindowId, PIXEL_FILL(0));
      PutWindowTilemap(sMoneyBoxWindowId);
      CopyWindowToVram(sMoneyBoxWindowId, COPYWIN_MAP);
      PrintMoneyAmountInMoneyBoxWithBorder(sMoneyBoxWindowId, _0x214, 14, amount);
      AddMoneyLabelObject((8 * x) + 24, (8 * y) + 11);
}

/** void HideMoneyBox(void) */
export function HideMoneyBox(): any {
  RemoveMoneyLabelObject();
      ClearStdWindowAndFrameToTransparent(sMoneyBoxWindowId, FALSE);
      CopyWindowToVram(sMoneyBoxWindowId, COPYWIN_GFX);
      RemoveWindow(sMoneyBoxWindowId);
}

/** void AddMoneyLabelObject(u16 x, u16 y) */
export function AddMoneyLabelObject(x: any, y: any): any {
  LoadCompressedSpriteSheet(sSpriteSheet_MoneyLabel);
      LoadCompressedSpritePalette(sSpritePalette_MoneyLabel);
      sMoneyLabelSpriteId = CreateSprite(sSpriteTemplate_MoneyLabel, x, y, 0);
}

/** void RemoveMoneyLabelObject(void) */
export function RemoveMoneyLabelObject(): any {
  DestroySpriteAndFreeResources(gSprites[sMoneyLabelSpriteId]);
}

// ─── callsTo manifest (= 26 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AddMoneyLabelObject","AddTextPrinterParameterized","AddWindow","ClearStdWindowAndFrameToTransparent","ConvertIntToDecimalStringN","CopyWindowToVram","CreateSprite","DestroySpriteAndFreeResources","DrawStdFrameWithCustomTileAndPalette","FillWindowPixelBuffer","GetMoney","IsEnoughMoney","LoadCompressedSpritePalette","LoadCompressedSpriteSheet","PIXEL_FILL","PrintMoneyAmount","PrintMoneyAmountInMoneyBox","PrintMoneyAmountInMoneyBoxWithBorder","PutWindowTilemap","RemoveMoney","RemoveMoneyLabelObject","RemoveWindow","SetMoney","SetWindowTemplateFields","StringExpandPlaceholders","StringLength"];
