// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/play_time.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 5 functions extracted from src/play_time.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  PlayTimeCounter_SetToMax,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void PlayTimeCounter_Reset(void) */
export function PlayTimeCounter_Reset(): any {
  sPlayTimeCounterState = STOPPED;

      gSaveBlock2Ptr.playTimeHours = 0;
      gSaveBlock2Ptr.playTimeMinutes = 0;
      gSaveBlock2Ptr.playTimeSeconds = 0;
      gSaveBlock2Ptr.playTimeVBlanks = 0;
}

/** void PlayTimeCounter_Start(void) */
export function PlayTimeCounter_Start(): any {
  sPlayTimeCounterState = RUNNING;

      if (gSaveBlock2Ptr.playTimeHours > 999)
          PlayTimeCounter_SetToMax();
}

/** void PlayTimeCounter_Stop(void) */
export function PlayTimeCounter_Stop(): any {
  sPlayTimeCounterState = STOPPED;
}

/** void PlayTimeCounter_Update(void) */
export function PlayTimeCounter_Update(): any {
  if (sPlayTimeCounterState != RUNNING)
          return;

      gSaveBlock2Ptr.playTimeVBlanks++;

      if (gSaveBlock2Ptr.playTimeVBlanks < 60)
          return;

      gSaveBlock2Ptr.playTimeVBlanks = 0;
      gSaveBlock2Ptr.playTimeSeconds++;

      if (gSaveBlock2Ptr.playTimeSeconds < 60)
          return;

      gSaveBlock2Ptr.playTimeSeconds = 0;
      gSaveBlock2Ptr.playTimeMinutes++;

      if (gSaveBlock2Ptr.playTimeMinutes < 60)
          return;

      gSaveBlock2Ptr.playTimeMinutes = 0;
      gSaveBlock2Ptr.playTimeHours++;

      if (gSaveBlock2Ptr.playTimeHours > 999)
          PlayTimeCounter_SetToMax();
}

/** void PlayTimeCounter_SetToMax(void) */
export function PlayTimeCounter_SetToMax(): any {
  sPlayTimeCounterState = MAXED_OUT;

      gSaveBlock2Ptr.playTimeHours = 999;
      gSaveBlock2Ptr.playTimeMinutes = 59;
      gSaveBlock2Ptr.playTimeSeconds = 59;
      gSaveBlock2Ptr.playTimeVBlanks = 59;
}

// ─── callsTo manifest (= 1 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["PlayTimeCounter_SetToMax"];
