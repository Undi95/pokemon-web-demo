// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/lottery_corner.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 8 functions extracted from src/lottery_corner.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let gSpecialVar_0x8004: any = null;
let gSpecialVar_0x8005: any = null;
let gSpecialVar_0x8006: any = null;
let sOtIdDigit: any = null;
let sWinNumberDigit: any = null;
/** void ResetLotteryCorner(void) */
export function ResetLotteryCorner(): any {
  let rand: any = Random();

      SetLotteryNumber((Random() << 16) | rand);
      VarSet((0x4045), 0);
}

/** void SetRandomLotteryNumber(u16 i) */
export function SetRandomLotteryNumber(i: any): any {
  let _var: any = Random();

      while (--i != 0xFFFF)
          _var = ISO_RANDOMIZE2(_var);

      SetLotteryNumber(_var);
}

/** void RetrieveLotteryNumber(void) */
export function RetrieveLotteryNumber(): any {
  let lottoNumber: any = GetLotteryNumber();
      gSpecialVar_Result = lottoNumber;
}

/** void PickLotteryCornerTicket(void) */
export function PickLotteryCornerTicket(): any {
  let i: any = null;
      let j: any = null;
      let box: any = null;
      let slot: any = null;

      gSpecialVar_0x8004 = 0;
      slot = 0;
      box = 0;
      for (i = 0; i < (6); i++)
      {
          let mon: any =gPlayerParty[i];

          if (GetMonData(mon, MON_DATA_SPECIES) != (0))
          {
               
              if (!GetMonData(mon, MON_DATA_IS_EGG))
              {
                  let otId: any = GetMonData(mon, MON_DATA_OT_ID);
                  let numMatchingDigits: any = GetMatchingDigits(gSpecialVar_Result, otId);

                  if (numMatchingDigits > gSpecialVar_0x8004 && numMatchingDigits > 1)
                  {
                      gSpecialVar_0x8004 = numMatchingDigits - 1;
                      box = (14);
                      slot = i;
                  }
              }
          }
          else  
          {
              break;
          }
      }

      for (i = 0; i < (14); i++)
      {
          for (j = 0; j < (((5) * (6))); j++)
          {
              if (GetBoxMonData(gPokemonStoragePtr.boxes[i][j], MON_DATA_SPECIES) != (0) &&
              !GetBoxMonData(gPokemonStoragePtr.boxes[i][j], MON_DATA_IS_EGG))
              {
                  let otId: any = GetBoxMonData(gPokemonStoragePtr.boxes[i][j], MON_DATA_OT_ID);
                  let numMatchingDigits: any = GetMatchingDigits(gSpecialVar_Result, otId);

                  if (numMatchingDigits > gSpecialVar_0x8004 && numMatchingDigits > 1)
                  {
                      gSpecialVar_0x8004 = numMatchingDigits - 1;
                      box = i;
                      slot = j;
                  }
              }
          }
      }

      if (gSpecialVar_0x8004 != 0)
      {
          gSpecialVar_0x8005 = sLotteryPrizes[gSpecialVar_0x8004 - 1];

          if (box == (14))
          {
              gSpecialVar_0x8006 = 0;
              GetMonData(gPlayerParty[slot], MON_DATA_NICKNAME, gStringVar1);
          }
          else
          {
              gSpecialVar_0x8006 = 1;
              GetBoxMonData(gPokemonStoragePtr.boxes[box][slot], MON_DATA_NICKNAME, gStringVar1);
          }
          StringGet_Nickname(gStringVar1);
      }
}

/** static u8 GetMatchingDigits(u16 winNumber, u16 otId) */
export function GetMatchingDigits(winNumber: any, otId: any): any {
  let i: any = null;
      let matchingDigits: any = 0;

      for (i = 0; i < 5; i++)
      {
          sWinNumberDigit = winNumber % 10;
          sOtIdDigit = otId % 10;

          if (sWinNumberDigit == sOtIdDigit)
          {
              winNumber = winNumber / 10;
              otId = otId / 10;
              matchingDigits++;
          }
          else
          {
              break;
          }
      }
      return matchingDigits;
}

/** void SetLotteryNumber(u32 lotteryNum) */
export function SetLotteryNumber(lotteryNum: any): any {
  let lowNum: any = lotteryNum >> 16;
      let highNum: any = lotteryNum;

      VarSet((0x404B), highNum);
      VarSet((0x404C), lowNum);
}

/** u32 GetLotteryNumber(void) */
export function GetLotteryNumber(): any {
  let highNum: any = VarGet((0x404B));
      let lowNum: any = VarGet((0x404C));

      return (lowNum << 16) | highNum;
}

/** void SetLotteryNumber16_Unused(u16 lotteryNum) */
export function SetLotteryNumber16_Unused(lotteryNum: any): any {
  SetLotteryNumber(lotteryNum);
}

// ─── callsTo manifest (= 10 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["GetBoxMonData","GetLotteryNumber","GetMatchingDigits","GetMonData","ISO_RANDOMIZE2","Random","SetLotteryNumber","StringGet_Nickname","VarGet","VarSet"];
