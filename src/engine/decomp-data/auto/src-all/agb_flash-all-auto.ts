// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/agb_flash.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 15 functions extracted from src/agb_flash.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  DELAY, FLASH_WRITE, ProgramFlashSector, REG_TMCNT_L,
  SetReadFlash1, SwitchFlashBank, VerifyFlashSector, VerifyFlashSectorNBytes,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void SwitchFlashBank(u8 bankNum) */
export function SwitchFlashBank(bankNum: any): any {
  FLASH_WRITE(_0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, _0x55);
      FLASH_WRITE(_0x5555, 0xB0);
      FLASH_WRITE(_0x0000, bankNum);
}

/** u16 ReadFlashId(void) */
export function ReadFlashId(): any {
  let flashId: any = null;
      const readFlash1Buffer: any[] = [];
      let readFlash1: any = null;

      SetReadFlash1(readFlash1Buffer);
      readFlash1 = (readFlash1Buffer + 1);

       
      FLASH_WRITE(_0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, _0x55);
      FLASH_WRITE(_0x5555, _0x90);
      DELAY();

      flashId = readFlash1(FLASH_BASE + 1) << 8;
      flashId |= readFlash1(FLASH_BASE);

       
      FLASH_WRITE(_0x5555, 0xAA);
      FLASH_WRITE(0x2AAA, _0x55);
      FLASH_WRITE(_0x5555, 0xF0);
      FLASH_WRITE(_0x5555, 0xF0);
      DELAY();

      return flashId;
}

/** void FlashTimerIntr(void) */
export function FlashTimerIntr(): any {
  if (sTimerCount != 0 && --sTimerCount == 0)
          gFlashTimeoutFlag = 1;
}

/** u16 SetFlashTimerIntr(u8 timerNum, void (**intrFunc)(void)) */
export function SetFlashTimerIntr(timerNum: any): any {
  if (timerNum >= 4)
          return 1;

      sTimerNum = timerNum;
      sTimerReg =REG_TMCNT_L(sTimerNum);
      intrFunc = FlashTimerIntr;
      return 0;
}

/** void StartFlashTimer(u8 phase) */
export function StartFlashTimer(phase: any): any {
  let maxTime: any =gFlashMaxTime[phase * 3];
      sSavedIme = REG_IME;
      REG_IME = 0;
      sTimerReg[1] = 0;
      REG_IE |= (INTR_FLAG_TIMER0 << sTimerNum);
      gFlashTimeoutFlag = 0;
      sTimerCount = maxTime++;
      sTimerReg =  maxTime++;
      sTimerReg =  maxTime++;
      REG_IF = (INTR_FLAG_TIMER0 << sTimerNum);
      REG_IME = 1;
}

/** void StopFlashTimer(void) */
export function StopFlashTimer(): any {
  REG_IME = 0;
      sTimerReg =  0;
      sTimerReg =  0;
      REG_IE &= ~(INTR_FLAG_TIMER0 << sTimerNum);
      REG_IME = sSavedIme;
}

/** u8 ReadFlash1(u8 *addr) */
export function ReadFlash1(addr: any): any {
  return addr;
}

/** void SetReadFlash1(u16 *dest) */
export function SetReadFlash1(dest: any): any {
  let src: any = null;
      let i: any = null;

      PollFlashStatus = (dest + 1);

      src = ReadFlash1;
      src = (src ^ 1);

      i = (SetReadFlash1 - ReadFlash1) >> 1;

      while (i != 0)
      {
          dest =  src++;
          i--;
      }
}

/** void ReadFlash_Core(vu8 *src, u8 *dest, u32 size) */
export function ReadFlash_Core(src: any, dest: any, size: any): any {
  while (size-- != 0)
      {
          dest =  src++;
      }
}

/** void ReadFlash(u16 sectorNum, u32 offset, u8 *dest, u32 size) */
export function ReadFlash(sectorNum: any, offset: any, dest: any, size: any): any {
  let src: any = null;
      let i: any = null;
      const readFlash_Core_Buffer: any[] = [];
      let funcSrc: any = null;
      let funcDest: any = null;
      let readFlash_Core: any = null;

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | WAITCNT_SRAM_8;

      if (gFlash.romSize == FLASH_ROM_SIZE_1M)
      {
          SwitchFlashBank(sectorNum / SECTORS_PER_BANK);
          sectorNum %= SECTORS_PER_BANK;
      }

      funcSrc = ReadFlash_Core;
      funcSrc = (funcSrc ^ 1);
      funcDest = readFlash_Core_Buffer;

      i = (ReadFlash - ReadFlash_Core) >> 1;

      while (i != 0)
      {
          funcDest =  funcSrc++;
          i--;
      }

      readFlash_Core = (readFlash_Core_Buffer + 1);

      src = FLASH_BASE + (sectorNum << gFlash.sector.shift) + offset;

      readFlash_Core(src, dest, size);
}

/** u32 VerifyFlashSector_Core(u8 *src, u8 *tgt, u32 size) */
export function VerifyFlashSector_Core(src: any, tgt: any, size: any): any {
  while (size-- != 0)
      {
          if (tgt++ != src++)
              return (tgt - 1);
      }

      return 0;
}

/** u32 VerifyFlashSector(u16 sectorNum, u8 *src) */
export function VerifyFlashSector(sectorNum: any, src: any): any {
  let i: any = null;
      const verifyFlashSector_Core_Buffer: any[] = [];
      let funcSrc: any = null;
      let funcDest: any = null;
      let tgt: any = null;
      let size: any = null;
      let verifyFlashSector_Core: any = null;

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | WAITCNT_SRAM_8;

      if (gFlash.romSize == FLASH_ROM_SIZE_1M)
      {
          SwitchFlashBank(sectorNum / SECTORS_PER_BANK);
          sectorNum %= SECTORS_PER_BANK;
      }

      funcSrc = VerifyFlashSector_Core;
      funcSrc = (funcSrc ^ 1);
      funcDest = verifyFlashSector_Core_Buffer;

      i = (VerifyFlashSector - VerifyFlashSector_Core) >> 1;

      while (i != 0)
      {
          funcDest =  funcSrc++;
          i--;
      }

      verifyFlashSector_Core = (verifyFlashSector_Core_Buffer + 1);

      tgt = FLASH_BASE + (sectorNum << gFlash.sector.shift);
      size = gFlash.sector.size;

      return verifyFlashSector_Core(src, tgt, size);
}

/** u32 VerifyFlashSectorNBytes(u16 sectorNum, u8 *src, u32 n) */
export function VerifyFlashSectorNBytes(sectorNum: any, src: any, n: any): any {
  let i: any = null;
      const verifyFlashSector_Core_Buffer: any[] = [];
      let funcSrc: any = null;
      let funcDest: any = null;
      let tgt: any = null;
      let verifyFlashSector_Core: any = null;

      if (gFlash.romSize == FLASH_ROM_SIZE_1M)
      {
          SwitchFlashBank(sectorNum / SECTORS_PER_BANK);
          sectorNum %= SECTORS_PER_BANK;
      }

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | WAITCNT_SRAM_8;

      funcSrc = VerifyFlashSector_Core;
      funcSrc = (funcSrc ^ 1);
      funcDest = verifyFlashSector_Core_Buffer;

      i = (VerifyFlashSector - VerifyFlashSector_Core) >> 1;

      while (i != 0)
      {
          funcDest =  funcSrc++;
          i--;
      }

      verifyFlashSector_Core = (verifyFlashSector_Core_Buffer + 1);

      tgt = FLASH_BASE + (sectorNum << gFlash.sector.shift);

      return verifyFlashSector_Core(src, tgt, n);
}

/** u32 ProgramFlashSectorAndVerify(u16 sectorNum, u8 *src) */
export function ProgramFlashSectorAndVerify(sectorNum: any, src: any): any {
  let i: any = null;
      let result: any = null;

      for (i = 0; i < 3; i++)
      {
          result = ProgramFlashSector(sectorNum, src);
          if (result != 0)
              continue;

          result = VerifyFlashSector(sectorNum, src);
          if (result == 0)
              break;
      }

      return result;
}

/** u32 ProgramFlashSectorAndVerifyNBytes(u16 sectorNum, u8 *src, u32 n) */
export function ProgramFlashSectorAndVerifyNBytes(sectorNum: any, src: any, n: any): any {
  let i: any = null;
      let result: any = null;

      for (i = 0; i < 3; i++)
      {
          result = ProgramFlashSector(sectorNum, src);
          if (result != 0)
              continue;

          result = VerifyFlashSectorNBytes(sectorNum, src, n);
          if (result == 0)
              break;
      }

      return result;
}

// ─── callsTo manifest (= 8 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["DELAY","FLASH_WRITE","ProgramFlashSector","REG_TMCNT_L","SetReadFlash1","SwitchFlashBank","VerifyFlashSector","VerifyFlashSectorNBytes"];
