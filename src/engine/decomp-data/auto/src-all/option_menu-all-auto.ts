// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/option_menu.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 24 functions extracted from src/option_menu.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, AddTextPrinterParameterized, AnimateSprites, BG_PLTT_ID,
  BLDCNT_EFFECT_DARKEN, BLDCNT_TGT1_BG0, BeginNormalPaletteFade, BuildOamBuffer,
  ChangeBgX, ChangeBgY, CopyBgTilemapBufferToVram, CopyWindowToVram,
  CreateTask, DISPCNT_OBJ_1D_MAP, DISPCNT_OBJ_ON, DISPCNT_WIN0_ON,
  DISPLAY_WIDTH, DeactivateAllTextPrinters, DestroyTask, DmaClear16,
  DmaClear32, DmaClearLarge16, FALSE, FillBgTilemapBufferRect,
  FillWindowPixelBuffer, FreeAllWindowBuffers, GetStringRightAlignXOffset, GetStringWidth,
  GetWindowFrameTilesPal, InitBgsFromTemplates, InitWindows, JOY_NEW,
  LoadBgTiles, LoadOam, LoadPalette, NULL,
  PALETTES_ALL, PIXEL_FILL, PLTT_SIZE, PLTT_SIZE_4BPP,
  ProcessSpriteCopyRequests, PutWindowTilemap, REG_OFFSET_BLDALPHA, REG_OFFSET_BLDCNT,
  REG_OFFSET_BLDY, REG_OFFSET_DISPCNT, REG_OFFSET_WIN0H, REG_OFFSET_WIN0V,
  REG_OFFSET_WININ, REG_OFFSET_WINOUT, ResetBgsAndClearDma3BusyFlags, ResetPaletteFade,
  ResetSpriteData, ResetTasks, RunTasks, ScanlineEffect_Stop,
  SetGpuReg, SetMainCallback2, SetPokemonCryStereo, SetVBlankCallback,
  ShowBg, TRUE, TransferPlttBuffer, UpdatePaletteFade,
  VRAM_SIZE, WIN_RANGE,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static void MainCB2(void) */
export function MainCB2(): any {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
}

/** static void VBlankCB(void) */
export function VBlankCB(): any {
  LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
}

/** void CB2_InitOptionMenu(void) */
export function CB2_InitOptionMenu(): any {
  switch (gMain.state)
      {
      default:
      case 0:
          SetVBlankCallback(NULL);
          gMain.state++;
          break;
      case 1:
          DmaClearLarge16(3, (VRAM), VRAM_SIZE, _0x1000);
          DmaClear32(3, OAM, OAM_SIZE);
          DmaClear16(3, PLTT, PLTT_SIZE);
          SetGpuReg(REG_OFFSET_DISPCNT, 0);
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sOptionMenuBgTemplates, ARRAY_COUNT(sOptionMenuBgTemplates));
          ChangeBgX(0, 0, BG_COORD_SET);
          ChangeBgY(0, 0, BG_COORD_SET);
          ChangeBgX(1, 0, BG_COORD_SET);
          ChangeBgY(1, 0, BG_COORD_SET);
          ChangeBgX(2, 0, BG_COORD_SET);
          ChangeBgY(2, 0, BG_COORD_SET);
          ChangeBgX(3, 0, BG_COORD_SET);
          ChangeBgY(3, 0, BG_COORD_SET);
          InitWindows(sOptionMenuWinTemplates);
          DeactivateAllTextPrinters();
          SetGpuReg(REG_OFFSET_WIN0H, 0);
          SetGpuReg(REG_OFFSET_WIN0V, 0);
          SetGpuReg(REG_OFFSET_WININ, WININ_WIN0_BG0);
          SetGpuReg(REG_OFFSET_WINOUT, WINOUT_WIN01_BG0 | WINOUT_WIN01_BG1 | WINOUT_WIN01_CLR);
          SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG0 | BLDCNT_EFFECT_DARKEN);
          SetGpuReg(REG_OFFSET_BLDALPHA, 0);
          SetGpuReg(REG_OFFSET_BLDY, 4);
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_WIN0_ON | DISPCNT_OBJ_ON | DISPCNT_OBJ_1D_MAP);
          ShowBg(0);
          ShowBg(1);
          gMain.state++;
          break;
      case 2:
          ResetPaletteFade();
          ScanlineEffect_Stop();
          ResetTasks();
          ResetSpriteData();
          gMain.state++;
          break;
      case 3:
          LoadBgTiles(1, GetWindowFrameTilesPal(gSaveBlock2Ptr.optionsWindowFrameType).tiles, _0x120, 0x1A2);
          gMain.state++;
          break;
      case 4:
          LoadPalette(sOptionMenuBg_Pal, BG_PLTT_ID(0), 0);
          LoadPalette(GetWindowFrameTilesPal(gSaveBlock2Ptr.optionsWindowFrameType).pal, BG_PLTT_ID(7), PLTT_SIZE_4BPP);
          gMain.state++;
          break;
      case 5:
          LoadPalette(sOptionMenuText_Pal, BG_PLTT_ID(1), 0);
          gMain.state++;
          break;
      case 6:
          PutWindowTilemap(WIN_HEADER);
          DrawHeaderText();
          gMain.state++;
          break;
      case 7:
          gMain.state++;
          break;
      case 8:
          PutWindowTilemap(WIN_OPTIONS);
          DrawOptionMenuTexts();
          gMain.state++;
      case 9:
          DrawBgWindowFrames();
          gMain.state++;
          break;
      case 10:
      {
          let taskId: any = CreateTask(Task_OptionMenuFadeIn, 0);

          gTasks[taskId].tMenuSelection = 0;
          gTasks[taskId].tTextSpeed = gSaveBlock2Ptr.optionsTextSpeed;
          gTasks[taskId].tBattleSceneOff = gSaveBlock2Ptr.optionsBattleSceneOff;
          gTasks[taskId].tBattleStyle = gSaveBlock2Ptr.optionsBattleStyle;
          gTasks[taskId].tSound = gSaveBlock2Ptr.optionsSound;
          gTasks[taskId].tButtonMode = gSaveBlock2Ptr.optionsButtonMode;
          gTasks[taskId].tWindowFrameType = gSaveBlock2Ptr.optionsWindowFrameType;

          TextSpeed_DrawChoices(gTasks[taskId].tTextSpeed);
          BattleScene_DrawChoices(gTasks[taskId].tBattleSceneOff);
          BattleStyle_DrawChoices(gTasks[taskId].tBattleStyle);
          Sound_DrawChoices(gTasks[taskId].tSound);
          ButtonMode_DrawChoices(gTasks[taskId].tButtonMode);
          FrameType_DrawChoices(gTasks[taskId].tWindowFrameType);
          HighlightOptionMenuItem(gTasks[taskId].tMenuSelection);

          CopyWindowToVram(WIN_OPTIONS, COPYWIN_FULL);
          gMain.state++;
          break;
      }
      case 11:
          BeginNormalPaletteFade(PALETTES_ALL, 0, 16, 0, RGB_BLACK);
          SetVBlankCallback(VBlankCB);
          SetMainCallback2(MainCB2);
          return;
      }
}

/** static void Task_OptionMenuFadeIn(u8 taskId) */
export function Task_OptionMenuFadeIn(taskId: any): any {
  if (!gPaletteFade.active)
          gTasks[taskId].func = Task_OptionMenuProcessInput;
}

/** static void Task_OptionMenuProcessInput(u8 taskId) */
export function Task_OptionMenuProcessInput(taskId: any): any {
  if (JOY_NEW(A_BUTTON))
      {
          if (gTasks[taskId].tMenuSelection == MENUITEM_CANCEL)
              gTasks[taskId].func = Task_OptionMenuSave;
      }
      else if (JOY_NEW(B_BUTTON))
      {
          gTasks[taskId].func = Task_OptionMenuSave;
      }
      else if (JOY_NEW(DPAD_UP))
      {
          if (gTasks[taskId].tMenuSelection > 0)
              gTasks[taskId].tMenuSelection--;
          else
              gTasks[taskId].tMenuSelection = MENUITEM_CANCEL;
          HighlightOptionMenuItem(gTasks[taskId].tMenuSelection);
      }
      else if (JOY_NEW(DPAD_DOWN))
      {
          if (gTasks[taskId].tMenuSelection < MENUITEM_CANCEL)
              gTasks[taskId].tMenuSelection++;
          else
              gTasks[taskId].tMenuSelection = 0;
          HighlightOptionMenuItem(gTasks[taskId].tMenuSelection);
      }
      else
      {
          let previousOption: any = null;

          switch (gTasks[taskId].tMenuSelection)
          {
          case MENUITEM_TEXTSPEED:
              previousOption = gTasks[taskId].tTextSpeed;
              gTasks[taskId].tTextSpeed = TextSpeed_ProcessInput(gTasks[taskId].tTextSpeed);

              if (previousOption != gTasks[taskId].tTextSpeed)
                  TextSpeed_DrawChoices(gTasks[taskId].tTextSpeed);
              break;
          case MENUITEM_BATTLESCENE:
              previousOption = gTasks[taskId].tBattleSceneOff;
              gTasks[taskId].tBattleSceneOff = BattleScene_ProcessInput(gTasks[taskId].tBattleSceneOff);

              if (previousOption != gTasks[taskId].tBattleSceneOff)
                  BattleScene_DrawChoices(gTasks[taskId].tBattleSceneOff);
              break;
          case MENUITEM_BATTLESTYLE:
              previousOption = gTasks[taskId].tBattleStyle;
              gTasks[taskId].tBattleStyle = BattleStyle_ProcessInput(gTasks[taskId].tBattleStyle);

              if (previousOption != gTasks[taskId].tBattleStyle)
                  BattleStyle_DrawChoices(gTasks[taskId].tBattleStyle);
              break;
          case MENUITEM_SOUND:
              previousOption = gTasks[taskId].tSound;
              gTasks[taskId].tSound = Sound_ProcessInput(gTasks[taskId].tSound);

              if (previousOption != gTasks[taskId].tSound)
                  Sound_DrawChoices(gTasks[taskId].tSound);
              break;
          case MENUITEM_BUTTONMODE:
              previousOption = gTasks[taskId].tButtonMode;
              gTasks[taskId].tButtonMode = ButtonMode_ProcessInput(gTasks[taskId].tButtonMode);

              if (previousOption != gTasks[taskId].tButtonMode)
                  ButtonMode_DrawChoices(gTasks[taskId].tButtonMode);
              break;
          case MENUITEM_FRAMETYPE:
              previousOption = gTasks[taskId].tWindowFrameType;
              gTasks[taskId].tWindowFrameType = FrameType_ProcessInput(gTasks[taskId].tWindowFrameType);

              if (previousOption != gTasks[taskId].tWindowFrameType)
                  FrameType_DrawChoices(gTasks[taskId].tWindowFrameType);
              break;
          default:
              return;
          }

          if (sArrowPressed)
          {
              sArrowPressed = FALSE;
              CopyWindowToVram(WIN_OPTIONS, COPYWIN_GFX);
          }
      }
}

/** static void Task_OptionMenuSave(u8 taskId) */
export function Task_OptionMenuSave(taskId: any): any {
  gSaveBlock2Ptr.optionsTextSpeed = gTasks[taskId].tTextSpeed;
      gSaveBlock2Ptr.optionsBattleSceneOff = gTasks[taskId].tBattleSceneOff;
      gSaveBlock2Ptr.optionsBattleStyle = gTasks[taskId].tBattleStyle;
      gSaveBlock2Ptr.optionsSound = gTasks[taskId].tSound;
      gSaveBlock2Ptr.optionsButtonMode = gTasks[taskId].tButtonMode;
      gSaveBlock2Ptr.optionsWindowFrameType = gTasks[taskId].tWindowFrameType;

      BeginNormalPaletteFade(PALETTES_ALL, 0, 0, 16, RGB_BLACK);
      gTasks[taskId].func = Task_OptionMenuFadeOut;
}

/** static void Task_OptionMenuFadeOut(u8 taskId) */
export function Task_OptionMenuFadeOut(taskId: any): any {
  if (!gPaletteFade.active)
      {
          DestroyTask(taskId);
          FreeAllWindowBuffers();
          SetMainCallback2(gMain.savedCallback);
      }
}

/** static void HighlightOptionMenuItem(u8 index) */
export function HighlightOptionMenuItem(index: any): any {
  SetGpuReg(REG_OFFSET_WIN0H, WIN_RANGE(16, DISPLAY_WIDTH - 16));
      SetGpuReg(REG_OFFSET_WIN0V, WIN_RANGE(index * 16 + 40, index * 16 + 56));
}

/** static void DrawOptionMenuChoice(const u8 *text, u8 x, u8 y, u8 style) */
export function DrawOptionMenuChoice(text: any, x: any, y: any, style: any): any {
  const dst: any[] = [];
      let i: any = null;

      for (i = 0; text != EOS && i < ARRAY_COUNT(dst) - 1; i++)
          dst[i] = (text++);

      if (style != 0)
      {
          dst[2] = TEXT_COLOR_RED;
          dst[5] = TEXT_COLOR_LIGHT_RED;
      }

      dst[i] = EOS;
      AddTextPrinterParameterized(WIN_OPTIONS, FONT_NORMAL, dst, x, y + 1, TEXT_SKIP_DRAW, NULL);
}

/** static u8 TextSpeed_ProcessInput(u8 selection) */
export function TextSpeed_ProcessInput(selection: any): any {
  if (JOY_NEW(DPAD_RIGHT))
      {
          if (selection <= 1)
              selection++;
          else
              selection = 0;

          sArrowPressed = TRUE;
      }
      if (JOY_NEW(DPAD_LEFT))
      {
          if (selection != 0)
              selection--;
          else
              selection = 2;

          sArrowPressed = TRUE;
      }
      return selection;
}

/** static void TextSpeed_DrawChoices(u8 selection) */
export function TextSpeed_DrawChoices(selection: any): any {
  const styles: any[] = [];
      let widthSlow, widthMid, widthFast, xMid;

      styles[0] = 0;
      styles[1] = 0;
      styles[2] = 0;
      styles[selection] = 1;

      DrawOptionMenuChoice(gText_TextSpeedSlow, 104, YPOS_TEXTSPEED, styles[0]);

      widthSlow = GetStringWidth(FONT_NORMAL, gText_TextSpeedSlow, 0);
      widthMid = GetStringWidth(FONT_NORMAL, gText_TextSpeedMid, 0);
      widthFast = GetStringWidth(FONT_NORMAL, gText_TextSpeedFast, 0);

      widthMid -= 94;
      xMid = (widthSlow - widthMid - widthFast) / 2 + 104;
      DrawOptionMenuChoice(gText_TextSpeedMid, xMid, YPOS_TEXTSPEED, styles[1]);

      DrawOptionMenuChoice(gText_TextSpeedFast, GetStringRightAlignXOffset(FONT_NORMAL, gText_TextSpeedFast, 198), YPOS_TEXTSPEED, styles[2]);
}

/** static u8 BattleScene_ProcessInput(u8 selection) */
export function BattleScene_ProcessInput(selection: any): any {
  if (JOY_NEW(DPAD_LEFT | DPAD_RIGHT))
      {
          selection ^= 1;
          sArrowPressed = TRUE;
      }

      return selection;
}

/** static void BattleScene_DrawChoices(u8 selection) */
export function BattleScene_DrawChoices(selection: any): any {
  const styles: any[] = [];

      styles[0] = 0;
      styles[1] = 0;
      styles[selection] = 1;

      DrawOptionMenuChoice(gText_BattleSceneOn, 104, YPOS_BATTLESCENE, styles[0]);
      DrawOptionMenuChoice(gText_BattleSceneOff, GetStringRightAlignXOffset(FONT_NORMAL, gText_BattleSceneOff, 198), YPOS_BATTLESCENE, styles[1]);
}

/** static u8 BattleStyle_ProcessInput(u8 selection) */
export function BattleStyle_ProcessInput(selection: any): any {
  if (JOY_NEW(DPAD_LEFT | DPAD_RIGHT))
      {
          selection ^= 1;
          sArrowPressed = TRUE;
      }

      return selection;
}

/** static void BattleStyle_DrawChoices(u8 selection) */
export function BattleStyle_DrawChoices(selection: any): any {
  const styles: any[] = [];

      styles[0] = 0;
      styles[1] = 0;
      styles[selection] = 1;

      DrawOptionMenuChoice(gText_BattleStyleShift, 104, YPOS_BATTLESTYLE, styles[0]);
      DrawOptionMenuChoice(gText_BattleStyleSet, GetStringRightAlignXOffset(FONT_NORMAL, gText_BattleStyleSet, 198), YPOS_BATTLESTYLE, styles[1]);
}

/** static u8 Sound_ProcessInput(u8 selection) */
export function Sound_ProcessInput(selection: any): any {
  if (JOY_NEW(DPAD_LEFT | DPAD_RIGHT))
      {
          selection ^= 1;
          SetPokemonCryStereo(selection);
          sArrowPressed = TRUE;
      }

      return selection;
}

/** static void Sound_DrawChoices(u8 selection) */
export function Sound_DrawChoices(selection: any): any {
  const styles: any[] = [];

      styles[0] = 0;
      styles[1] = 0;
      styles[selection] = 1;

      DrawOptionMenuChoice(gText_SoundMono, 104, YPOS_SOUND, styles[0]);
      DrawOptionMenuChoice(gText_SoundStereo, GetStringRightAlignXOffset(FONT_NORMAL, gText_SoundStereo, 198), YPOS_SOUND, styles[1]);
}

/** static u8 FrameType_ProcessInput(u8 selection) */
export function FrameType_ProcessInput(selection: any): any {
  if (JOY_NEW(DPAD_RIGHT))
      {
          if (selection < WINDOW_FRAMES_COUNT - 1)
              selection++;
          else
              selection = 0;

          LoadBgTiles(1, GetWindowFrameTilesPal(selection).tiles, _0x120, 0x1A2);
          LoadPalette(GetWindowFrameTilesPal(selection).pal, BG_PLTT_ID(7), PLTT_SIZE_4BPP);
          sArrowPressed = TRUE;
      }
      if (JOY_NEW(DPAD_LEFT))
      {
          if (selection != 0)
              selection--;
          else
              selection = WINDOW_FRAMES_COUNT - 1;

          LoadBgTiles(1, GetWindowFrameTilesPal(selection).tiles, _0x120, 0x1A2);
          LoadPalette(GetWindowFrameTilesPal(selection).pal, BG_PLTT_ID(7), PLTT_SIZE_4BPP);
          sArrowPressed = TRUE;
      }
      return selection;
}

/** static void FrameType_DrawChoices(u8 selection) */
export function FrameType_DrawChoices(selection: any): any {
  const text: any[] = [];
      let n: any = selection + 1;
      let i: any = null;
      let width: any = null;  

      for (i = 0; gText_FrameTypeNumber[i] != EOS && i <= 5; i++)
          text[i] = gText_FrameTypeNumber[i];

       
      if (n / 10 != 0)
      {
          text[i] = n / 10 + CHAR_0;
          i++;
          text[i] = n % 10 + CHAR_0;
          i++;
      }
      else
      {
          text[i] = n % 10 + CHAR_0;
          i++;
          text[i] = CHAR_SPACER;
          i++;
      }

      text[i] = EOS;

       
      width = GetStringWidth(1, gText_FrameType, 0);
      DrawOptionMenuChoice(gText_FrameType, 104, YPOS_FRAMETYPE, 0);
      DrawOptionMenuChoice(text, width + 107, YPOS_FRAMETYPE, 1);
}

/** static u8 ButtonMode_ProcessInput(u8 selection) */
export function ButtonMode_ProcessInput(selection: any): any {
  if (JOY_NEW(DPAD_RIGHT))
      {
          if (selection <= 1)
              selection++;
          else
              selection = 0;

          sArrowPressed = TRUE;
      }
      if (JOY_NEW(DPAD_LEFT))
      {
          if (selection != 0)
              selection--;
          else
              selection = 2;

          sArrowPressed = TRUE;
      }
      return selection;
}

/** static void ButtonMode_DrawChoices(u8 selection) */
export function ButtonMode_DrawChoices(selection: any): any {
  let widthNormal, widthLR, widthLA, xLR;
      const styles: any[] = [];

      styles[0] = 0;
      styles[1] = 0;
      styles[2] = 0;
      styles[selection] = 1;

      DrawOptionMenuChoice(gText_ButtonTypeNormal, 104, YPOS_BUTTONMODE, styles[0]);

      widthNormal = GetStringWidth(FONT_NORMAL, gText_ButtonTypeNormal, 0);
      widthLR = GetStringWidth(FONT_NORMAL, gText_ButtonTypeLR, 0);
      widthLA = GetStringWidth(FONT_NORMAL, gText_ButtonTypeLEqualsA, 0);

      widthLR -= 94;
      xLR = (widthNormal - widthLR - widthLA) / 2 + 104;
      DrawOptionMenuChoice(gText_ButtonTypeLR, xLR, YPOS_BUTTONMODE, styles[1]);

      DrawOptionMenuChoice(gText_ButtonTypeLEqualsA, GetStringRightAlignXOffset(FONT_NORMAL, gText_ButtonTypeLEqualsA, 198), YPOS_BUTTONMODE, styles[2]);
}

/** static void DrawHeaderText(void) */
export function DrawHeaderText(): any {
  FillWindowPixelBuffer(WIN_HEADER, PIXEL_FILL(1));
      AddTextPrinterParameterized(WIN_HEADER, FONT_NORMAL, gText_Option, 8, 1, TEXT_SKIP_DRAW, NULL);
      CopyWindowToVram(WIN_HEADER, COPYWIN_FULL);
}

/** static void DrawOptionMenuTexts(void) */
export function DrawOptionMenuTexts(): any {
  let i: any = null;

      FillWindowPixelBuffer(WIN_OPTIONS, PIXEL_FILL(1));
      for (i = 0; i < MENUITEM_COUNT; i++)
          AddTextPrinterParameterized(WIN_OPTIONS, FONT_NORMAL, sOptionMenuItemsNames[i], 8, (i * 16) + 1, TEXT_SKIP_DRAW, NULL);
      CopyWindowToVram(WIN_OPTIONS, COPYWIN_FULL);
}

/** static void DrawBgWindowFrames(void) */
export function DrawBgWindowFrames(): any {
  FillBgTilemapBufferRect(1, TILE_TOP_CORNER_L,  1,  0,  1,  1,  7);
      FillBgTilemapBufferRect(1, TILE_TOP_EDGE,      2,  0, 27,  1,  7);
      FillBgTilemapBufferRect(1, TILE_TOP_CORNER_R, 28,  0,  1,  1,  7);
      FillBgTilemapBufferRect(1, TILE_LEFT_EDGE,     1,  1,  1,  2,  7);
      FillBgTilemapBufferRect(1, TILE_RIGHT_EDGE,   28,  1,  1,  2,  7);
      FillBgTilemapBufferRect(1, TILE_BOT_CORNER_L,  1,  3,  1,  1,  7);
      FillBgTilemapBufferRect(1, TILE_BOT_EDGE,      2,  3, 27,  1,  7);
      FillBgTilemapBufferRect(1, TILE_BOT_CORNER_R, 28,  3,  1,  1,  7);

       
      FillBgTilemapBufferRect(1, TILE_TOP_CORNER_L,  1,  4,  1,  1,  7);
      FillBgTilemapBufferRect(1, TILE_TOP_EDGE,      2,  4, 26,  1,  7);
      FillBgTilemapBufferRect(1, TILE_TOP_CORNER_R, 28,  4,  1,  1,  7);
      FillBgTilemapBufferRect(1, TILE_LEFT_EDGE,     1,  5,  1, 18,  7);
      FillBgTilemapBufferRect(1, TILE_RIGHT_EDGE,   28,  5,  1, 18,  7);
      FillBgTilemapBufferRect(1, TILE_BOT_CORNER_L,  1, 19,  1,  1,  7);
      FillBgTilemapBufferRect(1, TILE_BOT_EDGE,      2, 19, 26,  1,  7);
      FillBgTilemapBufferRect(1, TILE_BOT_CORNER_R, 28, 19,  1,  1,  7);

      CopyBgTilemapBufferToVram(1);
}

// ─── callsTo manifest (= 62 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AddTextPrinterParameterized","AnimateSprites","BG_PLTT_ID","BattleScene_DrawChoices","BattleScene_ProcessInput","BattleStyle_DrawChoices","BattleStyle_ProcessInput","BeginNormalPaletteFade","BuildOamBuffer","ButtonMode_DrawChoices","ButtonMode_ProcessInput","ChangeBgX","ChangeBgY","CopyBgTilemapBufferToVram","CopyWindowToVram","CreateTask","DeactivateAllTextPrinters","DestroyTask","DmaClear16","DmaClear32","DmaClearLarge16","DrawBgWindowFrames","DrawHeaderText","DrawOptionMenuChoice","DrawOptionMenuTexts","FillBgTilemapBufferRect","FillWindowPixelBuffer","FrameType_DrawChoices","FrameType_ProcessInput","FreeAllWindowBuffers","GetStringRightAlignXOffset","GetStringWidth","GetWindowFrameTilesPal","HighlightOptionMenuItem","InitBgsFromTemplates","InitWindows","JOY_NEW","LoadBgTiles","LoadOam","LoadPalette","PIXEL_FILL","ProcessSpriteCopyRequests","PutWindowTilemap","ResetBgsAndClearDma3BusyFlags","ResetPaletteFade","ResetSpriteData","ResetTasks","RunTasks","ScanlineEffect_Stop","SetGpuReg","SetMainCallback2","SetPokemonCryStereo","SetVBlankCallback","ShowBg","Sound_DrawChoices","Sound_ProcessInput","TextSpeed_DrawChoices","TextSpeed_ProcessInput","TransferPlttBuffer","UpdatePaletteFade","WIN_RANGE"];
