// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/agb_flash_1m.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 2 functions extracted from src/agb_flash_1m.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  FLASH_WRITE, PollFlashStatus, ReadFlashId, StartFlashTimer,
  StopFlashTimer,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** u16 IdentifyFlash(void) */
export function IdentifyFlash(): any {
  let result: any = null;
      let flashId: any = null;
      const struct FlashSetupInfo const setupInfo;

      REG_WAITCNT = (REG_WAITCNT & ~WAITCNT_SRAM_MASK) | WAITCNT_SRAM_8;

      flashId = ReadFlashId();

      setupInfo = sSetupInfos;
      result = 1;

      for (;;)
      {
          if ((setupInfo).type.ids.separate.makerId == 0)
              break;

          if (flashId == (setupInfo).type.ids.joined)
          {
              result = 0;
              break;
          }

          setupInfo++;
      }

      ProgramFlashByte = (setupInfo).programFlashByte;
      ProgramFlashSector = (setupInfo).programFlashSector;
      EraseFlashChip = (setupInfo).eraseFlashChip;
      EraseFlashSector = (setupInfo).eraseFlashSector;
      WaitForFlashWrite = (setupInfo).WaitForFlashWrite;
      gFlashMaxTime = (setupInfo).maxTime;
      gFlash =(setupInfo).type;

      return result;
}

/** u16 WaitForFlashWrite_Common(u8 phase, u8 *addr, u8 lastData) */
export function WaitForFlashWrite_Common(phase: any, addr: any, lastData: any): any {
  let result: any = 0;
      let status: any = null;

      StartFlashTimer(phase);

      while ((status = PollFlashStatus(addr)) != lastData)
      {
          if (status & _0x20)
          {
               

              if (PollFlashStatus(addr) == lastData)
                  break;

              FLASH_WRITE(_0x5555, 0xF0);
              result = phase | 0xA000;
              break;
          }

          if (gFlashTimeoutFlag)
          {
              if (PollFlashStatus(addr) == lastData)
                  break;

              FLASH_WRITE(_0x5555, 0xF0);
              result = phase | 0xC000;
              break;
          }
      }

      StopFlashTimer();

      return result;
}

// ─── callsTo manifest (= 5 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["FLASH_WRITE","PollFlashStatus","ReadFlashId","StartFlashTimer","StopFlashTimer"];
