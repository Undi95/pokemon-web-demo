// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/post_battle_event_funcs.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 2 functions extracted from src/post_battle_event_funcs.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let gHasHallOfFameRecords: any = null;
/** int GameClear(void) */
export function GameClear(): any {
  let i: any = null;
      let ribbonGet: any = null;
      let ribbonCounts: any[] = []; /* transpiler bug : inline struct decl */

      HealPlayerParty();

      if (FlagGet((((((((0x500) + (864) - 1)) + 1)) + 0x4))) == TRUE)
      {
          gHasHallOfFameRecords = TRUE;
      }
      else
      {
          gHasHallOfFameRecords = FALSE;
          FlagSet((((((((0x500) + (864) - 1)) + 1)) + 0x4)));
      }

      if (GetGameStat((1)) == 0)
          SetGameStat((1), (gSaveBlock2Ptr.playTimeHours << 16) | (gSaveBlock2Ptr.playTimeMinutes << 8) | gSaveBlock2Ptr.playTimeSeconds);

      SetContinueGameWarpStatus();

      if (gSaveBlock2Ptr.playerGender == (0))
          SetContinueGameWarpToHealLocation(HEAL_LOCATION_LITTLEROOT_TOWN_BRENDANS_HOUSE_2F);
      else
          SetContinueGameWarpToHealLocation(HEAL_LOCATION_LITTLEROOT_TOWN_MAYS_HOUSE_2F);

      ribbonGet = FALSE;

      for (i = 0; i < (6); i++)
      {
          let mon: any =gPlayerParty[i];

          ribbonCounts[i].partyIndex = i;
          ribbonCounts[i].count = 0;

          if (GetMonData(mon, MON_DATA_SANITY_HAS_SPECIES)
           && !GetMonData(mon, MON_DATA_SANITY_IS_EGG)
           && !GetMonData(mon, MON_DATA_CHAMPION_RIBBON))
          {
              const val: any = [TRUE];
              SetMonData(mon, MON_DATA_CHAMPION_RIBBON, val);
              ribbonCounts[i].count = GetRibbonCount(mon);
              ribbonGet = TRUE;
          }
      }

      if (ribbonGet == TRUE)
      {
          IncrementGameStat((42));
          FlagSet((((((((0x500) + (864) - 1)) + 1)) + 0x3B)));

          for (i = 1; i < 6; i++)
          {
              if (ribbonCounts[i].count > ribbonCounts[0].count)
              {
                  let prevBest: any = ribbonCounts[0];
                  ribbonCounts[0] = ribbonCounts[i];
                  ribbonCounts[i] = prevBest;
              }
          }

          if (ribbonCounts[0].count > (4))
          {
              TryPutSpotTheCutiesOnAir(gPlayerParty[ribbonCounts[0].partyIndex], MON_DATA_CHAMPION_RIBBON);
          }
      }

      SetMainCallback2(CB2_DoHallOfFameScreen);
      return 0;
}

/** bool8 SetCB2WhiteOut(void) */
export function SetCB2WhiteOut(): any {
  SetMainCallback2(CB2_WhiteOut);
      return FALSE;
}

// ─── callsTo manifest (= 13 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["FlagGet","FlagSet","GetGameStat","GetMonData","GetRibbonCount","HealPlayerParty","IncrementGameStat","SetContinueGameWarpStatus","SetContinueGameWarpToHealLocation","SetGameStat","SetMainCallback2","SetMonData","TryPutSpotTheCutiesOnAir"];
