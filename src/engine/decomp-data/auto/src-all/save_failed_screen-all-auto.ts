// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/save_failed_screen.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 12 functions extracted from src/save_failed_screen.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let gGameContinueCallback: any = null;
let sSaveFailedType: any = null;
/** static void SaveFailedScreenTextPrint(const u8 *text, u8 x, u8 y) */
export function SaveFailedScreenTextPrint(text: any, x: any, y: any): any {
  let color: any = [];

      color[0] = (0x0);
      color[1] = (0xF);
      color[2] = (0x3);
      AddTextPrinterParameterized4(sWindowIds[TEXT_WIN_ID], FONT_NORMAL, x * 8, y * 8 + 1, 0, 0, color, 0, text);
}

/** void DoSaveFailedScreen(u8 saveType) */
export function DoSaveFailedScreen(saveType: any): any {
  SetMainCallback2(CB2_SaveFailedScreen);
      sSaveFailedType = saveType;
      sClockInfo[CLOCK_RUNNING] = FALSE;
      sClockInfo[DEBUG_TIMER] = 0;
      sWindowIds[TEXT_WIN_ID] = 0;
      sWindowIds[CLOCK_WIN_ID] = 0;
}

/** static void VBlankCB(void) */
export function VBlankCB(): any {
  LoadOam();
      ProcessSpriteCopyRequests();
      TransferPlttBuffer();
}

/** static void CB2_SaveFailedScreen(void) */
export function CB2_SaveFailedScreen(): any {
  switch (gMain.state)
      {
      case 0:
      default:
          SetVBlankCallback(NULL);
          SetGpuReg(REG_OFFSET_DISPCNT, 0);
          SetGpuReg(REG_OFFSET_BG3CNT, 0);
          SetGpuReg(REG_OFFSET_BG2CNT, 0);
          SetGpuReg(REG_OFFSET_BG1CNT, 0);
          SetGpuReg(REG_OFFSET_BG0CNT, 0);
          SetGpuReg(REG_OFFSET_BG3HOFS, 0);
          SetGpuReg(REG_OFFSET_BG3VOFS, 0);
          SetGpuReg(REG_OFFSET_BG2HOFS, 0);
          SetGpuReg(REG_OFFSET_BG2VOFS, 0);
          SetGpuReg(REG_OFFSET_BG1HOFS, 0);
          SetGpuReg(REG_OFFSET_BG1VOFS, 0);
          SetGpuReg(REG_OFFSET_BG0HOFS, 0);
          SetGpuReg(REG_OFFSET_BG0VOFS, 0);
          DmaFill16(3, 0, VRAM, VRAM_SIZE);
          DmaFill32(3, 0, OAM, OAM_SIZE);
          DmaFill16(3, 0, PLTT, PLTT_SIZE);
          LZ77UnCompVram(gBirchBagGrass_Gfx, VRAM);
          LZ77UnCompVram(gBirchBagTilemap, (BG_SCREEN_ADDR(14)));
          LZ77UnCompVram(gBirchGrassTilemap, (BG_SCREEN_ADDR(15)));
          LZ77UnCompVram(sSaveFailedClockGfx, (OBJ_VRAM0 + 0x20));
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sBgTemplates, ARRAY_COUNT(sBgTemplates));
          SetBgTilemapBuffer(0,gDecompressionBuffer[0x2000]);
          CpuFill32(0,gDecompressionBuffer[0x2000], 0x800);
          LoadBgTiles(0, gTextWindowFrame1_Gfx, 0x120, 0x214);
          InitWindows(sDummyWindowTemplate);
          sWindowIds[TEXT_WIN_ID] = AddWindowWithoutTileMap(sWindowTemplate_Text);
          SetWindowAttribute(sWindowIds[TEXT_WIN_ID], 7, (u32)&gDecompressionBuffer[0x2800]);
          sWindowIds[CLOCK_WIN_ID] = AddWindowWithoutTileMap(sWindowTemplate_Clock);
          SetWindowAttribute(sWindowIds[CLOCK_WIN_ID], 7, (u32)&gDecompressionBuffer[0x3D00]);
          DeactivateAllTextPrinters();
          ResetSpriteData();
          ResetTasks();
          ResetPaletteFade();
          LoadPalette(gBirchBagGrass_Pal, BG_PLTT_ID(0), 2 * PLTT_SIZE_4BPP);
          LoadPalette(sSaveFailedClockPal, OBJ_PLTT_ID(0), PLTT_SIZE_4BPP);
          LoadPalette(gTextWindowFrame1_Pal, BG_PLTT_ID(14), PLTT_SIZE_4BPP);
          LoadPalette(gStandardMenuPalette, BG_PLTT_ID(15), PLTT_SIZE_4BPP);
          DrawStdFrameWithCustomTileAndPalette(sWindowIds[TEXT_WIN_ID], FALSE, 0x214, 0xE);
          DrawStdFrameWithCustomTileAndPalette(sWindowIds[CLOCK_WIN_ID], FALSE, 0x214, 0xE);
          FillWindowPixelBuffer(sWindowIds[CLOCK_WIN_ID], PIXEL_FILL(1));  
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          CopyWindowToVram(sWindowIds[CLOCK_WIN_ID], COPYWIN_GFX);  
          CopyWindowToVram(sWindowIds[TEXT_WIN_ID], COPYWIN_MAP);
          SaveFailedScreenTextPrint(gText_SaveFailedCheckingBackup, 1, 0);
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 16, 0, (RGB(0, 0, 0)));
          EnableInterrupts(1);
          SetVBlankCallback(VBlankCB);
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_OBJ_ON | DISPCNT_OBJ_1D_MAP);
          ShowBg(0);
          ShowBg(2);
          ShowBg(3);
          gMain.state++;
          break;
      case 1:
          if (!UpdatePaletteFade())
          {
              SetMainCallback2(CB2_WipeSave);
              SetVBlankCallback(VBlankCB_UpdateClockGraphics);
          }
          break;
      }
}

/** static void CB2_WipeSave(void) */
export function CB2_WipeSave(): any {
  let wipeTries: any = 0;

      sClockInfo[CLOCK_RUNNING] = TRUE;

      while (gDamagedSaveSectors != 0 && wipeTries < 3)
      {
          if (WipeSectors(gDamagedSaveSectors))
          {
              FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
              SaveFailedScreenTextPrint(gText_BackupMemoryDamaged, 1, 0);
              SetMainCallback2(CB2_GameplayCannotBeContinued);
              return;
          }

          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          SaveFailedScreenTextPrint(gText_CheckCompleted, 1, 0);
          HandleSavingData(sSaveFailedType);

          if (gDamagedSaveSectors != 0)
          {
              FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
              SaveFailedScreenTextPrint(gText_SaveFailedCheckingBackup, 1, 0);
          }

          wipeTries++;
      }

      if (wipeTries == 3)
      {
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          SaveFailedScreenTextPrint(gText_BackupMemoryDamaged, 1, 0);
      }
      else
      {
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));

          if (gGameContinueCallback == NULL)
              SaveFailedScreenTextPrint(gText_SaveCompleteGameCannotContinue, 1, 0);
          else
              SaveFailedScreenTextPrint(gText_SaveCompletePressA, 1, 0);
      }

      SetMainCallback2(CB2_FadeAndReturnToTitleScreen);
}

/** static void CB2_GameplayCannotBeContinued(void) */
export function CB2_GameplayCannotBeContinued(): any {
  sClockInfo[CLOCK_RUNNING] = FALSE;

      if (JOY_NEW(A_BUTTON))
      {
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          SaveFailedScreenTextPrint(gText_GamePlayCannotBeContinued, 1, 0);
          SetVBlankCallback(VBlankCB);
          SetMainCallback2(CB2_FadeAndReturnToTitleScreen);
      }
}

/** static void CB2_FadeAndReturnToTitleScreen(void) */
export function CB2_FadeAndReturnToTitleScreen(): any {
  sClockInfo[CLOCK_RUNNING] = FALSE;

      if (JOY_NEW(A_BUTTON))
      {
          BeginNormalPaletteFade((((0x0000FFFF) | (0xFFFF0000))), 0, 0, 16, (RGB(0, 0, 0)));
          SetVBlankCallback(VBlankCB);
          SetMainCallback2(CB2_ReturnToTitleScreen);
      }
}

/** static void CB2_ReturnToTitleScreen(void) */
export function CB2_ReturnToTitleScreen(): any {
  if (!UpdatePaletteFade())
      {
          if (gGameContinueCallback == NULL)  
          {
              DoSoftReset();
          }
          else
          {
              SetMainCallback2(gGameContinueCallback);
              gGameContinueCallback = NULL;
          }
      }
}

/** static void VBlankCB_UpdateClockGraphics(void) */
export function VBlankCB_UpdateClockGraphics(): any {
  let n: any = (gMain.vblankCounter2 >> 3) & 7;

      gMain.oamBuffer[0] = sClockOamData;
      gMain.oamBuffer[0].x = 112;
      gMain.oamBuffer[0].y = ((((12) - 4)) + 1) * 8;

      if (sClockInfo[CLOCK_RUNNING])
      {
          gMain.oamBuffer[0].tileNum = sClockFrames[n][0];
          gMain.oamBuffer[0].matrixNum = (sClockFrames[n][2] << 4) | (sClockFrames[n][1] << 3);
      }
      else
      {
          gMain.oamBuffer[0].tileNum = 1;
      }

      CpuFastCopy(gMain.oamBuffer, OAM, 4);

      if (sClockInfo[DEBUG_TIMER])
          sClockInfo[DEBUG_TIMER]--;
}

/** static bool8 VerifySectorWipe(u16 sector) */
export function VerifySectorWipe(sector: any): any {
  let ptr: any =gSaveDataBuffer;
      let i: any = null;

      ReadFlash(sector, 0, ptr, (((3968) + (128))));

       
      for (i = 0; i < (((3968) + (128))) / 4; i++, ptr++)
          if (ptr)
              return TRUE;  

      return FALSE;
}

/** static bool8 WipeSector(u16 sector) */
export function WipeSector(sector: any): any {
  let i, j;
      let failed: any = TRUE;

       
      for (i = 0; failed && i < 130; i++)
      {
          for (j = 0; j < (((3968) + (128))); j++)
              ProgramFlashByte(sector, j, 0);

          failed = VerifySectorWipe(sector);
      }

      return failed;
}

/** static bool8 WipeSectors(u32 sectorBits) */
export function WipeSectors(sectorBits: any): any {
  let i: any = null;

      for (i = 0; i < (32); i++)
          if ((sectorBits & (1 << i)) && !WipeSector(i))
              sectorBits &= ~(1 << i);

      if (sectorBits == 0)
          return FALSE;
      else
          return TRUE;
}

// ─── callsTo manifest (= 45 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","AddTextPrinterParameterized4","AddWindowWithoutTileMap","BG_PLTT_ID","BG_SCREEN_ADDR","BeginNormalPaletteFade","CopyWindowToVram","CpuFastCopy","CpuFill32","DeactivateAllTextPrinters","DmaFill16","DmaFill32","DoSoftReset","DrawStdFrameWithCustomTileAndPalette","EnableInterrupts","FillWindowPixelBuffer","HandleSavingData","InitBgsFromTemplates","InitWindows","JOY_NEW","LZ77UnCompVram","LoadBgTiles","LoadOam","LoadPalette","OBJ_PLTT_ID","PIXEL_FILL","ProcessSpriteCopyRequests","ProgramFlashByte","ReadFlash","ResetBgsAndClearDma3BusyFlags","ResetPaletteFade","ResetSpriteData","ResetTasks","SaveFailedScreenTextPrint","SetBgTilemapBuffer","SetGpuReg","SetMainCallback2","SetVBlankCallback","SetWindowAttribute","ShowBg","TransferPlttBuffer","UpdatePaletteFade","VerifySectorWipe","WipeSector","WipeSectors"];
