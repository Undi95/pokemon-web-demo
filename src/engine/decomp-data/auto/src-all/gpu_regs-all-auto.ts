// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/gpu_regs.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 12 functions extracted from src/gpu_regs.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** void InitGpuRegManager(void) */
export function InitGpuRegManager(): any {
  let i: any = null;

      for (i = 0; i < GPU_REG_BUF_SIZE; i++)
      {
          sGpuRegBuffer[i] = 0;
          sGpuRegWaitingList[i] = EMPTY_SLOT;
      }

      sGpuRegBufferLocked = FALSE;
      sShouldSyncRegIE = FALSE;
      sRegIE = 0;
}

/** static void CopyBufferedValueToGpuReg(u8 regOffset) */
export function CopyBufferedValueToGpuReg(regOffset: any): any {
  if (regOffset == REG_OFFSET_DISPSTAT)
      {
          REG_DISPSTAT &= ~(DISPSTAT_HBLANK_INTR | DISPSTAT_VBLANK_INTR);
          REG_DISPSTAT |= ((sGpuRegBuffer[(REG_OFFSET_DISPSTAT)]));
      }
      else
      {
          /* transpiler bug LHS : ((REG_BASE + (regOffset))) = ((sGpuRegBuffer[(regOffset)])); */
      }
}

/** void CopyBufferedValuesToGpuRegs(void) */
export function CopyBufferedValuesToGpuRegs(): any {
  if (!sGpuRegBufferLocked)
      {
          let i: any = null;

          for (i = 0; i < GPU_REG_BUF_SIZE; i++)
          {
              let regOffset: any = sGpuRegWaitingList[i];
              if (regOffset == EMPTY_SLOT)
                  return;
              CopyBufferedValueToGpuReg(regOffset);
              sGpuRegWaitingList[i] = EMPTY_SLOT;
          }
      }
}

/** void SetGpuReg(u8 regOffset, u16 value) */
export function SetGpuReg(regOffset: any, value: any): any {
  if (regOffset < GPU_REG_BUF_SIZE)
      {
          let vcount: any = null;

          /* transpiler bug LHS : ((sGpuRegBuffer[(regOffset)])) = value; */
          vcount = REG_VCOUNT & 0xFF;

          if ((vcount >= 161 && vcount <= 225) || (REG_DISPCNT & DISPCNT_FORCED_BLANK))
          {
              CopyBufferedValueToGpuReg(regOffset);
          }
          else
          {
              let i: any = null;

              sGpuRegBufferLocked = TRUE;

              for (i = 0; i < GPU_REG_BUF_SIZE && sGpuRegWaitingList[i] != EMPTY_SLOT; i++)
              {
                  if (sGpuRegWaitingList[i] == regOffset)
                  {
                      sGpuRegBufferLocked = FALSE;
                      return;
                  }
              }

              sGpuRegWaitingList[i] = regOffset;
              sGpuRegBufferLocked = FALSE;
          }
      }
}

/** void SetGpuReg_ForcedBlank(u8 regOffset, u16 value) */
export function SetGpuReg_ForcedBlank(regOffset: any, value: any): any {
  if (regOffset < GPU_REG_BUF_SIZE)
      {
          /* transpiler bug LHS : ((sGpuRegBuffer[(regOffset)])) = value; */

          if (REG_DISPCNT & DISPCNT_FORCED_BLANK)
          {
              CopyBufferedValueToGpuReg(regOffset);
          }
          else
          {
              let i: any = null;

              sGpuRegBufferLocked = TRUE;

              for (i = 0; i < GPU_REG_BUF_SIZE && sGpuRegWaitingList[i] != EMPTY_SLOT; i++)
              {
                  if (sGpuRegWaitingList[i] == regOffset)
                  {
                      sGpuRegBufferLocked = FALSE;
                      return;
                  }
              }

              sGpuRegWaitingList[i] = regOffset;
              sGpuRegBufferLocked = FALSE;
          }
      }
}

/** u16 GetGpuReg(u8 regOffset) */
export function GetGpuReg(regOffset: any): any {
  if (regOffset == REG_OFFSET_DISPSTAT)
          return REG_DISPSTAT;

      if (regOffset == REG_OFFSET_VCOUNT)
          return REG_VCOUNT;

      return ((sGpuRegBuffer[(regOffset)]));
}

/** void SetGpuRegBits(u8 regOffset, u16 mask) */
export function SetGpuRegBits(regOffset: any, mask: any): any {
  let regValue: any = ((sGpuRegBuffer[(regOffset)]));
      SetGpuReg(regOffset, regValue | mask);
}

/** void ClearGpuRegBits(u8 regOffset, u16 mask) */
export function ClearGpuRegBits(regOffset: any, mask: any): any {
  let regValue: any = ((sGpuRegBuffer[(regOffset)]));
      SetGpuReg(regOffset, regValue & ~mask);
}

/** static void SyncRegIE(void) */
export function SyncRegIE(): any {
  if (sShouldSyncRegIE)
      {
          let temp: any = REG_IME;
          REG_IME = 0;
          REG_IE = sRegIE;
          REG_IME = temp;
          sShouldSyncRegIE = FALSE;
      }
}

/** void EnableInterrupts(u16 mask) */
export function EnableInterrupts(mask: any): any {
  sRegIE |= mask;
      sShouldSyncRegIE = TRUE;
      SyncRegIE();
      UpdateRegDispstatIntrBits(sRegIE);
}

/** void DisableInterrupts(u16 mask) */
export function DisableInterrupts(mask: any): any {
  sRegIE &= ~mask;
      sShouldSyncRegIE = TRUE;
      SyncRegIE();
      UpdateRegDispstatIntrBits(sRegIE);
}

/** static void UpdateRegDispstatIntrBits(u16 regIE) */
export function UpdateRegDispstatIntrBits(regIE: any): any {
  let oldValue: any = GetGpuReg(REG_OFFSET_DISPSTAT) & (DISPSTAT_HBLANK_INTR | DISPSTAT_VBLANK_INTR);
      let newValue: any = 0;

      if (regIE & INTR_FLAG_VBLANK)
          newValue |= DISPSTAT_VBLANK_INTR;

      if (regIE & INTR_FLAG_HBLANK)
          newValue |= DISPSTAT_HBLANK_INTR;

      if (oldValue != newValue)
          SetGpuReg(REG_OFFSET_DISPSTAT, newValue);
}

// ─── callsTo manifest (= 7 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["CopyBufferedValueToGpuReg","GPU_REG","GPU_REG_BUF","GetGpuReg","SetGpuReg","SyncRegIE","UpdateRegDispstatIntrBits"];
