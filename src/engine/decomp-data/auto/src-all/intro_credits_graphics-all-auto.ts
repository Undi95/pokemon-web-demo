// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/intro_credits_graphics.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 19 functions extracted from src/intro_credits_graphics.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let gIntroCredits_MovingSceneryState: any = null;
let gReservedSpritePaletteCount: any = null;
/** void LoadIntroPart2Graphics(u8 scenery) */
export function LoadIntroPart2Graphics(scenery: any): any {
  LZ77UnCompVram(sGrass_Gfx, (BG_CHAR_ADDR(1)));
      LZ77UnCompVram(sGrass_Tilemap, (BG_SCREEN_ADDR(15)));
      LoadPalette(sGrass_Pal, BG_PLTT_ID(15), 0);
      switch (scenery)
      {
      case 0:
      default:
           
           
          LZ77UnCompVram(sCloudsBg_Gfx, (VRAM));
          LZ77UnCompVram(sCloudsBg_Tilemap, (BG_SCREEN_ADDR(6)));
          LoadPalette(sCloudsBg_Pal, BG_PLTT_ID(0), 0);
          LoadCompressedSpriteSheet(sSpriteSheet_Clouds);
          LoadPalette(sClouds_Pal, OBJ_PLTT_ID(0), 0);
          CreateCloudSprites();
          break;
      case 1:
          LZ77UnCompVram(sTrees_Gfx, (VRAM));
          LZ77UnCompVram(sTrees_Tilemap, (BG_SCREEN_ADDR(6)));
          LoadPalette(sTrees_Pal, BG_PLTT_ID(0), 0);
          LoadCompressedSpriteSheet(sSpriteSheet_TreesSmall);
          LoadPalette(sTreesSmall_Pal, OBJ_PLTT_ID(0), 0);
          CreateTreeSprites();
          break;
      }
      gIntroCredits_MovingSceneryState = INTROCRED_SCENERY_NORMAL;
      gReservedSpritePaletteCount = 8;
}

/** void SetIntroPart2BgCnt(u8 scenery) */
export function SetIntroPart2BgCnt(scenery: any): any {
  switch (scenery)
      {
      default:
      case 0:
          SetGpuReg(REG_OFFSET_BG3CNT, BGCNT_PRIORITY(3)
                                     | BGCNT_CHARBASE(0)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(6)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG2CNT, BGCNT_PRIORITY(2)
                                     | BGCNT_CHARBASE(0)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(7)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG1CNT, BGCNT_PRIORITY(1)
                                     | BGCNT_CHARBASE(1)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(15)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                      | DISPCNT_OBJ_1D_MAP
                                      | DISPCNT_BG1_ON
                                      | DISPCNT_BG2_ON
                                      | DISPCNT_BG3_ON
                                      | DISPCNT_OBJ_ON);
          break;
      case 1:
          SetGpuReg(REG_OFFSET_BG3CNT, BGCNT_PRIORITY(3)
                                     | BGCNT_CHARBASE(0)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(6)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG2CNT, BGCNT_PRIORITY(2)
                                     | BGCNT_CHARBASE(0)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(7)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG1CNT, BGCNT_PRIORITY(1)
                                     | BGCNT_CHARBASE(1)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(15)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                      | DISPCNT_OBJ_1D_MAP
                                      | DISPCNT_BG1_ON
                                      | DISPCNT_BG2_ON
                                      | DISPCNT_BG3_ON
                                      | DISPCNT_OBJ_ON);
          break;
      case 2:
          SetGpuReg(REG_OFFSET_BG3CNT, BGCNT_PRIORITY(3)
                                     | BGCNT_CHARBASE(0)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(6)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG2CNT, BGCNT_PRIORITY(2)
                                     | BGCNT_CHARBASE(0)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(7)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_BG1CNT, BGCNT_PRIORITY(1)
                                     | BGCNT_CHARBASE(1)
                                     | BGCNT_16COLOR
                                     | BGCNT_SCREENBASE(15)
                                     | BGCNT_TXT256x256);
          SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                      | DISPCNT_OBJ_1D_MAP
                                      | DISPCNT_BG1_ON
                                      | DISPCNT_BG2_ON
                                      | DISPCNT_BG3_ON
                                      | DISPCNT_OBJ_ON);
          break;
      }
}

/** void LoadCreditsSceneGraphics(u8 scene) */
export function LoadCreditsSceneGraphics(scene: any): any {
  LZ77UnCompVram(sGrass_Gfx, (BG_CHAR_ADDR(1)));
      LZ77UnCompVram(sGrass_Tilemap, (BG_SCREEN_ADDR(15)));
      switch (scene)
      {
      case SCENE_OCEAN_MORNING:
      default:
          LoadPalette(sGrass_Pal, BG_PLTT_ID(15), 0);
          LZ77UnCompVram(sCloudsBg_Gfx, (VRAM));
          LZ77UnCompVram(sCloudsBg_Tilemap, (BG_SCREEN_ADDR(6)));
          LoadPalette(sCloudsBg_Pal, BG_PLTT_ID(0), 0);
          LoadCompressedSpriteSheet(sSpriteSheet_Clouds);
          LZ77UnCompVram(sClouds_Gfx, (OBJ_VRAM0));
          LoadPalette(sClouds_Pal, OBJ_PLTT_ID(0), 0);
          CreateCloudSprites();
          break;
      case SCENE_OCEAN_SUNSET:
          LoadPalette(sGrassSunset_Pal, BG_PLTT_ID(15), 0);
          LZ77UnCompVram(sCloudsBg_Gfx, (VRAM));
          LZ77UnCompVram(sCloudsBg_Tilemap, (BG_SCREEN_ADDR(6)));
          LoadPalette(sCloudsBgSunset_Pal, BG_PLTT_ID(0), 0);
          LoadCompressedSpriteSheet(sSpriteSheet_Clouds);
          LZ77UnCompVram(sClouds_Gfx, (OBJ_VRAM0));
          LoadPalette(sCloudsSunset_Pal, OBJ_PLTT_ID(0), 0);
          CreateCloudSprites();
          break;
      case SCENE_FOREST_RIVAL_ARRIVE:
      case SCENE_FOREST_CATCH_RIVAL:
          LoadPalette(sGrassSunset_Pal, BG_PLTT_ID(15), 0);
          LZ77UnCompVram(sTrees_Gfx, (VRAM));
          LZ77UnCompVram(sTrees_Tilemap, (BG_SCREEN_ADDR(6)));
          LoadPalette(sTreesSunset_Pal, BG_PLTT_ID(0), 0);
          LoadCompressedSpriteSheet(sSpriteSheet_TreesSmall);
          LoadPalette(sTreesSunset_Pal, OBJ_PLTT_ID(0), 0);
          CreateTreeSprites();
          break;
      case SCENE_CITY_NIGHT:
          LoadPalette(sGrassNight_Pal, BG_PLTT_ID(15), 0);
          LZ77UnCompVram(sHouses_Gfx, (VRAM));
          LZ77UnCompVram(sHouses_Tilemap, (BG_SCREEN_ADDR(6)));
          LoadPalette(sHouses_Pal, BG_PLTT_ID(0), 0);
          LoadCompressedSpriteSheet(sSpriteSheet_HouseSilhouette);
          LoadPalette(sHouseSilhouette_Pal, OBJ_PLTT_ID(0), 0);
          CreateHouseSprites();
          break;
      }
      gReservedSpritePaletteCount = 8;
      gIntroCredits_MovingSceneryState = INTROCRED_SCENERY_NORMAL;
}

/** void SetCreditsSceneBgCnt(u8 scene) */
export function SetCreditsSceneBgCnt(scene: any): any {
  SetGpuReg(REG_OFFSET_BG3CNT, BGCNT_PRIORITY(3)
                                 | BGCNT_CHARBASE(0)
                                 | BGCNT_16COLOR
                                 | BGCNT_SCREENBASE(6)
                                 | BGCNT_TXT256x256);
      SetGpuReg(REG_OFFSET_BG2CNT, BGCNT_PRIORITY(2)
                                 | BGCNT_CHARBASE(0)
                                 | BGCNT_16COLOR
                                 | BGCNT_SCREENBASE(7)
                                 | BGCNT_TXT256x256);
      SetGpuReg(REG_OFFSET_BG1CNT, BGCNT_PRIORITY(1)
                                 | BGCNT_CHARBASE(1)
                                 | BGCNT_16COLOR
                                 | BGCNT_SCREENBASE(15)
                                 | BGCNT_TXT256x256);
      SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                  | DISPCNT_OBJ_1D_MAP
                                  | DISPCNT_BG_ALL_ON
                                  | DISPCNT_OBJ_ON);
}

/** u8 CreateBicycleBgAnimationTask(u8 mode, u16 bg1Speed, u16 bg2Speed, u16 bg3Speed) */
export function CreateBicycleBgAnimationTask(mode: any, bg1Speed: any, bg2Speed: any, bg3Speed: any): any {
  let taskId: any = CreateTask(Task_BicycleBgAnimation, 0);

      gTasks[taskId].tMode = mode;
      gTasks[taskId].tBg1Speed = bg1Speed;
      gTasks[taskId].tBg1PosHi = 0;
      gTasks[taskId].tBg1PosLo = 0;
      gTasks[taskId].tBg2Speed = bg2Speed;
      gTasks[taskId].tBg2PosHi = 0;
      gTasks[taskId].tBg2PosLo = 0;
      gTasks[taskId].tBg3Speed = bg3Speed;
      gTasks[taskId].tBg3PosHi = 8;
      gTasks[taskId].tBg3PosLo = 0;
      Task_BicycleBgAnimation(taskId);
      return taskId;
}

/** static void Task_BicycleBgAnimation(u8 taskId) */
export function Task_BicycleBgAnimation(taskId: any): any {
  let bg1Speed: any = null;
      let bg2Speed: any = null;
      let bg3Speed: any = null;
      let offset: any = null;

       
      bg1Speed = gTasks[taskId].tBg1Speed;
      if (bg1Speed != 0)
      {
          offset = (gTasks[taskId].tBg1PosHi << 16) + gTasks[taskId].tBg1PosLo;
          offset -= bg1Speed << 4;
          gTasks[taskId].tBg1PosHi = offset >> 16;
          gTasks[taskId].tBg1PosLo = offset;
          SetGpuReg(REG_OFFSET_BG1HOFS, gTasks[taskId].tBg1PosHi);
          SetGpuReg(REG_OFFSET_BG1VOFS, gIntroCredits_MovingSceneryVBase + gIntroCredits_MovingSceneryVOffset);
      }

       
      bg2Speed = gTasks[taskId].tBg2Speed;
      if (bg2Speed != 0)
      {
          offset = (gTasks[taskId].tBg2PosHi << 16) + gTasks[taskId].tBg2PosLo;
          offset -= bg2Speed << 4;
          gTasks[taskId].tBg2PosHi = offset >> 16;
          gTasks[taskId].tBg2PosLo = offset;
          SetGpuReg(REG_OFFSET_BG2HOFS, gTasks[taskId].tBg2PosHi);
          if (gTasks[taskId].tMode != 0)
              SetGpuReg(REG_OFFSET_BG2VOFS, gIntroCredits_MovingSceneryVBase + gIntroCredits_MovingSceneryVOffset);
          else
              SetGpuReg(REG_OFFSET_BG2VOFS, gIntroCredits_MovingSceneryVBase);
      }

       
      bg3Speed = gTasks[taskId].tBg3Speed;
      if (bg3Speed != 0)
      {
          offset = (gTasks[taskId].tBg3PosHi << 16) + gTasks[taskId].tBg3PosLo;
          offset -= bg3Speed << 4;
          gTasks[taskId].tBg3PosHi = offset >> 16;
          gTasks[taskId].tBg3PosLo = offset;
          SetGpuReg(REG_OFFSET_BG3HOFS, gTasks[taskId].tBg3PosHi);
          SetGpuReg(REG_OFFSET_BG3VOFS, gIntroCredits_MovingSceneryVBase);
      }
}

/** void CycleSceneryPalette(u8 mode) */
export function CycleSceneryPalette(mode: any): any {
  let x: any = null;
      let y: any = null;
      switch (mode)
      {
          case 0:
          default:
              if (gMain.vblankCounter1 & 3 || gPaletteFade.active)
                  break;
              if (gMain.vblankCounter1 & 4)
              {
                  x = gPlttBufferUnfaded[BG_PLTT_ID(0) + 9];
                  y = gPlttBufferUnfaded[BG_PLTT_ID(0) + 10];
              }
              else
              {
                  x = gPlttBufferUnfaded[BG_PLTT_ID(0) + 10];
                  y = gPlttBufferUnfaded[BG_PLTT_ID(0) + 9];
              }
              LoadPalette(x, BG_PLTT_ID(0) + 9, 0);
              LoadPalette(y, BG_PLTT_ID(0) + 10, 0);
              break;
          case 2:
              if (gMain.vblankCounter1 & 3 || gPaletteFade.active)
                  break;
              if (gMain.vblankCounter1 & 4)
              {
                  x = RGB(7, 9, 15);
                  y = RGB(21, 20, 0);
              }
              else
              {
                  x = RGB(28, 24, 0);
                  y = RGB(7, 9, 15);
              }
              LoadPalette(x, BG_PLTT_ID(0) + 12, 0);
              LoadPalette(y, BG_PLTT_ID(0) + 13, 0);
              break;
          case 1:
              break;
      }
}

/** static void SpriteCB_MovingScenery(struct Sprite *sprite) */
export function SpriteCB_MovingScenery(sprite: any): any {
  let x: any = null;
      let state: any = gIntroCredits_MovingSceneryState;

      if (state != INTROCRED_SCENERY_FROZEN)
      {
          switch (state)
          {
          default:  
              DestroySprite(sprite);
              break;
          case INTROCRED_SCENERY_NORMAL:
              x = ((sprite.x << 16) | sprite.tXPos) + sprite.tXOffset;
              sprite.x = x >> 16;
              sprite.tXPos = x;
              if (sprite.x > 255)
                  sprite.x = -32;
              if (sprite.tHasVerticalMove)
                  sprite.y2 = -(gIntroCredits_MovingSceneryVBase + gIntroCredits_MovingSceneryVOffset);
              else
                  sprite.y2 = -gIntroCredits_MovingSceneryVBase;
              break;
          }
      }
}

/** static void CreateMovingScenerySprites(bool8 hasVerticalMove, const struct IntroCreditsSpriteMetadata *metadata, const union AnimCmd *const *anims, u8 numSprites) */
export function CreateMovingScenerySprites(hasVerticalMove: any, metadata: any, anims: any, numSprites: any): any {
  let i: any = null;

      for(i = 0; i < numSprites; i++)
      {
          let sprite: any = CreateSprite(sSpriteTemplate_MovingScenery, metadata[i].x, metadata[i].y, metadata[i].subpriority);
          CalcCenterToCornerVec(gSprites[sprite], metadata[i].shape, metadata[i].size, ST_OAM_AFFINE_OFF);
          gSprites[sprite].oam.priority = 3;
          gSprites[sprite].oam.shape = metadata[i].shape;
          gSprites[sprite].oam.size = metadata[i].size;
          gSprites[sprite].oam.paletteNum = 0;
          gSprites[sprite].anims = anims;
          StartSpriteAnim(gSprites[sprite], metadata[i].animNum);
          gSprites[sprite].tHasVerticalMove = hasVerticalMove;
          gSprites[sprite].tXOffset = metadata[i].xOff;
          gSprites[sprite].tXPos = 0;
      }
}

/** static void CreateCloudSprites(void) */
export function CreateCloudSprites(): any {
  CreateMovingScenerySprites(FALSE, sSpriteMetadata_Clouds, sAnims_Clouds, 9);
}

/** static void CreateTreeSprites(void) */
export function CreateTreeSprites(): any {
  CreateMovingScenerySprites(TRUE, sSpriteMetadata_Trees, sAnims_Trees, 12);
}

/** static void CreateHouseSprites(void) */
export function CreateHouseSprites(): any {
  CreateMovingScenerySprites(TRUE, sSpriteMetadata_HouseSilhouette, sAnims_HouseSilhouette, 6);
}

/** static void SpriteCB_Bicycle(struct Sprite *sprite) */
export function SpriteCB_Bicycle(sprite: any): any {
  sprite.invisible = gSprites[sprite.sPlayerSpriteId].invisible;
      sprite.x = gSprites[sprite.sPlayerSpriteId].x;
      sprite.y = gSprites[sprite.sPlayerSpriteId].y + 8;
      sprite.x2 = gSprites[sprite.sPlayerSpriteId].x2;
      sprite.y2 = gSprites[sprite.sPlayerSpriteId].y2;
}

/** u8 CreateIntroBrendanSprite(s16 x, s16 y) */
export function CreateIntroBrendanSprite(x: any, y: any): any {
  let playerSpriteId: any = CreateSprite(sSpriteTemplate_Brendan, x, y, 2);
      let bicycleSpriteId: any = CreateSprite(sSpriteTemplate_BrendanBicycle, x, y + 8, 3);
      gSprites[bicycleSpriteId].sPlayerSpriteId = playerSpriteId;
      return playerSpriteId;
}

/** u8 CreateIntroMaySprite(s16 x, s16 y) */
export function CreateIntroMaySprite(x: any, y: any): any {
  let playerSpriteId: any = CreateSprite(sSpriteTemplate_May, x, y, 2);
      let bicycleSpriteId: any = CreateSprite(sSpriteTemplate_MayBicycle, x, y + 8, 3);
      gSprites[bicycleSpriteId].sPlayerSpriteId = playerSpriteId;
      return playerSpriteId;
}

/** static void SpriteCB_FlygonRightHalf(struct Sprite *sprite) */
export function SpriteCB_FlygonRightHalf(sprite: any): any {
  sprite.invisible = gSprites[sprite.sLeftSpriteId].invisible;
      sprite.y = gSprites[sprite.sLeftSpriteId].y;
      sprite.x2 = gSprites[sprite.sLeftSpriteId].x2;
      sprite.y2 = gSprites[sprite.sLeftSpriteId].y2;
}

/** u8 CreateIntroFlygonSprite(s16 x, s16 y) */
export function CreateIntroFlygonSprite(x: any, y: any): any {
  let leftSpriteId: any = CreateSprite(sSpriteTemplate_FlygonLatias, x - 32, y, 5);
      let rightSpriteId: any = CreateSprite(sSpriteTemplate_FlygonLatias, x + 32, y, 6);
      gSprites[rightSpriteId].sLeftSpriteId = leftSpriteId;
      StartSpriteAnim(gSprites[rightSpriteId], 1);
      gSprites[rightSpriteId].callback =SpriteCB_FlygonRightHalf;
      return leftSpriteId;
}

// ─── callsTo manifest (= 21 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["BGCNT_CHARBASE","BGCNT_PRIORITY","BGCNT_SCREENBASE","BG_CHAR_ADDR","BG_PLTT_ID","BG_SCREEN_ADDR","CalcCenterToCornerVec","CreateCloudSprites","CreateHouseSprites","CreateMovingScenerySprites","CreateSprite","CreateTask","CreateTreeSprites","DestroySprite","LZ77UnCompVram","LoadCompressedSpriteSheet","LoadPalette","OBJ_PLTT_ID","SetGpuReg","StartSpriteAnim","Task_BicycleBgAnimation"];

// ─── Failed transpiles (2) ───────────────────────────────
// SpriteCB_Player : empty body
// SpriteCB_FlygonLeftHalf : empty body