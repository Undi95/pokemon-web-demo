// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/rotating_tile_puzzle.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 6 functions extracted from src/rotating_tile_puzzle.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let sRotatingTilePuzzle: any = null;
/** void InitRotatingTilePuzzle(bool8 isTrickHouse) */
export function InitRotatingTilePuzzle(isTrickHouse: any): any {
  if (sRotatingTilePuzzle == NULL)
          sRotatingTilePuzzle = AllocZeroed(0);

      sRotatingTilePuzzle.isTrickHouse = isTrickHouse;
}

/** void FreeRotatingTilePuzzle(void) */
export function FreeRotatingTilePuzzle(): any {
  let id: any = null;

      TRY_FREE_AND_SET_NULL(sRotatingTilePuzzle);

      id = GetObjectEventIdByLocalIdAndMap((255), 0, 0);
      ObjectEventClearHeldMovementIfFinished(gObjectEvents[id]);
      ScriptMovement_UnfreezeObjectEvents();
}

/** u16 MoveRotatingTileObjects(u8 puzzleNumber) */
export function MoveRotatingTileObjects(puzzleNumber: any): any {
  let i: any = null;
      let objectEvents: any = gSaveBlock1Ptr.objectEventTemplates;
      let localId: any = (0);

      for (i = 0; i < (64); i++)
      {
          let puzzleTileStart: any = null;
          let puzzleTileNum: any = null;
          let x: any = objectEvents[i].x + (7);
          let y: any = objectEvents[i].y + (7);
          let metatile: any = MapGridGetMetatileIdAt(x, y);

          if (!sRotatingTilePuzzle.isTrickHouse)
              puzzleTileStart = (0x250);
          else
              puzzleTileStart = (0x298);

           
           
          if (metatile < (0x250))
              continue;

           
          if (((metatile - puzzleTileStart) / (8)) >= 5)
              continue;

           
          if (((metatile - puzzleTileStart) / (8)) != puzzleNumber)
              continue;

          puzzleTileNum = ((metatile - puzzleTileStart) % (8));

           
          if (puzzleTileNum < 4)
          {
              let x: any = 0;
              let y: any = 0;
              let movementScript: any = null;

              switch (puzzleTileNum)
              {
              case 0:  
                  movementScript = sMovement_ShiftRight;
                  x = 1;
                  break;
              case 1:  
                  movementScript = sMovement_ShiftDown;
                  y = 1;
                  break;
              case 2:  
                  movementScript = sMovement_ShiftLeft;
                  x = -1;
                  break;
              case 3:  
                  movementScript = sMovement_ShiftUp;
                  y = -1;
                  break;
              default:
                  continue;
              }

              objectEvents[i].x += x;
              objectEvents[i].y += y;
              if (GetObjectEventIdByLocalIdAndMap(objectEvents[i].localId, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup) != (16))
              {
                  SaveRotatingTileObject(i, puzzleTileNum);
                  localId = objectEvents[i].localId;
                  ScriptMovement_StartObjectMovementScript(localId, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, movementScript);
              }
               
              else
              {
                  TurnUnsavedRotatingTileObject(i, puzzleTileNum);
              }
          }
      }

      return localId;
}

/** void TurnRotatingTileObjects(void) */
export function TurnRotatingTileObjects(): any {
  let i: any = null;
      let puzzleTileStart: any = null;
      let objectEvents: any = null;

      if (sRotatingTilePuzzle == NULL)
          return;

      if (!sRotatingTilePuzzle.isTrickHouse)
          puzzleTileStart = (0x250);
      else
          puzzleTileStart = (0x298);

      objectEvents = gSaveBlock1Ptr.objectEventTemplates;
      for (i = 0; i < sRotatingTilePuzzle.numObjects; i++)
      {
          let rotation: any = null;
          let tileDifference: any = null;
          let objectEventId: any = null;
          let x: any = objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].x + (7);
          let y: any = objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].y + (7);
          let metatile: any = MapGridGetMetatileIdAt(x, y);

           
           
           
           
           
           
          tileDifference = ((metatile - puzzleTileStart) % (8));
          tileDifference -= (sRotatingTilePuzzle.objects[i].prevPuzzleTileNum);

           
          if (tileDifference < 0 || tileDifference == 3)
          {
               
              if (tileDifference == -3)
                  rotation = (1);
              else
                  rotation = (0);
          }
          else
          {
              if (tileDifference > 0)
                  rotation = (1);
              else
                  rotation = (2);
          }

          objectEventId = GetObjectEventIdByLocalIdAndMap(objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].localId, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup);
          if (objectEventId != (16))
          {
              let movementScript: any = null;
              let direction: any = gObjectEvents[objectEventId].facingDirection;
              if (rotation == (0))
              {
                  switch (direction)
                  {
                  case (4):
                      movementScript = sMovement_FaceUp;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0x7);
                      break;
                  case (1):
                      movementScript = sMovement_FaceRight;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0xA);
                      break;
                  case (3):
                      movementScript = sMovement_FaceDown;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0x8);
                      break;
                  case (2):
                      movementScript = sMovement_FaceLeft;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0x9);
                      break;
                  default:
                      continue;
                  }
                  ScriptMovement_StartObjectMovementScript(objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].localId,
                                                           gSaveBlock1Ptr.location.mapNum,
                                                           gSaveBlock1Ptr.location.mapGroup,
                                                           movementScript);
              }
               
              else if (rotation == (1))
              {
                  switch (direction)
                  {
                  case (4):
                      movementScript = sMovement_FaceDown;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0x8);
                      break;
                  case (1):
                      movementScript = sMovement_FaceLeft;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0x9);
                      break;
                  case (3):
                      movementScript = sMovement_FaceUp;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0x7);
                      break;
                  case (2):
                      movementScript = sMovement_FaceRight;
                      objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].movementType = (0xA);
                      break;
                  default:
                      continue;
                  }
                  ScriptMovement_StartObjectMovementScript(objectEvents[sRotatingTilePuzzle.objects[i].eventTemplateId].localId,
                                                           gSaveBlock1Ptr.location.mapNum,
                                                           gSaveBlock1Ptr.location.mapGroup,
                                                           movementScript);
              }
          }
      }
}

/** static void SaveRotatingTileObject(u8 eventTemplateId, u8 puzzleTileNum) */
export function SaveRotatingTileObject(eventTemplateId: any, puzzleTileNum: any): any {
  sRotatingTilePuzzle.objects[sRotatingTilePuzzle.numObjects].eventTemplateId = eventTemplateId;
      sRotatingTilePuzzle.objects[sRotatingTilePuzzle.numObjects].prevPuzzleTileNum = puzzleTileNum;
      sRotatingTilePuzzle.numObjects++;
}

/** static void TurnUnsavedRotatingTileObject(u8 eventTemplateId, u8 puzzleTileNum) */
export function TurnUnsavedRotatingTileObject(eventTemplateId: any, puzzleTileNum: any): any {
  let tileDifference: any = null;
      let rotation: any = null;
      let puzzleTileStart: any = null;
      let movementType: any = null;
      let objectEvents: any = gSaveBlock1Ptr.objectEventTemplates;
      let x: any = objectEvents[eventTemplateId].x + (7);
      let y: any = objectEvents[eventTemplateId].y + (7);
      let metatile: any = MapGridGetMetatileIdAt(x, y);

      if (!sRotatingTilePuzzle.isTrickHouse)
          puzzleTileStart = (0x250);
      else
          puzzleTileStart = (0x298);

      tileDifference = ((metatile - puzzleTileStart) % (8));
      tileDifference -= puzzleTileNum;

      if (tileDifference < 0 || tileDifference == 3)
          rotation = (0);
      else if (tileDifference > 0 || tileDifference == -3)
          rotation = (1);
      else
          rotation = (2);

      movementType = objectEvents[eventTemplateId].movementType;
      if (rotation == (0))
      {
          switch (movementType)
          {
          case (0xA):
              objectEvents[eventTemplateId].movementType = (0x7);
              break;
          case (0x8):
              objectEvents[eventTemplateId].movementType = (0xA);
              break;
          case (0x9):
              objectEvents[eventTemplateId].movementType = (0x8);
              break;
          case (0x7):
              objectEvents[eventTemplateId].movementType = (0x9);
              break;
          default:
              break;
          }
      }
      else if (rotation == (1))
      {
          switch (movementType)
          {
          case (0xA):
              objectEvents[eventTemplateId].movementType = (0x8);
              break;
          case (0x8):
              objectEvents[eventTemplateId].movementType = (0x9);
              break;
          case (0x9):
              objectEvents[eventTemplateId].movementType = (0x7);
              break;
          case (0x7):
              objectEvents[eventTemplateId].movementType = (0xA);
              break;
          default:
              break;
          }
      }
}

// ─── callsTo manifest (= 9 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AllocZeroed","GetObjectEventIdByLocalIdAndMap","MapGridGetMetatileIdAt","ObjectEventClearHeldMovementIfFinished","SaveRotatingTileObject","ScriptMovement_StartObjectMovementScript","ScriptMovement_UnfreezeObjectEvents","TRY_FREE_AND_SET_NULL","TurnUnsavedRotatingTileObject"];
