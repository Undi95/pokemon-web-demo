// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/rtc.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 21 functions extracted from src/rtc.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  FALSE, SiiRtcGetDateTime, SiiRtcGetStatus, SiiRtcProbe,
  SiiRtcReset, SiiRtcUnprotect, TRUE,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void RtcDisableInterrupts(void) */
export function RtcDisableInterrupts(): any {
  sSavedIme = REG_IME;
      REG_IME = 0;
}

/** void RtcRestoreInterrupts(void) */
export function RtcRestoreInterrupts(): any {
  REG_IME = sSavedIme;
}

/** u32 ConvertBcdToBinary(u8 bcd) */
export function ConvertBcdToBinary(bcd: any): any {
  if (bcd > 0x9F)
          return 0xFF;

      if ((bcd & 0xF) <= 9)
          return (10 * ((bcd >> 4) & 0xF)) + (bcd & 0xF);
      else
          return 0xFF;
}

/** bool8 IsLeapYear(u32 year) */
export function IsLeapYear(year: any): any {
  if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))
          return TRUE;

      return FALSE;
}

/** u16 ConvertDateToDayCount(u8 year, u8 month, u8 day) */
export function ConvertDateToDayCount(year: any, month: any, day: any): any {
  let i: any = null;
      let dayCount: any = 0;

      for (i = year - 1; i >= 0; i--)
      {
          dayCount += 365;

          if (IsLeapYear(i) == TRUE)
              dayCount++;
      }

      for (i = 0; i < month - 1; i++)
          dayCount += sNumDaysInMonths[i];

      if (month > MONTH_FEB && IsLeapYear(year) == TRUE)
          dayCount++;

      dayCount += day;

      return dayCount;
}

/** u16 RtcGetDayCount(struct SiiRtcInfo *rtc) */
export function RtcGetDayCount(rtc: any): any {
  let year: any = ConvertBcdToBinary(rtc.year);
      let month: any = ConvertBcdToBinary(rtc.month);
      let day: any = ConvertBcdToBinary(rtc.day);
      return ConvertDateToDayCount(year, month, day);
}

/** void RtcInit(void) */
export function RtcInit(): any {
  sErrorStatus = 0;

      RtcDisableInterrupts();
      SiiRtcUnprotect();
      sProbeResult = SiiRtcProbe();
      RtcRestoreInterrupts();

      if ((sProbeResult & 0xF) != 1)
      {
          sErrorStatus = RTC_INIT_ERROR;
          return;
      }

      if (sProbeResult & 0xF0)
          sErrorStatus = RTC_INIT_WARNING;
      else
          sErrorStatus = 0;

      RtcGetRawInfo(sRtc);
      sErrorStatus = RtcCheckInfo(sRtc);
}

/** u16 RtcGetErrorStatus(void) */
export function RtcGetErrorStatus(): any {
  return sErrorStatus;
}

/** void RtcGetInfo(struct SiiRtcInfo *rtc) */
export function RtcGetInfo(rtc: any): any {
  if (sErrorStatus & RTC_ERR_FLAG_MASK)
          rtc = sRtcDummy;
      else
          RtcGetRawInfo(rtc);
}

/** void RtcGetDateTime(struct SiiRtcInfo *rtc) */
export function RtcGetDateTime(rtc: any): any {
  RtcDisableInterrupts();
      SiiRtcGetDateTime(rtc);
      RtcRestoreInterrupts();
}

/** void RtcGetStatus(struct SiiRtcInfo *rtc) */
export function RtcGetStatus(rtc: any): any {
  RtcDisableInterrupts();
      SiiRtcGetStatus(rtc);
      RtcRestoreInterrupts();
}

/** void RtcGetRawInfo(struct SiiRtcInfo *rtc) */
export function RtcGetRawInfo(rtc: any): any {
  RtcGetStatus(rtc);
      RtcGetDateTime(rtc);
}

/** u16 RtcCheckInfo(struct SiiRtcInfo *rtc) */
export function RtcCheckInfo(rtc: any): any {
  let errorFlags: any = 0;
      let year: any = null;
      let month: any = null;
      let value: any = null;

      if (rtc.status & SIIRTCINFO_POWER)
          errorFlags |= RTC_ERR_POWER_FAILURE;

      if (!(rtc.status & SIIRTCINFO_24HOUR))
          errorFlags |= RTC_ERR_12HOUR_CLOCK;

      year = ConvertBcdToBinary(rtc.year);

      if (year == 0xFF)
          errorFlags |= RTC_ERR_INVALID_YEAR;

      month = ConvertBcdToBinary(rtc.month);

      if (month == 0xFF || month == 0 || month > MONTH_COUNT)
          errorFlags |= RTC_ERR_INVALID_MONTH;

      value = ConvertBcdToBinary(rtc.day);

      if (value == 0xFF)
          errorFlags |= RTC_ERR_INVALID_DAY;

      if (month == MONTH_FEB)
      {
          if (value > IsLeapYear(year) + sNumDaysInMonths[month - 1])
              errorFlags |= RTC_ERR_INVALID_DAY;
      }
      else
      {
          if (value > sNumDaysInMonths[month - 1])
              errorFlags |= RTC_ERR_INVALID_DAY;
      }

      value = ConvertBcdToBinary(rtc.hour);

      if (value > HOURS_PER_DAY)
          errorFlags |= RTC_ERR_INVALID_HOUR;

      value = ConvertBcdToBinary(rtc.minute);

      if (value > MINUTES_PER_HOUR)
          errorFlags |= RTC_ERR_INVALID_MINUTE;

      value = ConvertBcdToBinary(rtc.second);

      if (value > SECONDS_PER_MINUTE)
          errorFlags |= RTC_ERR_INVALID_SECOND;

      return errorFlags;
}

/** void RtcReset(void) */
export function RtcReset(): any {
  RtcDisableInterrupts();
      SiiRtcReset();
      RtcRestoreInterrupts();
}

/** void RtcCalcTimeDifference(struct SiiRtcInfo *rtc, struct Time *result, struct Time *t) */
export function RtcCalcTimeDifference(rtc: any, result: any, t: any): any {
  let days: any = RtcGetDayCount(rtc);
      result.seconds = ConvertBcdToBinary(rtc.second) - t.seconds;
      result.minutes = ConvertBcdToBinary(rtc.minute) - t.minutes;
      result.hours = ConvertBcdToBinary(rtc.hour) - t.hours;
      result.days = days - t.days;

      if (result.seconds < 0)
      {
          result.seconds += SECONDS_PER_MINUTE;
          --result.minutes;
      }

      if (result.minutes < 0)
      {
          result.minutes += MINUTES_PER_HOUR;
          --result.hours;
      }

      if (result.hours < 0)
      {
          result.hours += HOURS_PER_DAY;
          --result.days;
      }
}

/** void RtcCalcLocalTime(void) */
export function RtcCalcLocalTime(): any {
  RtcGetInfo(sRtc);
      RtcCalcTimeDifference(sRtc,gLocalTime,gSaveBlock2Ptr.localTimeOffset);
}

/** void RtcInitLocalTimeOffset(s32 hour, s32 minute) */
export function RtcInitLocalTimeOffset(hour: any, minute: any): any {
  RtcCalcLocalTimeOffset(0, hour, minute, 0);
}

/** void RtcCalcLocalTimeOffset(s32 days, s32 hours, s32 minutes, s32 seconds) */
export function RtcCalcLocalTimeOffset(days: any, hours: any, minutes: any, seconds: any): any {
  gLocalTime.days = days;
      gLocalTime.hours = hours;
      gLocalTime.minutes = minutes;
      gLocalTime.seconds = seconds;
      RtcGetInfo(sRtc);
      RtcCalcTimeDifference(sRtc,gSaveBlock2Ptr.localTimeOffset,gLocalTime);
}

/** void CalcTimeDifference(struct Time *result, struct Time *t1, struct Time *t2) */
export function CalcTimeDifference(result: any, t1: any, t2: any): any {
  result.seconds = t2.seconds - t1.seconds;
      result.minutes = t2.minutes - t1.minutes;
      result.hours = t2.hours - t1.hours;
      result.days = t2.days - t1.days;

      if (result.seconds < 0)
      {
          result.seconds += SECONDS_PER_MINUTE;
          --result.minutes;
      }

      if (result.minutes < 0)
      {
          result.minutes += MINUTES_PER_HOUR;
          --result.hours;
      }

      if (result.hours < 0)
      {
          result.hours += HOURS_PER_DAY;
          --result.days;
      }
}

/** u32 RtcGetMinuteCount(void) */
export function RtcGetMinuteCount(): any {
  RtcGetInfo(sRtc);
      return (HOURS_PER_DAY * MINUTES_PER_HOUR) * RtcGetDayCount(sRtc) + MINUTES_PER_HOUR * sRtc.hour + sRtc.minute;
}

/** u32 RtcGetLocalDayCount(void) */
export function RtcGetLocalDayCount(): any {
  return RtcGetDayCount(sRtc);
}

// ─── callsTo manifest (= 18 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ConvertBcdToBinary","ConvertDateToDayCount","IsLeapYear","RtcCalcLocalTimeOffset","RtcCalcTimeDifference","RtcCheckInfo","RtcDisableInterrupts","RtcGetDateTime","RtcGetDayCount","RtcGetInfo","RtcGetRawInfo","RtcGetStatus","RtcRestoreInterrupts","SiiRtcGetDateTime","SiiRtcGetStatus","SiiRtcProbe","SiiRtcReset","SiiRtcUnprotect"];
