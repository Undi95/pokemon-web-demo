// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/script_movement.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 18 functions extracted from src/script_movement.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  ClearMovementScriptFinished, CreateTask, DestroyTask, FindTaskIdByFunc,
  FreezeObjectEvent, FuncIsActiveTask, GetMoveObjectsTaskId, GetMovementScript,
  GetMovementScriptIdFromObjectEventId, IsMovementScriptFinished, LoadObjectEventIdFromMovementScript, LoadObjectEventIdPtrFromMovementScript,
  ObjectEventClearHeldMovementIfFinished, ObjectEventIsHeldMovementActive, ObjectEventSetHeldMovement, ScriptMovement_AddNewMovement,
  ScriptMovement_StartMoveObjects, ScriptMovement_TakeStep, ScriptMovement_TryAddNewMovement, ScriptMovement_UnfreezeActiveObjects,
  SetMovementScript, SetMovementScriptFinished, SetObjectEventIdAtMovementScript, TryGetObjectEventIdByLocalIdAndMap,
  UnfreezeObjectEvent,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** bool8 ScriptMovement_StartObjectMovementScript(u8 localId, u8 mapNum, u8 mapGroup, const u8 *movementScript) */
export function ScriptMovement_StartObjectMovementScript(localId: any, mapNum: any, mapGroup: any, movementScript: any): any {
  let objEventId: any = null;

      if (TryGetObjectEventIdByLocalIdAndMap(localId, mapNum, mapGroup,objEventId))
          return TRUE;
      if (!FuncIsActiveTask(ScriptMovement_MoveObjects))
          ScriptMovement_StartMoveObjects(50);
      return ScriptMovement_TryAddNewMovement(GetMoveObjectsTaskId(), objEventId, movementScript);
}

/** bool8 ScriptMovement_IsObjectMovementFinished(u8 localId, u8 mapNum, u8 mapGroup) */
export function ScriptMovement_IsObjectMovementFinished(localId: any, mapNum: any, mapGroup: any): any {
  let objEventId: any = null;
      let taskId: any = null;
      let moveScrId: any = null;

      if (TryGetObjectEventIdByLocalIdAndMap(localId, mapNum, mapGroup,objEventId))
          return TRUE;
      taskId = GetMoveObjectsTaskId();
      moveScrId = GetMovementScriptIdFromObjectEventId(taskId, objEventId);
      if (moveScrId == OBJECT_EVENTS_COUNT)
          return TRUE;
      return IsMovementScriptFinished(taskId, moveScrId);
}

/** void ScriptMovement_UnfreezeObjectEvents(void) */
export function ScriptMovement_UnfreezeObjectEvents(): any {
  let taskId: any = null;

      taskId = GetMoveObjectsTaskId();
      if (taskId != TASK_NONE)
      {
          ScriptMovement_UnfreezeActiveObjects(taskId);
          DestroyTask(taskId);
      }
}

/** static void ScriptMovement_StartMoveObjects(u8 priority) */
export function ScriptMovement_StartMoveObjects(priority: any): any {
  let taskId: any = null;
      let i: any = null;

      taskId = CreateTask(ScriptMovement_MoveObjects, priority);

      for (i = 1; i < NUM_TASK_DATA; i++)
          gTasks[taskId].data[i] = 0xFFFF;
}

/** static u8 GetMoveObjectsTaskId(void) */
export function GetMoveObjectsTaskId(): any {
  return FindTaskIdByFunc(ScriptMovement_MoveObjects);
}

/** static bool8 ScriptMovement_TryAddNewMovement(u8 taskId, u8 objEventId, const u8 *movementScript) */
export function ScriptMovement_TryAddNewMovement(taskId: any, objEventId: any, movementScript: any): any {
  let moveScrId: any = null;

      moveScrId = GetMovementScriptIdFromObjectEventId(taskId, objEventId);
      if (moveScrId != OBJECT_EVENTS_COUNT)
      {
          if (IsMovementScriptFinished(taskId, moveScrId) == 0)
          {
              return TRUE;
          }
          else
          {
              ScriptMovement_AddNewMovement(taskId, moveScrId, objEventId, movementScript);
              return FALSE;
          }
      }
      moveScrId = GetMovementScriptIdFromObjectEventId(taskId, LOCALID_PLAYER);
      if (moveScrId == OBJECT_EVENTS_COUNT)
      {
          return TRUE;
      }
      else
      {
          ScriptMovement_AddNewMovement(taskId, moveScrId, objEventId, movementScript);
          return FALSE;
      }
}

/** static u8 GetMovementScriptIdFromObjectEventId(u8 taskId, u8 objEventId) */
export function GetMovementScriptIdFromObjectEventId(taskId: any, objEventId: any): any {
  let moveScriptId: any = null;
      let i: any = null;

      moveScriptId =gTasks[taskId].data[1];
      for (i = 0; i < OBJECT_EVENTS_COUNT; i++, moveScriptId++)
      {
          if (moveScriptId == objEventId)
              return i;
      }
      return OBJECT_EVENTS_COUNT;
}

/** static void LoadObjectEventIdPtrFromMovementScript(u8 taskId, u8 moveScrId, u8 **pObjEventId) */
export function LoadObjectEventIdPtrFromMovementScript(taskId: any, moveScrId: any, pObjEventId: any): any {
  let i: any = null;

      pObjEventId =gTasks[taskId].data[1];
      for (i = 0; i < moveScrId; i++, pObjEventId++)
          ;
}

/** static void SetObjectEventIdAtMovementScript(u8 taskId, u8 moveScrId, u8 objEventId) */
export function SetObjectEventIdAtMovementScript(taskId: any, moveScrId: any, objEventId: any): any {
  let ptr: any = null;

      LoadObjectEventIdPtrFromMovementScript(taskId, moveScrId,ptr);
      ptr = objEventId;
}

/** static void LoadObjectEventIdFromMovementScript(u8 taskId, u8 moveScrId, u8 *objEventId) */
export function LoadObjectEventIdFromMovementScript(taskId: any, moveScrId: any, objEventId: any): any {
  let ptr: any = null;

      LoadObjectEventIdPtrFromMovementScript(taskId, moveScrId,ptr);
      objEventId = ptr;
}

/** static void ClearMovementScriptFinished(u8 taskId, u8 moveScrId) */
export function ClearMovementScriptFinished(taskId: any, moveScrId: any): any {
  let mask: any = ~gBitTable[moveScrId];

      gTasks[taskId].data[0] &= mask;
}

/** static void SetMovementScriptFinished(u8 taskId, u8 moveScrId) */
export function SetMovementScriptFinished(taskId: any, moveScrId: any): any {
  gTasks[taskId].data[0] |= gBitTable[moveScrId];
}

/** static bool8 IsMovementScriptFinished(u8 taskId, u8 moveScrId) */
export function IsMovementScriptFinished(taskId: any, moveScrId: any): any {
  let moveScriptFinished: any = gTasks[taskId].data[0] & gBitTable[moveScrId];

      if (moveScriptFinished != 0)
          return TRUE;
      else
          return FALSE;
}

/** static void SetMovementScript(u8 moveScrId, const u8 *movementScript) */
export function SetMovementScript(moveScrId: any, movementScript: any): any {
  sMovementScripts[moveScrId] = movementScript;
}

/** static void ScriptMovement_AddNewMovement(u8 taskId, u8 moveScrId, u8 objEventId, const u8 *movementScript) */
export function ScriptMovement_AddNewMovement(taskId: any, moveScrId: any, objEventId: any, movementScript: any): any {
  ClearMovementScriptFinished(taskId, moveScrId);
      SetMovementScript(moveScrId, movementScript);
      SetObjectEventIdAtMovementScript(taskId, moveScrId, objEventId);
}

/** static void ScriptMovement_UnfreezeActiveObjects(u8 taskId) */
export function ScriptMovement_UnfreezeActiveObjects(taskId: any): any {
  let pObjEventId: any = null;
      let i: any = null;

      pObjEventId =gTasks[taskId].data[1];
      for (i = 0; i < OBJECT_EVENTS_COUNT; i++, pObjEventId++)
      {
          if (pObjEventId != 0xFF)
              UnfreezeObjectEvent(gObjectEvents[pObjEventId]);
      }
}

/** static void ScriptMovement_MoveObjects(u8 taskId) */
export function ScriptMovement_MoveObjects(taskId: any): any {
  let i: any = null;
      let objEventId: any = null;

      for (i = 0; i < OBJECT_EVENTS_COUNT; i++)
      {
          LoadObjectEventIdFromMovementScript(taskId, i,objEventId);
          if (objEventId != 0xFF)
              ScriptMovement_TakeStep(taskId, i, objEventId, GetMovementScript(i));
      }
}

/** static void ScriptMovement_TakeStep(u8 taskId, u8 moveScrId, u8 objEventId, const u8 *movementScript) */
export function ScriptMovement_TakeStep(taskId: any, moveScrId: any, objEventId: any, movementScript: any): any {
  let nextMoveActionId: any = null;

      if (ObjectEventIsHeldMovementActive(gObjectEvents[objEventId])
       && !ObjectEventClearHeldMovementIfFinished(gObjectEvents[objEventId]))
          return;

      nextMoveActionId = movementScript;
      if (nextMoveActionId == MOVEMENT_ACTION_STEP_END)
      {
          SetMovementScriptFinished(taskId, moveScrId);
          FreezeObjectEvent(gObjectEvents[objEventId]);
      }
      else
      {
          if (!ObjectEventSetHeldMovement(gObjectEvents[objEventId], nextMoveActionId))
          {
              movementScript++;
              SetMovementScript(moveScrId, movementScript);
          }
      }
}

// ─── callsTo manifest (= 25 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ClearMovementScriptFinished","CreateTask","DestroyTask","FindTaskIdByFunc","FreezeObjectEvent","FuncIsActiveTask","GetMoveObjectsTaskId","GetMovementScript","GetMovementScriptIdFromObjectEventId","IsMovementScriptFinished","LoadObjectEventIdFromMovementScript","LoadObjectEventIdPtrFromMovementScript","ObjectEventClearHeldMovementIfFinished","ObjectEventIsHeldMovementActive","ObjectEventSetHeldMovement","ScriptMovement_AddNewMovement","ScriptMovement_StartMoveObjects","ScriptMovement_TakeStep","ScriptMovement_TryAddNewMovement","ScriptMovement_UnfreezeActiveObjects","SetMovementScript","SetMovementScriptFinished","SetObjectEventIdAtMovementScript","TryGetObjectEventIdByLocalIdAndMap","UnfreezeObjectEvent"];
