// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/international_string_util.c
// Generated : 2026-05-08
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 15 functions extracted from src/international_string_util.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** int GetStringCenterAlignXOffset(int fontId, const u8 *str, int totalWidth) */
export function GetStringCenterAlignXOffset(...args: any[]): any {
  return GetStringCenterAlignXOffsetWithLetterSpacing(fontId, str, totalWidth, 0);
}

/** int GetStringRightAlignXOffset(int fontId, const u8 *str, int totalWidth) */
export function GetStringRightAlignXOffset(...args: any[]): any {
  return GetStringWidthDifference(fontId, str, totalWidth, 0);
}

/** int GetStringCenterAlignXOffsetWithLetterSpacing(int fontId, const u8 *str, int totalWidth, int letterSpacing) */
export function GetStringCenterAlignXOffsetWithLetterSpacing(...args: any[]): any {
  return GetStringWidthDifference(fontId, str, totalWidth, letterSpacing) / 2;
}

/** int GetStringWidthDifference(int fontId, const u8 *str, int totalWidth, int letterSpacing) */
export function GetStringWidthDifference(...args: any[]): any {
  let stringWidth = GetStringWidth(fontId, str, letterSpacing);
      if (totalWidth > stringWidth)
          return totalWidth - stringWidth;
      else
          return 0;
}

/** int GetMaxWidthInMenuTable(const struct MenuAction *actions, int numActions) */
export function GetMaxWidthInMenuTable(...args: any[]): any {
  let i, maxWidth;

      for (maxWidth = 0, i = 0; i < numActions; i++)
      {
          let stringWidth = GetStringWidth(FONT_NORMAL, actions[i].text, 0);
          if (stringWidth > maxWidth)
              maxWidth = stringWidth;
      }

      return ConvertPixelWidthToTileWidth(maxWidth);
}

/** int GetMaxWidthInSubsetOfMenuTable(const struct MenuAction *actions, const u8 *actionIds, int numActions) */
export function GetMaxWidthInSubsetOfMenuTable(...args: any[]): any {
  let i, maxWidth;

      for (maxWidth = 0, i = 0; i < numActions; i++)
      {
          let stringWidth = GetStringWidth(FONT_NORMAL, actions[actionIds[i]].text, 0);
          if (stringWidth > maxWidth)
              maxWidth = stringWidth;
      }

      return ConvertPixelWidthToTileWidth(maxWidth);
}

/** int Intl_GetListMenuWidth(const struct ListMenuTemplate *listMenu) */
export function Intl_GetListMenuWidth(...args: any[]): any {
  let i, maxWidth, finalWidth;
      let items: any = listMenu.items;

      maxWidth = 0;
      for (i = 0; i < listMenu.totalItems; i++)
      {
          let width = GetStringWidth(listMenu.fontId, items[i].name, 0);
          if (width > maxWidth)
              maxWidth = width;
      }

      finalWidth = maxWidth + listMenu.item_X + 9;
      finalWidth /= 8;
      if (finalWidth > 28)
          finalWidth = 28;

      return finalWidth;
}

/** void CopyMonCategoryText(int dexNum, u8 *dest) */
export function CopyMonCategoryText(...args: any[]): any {
  let str = StringCopy(dest, gPokedexEntries[dexNum].categoryName);
}

/** void PadNameString(u8 *dest, u8 padChar) */
export function PadNameString(...args: any[]): any {
  let length: any = null;

      StripExtCtrlCodes(dest);
      length = StringLength(dest);
      if (padChar == EXT_CTRL_CODE_BEGIN)
      {
          while (length < PLAYER_NAME_LENGTH - 1)
          {
              dest[length] = EXT_CTRL_CODE_BEGIN;
              dest[length + 1] = EXT_CTRL_CODE_RESET_FONT;
              length += 2;
          }
      }
      else
      {
          while (length < PLAYER_NAME_LENGTH - 1)
          {
              dest[length] = padChar;
              length++;
          }
      }

      dest[length] = EOS;
}

/** void ConvertInternationalPlayerName(u8 *str) */
export function ConvertInternationalPlayerName(...args: any[]): any {
  if (StringLength(str) < PLAYER_NAME_LENGTH - 1)
          ConvertInternationalString(str, LANGUAGE_JAPANESE);
      else
          StripExtCtrlCodes(str);
}

/** void ConvertInternationalPlayerNameStripChar(u8 *str, u8 removeChar) */
export function ConvertInternationalPlayerNameStripChar(...args: any[]): any {
  let buffer: any = null;
      if (StringLength(str) < PLAYER_NAME_LENGTH - 1)
      {
          ConvertInternationalString(str, LANGUAGE_JAPANESE);
      }
      else if (removeChar == EXT_CTRL_CODE_BEGIN)
      {
          StripExtCtrlCodes(str);
      }
      else
      {
          buffer = str;
          while (buffer[1] != EOS)
              buffer++;

          while (buffer >= str && buffer[0] == removeChar)
          {
              buffer[0] = EOS;
              buffer--;
          }
      }
}

/** void ConvertInternationalContestantName(u8 *str) */
export function ConvertInternationalContestantName(...args: any[]): any {
  if (*str++ == EXT_CTRL_CODE_BEGIN && *str++ == EXT_CTRL_CODE_JPN)
      {
          while (*str != EOS)
          {
              if (str[0] == EXT_CTRL_CODE_BEGIN && str[1] == EXT_CTRL_CODE_ENG)
                  return;

              str++;
          }

          *str++ = EXT_CTRL_CODE_BEGIN;
          *str++ = EXT_CTRL_CODE_ENG;
          *str = EOS;
      }
}

/** void TVShowConvertInternationalString(u8 *dest, const u8 *src, int language) */
export function TVShowConvertInternationalString(...args: any[]): any {
  StringCopy(dest, src);
      ConvertInternationalString(dest, language);
}

/** int GetNicknameLanguage(u8 *str) */
export function GetNicknameLanguage(...args: any[]): any {
  if (str[0] == EXT_CTRL_CODE_BEGIN && str[1] == EXT_CTRL_CODE_JPN)
          return LANGUAGE_JAPANESE;
      else
          return LANGUAGE_ENGLISH;
}

/** void FillWindowTilesByRow(int windowId, int columnStart, int rowStart, int numFillTiles, int numRows) */
export function FillWindowTilesByRow(...args: any[]): any {
  let windowTileData: any = null;
      let fillSize, windowRowSize, i;
      let window: any = gWindows[windowId];

      fillSize = numFillTiles * TILE_SIZE_4BPP;
      windowRowSize = window.window.width * TILE_SIZE_4BPP;
      windowTileData = window.tileData + (rowStart * windowRowSize) + (columnStart * TILE_SIZE_4BPP);
      if (numRows > 0)
      {
          for (i = numRows; i != 0; i--)
          {
              CpuFastFill8(0x11, windowTileData, fillSize);
              windowTileData += windowRowSize;
          }
      }
}

// ─── callsTo manifest (= 9 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ConvertInternationalString","ConvertPixelWidthToTileWidth","CpuFastFill8","GetStringCenterAlignXOffsetWithLetterSpacing","GetStringWidth","GetStringWidthDifference","StringCopy","StringLength","StripExtCtrlCodes"];
