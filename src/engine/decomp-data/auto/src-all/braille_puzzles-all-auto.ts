// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/braille_puzzles.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 17 functions extracted from src/braille_puzzles.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck


// ─── AUTO-INJECTED file-scope vars (= EWRAM/IWRAM static C decls) ──
let sIsRegisteelPuzzle: any = null;
/** bool8 ShouldDoBrailleDigEffect(void) */
export function ShouldDoBrailleDigEffect(): any {
  if (!FlagGet((((((((0x500) + (864) - 1)) + 1)) + 0x4F)))
       && (gSaveBlock1Ptr.location.mapGroup == MAP_GROUP(MAP_SEALED_CHAMBER_OUTER_ROOM)
       && gSaveBlock1Ptr.location.mapNum == MAP_NUM(MAP_SEALED_CHAMBER_OUTER_ROOM)))
      {
          if (gSaveBlock1Ptr.pos.x == 10 && gSaveBlock1Ptr.pos.y == 3)
              return TRUE;
          if (gSaveBlock1Ptr.pos.x == 9 && gSaveBlock1Ptr.pos.y == 3)
              return TRUE;
          if (gSaveBlock1Ptr.pos.x == 11 && gSaveBlock1Ptr.pos.y == 3)
              return TRUE;
      }

      return FALSE;
}

/** void DoBrailleDigEffect(void) */
export function DoBrailleDigEffect(): any {
  MapGridSetMetatileIdAt( 9 + (7), 1 + (7), (0x22A));
      MapGridSetMetatileIdAt(10 + (7), 1 + (7), (0x22B));
      MapGridSetMetatileIdAt(11 + (7), 1 + (7), (0x22C));
      MapGridSetMetatileIdAt( 9 + (7), 2 + (7), (0x232) | ((0x0C00)));
      MapGridSetMetatileIdAt(10 + (7), 2 + (7), (0x233));
      MapGridSetMetatileIdAt(11 + (7), 2 + (7), (0x234) | ((0x0C00)));
      DrawWholeMapView();
      PlaySE((20));
      FlagSet((((((((0x500) + (864) - 1)) + 1)) + 0x4F)));
      UnlockPlayerFieldControls();
}

/** bool8 CheckRelicanthWailord(void) */
export function CheckRelicanthWailord(): any {
  if (GetMonData(gPlayerParty[0], MON_DATA_SPECIES_OR_EGG, 0) == (314))
      {
          CalculatePlayerPartyCount();
           
          if (GetMonData(gPlayerParty[gPlayerPartyCount - 1], MON_DATA_SPECIES_OR_EGG, 0) == (381))
              return TRUE;
      }
      return FALSE;
}

/** void DoSealedChamberShakingEffect_Long(void) */
export function DoSealedChamberShakingEffect_Long(): any {
  let taskId: any = CreateTask(Task_SealedChamberShakingEffect, 9);

      gTasks[taskId].tDelayCounter = 0;
      gTasks[taskId].tShakeCounter = 0;
      gTasks[taskId].tVerticalPan = 2;
      gTasks[taskId].tDelay = 5;
      gTasks[taskId].tNumShakes = 50;
      SetCameraPanningCallback(0);
}

/** void DoSealedChamberShakingEffect_Short(void) */
export function DoSealedChamberShakingEffect_Short(): any {
  let taskId: any = CreateTask(Task_SealedChamberShakingEffect, 9);

      gTasks[taskId].tDelayCounter = 0;
      gTasks[taskId].tShakeCounter = 0;
      gTasks[taskId].tVerticalPan = 3;
      gTasks[taskId].tDelay = 5;
      gTasks[taskId].tNumShakes = 2;
      SetCameraPanningCallback(0);
}

/** static void Task_SealedChamberShakingEffect(u8 taskId) */
export function Task_SealedChamberShakingEffect(taskId: any): any {
  let task: any =gTasks[taskId];

      task.tDelayCounter++;
      if (task.tDelayCounter % task.tDelay == 0)
      {
          task.tDelayCounter = 0;
          task.tShakeCounter++;
          task.tVerticalPan = -task.tVerticalPan;
          SetCameraPanning(0, task.tVerticalPan);
          if (task.tShakeCounter == task.tNumShakes)
          {
              DestroyTask(taskId);
              ScriptContext_Enable();
              InstallCameraPanAheadCallback();
          }
      }
}

/** bool8 ShouldDoBrailleRegirockEffect(void) */
export function ShouldDoBrailleRegirockEffect(): any {
  if (!FlagGet((((((((0x500) + (864) - 1)) + 1)) + 0x50)))
          && gSaveBlock1Ptr.location.mapGroup == MAP_GROUP(MAP_DESERT_RUINS)
          && gSaveBlock1Ptr.location.mapNum == MAP_NUM(MAP_DESERT_RUINS))
      {
          if (gSaveBlock1Ptr.pos.x == 6 && gSaveBlock1Ptr.pos.y == 23)
          {
              sIsRegisteelPuzzle = FALSE;
              return TRUE;
          }
          else if (gSaveBlock1Ptr.pos.x == 5 && gSaveBlock1Ptr.pos.y == 23)
          {
              sIsRegisteelPuzzle = FALSE;
              return TRUE;
          }
          else if (gSaveBlock1Ptr.pos.x == 7 && gSaveBlock1Ptr.pos.y == 23)
          {
              sIsRegisteelPuzzle = FALSE;
              return TRUE;
          }
      }

      return FALSE;
}

/** void SetUpPuzzleEffectRegirock(void) */
export function SetUpPuzzleEffectRegirock(): any {
  gFieldEffectArguments[0] = GetCursorSelectionMonId();
      FieldEffectStart((60));
}

/** void UseRegirockHm_Callback(void) */
export function UseRegirockHm_Callback(): any {
  FieldEffectActiveListRemove((60));
      DoBrailleRegirockEffect();
}

/** static void DoBrailleRegirockEffect(void) */
export function DoBrailleRegirockEffect(): any {
  MapGridSetMetatileIdAt(7 + (7), 19 + (7), (0x22A));
      MapGridSetMetatileIdAt(8 + (7), 19 + (7), (0x22B));
      MapGridSetMetatileIdAt(9 + (7), 19 + (7), (0x22C));
      MapGridSetMetatileIdAt(7 + (7), 20 + (7), (0x232) | ((0x0C00)));
      MapGridSetMetatileIdAt(8 + (7), 20 + (7), (0x233));
      MapGridSetMetatileIdAt(9 + (7), 20 + (7), (0x234) | ((0x0C00)));
      DrawWholeMapView();
      PlaySE((20));
      FlagSet((((((((0x500) + (864) - 1)) + 1)) + 0x50)));
      UnlockPlayerFieldControls();
}

/** bool8 ShouldDoBrailleRegisteelEffect(void) */
export function ShouldDoBrailleRegisteelEffect(): any {
  if (!FlagGet((((((((0x500) + (864) - 1)) + 1)) + 0x52))) && (gSaveBlock1Ptr.location.mapGroup == MAP_GROUP(MAP_ANCIENT_TOMB) && gSaveBlock1Ptr.location.mapNum == MAP_NUM(MAP_ANCIENT_TOMB)))
      {
          if (gSaveBlock1Ptr.pos.x == 8 && gSaveBlock1Ptr.pos.y == 25)
          {
              sIsRegisteelPuzzle = TRUE;
              return TRUE;
          }
      }
      return FALSE;
}

/** void SetUpPuzzleEffectRegisteel(void) */
export function SetUpPuzzleEffectRegisteel(): any {
  gFieldEffectArguments[0] = GetCursorSelectionMonId();
      FieldEffectStart((60));
}

/** void UseRegisteelHm_Callback(void) */
export function UseRegisteelHm_Callback(): any {
  FieldEffectActiveListRemove((60));
      DoBrailleRegisteelEffect();
}

/** static void DoBrailleRegisteelEffect(void) */
export function DoBrailleRegisteelEffect(): any {
  MapGridSetMetatileIdAt(7 + (7), 19 + (7), (0x22A));
      MapGridSetMetatileIdAt(8 + (7), 19 + (7), (0x22B));
      MapGridSetMetatileIdAt(9 + (7), 19 + (7), (0x22C));
      MapGridSetMetatileIdAt(7 + (7), 20 + (7), (0x232) | ((0x0C00)));
      MapGridSetMetatileIdAt(8 + (7), 20 + (7), (0x233));
      MapGridSetMetatileIdAt(9 + (7), 20 + (7), (0x234) | ((0x0C00)));
      DrawWholeMapView();
      PlaySE((20));
      FlagSet((((((((0x500) + (864) - 1)) + 1)) + 0x52)));
      UnlockPlayerFieldControls();
}

/** bool8 FldEff_UsePuzzleEffect(void) */
export function FldEff_UsePuzzleEffect(): any {
  let taskId: any = CreateFieldMoveTask();

      if (sIsRegisteelPuzzle == TRUE)
      {
          gTasks[taskId].data[8] = UseRegisteelHm_Callback >> 16;
          gTasks[taskId].data[9] = UseRegisteelHm_Callback;
      }
      else
      {
          gTasks[taskId].data[8] = UseRegirockHm_Callback >> 16;
          gTasks[taskId].data[9] = UseRegirockHm_Callback;
      }
      return FALSE;
}

/** bool8 ShouldDoBrailleRegicePuzzle(void) */
export function ShouldDoBrailleRegicePuzzle(): any {
  let i: any = null;

      if (gSaveBlock1Ptr.location.mapGroup == MAP_GROUP(MAP_ISLAND_CAVE)
          && gSaveBlock1Ptr.location.mapNum == MAP_NUM(MAP_ISLAND_CAVE))
      {
          if (FlagGet((((((((0x500) + (864) - 1)) + 1)) + 0x51))))
              return FALSE;
           
          if (FlagGet(((((0x0) + 0x2)))) == FALSE)
              return FALSE;
           
          if (FlagGet(((((0x0) + 0x3)))) == TRUE)
              return FALSE;

          for (i = 0; i < ARRAY_COUNT(sRegicePathCoords); i++)
          {
              let xPos: any = sRegicePathCoords[i][0];
              let yPos: any = sRegicePathCoords[i][1];
              if (gSaveBlock1Ptr.pos.x == xPos && gSaveBlock1Ptr.pos.y == yPos)
              {
                   
                  if (i < 16)
                  {
                      let val: any = VarGet((0x403B));
                      val |= 1 << i;
                      VarSet((0x403B), val);
                  }
                  else if (i < 32)
                  {
                      let val: any = VarGet((0x403C));
                      val |= 1 << (i - 16);
                      VarSet((0x403C), val);
                  }
                  else
                  {
                      let val: any = VarGet((0x403D));
                      val |= 1 << (i - 32);
                      VarSet((0x403D), val);
                  }

                   
                  if (VarGet((0x403B)) != 0xFFFF || VarGet((0x403C)) != 0xFFFF || VarGet((0x403D)) != 0xF)
                      return FALSE;

                   
                  if (gSaveBlock1Ptr.pos.x == 8 && gSaveBlock1Ptr.pos.y == 21)
                      return TRUE;
                  else
                      return FALSE;
              }
          }

           
          FlagSet(((((0x0) + 0x3))));
          FlagClear(((((0x0) + 0x2))));
      }

      return FALSE;
}

// ─── callsTo manifest (= 26 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","CalculatePlayerPartyCount","CreateFieldMoveTask","CreateTask","DestroyTask","DoBrailleRegirockEffect","DoBrailleRegisteelEffect","DrawWholeMapView","FieldEffectActiveListRemove","FieldEffectStart","FlagClear","FlagGet","FlagSet","GetCursorSelectionMonId","GetMonData","InstallCameraPanAheadCallback","MAP_GROUP","MAP_NUM","MapGridSetMetatileIdAt","PlaySE","ScriptContext_Enable","SetCameraPanning","SetCameraPanningCallback","UnlockPlayerFieldControls","VarGet","VarSet"];

// ─── Failed transpiles (1) ───────────────────────────────
// ShouldDoBrailleRegirockEffectOld : empty body