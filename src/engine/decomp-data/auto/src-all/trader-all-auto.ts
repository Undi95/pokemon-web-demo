// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/trader.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 13 functions extracted from src/trader.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  AddTextPrinterParameterized, AddWindow, ClearStdWindowAndFrameToTransparent, ClearWindowTilemap,
  ConvertInternationalString, ConvertPixelWidthToTileWidth, CopyDecorationCategoryName, CreateTask,
  DecorationAdd, DecorationRemove, DestroyTask, DrawStdFrameWithCustomTileAndPalette,
  FALSE, GetFirstEmptyDecorSlot, GetNumOwnedDecorationsInCategory, GetStringWidth,
  InitMenuInUpperLeftCornerNormal, IsSelectedDecorInThePC, Menu_ProcessInput, NULL,
  PlaySE, RemoveWindow, ScheduleBgCopyTilemapToVram, ScriptContext_Enable,
  StringCopy, TRUE,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void TraderSetup(void) */
export function TraderSetup(): any {
  let i: any = null;
      let trader: any =gSaveBlock1Ptr.oldMan.trader;

      trader.id = MAUVILLE_MAN_TRADER;
      trader.alreadyTraded = FALSE;

      for (i = 0; i < NUM_TRADER_ITEMS; i++)
      {
          StringCopy(trader.playerNames[i], sDefaultTraderNames[i]);
          trader.decorations[i] = sDefaultTraderDecorations[i];
          trader.language[i] = GAME_LANGUAGE;
      }
}

/** void Trader_ResetFlag(void) */
export function Trader_ResetFlag(): any {
  let trader: any =gSaveBlock1Ptr.oldMan.trader;
      trader.alreadyTraded = FALSE;
}

/** void CreateAvailableDecorationsMenu(u8 taskId) */
export function CreateAvailableDecorationsMenu(taskId: any): any {
  let i: any = null;
      let data: any = gTasks[taskId].data;
      let trader: any =gSaveBlock1Ptr.oldMan.trader;
      let windowTemplate: any = [ 0, 1, 1, 10, 10, 15, 1
      ];
      let windowWidth: any = GetStringWidth(FONT_NORMAL, gText_Exit, 0);
      let fiveMarksWidth: any = GetStringWidth(FONT_NORMAL, gText_FiveMarks, 0);
      for (i = 0; i < NUM_TRADER_ITEMS; i++)
      {
          let curWidth: any = null;
          if (trader.decorations[i] > NUM_DECORATIONS)
              curWidth = fiveMarksWidth;
          else
              curWidth = GetStringWidth(FONT_NORMAL, gDecorations[trader.decorations[i]].name, 0);
          if (curWidth > windowWidth)
              windowWidth = curWidth;
      }
      windowTemplate.width = ConvertPixelWidthToTileWidth(windowWidth);
      tWindowId = AddWindow(windowTemplate);
      DrawStdFrameWithCustomTileAndPalette(tWindowId, FALSE, _0x214, 14);
      for (i = 0; i < NUM_TRADER_ITEMS; i++)
      {
          if (trader.decorations[i] > NUM_DECORATIONS)
              AddTextPrinterParameterized(tWindowId, FONT_NORMAL, gText_FiveMarks, 8, 16 * i + 1, TEXT_SKIP_DRAW, NULL);
          else
              AddTextPrinterParameterized(tWindowId, FONT_NORMAL, gDecorations[trader.decorations[i]].name, 8, 16 * i + 1, TEXT_SKIP_DRAW, NULL);
      }
      AddTextPrinterParameterized(tWindowId, FONT_NORMAL, gText_Exit, 8, 16 * i + 1, TEXT_SKIP_DRAW, NULL);
      InitMenuInUpperLeftCornerNormal(tWindowId, NUM_TRADER_ITEMS + 1, 0);
      ScheduleBgCopyTilemapToVram(0);
}

/** void Task_BufferDecorSelectionAndCloseWindow(u8 taskId, u8 decorationId) */
export function Task_BufferDecorSelectionAndCloseWindow(taskId: any, decorationId: any): any {
  let data: any = gTasks[taskId].data;
      if (decorationId > NUM_DECORATIONS)
          gSpecialVar_0x8004 = 0xFFFF;
      else
          gSpecialVar_0x8004 = decorationId;

      ClearStdWindowAndFrameToTransparent(tWindowId, FALSE);
      ClearWindowTilemap(tWindowId);
      RemoveWindow(tWindowId);
      ScheduleBgCopyTilemapToVram(0);
      DestroyTask(taskId);
      ScriptContext_Enable();
}

/** void Task_HandleGetDecorationMenuInput(u8 taskId) */
export function Task_HandleGetDecorationMenuInput(taskId: any): any {
  let trader: any =gSaveBlock1Ptr.oldMan.trader;
      let input: any = Menu_ProcessInput();

      switch (input)
      {
          case MENU_NOTHING_CHOSEN:
              break;
          case MENU_B_PRESSED:
          case NUM_TRADER_ITEMS:  
              PlaySE(SE_SELECT);
              Task_BufferDecorSelectionAndCloseWindow(taskId, 0);
              break;
          default:
              PlaySE(SE_SELECT);
              gSpecialVar_0x8005 = input;
              StringCopy(gStringVar1, trader.playerNames[input]);
              ConvertInternationalString(gStringVar1, trader.language[input]);
              Task_BufferDecorSelectionAndCloseWindow(taskId, trader.decorations[input]);
              break;
      }
}

/** void GetTraderTradedFlag(void) */
export function GetTraderTradedFlag(): any {
  let trader: any =gSaveBlock1Ptr.oldMan.trader;
      gSpecialVar_Result = trader.alreadyTraded;
}

/** void DoesPlayerHaveNoDecorations(void) */
export function DoesPlayerHaveNoDecorations(): any {
  let i: any = null;

      for (i = 0; i < DECORCAT_COUNT; i++)
      {
          if (GetNumOwnedDecorationsInCategory(i))
          {
              gSpecialVar_Result = FALSE;
              return;
          }
      }
      gSpecialVar_Result = TRUE;
}

/** void IsDecorationCategoryFull(void) */
export function IsDecorationCategoryFull(): any {
  gSpecialVar_Result = FALSE;
      if (gDecorations[gSpecialVar_0x8004].category != gDecorations[gSpecialVar_0x8006].category
          && GetFirstEmptyDecorSlot(gDecorations[gSpecialVar_0x8004].category) == -1)
      {
          CopyDecorationCategoryName(gStringVar2, gDecorations[gSpecialVar_0x8004].category);
          gSpecialVar_Result = TRUE;
      }
}

/** void TraderShowDecorationMenu(void) */
export function TraderShowDecorationMenu(): any {
  CreateTask(ShowDecorationCategoriesWindow, 0);
}

/** void DecorationItemsMenuAction_Trade(u8 taskId) */
export function DecorationItemsMenuAction_Trade(taskId: any): any {
  if (IsSelectedDecorInThePC() == TRUE)
      {
          gSpecialVar_0x8006 = gCurDecorationItems[gCurDecorationIndex];
          StringCopy(gStringVar3, gDecorations[gSpecialVar_0x8004].name);
          StringCopy(gStringVar2, gDecorations[gSpecialVar_0x8006].name);
      }
      else
      {
          gSpecialVar_0x8006 = 0xFFFF;
      }
      DestroyTask(taskId);
      ScriptContext_Enable();
}

/** void ExitTraderMenu(u8 taskId) */
export function ExitTraderMenu(taskId: any): any {
  gSpecialVar_0x8006 = 0;
      DestroyTask(taskId);
      ScriptContext_Enable();
}

/** void TraderDoDecorationTrade(void) */
export function TraderDoDecorationTrade(): any {
  let trader: any =gSaveBlock1Ptr.oldMan.trader;

      DecorationRemove(gSpecialVar_0x8006);
      DecorationAdd(gSpecialVar_0x8004);
      StringCopy(trader.playerNames[gSpecialVar_0x8005], gSaveBlock2Ptr.playerName);
      trader.decorations[gSpecialVar_0x8005] = gSpecialVar_0x8006;
      trader.language[gSpecialVar_0x8005] = GAME_LANGUAGE;
      trader.alreadyTraded = TRUE;
}

/** void TraderMenuGetDecoration(void) */
export function TraderMenuGetDecoration(): any {
  let taskId: any = CreateTask(Task_HandleGetDecorationMenuInput, 0);
      CreateAvailableDecorationsMenu(taskId);
}

// ─── callsTo manifest (= 25 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AddTextPrinterParameterized","AddWindow","ClearStdWindowAndFrameToTransparent","ClearWindowTilemap","ConvertInternationalString","ConvertPixelWidthToTileWidth","CopyDecorationCategoryName","CreateAvailableDecorationsMenu","CreateTask","DecorationAdd","DecorationRemove","DestroyTask","DrawStdFrameWithCustomTileAndPalette","GetFirstEmptyDecorSlot","GetNumOwnedDecorationsInCategory","GetStringWidth","InitMenuInUpperLeftCornerNormal","IsSelectedDecorInThePC","Menu_ProcessInput","PlaySE","RemoveWindow","ScheduleBgCopyTilemapToVram","ScriptContext_Enable","StringCopy","Task_BufferDecorSelectionAndCloseWindow"];
