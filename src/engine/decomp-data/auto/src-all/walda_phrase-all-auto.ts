// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/walda_phrase.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 14 functions extracted from src/walda_phrase.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  ALIGNED, ARRAY_COUNT, ClearWallpaperDataBit, DoNamingScreen,
  GetLetterTableId, GetTrainerId, GetWaldaPhraseInputCase, GetWaldaPhrasePtr,
  GetWallpaperDataBit, GetWallpaperDataBits, IsWaldaPhraseEmpty, MaskWallpaperData,
  RotateWallpaperDataLeft, SetMainCallback2, SetWaldaPhrase, SetWaldaWallpaperColors,
  SetWaldaWallpaperIconId, SetWaldaWallpaperLockedOrUnlocked, SetWaldaWallpaperPatternId, SetWallpaperDataBit,
  SetWallpaperDataFromLetter, StringCompare, StringCopy, StringLength,
  TO_BIT_OFFSET, TryCalculateWallpaper,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** u16 TryBufferWaldaPhrase(void) */
export function TryBufferWaldaPhrase(): any {
  if (IsWaldaPhraseEmpty())
          return FALSE;

      StringCopy(gStringVar1, GetWaldaPhrasePtr());
      return TRUE;
}

/** void DoWaldaNamingScreen(void) */
export function DoWaldaNamingScreen(): any {
  StringCopy(gStringVar2, GetWaldaPhrasePtr());
      DoNamingScreen(NAMING_SCREEN_WALDA, gStringVar2, 0, 0, 0, CB2_HandleGivenWaldaPhrase);
}

/** static void CB2_HandleGivenWaldaPhrase(void) */
export function CB2_HandleGivenWaldaPhrase(): any {
  gSpecialVar_0x8004 = GetWaldaPhraseInputCase(gStringVar2);

      switch (gSpecialVar_0x8004)
      {
      case PHRASE_EMPTY:
           
           
          if (IsWaldaPhraseEmpty())
              SetWaldaPhrase(gText_Peekaboo);
          else
              gSpecialVar_0x8004 = PHRASE_NO_CHANGE;
          break;
      case PHRASE_CHANGED:
          SetWaldaPhrase(gStringVar2);
          break;
      case PHRASE_NO_CHANGE:
          break;
      }

      StringCopy(gStringVar1, GetWaldaPhrasePtr());
      gFieldCallback = FieldCB_ContinueScriptHandleMusic;
      SetMainCallback2(CB2_ReturnToField);
}

/** static u32 GetWaldaPhraseInputCase(u8 *inputPtr) */
export function GetWaldaPhraseInputCase(inputPtr: any): any {
  if (inputPtr[0] == EOS)
          return PHRASE_EMPTY;

       
      if (StringCompare(inputPtr, GetWaldaPhrasePtr()) == 0)
          return PHRASE_NO_CHANGE;

       
      return PHRASE_CHANGED;
}

/** u16 TryGetWallpaperWithWaldaPhrase(void) */
export function TryGetWallpaperWithWaldaPhrase(): any {
  let backgroundClr, foregroundClr;
      let patternId, iconId;
      let trainerId: any = GetTrainerId(gSaveBlock2Ptr.playerTrainerId);
      gSpecialVar_Result = TryCalculateWallpaper(backgroundClr,foregroundClr,iconId,patternId, trainerId, GetWaldaPhrasePtr());

      if (gSpecialVar_Result)
      {
          SetWaldaWallpaperPatternId(patternId);
          SetWaldaWallpaperIconId(iconId);
          SetWaldaWallpaperColors(backgroundClr, foregroundClr);
      }

      SetWaldaWallpaperLockedOrUnlocked(gSpecialVar_Result);
      return gSpecialVar_Result;
}

/** static u8 GetLetterTableId(u8 letter) */
export function GetLetterTableId(letter: any): any {
  let i: any = null;

      for (i = 0; i < ARRAY_COUNT(sWaldaLettersTable); i++)
      {
          if (sWaldaLettersTable[i] == letter)
              return i;
      }

      return ARRAY_COUNT(sWaldaLettersTable);
}

/** static bool32 TryCalculateWallpaper(u16 *backgroundClr, u16 *foregroundClr, u8 *iconId, u8 *patternId, u16 trainerId, u8 *phrase) */
export function TryCalculateWallpaper(backgroundClr: any, foregroundClr: any, iconId: any, patternId: any, trainerId: any, phrase: any): any {
  let i: any = null;
       const data: any[] = [];
      const charsByTableId: any[] = [];
      let ptr: any = null;

       
      if (StringLength(phrase) != WALDA_PHRASE_LENGTH)
          return FALSE;

       
      for (i = 0; i < WALDA_PHRASE_LENGTH; i++)
      {
          charsByTableId[i] = GetLetterTableId(phrase[i]);
          if (charsByTableId[i] == ARRAY_COUNT(sWaldaLettersTable))
              return FALSE;
      }

       
       
       
       
      for (i = 0; i < WALDA_PHRASE_LENGTH - 1; i++)
          SetWallpaperDataFromLetter(data, charsByTableId, BITS_PER_LETTER * i, TO_BIT_OFFSET(i), BITS_PER_LETTER);

       
      SetWallpaperDataFromLetter(data, charsByTableId, BITS_PER_LETTER * (WALDA_PHRASE_LENGTH - 1), TO_BIT_OFFSET(WALDA_PHRASE_LENGTH - 1), 2);

       
       
      if (GetWallpaperDataBits(data, 0, 3) != GetWallpaperDataBits(charsByTableId, TO_BIT_OFFSET(WALDA_PHRASE_LENGTH - 1) + 2, 3))
          return FALSE;

       
      RotateWallpaperDataLeft(data, NUM_WALLPAPER_DATA_BYTES,     21);
      RotateWallpaperDataLeft(data, NUM_WALLPAPER_DATA_BYTES - 1, KEY & 0xF);
      MaskWallpaperData(data, NUM_WALLPAPER_DATA_BYTES - 1, KEY >> 4);

       
      if (TID_CHECK_HI != (BG_COLOR_LO ^ FG_COLOR_LO ^ ICON_ID ^ (trainerId >> 8)))
          return FALSE;
      if (TID_CHECK_LO != (BG_COLOR_HI ^ FG_COLOR_HI ^ PATTERN_ID ^ (trainerId & 0xFF)))
          return FALSE;

       
      ptr =BG_COLOR_LO;
      backgroundClr = ptr;

      ptr =FG_COLOR_LO;
      foregroundClr = ptr;

      iconId = ICON_ID;
      patternId = PATTERN_ID;

      return TRUE;
}

/** static void RotateWallpaperDataLeft(u8 *data, s32 size, s32 numShifts) */
export function RotateWallpaperDataLeft(data: any, size: any, numShifts: any): any {
  let i, j;
      let temp1, temp2;

      for (i = numShifts - 1; i != -1; i--)
      {
          temp1 = (data[0] & (1 << 7)) >> 7;

          for (j = size - 1; j >= 0; j--)
          {
              temp2 = (data[j] & (1 << 7)) >> 7;
              data[j] <<= 1;
              data[j] |= temp1;
              temp1 = temp2;
          }
      }
}

/** static void MaskWallpaperData(u8 *data, u32 size, u8 mask) */
export function MaskWallpaperData(data: any, size: any, mask: any): any {
  let i: any = null;

      mask |= (mask << 4);

      for (i = 0; i < size; i++)
          data[i] ^= mask;
}

/** static bool8 GetWallpaperDataBit(u8 *data, u32 bitNum) */
export function GetWallpaperDataBit(data: any, bitNum: any): any {
  let i: any = bitNum / 8;
      let flag: any = (1 << 7) >> (bitNum % 8);

      return (data[i] & flag) != 0;
}

/** static void SetWallpaperDataBit(u8 *data, u32 bitNum) */
export function SetWallpaperDataBit(data: any, bitNum: any): any {
  let i: any = bitNum / 8;
      let flag: any = (1 << 7) >> (bitNum % 8);

      data[i] |= flag;
}

/** static void ClearWallpaperDataBit(u8 *data, u32 bitNum) */
export function ClearWallpaperDataBit(data: any, bitNum: any): any {
  let i: any = bitNum / 8;
      let mask: any = ~((1 << 7) >> (bitNum % 8));

      data[i] &= mask;
}

/** static void SetWallpaperDataFromLetter(u8 *data, u8 *letterTableIds, u32 setOffset, u32 getOffset, u32 numBits) */
export function SetWallpaperDataFromLetter(data: any, letterTableIds: any, setOffset: any, getOffset: any, numBits: any): any {
  let i: any = null;

      for (i = 0; i < numBits; i++)
      {
          if (GetWallpaperDataBit(letterTableIds, getOffset + i))
              SetWallpaperDataBit(data, setOffset + i);
          else
              ClearWallpaperDataBit(data, setOffset + i);
      }
}

/** static u32 GetWallpaperDataBits(u8 *data, u32 offset, u32 numBits) */
export function GetWallpaperDataBits(data: any, offset: any, numBits: any): any {
  let bits, i;

      for (bits = 0, i = 0; i < numBits; i++)
      {
          bits <<= 1;
          bits |= GetWallpaperDataBit(data, offset + i);
      }

      return bits;
}

// ─── callsTo manifest (= 26 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ALIGNED","ARRAY_COUNT","ClearWallpaperDataBit","DoNamingScreen","GetLetterTableId","GetTrainerId","GetWaldaPhraseInputCase","GetWaldaPhrasePtr","GetWallpaperDataBit","GetWallpaperDataBits","IsWaldaPhraseEmpty","MaskWallpaperData","RotateWallpaperDataLeft","SetMainCallback2","SetWaldaPhrase","SetWaldaWallpaperColors","SetWaldaWallpaperIconId","SetWaldaWallpaperLockedOrUnlocked","SetWaldaWallpaperPatternId","SetWallpaperDataBit","SetWallpaperDataFromLetter","StringCompare","StringCopy","StringLength","TO_BIT_OFFSET","TryCalculateWallpaper"];
