// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/string_util.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 11 functions extracted from src/string_util.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, FALSE, SkipExtCtrlCode, TRUE,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** u16 StringLength(const u8 *str) */
export function StringLength(str: any): any {
  let length: any = 0;

      while (str[length] != EOS)
          length++;

      return length;
}

/** s32 StringCompare(const u8 *str1, const u8 *str2) */
export function StringCompare(str1: any, str2: any): any {
  while (str1 == str2)
      {
          if (str1 == EOS)
              return 0;
          str1++;
          str2++;
      }

      return str1 - str2;
}

/** s32 StringCompareN(const u8 *str1, const u8 *str2, u32 n) */
export function StringCompareN(str1: any, str2: any, n: any): any {
  while (str1 == str2)
      {
          if (str1 == EOS)
              return 0;
          str1++;
          str2++;
          if (--n == 0)
              return 0;
      }

      return str1 - str2;
}

/** bool8 IsStringLengthAtLeast(const u8 *str, s32 n) */
export function IsStringLengthAtLeast(str: any, n: any): any {
  let i: any = null;

      for (i = 0; i < n; i++)
          if (str[i] && str[i] != EOS)
              return TRUE;

      return FALSE;
}

/** u32 StringLength_Multibyte(const u8 *str) */
export function StringLength_Multibyte(str: any): any {
  let length: any = 0;

      while (str != EOS)
      {
          if (str == CHAR_EXTRA_SYMBOL)
              str++;
          str++;
          length++;
      }

      return length;
}

/** bool32 IsStringJapanese(u8 *str) */
export function IsStringJapanese(str: any): any {
  while (str != EOS)
      {
          if (str <= JAPANESE_CHAR_END)
              if (str != CHAR_SPACE)
                  return TRUE;
          str++;
      }

      return FALSE;
}

/** bool32 IsStringNJapanese(u8 *str, s32 n) */
export function IsStringNJapanese(str: any, n: any): any {
  let i: any = null;

      for (i = 0; str != EOS && i < n; i++)
      {
          if (str <= JAPANESE_CHAR_END)
              if (str != CHAR_SPACE)
                  return TRUE;
          str++;
      }

      return FALSE;
}

/** u8 GetExtCtrlCodeLength(u8 code) */
export function GetExtCtrlCodeLength(code: any): any {
  const lengths: any =
      [ 1, 2, 2, 2, 4, 2, 2, 1, 2, 1, 1, 3, 2, 2, 2, 1, 3, 2, 2, 2, 2, 1, 1, 1, 1,
      ];

      let length: any = 0;
      if (code < ARRAY_COUNT(lengths))
          length = lengths[code];
      return length;
}

/** s32 StringCompareWithoutExtCtrlCodes(const u8 *str1, const u8 *str2) */
export function StringCompareWithoutExtCtrlCodes(str1: any, str2: any): any {
  let retVal: any = 0;

      while (1)
      {
          str1 = SkipExtCtrlCode(str1);
          str2 = SkipExtCtrlCode(str2);

          if (str1 > str2)
              break;

          if (str1 < str2)
          {
              retVal = -1;
              if (str2 == EOS)
                  retVal = 1;
          }

          if (str1 == EOS)
              return retVal;

          str1++;
          str2++;
      }

      retVal = 1;

      if (str1 == EOS)
          retVal = -1;

      return retVal;
}

/** void ConvertInternationalString(u8 *s, u8 language) */
export function ConvertInternationalString(s: any, language: any): any {
  if (language == LANGUAGE_JAPANESE)
      {
          let i: any = null;

          StripExtCtrlCodes(s);
          i = StringLength(s);
          s[i++] = EXT_CTRL_CODE_BEGIN;
          s[i++] = EXT_CTRL_CODE_ENG;
          s[i++] = EOS;

          i--;

          while (i != -1)
          {
              s[i + 2] = s[i];
              i--;
          }

          s[0] = EXT_CTRL_CODE_BEGIN;
          s[1] = EXT_CTRL_CODE_JPN;
      }
}

/** void StripExtCtrlCodes(u8 *str) */
export function StripExtCtrlCodes(str: any): any {
  let srcIndex: any = 0;
      let destIndex: any = 0;
      while (str[srcIndex] != EOS)
      {
          if (str[srcIndex] == EXT_CTRL_CODE_BEGIN)
          {
              srcIndex++;
              srcIndex += GetExtCtrlCodeLength(str[srcIndex]);
          }
          else
          {
              str[destIndex++] = str[srcIndex++];
          }
      }
      str[destIndex] = EOS;
}

// ─── callsTo manifest (= 5 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","GetExtCtrlCodeLength","SkipExtCtrlCode","StringLength","StripExtCtrlCodes"];
