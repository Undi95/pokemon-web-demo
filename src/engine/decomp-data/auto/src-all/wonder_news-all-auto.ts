// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/wonder_news.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 9 functions extracted from src/wonder_news.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** void WonderNews_SetReward(u32 newsType) */
export function WonderNews_SetReward(newsType: any): any {
  let data: any = GetSavedWonderNewsMetadata();

      data.newsType = newsType;
      switch (newsType)
      {
      case WONDER_NEWS_NONE:
          break;
      case WONDER_NEWS_RECV_FRIEND:
      case WONDER_NEWS_RECV_WIRELESS:
           
          data.berry = (Random() % 15) + ITEM_TO_BERRY((148));
          break;
      case WONDER_NEWS_SENT:
           
          data.berry = (Random() % 15) + ITEM_TO_BERRY((133));
          break;
      }
}

/** void WonderNews_Reset(void) */
export function WonderNews_Reset(): any {
  let data: any = GetSavedWonderNewsMetadata();

      data.newsType = WONDER_NEWS_NONE;
      data.sentRewardCounter = 0;
      data.rewardCounter = 0;
      data.berry = 0;
      VarSet((0x402E), 0);
}

/** void WonderNews_IncrementStepCounter(void) */
export function WonderNews_IncrementStepCounter(): any {
  let stepCounter: any = GetVarPointer((0x402E));
      let data: any = GetSavedWonderNewsMetadata();

       
       
       
      if (data.rewardCounter >= (5) && ++(stepCounter) >= 500)
      {
          data.rewardCounter = 0;
          stepCounter = 0;
      }
}

/** u16 WonderNews_GetRewardInfo(void) */
export function WonderNews_GetRewardInfo(): any {
  let result: any =gSpecialVar_Result;
      let data: any = GetSavedWonderNewsMetadata();
      let rewardType: any = null;

       
      if (!IsMysteryEventEnabled() || !ValidateSavedWonderNews())
          return (0);

      rewardType = GetRewardType(data);

      switch (rewardType)
      {
      case (1):
      case (2):
          result = GetRewardItem(data);
          break;
      case (4):
          result = GetRewardItem(data);
          IncrementSentRewardCounter(data);
          break;
      case (5):
          result = GetRewardItem(data);
          ResetSentRewardCounter(data);
          break;
      case (0):
      case (3):
      case (6):
          break;
      }

      return rewardType;
}

/** static u32 GetRewardItem(struct WonderNewsMetadata *data) */
export function GetRewardItem(data: any): any {
  let itemId: any = null;
      data.newsType = WONDER_NEWS_NONE;
      itemId = data.berry + ((133)) - 1;
      data.berry = 0;
      IncrementRewardCounter(data);
      return itemId;
}

/** static void ResetSentRewardCounter(struct WonderNewsMetadata *data) */
export function ResetSentRewardCounter(data: any): any {
  data.sentRewardCounter = 0;
}

/** static void IncrementSentRewardCounter(struct WonderNewsMetadata *data) */
export function IncrementSentRewardCounter(data: any): any {
  data.sentRewardCounter++;
      if (data.sentRewardCounter > (4))
          data.sentRewardCounter = (4);
}

/** static void IncrementRewardCounter(struct WonderNewsMetadata *data) */
export function IncrementRewardCounter(data: any): any {
  data.rewardCounter++;
      if (data.rewardCounter > (5))
          data.rewardCounter = (5);
}

/** static u32 GetRewardType(struct WonderNewsMetadata *data) */
export function GetRewardType(data: any): any {
  if (data.rewardCounter == (5))
          return (6);

      switch (data.newsType)
      {
      case WONDER_NEWS_NONE:
          return (3);
      case WONDER_NEWS_RECV_FRIEND:
          return (1);
      case WONDER_NEWS_RECV_WIRELESS:
          return (2);
      case WONDER_NEWS_SENT:
          if (data.sentRewardCounter < (4) - 1)
              return (4);
          return (5);
      default:
          AGB_ASSERT(0);
          return (0);
      }
}

// ─── callsTo manifest (= 13 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AGB_ASSERT","GetRewardItem","GetRewardType","GetSavedWonderNewsMetadata","GetVarPointer","ITEM_TO_BERRY","IncrementRewardCounter","IncrementSentRewardCounter","IsMysteryEventEnabled","Random","ResetSentRewardCounter","ValidateSavedWonderNews","VarSet"];
