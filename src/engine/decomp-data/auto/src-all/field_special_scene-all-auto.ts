// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/field_special_scene.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 13 functions extracted from src/field_special_scene.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  ARRAY_COUNT, CountSSTidalStep, CpuFastFill, CreateObjectGraphicsSprite,
  CreateTask, DIR_EAST, DIR_WEST, DestroyTask,
  DoDiveWarp, DoPortholeWarp, DrawWholeMapView, FALSE,
  FadeInFromBlack, FlagClear, FlagSet, FuncIsActiveTask,
  GetFaceDirectionAnimNum, GetSSTidalLocation, GetVarPointer, InstallCameraPanAheadCallback,
  JOY_NEW, LOCALID_PLAYER, LockPlayerFieldControls, MapGridSetMetatileIdAt,
  NULL, PLTT_SIZE, PlaySE, ScriptMovement_IsObjectMovementFinished,
  ScriptMovement_StartObjectMovementScript, SetCameraPanning, SetCameraPanningCallback, SetDynamicWarp,
  SetObjectEventSpritePosByLocalIdAndMap, SetWarpDestination, SetWarpDestinationToDynamicWarp, SpriteCallbackDummy,
  StartSpriteAnim, TRUE, UnlockPlayerFieldControls, VarGet,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static s16 GetTruckCameraBobbingY(int time) */
export function GetTruckCameraBobbingY(time: any): any {
  if (!(time % 120))
          return -1;
      else if ((time % 10) <= 4)
          return 1;

      return 0;
}

/** static s16 GetTruckBoxYMovement(int time) */
export function GetTruckBoxYMovement(time: any): any {
  if (!((time + 120) % 180))
          return -1;

      return 0;
}

/** static void Task_Truck1(u8 taskId) */
export function Task_Truck1(taskId: any): any {
  let data: any = gTasks[taskId].data;
      let cameraXpan: any = 0, cameraYpan = 0;
      let yBox1, yBox2, yBox3;

      yBox1 = GetTruckBoxYMovement(tTimer + 30) * 4;
      SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_TOP,      gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX1_X_OFFSET - cameraXpan, BOX1_Y_OFFSET + yBox1);
      yBox2 = GetTruckBoxYMovement(tTimer) * 2;
      SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_L, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX2_X_OFFSET - cameraXpan, BOX2_Y_OFFSET + yBox2);
      yBox3 = GetTruckBoxYMovement(tTimer) * 4;
      SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_R, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX3_X_OFFSET - cameraXpan, BOX3_Y_OFFSET + yBox3);

       
      if (++tTimer == 30000)
          tTimer = 0;

      cameraYpan = GetTruckCameraBobbingY(tTimer);
      SetCameraPanning(cameraXpan, cameraYpan);
}

/** static void Task_Truck2(u8 taskId) */
export function Task_Truck2(taskId: any): any {
  let data: any = gTasks[taskId].data;
      let cameraYpan, cameraXpan;
      let yBox1, yBox2, yBox3;

      tTimerHorizontal++;
      tTimerVertical++;

      if (tTimerHorizontal > 5)
      {
          tTimerHorizontal = 0;
          tMoveStep++;
      }
      if (tMoveStep == ARRAY_COUNT(sTruckCamera_HorizontalTable))
      {
           
          DestroyTask(taskId);
      }
      else
      {
          if (sTruckCamera_HorizontalTable[tMoveStep] == 2)
              gTasks[taskId].func = Task_Truck3;

          cameraXpan = sTruckCamera_HorizontalTable[tMoveStep];
          cameraYpan = GetTruckCameraBobbingY(tTimerVertical);
          SetCameraPanning(cameraXpan, cameraYpan);
          yBox1 = GetTruckBoxYMovement(tTimerVertical + 30) * 4;
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_TOP,      gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX1_X_OFFSET - cameraXpan, BOX1_Y_OFFSET + yBox1);
          yBox2 = GetTruckBoxYMovement(tTimerVertical) * 2;
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_L, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX2_X_OFFSET - cameraXpan, BOX2_Y_OFFSET + yBox2);
          yBox3 = GetTruckBoxYMovement(tTimerVertical) * 4;
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_R, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX3_X_OFFSET - cameraXpan, BOX3_Y_OFFSET + yBox3);
      }
}

/** static void Task_Truck3(u8 taskId) */
export function Task_Truck3(taskId: any): any {
  let data: any = gTasks[taskId].data;
     let cameraXpan, cameraYpan;

     tTimerHorizontal++;

     if (tTimerHorizontal > 5)
     {
         tTimerHorizontal = 0;
         tMoveStep++;
     }

     if (tMoveStep == ARRAY_COUNT(sTruckCamera_HorizontalTable))
     {
         DestroyTask(taskId);
     }
     else
     {
         cameraXpan = sTruckCamera_HorizontalTable[tMoveStep];
         cameraYpan = 0;
         SetCameraPanning(cameraXpan, cameraYpan);
         SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_TOP,      gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX1_X_OFFSET - cameraXpan, BOX1_Y_OFFSET + cameraYpan);
         SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_L, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX2_X_OFFSET - cameraXpan, BOX2_Y_OFFSET + cameraYpan);
         SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_R, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX3_X_OFFSET - cameraXpan, BOX3_Y_OFFSET + cameraYpan);
     }
}

/** static void Task_HandleTruckSequence(u8 taskId) */
export function Task_HandleTruckSequence(taskId: any): any {
  let data: any = gTasks[taskId].data;

      switch (tState)
      {
      case 0:
          tTimer++;
          if (tTimer == 90)
          {
              SetCameraPanningCallback(NULL);
              tTimer = 0;
              tTaskId1 = CreateTask(Task_Truck1, 0xA);
              tState = 1;
              PlaySE(SE_TRUCK_MOVE);
          }
          break;
      case 1:
          tTimer++;
          if (tTimer == 150)
          {
              FadeInFromBlack();
              tTimer = 0;
              tState = 2;
          }
          break;
      case 2:
          tTimer++;
          if (!gPaletteFade.active && tTimer > 300)
          {
              tTimer = 0;
              DestroyTask(tTaskId1);
              tTaskId2 = CreateTask(Task_Truck2, 0xA);
              tState = 3;
              PlaySE(SE_TRUCK_STOP);
          }
          break;
      case 3:
          if (!gTasks[tTaskId2].isActive)
          {
               
              InstallCameraPanAheadCallback();
              tTimer = 0;
              tState = 4;
          }
          break;
      case 4:
          tTimer++;
          if (tTimer == 90)
          {
              PlaySE(SE_TRUCK_UNLOAD);
              tTimer = 0;
              tState = 5;
          }
          break;
      case 5:
          tTimer++;
          if (tTimer == 120)
          {
              MapGridSetMetatileIdAt(4 + MAP_OFFSET, 1 + MAP_OFFSET, METATILE_InsideOfTruck_ExitLight_Top);
              MapGridSetMetatileIdAt(4 + MAP_OFFSET, 2 + MAP_OFFSET, METATILE_InsideOfTruck_ExitLight_Mid);
              MapGridSetMetatileIdAt(4 + MAP_OFFSET, 3 + MAP_OFFSET, METATILE_InsideOfTruck_ExitLight_Bottom);
              DrawWholeMapView();
              PlaySE(SE_TRUCK_DOOR);
              DestroyTask(taskId);
              UnlockPlayerFieldControls();
          }
          break;
      }
}

/** void ExecuteTruckSequence(void) */
export function ExecuteTruckSequence(): any {
  MapGridSetMetatileIdAt(4 + MAP_OFFSET, 1 + MAP_OFFSET, METATILE_InsideOfTruck_DoorClosedFloor_Top);
      MapGridSetMetatileIdAt(4 + MAP_OFFSET, 2 + MAP_OFFSET, METATILE_InsideOfTruck_DoorClosedFloor_Mid);
      MapGridSetMetatileIdAt(4 + MAP_OFFSET, 3 + MAP_OFFSET, METATILE_InsideOfTruck_DoorClosedFloor_Bottom);
      DrawWholeMapView();
      LockPlayerFieldControls();
      CpuFastFill(0, gPlttBufferFaded, PLTT_SIZE);
      CreateTask(Task_HandleTruckSequence, 0xA);
}

/** void EndTruckSequence(u8 taskId) */
export function EndTruckSequence(taskId: any): any {
  if (!FuncIsActiveTask(Task_HandleTruckSequence))
      {
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_TOP,      gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX1_X_OFFSET, BOX1_Y_OFFSET);
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_L, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX2_X_OFFSET, BOX2_Y_OFFSET);
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_R, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX3_X_OFFSET, BOX3_Y_OFFSET);
      }
}

/** bool8 TrySetPortholeWarpDestination(void) */
export function TrySetPortholeWarpDestination(): any {
  let mapGroup, mapNum;
      let x, y;

      if (GetSSTidalLocation(mapGroup,mapNum,x,y) != SS_TIDAL_LOCATION_CURRENTS)
      {
          return FALSE;
      }
      else
      {
          SetWarpDestination(mapGroup, mapNum, WARP_ID_NONE, x, y);
          return TRUE;
      }
}

/** void Task_HandlePorthole(u8 taskId) */
export function Task_HandlePorthole(taskId: any): any {
  let data: any = gTasks[taskId].data;
      let cruiseState: any = GetVarPointer(VAR_SS_TIDAL_STATE);
      let location: any =gSaveBlock1Ptr.location;

      switch (data[0])
      {
      case INIT_PORTHOLE:  
          if (!gPaletteFade.active)
          {
              data[1] = 0;
              data[0] = EXECUTE_MOVEMENT;  
          }
          break;
      case IDLE_CHECK:
          if (JOY_NEW(A_BUTTON))
              data[1] = 1;
          if (!ScriptMovement_IsObjectMovementFinished(LOCALID_PLAYER, location.mapNum, location.mapGroup))
              return;
          if (CountSSTidalStep(1) == TRUE)
          {
              if (cruiseState == SS_TIDAL_DEPART_SLATEPORT)
                  cruiseState = SS_TIDAL_EXIT_CURRENTS_RIGHT;
              else
                  cruiseState = SS_TIDAL_EXIT_CURRENTS_LEFT;
              data[0] = EXIT_PORTHOLE;
              return;
          }
          data[0] = EXECUTE_MOVEMENT;
           
      case EXECUTE_MOVEMENT:
          if (data[1])
          {
              data[0] = EXIT_PORTHOLE;
              return;
          }

          if (cruiseState == SS_TIDAL_DEPART_SLATEPORT)
          {
              ScriptMovement_StartObjectMovementScript(LOCALID_PLAYER, location.mapNum, location.mapGroup, sSSTidalSailEastMovementScript);
              data[0] = IDLE_CHECK;
          }
          else
          {
              ScriptMovement_StartObjectMovementScript(LOCALID_PLAYER, location.mapNum, location.mapGroup, sSSTidalSailWestMovementScript);
              data[0] = IDLE_CHECK;
          }
          break;
      case EXIT_PORTHOLE:
          FlagClear(FLAG_DONT_TRANSITION_MUSIC);
          FlagClear(FLAG_HIDE_MAP_NAME_POPUP);
          SetWarpDestinationToDynamicWarp(0);
          DoDiveWarp();
          DestroyTask(taskId);
          break;
      }
}

/** static void ShowSSTidalWhileSailing(void) */
export function ShowSSTidalWhileSailing(): any {
  let spriteId: any = CreateObjectGraphicsSprite(OBJ_EVENT_GFX_SS_TIDAL, SpriteCallbackDummy, 112, 80, 0);

      gSprites[spriteId].coordOffsetEnabled = FALSE;

      if (VarGet(VAR_SS_TIDAL_STATE) == SS_TIDAL_DEPART_SLATEPORT)
          StartSpriteAnim(gSprites[spriteId], GetFaceDirectionAnimNum(DIR_EAST));
      else
          StartSpriteAnim(gSprites[spriteId], GetFaceDirectionAnimNum(DIR_WEST));
}

/** void FieldCB_ShowPortholeView(void) */
export function FieldCB_ShowPortholeView(): any {
  ShowSSTidalWhileSailing();
      gObjectEvents[gPlayerAvatar.objectEventId].invisible = TRUE;
      FadeInFromBlack();
      CreateTask(Task_HandlePorthole, 80);
      LockPlayerFieldControls();
}

/** void LookThroughPorthole(void) */
export function LookThroughPorthole(): any {
  FlagSet(FLAG_SYS_CRUISE_MODE);
      FlagSet(FLAG_DONT_TRANSITION_MUSIC);
      FlagSet(FLAG_HIDE_MAP_NAME_POPUP);
      SetDynamicWarp(0, gSaveBlock1Ptr.location.mapGroup, gSaveBlock1Ptr.location.mapNum, WARP_ID_NONE);
      TrySetPortholeWarpDestination();
      DoPortholeWarp();
}

// ─── callsTo manifest (= 36 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ARRAY_COUNT","CountSSTidalStep","CpuFastFill","CreateObjectGraphicsSprite","CreateTask","DestroyTask","DoDiveWarp","DoPortholeWarp","DrawWholeMapView","FadeInFromBlack","FlagClear","FlagSet","FuncIsActiveTask","GetFaceDirectionAnimNum","GetSSTidalLocation","GetTruckBoxYMovement","GetTruckCameraBobbingY","GetVarPointer","InstallCameraPanAheadCallback","JOY_NEW","LockPlayerFieldControls","MapGridSetMetatileIdAt","PlaySE","ScriptMovement_IsObjectMovementFinished","ScriptMovement_StartObjectMovementScript","SetCameraPanning","SetCameraPanningCallback","SetDynamicWarp","SetObjectEventSpritePosByLocalIdAndMap","SetWarpDestination","SetWarpDestinationToDynamicWarp","ShowSSTidalWhileSailing","StartSpriteAnim","TrySetPortholeWarpDestination","UnlockPlayerFieldControls","VarGet"];
