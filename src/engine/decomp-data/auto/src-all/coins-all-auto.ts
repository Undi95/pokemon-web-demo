// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/coins.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 7 functions extracted from src/coins.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  AddTextPrinterParameterized, AddWindow, ClearStdWindowAndFrame, ConvertIntToDecimalStringN,
  DrawStdFrameWithCustomTileAndPalette, FillWindowPixelBuffer, GetStringRightAlignXOffset, PIXEL_FILL,
  PutWindowTilemap, RemoveWindow, SetWindowTemplateFields, StringExpandPlaceholders,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** void PrintCoinsString(u32 coinAmount) */
export function PrintCoinsString(coinAmount: any): any {
  let xAlign: any = null;

      ConvertIntToDecimalStringN(gStringVar1, coinAmount, STR_CONV_MODE_RIGHT_ALIGN, MAX_COIN_DIGITS);
      StringExpandPlaceholders(gStringVar4, gText_Coins);

      xAlign = GetStringRightAlignXOffset(FONT_NARROW, gStringVar4, 56);
      AddTextPrinterParameterized(sCoinsWindowId, FONT_NARROW, gStringVar4, xAlign, 1, 0, NULL);
}

/** void ShowCoinsWindow(u32 coinAmount, u8 x, u8 y) */
export function ShowCoinsWindow(coinAmount: any, x: any, y: any): any {
  let template: any = null;
      SetWindowTemplateFields(template, 0, x, y, 7, 2, 0xF, _0x141);  
      sCoinsWindowId = AddWindow(template);
      FillWindowPixelBuffer(sCoinsWindowId, PIXEL_FILL(0));
      PutWindowTilemap(sCoinsWindowId);
      DrawStdFrameWithCustomTileAndPalette(sCoinsWindowId, FALSE, _0x214, 0xE);
      PrintCoinsString(coinAmount);
}

/** void HideCoinsWindow(void) */
export function HideCoinsWindow(): any {
  ClearStdWindowAndFrame(sCoinsWindowId, TRUE);
      RemoveWindow(sCoinsWindowId);
}

/** u16 GetCoins(void) */
export function GetCoins(): any {
  return gSaveBlock1Ptr.coins ^ gSaveBlock2Ptr.encryptionKey;
}

/** void SetCoins(u16 coinAmount) */
export function SetCoins(coinAmount: any): any {
  gSaveBlock1Ptr.coins = coinAmount ^ gSaveBlock2Ptr.encryptionKey;
}

/** bool8 AddCoins(u16 toAdd) */
export function AddCoins(toAdd: any): any {
  let newAmount: any = null;
      let ownedCoins: any = GetCoins();
      if (ownedCoins >= MAX_COINS)
          return FALSE;
       
      if (ownedCoins > ownedCoins + toAdd)
      {
          newAmount = MAX_COINS;
      }
      else
      {
          ownedCoins += toAdd;
          if (ownedCoins > MAX_COINS)
              ownedCoins = MAX_COINS;
          newAmount = ownedCoins;
      }
      SetCoins(newAmount);
      return TRUE;
}

/** bool8 RemoveCoins(u16 toSub) */
export function RemoveCoins(toSub: any): any {
  let ownedCoins: any = GetCoins();
      if (ownedCoins >= toSub)
      {
          SetCoins(ownedCoins - toSub);
          return TRUE;
      }
      return FALSE;
}

// ─── callsTo manifest (= 15 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["AddTextPrinterParameterized","AddWindow","ClearStdWindowAndFrame","ConvertIntToDecimalStringN","DrawStdFrameWithCustomTileAndPalette","FillWindowPixelBuffer","GetCoins","GetStringRightAlignXOffset","PIXEL_FILL","PrintCoinsString","PutWindowTilemap","RemoveWindow","SetCoins","SetWindowTemplateFields","StringExpandPlaceholders"];
