// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/item_icon.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 5 functions extracted from src/item_icon.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
import * as _bridge from '../../../decomp-bridge';
const {
  Alloc, AllocItemIconTemporaryBuffers, AllocZeroed, CopyItemIconPicTo4x4Buffer,
  CpuCopy16, CreateSprite, Free, FreeItemIconTemporaryBuffers,
  GetItemIconPicOrPalette, LZDecompressWram, LoadCompressedSpritePalette, LoadSpriteSheet,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** bool8 AllocItemIconTemporaryBuffers(void) */
export function AllocItemIconTemporaryBuffers(): any {
  gItemIconDecompressionBuffer = Alloc(_0x120);
      if (gItemIconDecompressionBuffer == NULL)
          return FALSE;

      gItemIcon4x4Buffer = AllocZeroed(_0x200);
      if (gItemIcon4x4Buffer == NULL)
      {
          Free(gItemIconDecompressionBuffer);
          return FALSE;
      }

      return TRUE;
}

/** void FreeItemIconTemporaryBuffers(void) */
export function FreeItemIconTemporaryBuffers(): any {
  Free(gItemIconDecompressionBuffer);
      Free(gItemIcon4x4Buffer);
}

/** void CopyItemIconPicTo4x4Buffer(const void *src, void *dest) */
export function CopyItemIconPicTo4x4Buffer(src: any, dest: any): any {
  let i: any = null;

      for (i = 0; i < 3; i++)
          CpuCopy16(src + i * 96, dest + i * 128, _0x60);
}

/** u8 AddItemIconSprite(u16 tilesTag, u16 paletteTag, u16 itemId) */
export function AddItemIconSprite(tilesTag: any, paletteTag: any, itemId: any): any {
  if (!AllocItemIconTemporaryBuffers())
      {
          return MAX_SPRITES;
      }
      else
      {
          let spriteId: any = null;
          let spriteSheet: any = null;
          let spritePalette: any = null;
          let spriteTemplate: any = null;

          LZDecompressWram(GetItemIconPicOrPalette(itemId, 0), gItemIconDecompressionBuffer);
          CopyItemIconPicTo4x4Buffer(gItemIconDecompressionBuffer, gItemIcon4x4Buffer);
          spriteSheet.data = gItemIcon4x4Buffer;
          spriteSheet.size = _0x200;
          spriteSheet.tag = tilesTag;
          LoadSpriteSheet(spriteSheet);

          spritePalette.data = GetItemIconPicOrPalette(itemId, 1);
          spritePalette.tag = paletteTag;
          LoadCompressedSpritePalette(spritePalette);

          spriteTemplate = Alloc(0);
          CpuCopy16(gItemIconSpriteTemplate, spriteTemplate, 0);
          spriteTemplate.tileTag = tilesTag;
          spriteTemplate.paletteTag = paletteTag;
          spriteId = CreateSprite(spriteTemplate, 0, 0, 0);

          FreeItemIconTemporaryBuffers();
          Free(spriteTemplate);

          return spriteId;
      }
}

/** u8 AddCustomItemIconSprite(const struct SpriteTemplate *customSpriteTemplate, u16 tilesTag, u16 paletteTag, u16 itemId) */
export function AddCustomItemIconSprite(customSpriteTemplate: any, tilesTag: any, paletteTag: any, itemId: any): any {
  if (!AllocItemIconTemporaryBuffers())
      {
          return MAX_SPRITES;
      }
      else
      {
          let spriteId: any = null;
          let spriteSheet: any = null;
          let spritePalette: any = null;
          let spriteTemplate: any = null;

          LZDecompressWram(GetItemIconPicOrPalette(itemId, 0), gItemIconDecompressionBuffer);
          CopyItemIconPicTo4x4Buffer(gItemIconDecompressionBuffer, gItemIcon4x4Buffer);
          spriteSheet.data = gItemIcon4x4Buffer;
          spriteSheet.size = _0x200;
          spriteSheet.tag = tilesTag;
          LoadSpriteSheet(spriteSheet);

          spritePalette.data = GetItemIconPicOrPalette(itemId, 1);
          spritePalette.tag = paletteTag;
          LoadCompressedSpritePalette(spritePalette);

          spriteTemplate = Alloc(0);
          CpuCopy16(customSpriteTemplate, spriteTemplate, 0);
          spriteTemplate.tileTag = tilesTag;
          spriteTemplate.paletteTag = paletteTag;
          spriteId = CreateSprite(spriteTemplate, 0, 0, 0);

          FreeItemIconTemporaryBuffers();
          Free(spriteTemplate);

          return spriteId;
      }
}

// ─── callsTo manifest (= 12 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["Alloc","AllocItemIconTemporaryBuffers","AllocZeroed","CopyItemIconPicTo4x4Buffer","CpuCopy16","CreateSprite","Free","FreeItemIconTemporaryBuffers","GetItemIconPicOrPalette","LZDecompressWram","LoadCompressedSpritePalette","LoadSpriteSheet"];
