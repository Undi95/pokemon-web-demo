// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/clock.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 6 functions extracted from src/clock.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

// ─── BRIDGE IMPORT (auto-injected by inject-bridge-imports.mjs) ───
// Pull tous les callees ce module fait depuis le bridge unifié.
// Si un helper est bridgé : binding actif. Sinon : undefined → throw au call.
// Names already defined in this file via 'export function' are EXCLUDED
// to avoid "already declared" esbuild errors.
import * as _bridge from '../../../decomp-bridge';
const {
  BerryTreeTimeUpdate, CalcTimeDifference, ClearDailyFlags, FlagGet,
  FlagSet, GetVarPointer, InPokemonCenter, RtcCalcLocalTime,
  SetMainCallback2, SetRandomLotteryNumber, SetShoalItemFlag, UpdateBirchState,
  UpdateDewfordTrendPerDay, UpdateFrontierGambler, UpdateFrontierManiac, UpdateMirageRnd,
  UpdatePartyPokerusTime, UpdateTVShowsPerDay, UpdateWeatherPerDay, VarSet,  // 4-per-line for readability
} = _bridge;
// ─── END BRIDGE IMPORT ───
/** static void InitTimeBasedEvents(void) */
export function InitTimeBasedEvents(): any {
  FlagSet(FLAG_SYS_CLOCK_SET);
      RtcCalcLocalTime();
      gSaveBlock2Ptr.lastBerryTreeUpdate = gLocalTime;
      VarSet(VAR_DAYS, gLocalTime.days);
}

/** void DoTimeBasedEvents(void) */
export function DoTimeBasedEvents(): any {
  if (FlagGet(FLAG_SYS_CLOCK_SET) && !InPokemonCenter())
      {
          RtcCalcLocalTime();
          UpdatePerDay(gLocalTime);
          UpdatePerMinute(gLocalTime);
      }
}

/** static void UpdatePerDay(struct Time *localTime) */
export function UpdatePerDay(localTime: any): any {
  let days: any = GetVarPointer(VAR_DAYS);
      let daysSince: any = null;

      if (days != localTime.days && days <= localTime.days)
      {
          daysSince = localTime.days - days;
          ClearDailyFlags();
          UpdateDewfordTrendPerDay(daysSince);
          UpdateTVShowsPerDay(daysSince);
          UpdateWeatherPerDay(daysSince);
          UpdatePartyPokerusTime(daysSince);
          UpdateMirageRnd(daysSince);
          UpdateBirchState(daysSince);
          UpdateFrontierManiac(daysSince);
          UpdateFrontierGambler(daysSince);
          SetShoalItemFlag(daysSince);
          SetRandomLotteryNumber(daysSince);
          days = localTime.days;
      }
}

/** static void UpdatePerMinute(struct Time *localTime) */
export function UpdatePerMinute(localTime: any): any {
  let difference: any = null;
      let minutes: any = null;

      CalcTimeDifference(difference,gSaveBlock2Ptr.lastBerryTreeUpdate, localTime);
      minutes = 24 * 60 * difference.days + 60 * difference.hours + difference.minutes;
      if (minutes != 0)
      {
          if (minutes >= 0)
          {
              BerryTreeTimeUpdate(minutes);
              gSaveBlock2Ptr.lastBerryTreeUpdate = localTime;
          }
      }
}

/** static void ReturnFromStartWallClock(void) */
export function ReturnFromStartWallClock(): any {
  InitTimeBasedEvents();
      SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic);
}

/** void StartWallClock(void) */
export function StartWallClock(): any {
  SetMainCallback2(CB2_StartWallClock);
      gMain.savedCallback = ReturnFromStartWallClock;
}

// ─── callsTo manifest (= 23 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["BerryTreeTimeUpdate","CalcTimeDifference","ClearDailyFlags","FlagGet","FlagSet","GetVarPointer","InPokemonCenter","InitTimeBasedEvents","RtcCalcLocalTime","SetMainCallback2","SetRandomLotteryNumber","SetShoalItemFlag","UpdateBirchState","UpdateDewfordTrendPerDay","UpdateFrontierGambler","UpdateFrontierManiac","UpdateMirageRnd","UpdatePartyPokerusTime","UpdatePerDay","UpdatePerMinute","UpdateTVShowsPerDay","UpdateWeatherPerDay","VarSet"];
