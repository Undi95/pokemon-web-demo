// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/util.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 11 functions extracted from src/util.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** u8 CreateInvisibleSpriteWithCallback(void (*callback)(struct Sprite *)) */
export function CreateInvisibleSpriteWithCallback(callback: any): any {
  let sprite: any = CreateSprite(sInvisibleSpriteTemplate, DISPLAY_WIDTH + 8, DISPLAY_HEIGHT + 8, 14);
      gSprites[sprite].invisible = TRUE;
      gSprites[sprite].callback = callback;
      return sprite;
}

/** void StoreWordInTwoHalfwords(u16 *h, u32 w) */
export function StoreWordInTwoHalfwords(h: any, w: any): any {
  h[0] = (w);
      h[1] = (w >> 16);
}

/** void LoadWordFromTwoHalfwords(u16 *h, u32 *w) */
export function LoadWordFromTwoHalfwords(h: any, w: any): any {
  w = h[0] | h[1] << 16;
}

/** void SetBgAffineStruct(struct BgAffineSrcData *src, u32 texX, u32 texY, s16 scrX, s16 scrY, s16 sx, s16 sy, u16 alpha) */
export function SetBgAffineStruct(src: any, texX: any, texY: any, scrX: any, scrY: any, sx: any, sy: any, alpha: any): any {
  src.texX = texX;
      src.texY = texY;
      src.scrX = scrX;
      src.scrY = scrY;
      src.sx = sx;
      src.sy = sy;
      src.alpha = alpha;
}

/** void DoBgAffineSet(struct BgAffineDstData *dest, u32 texX, u32 texY, s16 scrX, s16 scrY, s16 sx, s16 sy, u16 alpha) */
export function DoBgAffineSet(dest: any, texX: any, texY: any, scrX: any, scrY: any, sx: any, sy: any, alpha: any): any {
  let src: any = null;

      SetBgAffineStruct(src, texX, texY, scrX, scrY, sx, sy, alpha);
      BgAffineSet(src, dest, 1);
}

/** void CopySpriteTiles(u8 shape, u8 size, u8 *tiles, u16 *tilemap, u8 *output) */
export function CopySpriteTiles(shape: any, size: any, tiles: any, tilemap: any, output: any): any {
  let x, y;
      let i, j;
      const xflip: any[] = [];
      let h: any = sSpriteDimensions[shape][size][1];
      let w: any = sSpriteDimensions[shape][size][0];

      for (y = 0; y < h; y++)
      {
          for (x = 0; x < w; x++)
          {
              let tile: any = (tilemap & 0x3ff) * 32;

              if ((tilemap & 0xc00) == 0)
              {
                  CpuCopy32(tiles + tile, output, 32);
              }
              else if ((tilemap & 0xc00) == 0x800)   
              {
                  for (i = 0; i < 8; i++)
                      CpuCopy32(tiles + (tile + (7 - i) * 4), output + i * 4, 4);
              }
              else   
              {
                  for (i = 0; i < 8; i++)
                  {
                      for (j = 0; j < 4; j++)
                      {
                          let i2: any = i * 4;
                          xflip[i2 + (3-j)] = (tiles[tile + i2 + j] & 0xf) << 4;
                          xflip[i2 + (3-j)] |= tiles[tile + i2 + j] >> 4;
                      }
                  }
                  if (tilemap & 0x800)   
                  {
                      for (i = 0; i < 8; i++)
                          CpuCopy32(xflip + (7 - i) * 4, output + i * 4, 4);
                  }
                  else
                  {
                      CpuCopy32(xflip, output, 32);
                  }
              }
              tilemap++;
              output += 32;
          }
          tilemap += (32 - w);
      }
}

/** int CountTrailingZeroBits(u32 value) */
export function CountTrailingZeroBits(value: any): any {
  let i: any = null;

      for (i = 0; i < 32; i++)
      {
          if ((value & 1) == 0)
              value >>= 1;
          else
              return i;
      }
      return 0;
}

/** u16 CalcCRC16(const u8 *data, s32 length) */
export function CalcCRC16(data: any, length: any): any {
  let i, j;
      let crc: any = 0x1121;

      for (i = 0; i < length; i++)
      {
          crc ^= data[i];
          for (j = 0; j < 8; j++)
          {
              if (crc & 1)
                  crc = (crc >> 1) ^ 0x8408;
              else
                  crc >>= 1;
          }
      }
      return ~crc;
}

/** u16 CalcCRC16WithTable(const u8 *data, u32 length) */
export function CalcCRC16WithTable(data: any, length: any): any {
  let i: any = null;
      let crc: any = 0x1121;
      let byte: any = null;

      for (i = 0; i < length; i++)
      {
          byte = crc >> 8;
          crc ^= data[i];
          crc = byte ^ sCrc16Table[crc];
      }
      return ~crc;
}

/** u32 CalcByteArraySum(const u8 *data, u32 length) */
export function CalcByteArraySum(data: any, length: any): any {
  let sum, i;
      for (sum = 0, i = 0; i < length; i++)
          sum += data[i];
      return sum;
}

/** void BlendPalette(u16 palOffset, u16 numEntries, u8 coeff, u16 blendColor) */
export function BlendPalette(palOffset: any, numEntries: any, coeff: any, blendColor: any): any {
  let i: any = null;
      for (i = 0; i < numEntries; i++)
      {
          let index: any = i + palOffset;
          let data1: any =gPlttBufferUnfaded[index];
          let r: any = data1.r;
          let g: any = data1.g;
          let b: any = data1.b;
          let data2: any =blendColor;
          gPlttBufferFaded[index] = RGB(r + (((data2.r - r) * coeff) >> 4),
                                        g + (((data2.g - g) * coeff) >> 4),
                                        b + (((data2.b - b) * coeff) >> 4));
      }
}

// ─── callsTo manifest (= 5 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["ALIGNED","BgAffineSet","CpuCopy32","CreateSprite","SetBgAffineStruct"];
