// AUTO-GENERATED by scripts/transpile-decomp-all.mjs
// Source : src/pokemon_size_record.c
// Generated : 2026-05-09
// DO NOT EDIT — re-run `node scripts/transpile-decomp-all.mjs` to refresh.
//
// Total : 12 functions extracted from src/pokemon_size_record.c.
//
// IMPORTANT :
//   - Tous les imports manquants : marqués @ts-nocheck (= pas de typecheck).
//   - Les bodies référencent des helpers (= FaceDirection, InitMovementNormal, etc.)
//     qui doivent être bridged vers nos TS impls. Cf. memory/audit pour la liste.
//   - Pour activer une fonction, la importer depuis runtime engine + bridge
//     ses callsTo manquants.

/* eslint-disable */
// @ts-nocheck

/** static u32 GetMonSizeHash(struct Pokemon *pkmn) */
export function GetMonSizeHash(pkmn: any): any {
  let personality: any = GetMonData(pkmn, MON_DATA_PERSONALITY);
      let hpIV: any = GetMonData(pkmn, MON_DATA_HP_IV) & 0xF;
      let attackIV: any = GetMonData(pkmn, MON_DATA_ATK_IV) & 0xF;
      let defenseIV: any = GetMonData(pkmn, MON_DATA_DEF_IV) & 0xF;
      let speedIV: any = GetMonData(pkmn, MON_DATA_SPEED_IV) & 0xF;
      let spAtkIV: any = GetMonData(pkmn, MON_DATA_SPATK_IV) & 0xF;
      let spDefIV: any = GetMonData(pkmn, MON_DATA_SPDEF_IV) & 0xF;
      let hibyte: any = ((attackIV ^ defenseIV) * hpIV) ^ (personality & 0xFF);
      let lobyte: any = ((spAtkIV ^ spDefIV) * speedIV) ^ (personality >> 8);

      return (hibyte << 8) + lobyte;
}

/** static u8 TranslateBigMonSizeTableIndex(u16 a) */
export function TranslateBigMonSizeTableIndex(a: any): any {
  let i: any = null;

      for (i = 1; i < 15; i++)
      {
          if (a < sBigMonSizeTable[i].unk4)
              return i - 1;
      }
      return i;
}

/** static u32 GetMonSize(u16 species, u16 b) */
export function GetMonSize(species: any, b: any): any {
  let unk2: any = null;
      let unk4: any = null;
      let unk0: any = null;
      let height: any = null;
      let _var: any = null;

      height = GetPokedexHeightWeight(SpeciesToNationalPokedexNum(species), 0);
      _var = TranslateBigMonSizeTableIndex(b);
      unk0 = sBigMonSizeTable[_var].unk0;
      unk2 = sBigMonSizeTable[_var].unk2;
      unk4 = sBigMonSizeTable[_var].unk4;
      unk0 += (b - unk4) / unk2;
      return height * unk0 / 10;
}

/** static void FormatMonSizeRecord(u8 *string, u32 size) */
export function FormatMonSizeRecord(string: any, size: any): any {
       
      size = (size * 10) / (CM_PER_INCH * 10);

      string = ConvertIntToDecimalStringN(string, size / 10, STR_CONV_MODE_LEFT_ALIGN, 8);
      string = StringAppend(string, gText_DecimalPoint);
      ConvertIntToDecimalStringN(string, size % 10, STR_CONV_MODE_LEFT_ALIGN, 1);
}

/** static u8 CompareMonSize(u16 species, u16 *sizeRecord) */
export function CompareMonSize(species: any, sizeRecord: any): any {
  if (gSpecialVar_Result == (0xFF))
      {
          return COMPARE_SIZE_NONE;
      }
      else
      {
          let pkmn: any =gPlayerParty[gSpecialVar_Result];

          if (GetMonData(pkmn, MON_DATA_IS_EGG) == TRUE || GetMonData(pkmn, MON_DATA_SPECIES) != species)
          {
              return COMPARE_SIZE_INCORRECT_SPECIES;
          }
          else
          {
              let oldSize: any = null;
              let newSize: any = null;
              let sizeParams: any = null;

              MEM_WRITE((sizeParams), GetMonSizeHash(pkmn));
              newSize = GetMonSize(species, sizeParams);
              oldSize = GetMonSize(species, sizeRecord);
              FormatMonSizeRecord(gStringVar2, newSize);
              if (newSize <= oldSize)
              {
                  return COMPARE_SIZE_SMALLER;
              }
              else
              {
                  sizeRecord = sizeParams;
                  return COMPARE_SIZE_LARGER;
              }
          }
      }
}

/** static void GetMonSizeRecordInfo(u16 species, u16 *sizeRecord) */
export function GetMonSizeRecordInfo(species: any, sizeRecord: any): any {
  let size: any = GetMonSize(species, sizeRecord);

      FormatMonSizeRecord(gStringVar3, size);
      StringCopy(gStringVar1, gSpeciesNames[species]);
      if (sizeRecord == (0x8000))
          StringCopy(gStringVar2, gText_Marco);
      else
          StringCopy(gStringVar2, gSaveBlock2Ptr.playerName);
}

/** void InitSeedotSizeRecord(void) */
export function InitSeedotSizeRecord(): any {
  VarSet((0x4047), (0x8000));
}

/** void GetSeedotSizeRecordInfo(void) */
export function GetSeedotSizeRecordInfo(): any {
  let sizeRecord: any = GetVarPointer((0x4047));

      GetMonSizeRecordInfo((298), sizeRecord);
}

/** void CompareSeedotSize(void) */
export function CompareSeedotSize(): any {
  let sizeRecord: any = GetVarPointer((0x4047));

      gSpecialVar_Result = CompareMonSize((298), sizeRecord);
}

/** void InitLotadSizeRecord(void) */
export function InitLotadSizeRecord(): any {
  VarSet((0x404F), (0x8000));
}

/** void GetLotadSizeRecordInfo(void) */
export function GetLotadSizeRecordInfo(): any {
  let sizeRecord: any = GetVarPointer((0x404F));

      GetMonSizeRecordInfo((295), sizeRecord);
}

/** void CompareLotadSize(void) */
export function CompareLotadSize(): any {
  let sizeRecord: any = GetVarPointer((0x404F));

      gSpecialVar_Result = CompareMonSize((295), sizeRecord);
}

// ─── callsTo manifest (= 14 unique callees) ───────────────────────
// (Sorted alphabetically. Used for helper bridge detection.)
export const __callsTo__: ReadonlyArray<string> = ["CompareMonSize","ConvertIntToDecimalStringN","FormatMonSizeRecord","GetMonData","GetMonSize","GetMonSizeHash","GetMonSizeRecordInfo","GetPokedexHeightWeight","GetVarPointer","SpeciesToNationalPokedexNum","StringAppend","StringCopy","TranslateBigMonSizeTableIndex","VarSet"];
