// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/mirage_tower.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const FOSSIL_DISINTEGRATE_LENGTH = 256;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const FREE_AND_SET_NULL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const SWAP: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: mirage_tower.c → SpriteCB_CeilingCrumble */
export const SpriteCB_CeilingCrumble: SpriteCallback = (sprite, rt) => {
  sprite.data[1] += 2;
      sprite.y2 = sprite.data[1] / 2;
      if ((sprite.y + sprite.y2) >  sCeilingCrumblePositions[sprite.data[0]][2])
      {
          rt.DestroySprite(sprite.spriteId);
          IncrementCeilingCrumbleFinishedCount();
      }
};

/** Source: mirage_tower.c → SpriteCB_FallingFossil */
export const SpriteCB_FallingFossil: SpriteCallback = (sprite, rt) => {
  if (sFallingFossil.disintegrateIdx >= FOSSIL_DISINTEGRATE_LENGTH)
      {
           
          sprite.callback = SpriteCallbackDummy;
      }
      else if (sprite.y >= 96)
      {
           
          let i = 0;
          for (i = 0; i < 2; i++)
              UpdateDisintegrationEffect(sFallingFossil.frameImageTiles, sFallingFossil.disintegrateRand[sFallingFossil.disintegrateIdx++], 0, 16, 0);

          rt.StartSpriteAnim(sprite.spriteId, 0);
      }
      else
      {
           
          sprite.y++;
      }
};

/** Source: mirage_tower.c → Task_FossilFallAndSink */
export const Task_FossilFallAndSink: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;
      let buffer = 0;

      switch (task.data[0])
      {
      case 1:
          sFallingFossil = AllocZeroed(((sFallingFossil)?.length ?? 32));
          sFallingFossil.frameImageTiles = AllocZeroed(((sFossil_Gfx)?.length ?? 32));
          sFallingFossil.frameImage = AllocZeroed(/* sizeof(sFallingFossil.frameImage) */ 0);
          sFallingFossil.disintegrateRand = AllocZeroed(FOSSIL_DISINTEGRATE_LENGTH * sizeof);
          sFallingFossil.disintegrateIdx = 0;
          break;
      case 2:
          buffer = sFallingFossil.frameImageTiles;
          for (i = 0; i < ((sFossil_Gfx)?.length ?? 32); i++, buffer++)
              buffer = sFossil_Gfx[i];
          break;
      case 3:
          sFallingFossil.frameImage.data = sFallingFossil.frameImageTiles;
          sFallingFossil.frameImage.size = ((sFossil_Gfx)?.length ?? 32);
          break;
      case 4:
          {
              let fossilTemplate: any = sSpriteTemplate_FallingFossil;
              fossilTemplate.images = sFallingFossil.frameImage;
              sFallingFossil.spriteId = CreateSprite(fossilTemplate, 128, -16, 1);
              _gs(rt, sFallingFossil.spriteId).centerToCornerVecX = 0;
              _gs(rt, sFallingFossil.spriteId).data[0] = _gs(rt, sFallingFossil.spriteId).x;
              _gs(rt, sFallingFossil.spriteId).data[1] = 1;
          }
      case 5:
           
          for (i = 0; i < FOSSIL_DISINTEGRATE_LENGTH; i++)
              sFallingFossil.disintegrateRand[i] = i;
          break;
      case 6:
           
          for (i = 0; i < FOSSIL_DISINTEGRATE_LENGTH * sizeof; i++)
          {
              let rand1, rand2, temp;
              rand1 = Math.floor(Math.random() * 0x10000) % FOSSIL_DISINTEGRATE_LENGTH;
              rand2 = Math.floor(Math.random() * 0x10000) % FOSSIL_DISINTEGRATE_LENGTH;
              SWAP(sFallingFossil.disintegrateRand[rand2], sFallingFossil.disintegrateRand[rand1], temp);
          }
          rt.setSpriteCallback(sFallingFossil.spriteId, SpriteCB_FallingFossil);
          break;
      case 7:
           
          if (_gs(rt, sFallingFossil.spriteId).callback != SpriteCallbackDummy)
              return;
          rt.DestroySprite(sFallingFossil.spriteId);
          FREE_AND_SET_NULL(sFallingFossil.disintegrateRand);;
          FREE_AND_SET_NULL(sFallingFossil.frameImage);
          FREE_AND_SET_NULL(sFallingFossil.frameImageTiles);
          FREE_AND_SET_NULL(sFallingFossil);
          break;
      case 8:
          ScriptContext_Enable();
          break;
      }
      task.data[0]++;
};
