// AUTO-GENERATED by scripts/extract-mon-anim-tables.mjs
//
// Stub initial : empty arrays. Runs `node scripts/extract-mon-anim-tables.mjs`
// pour populate avec les valeurs extracted depuis ton checkout local
// pokeemeraude (= D:/Projet 1/decomps/pokeemeraude/src/pokemon.c).
//
// Le framework consumer (pokemon-anim-funcs.ts) fait fallback graceful sur
// les valeurs minimales hardcoded si les arrays sont vides.

/** Raw species → ANIM_* mapping extracted from sMonFrontAnimIdsTable.
 *  String form (= SPECIES_X / ANIM_Y identifiers) ; resolved au run-time
 *  via imports dans pokemon-anim-funcs.ts. */
export const RAW_MON_FRONT_ANIM_IDS: ReadonlyArray<readonly [string, string]> = [];

/** Raw species → delay frames mapping. */
export const RAW_MON_ANIM_DELAYS: ReadonlyArray<readonly [string, string]> = [];

/** Raw species → boolean (= has 2-frame anim_front sheet). */
export const RAW_MON_HAS_TWO_FRAMES: ReadonlyArray<readonly [string, string]> = [];
