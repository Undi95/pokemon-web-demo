// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/wallclock.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  ClearWindowTilemap,
  CreateYesNoMenu,
  FreeAllWindowBuffers,
  JOY_HELD,
  JOY_NEW,
  LZ77UnCompVram,
  Menu_ProcessInputNoWrapClearOnChoose,
  PutWindowTilemap,
  UpdatePaletteFade,
  m4aSongNumStart,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const DPAD_LEFT = 32;
const DPAD_RIGHT = 16;
const FONT_NORMAL = 1;
const MENU_B_PRESSED = -1;
const MOVE_BACKWARD = 1;
const MOVE_FORWARD = 2;
const MOVE_NONE = 0;
const PERIOD_AM = 0;
const SE_SELECT = 5;
const WIN_BUTTON_LABEL = 1;
const WIN_MSG = 2;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: wallclock.c → SpriteCB_MinuteHand */
export const SpriteCB_MinuteHand: SpriteCallback = (sprite, rt) => {
  let angle = _gt(rt, sprite.data[0]).data[0];
      let sin = Sin2(angle) / 16;
      let cos = Cos2(angle) / 16;
      let x, y;

      SetOamMatrix(rt.gba, 0, cos, sin, -sin, cos);
      x = sClockHandCoords[angle][0];
      y = sClockHandCoords[angle][1];

      if (x > 128)
          x |= 0xff00;
      if (y > 128)
          y |= 0xff00;

      sprite.x2 = x;
      sprite.y2 = y;
};

/** Source: wallclock.c → SpriteCB_HourHand */
export const SpriteCB_HourHand: SpriteCallback = (sprite, rt) => {
  let angle = _gt(rt, sprite.data[0]).data[1];
      let sin = Sin2(angle) / 16;
      let cos = Cos2(angle) / 16;
      let x, y;

      SetOamMatrix(rt.gba, 1, cos, sin, -sin, cos);
      x = sClockHandCoords[angle][0];
      y = sClockHandCoords[angle][1];

      if (x > 128)
          x |= 0xff00;
      if (y > 128)
          y |= 0xff00;

      sprite.x2 = x;
      sprite.y2 = y;
};

/** Source: wallclock.c → SpriteCB_PMIndicator */
export const SpriteCB_PMIndicator: SpriteCallback = (sprite, rt) => {
  if (_gt(rt, sprite.data[0]).data[5] != PERIOD_AM)
      {
          if (sprite.data[1] >= 60 && sprite.data[1] < 90)
              sprite.data[1] += 5;
          if (sprite.data[1] < 60)
              sprite.data[1]++;
      }
      else
      {
          if (sprite.data[1] >= 46 && sprite.data[1] < 76)
              sprite.data[1] -= 5;
          if (sprite.data[1] > 75)
              sprite.data[1]--;
      }
      sprite.x2 = Cos2(sprite.data[1]) * 30 / 0x1000;
      sprite.y2 = Sin2(sprite.data[1]) * 30 / 0x1000;
};

/** Source: wallclock.c → SpriteCB_AMIndicator */
export const SpriteCB_AMIndicator: SpriteCallback = (sprite, rt) => {
  if (_gt(rt, sprite.data[0]).data[5] != PERIOD_AM)
      {
          if (sprite.data[1] >= 105 && sprite.data[1] < 135)
              sprite.data[1] += 5;
          if (sprite.data[1] < 105)
              sprite.data[1]++;
      }
      else
      {
          if (sprite.data[1] >= 91 && sprite.data[1] < 121)
              sprite.data[1] -= 5;
          if (sprite.data[1] > 120)
              sprite.data[1]--;
      }
      sprite.x2 = Cos2(sprite.data[1]) * 30 / 0x1000;
      sprite.y2 = Sin2(sprite.data[1]) * 30 / 0x1000;
};

/** Source: wallclock.c → Task_SetClock_WaitFadeIn */
export const Task_SetClock_WaitFadeIn: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          task.func = (t) => Task_SetClock_HandleInput(t, rt);
      }
};

/** Source: wallclock.c → Task_SetClock_HandleInput */
export const Task_SetClock_HandleInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (task.data[0] % 6)
      {
          task.data[0] = CalcNewMinHandAngle(task.data[0], task.data[4], task.data[6]);
      }
      else
      {
          task.data[0] = task.data[3] * 6;
          task.data[1] = (task.data[2] % 12) * 30 + (task.data[3] / 10) * 5;
          if (JOY_NEW(A_BUTTON))
          {
              task.func = (t) => Task_SetClock_AskConfirm(t, rt);
          }
          else
          {
              task.data[4] = MOVE_NONE;

              if (JOY_HELD(DPAD_LEFT))
                  task.data[4] = MOVE_BACKWARD;

              if (JOY_HELD(DPAD_RIGHT))
                  task.data[4] = MOVE_FORWARD;

              if (task.data[4] != MOVE_NONE)
              {
                  if (task.data[6] < 0xFF)
                      task.data[6]++;

                  task.data[0] = CalcNewMinHandAngle(task.data[0], task.data[4], task.data[6]);
                  AdvanceClock(taskId, task.data[4]);
              }
              else
              {
                  task.data[6] = 0;
              }
          }
      }
};

/** Source: wallclock.c → Task_SetClock_AskConfirm */
export const Task_SetClock_AskConfirm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  DrawStdFrameWithCustomTileAndPalette(WIN_MSG, false, 0x250, 0x0d);
      AddTextPrinterParameterized(WIN_MSG, FONT_NORMAL, gText_IsThisTheCorrectTime, 0, 1, 0, null);
      PutWindowTilemap(WIN_MSG);
      ScheduleBgCopyTilemapToVram(0);
      CreateYesNoMenu(sWindowTemplate_ConfirmYesNo, 0x250, 0x0d, 1);
      task.func = (t) => Task_SetClock_HandleConfirmInput(t, rt);
};

/** Source: wallclock.c → Task_SetClock_HandleConfirmInput */
export const Task_SetClock_HandleConfirmInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (Menu_ProcessInputNoWrapClearOnChoose())
      {
      case 0:  
          PlaySE(SE_SELECT);
          task.func = (t) => Task_SetClock_Confirmed(t, rt);
          break;
      case 1:  
      case MENU_B_PRESSED:
          PlaySE(SE_SELECT);
          ClearStdWindowAndFrameToTransparent(WIN_MSG, false);
          ClearWindowTilemap(WIN_MSG);
          task.func = (t) => Task_SetClock_HandleInput(t, rt);
          break;
      }
};

/** Source: wallclock.c → Task_SetClock_Confirmed */
export const Task_SetClock_Confirmed: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  RtcInitLocalTimeOffset(task.data[2], task.data[3]);
      rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "RGB_BLACK");
      task.func = (t) => Task_SetClock_Exit(t, rt);
};

/** Source: wallclock.c → Task_SetClock_Exit */
export const Task_SetClock_Exit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          FreeAllWindowBuffers();
          rt.SetMainCallback2(gMain.savedCallback ?? null);
      }
};

/** Source: wallclock.c → Task_ViewClock_WaitFadeIn */
export const Task_ViewClock_WaitFadeIn: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
          task.func = (t) => Task_ViewClock_HandleInput(t, rt);
};

/** Source: wallclock.c → Task_ViewClock_HandleInput */
export const Task_ViewClock_HandleInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  InitClockWithRtc(taskId);
      if (JOY_NEW(A_BUTTON | B_BUTTON))
          task.func = (t) => Task_ViewClock_FadeOut(t, rt);
};

/** Source: wallclock.c → Task_ViewClock_FadeOut */
export const Task_ViewClock_FadeOut: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "RGB_BLACK");
      task.func = (t) => Task_ViewClock_Exit(t, rt);
};

/** Source: wallclock.c → Task_ViewClock_Exit */
export const Task_ViewClock_Exit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
          rt.SetMainCallback2(gMain.savedCallback ?? null);
};

/** Source: wallclock.c → CB2_StartWallClock */
export const CB2_StartWallClock: CB2Callback = (rt) => {
  let taskId = 0;
      let spriteId = 0;

      LoadWallClockGraphics();
      LZ77UnCompVram(gWallClockStart_Tilemap, BG_SCREEN_ADDR(7));

      taskId = rt.CreateTask((t) => Task_SetClock_WaitFadeIn(t, rt), 0);
      task.data[2] = 10;
      task.data[3] = 0;
      task.data[4] = 0;
      task.data[5] = 0;
      task.data[6] = 0;
      task.data[0] = 0;
      task.data[1] = 300;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_MinuteHand',  120, 80, 1);
      _gs(rt, spriteId).data[0] = taskId;
      rt.gba.oam[_gs(rt, spriteId).oamIndex].affineMode = ST_OAM_AFFINE_NORMAL;
      _gs(rt, spriteId).matrixNum = 0;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_HourHand',  120, 80, 0);
      _gs(rt, spriteId).data[0] = taskId;
      rt.gba.oam[_gs(rt, spriteId).oamIndex].affineMode = ST_OAM_AFFINE_NORMAL;
      _gs(rt, spriteId).matrixNum = 1;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_PM',  120, 80, 2);
      _gs(rt, spriteId).data[0] = taskId;
      _gs(rt, spriteId).data[1] = 45;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_AM',  120, 80, 2);
      _gs(rt, spriteId).data[0] = taskId;
      _gs(rt, spriteId).data[1] = 90;

      WallClockInit();

      AddTextPrinterParameterized(WIN_BUTTON_LABEL, FONT_NORMAL, gText_Confirm3, 0, 1, 0, null);
      PutWindowTilemap(WIN_BUTTON_LABEL);
      ScheduleBgCopyTilemapToVram(2);
};

/** Source: wallclock.c → CB2_ViewWallClock */
export const CB2_ViewWallClock: CB2Callback = (rt) => {
  let taskId = 0;
      let spriteId = 0;
      let angle1 = 0;
      let angle2 = 0;

      LoadWallClockGraphics();
      LZ77UnCompVram(gWallClockView_Tilemap, BG_SCREEN_ADDR(7));

      taskId = rt.CreateTask((t) => Task_ViewClock_WaitFadeIn(t, rt), 0);
      InitClockWithRtc(taskId);
      if (task.data[5] == PERIOD_AM)
      {
          angle1 = 45;
          angle2 = 90;
      }
      else
      {
          angle1 = 90;
          angle2 = 135;
      }

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_MinuteHand',  120, 80, 1);
      _gs(rt, spriteId).data[0] = taskId;
      rt.gba.oam[_gs(rt, spriteId).oamIndex].affineMode = ST_OAM_AFFINE_NORMAL;
      _gs(rt, spriteId).matrixNum = 0;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_HourHand',  120, 80, 0);
      _gs(rt, spriteId).data[0] = taskId;
      rt.gba.oam[_gs(rt, spriteId).oamIndex].affineMode = ST_OAM_AFFINE_NORMAL;
      _gs(rt, spriteId).matrixNum = 1;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_PM',  120, 80, 2);
      _gs(rt, spriteId).data[0] = taskId;
      _gs(rt, spriteId).data[1] = angle1;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_AM',  120, 80, 2);
      _gs(rt, spriteId).data[0] = taskId;
      _gs(rt, spriteId).data[1] = angle2;

      WallClockInit();

      AddTextPrinterParameterized(WIN_BUTTON_LABEL, FONT_NORMAL, gText_Cancel4, 0, 1, 0, null);
      PutWindowTilemap(WIN_BUTTON_LABEL);
      ScheduleBgCopyTilemapToVram(2);
};

/** Source: wallclock.c → CB2_WallClock */
export const CB2_WallClock: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      DoScheduledBgTilemapCopiesToVram();
      UpdatePaletteFade();
};
