// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/pokenav_menu_handler_gfx.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  ChangeBgX,
  LoadPalette,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const BG_COORD_ADD = 1;
const BLDCNT_EFFECT_NONE = 0;
const BLDCNT_TGT2_ALL = 16128;
const POKENAV_SUBSTRUCT_MENU_GFX = 2;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: pokenav_menu_handler_gfx.c → SpriteCB_OptionSlide */
export const SpriteCB_OptionSlide: SpriteCallback = (sprite, rt) => {
  sprite.data[0]--;
      if (sprite.data[0] != -1)
      {
          sprite.data[2] += sprite.data[1];
          sprite.x = sprite.data[2] >> 4;
      }
      else
      {
          sprite.x = sprite.data[7];
          sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: pokenav_menu_handler_gfx.c → SpriteCB_OptionZoom */
export const SpriteCB_OptionZoom: SpriteCallback = (sprite, rt) => {
  let temp = 0;
      let x = 0;
      if (sprite.data[0] == 0)
      {
          if (!sprite.data[1])
          {
              rt.StartSpriteAffineAnim(sprite.spriteId, 1);
              sprite.data[1]++;
              sprite.data[2] = 0x100;
              sprite.x += sprite.x2;
              sprite.x2 = 0;
          }
          else
          {
              sprite.data[2] += 16;
              temp = sprite.data[2];
              x = temp >> 3;x = Math.trunc((x - 32) / 2);

               
              switch (sprite.data[7])
              {
              case 0:
                  sprite.x2 = -x * 3;
                  break;
              case 1:
                  sprite.x2 = -x;
                  break;
              case 2:
                  sprite.x2 = x;
                  break;
              case 3:
                  sprite.x2 = x * 3;
                  break;
              }
              if (sprite.affineAnimEnded)
              {
                  sprite.invisible = true;
                  FreeOamMatrix(rt.gba.oam[sprite.oamIndex].affineParamIndex);
                  (() => { const _ctcv = CalcCenterToCornerVec(0, 0, 0); sprite.centerToCornerVecX = _ctcv.centerToCornerVecX; sprite.centerToCornerVecY = _ctcv.centerToCornerVecY; })();
                  rt.gba.oam[sprite.oamIndex].affineMode = ST_OAM_AFFINE_OFF;
                  rt.gba.oam[sprite.oamIndex].objMode = ST_OAM_OBJ_NORMAL;
                  sprite.callback = SpriteCallbackDummy;
              }
          }
      }
      else
      {
          sprite.data[0]--;
      }
};

/** Source: pokenav_menu_handler_gfx.c → SpriteCB_BlinkingBlueLight */
export const SpriteCB_BlinkingBlueLight: SpriteCallback = (sprite, rt) => {
  sprite.data[0]++;
      if (sprite.data[0] > 8)
      {
          sprite.data[0] = 0;
          sprite.invisible ^= 1;
      }
};

/** Source: pokenav_menu_handler_gfx.c → Task_OptionBlend */
export const Task_OptionBlend: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (data[0] == 0)
      {
          switch (data[1])
          {
          case 0:
              data[2] = 16;
              data[3] = 0;
              rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_EFFECT_NONE | BLDCNT_TGT2_ALL);
              rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 0));
              data[1]++;
              break;
          case 1:
              if (data[4] & 1)
              {
                  data[2] -= 3;
                  if (data[2] < 0)
                      data[2] = 0;
              }
              else
              {
                  data[3] += 3;
                  if (data[3] > 16)
                      data[3] = 16;
              }
              rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(data[2], data[3]));
              data[4]++;
              if (data[4] == 12)
              {
                  (GetSubstructPtr(POKENAV_SUBSTRUCT_MENU_GFX)).numIconsBlending--;
                  rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(0, 16));
                  rt.DestroyTask(taskId);
              }
              break;
          }
      }
      else
      {
          data[0]--;
      }
};

/** Source: pokenav_menu_handler_gfx.c → Task_MoveBgDots */
export const Task_MoveBgDots: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  ChangeBgX(3, 0x80, BG_COORD_ADD);
};

/** Source: pokenav_menu_handler_gfx.c → Task_UpdateBgDotsPalette */
export const Task_UpdateBgDotsPalette: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let sp8: number[] = new Array(2).fill(0);
      const data = task.data;
      let pal1: any = GetWordTaskArg(taskId, 1);
      let pal2: any = GetWordTaskArg(taskId, 3);

      PokenavCopyPalette(pal1, pal2, 2, 12, ++data[0], sp8);
      LoadPalette(sp8, BG_PLTT_ID(3) + 1, PLTT_SIZEOF(2));
      if (data[0] == 12)
          rt.DestroyTask(taskId);
};

/** Source: pokenav_menu_handler_gfx.c → Task_CurrentMenuOptionGlow */
export const Task_CurrentMenuOptionGlow: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      data[0]++;
      if (data[0] > 0)
      {
          data[0] = 0;
          data[1] += 3;
          data[1] &= 0x7;
          rt.SetGpuReg(REG_OFFSET_BLDY, gSineTable(data[1]) >> 5);
      }
};
