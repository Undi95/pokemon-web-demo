// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/trainer_card.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const COPYWIN_FULL = 3;
const FONT_NORMAL = 1;
const OAM = 117440512;
const OAM_SIZE = 1024;
const STATE_CLOSE_CARD = 8;
const STATE_CLOSE_CARD_LINK = 16;
const STATE_HANDLE_INPUT_BACK = 11;
const STATE_HANDLE_INPUT_FRONT = 10;
const STATE_WAIT_FLIP_TO_BACK = 12;
const STATE_WAIT_FLIP_TO_FRONT = 13;
const STATE_WAIT_LINK_PARTNER = 15;
const WIN_CARD_TEXT = 1;
const WIN_MSG = 2;
const WIN_TRAINER_PIC = 2;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const JOY_NEW: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: trainer_card.c → Task_TrainerCard */
export const Task_TrainerCard: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (sData.mainState)
      {
       
      case 0:
          if (!IsDma3ManagerBusyWithBgCopy())
          {
              FillWindowPixelBuffer(WIN_CARD_TEXT, PIXEL_FILL(0));
              sData.mainState++;
          }
          break;
      case 1:
          if (PrintAllOnCardFront())
              sData.mainState++;
          break;
      case 2:
          DrawTrainerCardWindow(WIN_CARD_TEXT);
          sData.mainState++;
          break;
      case 3:
          FillWindowPixelBuffer(WIN_TRAINER_PIC, PIXEL_FILL(0));
          CreateTrainerCardTrainerPic();
          DrawTrainerCardWindow(WIN_TRAINER_PIC);
          sData.mainState++;
          break;
      case 4:
          DrawCardScreenBackground(sData.bgTilemap);
          sData.mainState++;
          break;
      case 5:
          DrawCardFrontOrBack(sData.frontTilemap);
          sData.mainState++;
          break;
      case 6:
          DrawStarsAndBadgesOnCard();
          sData.mainState++;
          break;
       
      case 7:
          if (gWirelessCommType == 1 && gReceivedRemoteLinkPlayers == true)
          {
              LoadWirelessStatusIndicatorSpriteGfx();
              CreateWirelessStatusIndicatorSprite(DISPLAY_WIDTH - 10, DISPLAY_HEIGHT - 10);
          }
          BlendPalettes(PALETTES_ALL, 16, sData.blendColor);
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 16, 0, "sData.blendColor");
          /* noop SetVBlankCallback */;
          sData.mainState++;
          break;
      case 8:
          if (!UpdatePaletteFade() && !IsDma3ManagerBusyWithBgCopy())
          {
              /* TODO sound PlaySE */;
              sData.mainState = STATE_HANDLE_INPUT_FRONT;
          }
          break;
      case 9:
          if (!IsSEPlaying())
              sData.mainState++;
          break;
      case STATE_HANDLE_INPUT_FRONT:
           
          if (!gReceivedRemoteLinkPlayers && sData.timeColonNeedDraw)
          {
              PrintTimeOnCard();
              DrawTrainerCardWindow(WIN_CARD_TEXT);
              sData.timeColonNeedDraw = false;
          }
          if (JOY_NEW(A_BUTTON))
          {
              FlipTrainerCard();
              /* TODO sound PlaySE */;
              sData.mainState = STATE_WAIT_FLIP_TO_BACK;
          }
          else if (JOY_NEW(B_BUTTON))
          {
              if (gReceivedRemoteLinkPlayers && sData.isLink && InUnionRoom() == true)
              {
                  sData.mainState = STATE_WAIT_LINK_PARTNER;
              }
              else
              {
                  rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "sData.blendColor");
                  sData.mainState = STATE_CLOSE_CARD;
              }
          }
          break;
      case STATE_WAIT_FLIP_TO_BACK:
          if (IsCardFlipTaskActive() && Overworld_IsRecvQueueAtMax() != true)
          {
              /* TODO sound PlaySE */;
              sData.mainState = STATE_HANDLE_INPUT_BACK;
          }
          break;
      case STATE_HANDLE_INPUT_BACK:
          if (JOY_NEW(B_BUTTON))
          {
              if (gReceivedRemoteLinkPlayers && sData.isLink && InUnionRoom() == true)
              {
                  sData.mainState = STATE_WAIT_LINK_PARTNER;
              }
              else if (gReceivedRemoteLinkPlayers)
              {
                  rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "sData.blendColor");
                  sData.mainState = STATE_CLOSE_CARD;
              }
              else
              {
                  FlipTrainerCard();
                  sData.mainState = STATE_WAIT_FLIP_TO_FRONT;
                  /* TODO sound PlaySE */;
              }
          }
          else if (JOY_NEW(A_BUTTON))
          {
             if (gReceivedRemoteLinkPlayers && sData.isLink && InUnionRoom() == true)
             {
                 sData.mainState = STATE_WAIT_LINK_PARTNER;
             }
             else
             {
                 rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "sData.blendColor");
                 sData.mainState = STATE_CLOSE_CARD;
             }
          }
          break;
      case STATE_WAIT_LINK_PARTNER:
          SetCloseLinkCallback();
          DrawDialogueFrame(WIN_MSG, true);
          AddTextPrinterParameterized(WIN_MSG, FONT_NORMAL, gText_WaitingTrainerFinishReading, 0, 1, 255, 0);
          CopyWindowToVram(WIN_MSG, COPYWIN_FULL);
          sData.mainState = STATE_CLOSE_CARD_LINK;
          break;
      case STATE_CLOSE_CARD_LINK:
          if (!gReceivedRemoteLinkPlayers)
          {
              rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "sData.blendColor");
              sData.mainState = STATE_CLOSE_CARD;
          }
          break;
      case STATE_CLOSE_CARD:
          if (!UpdatePaletteFade())
              CloseTrainerCard(taskId);
          break;
      case STATE_WAIT_FLIP_TO_FRONT:
          if (IsCardFlipTaskActive() && Overworld_IsRecvQueueAtMax() != true)
          {
              sData.mainState = STATE_HANDLE_INPUT_FRONT;
              /* TODO sound PlaySE */;
          }
          break;
     }
};

/** Source: trainer_card.c → Task_DoCardFlipTask */
export const Task_DoCardFlipTask: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while(sTrainerCardFlipTasks[task.data[0]](_gt(rt, taskId)))
          ;
};

/** Source: trainer_card.c → CB2_TrainerCard */
export const CB2_TrainerCard: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** Source: trainer_card.c → CB2_InitTrainerCard */
export const CB2_InitTrainerCard: CB2Callback = (rt) => {
  switch (gMain.state)
      {
      case 0:
          ResetGpuRegs();
          SetUpTrainerCardTask();
          gMain.state++;
          break;
      case 1:
          DmaClear32(3, OAM, OAM_SIZE);
          gMain.state++;
          break;
      case 2:
          if (!sData.blendColor)
              /* TODO DmaClear16 (memory clear) */;
          gMain.state++;
          break;
      case 3:
          rt.ResetSpriteData();
          /* TODO FreeAllSpritePalettes */;
          ResetPaletteFade();
          gMain.state++;
      case 4:
          InitBgsAndWindows();
          gMain.state++;
          break;
      case 5:
          LoadMonIconGfx();
          gMain.state++;
          break;
      case 6:
          if (LoadCardGfx() == true)
              gMain.state++;
          break;
      case 7:
          LoadStickerGfx();
          gMain.state++;
          break;
      case 8:
          InitGpuRegs();
          gMain.state++;
          break;
      case 9:
          BufferTextsVarsForCardPage2();
          gMain.state++;
          break;
      case 10:
          if (SetCardBgsAndPals() == true)
              gMain.state++;
          break;
      default:
          SetTrainerCardCb2();
          break;
      }
};
