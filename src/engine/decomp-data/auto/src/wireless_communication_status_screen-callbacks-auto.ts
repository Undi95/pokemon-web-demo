// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/wireless_communication_status_screen.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const BG_COORD_SET = 0;
const COLORMODE_RED = 2;
const COLORMODE_WHITE_LGRAY = 1;
const COPYWIN_FULL = 3;
const FONT_NORMAL = 1;
const GROUPTYPE_TOTAL = 3;
const NUM_GROUPTYPES = 4;
const STR_CONV_MODE_RIGHT_ALIGN = 1;
const WIN_GROUP_COUNTS = 2;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const JOY_NEW: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: wireless_communication_status_screen.c → Task_WirelessCommunicationScreen */
export const Task_WirelessCommunicationScreen: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;
      switch (task.data[0])
      {
      case 0:
          PrintHeaderTexts();
          task.data[0]++;
          break;
      case 1:
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 16, 0, "RGB_BLACK");
          ShowBg(1);
          CopyBgTilemapBufferToVram(0);
          ShowBg(0);
          task.data[0]++;
          break;
      case 2:
          if (!rt.gPaletteFade.active)
              task.data[0]++;
          break;
      case 3:
          if (UpdateCommunicationCounts(sStatusScreen.groupCounts, sStatusScreen.prevGroupCounts, sStatusScreen.activities, sStatusScreen.rfuTaskId))
          {
              FillWindowPixelBuffer(WIN_GROUP_COUNTS, PIXEL_FILL(0));
              for (i = 0; i < NUM_GROUPTYPES; i++)
              {
                  ConvertIntToDecimalStringN(gStringVar4, sStatusScreen.groupCounts[i], STR_CONV_MODE_RIGHT_ALIGN, 2);
                  if (i != GROUPTYPE_TOTAL)
                      WCSS_AddTextPrinterParameterized(WIN_GROUP_COUNTS, FONT_NORMAL, gStringVar4, 12, 30 * i + 8, COLORMODE_WHITE_LGRAY);
                  else
                      WCSS_AddTextPrinterParameterized(WIN_GROUP_COUNTS, FONT_NORMAL, gStringVar4, 12, 98, COLORMODE_RED);
              }
              PutWindowTilemap(WIN_GROUP_COUNTS);
              CopyWindowToVram(WIN_GROUP_COUNTS, COPYWIN_FULL);
          }
          if (JOY_NEW(A_BUTTON) || JOY_NEW(B_BUTTON))
          {
              /* TODO sound PlaySE */;
              _gt(rt, sStatusScreen.rfuTaskId).data[15] = 0xFF;
              task.data[0]++;
          }
          CyclePalette(task.data[7], task.data[8]);
          break;
      case 4:
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "RGB_BLACK");
          task.data[0]++;
          break;
      case 5:
          if (!rt.gPaletteFade.active)
          {
              /* TODO scene transition: SetMainCallback2(CB2_ExitWirelessCommunicationStatusScreen) */;
              rt.DestroyTask(taskId);
          }
          break;
      }
};

/** Source: wireless_communication_status_screen.c → CB2_RunWirelessCommunicationScreen */
export const CB2_RunWirelessCommunicationScreen: CB2Callback = (rt) => {
  if (!IsDma3ManagerBusyWithBgCopy())
      {
          RunTasks();
          RunTextPrinters();
          AnimateSprites();
          BuildOamBuffer();
          UpdatePaletteFade();
      }
};

/** Source: wireless_communication_status_screen.c → CB2_InitWirelessCommunicationScreen */
export const CB2_InitWirelessCommunicationScreen: CB2Callback = (rt) => {
  rt.SetGpuReg(REG_OFFSET_DISPCNT, 0);
      sStatusScreen = AllocZeroed(/* ((struct)?.length ?? 32) */ 0);
      /* noop SetVBlankCallback */;
      ResetBgsAndClearDma3BusyFlags(0);
      InitBgsFromTemplates(0, sBgTemplates, ((sBgTemplates)?.length ?? 0));
      SetBgTilemapBuffer(1, Alloc(BG_SCREEN_SIZE));
      SetBgTilemapBuffer(0, Alloc(BG_SCREEN_SIZE));
      DecompressAndLoadBgGfxUsingHeap(1, sBgTiles_Gfx, 0, 0, 0);
      CopyToBgTilemapBuffer(1, sBgTiles_Tilemap, 0, 0);
      InitWindows(sWindowTemplates);
      DeactivateAllTextPrinters();
      ResetPaletteFade();
      rt.ResetSpriteData();
      ResetTasks();
      ScanlineEffect_Stop();
      m4aSoundVSyncOn();
      /* noop SetVBlankCallback */;
      sStatusScreen.taskId = rt.CreateTask((t) => Task_WirelessCommunicationScreen(t, rt), 0);
      sStatusScreen.rfuTaskId = CreateTask_ListenToWireless();
      sStatusScreen.prevGroupCounts[GROUPTYPE_TOTAL] = 1;
      ChangeBgX(0, 0, BG_COORD_SET);
      ChangeBgY(0, 0, BG_COORD_SET);
      ChangeBgX(1, 0, BG_COORD_SET);
      ChangeBgY(1, 0, BG_COORD_SET);
      LoadPalette(sPalettes, BG_PLTT_ID(0), PLTT_SIZE_4BPP);
      Menu_LoadStdPalAt(BG_PLTT_ID(15));
      DynamicPlaceholderTextUtil_Reset();
      FillBgTilemapBufferRect(0, 0, 0, 0, 32, 32, 15);
      CopyBgTilemapBufferToVram(1);
      /* TODO scene transition: SetMainCallback2(CB2_RunWirelessCommunicationScreen) */;
      RunTasks();
      RunTextPrinters();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** Source: wireless_communication_status_screen.c → CB2_ExitWirelessCommunicationStatusScreen */
export const CB2_ExitWirelessCommunicationStatusScreen: CB2Callback = (rt) => {
  let i = 0;
      FreeAllWindowBuffers();
      for (i = 0; i < ((sBgTemplates)?.length ?? 0); i++)
      {
          Free(GetBgTilemapBuffer(i));
      }
      Free(sStatusScreen);
      /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
};
