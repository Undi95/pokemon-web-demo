// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_anim_throw.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  m4aMPlayAllStop,
  m4aSongNumStart,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const B_POSITION_OPPONENT_LEFT = 1;
const B_POSITION_PLAYER_LEFT = 0;
const B_SIDE_PLAYER = 0;
const BALL_3_SHAKES_SUCCESS = 4;
const BALL_FALLING = 0;
const BALL_NEXT_MOVE = 5;
const BALL_NO_SHAKES = 0;
const BALL_PIVOT_1 = 1;
const BALL_PIVOT_2 = 3;
const BALL_RISING = 1;
const BALL_ROLL_1 = 0;
const BALL_ROLL_2 = 2;
const BALL_ROLL_3 = 4;
const BALL_ROTATE_LEFT = 2;
const BALL_ROTATE_RIGHT = 1;
const BALL_TRAINER_BLOCK = 5;
const BALL_WAIT_NEXT_SHAKE = 6;
const BATTLER_AFFINE_EMERGE = 1;
const BATTLER_AFFINE_NORMAL = 0;
const BATTLER_COORD_X = 0;
const BATTLER_COORD_Y = 1;
const BLDCNT_TGT2_ALL = 16128;
const MAX_SPRITES = 64;
const MON_SHRINK = 0;
const MON_SHRINK_FREE = 3;
const MON_SHRINK_INVISIBLE = 2;
const MON_SHRINK_STEP = 1;
const MUS_RG_CAUGHT_INTRO = 531;
const SE_BALL = 23;
const SE_BALL_BOUNCE_1 = 56;
const SE_BALL_BOUNCE_2 = 57;
const SE_BALL_BOUNCE_3 = 58;
const SE_BALL_BOUNCE_4 = 59;
const SE_BALL_TRADE = 60;
const SE_RG_BALL_CLICK = 254;
const SE_SHINY = 102;
const SHINY_STAR_DIAGONAL = 1;
const SHINY_STAR_ENCIRCLE = 0;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const BOUNCES: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const DIRECTION: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const FALL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const PHASE_DELTA: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const RESET_STATE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const RISE_FASTER: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const SHAKES: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const SHAKE_INC: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const STATE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_anim_throw.c → SpriteCB_Ball_Throw */
export const SpriteCB_Ball_Throw: SpriteCallback = (sprite, rt) => {
  let targetX = sprite.data[1];
      let targetY = sprite.data[2];

      sprite.data[1] = sprite.x;
      sprite.data[2] = targetX;
      sprite.data[1] = sprite.y;
      sprite.data[4] = targetY;
      sprite.data[5] = -40;
      InitAnimArcTranslation(sprite);
      sprite.callback = SpriteCB_Ball_Arc;
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Arc */
export const SpriteCB_Ball_Arc: SpriteCallback = (sprite, rt) => {
  let i = 0;
      let ballId = 0;

      if (TranslateAnimHorizontalArc(sprite))
      {
          if (gBattleSpritesDataPtr.animationData.ballThrowCaseId == BALL_TRAINER_BLOCK)
          {
              sprite.callback = SpriteCB_Ball_Block;
          }
          else
          {
              rt.StartSpriteAnim(sprite.spriteId, 1);
              sprite.x += sprite.x2;
              sprite.y += sprite.y2;
              sprite.x2 = 0;
              sprite.y2 = 0;

              for (i = 0; i < 8; i++)
                  sprite.data[i] = 0;

              sprite.data[1] = 0;
              sprite.callback = SpriteCB_Ball_MonShrink;

              ballId = ItemIdToBallId(gLastUsedItem);
              switch (ballId)
              {
              case 0: /* ... case POKEBALL_COUNT - 1: range collapsed */
                  AnimateBallOpenParticles(sprite.x, sprite.y - 5, 1, 28, ballId);
                  LaunchBallFadeMonTask(false, gBattleAnimTarget, 14, ballId);
                  break;
              }
          }
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_MonShrink */
export const SpriteCB_Ball_MonShrink: SpriteCallback = (sprite, rt) => {
  if (++sprite.data[1] == 10)
      {
          sprite.data[0] = rt.CreateTask((t) => TaskDummy(t, rt), 50);
          sprite.callback = SpriteCB_Ball_MonShrink_Step;
          _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).data[1] = 0;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_MonShrink_Step */
export const SpriteCB_Ball_MonShrink_Step: SpriteCallback = (sprite, rt) => {
  let spriteId = 0;
      let taskId = 0;

      spriteId = gBattlerSpriteIds[gBattleAnimTarget];
      taskId = sprite.data[0];

      if (++task.data[1] == 11)
          m4aSongNumStart(SE_BALL_TRADE);

      switch (task.data[0])
      {
      case MON_SHRINK:
          PrepareBattlerSpriteForRotScale(spriteId, ST_OAM_OBJ_NORMAL);
          task.data[10] = 256;
          gMonShrinkDuration = 28;
          gMonShrinkDistance = (_gs(rt, spriteId).y + _gs(rt, spriteId).y2) - (sprite.y + sprite.y2);
          gMonShrinkDelta = (gMonShrinkDistance * 256) / gMonShrinkDuration;
          task.data[2] = gMonShrinkDelta;
          task.data[0]++;  
          break;
      case MON_SHRINK_STEP:
          task.data[10] += 32;
          SetSpriteRotScale(spriteId, task.data[10], task.data[10], 0);
          task.data[3] += task.data[2];
          _gs(rt, spriteId).y2 = -task.data[3] >> 8;
          if (task.data[10] >= 1152)
              task.data[0]++;  
          break;
      case MON_SHRINK_INVISIBLE:
          ResetSpriteRotScale(spriteId);
          _gs(rt, spriteId).invisible = true;
          task.data[0]++;  
          break;
      case MON_SHRINK_FREE:
      default:
          if (task.data[1] > 10)
          {
              rt.DestroyTask(taskId);
              rt.StartSpriteAnim(sprite.spriteId, 2);
              sprite.data[5] = 0;
              sprite.callback = SpriteCB_Ball_Bounce;
          }
          break;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Bounce */
export const SpriteCB_Ball_Bounce: SpriteCallback = (sprite, rt) => {
  let phase = 0;

      if (sprite.animEnded)
      {
          sprite.data[0] = 0;
          sprite.data[5] = 40;
          sprite.data[1] = 0;
          phase = 0;
          sprite.y += Cos(phase, 40);
          sprite.y2 = -Cos(phase, sprite.data[5]);
          sprite.callback = SpriteCB_Ball_Bounce_Step;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Bounce_Step */
export const SpriteCB_Ball_Bounce_Step: SpriteCallback = (sprite, rt) => {
  let lastBounce = 0;
      let bounceCount = 0;

      lastBounce = false;

      switch (DIRECTION(sprite.data[0]))
      {
      case BALL_FALLING:
          sprite.y2 = -Cos(sprite.data[1], sprite.data[5]);
          sprite.data[1] += PHASE_DELTA(sprite.data[0]) + 4;
           
          if (sprite.data[1] >= 64)
          {
              sprite.data[5] -= 10;
              RISE_FASTER(sprite.data[0]);

              bounceCount = BOUNCES(sprite.data[0]);
              if (bounceCount == 4)
                  lastBounce = true;

              switch (bounceCount)
              {
              case 1:
                  m4aSongNumStart(SE_BALL_BOUNCE_1);
                  break;
              case 2:
                  m4aSongNumStart(SE_BALL_BOUNCE_2);
                  break;
              case 3:
                  m4aSongNumStart(SE_BALL_BOUNCE_3);
                  break;
              default:
                  m4aSongNumStart(SE_BALL_BOUNCE_4);
                  break;
              }
          }
          break;
      case BALL_RISING:
          sprite.y2 = -Cos(sprite.data[1], sprite.data[5]);
          sprite.data[1] -= PHASE_DELTA(sprite.data[0]) + 4;
           
          if (sprite.data[1] <= 0)
          {
               
              sprite.data[1] = 0;
              FALL(sprite.data[0]);
          }
          break;
      }

      if (lastBounce)
      {
          sprite.data[0] = 0;
          sprite.y += Cos(64, 40);
          sprite.y2 = 0;
          if (gBattleSpritesDataPtr.animationData.ballThrowCaseId == BALL_NO_SHAKES)
          {
              sprite.data[1] = 0;
              sprite.callback = SpriteCB_Ball_Release;
          }
          else
          {
              sprite.callback = SpriteCB_Ball_Wobble;
              sprite.data[4] = 1;
              sprite.data[5] = 0;
          }
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Wobble */
export const SpriteCB_Ball_Wobble: SpriteCallback = (sprite, rt) => {
  if (++sprite.data[1] == 31)
      {
          sprite.data[0] = 0;
          sprite.affineAnimPaused = true;
          rt.StartSpriteAffineAnim(sprite.spriteId, BALL_ROTATE_RIGHT);
          gBattleSpritesDataPtr.animationData.ballSubpx = 0;
          sprite.callback = SpriteCB_Ball_Wobble_Step;
          m4aSongNumStart(SE_BALL);
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Wobble_Step */
export const SpriteCB_Ball_Wobble_Step: SpriteCallback = (sprite, rt) => {
  let shakes = 0;
      let frame = 0;

      switch (STATE(sprite.data[0]))
      {
      case BALL_ROLL_1:
           
          if (gBattleSpritesDataPtr.animationData.ballSubpx > 255)
          {
              sprite.x2 += sprite.data[4];
              gBattleSpritesDataPtr.animationData.ballSubpx &= 0xFF;
          }
          else
          {
              gBattleSpritesDataPtr.animationData.ballSubpx += 176;
          }

          sprite.data[1]++;
          sprite.affineAnimPaused = false;
          frame = sprite.data[1] + 7;
          if (frame > 14)
          {
              gBattleSpritesDataPtr.animationData.ballSubpx = 0;
              sprite.data[0]++;  
              sprite.data[1] = 0;
          }
          break;
      case BALL_PIVOT_1:
          if (++sprite.data[1] == 1)
          {
              sprite.data[1] = 0;
              sprite.data[4] = -sprite.data[4];
              sprite.data[0]++;  
              sprite.affineAnimPaused = false;
              if (sprite.data[4] < 0)
                  ChangeSpriteAffineAnim(sprite, BALL_ROTATE_LEFT);
              else
                  ChangeSpriteAffineAnim(sprite, BALL_ROTATE_RIGHT);
          }
          else
          {
              sprite.affineAnimPaused = true;
          }
          break;
      case BALL_ROLL_2:
          if (gBattleSpritesDataPtr.animationData.ballSubpx > 255)
          {
              sprite.x2 += sprite.data[4];
              gBattleSpritesDataPtr.animationData.ballSubpx &= 0xFF;
          }
          else
          {
              gBattleSpritesDataPtr.animationData.ballSubpx += 176;
          }

          sprite.data[1]++;
          sprite.affineAnimPaused = false;
          frame = sprite.data[1] + 12;
          if (frame > 24)
          {
              gBattleSpritesDataPtr.animationData.ballSubpx = 0;
              sprite.data[0]++;  
              sprite.data[1] = 0;
          }
          break;
      case BALL_PIVOT_2:
          if (sprite.data[1]++ < 0)
          {
              sprite.affineAnimPaused = true;
              break;
          }

          sprite.data[1] = 0;
          sprite.data[4] = -sprite.data[4];
          sprite.data[0]++;  
          sprite.affineAnimPaused = false;
          if (sprite.data[4] < 0)
              ChangeSpriteAffineAnim(sprite, BALL_ROTATE_LEFT);
          else
              ChangeSpriteAffineAnim(sprite, BALL_ROTATE_RIGHT);
           
      case BALL_ROLL_3:
          if (gBattleSpritesDataPtr.animationData.ballSubpx > 0xFF)
          {
              sprite.x2 += sprite.data[4];
              gBattleSpritesDataPtr.animationData.ballSubpx &= 0xFF;
          }
          else
          {
              gBattleSpritesDataPtr.animationData.ballSubpx += 176;
          }

          sprite.data[1]++;
          sprite.affineAnimPaused = false;
          frame = sprite.data[1] + 4;
          if (frame > 8)
          {
              gBattleSpritesDataPtr.animationData.ballSubpx = 0;
              sprite.data[0]++;  
              sprite.data[1] = 0;
              sprite.data[4] = -sprite.data[4];
          }
          break;
      case BALL_NEXT_MOVE:
          SHAKE_INC(sprite.data[0]);
          shakes = SHAKES(sprite.data[0]);
          if (shakes == gBattleSpritesDataPtr.animationData.ballThrowCaseId)
          {
              sprite.affineAnimPaused = true;
              sprite.callback = SpriteCB_Ball_Release;
          }
          else
          {
              if (gBattleSpritesDataPtr.animationData.ballThrowCaseId == BALL_3_SHAKES_SUCCESS && shakes == 3)
              {
                  sprite.callback = SpriteCB_Ball_Capture;
                  sprite.affineAnimPaused = true;
              }
              else
              {
                  sprite.data[0]++;  
                  sprite.affineAnimPaused = true;
              }
          }
          break;
      case BALL_WAIT_NEXT_SHAKE:
      default:
          if (++sprite.data[1] == 31)
          {
              sprite.data[1] = 0;
              RESET_STATE(sprite.data[0]);
              rt.StartSpriteAffineAnim(sprite.spriteId, 3);
              if (sprite.data[4] < 0)
                  rt.StartSpriteAffineAnim(sprite.spriteId, BALL_ROTATE_LEFT);
              else
                  rt.StartSpriteAffineAnim(sprite.spriteId, BALL_ROTATE_RIGHT);

              m4aSongNumStart(SE_BALL);
          }
          break;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Release */
export const SpriteCB_Ball_Release: SpriteCallback = (sprite, rt) => {
  if (++sprite.data[1] == 31)
      {
          sprite.data[5] = 0;
          sprite.callback = SpriteCB_Ball_Release_Step;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Capture */
export const SpriteCB_Ball_Capture: SpriteCallback = (sprite, rt) => {
  sprite.animPaused = true;
      sprite.callback = SpriteCB_Ball_Capture_Step;
      sprite.data[3] = 0;
      sprite.data[1] = 0;
      sprite.data[5] = 0;
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Capture_Step */
export const SpriteCB_Ball_Capture_Step: SpriteCallback = (sprite, rt) => {
  let battler = gBattleAnimTarget;

      sprite.data[1]++;
      if (sprite.data[1] == 40)
      {
          m4aSongNumStart(SE_RG_BALL_CLICK);
          BlendPalettes(0x10000 << rt.gba.oam[sprite.oamIndex].paletteBank, 6, RGB_BLACK);
          MakeCaptureStars(sprite);
      }
      else if (sprite.data[1] == 60)
      {
          rt.BeginNormalPaletteFade("0x10000 << rt.gba.oam[sprite.oamIndex].paletteBank", 2, 6, 0, "RGB_BLACK");
      }
      else if (sprite.data[1] == 95)
      {
          gDoingBattleAnim = false;
          UpdateOamPriorityInAllHealthboxes(1);
          m4aMPlayAllStop();
          m4aSongNumStart(MUS_RG_CAUGHT_INTRO);
      }
      else if (sprite.data[1] == 315)
      {
          FreeOamMatrix(rt.gba.oam[_gs(rt, gBattlerSpriteIds[battler]).oamIndex].affineParamIndex);
          rt.DestroySprite(gBattlerSpriteIds[battler]);

          sprite.data[0] = 0;
          sprite.callback = SpriteCB_Ball_FadeOut;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_FadeOut */
export const SpriteCB_Ball_FadeOut: SpriteCallback = (sprite, rt) => {
  let paletteIndex = 0;

      switch (sprite.data[0])
      {
      case 0:
          sprite.data[1] = 0;
          sprite.data[2] = 0;
          rt.gba.oam[sprite.oamIndex].objMode = ST_OAM_OBJ_BLEND;
          rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_ALL);
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 0));
          paletteIndex = IndexOfSpritePaletteTag(sprite.template.paletteTag);
          rt.BeginNormalPaletteFade("1 << (paletteIndex + 0x10)", 0, 0, 16, "RGB_WHITE");
          sprite.data[0]++;
          break;
      case 1:
          if (sprite.data[1]++ > 0)
          {
              sprite.data[1] = 0;
              sprite.data[2]++;
              rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16 - sprite.data[2], sprite.data[2]));
              if (sprite.data[2] == 16)
                  sprite.data[0]++;
          }
          break;
      case 2:
          sprite.invisible = true;
          sprite.data[0]++;
          break;
      default:
          if (!rt.gPaletteFade.active)
          {
              rt.SetGpuReg(REG_OFFSET_BLDCNT, 0);
              rt.SetGpuReg(REG_OFFSET_BLDALPHA, 0);

              sprite.data[0] = 0;
              sprite.callback = DestroySpriteAfterOneFrame;
          }
          break;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_CaptureStar_Flicker */
export const SpriteCB_CaptureStar_Flicker: SpriteCallback = (sprite, rt) => {
  sprite.invisible = !sprite.invisible;
      if (TranslateAnimHorizontalArc(sprite))
          rt.DestroySprite(sprite.spriteId);
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Release_Step */
export const SpriteCB_Ball_Release_Step: SpriteCallback = (sprite, rt) => {
  let ballId = 0;

      rt.StartSpriteAnim(sprite.spriteId, 1);
      rt.StartSpriteAffineAnim(sprite.spriteId, 0);
      sprite.callback = SpriteCB_Ball_Release_Wait;

      ballId = ItemIdToBallId(gLastUsedItem);
      switch (ballId)
      {
      case 0: /* ... case POKEBALL_COUNT - 1: range collapsed */
          AnimateBallOpenParticles(sprite.x, sprite.y - 5, 1, 28, ballId);
          LaunchBallFadeMonTask(true, gBattleAnimTarget, 14, ballId);
          break;
      }

       
      _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).invisible = false;
      rt.StartSpriteAffineAnim(gBattlerSpriteIds[gBattleAnimTarget], BATTLER_AFFINE_EMERGE);
      AnimateSprite(_gs(rt, gBattlerSpriteIds[gBattleAnimTarget]));
      _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).data[1] = 4096;
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Release_Wait */
export const SpriteCB_Ball_Release_Wait: SpriteCallback = (sprite, rt) => {
  let released = false;

      if (sprite.animEnded)
          sprite.invisible = true;

      if (_gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).affineAnimEnded)
      {
          rt.StartSpriteAffineAnim(gBattlerSpriteIds[gBattleAnimTarget], BATTLER_AFFINE_NORMAL);
          released = true;
      }
      else
      {
          _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).data[1] -= 288;
          _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).y2 = _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).data[1] >> 8;
      }

      if (sprite.animEnded && released)
      {
          _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).y2 = 0;
          _gs(rt, gBattlerSpriteIds[gBattleAnimTarget]).invisible = gBattleSpritesDataPtr.animationData.wildMonInvisible;
          sprite.data[0] = 0;
          sprite.callback = DestroySpriteAfterOneFrame;
          gDoingBattleAnim = 0;
          UpdateOamPriorityInAllHealthboxes(1);
      }
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Block */
export const SpriteCB_Ball_Block: SpriteCallback = (sprite, rt) => {
  let i = 0;

      sprite.x += sprite.x2;
      sprite.y += sprite.y2;
      sprite.y2 = 0;
      sprite.x2 = 0;
      for (i = 0; i < 6; i++)
          sprite.data[i] = 0;

      sprite.callback = SpriteCB_Ball_Block_Step;
};

/** Source: battle_anim_throw.c → SpriteCB_Ball_Block_Step */
export const SpriteCB_Ball_Block_Step: SpriteCallback = (sprite, rt) => {
  let dy = sprite.data[0] + 0x800;
      let dx = sprite.data[1] + 0x680;
      sprite.x2 -= dx >> 8;
      sprite.y2 += dy >> 8;
      sprite.data[0] = (sprite.data[0] + 0x800) & 0xFF;
      sprite.data[1] = (sprite.data[1] + 0x680) & 0xFF;

      if (sprite.y + sprite.y2 > DISPLAY_HEIGHT
       || sprite.x + sprite.x2 < -8)
      {
          sprite.data[0] = 0;
          sprite.callback = DestroySpriteAfterOneFrame;
          gDoingBattleAnim = 0;
          UpdateOamPriorityInAllHealthboxes(1);
      }
};

/** Source: battle_anim_throw.c → SpriteCB_ShinyStars_Encircle */
export const SpriteCB_ShinyStars_Encircle: SpriteCallback = (sprite, rt) => {
  sprite.x2 = Sin(sprite.data[1], 24);
      sprite.y2 = Cos(sprite.data[1], 24);
      sprite.data[1] += 12;
      if (sprite.data[1] > 255)
      {
          _gt(rt, sprite.data[0]).data[12]--;
          FreeSpriteOamMatrix(sprite);
          rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: battle_anim_throw.c → SpriteCB_ShinyStars_Diagonal */
export const SpriteCB_ShinyStars_Diagonal: SpriteCallback = (sprite, rt) => {
  if (sprite.data[1] < 4)
      {
          sprite.data[1]++;
      }
      else
      {
          sprite.invisible = false;
          sprite.x2 += 5;
          sprite.y2 -= 5;
          if (sprite.x2 > 32)
          {
              _gt(rt, sprite.data[0]).data[12]--;
              FreeSpriteOamMatrix(sprite);
              rt.DestroySprite(sprite.spriteId);
          }
      }
};

/** Source: battle_anim_throw.c → SpriteCB_PokeBlock_Throw */
export const SpriteCB_PokeBlock_Throw: SpriteCallback = (sprite, rt) => {
  InitSpritePosToAnimAttacker(sprite, false);
      sprite.data[0] = 30;
      sprite.data[2] = GetBattlerSpriteCoord(GetBattlerAtPosition(B_POSITION_OPPONENT_LEFT), BATTLER_COORD_X) + gBattleAnimArgs[2];
      sprite.data[4] = GetBattlerSpriteCoord(GetBattlerAtPosition(B_POSITION_OPPONENT_LEFT), BATTLER_COORD_Y) + gBattleAnimArgs[3];
      sprite.data[5] = -32;
      InitAnimArcTranslation(sprite);
      rt.setSpriteCallback(gBattlerSpriteIds[gBattleAnimAttacker], SpriteCB_TrainerThrowObject);
      sprite.callback = SpriteCB_PokeBlock_LiftArm;
};

/** Source: battle_anim_throw.c → SpriteCB_PokeBlock_LiftArm */
export const SpriteCB_PokeBlock_LiftArm: SpriteCallback = (sprite, rt) => {
  if (_gs(rt, gBattlerSpriteIds[gBattleAnimAttacker]).animCmdIndex == 1)
          sprite.callback = SpriteCB_PokeBlock_Arc;
};

/** Source: battle_anim_throw.c → SpriteCB_PokeBlock_Arc */
export const SpriteCB_PokeBlock_Arc: SpriteCallback = (sprite, rt) => {
  if (TranslateAnimHorizontalArc(sprite))
      {
          sprite.data[0] = 0;
          sprite.invisible = true;
          sprite.callback = SpriteCB_ThrowPokeBlock_Free;
      }
};

/** Source: battle_anim_throw.c → SpriteCB_ThrowPokeBlock_Free */
export const SpriteCB_ThrowPokeBlock_Free: SpriteCallback = (sprite, rt) => {
  if (_gs(rt, gBattlerSpriteIds[gBattleAnimAttacker]).animEnded)
      {
          if (++sprite.data[0] > 0)
          {
              rt.StartSpriteAnim(gBattlerSpriteIds[gBattleAnimAttacker], 0);
              DestroyAnimSprite(sprite);
          }
      }
};

/** Source: battle_anim_throw.c → Task_PlayerThrow_Wait */
export const Task_PlayerThrow_Wait: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (_gs(rt, gBattlerSpriteIds[GetBattlerAtPosition(B_POSITION_PLAYER_LEFT)]).animEnded)
      {
          rt.StartSpriteAnim(gBattlerSpriteIds[GetBattlerAtPosition(B_POSITION_PLAYER_LEFT)], 0);
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_anim_throw.c → Task_FadeMon_ToBallColor */
export const Task_FadeMon_ToBallColor: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let ballId = task.data[15];

      if (task.data[13] <= 16)
      {
          BlendPalette(OBJ_PLTT_ID(task.data[3]), 16, task.data[0], gBallOpenFadeColors[ballId]);
          task.data[0] += task.tdCoeff;
          task.data[13]++;
      }
      else if (!rt.gPaletteFade.active)
      {
          let selectedPalettes = task.data[10] | (task.data[11] << 16);
          rt.BeginNormalPaletteFade("selectedPalettes", 0, 16, 0, "RGB_WHITE");
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_anim_throw.c → Task_FadeMon_ToNormal */
export const Task_FadeMon_ToNormal: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          let selectedPalettes = task.data[10] | (task.data[11] << 16);
          rt.BeginNormalPaletteFade("selectedPalettes", 0, 16, 0, "RGB_WHITE");
          task.func = (t) => Task_FadeMon_ToNormal_Step(t, rt);
      }
};

/** Source: battle_anim_throw.c → Task_FadeMon_ToNormal_Step */
export const Task_FadeMon_ToNormal_Step: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let ballId = task.data[15];

      if (task.data[13] <= 16)
      {
          BlendPalette(OBJ_PLTT_ID(task.data[3]), 16, task.data[0], gBallOpenFadeColors[ballId]);
          task.data[0] += task.tdCoeff;
          task.data[13]++;
      }
      else
      {
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_anim_throw.c → Task_ShinyStars */
export const Task_ShinyStars: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let battler = 0;
      let x, y;
      let spriteId = 0;
      let timer = 0;
      let starIdx = 0;
      let pan = 0;

      if (task.data[13] < 60)
      {
          task.data[13]++;
          return;
      }

       
      if (gBattleSpritesDataPtr.animationData.numBallParticles)
          return;

      timer = task.data[10]++;
      if (timer % 4)  
          return;

      battler = task.data[0];
      x = GetBattlerSpriteCoord(battler, BATTLER_COORD_X);
      y = GetBattlerSpriteCoord(battler, BATTLER_COORD_Y);

      starIdx = task.data[11];
      if (starIdx == 0)  
      {
          spriteId = rt.CreateSpriteFromTemplate('gWishStarSpriteTemplate',  x, y, 5);
      }
      else if (starIdx >= 0 && task.data[11] < 4)  
      {
          spriteId = rt.CreateSpriteFromTemplate('gMiniTwinklingStarSpriteTemplate',  x, y, 5);
          rt.gba.oam[_gs(rt, spriteId).oamIndex].tileId += 4;
      }
      else  
      {
          spriteId = rt.CreateSpriteFromTemplate('gMiniTwinklingStarSpriteTemplate',  x, y, 5);
          rt.gba.oam[_gs(rt, spriteId).oamIndex].tileId += 5;
      }

      if (task.data[1] == SHINY_STAR_ENCIRCLE)
      {
          rt.setSpriteCallback(spriteId, SpriteCB_ShinyStars_Encircle);
      }
      else
      {
          rt.setSpriteCallback(spriteId, SpriteCB_ShinyStars_Diagonal);
          _gs(rt, spriteId).x2 = -32;
          _gs(rt, spriteId).y2 = 32;
          _gs(rt, spriteId).invisible = true;
          if (task.data[11] == 0)
          {
              if (GetBattlerSide(battler) == B_SIDE_PLAYER)
                  pan = -64;
              else
                  pan = 63;

              PlaySE12WithPanning(SE_SHINY, pan);
          }
      }

      _gs(rt, spriteId).data[0] = taskId;
      task.data[11]++;
      if (spriteId != MAX_SPRITES)
          task.data[12]++;

      if (task.data[11] == 5)
          task.func = (t) => Task_ShinyStars_Wait(t, rt);
};

/** Source: battle_anim_throw.c → Task_ShinyStars_Wait */
export const Task_ShinyStars_Wait: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let battler = 0;

      if (task.data[12] == 0)
      {
          if (task.data[1] == SHINY_STAR_DIAGONAL)
          {
              battler = task.data[0];
              gBattleSpritesDataPtr.healthBoxesData[battler].finishedShinyMonAnim = true;
          }

          rt.DestroyTask(taskId);
      }
};
