// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/fldeff_softboiled.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  ClearWindowTilemap,
  StringExpandPlaceholders,
  gStringVar4,
  m4aSongNumStart,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const MON_DATA_HP = 57;
const MON_DATA_MAX_HP = 58;
const PARTY_MSG_CHOOSE_MON = 0;
const PARTY_MSG_USE_ON_WHICH_MON = 5;
const PARTY_SIZE = 6;
const SE_USE_ITEM = 1;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: fldeff_softboiled.c → Task_TryUseSoftboiledOnPartyMon */
export const Task_TryUseSoftboiledOnPartyMon: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let hp = 0;

      let userPartyId = gPartyMenu.slotId;
      let recipientPartyId = gPartyMenu.slotId2;
      if(recipientPartyId > PARTY_SIZE)
      {
          gPartyMenu.action = 0;
          DisplayPartyMenuStdMessage(PARTY_MSG_CHOOSE_MON);
          task.func = (t) => Task_HandleChooseMonInput(t, rt);
          return;
      }

      hp = GetMonData(gPlayerParty[recipientPartyId], MON_DATA_HP);
      if(hp == 0 || userPartyId == recipientPartyId || GetMonData(gPlayerParty[recipientPartyId], MON_DATA_MAX_HP) == hp)
      {
          CantUseSoftboiledOnMon(taskId);
          return;
      }

       
      m4aSongNumStart(SE_USE_ITEM);
      PartyMenuModifyHP(taskId, userPartyId, -1, GetMonData(gPlayerParty[userPartyId], MON_DATA_MAX_HP)/5, Task_SoftboiledRestoreHealth);
};

/** Source: fldeff_softboiled.c → Task_SoftboiledRestoreHealth */
export const Task_SoftboiledRestoreHealth: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  m4aSongNumStart(SE_USE_ITEM);
      PartyMenuModifyHP(taskId, gPartyMenu.slotId2, 1, GetMonData(gPlayerParty[gPartyMenu.slotId], MON_DATA_MAX_HP)/5, Task_DisplayHPRestoredMessage);
};

/** Source: fldeff_softboiled.c → Task_DisplayHPRestoredMessage */
export const Task_DisplayHPRestoredMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  GetMonNickname(gPlayerParty[gPartyMenu.slotId2], gStringVar1);
      StringExpandPlaceholders(gStringVar4, gText_PkmnHPRestoredByVar2);
      DisplayPartyMenuMessage(gStringVar4, false);
      ScheduleBgCopyTilemapToVram(2);
      task.func = (t) => Task_FinishSoftboiled(t, rt);
};

/** Source: fldeff_softboiled.c → Task_FinishSoftboiled */
export const Task_FinishSoftboiled: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if(IsPartyMenuTextPrinterActive() == true)
          return;
      gPartyMenu.action = 0;
      AnimatePartySlot(gPartyMenu.slotId, 0);
      gPartyMenu.slotId = gPartyMenu.slotId2;
      AnimatePartySlot(gPartyMenu.slotId2, 1);
      ClearStdWindowAndFrameToTransparent(6, false);
      ClearWindowTilemap(6);
      DisplayPartyMenuStdMessage(PARTY_MSG_CHOOSE_MON);
      task.func = (t) => Task_HandleChooseMonInput(t, rt);
};

/** Source: fldeff_softboiled.c → Task_ChooseNewMonForSoftboiled */
export const Task_ChooseNewMonForSoftboiled: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if(IsPartyMenuTextPrinterActive() == true)
          return;
      DisplayPartyMenuStdMessage(PARTY_MSG_USE_ON_WHICH_MON);
      task.func = (t) => Task_HandleChooseMonInput(t, rt);
};
