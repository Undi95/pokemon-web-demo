// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/berry_crush.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  AddTextPrinterParameterized3,
  AddWindow,
  ClearWindowTilemap,
  CopyWindowToVram,
  FillWindowPixelBuffer,
  JOY_NEW,
  PutWindowTilemap,
  RemoveWindow,
  StringExpandPlaceholders,
  gStringVar4,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const COLORID_BLUE = 3;
const COLORID_GRAY = 0;
const COPYWIN_FULL = 3;
const F_MOVE_HORIZ = 32768;
const FONT_NORMAL = 1;
const MASK_TARGET_Y = 32767;
const MAX_RFU_PLAYERS = 5;
const STR_CONV_MODE_LEADING_ZEROS = 2;
const STR_CONV_MODE_LEFT_ALIGN = 0;
const STR_CONV_MODE_RIGHT_ALIGN = 1;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const Q_8_8: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: berry_crush.c → SpriteCB_DropBerryIntoCrusher */
export const SpriteCB_DropBerryIntoCrusher: SpriteCallback = (sprite, rt) => {
  let data = sprite.data;

      data[1] += data[2];
      sprite.y2 += data[1] >> 8;
      if (data[7] & F_MOVE_HORIZ)
      {
          sprite.data[0] += data[3];
          data[4] += data[5];
          sprite.x2 = Sin(data[4] >> 7, data[6]);
          if ((data[7] & F_MOVE_HORIZ) && (data[4] >> 7) > 126)
          {
              sprite.x2 = 0;
              data[7] &= MASK_TARGET_Y;
          }
      }

      sprite.x = data[0] >> 7;
      if (sprite.y + sprite.y2 >= (data[7] & MASK_TARGET_Y))
      {
          sprite.callback = SpriteCallbackDummy;
          FreeSpriteOamMatrix(sprite);
          rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: berry_crush.c → SpriteCB_Impact */
export const SpriteCB_Impact: SpriteCallback = (sprite, rt) => {
  if (sprite.animEnded)
      {
          sprite.invisible = true;
          sprite.animPaused = true;
      }
};

/** Source: berry_crush.c → SpriteCB_Sparkle_End */
export const SpriteCB_Sparkle_End: SpriteCallback = (sprite, rt) => {
  let i = 0;
      for (i = 0; i < ((sprite.data)?.length ?? 0); i++)
          sprite.data[i] = 0;
      sprite.x2 = 0;
      sprite.y2 = 0;
      sprite.invisible = true;
      sprite.animPaused = true;
      sprite.callback = SpriteCallbackDummy;
};

/** Source: berry_crush.c → SpriteCB_Sparkle */
export const SpriteCB_Sparkle: SpriteCallback = (sprite, rt) => {
  let data = sprite.data;

      data[1] += data[2];
      sprite.y2 += data[1] >> 8;
      if (data[7] & F_MOVE_HORIZ)
      {
          sprite.data[0] += data[3];
          data[4] += data[5];
          sprite.x2 = Sin(data[4] >> 7, data[6]);
          if (data[7] & F_MOVE_HORIZ && data[4] >> 7 > 126)
          {
              sprite.x2 = 0;
              data[7] &= MASK_TARGET_Y;
          }
      }
      sprite.x = data[0] >> 7;
      if (sprite.y + sprite.y2 > (data[7] & MASK_TARGET_Y))
          sprite.callback = SpriteCB_Sparkle_End;
};

/** Source: berry_crush.c → SpriteCB_Sparkle_Init */
export const SpriteCB_Sparkle_Init: SpriteCallback = (sprite, rt) => {
  let data = sprite.data;
      let xMult, xDiv;
      let _var = 0;
      let zero = 0;

      _var = 640;
      data[1] = _var;
      data[2] = 32;
      data[7] = 168;  
      xMult = sprite.x2 * 128;
      xDiv = MathUtil_Div16Shift(7, (168 - sprite.y) << 7, (_var + 32) >> 1);
      sprite.data[0] = sprite.x << 7;
      data[3] = MathUtil_Div16Shift(7, xMult, xDiv);
      _var = MathUtil_Mul16Shift(7, xDiv, 85);
      data[4] = zero;
      data[5] = MathUtil_Div16Shift(7, Q_8_8(63.5), _var);data[6] = Math.trunc(sprite.x2 / 4);
      data[7] |= F_MOVE_HORIZ;
      sprite.y2 = zero;
      sprite.x2 = zero;
      sprite.callback = SpriteCB_Sparkle;
      sprite.animPaused = false;
      sprite.invisible = false;
};

/** Source: berry_crush.c → Task_ShowRankings */
export const Task_ShowRankings: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0, j, xPos, yPos;
      let score = 0;
      const data = task.data;

      switch (data[0])
      {
      case 0:
          data[1] = AddWindow(sWindowTemplate_Rankings);
          PutWindowTilemap(data[1]);
          FillWindowPixelBuffer(data[1], PIXEL_FILL(0));
          LoadUserWindowBorderGfx_(data[1], 541, BG_PLTT_ID(13));
          DrawStdFrameWithCustomTileAndPalette(data[1], false, 541, 13);
          break;
      case 1:
  xPos = Math.trunc(96 - GetStringWidth(FONT_NORMAL, gText_BerryCrush2, -1) / 2);
          AddTextPrinterParameterized3(data[1], FONT_NORMAL, xPos, 1, sTextColorTable[COLORID_BLUE], 0, gText_BerryCrush2);xPos = Math.trunc(96 - GetStringWidth(FONT_NORMAL, gText_PressingSpeedRankings, -1) / 2);
          AddTextPrinterParameterized3(data[1], FONT_NORMAL, xPos, 17, sTextColorTable[COLORID_BLUE], 0, gText_PressingSpeedRankings);

           
          yPos = 41;
          for (i = 0; i < MAX_RFU_PLAYERS - 1; i++)
          {
              ConvertIntToDecimalStringN(gStringVar1, i + 2, STR_CONV_MODE_LEFT_ALIGN, 1);
              StringExpandPlaceholders(gStringVar4, gText_Var1Players);
              AddTextPrinterParameterized3(data[1], FONT_NORMAL, 0, yPos, sTextColorTable[COLORID_GRAY], 0, gStringVar4);
              xPos = 192 - GetStringWidth(FONT_NORMAL, gText_TimesPerSec, -1);
              AddTextPrinterParameterized3(data[1], FONT_NORMAL, xPos, yPos, sTextColorTable[COLORID_GRAY], 0, gText_TimesPerSec);
              for (j = 0; j < 8; j++)
              {
                  if (((tPressingSpeeds(i) & 0xFF) >> (7 - j)) & 1)
                      score += sPressingSpeedConversionTable[j];
              }
              ConvertIntToDecimalStringN(gStringVar1, tPressingSpeeds(i) >> 8, STR_CONV_MODE_RIGHT_ALIGN, 3);
              ConvertIntToDecimalStringN(gStringVar2, score / 1000000, STR_CONV_MODE_LEADING_ZEROS, 2);
              StringExpandPlaceholders(gStringVar4, gText_XDotY3);
              xPos -= GetStringWidth(FONT_NORMAL, gStringVar4, -1);
              AddTextPrinterParameterized3(data[1], FONT_NORMAL, xPos, yPos, sTextColorTable[COLORID_GRAY], 0, gStringVar4);
              yPos += 16;
              score = 0;
          }
          CopyWindowToVram(data[1], COPYWIN_FULL);
          break;
      case 2:
          if (JOY_NEW(A_BUTTON | B_BUTTON))
              break;
          else
              return;
      case 3:
          ClearStdWindowAndFrameToTransparent(data[1], true);
          ClearWindowTilemap(data[1]);
          RemoveWindow(data[1]);
          rt.DestroyTask(taskId);
          ScriptContext_Enable();
          UnlockPlayerFieldControls();
          data[0] = 0;
          return;
      }
      data[0]++;
};
