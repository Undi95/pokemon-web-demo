// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/union_room_chat.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  FreeAllSpritePalettes,
  ResetTasks,
  UpdatePaletteFade,
  gMain,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const CHAT_EXIT_DISBANDED = 3;
const CHAT_EXIT_DROPPED = 2;
const CHAT_EXIT_NONE = 0;
const CHAT_EXIT_ONLY_LEADER = 1;
const CHAT_FUNC_DISBANDED = 8;
const CHAT_FUNC_DROP = 7;
const CHAT_FUNC_EXIT = 6;
const CHAT_MESSAGE_CHAT = 1;
const CHAT_MESSAGE_DISBAND = 5;
const CHAT_MESSAGE_DROP = 4;
const CHAT_MESSAGE_JOIN = 2;
const CHAT_MESSAGE_LEAVE = 3;
const CHATDISPLAY_FUNC_SCROLL_CHAT = 12;
const MAX_MESSAGE_LENGTH = 15;
const MAX_RFU_PLAYERS = 5;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: union_room_chat.c → SpriteCB_TextEntryCursor */
export const SpriteCB_TextEntryCursor: SpriteCallback = (sprite, rt) => {
  let pos = GetTextEntryCursorPosition();
      if (pos == MAX_MESSAGE_LENGTH)
      {
          sprite.invisible = true;
      }
      else
      {
          sprite.invisible = false;
          sprite.x = pos * 8 + 76;
      }
};

/** Source: union_room_chat.c → SpriteCB_TextEntryArrow */
export const SpriteCB_TextEntryArrow: SpriteCallback = (sprite, rt) => {
  if (++sprite.data[0] > 4)
      {
          sprite.data[0] = 0;
          if (++sprite.x2 > 4)
              sprite.x2 = 0;
      }
};

/** Source: union_room_chat.c → Task_HandlePlayerInput */
export const Task_HandlePlayerInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (sChat.exitType)
      {
      case CHAT_EXIT_ONLY_LEADER:
          SetChatFunction(CHAT_FUNC_EXIT);
          sChat.exitType = CHAT_EXIT_NONE;
          break;
      case CHAT_EXIT_DROPPED:
          SetChatFunction(CHAT_FUNC_DROP);
          sChat.exitType = CHAT_EXIT_NONE;
          break;
      case CHAT_EXIT_DISBANDED:
          SetChatFunction(CHAT_FUNC_DISBANDED);
          sChat.exitType = CHAT_EXIT_NONE;
          break;
      }

      sChatMainFunctions[sChat.funcId]();
};

/** Source: union_room_chat.c → Task_ReceiveChatMessage */
export const Task_ReceiveChatMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let buffer = 0;
      const data = task.data;

      switch (data[0])
      {
      case 0:
          if (!gReceivedRemoteLinkPlayers)
          {
              rt.DestroyTask(taskId);
              return;
          }

          data[0] = 1;
           
      case 1:
          data[4] = GetLinkPlayerCount();
          if (sChat.linkPlayerCount != data[4])
          {
              data[0] = 2;
              sChat.linkPlayerCount = data[4];
              return;
          }

          data[3] = GetBlockReceivedStatus();
          if (!data[3] && Rfu_IsPlayerExchangeActive())
              return;

          data[1] = 0;
          data[0] = 3;
           
      case 3:
          for (; data[1] < MAX_RFU_PLAYERS && ((data[3] >> data[1]) & 1) == 0; data[1]++)
              ;

          if (data[1] == MAX_RFU_PLAYERS)
          {
              data[0] = 1;
              return;
          }

          data[2] = data[1];
          ResetBlockReceivedFlag(data[2]);
          buffer = gBlockRecvBuffer[data[1]];
          switch (buffer[0])
          {
              default:
              case CHAT_MESSAGE_CHAT:    data[5] = 3; break;
              case CHAT_MESSAGE_JOIN:    data[5] = 3; break;
              case CHAT_MESSAGE_LEAVE:   data[5] = 4; break;
              case CHAT_MESSAGE_DROP:    data[5] = 5; break;
              case CHAT_MESSAGE_DISBAND: data[5] = 6; break;
          }

          if (ProcessReceivedChatMessage(sChat.receivedMessage, gBlockRecvBuffer[data[1]]))
          {
              sChat.receivedPlayerIndex = data[1];
              StartDisplaySubtask(CHATDISPLAY_FUNC_SCROLL_CHAT, 2);
              data[0] = 7;
          }
          else
          {
              data[0] = data[5];
          }

          data[1]++;
          break;
      case 7:
          if (!IsDisplaySubtaskActive(2))
              data[0] = data[5];
          break;
      case 4:
          if (!sChat.multiplayerId && data[2])
          {
              if (GetLinkPlayerCount() == 2)
              {
                  Rfu_StopPartnerSearch();
                  sChat.exitType = CHAT_EXIT_ONLY_LEADER;
                  rt.DestroyTask(taskId);
                  return;
              }
              Rfu_DisconnectPlayerById(data[2]);
          }

          data[0] = 3;
          break;
      case 5:
          if (sChat.multiplayerId)
              sChat.exitType = CHAT_EXIT_DROPPED;

          rt.DestroyTask(taskId);
          break;
      case 6:
          sChat.exitType = CHAT_EXIT_DISBANDED;
          rt.DestroyTask(taskId);
          break;
      case 2:
          if (!Rfu_IsPlayerExchangeActive())
          {
              if (!sChat.multiplayerId)
                  SetUnionRoomChatPlayerData(sChat.linkPlayerCount);

              data[0] = 1;
          }
          break;
      }
};

/** Source: union_room_chat.c → CB2_LoadInterface */
export const CB2_LoadInterface: CB2Callback = (rt) => {
  switch (gMain.state)
      {
      case 0:
          ResetTasks();
          rt.ResetSpriteData();
          FreeAllSpritePalettes();
          TryAllocDisplay();
          gMain.state++;
          break;
      case 1:
          RunDisplaySubtasks();
          if (!IsDisplaySubtask0Active())
          {
              BlendPalettes(PALETTES_ALL, 16, RGB_BLACK);
              rt.BeginNormalPaletteFade("PALETTES_ALL", -1, 16, 0, "RGB_BLACK");
              rt.SetVBlankCallback(VBlankCB);
              gMain.state++;
          }
          break;
      case 2:
          UpdatePaletteFade();
          if (!rt.gPaletteFade.active)
          {
              rt.SetMainCallback2(CB2_UnionRoomChatMain);
              sChat.handleInputTask = rt.CreateTask((t) => Task_HandlePlayerInput(t, rt), 8);
              sChat.receiveMessagesTask = rt.CreateTask((t) => Task_ReceiveChatMessage(t, rt), 7);
              LoadWirelessStatusIndicatorSpriteGfx();
              CreateWirelessStatusIndicatorSprite(DISPLAY_WIDTH - 8, DISPLAY_HEIGHT - 10);
          }
          break;
      }
};

/** Source: union_room_chat.c → CB2_UnionRoomChatMain */
export const CB2_UnionRoomChatMain: CB2Callback = (rt) => {
  RunTasks();
      RunDisplaySubtasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
