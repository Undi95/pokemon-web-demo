// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/cable_car.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const INTR_FLAG_VBLANK = 1;
const MUS_CABLE_CAR = 425;
const NUM_ASH_SPRITES = 20;
const OAM = 117440512;
const OAM_SIZE = 1024;
const PLTT = 83886080;
const PLTT_SIZE = 1024;
const STATE_END = 9;
const VRAM = 100663296;
const VRAM_SIZE = 98304;
const WEATHER_NONE = 0;
const WEATHER_SUNNY = 2;
const WEATHER_VOLCANIC_ASH = 7;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const FREE_AND_SET_NULL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const GOING_DOWN: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_IE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_IME: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const S16TOPOSFLOAT: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: cable_car.c (failed: empty bodyC) */
export const SpriteCB_Cable: SpriteCallback = (_sprite, _rt) => { /* TODO empty bodyC */ };

/** Source: cable_car.c → SpriteCB_CableCar */
export const SpriteCB_CableCar: SpriteCallback = (sprite, rt) => {
  if (sCableCar.state != STATE_END)
      {
          if (!GOING_DOWN)
          {
              sprite.x = sprite.data[0] - (0.14 * S16TOPOSFLOAT(sCableCar.timer));
              sprite.y = sprite.data[1] - (0.67 * S16TOPOSFLOAT(sCableCar.timer));
          }
          else
          {
              sprite.x = sprite.data[0] + (0.14 * S16TOPOSFLOAT(sCableCar.timer));
              sprite.y = sprite.data[1] + (0.67 * S16TOPOSFLOAT(sCableCar.timer));
          }
      }
};

/** Source: cable_car.c → SpriteCB_Player */
export const SpriteCB_Player: SpriteCallback = (sprite, rt) => {
  if (sCableCar.state != STATE_END)
      {
           
          if (!GOING_DOWN)
          {
              sprite.x = sprite.data[0] - (0.14 * S16TOPOSFLOAT(sCableCar.timer));
              sprite.y = sprite.data[1] - (0.67 * S16TOPOSFLOAT(sCableCar.timer));
          }
          else
          {
              sprite.x = sprite.data[0] + (0.14 * S16TOPOSFLOAT(sCableCar.timer));
              sprite.y = sprite.data[1] + (0.67 * S16TOPOSFLOAT(sCableCar.timer));
          }

           
          switch (sprite.data[2])
          {
          case 0:
              sprite.y2 = 17;
              if (sprite.data[0]++ > 9)
              {
                  sprite.data[0] = 0;
                  sprite.data[2]++;
              }
              break;
          default:
              sprite.y2 = 16;
              if (sprite.data[0]++ > 9)
              {
                  sprite.data[0] = 0;
                  sprite.data[2] = 0;
              }
              break;
          }
      }
};

/** Source: cable_car.c → SpriteCB_HikerGoingUp */
export const SpriteCB_HikerGoingUp: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0] == 0)
      {
          sprite.x += 2 * sprite.centerToCornerVecX;
          sprite.y += 16 + sprite.centerToCornerVecY;
      }

      if (++sprite.data[0] >= sprite.data[2])
      {
          switch (sprite.data[1])
          {
          case false:
              sprite.x++;
              if ((sprite.data[0] % 4) == 0)
                  sprite.y++;
              break;
          case true:
               
              if ((sprite.data[0] % 2) != 0)
              {
                  sprite.x++;
                  if ((sprite.x % 4) == 0)
                      sprite.y++;
              }
              break;
          }

          if (sprite.y > DISPLAY_HEIGHT)
              rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: cable_car.c → SpriteCB_HikerGoingDown */
export const SpriteCB_HikerGoingDown: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0] == 0)
          sprite.y += 16 + sprite.centerToCornerVecY;

      if (++sprite.data[0] >= sprite.data[2])
      {
          switch (sprite.data[1])
          {
          case false:
              sprite.x--;
              if ((sprite.data[0] % 4) == 0)
                  sprite.y--;
              break;
          case true:
               
              if ((sprite.data[0] % 2) != 0)
              {
                  sprite.x--;
                  if ((sprite.x % 4) == 0)
                      sprite.y--;
              }
              break;
          }

          if (sprite.y < 80)
              rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: cable_car.c → Task_LoadCableCar */
export const Task_LoadCableCar: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          /* TODO scene transition: SetMainCallback2(CB2_LoadCableCar) */;
          rt.DestroyTask(taskId);
      }
};

/** Source: cable_car.c → Task_CableCar */
export const Task_CableCar: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      sCableCar.timer++;
      switch (sCableCar.state)
      {
      case 0:
           
          if (sCableCar.timer == sCableCar.weatherDelay)
          {
              SetNextWeather(sCableCar.weather);
              sCableCar.state = 1;
          }
          break;
      case 1:
           
          switch (sCableCar.weather)
          {
          case WEATHER_VOLCANIC_ASH:
              if (gWeatherPtr.sprites.s2.ashSprites[0] != null && gWeatherPtr.sprites.s2.ashSprites[0].oam.priority != 0)
              {
                  for (; i < NUM_ASH_SPRITES; i++)
                  {
                      if (gWeatherPtr.sprites.s2.ashSprites[i])
                          gWeatherPtr.sprites.s2.ashSprites[i].oam.priority = 0;
                  }

                  sCableCar.state = 2;
              }
              break;
          case WEATHER_SUNNY:
              if (gWeatherPtr.currWeather == WEATHER_SUNNY)
              {
                  sCableCar.state = 2;
              }
              else if (sCableCar.timer >= sCableCar.weatherDelay + 8)
              {
                  for (; i < NUM_ASH_SPRITES; i++)
                  {
                      if (gWeatherPtr.sprites.s2.ashSprites[i])
                          gWeatherPtr.sprites.s2.ashSprites[i].invisible ^= 1;
                  }
              }
              break;
          }
          break;
      case 2:
           
          if (sCableCar.timer == 570)
          {
              sCableCar.state = 3;
              rt.BeginNormalPaletteFade("PALETTES_ALL", 3, 0, 16, "RGB_BLACK");
              FadeOutBGM(4);
          }
          break;
      case 3:
           
          if (!rt.gPaletteFade.active)
              sCableCar.state = STATE_END;
          break;
      case STATE_END:
          /* noop SetVBlankCallback */;
          rt.DestroyTask(taskId);
          rt.DestroyTask(sCableCar.bgTaskId);
          /* TODO scene transition: SetMainCallback2(CB2_EndCableCar) */;
          break;
      }
};

/** Source: cable_car.c → Task_AnimateBgGoingUp */
export const Task_AnimateBgGoingUp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (sCableCar.state != STATE_END)
      {
          sCableCar.bg3HorizontalOffset--;
          if ((sCableCar.timer % 2) == 0)
              sCableCar.bg3VerticalOffset--;

          if ((sCableCar.timer % 8) == 0)
          {
              sCableCar.bg1HorizontalOffset--;
              sCableCar.bg1VerticalOffset--;
          }

          switch (sCableCar.bg3HorizontalOffset)
          {
          case 175:
              FillBgTilemapBufferRect(3, 0, 0, 22, 2, 10, 17);
              break;
          case 40:
              FillBgTilemapBufferRect(3, 0, 3, 0, 2, 2, 17);
              break;
          case 32:
              FillBgTilemapBufferRect(3, 0, 2, 0, 1, 2, 17);
              break;
          case 16:
              CopyToBgTilemapBufferRect_ChangePalette(3, sCableCar.pylonTopTilemap, 0, 0, 5,  2, 17);
              CopyToBgTilemapBufferRect_ChangePalette(3, sCableCar.pylonPoleTilemap, 0, 2, 2, 30, 17);
              sCableCar.bg3VerticalOffset = 64;
              break;
          }
      }

      AnimateGroundGoingUp();
      gSpriteCoordOffsetX = (gSpriteCoordOffsetX + 1) % 128;
};

/** Source: cable_car.c → Task_AnimateBgGoingDown */
export const Task_AnimateBgGoingDown: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (sCableCar.state != STATE_END)
      {
          sCableCar.bg3HorizontalOffset++;
          if ((sCableCar.timer % 2) == 0)
              sCableCar.bg3VerticalOffset++;

          if ((sCableCar.timer % 8) == 0)
          {
              sCableCar.bg1HorizontalOffset++;
              sCableCar.bg1VerticalOffset++;
          }

          switch (sCableCar.bg3HorizontalOffset)
          {
          case 176:
              CopyToBgTilemapBufferRect_ChangePalette(3, sCableCar.pylonPoleTilemap, 0, 2, 2, 30, 17);
              break;
          case 16:
              FillBgTilemapBufferRect(3, 0, 2,  0, 3,  2, 17);
              FillBgTilemapBufferRect(3, 0, 0, 22, 2, 10, 17);
              sCableCar.bg3VerticalOffset = 192;
              break;
          case 32:
              FillBgTilemapBufferRect(3, sCableCar.pylonTopTilemap[2], 2, 0, 1, 1, 17);
              FillBgTilemapBufferRect(3, sCableCar.pylonTopTilemap[3], 3, 0, 1, 1, 17);
              FillBgTilemapBufferRect(3, sCableCar.pylonTopTilemap[7], 2, 1, 1, 1, 17);
              FillBgTilemapBufferRect(3, sCableCar.pylonTopTilemap[8], 3, 1, 1, 1, 17);
              break;
          case 40:
              FillBgTilemapBufferRect(3, sCableCar.pylonTopTilemap[4], 4, 0, 1, 1, 17);
              FillBgTilemapBufferRect(3, sCableCar.pylonTopTilemap[9], 4, 1, 1, 1, 17);
              break;
          }
      }

      AnimateGroundGoingDown();
      if (sCableCar.timer < sCableCar.weatherDelay)
          gSpriteCoordOffsetX = (gSpriteCoordOffsetX + 247) % 248;
      else
          gWeatherPtr.ashBaseSpritesX = (gWeatherPtr.ashBaseSpritesX + 247) % 248;
};

/** Source: cable_car.c → CB2_LoadCableCar */
export const CB2_LoadCableCar: CB2Callback = (rt) => {
  let imebak = 0;
      let i = 0;
      let sizeOut = 0;

      switch (gMain.state)
      {
      case 0:
      default:
          /* noop SetVBlankCallback */;
          SetBgRegs(false);
          ScanlineEffect_Stop();
          DmaFillLarge16(3, 0, VRAM, VRAM_SIZE, 0x1000);
          DmaFill32Defvars(3, 0, OAM, OAM_SIZE);
          DmaFill16Defvars(3, 0, PLTT, PLTT_SIZE);
          sCableCar = AllocZeroed(((sCableCar)?.length ?? 32));
          gMain.state++;
          break;
      case 1:
          rt.ResetSpriteData();
          ResetTasks();
          /* TODO FreeAllSpritePalettes */;
          ResetPaletteFade();
          ResetTempTileDataBuffers();
          StartWeather();
          for (i = 0; i < NUM_ASH_SPRITES; i++)
              gWeatherPtr.sprites.s2.ashSprites[i] = null;

          InitMapMusic();
          ResetMapMusic();
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sBgTemplates, ((sBgTemplates)?.length ?? 0));
          SetBgTilemapBuffer(0, sCableCar.bgTilemapBuffers[0]);
          SetBgTilemapBuffer(1, sCableCar.bgTilemapBuffers[1]);
          SetBgTilemapBuffer(2, sCableCar.bgTilemapBuffers[2]);
          SetBgTilemapBuffer(3, sCableCar.bgTilemapBuffers[3]);
          gSpriteCoordOffsetX = gSpriteCoordOffsetY = 0;
          gMain.state++;
          break;
      case 2:
          for (i = 0; i < ((sSpriteSheets)?.length ?? 0) - 1; i++)
              /* TODO LoadCompressedSpriteSheet — load via rt.LoadCompressedSpriteSheetsFromTable */;

          /* TODO LoadSpritePalettes — load via rt.LoadSpritePalettesFromTable */;
          sCableCar.groundTilemap = malloc_and_decompress(sGround_Tilemap, sizeOut);
          sCableCar.treesTilemap = malloc_and_decompress(sTrees_Tilemap, sizeOut);
          sCableCar.bgMountainsTilemap = malloc_and_decompress(sBgMountains_Tilemap, sizeOut);
          sCableCar.pylonPoleTilemap = malloc_and_decompress(sPylonPole_Tilemap, sizeOut);
          sCableCar.pylonTopTilemap = sPylonTop_Tilemap;
          DecompressAndCopyTileDataToVram(0, gCableCarBg_Gfx, 0, 0, 0);
          gMain.state++;
          break;
      case 3:
          if (!FreeTempTileDataBuffersIfPossible())
          {
              LoadPalette(gCableCarBg_Pal, BG_PLTT_ID(0), 4 * PLTT_SIZE_4BPP);
              gMain.state++;
          }
          break;
      case 4:
          CreateCableCarSprites();
          RunTasks();
          gMain.state++;
          break;
      case 5:
          if (sCableCar.weather == WEATHER_VOLCANIC_ASH)
          {
              gMain.state++;
          }
          else if (gWeatherPtr.sprites.s2.ashSprites[0])
          {
              for (i = 0; i < NUM_ASH_SPRITES; i++)
              {
                  if (gWeatherPtr.sprites.s2.ashSprites[i])
                      gWeatherPtr.sprites.s2.ashSprites[i].oam.priority = 0;
              }

              gMain.state++;
          }
          break;
      case 6:
          CopyToBgTilemapBufferRect_ChangePalette(1, sCableCar.treesTilemap, 0, 17, 32, 15, 17);
          CopyToBgTilemapBufferRect_ChangePalette(2, sCableCar.bgMountainsTilemap, 0, 0, 30, 20, 17);
          CopyToBgTilemapBufferRect_ChangePalette(3, sCableCar.pylonTopTilemap, 0, 0, 5, 2, 17);
          CopyToBgTilemapBufferRect_ChangePalette(3, sCableCar.pylonPoleTilemap, 0, 2, 2, 20, 17);
          gMain.state++;
          break;
      case 7:
          InitGroundTilemapData(GOING_DOWN);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +  0x48,  0, 14, 12, 3, 17);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +  0x6C, 12, 17, 12, 3, 17);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +  0x90, 24, 20, 12, 3, 17);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +   0x0,  0, 17, 12, 3, 17);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +  0x24,  0, 20, 12, 3, 17);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +   0x0, 12, 20, 12, 3, 17);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +  0x24, 12, 23, 12, 3, 17);
          CopyToBgTilemapBufferRect_ChangePalette(0, sCableCar.groundTilemap +   0x0, 24, 23, 12, 3, 17);
          gMain.state++;
          break;
      case 8:
          rt.BeginNormalPaletteFade("PALETTES_ALL", 3, 16, 0, "RGB_BLACK");
          FadeInNewBGM(MUS_CABLE_CAR, 1);
          SetBgRegs(true);
          gMain.state++;
          break;
      case 9:
          imebak = REG_IME;
          REG_IME = 0;
          REG_IE |= INTR_FLAG_VBLANK;
          REG_IME = imebak;
          /* noop SetVBlankCallback */;
          /* TODO scene transition: SetMainCallback2(CB2_CableCar) */;
          rt.CreateTask((t) => Task_CableCar(t, rt), 0);
          if (!GOING_DOWN)
              sCableCar.bgTaskId = rt.CreateTask((t) => Task_AnimateBgGoingUp(t, rt), 1);
          else
              sCableCar.bgTaskId = rt.CreateTask((t) => Task_AnimateBgGoingDown(t, rt), 1);
          break;
      }
};

/** Source: cable_car.c → CB2_CableCar */
export const CB2_CableCar: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
      MapMusicMain();
};

/** Source: cable_car.c → CB2_EndCableCar */
export const CB2_EndCableCar: CB2Callback = (rt) => {
  let i = 0;

      HideBg(0);
      HideBg(1);
      HideBg(2);
      HideBg(3);
      SetBgRegs(false);
      gSpriteCoordOffsetX = 0;
      SetCurrentAndNextWeatherNoDelay(WEATHER_NONE);
      for (i = 0; i < NUM_ASH_SPRITES; i++)
          gWeatherPtr.sprites.s2.ashSprites[i] = null;

      ResetTasks();
      rt.ResetSpriteData();
      ResetPaletteFade();
      UnsetBgTilemapBuffer(0);
      UnsetBgTilemapBuffer(1);
      UnsetBgTilemapBuffer(2);
      UnsetBgTilemapBuffer(3);
      ResetBgsAndClearDma3BusyFlags(0);
      sCableCar.pylonTopTilemap = null;
      FREE_AND_SET_NULL(sCableCar.pylonPoleTilemap);
      FREE_AND_SET_NULL(sCableCar.bgMountainsTilemap);
      FREE_AND_SET_NULL(sCableCar.treesTilemap);
      FREE_AND_SET_NULL(sCableCar.groundTilemap);
      FREE_AND_SET_NULL(sCableCar);
      DmaFillLarge16(3, 0, VRAM, VRAM_SIZE, 0x1000);
      DmaFill32Defvars(3, 0, OAM, OAM_SIZE);
      DmaFill16Defvars(3, 0, PLTT, PLTT_SIZE);
      WarpIntoMap();
      gFieldCallback = null;
      /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
};
