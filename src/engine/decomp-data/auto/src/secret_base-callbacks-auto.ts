// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/secret_base.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const VAR_CURRENT_SECRET_BASE = 16468;
const WARP_ID_NONE = -1;
const WARP_ID_SECRET_BASE = 126;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const GET_BASE_COMPUTER_X: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const GET_BASE_COMPUTER_Y: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const SECRET_BASE_ID_TO_GROUP: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: secret_base.c → Task_EnterSecretBase */
export const Task_EnterSecretBase: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let secretBaseIdx = 0;

      switch (task.data[1])
      {
      case 0:
          if (!rt.gPaletteFade.active)
              task.data[1] = 1;
          break;
      case 1:
          secretBaseIdx = VarGet(VAR_CURRENT_SECRET_BASE);
          if (gSaveBlock1Ptr.secretBases[secretBaseIdx].numTimesEntered < 255)
              gSaveBlock1Ptr.secretBases[secretBaseIdx].numTimesEntered++;

          SetSecretBaseWarpDestination();
          WarpIntoMap();
          gFieldCallback = FieldCB_ContinueScriptHandleMusic;
          /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: secret_base.c → Task_EnterNewlyCreatedSecretBase */
export const Task_EnterNewlyCreatedSecretBase: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          let secretBaseGroup = SECRET_BASE_ID_TO_GROUP(sCurSecretBaseId);
          SetWarpDestination(
              gSaveBlock1Ptr.location.mapGroup,
              gSaveBlock1Ptr.location.mapNum,
              WARP_ID_NONE,
              GET_BASE_COMPUTER_X(secretBaseGroup),
              GET_BASE_COMPUTER_Y(secretBaseGroup));
          WarpIntoMap();
          gFieldCallback = EnterNewlyCreatedSecretBase_StartFadeIn;
          /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
          rt.DestroyTask(taskId);
      }
};

/** Source: secret_base.c → Task_WarpOutOfSecretBase */
export const Task_WarpOutOfSecretBase: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[0])
      {
      case 0:
          LockPlayerFieldControls();
          task.data[0] = 1;
          break;
      case 1:
          if (!rt.gPaletteFade.active)
              task.data[0] = 2;
          break;
      case 2:
          SetWarpDestinationToDynamicWarp(WARP_ID_SECRET_BASE);
          WarpIntoMap();
          gFieldCallback = FieldCB_DefaultWarpExit;
          /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
          UnlockPlayerFieldControls();
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: secret_base.c → Task_ShowSecretBaseRegistryMenu */
export const Task_ShowSecretBaseRegistryMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      LockPlayerFieldControls();
      data[0] = GetNumRegisteredSecretBases();
      if (data[0] != 0)
      {
          data[1] = 0;
          data[2] = 0;
          ClearDialogWindowAndFrame(0, false);
          sRegistryMenu = AllocZeroed(((sRegistryMenu)?.length ?? 32));
          data[6] = AddWindow(sRegistryWindowTemplates[0]);
          BuildRegistryMenuItems(taskId);
          FinalizeRegistryMenu(taskId);
          task.func = (t) => HandleRegistryMenuInput(t, rt);
      }
      else
      {
          DisplayItemMessageOnField(taskId, gText_NoRegistry, GoToSecretBasePCRegisterMenu);
      }
};
