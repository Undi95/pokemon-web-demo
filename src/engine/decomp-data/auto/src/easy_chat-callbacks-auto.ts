// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/easy_chat.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  UpdatePaletteFade,
  gMain,
  gSaveBlock1Ptr,
  m4aSongNumStart,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const EC_EMPTY_WORD = 65535;
const ECFUNC_EXIT = 24;
const ECFUNC_NONE = 0;
const FADE_TO_BLACK = 1;
const MAINSTATE_EXIT = 6;
const MAINSTATE_FADE_IN = 0;
const MAINSTATE_HANDLE_INPUT = 1;
const MAINSTATE_RUN_FUNC = 2;
const MAINSTATE_TO_QUIZ_LADY = 3;
const MAINSTATE_WAIT_FADE_IN = 5;
const SE_SELECT = 5;
const TASKIDX_EXIT_CALLBACK = 4;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: easy_chat.c → SpriteCB_Cursor */
export const SpriteCB_Cursor: SpriteCallback = (sprite, rt) => {
  if (sprite.data[1])
      {
          if (++sprite.data[0] > 2)
          {
              sprite.data[0] = 0;
              if (++sprite.x2 > 0)
                  sprite.x2 = -6;
          }
      }
};

/** Source: easy_chat.c → SpriteCB_WordSelectCursor */
export const SpriteCB_WordSelectCursor: SpriteCallback = (sprite, rt) => {
  if (++sprite.data[0] > 2)
      {
          sprite.data[0] = 0;
          if (++sprite.x2 > 0)
              sprite.x2 = -6;
      }
};

/** Source: easy_chat.c → Task_InitEasyChatScreen */
export const Task_InitEasyChatScreen: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!IsOverworldLinkActive())
      {
          while (InitEasyChatScreen(taskId));
      }
      else
      {
          if (InitEasyChatScreen(taskId) == true)
              return;
      }
      StartEasyChatScreen(taskId, Task_EasyChatScreen);
};

/** Source: easy_chat.c → Task_EasyChatScreen */
export const Task_EasyChatScreen: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let funcId = 0;
      let data = 0;

      data = task.data;
      switch (data[0])
      {
      case MAINSTATE_FADE_IN:
          rt.SetVBlankCallback(VBlankCB);
          BlendPalettes(PALETTES_ALL, 16, 0);
          rt.BeginNormalPaletteFade("PALETTES_ALL", -1, 16, 0, "RGB_BLACK");
          data[0] = MAINSTATE_WAIT_FADE_IN;
          break;
      case MAINSTATE_HANDLE_INPUT:
          funcId = HandleEasyChatInput();
          if (IsFuncIdForQuizLadyScreen(funcId))
          {
               
              rt.BeginNormalPaletteFade("PALETTES_ALL", -2, 0, 16, "RGB_BLACK");
              data[0] = MAINSTATE_TO_QUIZ_LADY;
              data[6] = funcId;
          }
          else if (funcId == ECFUNC_EXIT)
          {
               
              rt.BeginNormalPaletteFade("PALETTES_ALL", -1, 0, 16, "RGB_BLACK");
              data[0] = MAINSTATE_EXIT;
          }
          else if (funcId != ECFUNC_NONE)
          {
              PlaySE(SE_SELECT);
              StartEasyChatFunction(funcId);
              data[0]++;  
          }
          break;
      case MAINSTATE_RUN_FUNC:
          if (!RunEasyChatFunction())
              data[0] = MAINSTATE_HANDLE_INPUT;
          break;
      case MAINSTATE_TO_QUIZ_LADY:
          if (!rt.gPaletteFade.active)
              EnterQuizLadyScreen(data[6]);
          break;
      case MAINSTATE_EXIT:
          if (!rt.gPaletteFade.active)
              ExitEasyChatScreen(GetWordTaskArg(taskId, TASKIDX_EXIT_CALLBACK));
          break;
      case MAINSTATE_WAIT_FADE_IN:
          if (!rt.gPaletteFade.active)
              data[0] = MAINSTATE_HANDLE_INPUT;
          break;
      }
};

/** Source: easy_chat.c → CB2_EasyChatScreen */
export const CB2_EasyChatScreen: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** Source: easy_chat.c → CB2_QuizLadyQuestion */
export const CB2_QuizLadyQuestion: CB2Callback = (rt) => {
  let lilycoveLady: any = null;
      UpdatePaletteFade();
      switch (gMain.state)
      {
      case 0:
          FadeScreen(FADE_TO_BLACK, 0);
          break;
      case 1:
          if (!rt.gPaletteFade.active)
          {
              lilycoveLady = gSaveBlock1Ptr.lilycoveLady;
              lilycoveLady.quiz.playerAnswer = EC_EMPTY_WORD;
              CleanupOverworldWindowsAndTilemaps();
              DoQuizQuestionEasyChatScreen();
          }
          return;
      }
      gMain.state ++;
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
