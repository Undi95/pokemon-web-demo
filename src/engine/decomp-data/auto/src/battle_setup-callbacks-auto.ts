// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_setup.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  CpuFill16,
  FreeAllWindowBuffers,
  ResetTasks,
  UpdatePaletteFade,
  gMain,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const B_TRANSITION_BLUR = 0;
const BATTLE_TYPE_FIRST_BATTLE = 16;
const BG_PLTT = 83886080;
const BG_PLTT_SIZE = 512;
const GAME_STAT_TOTAL_BATTLES = 7;
const GAME_STAT_WILD_BATTLES = 8;
const ITEM_NONE = 0;
const PYRAMID_LOCATION_NONE = 0;
const TRAINER_SECRET_BASE = 1024;
const VAR_STARTER_MON = 16419;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_setup.c → Task_BattleStart */
export const Task_BattleStart: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 0:
          if (!FldEffPoison_IsActive())  
          {
              BattleTransition_StartOnField(data[1]);
              ClearMirageTowerPulseBlendEffect();
              data[0]++;  
          }
          break;
      case 1:
          if (IsBattleTransitionDone() == true)
          {
              CleanupOverworldWindowsAndTilemaps();
              /* TODO scene transition: SetMainCallback2(CB2_InitBattle) */;
              RestartWildEncounterImmunitySteps();
              ClearPoisonStepCounter();
              rt.DestroyTask(taskId);
          }
          break;
      }
};

/** Source: battle_setup.c → CB2_EndWildBattle */
export const CB2_EndWildBattle: CB2Callback = (rt) => {
  CpuFill16(0, (BG_PLTT), BG_PLTT_SIZE);
      ResetOamRange(0, 128);

      if (IsPlayerDefeated(gBattleOutcome) == true && CurrentBattlePyramidLocation() == PYRAMID_LOCATION_NONE && !InBattlePike())
      {
          /* TODO scene transition: SetMainCallback2(CB2_WhiteOut) */;
      }
      else
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToField) */;
          gFieldCallback = FieldCB_ReturnToFieldNoScriptCheckMusic;
      }
};

/** Source: battle_setup.c → CB2_EndScriptedWildBattle */
export const CB2_EndScriptedWildBattle: CB2Callback = (rt) => {
  CpuFill16(0, (BG_PLTT), BG_PLTT_SIZE);
      ResetOamRange(0, 128);

      if (IsPlayerDefeated(gBattleOutcome) == true)
      {
          if (CurrentBattlePyramidLocation() != PYRAMID_LOCATION_NONE)
              /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
          else
              /* TODO scene transition: SetMainCallback2(CB2_WhiteOut) */;
      }
      else
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
      }
};

/** Source: battle_setup.c → CB2_GiveStarter */
export const CB2_GiveStarter: CB2Callback = (rt) => {
  let starterMon = 0;

      GetVarPointer(VAR_STARTER_MON) = gSpecialVar_Result;
      starterMon = GetStarterPokemon(gSpecialVar_Result);
      ScriptGiveMon(starterMon, 5, ITEM_NONE, 0, 0, 0);
      ResetTasks();
      PlayBattleBGM();
      /* TODO scene transition: SetMainCallback2(CB2_StartFirstBattle) */;
      BattleTransition_Start(B_TRANSITION_BLUR);
};

/** Source: battle_setup.c → CB2_StartFirstBattle */
export const CB2_StartFirstBattle: CB2Callback = (rt) => {
  UpdatePaletteFade();
      RunTasks();

      if (IsBattleTransitionDone() == true)
      {
          gBattleTypeFlags = BATTLE_TYPE_FIRST_BATTLE;
          gMain.savedCallback = CB2_EndFirstBattle;
          FreeAllWindowBuffers();
          /* TODO scene transition: SetMainCallback2(CB2_InitBattle) */;
          RestartWildEncounterImmunitySteps();
          ClearPoisonStepCounter();
          IncrementGameStat(GAME_STAT_TOTAL_BATTLES);
          IncrementGameStat(GAME_STAT_WILD_BATTLES);
          IncrementDailyWildBattles();
          TryUpdateGymLeaderRematchFromWild();
      }
};

/** Source: battle_setup.c → CB2_EndFirstBattle */
export const CB2_EndFirstBattle: CB2Callback = (rt) => {
  Overworld_ClearSavedMusic();
      /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
};

/** Source: battle_setup.c → CB2_EndTrainerBattle */
export const CB2_EndTrainerBattle: CB2Callback = (rt) => {
  if (gTrainerBattleOpponent_A == TRAINER_SECRET_BASE)
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
      }
      else if (IsPlayerDefeated(gBattleOutcome) == true)
      {
          if (CurrentBattlePyramidLocation() != PYRAMID_LOCATION_NONE || InTrainerHillChallenge())
              /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
          else
              /* TODO scene transition: SetMainCallback2(CB2_WhiteOut) */;
      }
      else
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
          if (CurrentBattlePyramidLocation() == PYRAMID_LOCATION_NONE && !InTrainerHillChallenge())
          {
              RegisterTrainerInMatchCall();
              SetBattledTrainersFlags();
          }
      }
};

/** Source: battle_setup.c → CB2_EndRematchBattle */
export const CB2_EndRematchBattle: CB2Callback = (rt) => {
  if (gTrainerBattleOpponent_A == TRAINER_SECRET_BASE)
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
      }
      else if (IsPlayerDefeated(gBattleOutcome) == true)
      {
          /* TODO scene transition: SetMainCallback2(CB2_WhiteOut) */;
      }
      else
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
          RegisterTrainerInMatchCall();
          SetBattledTrainersFlags();
          HandleRematchVarsOnBattleEnd();
      }
};
