// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_factory_screen.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  ChangeBgX,
  ChangeBgY,
  CopyBgTilemapBufferToVram,
  CpuFill32,
  DeactivateAllTextPrinters,
  FreeAllSpritePalettes,
  HideBg,
  InitBgsFromTemplates,
  InitWindows,
  LoadBgTiles,
  LoadCompressedSpriteSheet,
  LoadPalette,
  LoadSpritePalettes,
  PutWindowTilemap,
  ResetAllPicSprites,
  ResetBgsAndClearDma3BusyFlags,
  ResetPaletteFade,
  ResetTasks,
  RunTextPrinters,
  ShowBg,
  UpdatePaletteFade,
  VRAM,
  VRAM_SIZE,
  WININ_WIN0_BG_ALL,
  WININ_WIN0_OBJ,
  WINOUT_WIN01_CLR,
  WINOUT_WIN01_OBJ,
  gMain,
  gPlttBufferUnfaded,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const BG_COORD_SET = 0;
const FADESTATE_INIT = 0;
const FADESTATE_RUN = 1;
const PALNUM_FADE_TEXT = 14;
const PALNUM_TEXT = 15;
const PALTAG_BALL_SELECTED = 101;
const REG_OFFSET_MOSAIC = 76;
const SELECT_WIN_INFO = 2;
const SELECT_WIN_MON_CATEGORY = 5;
const SELECT_WIN_SPECIES = 1;
const SELECT_WIN_TITLE = 0;
const STATE_CHOOSE_MONS_INIT = 0;
const STATE_MENU_INIT = 2;
const STATE_MENU_RESHOW = 13;
const SWAP_PLAYER_SCREEN = 0;
const SWAP_WIN_INFO = 2;
const SWAP_WIN_MON_CATEGORY = 8;
const SWAP_WIN_OPTIONS = 3;
const SWAP_WIN_SPECIES = 1;
const SWAP_WIN_TITLE = 0;
const WININ_WIN0_CLR = 32;
const WINOUT_WIN01_BG0 = 1;
const WINOUT_WIN01_BG1 = 2;
const WINOUT_WIN01_BG2 = 4;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const TRY_FREE_AND_SET_NULL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_factory_screen.c → SpriteCB_Pokeball */
export const SpriteCB_Pokeball: SpriteCallback = (sprite, rt) => {
  if (rt.gba.oam[sprite.oamIndex].paletteBank == IndexOfSpritePaletteTag(PALTAG_BALL_SELECTED))
      {
           
          if (sprite.animEnded)
          {
              if (sprite.data[0] != 0)
              {
                  sprite.data[0]--;
              }
              else if (Math.floor(Math.random() * 0x10000) % 5 == 0)
              {
                  rt.StartSpriteAnim(sprite.spriteId, 0);
                  sprite.data[0] = 32;
              }
              else
              {
                  rt.StartSpriteAnim(sprite.spriteId, 1);
              }
          }
          else
          {
              rt.StartSpriteAnim(sprite.spriteId, 1);
          }
      }
      else
      {
           
          rt.StartSpriteAnim(sprite.spriteId, 0);
      }
};

/** Source: battle_factory_screen.c → SpriteCB_OpenChosenMonPics */
export const SpriteCB_OpenChosenMonPics: SpriteCallback = (sprite, rt) => {
  let taskId = 0;

       
      if (sprite.affineAnimEnded
          && _gs(rt, sFactorySelectScreen.monPics[0].bgSpriteId).affineAnimEnded
          && _gs(rt, sFactorySelectScreen.monPics[2].bgSpriteId).affineAnimEnded)
      {
          sprite.invisible = true;
          _gs(rt, sFactorySelectScreen.monPics[0].bgSpriteId).invisible = true;
          _gs(rt, sFactorySelectScreen.monPics[2].bgSpriteId).invisible = true;

          taskId = rt.CreateTask((t) => Select_Task_OpenChosenMonPics(t, rt), 1);
          task.func(taskId);

          sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: battle_factory_screen.c → SpriteCB_CloseChosenMonPics */
export const SpriteCB_CloseChosenMonPics: SpriteCallback = (sprite, rt) => {
  if (sprite.affineAnimEnded
          && _gs(rt, sFactorySelectScreen.monPics[0].bgSpriteId).affineAnimEnded
          && _gs(rt, sFactorySelectScreen.monPics[2].bgSpriteId).affineAnimEnded)
      {
          FreeOamMatrix(rt.gba.oam[sprite.oamIndex].affineParamIndex);
          FreeOamMatrix(rt.gba.oam[_gs(rt, sFactorySelectScreen.monPics[0].bgSpriteId).oamIndex].affineParamIndex);
          FreeOamMatrix(rt.gba.oam[_gs(rt, sFactorySelectScreen.monPics[2].bgSpriteId).oamIndex].affineParamIndex);

          sFactorySelectScreen.monPicAnimating = false;

          rt.DestroySprite(sFactorySelectScreen.monPics[0].bgSpriteId);
          rt.DestroySprite(sFactorySelectScreen.monPics[2].bgSpriteId);
          rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: battle_factory_screen.c → SpriteCB_OpenMonPic */
export const SpriteCB_OpenMonPic: SpriteCallback = (sprite, rt) => {
  let taskId = 0;

      if (sprite.affineAnimEnded)
      {
          sprite.invisible = true;
          taskId = rt.CreateTask((t) => Task_OpenMonPic(t, rt), 1);
          task.data[7] = sprite.data[7];
          task.func(taskId);
          sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: battle_factory_screen.c → SpriteCB_CloseMonPic */
export const SpriteCB_CloseMonPic: SpriteCallback = (sprite, rt) => {
  if (sprite.affineAnimEnded)
      {
          FreeOamMatrix(rt.gba.oam[sprite.oamIndex].affineParamIndex);
          if (sprite.data[7] == true)
              sFactorySwapScreen.monPicAnimating = false;
          else
              Select_SetMonPicAnimating(false);
          rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: battle_factory_screen.c → Task_OpenMonPic */
export const Task_OpenMonPic: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      switch (task.data[0])
      {
      case 0:
           
          task.data[3] = 88;
          task.data[4] = DISPLAY_WIDTH - 88;
          task.data[5] = 64;
          task.data[8] = 65;
          SetGpuRegBits(REG_OFFSET_DISPCNT, DISPCNT_WIN0_ON);
          rt.SetGpuReg(REG_OFFSET_WIN0H, WIN_RANGE(task.data[3], task.data[4]));
          rt.SetGpuReg(REG_OFFSET_WIN0V, WIN_RANGE(task.data[5], task.data[8]));
          rt.SetGpuReg(REG_OFFSET_WININ, WININ_WIN0_BG_ALL | WININ_WIN0_CLR | WININ_WIN0_OBJ);
          rt.SetGpuReg(REG_OFFSET_WINOUT, WINOUT_WIN01_BG0 | WINOUT_WIN01_BG1 | WINOUT_WIN01_BG2 | WINOUT_WIN01_CLR | WINOUT_WIN01_OBJ);
          break;
      case 1:
           
          ShowBg(3);
          rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG3 | BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_BG1 | BLDCNT_TGT2_OBJ);
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(11, 4));
          break;
      case 2:
           
          task.data[5] -= 4;
          task.data[8] += 4;
          if (task.data[5] <= 32 || task.data[8] >= 96)
          {
              task.data[5] = 32;
              task.data[8] = 96;
          }
          rt.SetGpuReg(REG_OFFSET_WIN0V, WIN_RANGE(task.data[5], task.data[8]));
          if (task.data[5] != 32)
              return;
          break;
      default:
          rt.DestroyTask(taskId);
           
          if (task.data[7] == true)
              Swap_CreateMonSprite();
          else
              Select_CreateMonSprite();
          return;
      }
      task.data[0]++;
};

/** Source: battle_factory_screen.c → Task_CloseMonPic */
export const Task_CloseMonPic: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      switch (task.data[0])
      {
      case 0:
           
          task.data[3] = 88;
          task.data[4] = DISPLAY_WIDTH - 88;
          task.data[5] = 32;
          task.data[8] = 96;
          SetGpuRegBits(REG_OFFSET_DISPCNT, DISPCNT_WIN0_ON);
          rt.SetGpuReg(REG_OFFSET_WIN0H, WIN_RANGE(task.data[3], task.data[4]));
          rt.SetGpuReg(REG_OFFSET_WIN0V, WIN_RANGE(task.data[5], task.data[8]));
          rt.SetGpuReg(REG_OFFSET_WININ, WININ_WIN0_BG_ALL | WININ_WIN0_CLR | WININ_WIN0_OBJ);
          rt.SetGpuReg(REG_OFFSET_WINOUT, WINOUT_WIN01_BG0 | WINOUT_WIN01_BG1 | WINOUT_WIN01_BG2 | WINOUT_WIN01_CLR | WINOUT_WIN01_OBJ);
          task.data[0]++;
          break;
      case 1:
           
          task.data[5] += 4;
          task.data[8] -= 4;
          if (task.data[5] >= 64 || task.data[8] <= 65)
          {
              task.data[5] = 64;
              task.data[8] = 65;
          }
          rt.SetGpuReg(REG_OFFSET_WIN0V, WIN_RANGE(task.data[5], task.data[8]));
          if (task.data[5] == 64)
              task.data[0]++;
          break;
      default:
           
          HideBg(3);
          _gs(rt, task.data[6]).data[7] = task.data[7];
          _gs(rt, task.data[6]).invisible = false;
          rt.setSpriteCallback(task.data[6], SpriteCB_CloseMonPic);
          rt.StartSpriteAffineAnim(task.data[6], 1);
          ClearGpuRegBits(REG_OFFSET_DISPCNT, DISPCNT_WIN0_ON);
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: battle_factory_screen.c → CB2_SelectScreen */
export const CB2_SelectScreen: CB2Callback = (rt) => {
  AnimateSprites();
      BuildOamBuffer();
      RunTextPrinters();
      UpdatePaletteFade();
      RunTasks();
};

/** Source: battle_factory_screen.c → CB2_InitSelectScreen */
export const CB2_InitSelectScreen: CB2Callback = (rt) => {
  let taskId = 0;

      switch (gMain.state)
      {
      case 0:
          TRY_FREE_AND_SET_NULL(sFactorySelectMons);
          SetHBlankCallback(null);
          rt.SetVBlankCallback(VBlankCB);
          CpuFill32(0, VRAM, VRAM_SIZE);
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sSelect_BgTemplates, ((sSelect_BgTemplates)?.length ?? 0));
          InitWindows(sSelect_WindowTemplates);
          DeactivateAllTextPrinters();
          gMain.state++;
          break;
      case 1:
          sSelectMenuTilesetBuffer = Alloc(((gFrontierFactoryMenu_Gfx)?.length ?? 32));
          sSelectMonPicBgTilesetBuffer = AllocZeroed(((sMonPicBg_Gfx)?.length ?? 32));
          sSelectMonPicBgTilesetBuffer = AllocZeroed(((gFrontierFactoryMenu_Gfx)?.length ?? 32));  
          sSelectMenuTilemapBuffer = Alloc(BG_SCREEN_SIZE);
          sSelectMonPicBgTilemapBuffer = AllocZeroed(BG_SCREEN_SIZE);
          ChangeBgX(0, 0, BG_COORD_SET);
          ChangeBgY(0, 0, BG_COORD_SET);
          ChangeBgX(1, 0, BG_COORD_SET);
          ChangeBgY(1, 0, BG_COORD_SET);
          ChangeBgX(3, 0, BG_COORD_SET);
          ChangeBgY(3, 0, BG_COORD_SET);
          rt.SetGpuReg(REG_OFFSET_BLDCNT, 0);
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, 0);
          rt.SetGpuReg(REG_OFFSET_BLDY, 0);
          rt.SetGpuReg(REG_OFFSET_MOSAIC, 0);
          rt.SetGpuReg(REG_OFFSET_WIN0H, 0);
          rt.SetGpuReg(REG_OFFSET_WIN0V, 0);
          rt.SetGpuReg(REG_OFFSET_WIN1H, 0);
          rt.SetGpuReg(REG_OFFSET_WIN1V, 0);
          rt.SetGpuReg(REG_OFFSET_WININ, 0);
          rt.SetGpuReg(REG_OFFSET_WINOUT, 0);
          gMain.state++;
          break;
      case 2:
          ResetPaletteFade();
          rt.ResetSpriteData();
          ResetTasks();
          FreeAllSpritePalettes();
          rt.CpuCopy16((rt.getExtraPalette("gFrontierFactoryMenu_Gfx") ?? new Uint16Array(0)), 0, /* TODO: dst sSelectMenuTilesetBuffer */ 0, Math.floor((((gFrontierFactoryMenu_Gfx)?.length ?? 32) / 2)));
          rt.CpuCopy16((rt.getExtraPalette("sMonPicBg_Gfx") ?? new Uint16Array(0)), 0, /* TODO: dst sSelectMonPicBgTilesetBuffer */ 0, Math.floor((((sMonPicBg_Gfx)?.length ?? 32) / 2)));
          LoadBgTiles(1, sSelectMenuTilesetBuffer, ((gFrontierFactoryMenu_Gfx)?.length ?? 32), 0);
          LoadBgTiles(3, sSelectMonPicBgTilesetBuffer, ((sMonPicBg_Gfx)?.length ?? 32), 0);
          rt.CpuCopy16((rt.getExtraPalette("gFrontierFactoryMenu_Tilemap") ?? new Uint16Array(0)), 0, /* TODO: dst sSelectMenuTilemapBuffer */ 0, Math.floor((BG_SCREEN_SIZE) / 2));
          LoadBgTilemap(1, sSelectMenuTilemapBuffer, BG_SCREEN_SIZE, 0);
          LoadPalette(gFrontierFactoryMenu_Pal, 0, 2 * PLTT_SIZE_4BPP);
          LoadPalette(sSelectText_Pal, BG_PLTT_ID(PALNUM_TEXT), PLTT_SIZEOF(4));
          LoadPalette(sSelectText_Pal, BG_PLTT_ID(PALNUM_FADE_TEXT), PLTT_SIZEOF(5));
          if (sFactorySelectScreen && sFactorySelectScreen.fromSummaryScreen)
          if (sFactorySelectScreen.fromSummaryScreen == true)
              rt.gPlttBufferUnfaded.set(BG_PLTT_ID(PALNUM_FADE_TEXT) + 4, sFactorySelectScreen.speciesNameColorBackup);
          LoadPalette(sMonPicBg_Pal, BG_PLTT_ID(2), PLTT_SIZEOF(2));
          gMain.state++;
          break;
      case 3:
          SetBgTilemapBuffer(3, sSelectMonPicBgTilemapBuffer);
          CopyToBgTilemapBufferRect(3, sMonPicBg_Tilemap, 11, 4, 8, 8);
          CopyToBgTilemapBufferRect(3, sMonPicBg_Tilemap,  2, 4, 8, 8);
          CopyToBgTilemapBufferRect(3, sMonPicBg_Tilemap, 20, 4, 8, 8);
          CopyBgTilemapBufferToVram(3);
          gMain.state++;
          break;
      case 4:
          LoadSpritePalettes(sSelect_SpritePalettes);
          LoadSpriteSheets(sSelect_SpriteSheets);
          LoadCompressedSpriteSheet(sSelect_BallGfx);
          ShowBg(0);
          ShowBg(1);
          rt.SetVBlankCallback(VBlankCB);
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 16, 0, "RGB_BLACK");
          rt.SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_OBJ_ON | DISPCNT_BG0_ON | DISPCNT_BG1_ON | DISPCNT_OBJ_1D_MAP);
          if (sFactorySelectScreen && sFactorySelectScreen.fromSummaryScreen)
          if (sFactorySelectScreen.fromSummaryScreen == true)
          {
              Select_SetWinRegs(88, 152, 32, 96);
              ShowBg(3);
              rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG3 | BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_BG1 | BLDCNT_TGT2_OBJ);
              rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(11, 4));
          }
          else
          {
              HideBg(3);
          }
          gMain.state++;
          break;
      case 5:
          if (sFactorySelectScreen && sFactorySelectScreen.fromSummaryScreen)
          if (sFactorySelectScreen.fromSummaryScreen == true)
              sFactorySelectScreen.cursorPos = gLastViewedMonIndex;
          Select_InitMonsData();
          Select_InitAllSprites();
          if (sFactorySelectScreen.fromSummaryScreen == true)
              Select_ReshowMonSprite();
          gMain.state++;
          break;
      case 6:
          Select_PrintSelectMonString();
          PutWindowTilemap(SELECT_WIN_INFO);
          gMain.state++;
          break;
      case 7:
          Select_PrintMonCategory();
          PutWindowTilemap(SELECT_WIN_MON_CATEGORY);
          gMain.state++;
          break;
      case 8:
          Select_PrintMonSpecies();
          PutWindowTilemap(SELECT_WIN_SPECIES);
          gMain.state++;
          break;
      case 9:
          Select_PrintRentalPkmnString();
          PutWindowTilemap(SELECT_WIN_TITLE);
          gMain.state++;
          break;
      case 10:
          sFactorySelectScreen.fadeSpeciesNameTaskId = rt.CreateTask((t) => Select_Task_FadeSpeciesName(t, rt), 0);
          if (!sFactorySelectScreen.fromSummaryScreen)
          {
              _gt(rt, sFactorySelectScreen.fadeSpeciesNameTaskId).data[0] = FADESTATE_INIT;
              taskId = rt.CreateTask((t) => Select_Task_HandleChooseMons(t, rt), 0);
              task.data[0] = STATE_CHOOSE_MONS_INIT;
          }
          else
          {
              _gt(rt, sFactorySelectScreen.fadeSpeciesNameTaskId).data[0] = FADESTATE_RUN;
              sFactorySelectScreen.fadeSpeciesNameActive = false;
              taskId = rt.CreateTask((t) => Select_Task_HandleMenu(t, rt), 0);
              task.data[0] = STATE_MENU_RESHOW;
          }
          rt.SetMainCallback2(CB2_SelectScreen);
          break;
      }
};

/** Source: battle_factory_screen.c → CB2_InitSwapScreen */
export const CB2_InitSwapScreen: CB2Callback = (rt) => {
  let taskId = 0;

      switch (gMain.state)
      {
      case 0:
          SetHBlankCallback(null);
          rt.SetVBlankCallback(VBlankCB);
          CpuFill32(0, VRAM, VRAM_SIZE);
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sSwap_BgTemplates, ((sSwap_BgTemplates)?.length ?? 0));
          InitWindows(sSwap_WindowTemplates);
          DeactivateAllTextPrinters();
          gMain.state++;
          break;
      case 1:
          sSwapMenuTilesetBuffer = Alloc(((gFrontierFactoryMenu_Gfx)?.length ?? 32));
          sSwapMonPicBgTilesetBuffer = AllocZeroed(((sMonPicBg_Gfx)?.length ?? 32));
          sSwapMonPicBgTilesetBuffer = AllocZeroed(((gFrontierFactoryMenu_Gfx)?.length ?? 32));  
          sSwapMenuTilemapBuffer = Alloc(BG_SCREEN_SIZE);
          sSwapMonPicBgTilemapBuffer = AllocZeroed(BG_SCREEN_SIZE);
          ChangeBgX(0, 0, BG_COORD_SET);
          ChangeBgY(0, 0, BG_COORD_SET);
          ChangeBgX(1, 0, BG_COORD_SET);
          ChangeBgY(1, 0, BG_COORD_SET);
          ChangeBgX(2, 0, BG_COORD_SET);
          ChangeBgY(2, 0, BG_COORD_SET);
          ChangeBgX(3, 0, BG_COORD_SET);
          ChangeBgY(3, 0, BG_COORD_SET);
          rt.SetGpuReg(REG_OFFSET_BLDY, 0);
          rt.SetGpuReg(REG_OFFSET_MOSAIC, 0);
          rt.SetGpuReg(REG_OFFSET_WIN0H, 0);
          rt.SetGpuReg(REG_OFFSET_WIN0V, 0);
          rt.SetGpuReg(REG_OFFSET_WIN1H, 0);
          rt.SetGpuReg(REG_OFFSET_WIN1V, 0);
          rt.SetGpuReg(REG_OFFSET_WININ, 0);
          rt.SetGpuReg(REG_OFFSET_WINOUT, 0);
          gMain.state++;
          break;
      case 2:
          ResetPaletteFade();
          rt.ResetSpriteData();
          ResetTasks();
          FreeAllSpritePalettes();
          ResetAllPicSprites();
          rt.CpuCopy16((rt.getExtraPalette("gFrontierFactoryMenu_Gfx") ?? new Uint16Array(0)), 0, /* TODO: dst sSwapMenuTilesetBuffer */ 0, Math.floor((((gFrontierFactoryMenu_Gfx)?.length ?? 32) / 2)));
          rt.CpuCopy16((rt.getExtraPalette("sMonPicBg_Gfx") ?? new Uint16Array(0)), 0, /* TODO: dst sSwapMonPicBgTilesetBuffer */ 0, Math.floor((((sMonPicBg_Gfx)?.length ?? 32) / 2)));
          LoadBgTiles(1, sSwapMenuTilesetBuffer, ((gFrontierFactoryMenu_Gfx)?.length ?? 32), 0);
          LoadBgTiles(3, sSwapMonPicBgTilesetBuffer, ((sMonPicBg_Gfx)?.length ?? 32), 0);
          rt.CpuCopy16((rt.getExtraPalette("gFrontierFactoryMenu_Tilemap") ?? new Uint16Array(0)), 0, /* TODO: dst sSwapMenuTilemapBuffer */ 0, Math.floor((BG_SCREEN_SIZE) / 2));
          LoadBgTilemap(1, sSwapMenuTilemapBuffer, BG_SCREEN_SIZE, 0);
          LoadPalette(gFrontierFactoryMenu_Pal, 0, 2 * PLTT_SIZE_4BPP);
          LoadPalette(sSwapText_Pal, BG_PLTT_ID(PALNUM_TEXT), ((sSwapText_Pal)?.length ?? 32));
          LoadPalette(sSwapText_Pal, BG_PLTT_ID(PALNUM_FADE_TEXT), ((sSwapText_Pal)?.length ?? 32));
          LoadPalette(sMonPicBg_Pal, BG_PLTT_ID(2), PLTT_SIZEOF(2));
          gMain.state++;
          break;
      case 3:
          SetBgTilemapBuffer(3, sSwapMonPicBgTilemapBuffer);
          CopyToBgTilemapBufferRect(3, sMonPicBg_Tilemap, 11, 4, 8, 8);
          CopyBgTilemapBufferToVram(3);
          gMain.state++;
          break;
      case 4:
          LoadSpritePalettes(sSwap_SpritePalettes);
          LoadSpriteSheets(sSwap_SpriteSheets);
          LoadCompressedSpriteSheet(sSwap_BallGfx);
          rt.SetVBlankCallback(VBlankCB);
          gMain.state++;
          break;
      case 5:
          if (sFactorySwapScreen && sFactorySwapScreen.fromSummaryScreen)
          if (sFactorySwapScreen.fromSummaryScreen == true)
              sFactorySwapScreen.cursorPos = gLastViewedMonIndex;
          gMain.state++;
          break;
      case 6:
          Swap_InitStruct();
          Swap_InitAllSprites();
          if (sFactorySwapScreen.fromSummaryScreen == true)
              Swap_ShowSummaryMonSprite();
          Swap_InitActions(SWAP_PLAYER_SCREEN);
          gMain.state++;
          break;
      case 7:
          Swap_PrintOnInfoWindow(gText_SelectPkmnToSwap);
          PutWindowTilemap(SWAP_WIN_INFO);
          gMain.state++;
          break;
      case 8:
          Swap_PrintMonCategory();
          PutWindowTilemap(SWAP_WIN_MON_CATEGORY);
          gMain.state++;
          break;
      case 9:
          if (!sFactorySwapScreen.fromSummaryScreen)
              Swap_PrintMonSpecies();
          PutWindowTilemap(SWAP_WIN_SPECIES);
          gMain.state++;
          break;
      case 10:
          Swap_PrintPkmnSwap();
          PutWindowTilemap(SWAP_WIN_TITLE);
          gMain.state++;
          break;
      case 11:
          gMain.state++;
          break;
      case 12:
          if (sFactorySwapScreen.fromSummaryScreen)
              Swap_PrintMonSpeciesAtFade();
          gMain.state++;
          break;
      case 13:
          Swap_PrintActionStrings2();
          PutWindowTilemap(SWAP_WIN_OPTIONS);
          gMain.state++;
          break;
      case 14:
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 16, 0, "RGB_BLACK");
          rt.SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_OBJ_ON | DISPCNT_OBJ_1D_MAP);
          ShowBg(0);
          ShowBg(1);
          ShowBg(2);
          if (sFactorySwapScreen.fromSummaryScreen == true)
          {
              ShowBg(3);
              rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG3 | BLDCNT_EFFECT_BLEND | BLDCNT_TGT2_BG1 | BLDCNT_TGT2_OBJ);
              rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(11, 4));
          }
          else
          {
              HideBg(3);
          }
          gMain.state++;
          break;
      case 15:
          sFactorySwapScreen.fadeSpeciesNameTaskId = rt.CreateTask((t) => Swap_Task_FadeSpeciesName(t, rt), 0);
          if (!sFactorySwapScreen.fromSummaryScreen)
          {
              _gt(rt, sFactorySwapScreen.fadeSpeciesNameTaskId).data[0] = FADESTATE_INIT;
              taskId = rt.CreateTask((t) => Swap_Task_HandleChooseMons(t, rt), 0);
              task.data[0] = STATE_CHOOSE_MONS_INIT;
          }
          else
          {
              Swap_EraseActionFadeWindow();
              _gt(rt, sFactorySwapScreen.fadeSpeciesNameTaskId).data[0] = FADESTATE_RUN;
              sFactorySwapScreen.fadeSpeciesNameActive = false;
              taskId = rt.CreateTask((t) => Swap_Task_HandleMenu(t, rt), 0);
              task.data[0] = STATE_MENU_INIT;
          }
          /* TODO scene transition: SetMainCallback2(Swap_CB2) */;
          break;
      }
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
