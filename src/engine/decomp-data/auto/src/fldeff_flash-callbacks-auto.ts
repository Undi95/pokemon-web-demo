// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/fldeff_flash.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const INTR_FLAG_VBLANK = 1;
const OAM = 117440512;
const OAM_SIZE = 1024;
const PLTT = 83886080;
const PLTT_SIZE = 1024;
const VRAM = 100663296;
const VRAM_SIZE = 98304;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const REG_IE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_IME: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: fldeff_flash.c → Task_ExitCaveTransition1 */
export const Task_ExitCaveTransition1: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.func = (t) => Task_ExitCaveTransition2(t, rt);
};

/** Source: fldeff_flash.c → Task_ExitCaveTransition2 */
export const Task_ExitCaveTransition2: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  rt.SetGpuReg(REG_OFFSET_DISPCNT, 0);
      LZ77UnCompVram(sCaveTransitionTiles, (VRAM + 0xC000));
      LZ77UnCompVram(sCaveTransitionTilemap, (VRAM + 0xF800));
      LoadPalette(sCaveTransitionPalette_White, BG_PLTT_ID(14), PLTT_SIZE_4BPP);
       
      LoadPalette(sCaveTransitionPalette_Exit, BG_PLTT_ID(14), PLTT_SIZEOF(8));
      rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG0
                                  | BLDCNT_EFFECT_BLEND
                                  | BLDCNT_TGT2_BG1
                                  | BLDCNT_TGT2_BG2
                                  | BLDCNT_TGT2_BG3
                                  | BLDCNT_TGT2_OBJ
                                  | BLDCNT_TGT2_BD);
      rt.SetGpuReg(REG_OFFSET_BLDALPHA, 0);
      rt.SetGpuReg(REG_OFFSET_BLDY, 0);
      rt.SetGpuReg(REG_OFFSET_BG0CNT, BGCNT_PRIORITY(0)
                                 | BGCNT_CHARBASE(3)
                                 | BGCNT_SCREENBASE(31)
                                 | BGCNT_16COLOR
                                 | BGCNT_TXT256x256);
      rt.SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                  | DISPCNT_OBJ_1D_MAP
                                  | DISPCNT_BG0_ON
                                  | DISPCNT_OBJ_ON);
      task.func = (t) => Task_ExitCaveTransition3(t, rt);
      task.data[0] = 16;
      task.data[1] = 0;
};

/** Source: fldeff_flash.c → Task_ExitCaveTransition3 */
export const Task_ExitCaveTransition3: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let count = task.data[1];
      let blend = count + 0x1000;

      rt.SetGpuReg(REG_OFFSET_BLDALPHA, blend);
      if (count <= 16)
      {
          task.data[1]++;
      }
      else
      {
          task.data[2] = 0;
          task.func = (t) => Task_ExitCaveTransition4(t, rt);
      }
};

/** Source: fldeff_flash.c → Task_ExitCaveTransition4 */
export const Task_ExitCaveTransition4: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let count = 0;

      rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 16));
      count = task.data[2];

      if (count < 8)
      {
          task.data[2]++;
           
          LoadPalette(sCaveTransitionPalette_Exit[count], BG_PLTT_ID(14), ((sCaveTransitionPalette_Exit)?.length ?? 32) - PLTT_SIZEOF(count));
      }
      else
      {
          LoadPalette(sCaveTransitionPalette_White, BG_PLTT_ID(0), PLTT_SIZE_4BPP);
          task.func = (t) => Task_ExitCaveTransition5(t, rt);
          task.data[2] = 8;
      }
};

/** Source: fldeff_flash.c → Task_ExitCaveTransition5 */
export const Task_ExitCaveTransition5: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (task.data[2])
          task.data[2]--;
      else
          /* TODO scene transition: SetMainCallback2(gMain.savedCallback) */;
};

/** Source: fldeff_flash.c → Task_EnterCaveTransition1 */
export const Task_EnterCaveTransition1: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.func = (t) => Task_EnterCaveTransition2(t, rt);
};

/** Source: fldeff_flash.c → Task_EnterCaveTransition2 */
export const Task_EnterCaveTransition2: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  rt.SetGpuReg(REG_OFFSET_DISPCNT, 0);
      LZ77UnCompVram(sCaveTransitionTiles, (VRAM + 0xC000));
      LZ77UnCompVram(sCaveTransitionTilemap, (VRAM + 0xF800));
      rt.SetGpuReg(REG_OFFSET_BLDCNT, 0);
      rt.SetGpuReg(REG_OFFSET_BLDALPHA, 0);
      rt.SetGpuReg(REG_OFFSET_BLDY, 0);
      rt.SetGpuReg(REG_OFFSET_BG0CNT, BGCNT_PRIORITY(0)
                                 | BGCNT_CHARBASE(3)
                                 | BGCNT_SCREENBASE(31)
                                 | BGCNT_16COLOR
                                 | BGCNT_TXT256x256);
      rt.SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0
                                  | DISPCNT_OBJ_1D_MAP
                                  | DISPCNT_BG0_ON
                                  | DISPCNT_OBJ_ON);
      LoadPalette(sCaveTransitionPalette_White, BG_PLTT_ID(14), PLTT_SIZE_4BPP);
      LoadPalette(sCaveTransitionPalette_Black, BG_PLTT_ID(0), PLTT_SIZE_4BPP);
      task.func = (t) => Task_EnterCaveTransition3(t, rt);
      task.data[0] = 16;
      task.data[1] = 0;
      task.data[2] = 0;
};

/** Source: fldeff_flash.c → Task_EnterCaveTransition3 */
export const Task_EnterCaveTransition3: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let count = task.data[2];

      if (count < 16)
      {
          task.data[2]++;
          task.data[2]++;
          LoadPalette(sCaveTransitionPalette_Enter[15 - count], BG_PLTT_ID(14), PLTT_SIZEOF(count + 1));
      }
      else
      {
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 16));
          rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT1_BG0
                                      | BLDCNT_EFFECT_BLEND
                                      | BLDCNT_TGT2_BG1
                                      | BLDCNT_TGT2_BG2
                                      | BLDCNT_TGT2_BG3
                                      | BLDCNT_TGT2_OBJ
                                      | BLDCNT_TGT2_BD);
          task.func = (t) => Task_EnterCaveTransition4(t, rt);
      }
};

/** Source: fldeff_flash.c → Task_EnterCaveTransition4 */
export const Task_EnterCaveTransition4: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let count = 16 - task.data[1];
      let blend = count + 0x1000;

      rt.SetGpuReg(REG_OFFSET_BLDALPHA, blend);
      if (count)
      {
          task.data[1]++;
      }
      else
      {
          LoadPalette(sCaveTransitionPalette_Black, BG_PLTT_ID(0), PLTT_SIZE_4BPP);
          /* TODO scene transition: SetMainCallback2(gMain.savedCallback) */;
      }
};

/** Source: fldeff_flash.c → CB2_ChangeMapMain */
export const CB2_ChangeMapMain: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** Source: fldeff_flash.c → CB2_DoChangeMap */
export const CB2_DoChangeMap: CB2Callback = (rt) => {
  let ime = 0;

      /* noop SetVBlankCallback */;
      rt.SetGpuReg(REG_OFFSET_DISPCNT, 0);
      rt.SetGpuReg(REG_OFFSET_BG2CNT, 0);
      rt.SetGpuReg(REG_OFFSET_BG1CNT, 0);
      rt.SetGpuReg(REG_OFFSET_BG0CNT, 0);
      rt.SetGpuReg(REG_OFFSET_BG2HOFS, 0);
      rt.SetGpuReg(REG_OFFSET_BG2VOFS, 0);
      rt.SetGpuReg(REG_OFFSET_BG1HOFS, 0);
      rt.SetGpuReg(REG_OFFSET_BG1VOFS, 0);
      rt.SetGpuReg(REG_OFFSET_BG0HOFS, 0);
      rt.SetGpuReg(REG_OFFSET_BG0VOFS, 0);
      DmaFill16(3, 0, VRAM, VRAM_SIZE);
      DmaFill32(3, 0, OAM, OAM_SIZE);
      DmaFill16(3, 0, (PLTT + 2), PLTT_SIZE - 2);
      ResetPaletteFade();
      ResetTasks();
      rt.ResetSpriteData();
      ime = REG_IME;
      REG_IME = 0;
      REG_IE |= INTR_FLAG_VBLANK;
      REG_IME = ime;
      /* noop SetVBlankCallback */;
      /* TODO scene transition: SetMainCallback2(CB2_ChangeMapMain) */;
      if (!TryDoMapTransition())
          /* TODO scene transition: SetMainCallback2(gMain.savedCallback) */;
};
