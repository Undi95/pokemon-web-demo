// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/save_failed_screen.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  CopyWindowToVram,
  CpuFill32,
  DeactivateAllTextPrinters,
  DmaFill16,
  DmaFill32,
  EnableInterrupts,
  FillWindowPixelBuffer,
  InitBgsFromTemplates,
  InitWindows,
  JOY_NEW,
  LZ77UnCompVram,
  LoadBgTiles,
  LoadPalette,
  OAM,
  OAM_SIZE,
  PLTT,
  PLTT_SIZE,
  ResetBgsAndClearDma3BusyFlags,
  ResetPaletteFade,
  ResetTasks,
  ShowBg,
  UpdatePaletteFade,
  VRAM,
  VRAM_SIZE,
  gMain,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const CLOCK_RUNNING = 0;
const CLOCK_WIN_ID = 1;
const COPYWIN_GFX = 2;
const COPYWIN_MAP = 1;
const OBJ_VRAM0 = 100728832;
const TEXT_WIN_ID = 0;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: save_failed_screen.c → CB2_SaveFailedScreen */
export const CB2_SaveFailedScreen: CB2Callback = (rt) => {
  switch (gMain.state)
      {
      case 0:
      default:
          /* noop SetVBlankCallback */;
          rt.SetGpuReg(REG_OFFSET_DISPCNT, 0);
          rt.SetGpuReg(REG_OFFSET_BG3CNT, 0);
          rt.SetGpuReg(REG_OFFSET_BG2CNT, 0);
          rt.SetGpuReg(REG_OFFSET_BG1CNT, 0);
          rt.SetGpuReg(REG_OFFSET_BG0CNT, 0);
          rt.SetGpuReg(REG_OFFSET_BG3HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG3VOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG2HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG2VOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG1HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG1VOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG0HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG0VOFS, 0);
          DmaFill16(3, 0, VRAM, VRAM_SIZE);
          DmaFill32(3, 0, OAM, OAM_SIZE);
          DmaFill16(3, 0, PLTT, PLTT_SIZE);
          LZ77UnCompVram(gBirchBagGrass_Gfx, VRAM);
          LZ77UnCompVram(gBirchBagTilemap, (BG_SCREEN_ADDR(14)));
          LZ77UnCompVram(gBirchGrassTilemap, (BG_SCREEN_ADDR(15)));
          LZ77UnCompVram(sSaveFailedClockGfx, (OBJ_VRAM0 + 0x20));
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sBgTemplates, ((sBgTemplates)?.length ?? 0));
          SetBgTilemapBuffer(0, gDecompressionBuffer[0x2000]);
          CpuFill32(0, gDecompressionBuffer[0x2000], 0x800);
          LoadBgTiles(0, gTextWindowFrame1_Gfx, 0x120, 0x214);
          InitWindows(sDummyWindowTemplate);
          sWindowIds[TEXT_WIN_ID] = AddWindowWithoutTileMap(sWindowTemplate_Text);
          SetWindowAttribute(sWindowIds[TEXT_WIN_ID], 7, gDecompressionBuffer[0x2800]);
          sWindowIds[CLOCK_WIN_ID] = AddWindowWithoutTileMap(sWindowTemplate_Clock);
          SetWindowAttribute(sWindowIds[CLOCK_WIN_ID], 7, gDecompressionBuffer[0x3D00]);
          DeactivateAllTextPrinters();
          rt.ResetSpriteData();
          ResetTasks();
          ResetPaletteFade();
          LoadPalette(gBirchBagGrass_Pal, BG_PLTT_ID(0), 2 * PLTT_SIZE_4BPP);
          LoadPalette(sSaveFailedClockPal, OBJ_PLTT_ID(0), PLTT_SIZE_4BPP);
          LoadPalette(gTextWindowFrame1_Pal, BG_PLTT_ID(14), PLTT_SIZE_4BPP);
          LoadPalette(gStandardMenuPalette, BG_PLTT_ID(15), PLTT_SIZE_4BPP);
          DrawStdFrameWithCustomTileAndPalette(sWindowIds[TEXT_WIN_ID], false, 0x214, 0xE);
          DrawStdFrameWithCustomTileAndPalette(sWindowIds[CLOCK_WIN_ID], false, 0x214, 0xE);
          FillWindowPixelBuffer(sWindowIds[CLOCK_WIN_ID], PIXEL_FILL(1));  
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          CopyWindowToVram(sWindowIds[CLOCK_WIN_ID], COPYWIN_GFX);  
          CopyWindowToVram(sWindowIds[TEXT_WIN_ID], COPYWIN_MAP);
          SaveFailedScreenTextPrint(gText_SaveFailedCheckingBackup, 1, 0);
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 16, 0, "RGB_BLACK");
          EnableInterrupts(1);
          /* noop SetVBlankCallback */;
          rt.SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_OBJ_ON | DISPCNT_OBJ_1D_MAP);
          ShowBg(0);
          ShowBg(2);
          ShowBg(3);
          gMain.state++;
          break;
      case 1:
          if (!UpdatePaletteFade())
          {
              /* TODO scene transition: SetMainCallback2(CB2_WipeSave) */;
              /* noop SetVBlankCallback */;
          }
          break;
      }
};

/** Source: save_failed_screen.c → CB2_WipeSave */
export const CB2_WipeSave: CB2Callback = (rt) => {
  let wipeTries = 0;

      sClockInfo[CLOCK_RUNNING] = true;

      while (gDamagedSaveSectors != 0 && wipeTries < 3)
      {
          if (WipeSectors(gDamagedSaveSectors))
          {
              FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
              SaveFailedScreenTextPrint(gText_BackupMemoryDamaged, 1, 0);
              /* TODO scene transition: SetMainCallback2(CB2_GameplayCannotBeContinued) */;
              return;
          }

          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          SaveFailedScreenTextPrint(gText_CheckCompleted, 1, 0);
          HandleSavingData(sSaveFailedType);

          if (gDamagedSaveSectors != 0)
          {
              FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
              SaveFailedScreenTextPrint(gText_SaveFailedCheckingBackup, 1, 0);
          }

          wipeTries++;
      }

      if (wipeTries == 3)
      {
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          SaveFailedScreenTextPrint(gText_BackupMemoryDamaged, 1, 0);
      }
      else
      {
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));

          if (gGameContinueCallback == null)
              SaveFailedScreenTextPrint(gText_SaveCompleteGameCannotContinue, 1, 0);
          else
              SaveFailedScreenTextPrint(gText_SaveCompletePressA, 1, 0);
      }

      /* TODO scene transition: SetMainCallback2(CB2_FadeAndReturnToTitleScreen) */;
};

/** Source: save_failed_screen.c → CB2_GameplayCannotBeContinued */
export const CB2_GameplayCannotBeContinued: CB2Callback = (rt) => {
  sClockInfo[CLOCK_RUNNING] = false;

      if (JOY_NEW(A_BUTTON))
      {
          FillWindowPixelBuffer(sWindowIds[TEXT_WIN_ID], PIXEL_FILL(1));
          SaveFailedScreenTextPrint(gText_GamePlayCannotBeContinued, 1, 0);
          /* noop SetVBlankCallback */;
          /* TODO scene transition: SetMainCallback2(CB2_FadeAndReturnToTitleScreen) */;
      }
};

/** Source: save_failed_screen.c → CB2_FadeAndReturnToTitleScreen */
export const CB2_FadeAndReturnToTitleScreen: CB2Callback = (rt) => {
  sClockInfo[CLOCK_RUNNING] = false;

      if (JOY_NEW(A_BUTTON))
      {
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 16, "RGB_BLACK");
          /* noop SetVBlankCallback */;
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToTitleScreen) */;
      }
};

/** Source: save_failed_screen.c → CB2_ReturnToTitleScreen */
export const CB2_ReturnToTitleScreen: CB2Callback = (rt) => {
  if (!UpdatePaletteFade())
      {
          if (gGameContinueCallback == null)  
          {
              DoSoftReset();
          }
          else
          {
              /* TODO scene transition: SetMainCallback2(gGameContinueCallback) */;
              gGameContinueCallback = null;
          }
      }
};
