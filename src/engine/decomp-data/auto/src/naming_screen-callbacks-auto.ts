// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/naming_screen.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const BUTTON_COUNT = 3;
const PALTAG_CURSOR = 5;
const STATE_EXIT = 9;
const STATE_FADE_IN = 0;
const STATE_FADE_OUT = 8;
const STATE_HANDLE_INPUT = 2;
const STATE_MOVE_TO_OK_BUTTON = 3;
const STATE_PRESSED_OK = 6;
const STATE_START_PAGE_SWAP = 4;
const STATE_WAIT_FADE_IN = 1;
const STATE_WAIT_PAGE_SWAP = 5;
const STATE_WAIT_SENT_TO_PC_MESSAGE = 7;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: naming_screen.c → SpriteCB_Cursor */
export const SpriteCB_Cursor: SpriteCallback = (sprite, rt) => {
  if (sprite.animEnded)
          rt.StartSpriteAnim(sprite.spriteId, 0);

       
      sprite.invisible = sprite.data[4];
      if (sprite.data[0] == GetCurrentPageColumnCount())
          sprite.invisible = true;

      if (sprite.invisible
         || !(sprite.data[4])
         || sprite.data[0] != sprite.data[2]
         || sprite.data[1] != sprite.data[3])
      {
          sprite.data[5] = 0;
          sprite.data[6] = 2;
          sprite.data[7] = 2;
      }

      sprite.data[7]--;
      if (sprite.data[7] == 0)
      {
          sprite.data[5] += sprite.data[6];
          if (sprite.data[5] == 16 || sprite.data[5] == 0)
              sprite.data[6] = -sprite.data[6];
          sprite.data[7] = 2;
      }

      if (sprite.data[4])
      {
          let gb = sprite.data[5];
          let r = sprite.data[5] >> 1;
          let index = OBJ_PLTT_ID(IndexOfSpritePaletteTag(PALTAG_CURSOR)) + 1;

          MultiplyInvertedPaletteRGBComponents(index, r, gb, gb);
      }
};

/** Source: naming_screen.c → SpriteCB_InputArrow */
export const SpriteCB_InputArrow: SpriteCallback = (sprite, rt) => {
  const x: any = [0, -4, -2, -1];

      if (sprite.data[2] == 0 || --sprite.data[2] == 0)
      {
          sprite.data[2] = 8;
          sprite.data[1] = ((sprite.data[1] + 1) % (((x)?.length ?? 0)));
      }
      sprite.x2 = x[sprite.data[1]];
};

/** Source: naming_screen.c → SpriteCB_Underscore */
export const SpriteCB_Underscore: SpriteCallback = (sprite, rt) => {
  const y: any = [2, 3, 2, 1];
      let pos = 0;

      pos = GetTextEntryPosition();
      if (pos != sprite.data[0])
      {
          sprite.y2 = 0;
          sprite.data[1] = 0;
          sprite.data[2] = 0;
      }
      else
      {
          sprite.y2 = y[sprite.data[1]];
          sprite.data[2]++;
          if (sprite.data[2] > 8)
          {
              sprite.data[1] = ((sprite.data[1] + 1) % (((y)?.length ?? 0)));
              sprite.data[2] = 0;
          }
      }
};

/** Source: naming_screen.c → SpriteCB_PageSwap */
export const SpriteCB_PageSwap: SpriteCallback = (sprite, rt) => {
  while (sPageSwapSpriteFuncs[sprite.data[0]](sprite));
};

/** Source: naming_screen.c → Task_NamingScreen */
export const Task_NamingScreen: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (sNamingScreen.state)
      {
      case STATE_FADE_IN:
          MainState_FadeIn();
          SetSpritesVisible();
          SetVBlank();
          break;
      case STATE_WAIT_FADE_IN:
          MainState_WaitFadeIn();
          break;
      case STATE_HANDLE_INPUT:
          MainState_HandleInput();
          break;
      case STATE_MOVE_TO_OK_BUTTON:
          MainState_MoveToOKButton();
          MainState_HandleInput();
          break;
      case STATE_START_PAGE_SWAP:
          MainState_StartPageSwap();
          break;
      case STATE_WAIT_PAGE_SWAP:
          MainState_WaitPageSwap();
          break;
      case STATE_PRESSED_OK:
          MainState_PressedOKButton();
          break;
      case STATE_WAIT_SENT_TO_PC_MESSAGE:
          MainState_WaitSentToPCMessage();
          break;
      case STATE_FADE_OUT:
          MainState_FadeOut();
          break;
      case STATE_EXIT:
          MainState_Exit();
          break;
      }
};

/** Source: naming_screen.c → Task_HandlePageSwapAnim */
export const Task_HandlePageSwapAnim: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sPageSwapAnimStateFuncs[task.data[0]](_gt(rt, taskId)) != 0);
};

/** Source: naming_screen.c → Task_UpdateButtonFlash */
export const Task_UpdateButtonFlash: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      if (task.data[2] == BUTTON_COUNT || !task.data[2])
          return;

      MultiplyInvertedPaletteRGBComponents(GetButtonPalOffset(task.data[2]), task.data[3], task.data[3], task.data[3]);

      if (task.data[5] && --task.data[5])
          return;

      task.data[5] = 2;
      if (task.data[4] >= 0)
      {
          if (task.data[3] < 14)
          {
              task.data[3] += task.data[4];
              task.data[6] += task.data[4];
          }
          else
          {
              task.data[3] = 16;
              task.data[6]++;
          }
      }
      else
      {
          task.data[3] += task.data[4];
          task.data[6] += task.data[4];
      }

      if (task.data[3] == 16 && task.data[6] == 22)
      {
          task.data[4] = -4;
      }
      else if (task.data[3] == 0)
      {
          task.data[2] = task.data[1];
          task.data[4] = 2;
          task.data[6] = 0;
      }
};

/** Source: naming_screen.c → Task_HandleInput */
export const Task_HandleInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sInputFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: naming_screen.c → CB2_LoadNamingScreen */
export const CB2_LoadNamingScreen: CB2Callback = (rt) => {
  switch (gMain.state)
      {
      case 0:
          ResetVHBlank();
          NamingScreen_Init();
          gMain.state++;
          break;
      case 1:
          NamingScreen_InitBGs();
          gMain.state++;
          break;
      case 2:
          ResetPaletteFade();
          gMain.state++;
          break;
      case 3:
          rt.ResetSpriteData();
          /* TODO FreeAllSpritePalettes */;
          gMain.state++;
          break;
      case 4:
          ResetTasks();
          gMain.state++;
          break;
      case 5:
          LoadPalettes();
          gMain.state++;
          break;
      case 6:
          LoadGfx();
          gMain.state++;
          break;
      case 7:
          CreateSprites();
          UpdatePaletteFade();
          NamingScreen_ShowBgs();
          gMain.state++;
          break;
      default:
          CreateHelperTasks();
          CreateNamingScreenTask();
          break;
      }
};

/** Source: naming_screen.c → CB2_NamingScreen */
export const CB2_NamingScreen: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** Source: naming_screen.c → CreateSprites (helper) */
export function CreateSprites(rt: DecompRuntime): number {
  let sprite: any = _emptySprite;
  let task: any = _emptyTask;
  CreateCursorSprite();
      CreatePageSwapButtonSprites();
      CreateBackOkSprites();
      CreateTextEntrySprites();
      CreateInputTargetIcon();
  return -1;
}

/** Source: naming_screen.c → CreateCursorSprite (helper) */
export function CreateCursorSprite(rt: DecompRuntime): number {
  let sprite: any = _emptySprite;
  let task: any = _emptyTask;
  sNamingScreen.cursorSpriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_Cursor',  38, 88, 1);
      SetCursorInvisibility(true);
      rt.gba.oam[_gs(rt, sNamingScreen.cursorSpriteId).oamIndex].priority = 1;
      rt.gba.oam[_gs(rt, sNamingScreen.cursorSpriteId).oamIndex].objMode = ST_OAM_OBJ_BLEND;
      _gs(rt, sNamingScreen.cursorSpriteId).data[6] = 1;  
      _gs(rt, sNamingScreen.cursorSpriteId).data[6] = 2;
      SetCursorPos(0, 0);
  return -1;
}

/** Source: naming_screen.c → CreatePageSwapButtonSprites (helper) */
export function CreatePageSwapButtonSprites(rt: DecompRuntime): number {
  let sprite: any = _emptySprite;
  let task: any = _emptyTask;
  let frameSpriteId = 0;
      let textSpriteId = 0;
      let buttonSpriteId = 0;

      frameSpriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_PageSwapFrame',  204, 88, 0);
      sNamingScreen.swapBtnFrameSpriteId = frameSpriteId;
      SetSubspriteTables(_gs(rt, frameSpriteId), sSubspriteTable_PageSwapFrame);
      _gs(rt, frameSpriteId).invisible = true;

      textSpriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_PageSwapText',  204, 84, 1);
      _gs(rt, frameSpriteId).data[6] = textSpriteId;
      SetSubspriteTables(_gs(rt, textSpriteId), sSubspriteTable_PageSwapText);
      _gs(rt, textSpriteId).invisible = true;

      buttonSpriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_PageSwapButton',  204, 83, 2);
      rt.gba.oam[_gs(rt, buttonSpriteId).oamIndex].priority = 1;
      _gs(rt, frameSpriteId).data[7] = buttonSpriteId;
      _gs(rt, buttonSpriteId).invisible = true;
  return -1;
}

/** Source: naming_screen.c → CreateBackOkSprites (helper) */
export function CreateBackOkSprites(rt: DecompRuntime): number {
  let sprite: any = _emptySprite;
  let task: any = _emptyTask;
  let spriteId = 0;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_BackButton',  204, 116, 0);
      SetSubspriteTables(_gs(rt, spriteId), sSubspriteTable_Button);
      _gs(rt, spriteId).invisible = true;

      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_OkButton',  204, 140, 0);
      SetSubspriteTables(_gs(rt, spriteId), sSubspriteTable_Button);
      _gs(rt, spriteId).invisible = true;
  return -1;
}

/** Source: naming_screen.c → CreateTextEntrySprites (helper) */
export function CreateTextEntrySprites(rt: DecompRuntime): number {
  let sprite: any = _emptySprite;
  let task: any = _emptyTask;
  let spriteId = 0;
      let xPos = 0;
      let i = 0;

      xPos = sNamingScreen.inputCharBaseXPos - 5;
      spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_InputArrow',  xPos, 56, 0);
      rt.gba.oam[_gs(rt, spriteId).oamIndex].priority = 3;
      _gs(rt, spriteId).invisible = true;
      xPos = sNamingScreen.inputCharBaseXPos;
      for (i = 0; i < sNamingScreen.template.maxChars; i++, xPos += 8)
      {
          spriteId = rt.CreateSpriteFromTemplate('sSpriteTemplate_Underscore',  xPos + 3, 60, 0);
          rt.gba.oam[_gs(rt, spriteId).oamIndex].priority = 3;
          _gs(rt, spriteId).data[0] = i;
          _gs(rt, spriteId).invisible = true;
      }
  return -1;
}
