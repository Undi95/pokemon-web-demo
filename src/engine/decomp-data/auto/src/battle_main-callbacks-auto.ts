// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_main.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import { CpuFill32, FreeAllSpritePalettes, FreeAllWindowBuffers, FreeMonSpritesGfx, ResetPaletteFade, ResetTasks, RunTextPrinters, ShowBg, UpdatePaletteFade, VRAM, VRAM_SIZE, WINOUT_WIN01_CLR, WINOUT_WIN01_OBJ, gMain, gSaveBlock2Ptr, m4aSongNumStart } from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const BATTLE_ENVIRONMENT_BUILDING = 8;
const BATTLE_TYPE_BATTLE_TOWER = 256;
const BATTLE_TYPE_FRONTIER = 4129024;
const BATTLE_TYPE_INGAME_PARTNER = 4194304;
const BATTLE_TYPE_IS_MASTER = 4;
const BATTLE_TYPE_LINK = 2;
const BATTLE_TYPE_LINK_IN_BATTLE = 32;
const BATTLE_TYPE_MULTI = 64;
const BATTLE_TYPE_RECORDED = 16777216;
const BATTLE_TYPE_RECORDED_LINK = 33554432;
const BATTLE_TYPE_TWO_OPPONENTS = 32768;
const BIT_SIDE = 1;
const BOUNCE_HEALTHBOX = 1;
const FRIENDSHIP_EVENT_LEAGUE_BATTLE = 3;
const HITMARKER_NO_ANIMATIONS = 128;
const MAX_BATTLERS_COUNT = 4;
const MAX_LINK_PLAYERS = 4;
const MON_DATA_PERSONALITY = 0;
const MULTI_PARTY_SIZE = 3;
const MULTIUSE_STATE = 0;
const NUM_SPECIES = 412;
const PARTY_SIZE = 6;
const REG_OFFSET_MOSAIC = 76;
const SE_MUGSHOT = 104;
const SPECIES_CASTFORM = 385;
const SPECIES_NONE = 0;
const SPECIES_UNOWN = 201;
const SPRITES_INIT_STATE1 = 1;
const SPRITES_INIT_STATE2 = 2;
const TRAINER_LINK_OPPONENT = 2048;
const TRAINER_STEVEN_PARTNER = 3075;
const TRAINER_UNION_ROOM = 3072;
const VERSION_EMERALD = 3;
const WINOUT_WIN01_BG0 = 1;
const WINOUT_WIN01_BG1 = 2;
const WINOUT_WIN01_BG2 = 4;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const FREE_AND_SET_NULL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const GET_UNOWN_LETTER: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_main.c → SpriteCB_UnusedBattleInit */
export const SpriteCB_UnusedBattleInit: SpriteCallback = (sprite, rt) => {
  sprite.data[0] = 0;
      sprite.callback = SpriteCB_UnusedBattleInit_Main;
};

/** Source: battle_main.c → SpriteCB_UnusedBattleInit_Main */
export const SpriteCB_UnusedBattleInit_Main: SpriteCallback = (sprite, rt) => {
  let arr = gDecompressionBuffer;

      switch (sprite.data[0])
      {
      case 0:
          sprite.data[0]++;
          sprite.data[1] = 0;
          sprite.data[2] = 0x281;
          sprite.data[3] = 0;
          sprite.data[4] = 1;
           
      case 1:
          sprite.data[4]--;
          if (sprite.data[4] == 0)
          {
              let i = 0;
              let r2 = 0;
              let r0 = 0;

              sprite.data[4] = 2;
              r2 = sprite.data[1] + sprite.data[3] * 32;
              r0 = sprite.data[2] - sprite.data[3] * 32;
              for (i = 0; i < 29; i += 2)
              {
                  arr[r2 + i] = 0x3D;
                  arr[r0 + i] = 0x3D;
              }
              sprite.data[3]++;
              if (sprite.data[3] == 21)
              {
                  sprite.data[0]++;
                  sprite.data[1] = 32;
              }
          }
          break;
      case 2:
          sprite.data[1]--;
          if (sprite.data[1] == 20)
              /* TODO scene transition: SetMainCallback2(CB2_InitBattle) */;
          break;
      }
};

/** Source: battle_main.c (failed: empty bodyC) */
export const SpriteCB_VsLetterDummy: SpriteCallback = (_sprite, _rt) => { /* TODO empty bodyC */ };

/** Source: battle_main.c → SpriteCB_VsLetter */
export const SpriteCB_VsLetter: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0] != 0)
          sprite.x = sprite.data[1] + ((sprite.data[2] & 0xFF00) >> 8);
      else
          sprite.x = sprite.data[1] - ((sprite.data[2] & 0xFF00) >> 8);

      sprite.data[2] += 0x180;

      if (sprite.affineAnimEnded)
      {
          FreeSpriteTilesByTag(ANIM_SPRITES_START);
          FreeSpritePaletteByTag(ANIM_SPRITES_START);
          FreeSpriteOamMatrix(sprite);
          rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: battle_main.c → SpriteCB_VsLetterInit */
export const SpriteCB_VsLetterInit: SpriteCallback = (sprite, rt) => {
  rt.StartSpriteAffineAnim(sprite.spriteId, 1);
      sprite.callback = SpriteCB_VsLetter;
      PlaySE(SE_MUGSHOT);
};

/** Source: battle_main.c → SpriteCB_WildMon */
export const SpriteCB_WildMon: SpriteCallback = (sprite, rt) => {
  sprite.callback = SpriteCB_MoveWildMonToRight;
      rt.StartSpriteAnim(sprite.spriteId, 0);
      rt.BeginNormalPaletteFade("0x20000", 0, 10, 10, "RGB(8, 8, 8)");
};

/** Source: battle_main.c → SpriteCB_MoveWildMonToRight */
export const SpriteCB_MoveWildMonToRight: SpriteCallback = (sprite, rt) => {
  if ((gIntroSlideFlags & 1) == 0)
      {
          sprite.x2 += 2;
          if (sprite.x2 == 0)
          {
              sprite.callback = SpriteCB_WildMonShowHealthbox;
          }
      }
};

/** Source: battle_main.c → SpriteCB_WildMonShowHealthbox */
export const SpriteCB_WildMonShowHealthbox: SpriteCallback = (sprite, rt) => {
  if (sprite.animEnded)
      {
          StartHealthboxSlideIn(sprite.data[0]);
          SetHealthboxSpriteVisible(gHealthboxSpriteIds[sprite.data[0]]);
          sprite.callback = SpriteCB_WildMonAnimate;
          rt.StartSpriteAnim(sprite.spriteId, 0);
          rt.BeginNormalPaletteFade("0x20000", 0, 10, 0, "RGB(8, 8, 8)");
      }
};

/** Source: battle_main.c → SpriteCB_WildMonAnimate */
export const SpriteCB_WildMonAnimate: SpriteCallback = (sprite, rt) => {
  if (!rt.gPaletteFade.active)
      {
          BattleAnimateFrontSprite(sprite, sprite.data[2], false, 1);
      }
};

/** Source: battle_main.c → SpriteCB_Flicker */
export const SpriteCB_Flicker: SpriteCallback = (sprite, rt) => {
  sprite.data[4]--;
      if (sprite.data[4] == 0)
      {
          sprite.data[4] = 8;
          sprite.invisible ^= 1;
          sprite.data[3]--;
          if (sprite.data[3] == 0)
          {
              sprite.invisible = false;
              sprite.callback = SpriteCallbackDummy_2;
              sFlickerArray[0] = 0;
          }
      }
};

/** Source: battle_main.c → SpriteCB_FaintOpponentMon */
export const SpriteCB_FaintOpponentMon: SpriteCallback = (sprite, rt) => {
  let battler = sprite.data[0];
      let species = 0;
      let yOffset = 0;

      if (gBattleSpritesDataPtr.battlerData[battler].transformSpecies != 0)
          species = gBattleSpritesDataPtr.battlerData[battler].transformSpecies;
      else
          species = sprite.data[2];

      GetMonData(gEnemyParty[gBattlerPartyIndexes[battler]], MON_DATA_PERSONALITY);   

      if (species == SPECIES_UNOWN)
      {
          let personalityValue = GetMonData(gEnemyParty[gBattlerPartyIndexes[battler]], MON_DATA_PERSONALITY);
          let unownForm = GET_UNOWN_LETTER(personalityValue);
          let unownSpecies = 0;

          if (unownForm == 0)
              unownSpecies = SPECIES_UNOWN;   
          else
              unownSpecies = NUM_SPECIES + unownForm;   

          yOffset = gMonFrontPicCoords[unownSpecies].y_offset;
      }
      else if (species == SPECIES_CASTFORM)
      {
          yOffset = gCastformFrontSpriteCoords[gBattleMonForms[battler]].y_offset;
      }
      else if (species > NUM_SPECIES)
      {
          yOffset = gMonFrontPicCoords[SPECIES_NONE].y_offset;
      }
      else
      {
          yOffset = gMonFrontPicCoords[species].y_offset;
      }sprite.data[3] = Math.trunc(8 - yOffset / 8);
      sprite.data[4] = 1;
      sprite.callback = SpriteCB_AnimFaintOpponent;
};

/** Source: battle_main.c → SpriteCB_AnimFaintOpponent */
export const SpriteCB_AnimFaintOpponent: SpriteCallback = (sprite, rt) => {
  let i = 0;

      if (--sprite.data[4] == 0)
      {
          sprite.data[4] = 2;
          sprite.y2 += 8;  
          if (--sprite.data[3] < 0)
          {
              FreeSpriteOamMatrix(sprite);
              rt.DestroySprite(sprite.spriteId);
          }
          else  
          {
              let dst = gMonSpritesGfxPtr.sprites.byte[GetBattlerPosition(sprite.data[0])] + (gBattleMonForms[sprite.data[0]] << 11) + (sprite.data[3] << 8);

              for (i = 0; i < 0x100; i++)
                  /* deref-write skipped: (dst++) = */ void 0 ;// 0;

              rt.StartSpriteAnim(sprite.spriteId, gBattleMonForms[sprite.data[0]]);
          }
      }
};

/** Source: battle_main.c → SpriteCB_ShowAsMoveTarget */
export const SpriteCB_ShowAsMoveTarget: SpriteCallback = (sprite, rt) => {
  sprite.data[3] = 8;
      sprite.data[4] = sprite.invisible;
      sprite.callback = SpriteCB_BlinkVisible;
};

/** Source: battle_main.c → SpriteCB_BlinkVisible */
export const SpriteCB_BlinkVisible: SpriteCallback = (sprite, rt) => {
  if (--sprite.data[3] == 0)
      {
          sprite.invisible ^= 1;
          sprite.data[3] = 8;
      }
};

/** Source: battle_main.c → SpriteCB_HideAsMoveTarget */
export const SpriteCB_HideAsMoveTarget: SpriteCallback = (sprite, rt) => {
  sprite.invisible = sprite.data[4];
      sprite.data[4] = false;
      sprite.callback = SpriteCallbackDummy_2;
};

/** Source: battle_main.c → SpriteCB_OpponentMonFromBall */
export const SpriteCB_OpponentMonFromBall: SpriteCallback = (sprite, rt) => {
  if (sprite.affineAnimEnded)
      {
          if (!(gHitMarker & HITMARKER_NO_ANIMATIONS) || gBattleTypeFlags & (BATTLE_TYPE_LINK | BATTLE_TYPE_RECORDED_LINK))
          {
              if (HasTwoFramesAnimation(sprite.data[2]))
                  rt.StartSpriteAnim(sprite.spriteId, 1);
          }
          BattleAnimateFrontSprite(sprite, sprite.data[2], true, 1);
      }
};

/** Source: battle_main.c → SpriteCB_BattleSpriteStartSlideLeft */
export const SpriteCB_BattleSpriteStartSlideLeft: SpriteCallback = (sprite, rt) => {
  sprite.callback = SpriteCB_BattleSpriteSlideLeft;
};

/** Source: battle_main.c → SpriteCB_BattleSpriteSlideLeft */
export const SpriteCB_BattleSpriteSlideLeft: SpriteCallback = (sprite, rt) => {
  if (!(gIntroSlideFlags & 1))
      {
          sprite.x2 -= 2;
          if (sprite.x2 == 0)
          {
              sprite.callback = SpriteCB_Idle;
              sprite.data[1] = 0;
          }
      }
};

/** Source: battle_main.c (failed: empty bodyC) */
export const SpriteCB_Idle: SpriteCallback = (_sprite, _rt) => { /* TODO empty bodyC */ };

/** Source: battle_main.c → SpriteCB_FaintSlideAnim */
export const SpriteCB_FaintSlideAnim: SpriteCallback = (sprite, rt) => {
  if (!(gIntroSlideFlags & 1))
      {
          sprite.x2 += sprite.data[1];
          sprite.y2 += sprite.data[2];
      }
};

/** Source: battle_main.c → SpriteCB_BounceEffect */
export const SpriteCB_BounceEffect: SpriteCallback = (sprite, rt) => {
  let bouncerSpriteId = sprite.data[3];
      let index = 0;

      if (sprite.data[4] == BOUNCE_HEALTHBOX)
          index = sprite.data[0];
      else
          index = sprite.data[0];

      _gs(rt, bouncerSpriteId).y2 = Sin(index, sprite.data[2]) + sprite.data[2];
      sprite.data[0] = (sprite.data[0] + sprite.data[1]) & 0xFF;
};

/** Source: battle_main.c → SpriteCB_PlayerMonFromBall */
export const SpriteCB_PlayerMonFromBall: SpriteCallback = (sprite, rt) => {
  if (sprite.affineAnimEnded)
          BattleAnimateBackSprite(sprite, sprite.data[2]);
};

/** Source: battle_main.c → SpriteCB_TrainerThrowObject_Main */
export const SpriteCB_TrainerThrowObject_Main: SpriteCallback = (sprite, rt) => {
  AnimSetCenterToCornerVecX(sprite);
      if (sprite.animEnded)
          sprite.callback = SpriteCB_Idle;
};

/** Source: battle_main.c → SpriteCB_TrainerThrowObject */
export const SpriteCB_TrainerThrowObject: SpriteCallback = (sprite, rt) => {
  rt.StartSpriteAnim(sprite.spriteId, 1);
      sprite.callback = SpriteCB_TrainerThrowObject_Main;
};

/** Source: battle_main.c → CB2_InitBattle */
export const CB2_InitBattle: CB2Callback = (rt) => {
  MoveSaveBlocks_ResetHeap();
      AllocateBattleResources();
      AllocateBattleSpritesData();
      AllocateMonSpritesGfx();
      RecordedBattle_ClearFrontierPassFlag();

      if (gBattleTypeFlags & BATTLE_TYPE_MULTI)
      {
          if (gBattleTypeFlags & BATTLE_TYPE_RECORDED)
          {
              CB2_InitBattleInternal();
          }
          else if (!(gBattleTypeFlags & BATTLE_TYPE_INGAME_PARTNER))
          {
              HandleLinkBattleSetup();
              /* TODO scene transition: SetMainCallback2(CB2_PreInitMultiBattle) */;
          }
          else
          {
              /* TODO scene transition: SetMainCallback2(CB2_PreInitIngamePlayerPartnerBattle) */;
          }
          gBattleCommunication[MULTIUSE_STATE] = 0;
      }
      else
      {
          CB2_InitBattleInternal();
      }
};

/** Source: battle_main.c → CB2_InitBattleInternal */
export const CB2_InitBattleInternal: CB2Callback = (rt) => {
  let i = 0;

      SetHBlankCallback(null);
      /* noop SetVBlankCallback */;

      CpuFill32(0, (VRAM), VRAM_SIZE);

      rt.SetGpuReg(REG_OFFSET_MOSAIC, 0);
      rt.SetGpuReg(REG_OFFSET_WIN0H, DISPLAY_WIDTH);
      rt.SetGpuReg(REG_OFFSET_WIN0V, WIN_RANGE(DISPLAY_HEIGHT / 2, DISPLAY_HEIGHT / 2 + 1));
      rt.SetGpuReg(REG_OFFSET_WININ, 0);
      rt.SetGpuReg(REG_OFFSET_WINOUT, 0);

      gBattle_WIN0H = DISPLAY_WIDTH;

      if (gBattleTypeFlags & BATTLE_TYPE_INGAME_PARTNER && gPartnerTrainerId != TRAINER_STEVEN_PARTNER)
      {
          gBattle_WIN0V = DISPLAY_HEIGHT - 1;
          gBattle_WIN1H = DISPLAY_WIDTH;
          gBattle_WIN1V = 32;
      }
      else
      {gBattle_WIN0V = Math.trunc(WIN_RANGE(DISPLAY_HEIGHT / 2, DISPLAY_HEIGHT / 2) + 1);
          ScanlineEffect_Clear();

          for (i = 0; i < DISPLAY_HEIGHT / 2; i++)
          {
              gScanlineEffectRegBuffers[0][i] = 0xF0;
              gScanlineEffectRegBuffers[1][i] = 0xF0;
          }

          for (; i < DISPLAY_HEIGHT; i++)
          {
              gScanlineEffectRegBuffers[0][i] = 0xFF10;
              gScanlineEffectRegBuffers[1][i] = 0xFF10;
          }

          ScanlineEffect_SetParams(sIntroScanlineParams16Bit);
      }

      ResetPaletteFade();
      gBattle_BG0_X = 0;
      gBattle_BG0_Y = 0;
      (globalThis as any).gBattle_BG1_X = 0;
      (globalThis as any).gBattle_BG1_Y = 0;
      gBattle_BG2_X = 0;
      gBattle_BG2_Y = 0;
      gBattle_BG3_X = 0;
      gBattle_BG3_Y = 0;

      gBattleEnvironment = BattleSetup_GetEnvironmentId();
      if (gBattleTypeFlags & BATTLE_TYPE_RECORDED)
          gBattleEnvironment = BATTLE_ENVIRONMENT_BUILDING;

      InitBattleBgsVideo();
      LoadBattleTextboxAndBackground();
      rt.ResetSpriteData();
      ResetTasks();
      DrawBattleEntryBackground();
      FreeAllSpritePalettes();
      (globalThis as any).gReservedSpritePaletteCount = MAX_BATTLERS_COUNT;
      /* noop SetVBlankCallback */;
      SetUpBattleVarsAndBirchZigzagoon();

      if (gBattleTypeFlags & BATTLE_TYPE_MULTI && gBattleTypeFlags & BATTLE_TYPE_BATTLE_TOWER)
          /* TODO scene transition: SetMainCallback2(CB2_HandleStartMultiPartnerBattle) */;
      else if (gBattleTypeFlags & BATTLE_TYPE_MULTI && gBattleTypeFlags & BATTLE_TYPE_INGAME_PARTNER)
          /* TODO scene transition: SetMainCallback2(CB2_HandleStartMultiPartnerBattle) */;
      else if (gBattleTypeFlags & BATTLE_TYPE_MULTI)
          /* TODO scene transition: SetMainCallback2(CB2_HandleStartMultiBattle) */;
      else
          /* TODO scene transition: SetMainCallback2(CB2_HandleStartBattle) */;

      if (!(gBattleTypeFlags & (BATTLE_TYPE_LINK | BATTLE_TYPE_RECORDED)))
      {
          CreateNPCTrainerParty(gEnemyParty[0], gTrainerBattleOpponent_A, true);
          if (gBattleTypeFlags & BATTLE_TYPE_TWO_OPPONENTS)
              CreateNPCTrainerParty(gEnemyParty[PARTY_SIZE / 2], gTrainerBattleOpponent_B, false);
          SetWildMonHeldItem();
      }

      gMain.inBattle = true;
      gSaveBlock2Ptr.frontier.disableRecordBattle = false;

      for (i = 0; i < PARTY_SIZE; i++)
          AdjustFriendship(gPlayerParty[i], FRIENDSHIP_EVENT_LEAGUE_BATTLE);

      gBattleCommunication[MULTIUSE_STATE] = 0;
};

/** Source: battle_main.c → CB2_HandleStartBattle */
export const CB2_HandleStartBattle: CB2Callback = (rt) => {
  let playerMultiplayerId = 0;
      let enemyMultiplayerId = 0;

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();

      playerMultiplayerId = GetMultiplayerId();
      gBattleScripting.multiplayerId = playerMultiplayerId;
      enemyMultiplayerId = playerMultiplayerId ^ BIT_SIDE;

      switch (gBattleCommunication[MULTIUSE_STATE])
      {
      case 0:
          if (!IsDma3ManagerBusyWithBgCopy())
          {
              ShowBg(0);
              ShowBg(1);
              ShowBg(2);
              ShowBg(3);
              FillAroundBattleWindows();
              gBattleCommunication[MULTIUSE_STATE] = 1;
          }
          if (gWirelessCommType)
              LoadWirelessStatusIndicatorSpriteGfx();
          break;
      case 1:
          if (gBattleTypeFlags & BATTLE_TYPE_LINK)
          {
              if (gReceivedRemoteLinkPlayers)
              {
                  if (IsLinkTaskFinished())
                  {
                       
                      /* deref-write skipped: (gBattleStruct.multiBuffer.linkBattlerHeader.versionSignatureLo) = */ void 0 ;// 0;
                      /* deref-write skipped: (gBattleStruct.multiBuffer.linkBattlerHeader.versionSignatureHi) = */ void 0 ;// 3;
                      BufferPartyVsScreenHealth_AtStart();
                      SetPlayerBerryDataInBattleStruct();

                      if (gTrainerBattleOpponent_A == TRAINER_UNION_ROOM)
                      {
                          gLinkPlayers[0].id = 0;
                          gLinkPlayers[1].id = 1;
                      }

                      SendBlock(BitmaskAllOtherLinkPlayers(), gBattleStruct.multiBuffer.linkBattlerHeader, 0);
                      gBattleCommunication[MULTIUSE_STATE] = 2;
                  }
                  if (gWirelessCommType)
                      CreateWirelessStatusIndicatorSprite(0, 0);
              }
          }
          else
          {
              if (!(gBattleTypeFlags & BATTLE_TYPE_RECORDED))
                  gBattleTypeFlags |= BATTLE_TYPE_IS_MASTER;
              gBattleCommunication[MULTIUSE_STATE] = 15;
              SetAllPlayersBerryData();
          }
          break;
      case 2:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
              let taskId = 0;

              ResetBlockReceivedFlags();
              FindLinkBattleMaster(2, playerMultiplayerId);
              SetAllPlayersBerryData();
              taskId = rt.CreateTask((t) => InitLinkBattleVsScreen(t, rt), 0);
              task.data[1] = 0x10E;
              task.data[2] = 0x5A;
              task.data[5] = 0;
              task.data[3] = gBattleStruct.multiBuffer.linkBattlerHeader.vsScreenHealthFlagsLo | (gBattleStruct.multiBuffer.linkBattlerHeader.vsScreenHealthFlagsHi << 8);
              task.data[4] = gBlockRecvBuffer[enemyMultiplayerId][1];
              RecordedBattle_SetFrontierPassFlagFromHword(gBlockRecvBuffer[playerMultiplayerId][1]);
              RecordedBattle_SetFrontierPassFlagFromHword(gBlockRecvBuffer[enemyMultiplayerId][1]);
              SetDeoxysStats();
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 3:
           
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gPlayerParty, 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 4:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              memcpy(gEnemyParty, gBlockRecvBuffer[enemyMultiplayerId], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 7:
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gPlayerParty[2], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 8:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              memcpy(gEnemyParty[2], gBlockRecvBuffer[enemyMultiplayerId], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 11:
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gPlayerParty[4], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 12:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              memcpy(gEnemyParty[4], gBlockRecvBuffer[enemyMultiplayerId], 0 * 2);

              TryCorrectShedinjaLanguage(gEnemyParty[0]);
              TryCorrectShedinjaLanguage(gEnemyParty[1]);
              TryCorrectShedinjaLanguage(gEnemyParty[2]);
              TryCorrectShedinjaLanguage(gEnemyParty[3]);
              TryCorrectShedinjaLanguage(gEnemyParty[4]);
              TryCorrectShedinjaLanguage(gEnemyParty[5]);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 15:
          InitBattleControllers();
          RecordedBattle_SetTrainerInfo();
          gBattleCommunication[SPRITES_INIT_STATE1] = 0;
          gBattleCommunication[SPRITES_INIT_STATE2] = 0;
          if (gBattleTypeFlags & BATTLE_TYPE_LINK)
          {
               
               
               
              let i = 0;
              for (i = 0; i < 2 && (gLinkPlayers[i].version & 0xFF) == VERSION_EMERALD; i++);

              if (i == 2)
                  gBattleCommunication[MULTIUSE_STATE] = 16;
              else
                  gBattleCommunication[MULTIUSE_STATE] = 18;
          }
          else
          {
              gBattleCommunication[MULTIUSE_STATE] = 18;
          }
          break;
      case 16:
           
          if (IsLinkTaskFinished())
          {
              SendBlock(BitmaskAllOtherLinkPlayers(), gRecordedBattleRngSeed, ((gRecordedBattleRngSeed)?.length ?? 32));
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 17:
           
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
              ResetBlockReceivedFlags();
              if (!(gBattleTypeFlags & BATTLE_TYPE_IS_MASTER))
                  memcpy(gRecordedBattleRngSeed, gBlockRecvBuffer[enemyMultiplayerId], ((gRecordedBattleRngSeed)?.length ?? 32));
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 18:
           
          if (BattleInitAllSprites(gBattleCommunication[SPRITES_INIT_STATE1], gBattleCommunication[SPRITES_INIT_STATE2]))
          {
              gPreBattleCallback1 = gMain.callback1;
              gMain.callback1 = BattleMainCB1;
              /* TODO scene transition: SetMainCallback2(BattleMainCB2) */;
              if (gBattleTypeFlags & BATTLE_TYPE_LINK)
                  gBattleTypeFlags |= BATTLE_TYPE_LINK_IN_BATTLE;
          }
          break;
       
      case 5:
      case 9:
      case 13:
          gBattleCommunication[MULTIUSE_STATE]++;
          gBattleCommunication[1] = 1;
      case 6:
      case 10:
      case 14:
          if (--gBattleCommunication[1] == 0)
              gBattleCommunication[MULTIUSE_STATE]++;
          break;
      }
};

/** Source: battle_main.c → CB2_HandleStartMultiPartnerBattle */
export const CB2_HandleStartMultiPartnerBattle: CB2Callback = (rt) => {
  let playerMultiplayerId = 0;
      let partnerMultiplayerId = 0;

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();

      playerMultiplayerId = GetMultiplayerId();
      gBattleScripting.multiplayerId = playerMultiplayerId;
      partnerMultiplayerId = playerMultiplayerId ^ BIT_SIDE;

      switch (gBattleCommunication[MULTIUSE_STATE])
      {
      case 0:
          if (!IsDma3ManagerBusyWithBgCopy())
          {
              ShowBg(0);
              ShowBg(1);
              ShowBg(2);
              ShowBg(3);
              FillAroundBattleWindows();
              gBattleCommunication[MULTIUSE_STATE] = 1;
          }
          if (gWirelessCommType)
              LoadWirelessStatusIndicatorSpriteGfx();
           
      case 1:
          if (gBattleTypeFlags & BATTLE_TYPE_LINK)
          {
              if (gReceivedRemoteLinkPlayers)
              {
                  let language = 0;

                  gLinkPlayers[0].id = 0;
                  gLinkPlayers[1].id = 2;
                  gLinkPlayers[2].id = 1;
                  gLinkPlayers[3].id = 3;
                  GetFrontierTrainerName(gLinkPlayers[2].name, gTrainerBattleOpponent_A);
                  GetFrontierTrainerName(gLinkPlayers[3].name, gTrainerBattleOpponent_B);
                  GetBattleTowerTrainerLanguage(language, gTrainerBattleOpponent_A);
                  gLinkPlayers[2].language = language;
                  GetBattleTowerTrainerLanguage(language, gTrainerBattleOpponent_B);
                  gLinkPlayers[3].language = language;

                  if (IsLinkTaskFinished())
                  {
                       
                      /* deref-write skipped: (gBattleStruct.multiBuffer.linkBattlerHeader.versionSignatureLo) = */ void 0 ;// 0;
                      /* deref-write skipped: (gBattleStruct.multiBuffer.linkBattlerHeader.versionSignatureHi) = */ void 0 ;// 3;
                      BufferPartyVsScreenHealth_AtStart();
                      SetPlayerBerryDataInBattleStruct();
                      SendBlock(BitmaskAllOtherLinkPlayers(), gBattleStruct.multiBuffer.linkBattlerHeader, 0);
                      gBattleCommunication[MULTIUSE_STATE] = 2;
                  }

                  if (gWirelessCommType)
                      CreateWirelessStatusIndicatorSprite(0, 0);
              }
          }
          else
          {
              if (!(gBattleTypeFlags & BATTLE_TYPE_RECORDED))
                  gBattleTypeFlags |= BATTLE_TYPE_IS_MASTER;
              gBattleCommunication[MULTIUSE_STATE] = 13;
              SetAllPlayersBerryData();
          }
          break;
      case 2:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
              let taskId = 0;

              ResetBlockReceivedFlags();
              FindLinkBattleMaster(2, playerMultiplayerId);
              SetAllPlayersBerryData();
              taskId = rt.CreateTask((t) => InitLinkBattleVsScreen(t, rt), 0);
              task.data[1] = 0x10E;
              task.data[2] = 0x5A;
              task.data[5] = 0;
              task.data[3] = 0x145;
              task.data[4] = 0x145;
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 3:
           
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gPlayerParty, 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 4:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              if (gLinkPlayers[playerMultiplayerId].id != 0)
              {
                  memcpy(gPlayerParty, gBlockRecvBuffer[partnerMultiplayerId], 0 * 2);
                  memcpy(gPlayerParty[MULTI_PARTY_SIZE], gBlockRecvBuffer[playerMultiplayerId], 0 * 2);
              }
              else
              {
                  memcpy(gPlayerParty, gBlockRecvBuffer[playerMultiplayerId], 0 * 2);
                  memcpy(gPlayerParty[MULTI_PARTY_SIZE], gBlockRecvBuffer[partnerMultiplayerId], 0 * 2);
              }
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 5:
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gPlayerParty[2], 0);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 6:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              if (gLinkPlayers[playerMultiplayerId].id != 0)
              {
                  memcpy(gPlayerParty[2], gBlockRecvBuffer[partnerMultiplayerId], 0);
                  memcpy(gPlayerParty[2 + MULTI_PARTY_SIZE], gBlockRecvBuffer[playerMultiplayerId], 0);
              }
              else
              {
                  memcpy(gPlayerParty[2], gBlockRecvBuffer[playerMultiplayerId], 0);
                  memcpy(gPlayerParty[2 + MULTI_PARTY_SIZE], gBlockRecvBuffer[partnerMultiplayerId], 0);
              }
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 7:
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gEnemyParty, 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 8:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              if (GetMultiplayerId() != 0)
                  memcpy(gEnemyParty, gBlockRecvBuffer[0], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 9:
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gEnemyParty[2], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 10:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              if (GetMultiplayerId() != 0)
                  memcpy(gEnemyParty[2], gBlockRecvBuffer[0], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 11:
          if (IsLinkTaskFinished())
          {
               
              SendBlock(BitmaskAllOtherLinkPlayers(), gEnemyParty[4], 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 12:
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
               
              ResetBlockReceivedFlags();
              if (GetMultiplayerId() != 0)
                  memcpy(gEnemyParty[4], gBlockRecvBuffer[0], 0 * 2);

              TryCorrectShedinjaLanguage(gPlayerParty[0]);
              TryCorrectShedinjaLanguage(gPlayerParty[1]);
              TryCorrectShedinjaLanguage(gPlayerParty[2]);
              TryCorrectShedinjaLanguage(gPlayerParty[3]);
              TryCorrectShedinjaLanguage(gPlayerParty[4]);
              TryCorrectShedinjaLanguage(gPlayerParty[5]);
              TryCorrectShedinjaLanguage(gEnemyParty[0]);
              TryCorrectShedinjaLanguage(gEnemyParty[1]);
              TryCorrectShedinjaLanguage(gEnemyParty[2]);
              TryCorrectShedinjaLanguage(gEnemyParty[3]);
              TryCorrectShedinjaLanguage(gEnemyParty[4]);
              TryCorrectShedinjaLanguage(gEnemyParty[5]);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 13:
          InitBattleControllers();
          RecordedBattle_SetTrainerInfo();
          gBattleCommunication[SPRITES_INIT_STATE1] = 0;
          gBattleCommunication[SPRITES_INIT_STATE2] = 0;
          if (gBattleTypeFlags & BATTLE_TYPE_LINK)
              gBattleCommunication[MULTIUSE_STATE] = 14;
          else
              gBattleCommunication[MULTIUSE_STATE] = 16;
          break;
      case 14:
           
          if (IsLinkTaskFinished())
          {
              SendBlock(BitmaskAllOtherLinkPlayers(), gRecordedBattleRngSeed, ((gRecordedBattleRngSeed)?.length ?? 32));
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 15:
           
          if ((GetBlockReceivedStatus() & 3) == 3)
          {
              ResetBlockReceivedFlags();
              if (!(gBattleTypeFlags & BATTLE_TYPE_IS_MASTER))
                  memcpy(gRecordedBattleRngSeed, gBlockRecvBuffer[partnerMultiplayerId], ((gRecordedBattleRngSeed)?.length ?? 32));
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 16:
           
          if (BattleInitAllSprites(gBattleCommunication[SPRITES_INIT_STATE1], gBattleCommunication[SPRITES_INIT_STATE2]))
          {
              TrySetLinkBattleTowerEnemyPartyLevel();
              gPreBattleCallback1 = gMain.callback1;
              gMain.callback1 = BattleMainCB1;
              /* TODO scene transition: SetMainCallback2(BattleMainCB2) */;
              if (gBattleTypeFlags & BATTLE_TYPE_LINK)
                  gBattleTypeFlags |= BATTLE_TYPE_LINK_IN_BATTLE;
          }
          break;
      }
};

/** Source: battle_main.c → CB2_PreInitMultiBattle */
export const CB2_PreInitMultiBattle: CB2Callback = (rt) => {
  let i = 0;
      let playerMultiplierId = 0;
      let numPlayers = MAX_BATTLERS_COUNT;
      let blockMask = 0xF;
      let savedBattleTypeFlags = 0;
      let savedCallback: any = null;

      if (gBattleTypeFlags & BATTLE_TYPE_BATTLE_TOWER)
      {
          numPlayers = 2;
          blockMask = 3;
      }

      playerMultiplierId = GetMultiplayerId();
      gBattleScripting.multiplayerId = playerMultiplierId;
      savedCallback = gBattleStruct.savedCallback;
      savedBattleTypeFlags = gBattleStruct.savedBattleTypeFlags;

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();

      switch (gBattleCommunication[MULTIUSE_STATE])
      {
      case 0:
          if (gReceivedRemoteLinkPlayers && IsLinkTaskFinished())
          {
              sMultiPartnerPartyBuffer = Alloc(((gMultiPartnerParty)?.length ?? 32));
              SetMultiPartnerMenuParty(0);
              SendBlock(BitmaskAllOtherLinkPlayers(), sMultiPartnerPartyBuffer, ((gMultiPartnerParty)?.length ?? 32));
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 1:
          if ((GetBlockReceivedStatus() & blockMask) == blockMask)
          {
              ResetBlockReceivedFlags();
              for (i = 0; i < numPlayers; i++)
              {
                  if (i == playerMultiplierId)
                      continue;

                  if (numPlayers == MAX_LINK_PLAYERS)
                  {
                      if ((!(gLinkPlayers[i].id & 1) && !(gLinkPlayers[playerMultiplierId].id & 1))
                          || (gLinkPlayers[i].id & 1 && gLinkPlayers[playerMultiplierId].id & 1))
                      {
                          memcpy(gMultiPartnerParty, gBlockRecvBuffer[i], ((gMultiPartnerParty)?.length ?? 32));
                      }
                  }
                  else
                  {
                      memcpy(gMultiPartnerParty, gBlockRecvBuffer[i], ((gMultiPartnerParty)?.length ?? 32));
                  }
              }
              gBattleCommunication[MULTIUSE_STATE]++;
              savedCallback = gMain.savedCallback;
              savedBattleTypeFlags = gBattleTypeFlags;
              gMain.savedCallback = CB2_PreInitMultiBattle;
              ShowPartyMenuToShowcaseMultiBattleParty();
          }
          break;
      case 2:
          if (IsLinkTaskFinished() && !rt.gPaletteFade.active)
          {
              gBattleCommunication[MULTIUSE_STATE]++;
              if (gWirelessCommType)
                  SetLinkStandbyCallback();
              else
                  SetCloseLinkCallback();
          }
          break;
      case 3:
          if (gWirelessCommType)
          {
              if (IsLinkRfuTaskFinished())
              {
                  gBattleTypeFlags = savedBattleTypeFlags;
                  gMain.savedCallback = savedCallback;
                  /* TODO scene transition: SetMainCallback2(CB2_InitBattleInternal) */;
                  FREE_AND_SET_NULL(sMultiPartnerPartyBuffer);
              }
          }
          else if (gReceivedRemoteLinkPlayers == 0)
          {
              gBattleTypeFlags = savedBattleTypeFlags;
              gMain.savedCallback = savedCallback;
              /* TODO scene transition: SetMainCallback2(CB2_InitBattleInternal) */;
              FREE_AND_SET_NULL(sMultiPartnerPartyBuffer);
          }
          break;
      }
};

/** Source: battle_main.c → CB2_PreInitIngamePlayerPartnerBattle */
export const CB2_PreInitIngamePlayerPartnerBattle: CB2Callback = (rt) => {
  let savedBattleTypeFlags = 0;
      let savedCallback: any = null;

      savedCallback = gBattleStruct.savedCallback;
      savedBattleTypeFlags = gBattleStruct.savedBattleTypeFlags;

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();

      switch (gBattleCommunication[MULTIUSE_STATE])
      {
      case 0:
          sMultiPartnerPartyBuffer = Alloc(((gMultiPartnerParty)?.length ?? 32));
          SetMultiPartnerMenuParty(MULTI_PARTY_SIZE);
          gBattleCommunication[MULTIUSE_STATE]++;
          savedCallback = gMain.savedCallback;
          savedBattleTypeFlags = gBattleTypeFlags;
          gMain.savedCallback = CB2_PreInitIngamePlayerPartnerBattle;
          ShowPartyMenuToShowcaseMultiBattleParty();
          break;
      case 1:
          if (!rt.gPaletteFade.active)
          {
              gBattleCommunication[MULTIUSE_STATE] = 2;
              gBattleTypeFlags = savedBattleTypeFlags;
              gMain.savedCallback = savedCallback;
              /* TODO scene transition: SetMainCallback2(CB2_InitBattleInternal) */;
              FREE_AND_SET_NULL(sMultiPartnerPartyBuffer);
          }
          break;
      }
};

/** Source: battle_main.c → CB2_HandleStartMultiBattle */
export const CB2_HandleStartMultiBattle: CB2Callback = (rt) => {
  let playerMultiplayerId = 0;
      let id = 0;
      let _var = 0;

      playerMultiplayerId = GetMultiplayerId();
      gBattleScripting.multiplayerId = playerMultiplayerId;

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();

      switch (gBattleCommunication[MULTIUSE_STATE])
      {
      case 0:
          if (!IsDma3ManagerBusyWithBgCopy())
          {
              ShowBg(0);
              ShowBg(1);
              ShowBg(2);
              ShowBg(3);
              FillAroundBattleWindows();
              gBattleCommunication[MULTIUSE_STATE] = 1;
          }
          if (gWirelessCommType)
              LoadWirelessStatusIndicatorSpriteGfx();
          break;
      case 1:
          if (gBattleTypeFlags & BATTLE_TYPE_LINK)
          {
              if (gReceivedRemoteLinkPlayers)
              {
                  if (IsLinkTaskFinished())
                  {
                       
                      /* deref-write skipped: (gBattleStruct.multiBuffer.linkBattlerHeader.versionSignatureLo) = */ void 0 ;// 0;
                      /* deref-write skipped: (gBattleStruct.multiBuffer.linkBattlerHeader.versionSignatureHi) = */ void 0 ;// 3;
                      BufferPartyVsScreenHealth_AtStart();
                      SetPlayerBerryDataInBattleStruct();

                      SendBlock(BitmaskAllOtherLinkPlayers(), gBattleStruct.multiBuffer.linkBattlerHeader, 0);
                      gBattleCommunication[MULTIUSE_STATE]++;
                  }
                  if (gWirelessCommType)
                      CreateWirelessStatusIndicatorSprite(0, 0);
              }
          }
          else
          {
              if (!(gBattleTypeFlags & BATTLE_TYPE_RECORDED))
                  gBattleTypeFlags |= BATTLE_TYPE_IS_MASTER;
              gBattleCommunication[MULTIUSE_STATE] = 7;
              SetAllPlayersBerryData();
          }
          break;
      case 2:
          if ((GetBlockReceivedStatus() & 0xF) == 0xF)
          {
              ResetBlockReceivedFlags();
              FindLinkBattleMaster(4, playerMultiplayerId);
              SetAllPlayersBerryData();
              SetDeoxysStats();
              _var = rt.CreateTask((t) => InitLinkBattleVsScreen(t, rt), 0);
              _gt(rt, _var).data[1] = 0x10E;
              _gt(rt, _var).data[2] = 0x5A;
              _gt(rt, _var).data[5] = 0;
              _gt(rt, _var).data[3] = 0;
              _gt(rt, _var).data[4] = 0;

              for (id = 0; id < MAX_LINK_PLAYERS; id++)
              {
                  RecordedBattle_SetFrontierPassFlagFromHword(gBlockRecvBuffer[id][1]);
                  switch (gLinkPlayers[id].id)
                  {
                  case 0:
                      _gt(rt, _var).data[3] |= gBlockRecvBuffer[id][1] & 0x3;
                      break;
                  case 1:
                      _gt(rt, _var).data[4] |= gBlockRecvBuffer[id][1] & 0x3;
                      break;
                  case 2:
                      _gt(rt, _var).data[3] |= (gBlockRecvBuffer[id][1] & 0x3) << 6;
                      break;
                  case 3:
                      _gt(rt, _var).data[4] |= (gBlockRecvBuffer[id][1] & 0x3) << 6;
                      break;
                  }
              }
              ZeroEnemyPartyMons();
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          else
          {
              break;
          }
           
      case 3:
          if (IsLinkTaskFinished())
          {
              SendBlock(BitmaskAllOtherLinkPlayers(), gPlayerParty, 0 * 2);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 4:
          if ((GetBlockReceivedStatus() & 0xF) == 0xF)
          {
              ResetBlockReceivedFlags();
              for (id = 0; id < MAX_LINK_PLAYERS; id++)
              {
                  if (id == playerMultiplayerId)
                  {
                      switch (gLinkPlayers[id].id)
                      {
                      case 0:
                      case 3:
                          memcpy(gPlayerParty, gBlockRecvBuffer[id], 0 * 2);
                          break;
                      case 1:
                      case 2:
                          memcpy(gPlayerParty + MULTI_PARTY_SIZE, gBlockRecvBuffer[id], 0 * 2);
                          break;
                      }
                  }
                  else
                  {
                      if ((!(gLinkPlayers[id].id & 1) && !(gLinkPlayers[playerMultiplayerId].id & 1))
                       || ((gLinkPlayers[id].id & 1) && (gLinkPlayers[playerMultiplayerId].id & 1)))
                      {
                          switch (gLinkPlayers[id].id)
                          {
                          case 0:
                          case 3:
                              memcpy(gPlayerParty, gBlockRecvBuffer[id], 0 * 2);
                              break;
                          case 1:
                          case 2:
                              memcpy(gPlayerParty + MULTI_PARTY_SIZE, gBlockRecvBuffer[id], 0 * 2);
                              break;
                          }
                      }
                      else
                      {
                          switch (gLinkPlayers[id].id)
                          {
                          case 0:
                          case 3:
                              memcpy(gEnemyParty, gBlockRecvBuffer[id], 0 * 2);
                              break;
                          case 1:
                          case 2:
                              memcpy(gEnemyParty + MULTI_PARTY_SIZE, gBlockRecvBuffer[id], 0 * 2);
                              break;
                          }
                      }
                  }
              }
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 5:
          if (IsLinkTaskFinished())
          {
              SendBlock(BitmaskAllOtherLinkPlayers(), gPlayerParty + 2, 0);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 6:
          if ((GetBlockReceivedStatus() & 0xF) == 0xF)
          {
              ResetBlockReceivedFlags();
              for (id = 0; id < MAX_LINK_PLAYERS; id++)
              {
                  if (id == playerMultiplayerId)
                  {
                      switch (gLinkPlayers[id].id)
                      {
                      case 0:
                      case 3:
                          memcpy(gPlayerParty + 2, gBlockRecvBuffer[id], 0);
                          break;
                      case 1:
                      case 2:
                          memcpy(gPlayerParty + 5, gBlockRecvBuffer[id], 0);
                          break;
                      }
                  }
                  else
                  {
                      if ((!(gLinkPlayers[id].id & 1) && !(gLinkPlayers[playerMultiplayerId].id & 1))
                       || ((gLinkPlayers[id].id & 1) && (gLinkPlayers[playerMultiplayerId].id & 1)))
                      {
                          switch (gLinkPlayers[id].id)
                          {
                          case 0:
                          case 3:
                              memcpy(gPlayerParty + 2, gBlockRecvBuffer[id], 0);
                              break;
                          case 1:
                          case 2:
                              memcpy(gPlayerParty + 5, gBlockRecvBuffer[id], 0);
                              break;
                          }
                      }
                      else
                      {
                          switch (gLinkPlayers[id].id)
                          {
                          case 0:
                          case 3:
                              memcpy(gEnemyParty + 2, gBlockRecvBuffer[id], 0);
                              break;
                          case 1:
                          case 2:
                              memcpy(gEnemyParty + 5, gBlockRecvBuffer[id], 0);
                              break;
                          }
                      }
                  }
              }
              TryCorrectShedinjaLanguage(gPlayerParty[0]);
              TryCorrectShedinjaLanguage(gPlayerParty[1]);
              TryCorrectShedinjaLanguage(gPlayerParty[2]);
              TryCorrectShedinjaLanguage(gPlayerParty[3]);
              TryCorrectShedinjaLanguage(gPlayerParty[4]);
              TryCorrectShedinjaLanguage(gPlayerParty[5]);

              TryCorrectShedinjaLanguage(gEnemyParty[0]);
              TryCorrectShedinjaLanguage(gEnemyParty[1]);
              TryCorrectShedinjaLanguage(gEnemyParty[2]);
              TryCorrectShedinjaLanguage(gEnemyParty[3]);
              TryCorrectShedinjaLanguage(gEnemyParty[4]);
              TryCorrectShedinjaLanguage(gEnemyParty[5]);

              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 7:
          InitBattleControllers();
          RecordedBattle_SetTrainerInfo();
          gBattleCommunication[SPRITES_INIT_STATE1] = 0;
          gBattleCommunication[SPRITES_INIT_STATE2] = 0;
          if (gBattleTypeFlags & BATTLE_TYPE_LINK)
          {
              for (id = 0; id < MAX_LINK_PLAYERS && (gLinkPlayers[id].version & 0xFF) == VERSION_EMERALD; id++);

              if (id == MAX_LINK_PLAYERS)
                  gBattleCommunication[MULTIUSE_STATE] = 8;
              else
                  gBattleCommunication[MULTIUSE_STATE] = 10;
          }
          else
          {
              gBattleCommunication[MULTIUSE_STATE] = 10;
          }
          break;
      case 8:
          if (IsLinkTaskFinished())
          {
              let ptr = gBattleStruct.multiBuffer.battleVideo;
              ptr[0] = gBattleTypeFlags;
              ptr[1] = gRecordedBattleRngSeed;  
              SendBlock(BitmaskAllOtherLinkPlayers(), ptr, 0);
              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 9:
          if ((GetBlockReceivedStatus() & 0xF) == 0xF)
          {
              ResetBlockReceivedFlags();
              for (_var = 0; _var < 4; _var++)
              {
                  let blockValue = gBlockRecvBuffer[_var][0];
                  if (blockValue & 4)
                  {
                      memcpy(gRecordedBattleRngSeed, gBlockRecvBuffer[_var][2], ((gRecordedBattleRngSeed)?.length ?? 32));
                      break;
                  }
              }

              gBattleCommunication[MULTIUSE_STATE]++;
          }
          break;
      case 10:
          if (BattleInitAllSprites(gBattleCommunication[SPRITES_INIT_STATE1], gBattleCommunication[SPRITES_INIT_STATE2]))
          {
              gPreBattleCallback1 = gMain.callback1;
              gMain.callback1 = BattleMainCB1;
              /* TODO scene transition: SetMainCallback2(BattleMainCB2) */;
              if (gBattleTypeFlags & BATTLE_TYPE_LINK)
              {
                  gTrainerBattleOpponent_A = TRAINER_LINK_OPPONENT;
                  gBattleTypeFlags |= BATTLE_TYPE_LINK_IN_BATTLE;
              }
          }
          break;
      }
};

/** Source: battle_main.c → CB2_QuitRecordedBattle */
export const CB2_QuitRecordedBattle: CB2Callback = (rt) => {
  UpdatePaletteFade();
      if (!rt.gPaletteFade.active)
      {
          m4aMPlayStop(gMPlayInfo_SE1);
          m4aMPlayStop(gMPlayInfo_SE2);
          FreeRestoreBattleData();
          FreeAllWindowBuffers();
          /* TODO scene transition: SetMainCallback2(gMain.savedCallback) */;
      }
};

/** Source: battle_main.c → CB2_InitEndLinkBattle */
export const CB2_InitEndLinkBattle: CB2Callback = (rt) => {
  let i = 0;
      let taskId = 0;

      SetHBlankCallback(null);
      /* noop SetVBlankCallback */;
      gBattleTypeFlags &= ~BATTLE_TYPE_LINK_IN_BATTLE;

      if (gBattleTypeFlags & BATTLE_TYPE_FRONTIER)
      {
          /* TODO scene transition: SetMainCallback2(gMain.savedCallback) */;
          FreeBattleResources();
          FreeBattleSpritesData();
          FreeMonSpritesGfx();
      }
      else
      {
          CpuFill32(0, (VRAM), VRAM_SIZE);
          rt.SetGpuReg(REG_OFFSET_MOSAIC, 0);
          rt.SetGpuReg(REG_OFFSET_WIN0H, DISPLAY_WIDTH);
          rt.SetGpuReg(REG_OFFSET_WIN0V, WIN_RANGE(DISPLAY_HEIGHT / 2, DISPLAY_HEIGHT / 2 + 1));
          rt.SetGpuReg(REG_OFFSET_WININ, 0);
          rt.SetGpuReg(REG_OFFSET_WINOUT, 0);
          gBattle_WIN0H = DISPLAY_WIDTH;gBattle_WIN0V = Math.trunc(WIN_RANGE(DISPLAY_HEIGHT / 2, DISPLAY_HEIGHT / 2) + 1);
          ScanlineEffect_Clear();

          i = 0;
          while (i < 80)
          {
              gScanlineEffectRegBuffers[0][i] = 0xF0;
              gScanlineEffectRegBuffers[1][i] = 0xF0;
              i++;
          }

          while (i < 160)
          {
              gScanlineEffectRegBuffers[0][i] = 0xFF10;
              gScanlineEffectRegBuffers[1][i] = 0xFF10;
              i++;
          }

          ResetPaletteFade();

          gBattle_BG0_X = 0;
          gBattle_BG0_Y = 0;
          (globalThis as any).gBattle_BG1_X = 0;
          (globalThis as any).gBattle_BG1_Y = 0;
          gBattle_BG2_X = 0;
          gBattle_BG2_Y = 0;
          gBattle_BG3_X = 0;
          gBattle_BG3_Y = 0;

          InitBattleBgsVideo();
          LoadCompressedPalette(gBattleTextboxPalette, BG_PLTT_ID(0), 2 * PLTT_SIZE_4BPP);
          LoadBattleMenuWindowGfx();
          rt.ResetSpriteData();
          ResetTasks();
          DrawBattleEntryBackground();
          rt.SetGpuReg(REG_OFFSET_WINOUT, WINOUT_WIN01_BG0 | WINOUT_WIN01_BG1 | WINOUT_WIN01_BG2 | WINOUT_WIN01_OBJ | WINOUT_WIN01_CLR);
          FreeAllSpritePalettes();
          (globalThis as any).gReservedSpritePaletteCount = MAX_BATTLERS_COUNT;
          /* noop SetVBlankCallback */;

           
          taskId = rt.CreateTask((t) => InitLinkBattleVsScreen(t, rt), 0);
          task.data[1] = 0x10E;
          task.data[2] = 0x5A;
          task.data[5] = 1;
          BufferPartyVsScreenHealth_AtEnd(taskId);

          /* TODO scene transition: SetMainCallback2(CB2_EndLinkBattle) */;
          gBattleCommunication[MULTIUSE_STATE] = 0;
      }
};

/** Source: battle_main.c → CB2_EndLinkBattle */
export const CB2_EndLinkBattle: CB2Callback = (rt) => {
  EndLinkBattleInSteps();
      AnimateSprites();
      BuildOamBuffer();
      RunTextPrinters();
      UpdatePaletteFade();
      RunTasks();
};

/** Source: battle_main.c → CB2_InitAskRecordBattle */
export const CB2_InitAskRecordBattle: CB2Callback = (rt) => {
  let i = 0;

      SetHBlankCallback(null);
      /* noop SetVBlankCallback */;
      CpuFill32(0, (VRAM), VRAM_SIZE);
      ResetPaletteFade();
      gBattle_BG0_X = 0;
      gBattle_BG0_Y = 0;
      (globalThis as any).gBattle_BG1_X = 0;
      (globalThis as any).gBattle_BG1_Y = 0;
      gBattle_BG2_X = 0;
      gBattle_BG2_Y = 0;
      gBattle_BG3_X = 0;
      gBattle_BG3_Y = 0;
      InitBattleBgsVideo();
      rt.SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_OBJ_ON | DISPCNT_OBJ_1D_MAP);
      LoadBattleMenuWindowGfx();

      for (i = 0; i < 2; i++)
          LoadChosenBattleElement(i);

      rt.ResetSpriteData();
      ResetTasks();
      FreeAllSpritePalettes();
      (globalThis as any).gReservedSpritePaletteCount = MAX_BATTLERS_COUNT;
      /* noop SetVBlankCallback */;
      /* TODO scene transition: SetMainCallback2(CB2_AskRecordBattle) */;
      rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0x10, 0, "RGB_BLACK");
      gBattleCommunication[MULTIUSE_STATE] = 0;
};

/** Source: battle_main.c → CB2_AskRecordBattle */
export const CB2_AskRecordBattle: CB2Callback = (rt) => {
  AskRecordBattle();
      AnimateSprites();
      BuildOamBuffer();
      RunTextPrinters();
      UpdatePaletteFade();
      RunTasks();
};
