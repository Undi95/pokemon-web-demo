// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/field_screen_effect.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const DISPCNT_WIN1_ON = 16384;
const DISPLAY_TILE_HEIGHT = 20;
const DISPLAY_TILE_WIDTH = 30;
const F_RFU_ERROR_6 = 8192;
const F_RFU_ERROR_7 = 16384;
const FADE_TO_BLACK = 1;
const LOCALID_PLAYER = 255;
const MOVEMENT_ACTION_WALK_NORMAL_DOWN = 8;
const MOVEMENT_ACTION_WALK_NORMAL_UP = 9;
const WININ_WIN0_BG_ALL = 15;
const WININ_WIN0_CLR = 32;
const WININ_WIN0_OBJ = 16;
const WINOUT_WIN01_BG1 = 2;
const WINOUT_WIN01_BG2 = 4;
const WINOUT_WIN01_BG3 = 8;
const WINOUT_WIN01_OBJ = 16;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_BLDALPHA: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_BLDCNT: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_DISPCNT: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_WININ: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const REG_WINOUT: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: field_screen_effect.c → Task_WaitForUnionRoomFade */
export const Task_WaitForUnionRoomFade: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (WaitForWeatherFadeIn() == true)
          rt.DestroyTask(taskId);
};

/** Source: field_screen_effect.c → Task_WaitForFadeAndEnableScriptCtx */
export const Task_WaitForFadeAndEnableScriptCtx: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (WaitForWeatherFadeIn() == true)
      {
          rt.DestroyTask(taskID);
          ScriptContext_Enable();
      }
};

/** Source: field_screen_effect.c → Task_ReturnToFieldCableLink */
export const Task_ReturnToFieldCableLink: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      switch (task.data[0])
      {
      case 0:
          task.data[1] = CreateTask_ReestablishCableClubLink();
          task.data[0]++;
          break;
      case 1:
          if (_gt(rt, task.data[1]).isActive != true)
          {
              WarpFadeInScreen();
              task.data[0]++;
          }
          break;
      case 2:
          if (WaitForWeatherFadeIn() == true)
          {
              UnlockPlayerFieldControls();
              rt.DestroyTask(taskId);
          }
          break;
      }
};

/** Source: field_screen_effect.c → Task_ReturnToFieldWirelessLink */
export const Task_ReturnToFieldWirelessLink: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      switch (task.data[0])
      {
      case 0:
          SetLinkStandbyCallback();
          task.data[0]++;
          break;
      case 1:
          if (!IsLinkTaskFinished())
          {
              if (++task.data[1] > 1800)
                  RfuSetErrorParams(F_RFU_ERROR_6 | F_RFU_ERROR_7);
          }
          else
          {
              WarpFadeInScreen();
              task.data[0]++;
          }
          break;
      case 2:
          if (WaitForWeatherFadeIn() == true)
          {
              StartSendingKeysToLink();
              UnlockPlayerFieldControls();
              rt.DestroyTask(taskId);
          }
          break;
      }
};

/** Source: field_screen_effect.c → Task_ReturnToFieldRecordMixing */
export const Task_ReturnToFieldRecordMixing: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      switch (task.data[0])
      {
      case 0:
          SetLinkStandbyCallback();
          task.data[0]++;
          break;
      case 1:
          if (IsLinkTaskFinished())
              task.data[0]++;
          break;
      case 2:
          StartSendingKeysToLink();
          ResetAllMultiplayerState();
          UnlockPlayerFieldControls();
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_ExitDoor */
export const Task_ExitDoor: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);
      let x = task.data[2];
      let y = task.data[3];

      switch (task.data[0])
      {
      case 0:
          SetPlayerVisibility(false);
          FreezeObjectEvents();
          PlayerGetDestCoords(x, y);
          FieldSetDoorOpened(x, y);
          task.data[0] = 1;
          break;
      case 1:
          if (WaitForWeatherFadeIn())
          {
              let objEventId = 0;
              SetPlayerVisibility(true);
              objEventId = GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0);
              ObjectEventSetHeldMovement(gObjectEvents[objEventId], MOVEMENT_ACTION_WALK_NORMAL_DOWN);
              task.data[0] = 2;
          }
          break;
      case 2:
          if (IsPlayerStandingStill())
          {
              let objEventId = 0;
              task.data[1] = FieldAnimateDoorClose(x, y);
              objEventId = GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0);
              ObjectEventClearHeldMovementIfFinished(gObjectEvents[objEventId]);
              task.data[0] = 3;
          }
          break;
      case 3:
          if (task.data[1] < 0 || _gt(rt, task.data[1]).isActive != true)
          {
              UnfreezeObjectEvents();
              task.data[0] = 4;
          }
          break;
      case 4:
          UnlockPlayerFieldControls();
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_ExitNonAnimDoor */
export const Task_ExitNonAnimDoor: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);
      let x = task.data[2];
      let y = task.data[3];

      switch (task.data[0])
      {
      case 0:
          SetPlayerVisibility(false);
          FreezeObjectEvents();
          PlayerGetDestCoords(x, y);
          task.data[0] = 1;
          break;
      case 1:
          if (WaitForWeatherFadeIn())
          {
              let objEventId = 0;
              SetPlayerVisibility(true);
              objEventId = GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0);
              ObjectEventSetHeldMovement(gObjectEvents[objEventId], GetWalkNormalMovementAction(GetPlayerFacingDirection()));
              task.data[0] = 2;
          }
          break;
      case 2:
          if (IsPlayerStandingStill())
          {
              UnfreezeObjectEvents();
              task.data[0] = 3;
          }
          break;
      case 3:
          UnlockPlayerFieldControls();
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_ExitNonDoor */
export const Task_ExitNonDoor: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[0])
      {
      case 0:
          FreezeObjectEvents();
          LockPlayerFieldControls();
          task.data[0]++;
          break;
      case 1:
          if (WaitForWeatherFadeIn())
          {
              UnfreezeObjectEvents();
              UnlockPlayerFieldControls();
              rt.DestroyTask(taskId);
          }
          break;
      }
};

/** Source: field_screen_effect.c → Task_WaitForFadeShowStartMenu */
export const Task_WaitForFadeShowStartMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (WaitForWeatherFadeIn() == true)
      {
          rt.DestroyTask(taskId);
          rt.CreateTask((t) => Task_ShowStartMenu(t, rt), 80);
      }
};

/** Source: field_screen_effect.c → Task_ReturnToFieldNoScript */
export const Task_ReturnToFieldNoScript: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (WaitForWeatherFadeIn() == 1)
      {
          UnlockPlayerFieldControls();
          rt.DestroyTask(taskId);
          ScriptUnfreezeObjectEvents();
      }
};

/** Source: field_screen_effect.c → Task_DoCableClubWarp */
export const Task_DoCableClubWarp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      switch (task.data[0])
      {
      case 0:
          LockPlayerFieldControls();
          task.data[0]++;
          break;
      case 1:
          if (!PaletteFadeActive() && BGMusicStopped())
              task.data[0]++;
          break;
      case 2:
          WarpIntoMap();
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldCableClub) */;
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_ReturnToWorldFromLinkRoom */
export const Task_ReturnToWorldFromLinkRoom: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 0:
          ClearLinkCallback_2();
          FadeScreen(FADE_TO_BLACK, 0);
          TryFadeOutOldMapMusic();
          /* TODO sound PlaySE */;
          data[0]++;
          break;
      case 1:
          if (!PaletteFadeActive() && BGMusicStopped())
          {
              SetCloseLinkCallback();
              data[0]++;
          }
          break;
      case 2:
          if (!gReceivedRemoteLinkPlayers)
          {
              WarpIntoMap();
              /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
              rt.DestroyTask(taskId);
          }
          break;
      }
};

/** Source: field_screen_effect.c → Task_WarpAndLoadMap */
export const Task_WarpAndLoadMap: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      switch (task.data[0])
      {
      case 0:
          FreezeObjectEvents();
          LockPlayerFieldControls();
          task.data[0]++;
          break;
      case 1:
          if (!PaletteFadeActive())
          {
              if (task.data[1] == 0)
              {
                  ClearMirageTowerPulseBlendEffect();
                  task.data[1] = 1;
              }
              if (BGMusicStopped())
                  task.data[0]++;
          }
          break;
      case 2:
          WarpIntoMap();
          /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_DoDoorWarp */
export const Task_DoDoorWarp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);
      let x = task.data[2];
      let y = task.data[3];

      switch (task.data[0])
      {
      case 0:
          FreezeObjectEvents();
          PlayerGetDestCoords(x, y);
          PlaySE(GetDoorSoundEffect(x, y - 1));
          task.data[1] = FieldAnimateDoorOpen(x, y - 1);
          task.data[0] = 1;
          break;
      case 1:
          if (task.data[1] < 0 || _gt(rt, task.data[1]).isActive != true)
          {
              let objEventId = 0;
              objEventId = GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0);
              ObjectEventClearHeldMovementIfActive(gObjectEvents[objEventId]);
              objEventId = GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0);
              ObjectEventSetHeldMovement(gObjectEvents[objEventId], MOVEMENT_ACTION_WALK_NORMAL_UP);
              task.data[0] = 2;
          }
          break;
      case 2:
          if (IsPlayerStandingStill())
          {
              let objEventId = 0;
              task.data[1] = FieldAnimateDoorClose(x, y - 1);
              objEventId = GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0);
              ObjectEventClearHeldMovementIfFinished(gObjectEvents[objEventId]);
              SetPlayerVisibility(false);
              task.data[0] = 3;
          }
          break;
      case 3:
          if (task.data[1] < 0 || _gt(rt, task.data[1]).isActive != true)
          {
              task.data[0] = 4;
          }
          break;
      case 4:
          TryFadeOutOldMapMusic();
          WarpFadeOutScreen();
          PlayRainStoppingSoundEffect();
          task.data[0] = 0;
          task.func = Task_WarpAndLoadMap;
          break;
      }
};

/** Source: field_screen_effect.c → Task_DoContestHallWarp */
export const Task_DoContestHallWarp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      switch (task.data[0])
      {
      case 0:
          FreezeObjectEvents();
          LockPlayerFieldControls();
          task.data[0]++;
          break;
      case 1:
          if (!PaletteFadeActive() && BGMusicStopped())
          {
              task.data[0]++;
          }
          break;
      case 2:
          WarpIntoMap();
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContestHall) */;
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_WaitForFlashUpdate */
export const Task_WaitForFlashUpdate: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!FuncIsActiveTask(UpdateFlashLevelEffect))
      {
          ScriptContext_Enable();
          rt.DestroyTask(taskId);
      }
};

/** Source: field_screen_effect.c → Task_SpinEnterWarp */
export const Task_SpinEnterWarp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[0])
      {
      case 0:
          FreezeObjectEvents();
          LockPlayerFieldControls();
          DoPlayerSpinEntrance();
          task.data[0]++;
          break;
      case 1:
          if (WaitForWeatherFadeIn() && IsPlayerSpinEntranceActive() != true)
          {
              UnfreezeObjectEvents();
              UnlockPlayerFieldControls();
              rt.DestroyTask(taskId);
          }
          break;
      }
};

/** Source: field_screen_effect.c → Task_SpinExitWarp */
export const Task_SpinExitWarp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = _gt(rt, taskId);

      switch (task.data[0])
      {
      case 0:
          FreezeObjectEvents();
          LockPlayerFieldControls();
          /* TODO sound PlaySE */;
          DoPlayerSpinExit();
          task.data[0]++;
          break;
      case 1:
          if (!IsPlayerSpinExitActive())
          {
              WarpFadeOutScreen();
              task.data[0]++;
          }
          break;
      case 2:
          if (!PaletteFadeActive() && BGMusicStopped())
              task.data[0]++;
          break;
      case 3:
          WarpIntoMap();
          /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_OrbEffect */
export const Task_OrbEffect: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 0:
          data[6] = REG_DISPCNT;
          data[7] = REG_BLDCNT;
          data[8] = REG_BLDALPHA;
          data[9] = REG_WININ;
          data[10] = REG_WINOUT;
          ClearGpuRegBits(REG_OFFSET_DISPCNT, DISPCNT_WIN1_ON);
          SetGpuRegBits(REG_OFFSET_BLDCNT, gOrbEffectBackgroundLayerFlags[0]);
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(12, 7));
          rt.SetGpuReg(REG_OFFSET_WININ, WININ_WIN0_BG_ALL | WININ_WIN0_OBJ | WININ_WIN0_CLR);
          rt.SetGpuReg(REG_OFFSET_WINOUT, WINOUT_WIN01_BG1 | WINOUT_WIN01_BG2 | WINOUT_WIN01_BG3 | WINOUT_WIN01_OBJ);
          SetBgTilemapPalette(0, 0, 0, DISPLAY_TILE_WIDTH, DISPLAY_TILE_HEIGHT, 0xF);
          ScheduleBgCopyTilemapToVram(0);
          SetOrbFlashScanlineEffectWindowBoundaries(gScanlineEffectRegBuffers[0][0], data[2], data[3], 1);
          CpuFastSet(gScanlineEffectRegBuffers[0], gScanlineEffectRegBuffers[1], 480);
          ScanlineEffect_SetParams(sFlashEffectParams);
          data[0] = 1;
          break;
      case 1:
          BgDmaFill(0, PIXEL_FILL(1), 0, 1);
          LoadOrbEffectPalette(data[1]);
          StartUpdateOrbFlashEffect(data[2], data[3], 1, 160, 1, 2);
          data[0] = 2;
          break;
      case 2:
          if (!FuncIsActiveTask(UpdateOrbFlashEffect))
          {
              ScriptContext_Enable();
              data[0] = 3;
          }
          break;
      case 3:
          InstallCameraPanAheadCallback();
          SetCameraPanningCallback(null);
          data[5] = 0;
          data[4] = 4;
          data[0] = 4;
          break;
      case 4:
          if (--data[4] == 0)
          {
              let panning = 0;
              data[4] = 4;
              data[5] ^= 1;
              if (data[5])
                  panning = 4;
              else
                  panning = -4;
              SetCameraPanning(0, panning);
          }
          break;
      case 6:
          InstallCameraPanAheadCallback();
          data[4] = 8;
          data[0] = 7;
          break;
      case 7:
          if (--data[4] == 0)
          {
              data[4] = 8;
              data[5] ^= 1;
              if (UpdateOrbEffectBlend(data[5]) == true)
              {
                  data[0] = 5;
                  BgDmaFill(0, PIXEL_FILL(0), 0, 1);
              }
          }
          break;
      case 5:
          rt.SetGpuReg(REG_OFFSET_WIN0H, 255);
          rt.SetGpuReg(REG_OFFSET_DISPCNT, data[6]);
          rt.SetGpuReg(REG_OFFSET_BLDCNT, data[7]);
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, data[8]);
          rt.SetGpuReg(REG_OFFSET_WININ, data[9]);
          rt.SetGpuReg(REG_OFFSET_WINOUT, data[10]);
          ScriptContext_Enable();
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: field_screen_effect.c → Task_EnableScriptAfterMusicFade */
export const Task_EnableScriptAfterMusicFade: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (BGMusicStopped() == true)
      {
          rt.DestroyTask(taskId);
          ScriptContext_Enable();
      }
};
