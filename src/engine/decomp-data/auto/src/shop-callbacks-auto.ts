// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/shop.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  ClearWindowTilemap,
  FillBgTilemapBufferRect_Palette0,
  FreeAllSpritePalettes,
  JOY_NEW,
  OAM,
  OAM_SIZE,
  PutWindowTilemap,
  RemoveWindow,
  ResetPaletteFade,
  ResetTasks,
  ScanlineEffect_Stop,
  StringExpandPlaceholders,
  UpdatePaletteFade,
  gMain,
  gSaveBlock1Ptr,
  gStringVar4,
  m4aSongNumStart,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const BAG_ITEM_CAPACITY_DIGITS = 2;
const COLORID_GRAY_CURSOR = 2;
const COLORID_NORMAL = 0;
const FADE_TO_BLACK = 1;
const ITEM_POKE_BALL = 4;
const ITEM_PREMIER_BALL = 12;
const LIST_CANCEL = -2;
const LIST_NOTHING_CHOSEN = -1;
const MART_TYPE_DECOR = 1;
const MART_TYPE_DECOR2 = 2;
const MART_TYPE_NORMAL = 0;
const MAX_BAG_ITEM_CAPACITY = 99;
const MAX_ITEM_DIGITS = 3;
const MENU_B_PRESSED = -1;
const MENU_NOTHING_CHOSEN = -2;
const POCKET_TM_HM = 3;
const POKENEWS_SLATEPORT = 1;
const SE_SELECT = 5;
const SPRITE_NONE = 255;
const STR_CONV_MODE_LEFT_ALIGN = 0;
const STR_CONV_MODE_RIGHT_ALIGN = 1;
const TASK_NONE = 255;
const WIN_ITEM_DESCRIPTION = 2;
const WIN_ITEM_LIST = 0;
const WIN_QUANTITY_IN_BAG = 3;
const WIN_QUANTITY_PRICE = 4;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: shop.c → Task_ShopMenu */
export const Task_ShopMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let inputCode = Menu_ProcessInputNoWrap();
      switch (inputCode)
      {
      case MENU_NOTHING_CHOSEN:
          break;
      case MENU_B_PRESSED:
          m4aSongNumStart(SE_SELECT);
          Task_HandleShopMenuQuit(taskId);
          break;
      default:
          sMartInfo.menuActions[inputCode].func.void_u8(taskId);
          break;
      }
};

/** Source: shop.c → Task_HandleShopMenuBuy */
export const Task_HandleShopMenuBuy: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      data[8] = CB2_InitBuyMenu >> 16;
      data[9] = CB2_InitBuyMenu;
      task.func = (t) => Task_GoToBuyOrSellMenu(t, rt);
      FadeScreen(FADE_TO_BLACK, 0);
};

/** Source: shop.c → Task_HandleShopMenuSell */
export const Task_HandleShopMenuSell: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      data[8] = CB2_GoToSellMenu >> 16;
      data[9] = CB2_GoToSellMenu;
      task.func = (t) => Task_GoToBuyOrSellMenu(t, rt);
      FadeScreen(FADE_TO_BLACK, 0);
};

/** Source: shop.c → Task_HandleShopMenuQuit */
export const Task_HandleShopMenuQuit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  ClearStdWindowAndFrameToTransparent(sMartInfo.windowId, 2);  
      RemoveWindow(sMartInfo.windowId);
      TryPutSmartShopperOnAir();
      UnlockPlayerFieldControls();
      rt.DestroyTask(taskId);

      if (sMartInfo.callback)
          sMartInfo.callback();
};

/** Source: shop.c → Task_GoToBuyOrSellMenu */
export const Task_GoToBuyOrSellMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      if (!rt.gPaletteFade.active)
      {
          rt.DestroyTask(taskId);
          SetMainCallback2((MainCallback)(data[8] << 16 | data[9]));
      }
};

/** Source: shop.c → Task_ReturnToShopMenu */
export const Task_ReturnToShopMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (IsWeatherNotFadingIn() == true)
      {
          if (sMartInfo.martType == MART_TYPE_DECOR2)
              DisplayItemMessageOnField(taskId, gText_CanIHelpWithAnythingElse, ShowShopMenuAfterExitingBuyOrSellMenu);
          else
              DisplayItemMessageOnField(taskId, gText_AnythingElseICanHelp, ShowShopMenuAfterExitingBuyOrSellMenu);
      }
};

/** Source: shop.c → Task_BuyMenu */
export const Task_BuyMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (!rt.gPaletteFade.active)
      {
          let itemId = ListMenu_ProcessInput(data[7]);
          ListMenuGetScrollAndRow(data[7], sShopData.scrollOffset, sShopData.selectedRow);

          switch (itemId)
          {
          case LIST_NOTHING_CHOSEN:
              break;
          case LIST_CANCEL:
              m4aSongNumStart(SE_SELECT);
              ExitBuyMenu(taskId);
              break;
          default:
              m4aSongNumStart(SE_SELECT);
              data[5] = itemId;
              ClearWindowTilemap(WIN_ITEM_DESCRIPTION);
              BuyMenuRemoveScrollIndicatorArrows();
              BuyMenuPrintCursor(data[7], COLORID_GRAY_CURSOR);

              if (sMartInfo.martType == MART_TYPE_NORMAL)
                  sShopData.totalCost = (GetItemPrice(itemId) >> IsPokeNewsActive(POKENEWS_SLATEPORT));
              else
                  sShopData.totalCost = gDecorations[itemId].price;

              if (!IsEnoughMoney(gSaveBlock1Ptr.money, sShopData.totalCost))
              {
                  BuyMenuDisplayMessage(taskId, gText_YouDontHaveMoney, BuyMenuReturnToItemList);
              }
              else
              {
                  if (sMartInfo.martType == MART_TYPE_NORMAL)
                  {
                      CopyItemName(itemId, gStringVar1);
                      if (GetItemPocket(itemId) == POCKET_TM_HM)
                      {
                          StringCopy(gStringVar2, gMoveNames[ItemIdToBattleMoveId(itemId)]);
                          BuyMenuDisplayMessage(taskId, gText_Var1CertainlyHowMany2, Task_BuyHowManyDialogueInit);
                      }
                      else
                      {
                          BuyMenuDisplayMessage(taskId, gText_Var1CertainlyHowMany, Task_BuyHowManyDialogueInit);
                      }
                  }
                  else
                  {
                      StringCopy(gStringVar1, gDecorations[itemId].name);
                      ConvertIntToDecimalStringN(gStringVar2, sShopData.totalCost, STR_CONV_MODE_LEFT_ALIGN, 6);

                      if (sMartInfo.martType == MART_TYPE_DECOR)
                          StringExpandPlaceholders(gStringVar4, gText_Var1IsItThatllBeVar2);
                      else  
                          StringExpandPlaceholders(gStringVar4, gText_YouWantedVar1ThatllBeVar2);

                      BuyMenuDisplayMessage(taskId, gStringVar4, BuyMenuConfirmPurchase);
                  }
              }
              break;
          }
      }
};

/** Source: shop.c → Task_BuyHowManyDialogueInit */
export const Task_BuyHowManyDialogueInit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      let quantityInBag = CountTotalItemQuantityInBag(data[5]);
      let maxQuantity = 0;

      DrawStdFrameWithCustomTileAndPalette(WIN_QUANTITY_IN_BAG, false, 1, 13);
      ConvertIntToDecimalStringN(gStringVar1, quantityInBag, STR_CONV_MODE_RIGHT_ALIGN, MAX_ITEM_DIGITS + 1);
      StringExpandPlaceholders(gStringVar4, gText_InBagVar1);
      BuyMenuPrint(WIN_QUANTITY_IN_BAG, gStringVar4, 0, 1, 0, COLORID_NORMAL);
      data[1] = 1;
      DrawStdFrameWithCustomTileAndPalette(WIN_QUANTITY_PRICE, false, 1, 13);
      BuyMenuPrintItemQuantityAndPrice(taskId);
      ScheduleBgCopyTilemapToVram(0);

      maxQuantity = GetMoney(gSaveBlock1Ptr.money) / sShopData.totalCost;

      if (maxQuantity > MAX_BAG_ITEM_CAPACITY)
          sShopData.maxQuantity = MAX_BAG_ITEM_CAPACITY;
      else
          sShopData.maxQuantity = maxQuantity;

      task.func = (t) => Task_BuyHowManyDialogueHandleInput(t, rt);
};

/** Source: shop.c → Task_BuyHowManyDialogueHandleInput */
export const Task_BuyHowManyDialogueHandleInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (AdjustQuantityAccordingToDPadInput(data[1], sShopData.maxQuantity) == true)
      {
          sShopData.totalCost = (GetItemPrice(data[5]) >> IsPokeNewsActive(POKENEWS_SLATEPORT)) * data[1];
          BuyMenuPrintItemQuantityAndPrice(taskId);
      }
      else
      {
          if (JOY_NEW(A_BUTTON))
          {
              m4aSongNumStart(SE_SELECT);
              ClearStdWindowAndFrameToTransparent(WIN_QUANTITY_PRICE, false);
              ClearStdWindowAndFrameToTransparent(WIN_QUANTITY_IN_BAG, false);
              ClearWindowTilemap(WIN_QUANTITY_PRICE);
              ClearWindowTilemap(WIN_QUANTITY_IN_BAG);
              PutWindowTilemap(WIN_ITEM_LIST);
              CopyItemName(data[5], gStringVar1);
              ConvertIntToDecimalStringN(gStringVar2, data[1], STR_CONV_MODE_LEFT_ALIGN, BAG_ITEM_CAPACITY_DIGITS);
              ConvertIntToDecimalStringN(gStringVar3, sShopData.totalCost, STR_CONV_MODE_LEFT_ALIGN, 6);
              BuyMenuDisplayMessage(taskId, gText_Var1AndYouWantedVar2, BuyMenuConfirmPurchase);
          }
          else if (JOY_NEW(B_BUTTON))
          {
              m4aSongNumStart(SE_SELECT);
              ClearStdWindowAndFrameToTransparent(WIN_QUANTITY_PRICE, false);
              ClearStdWindowAndFrameToTransparent(WIN_QUANTITY_IN_BAG, false);
              ClearWindowTilemap(WIN_QUANTITY_PRICE);
              ClearWindowTilemap(WIN_QUANTITY_IN_BAG);
              BuyMenuReturnToItemList(taskId);
          }
      }
};

/** Source: shop.c → Task_ReturnToItemListAfterItemPurchase */
export const Task_ReturnToItemListAfterItemPurchase: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (JOY_NEW(A_BUTTON | B_BUTTON))
      {
          m4aSongNumStart(SE_SELECT);

           
          if (data[5] == ITEM_POKE_BALL && data[1] >= 10 && AddBagItem(ITEM_PREMIER_BALL, 1) == true)
              BuyMenuDisplayMessage(taskId, gText_ThrowInPremierBall, BuyMenuReturnToItemList);
          else
              BuyMenuReturnToItemList(taskId);
      }
};

/** Source: shop.c → Task_ReturnToItemListAfterDecorationPurchase */
export const Task_ReturnToItemListAfterDecorationPurchase: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (JOY_NEW(A_BUTTON | B_BUTTON))
      {
          m4aSongNumStart(SE_SELECT);
          BuyMenuReturnToItemList(taskId);
      }
};

/** Source: shop.c → Task_ExitBuyMenu */
export const Task_ExitBuyMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          RemoveMoneyLabelObject();
          BuyMenuFreeMemory();
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToField) */;
          rt.DestroyTask(taskId);
      }
};

/** Source: shop.c → CB2_ExitSellMenu */
export const CB2_ExitSellMenu: CB2Callback = (rt) => {
  gFieldCallback = MapPostLoadHook_ReturnToShopMenu;
      /* TODO scene transition: SetMainCallback2(CB2_ReturnToField) */;
};

/** Source: shop.c → CB2_BuyMenu */
export const CB2_BuyMenu: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      DoScheduledBgTilemapCopiesToVram();
      UpdatePaletteFade();
};

/** Source: shop.c → CB2_InitBuyMenu */
export const CB2_InitBuyMenu: CB2Callback = (rt) => {
  let taskId = 0;

      switch (gMain.state)
      {
      case 0:
          SetVBlankHBlankCallbacksToNull();
          CpuFastFill(0, OAM, OAM_SIZE);
          ScanlineEffect_Stop();
          ResetTempTileDataBuffers();
          FreeAllSpritePalettes();
          ResetPaletteFade();
          rt.ResetSpriteData();
          ResetTasks();
          ClearScheduledBgCopiesToVram();
          sShopData = AllocZeroed(0);
          sShopData.scrollIndicatorsTaskId = TASK_NONE;
          sShopData.itemSpriteIds[0] = SPRITE_NONE;
          sShopData.itemSpriteIds[1] = SPRITE_NONE;
          BuyMenuBuildListMenuTemplate();
          BuyMenuInitBgs();
          FillBgTilemapBufferRect_Palette0(0, 0, 0, 0, 0x20, 0x20);
          FillBgTilemapBufferRect_Palette0(1, 0, 0, 0, 0x20, 0x20);
          FillBgTilemapBufferRect_Palette0(2, 0, 0, 0, 0x20, 0x20);
          FillBgTilemapBufferRect_Palette0(3, 0, 0, 0, 0x20, 0x20);
          BuyMenuInitWindows();
          BuyMenuDecompressBgGraphics();
          gMain.state++;
          break;
      case 1:
          if (!FreeTempTileDataBuffersIfPossible())
              gMain.state++;
          break;
      default:
          BuyMenuDrawGraphics();
          BuyMenuAddScrollIndicatorArrows();
          taskId = rt.CreateTask((t) => Task_BuyMenu(t, rt), 8);
          task.data[7] = ListMenuInit(gMultiuseListMenuTemplate, 0, 0);
          BlendPalettes(PALETTES_ALL, 16, RGB_BLACK);
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 16, 0, "RGB_BLACK");
          /* noop SetVBlankCallback */;
          /* TODO scene transition: SetMainCallback2(CB2_BuyMenu) */;
          break;
      }
};
