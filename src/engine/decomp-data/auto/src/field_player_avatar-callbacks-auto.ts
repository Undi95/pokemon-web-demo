// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/field_player_avatar.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-27
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer,
} from '../../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const BOB_JUST_MON = 2;
const PLAYER_AVATAR_STATE_NORMAL = 0;
const SUBSPRITES_OFF = 0;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: field_player_avatar.c → Task_PushBoulder */
export const Task_PushBoulder: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sPushBoulderFuncs[task.data[0]](_gt(rt, taskId),
                                                       gObjectEvents[gPlayerAvatar.objectEventId],
                                                       gObjectEvents[task.data[1]]))
          ;
};

/** Source: field_player_avatar.c → Task_StopSurfingInit */
export const Task_StopSurfingInit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let playerObjEvent: any = gObjectEvents[gPlayerAvatar.objectEventId];

      if (ObjectEventIsMovementOverridden(playerObjEvent))
      {
          if (!ObjectEventClearHeldMovementIfFinished(playerObjEvent))
              return;
      }
      SetSurfBlob_BobState(playerObjEvent.fieldEffectSpriteId, BOB_JUST_MON);
      ObjectEventSetHeldMovement(playerObjEvent, GetJumpSpecialMovementAction(task.data[0]));
      task.func = (t) => Task_WaitStopSurfing(t, rt);
};

/** Source: field_player_avatar.c → Task_WaitStopSurfing */
export const Task_WaitStopSurfing: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let playerObjEvent: any = gObjectEvents[gPlayerAvatar.objectEventId];

      if (ObjectEventClearHeldMovementIfFinished(playerObjEvent))
      {
          ObjectEventSetGraphicsId(playerObjEvent, GetPlayerAvatarGraphicsIdByStateId(PLAYER_AVATAR_STATE_NORMAL));
          ObjectEventSetHeldMovement(playerObjEvent, GetFaceDirectionMovementAction(playerObjEvent.facingDirection));
          gPlayerAvatar.preventStep = false;
          UnlockPlayerFieldControls();
          rt.DestroySprite(playerObjEvent.fieldEffectSpriteId);
           
          playerObjEvent.triggerGroundEffectsOnMove = true;
          rt.DestroyTask(taskId);
      }
};

/** Source: field_player_avatar.c → Task_Fishing */
export const Task_Fishing: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFishingStateFuncs[task.data[0]](_gt(rt, taskId)))
          ;
};

/** Source: field_player_avatar.c → Task_DoPlayerSpinExit */
export const Task_DoPlayerSpinExit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let object: any = gObjectEvents[gPlayerAvatar.objectEventId];
      let sprite: any = _gs(rt, object.spriteId);
      const data = task.data;

      switch (data[0])
      {
          case 0:  
              if (!ObjectEventClearHeldMovementIfFinished(object))
                  return;

              SetSpinStartFacingDir(object.facingDirection);
              data[1] = 0;
              data[2] = 1;
              data[3] = (sprite.y + sprite.y2) << 4;
              sprite.y2 = 0;
              CameraObjectFreeze();
              object.fixedPriority = true;
              rt.gba.oam[sprite.oamIndex].priority = 0;
              sprite.subpriority = 0;
              sprite.subspriteMode = SUBSPRITES_OFF;
              data[0]++;
          case 1:  
              TrySpinPlayerForWarp(object, data[1]);

               
              data[3] -= data[2];
              data[2] += 3;
              sprite.y = data[3] >> 4;

               
              if (sprite.y + gTotalCameraPixelOffsetY < -32)
                  data[0]++;
              break;
          case 2:
              rt.DestroyTask(taskId);
              break;
      }
};

/** Source: field_player_avatar.c → Task_DoPlayerSpinEntrance */
export const Task_DoPlayerSpinEntrance: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let object: any = gObjectEvents[gPlayerAvatar.objectEventId];
      let sprite: any = _gs(rt, object.spriteId);
      const data = task.data;

      switch (data[0])
      {
          case 0:
               
               
               
              data[5] = GetSpinStartFacingDir();
              ObjectEventForceSetHeldMovement(object, GetFaceDirectionMovementAction(sSpinDirections[data[5]]));
              data[1] = 0;
              data[2] = 116;
              data[4] = sprite.y;
              data[6] = rt.gba.oam[sprite.oamIndex].priority;
              data[7] = sprite.subpriority;
              data[3] = -(sprite.y2 + 32) * 16;
              sprite.y2 = 0;
              CameraObjectFreeze();
              object.fixedPriority = true;
              rt.gba.oam[sprite.oamIndex].priority = 1;
              sprite.subpriority = 0;
              sprite.subspriteMode = SUBSPRITES_OFF;
              data[0]++;
          case 1:  
              TrySpinPlayerForWarp(object, data[1]);

               
              data[3] += data[2];
              data[2] -= 3;
              if (data[2] < 4)
                  data[2] = 4;
              sprite.y = data[3] >> 4;

               
              if (sprite.y >= data[4])
              {
                  sprite.y = data[4];
                  data[8] = 0;
                  data[0]++;
              }
              break;
          case 2:  
              TrySpinPlayerForWarp(object, data[1]);
              if (++data[8] > 8)
                  data[0]++;
              break;
          case 3:  
              if (data[5] == TrySpinPlayerForWarp(object, data[1]))
              {
                  object.fixedPriority = 0;
                  rt.gba.oam[sprite.oamIndex].priority = data[6];
                  sprite.subpriority = data[7];
                  CameraObjectReset();
                  rt.DestroyTask(taskId);
              }
              break;
      }
};
