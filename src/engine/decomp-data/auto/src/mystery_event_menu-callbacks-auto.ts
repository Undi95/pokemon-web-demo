// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/mystery_event_menu.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../../decomp-helpers';
import {
  CopyWindowToVram,
  CpuFill32,
  DeactivateAllTextPrinters,
  FillBgTilemapBufferRect_Palette0,
  FillWindowPixelBuffer,
  FreeAllSpritePalettes,
  InitBgsFromTemplates,
  InitWindows,
  IsTextPrinterActive,
  JOY_NEW,
  PutWindowTilemap,
  ResetBgsAndClearDma3BusyFlags,
  ResetTasks,
  RunTextPrinters,
  ShowBg,
  UpdatePaletteFade,
  gMain,
  gStringVar4,
  m4aSongNumStart,
} from '../../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const COPYWIN_FULL = 3;
const DISPLAY_TILE_HEIGHT = 20;
const DISPLAY_TILE_WIDTH = 30;
const EXCHANGE_DIFF_SELECTIONS = 3;
const LINK_STAT_CONN_ESTABLISHED = 64;
const LINK_STAT_MASTER = 32;
const LINK_STAT_PLAYER_COUNT = 28;
const LINKTYPE_MYSTERY_EVENT = 21761;
const MEVENT_STATUS_LOAD_ERROR = 1;
const SAVE_NORMAL = 0;
const SE_PIN = 21;
const SE_SELECT = 5;
const WIN_LOADING = 1;
const WIN_MSG = 2;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: mystery_event_menu.c → CB2_InitMysteryEventMenu */
export const CB2_InitMysteryEventMenu: CB2Callback = (rt) => {
  rt.ResetSpriteData();
      FreeAllSpritePalettes();
      ResetTasks();
      rt.SetVBlankCallback(VBlankCB);
      ResetBgsAndClearDma3BusyFlags(0);
      InitBgsFromTemplates(0, sBgTemplates, ((sBgTemplates)?.length ?? 0));
      if (InitWindows(sWindowTemplates))
      {
          let i = 0;

          DeactivateAllTextPrinters();
          for (i = 0; i < ((sWindowTemplates)?.length ?? 0) - 1; i++)
              FillWindowPixelBuffer(i, PIXEL_FILL(0));

          FillBgTilemapBufferRect_Palette0(0, 0, 0, 0, DISPLAY_TILE_WIDTH, DISPLAY_TILE_HEIGHT);
          LoadUserWindowBorderGfx(0, 1, BG_PLTT_ID(13));
          Menu_LoadStdPalAt(BG_PLTT_ID(14));
          rt.SetGpuReg(REG_OFFSET_DISPCNT, DISPCNT_MODE_0 | DISPCNT_OBJ_1D_MAP | DISPCNT_BG0_ON);
          rt.SetGpuReg(REG_OFFSET_BLDCNT, 0);
          rt.CreateTask((t) => Task_DestroySelf(t, rt), 0);
          StopMapMusic();
          RunTasks();
          AnimateSprites();
          BuildOamBuffer();
          RunTextPrinters();
          UpdatePaletteFade();
          SetBackdropFromColor(RGB_BLACK);
          rt.SetMainCallback2(CB2_MysteryEventMenu);
      }
};

/** Source: mystery_event_menu.c → CB2_MysteryEventMenu */
export const CB2_MysteryEventMenu: CB2Callback = (rt) => {
  switch (gMain.state)
      {
      case 0:
          DrawStdFrameWithCustomTileAndPalette(WIN_MSG, true, 1, 0xD);
          PutWindowTilemap(WIN_MSG);
          CopyWindowToVram(WIN_MSG, COPYWIN_FULL);
          ShowBg(0);
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0x10, 0, "RGB_BLACK");
          gMain.state++;
          break;
      case 1:
          if (!rt.gPaletteFade.active)
          {
              PrintMysteryMenuText(WIN_MSG, gText_LinkStandby2, 1, 2, 1);
              gMain.state++;
          }
          break;
      case 2:
          if (!IsTextPrinterActive(WIN_MSG))
          {
              gMain.state++;
              gLinkType = LINKTYPE_MYSTERY_EVENT;
              OpenLink();
          }
          break;
      case 3:
          if ((gLinkStatus & LINK_STAT_MASTER) && (gLinkStatus & LINK_STAT_PLAYER_COUNT) > 4)
          {
              PlaySE(SE_PIN);
              PrintMysteryMenuText(WIN_MSG, gText_PressAToLoadEvent, 1, 2, 1);
              gMain.state++;
          }
          if (JOY_NEW(B_BUTTON))
          {
              PlaySE(SE_SELECT);
              CloseLink();
              gMain.state = 15;
          }
          break;
      case 4:
          if (!IsTextPrinterActive(WIN_MSG))
              gMain.state++;
          break;
      case 5:
          if (GetLinkPlayerCount_2() == 2)
          {
              if (JOY_NEW(A_BUTTON))
              {
                  PlaySE(SE_SELECT);
                  CheckShouldAdvanceLinkState();
                  DrawStdFrameWithCustomTileAndPalette(WIN_LOADING, true, 1, 0xD);
                  PrintMysteryMenuText(WIN_LOADING, gText_LoadingEvent, 1, 2, 0);
                  PutWindowTilemap(WIN_LOADING);
                  CopyWindowToVram(WIN_LOADING, COPYWIN_FULL);
                  gMain.state++;
              }
              else if (JOY_NEW(B_BUTTON))
              {
                  PlaySE(SE_SELECT);
                  CloseLink();
                  gMain.state = 15;
              }
          }
          else
          {
              GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
              PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
              gMain.state = 13;
          }
          break;
      case 6:
          if (IsLinkConnectionEstablished())
          {
              if (gReceivedRemoteLinkPlayers)
              {
                  if (GetLinkPlayerDataExchangeStatusTimed(2, 2) == EXCHANGE_DIFF_SELECTIONS)
                  {
                      SetCloseLinkCallback();
                      GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
                      PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
                      gMain.state = 13;
                  }
                  else if (CheckLanguageMatch())
                  {
                      PrintMysteryMenuText(WIN_MSG, gText_DontRemoveCableTurnOff, 1, 2, 1);
                      gMain.state++;
                  }
                  else
                  {
                      CloseLink();
                      GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
                      PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
                      gMain.state = 13;
                  }
              }
          }
          else if (JOY_NEW(B_BUTTON))
          {
              PlaySE(SE_SELECT);
              CloseLink();
              gMain.state = 15;
          }
          break;
      case 7:
          if (!IsTextPrinterActive(WIN_MSG))
              gMain.state++;
          break;
      case 8:
          if (GetBlockReceivedStatus())
          {
              ResetBlockReceivedFlags();
              gMain.state++;
          }
          break;
      case 9:
          gMain.state++;
          break;
      case 10:
          SetCloseLinkCallback();
          gMain.state++;
          break;
      case 11:
          if (gReceivedRemoteLinkPlayers == 0)
          {
              let status = RunMysteryEventScript(gDecompressionBuffer);
              CpuFill32(0, gDecompressionBuffer, 0x7D4);
              if (!GetEventLoadMessage(gStringVar4, status))
                  TrySavingData(SAVE_NORMAL);
              gMain.state++;
          }
          break;
      case 12:
          PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
          gMain.state++;
          break;
      case 13:
          if (!IsTextPrinterActive(WIN_MSG))
          {
              gMain.state++;
              sUnused = 0;
          }
          break;
      case 14:
          if (JOY_NEW(A_BUTTON))
          {
              PlaySE(SE_SELECT);
              gMain.state++;
          }
          break;
      case 15:
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 0x10, "RGB_BLACK");
          gMain.state++;
          break;
      case 16:
          if (!rt.gPaletteFade.active)
              DoSoftReset();
          break;
      }

      if (gLinkStatus & LINK_STAT_CONN_ESTABLISHED && !IsLinkMaster())
      {
          CloseLink();
          GetEventLoadMessage(gStringVar4, MEVENT_STATUS_LOAD_ERROR);
          PrintMysteryMenuText(WIN_MSG, gStringVar4, 1, 2, 1);
          gMain.state = 13;
      }

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      RunTextPrinters();
      UpdatePaletteFade();
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
