// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_transition_frontier.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_transition_frontier.c → SpriteCB_LogoCircleSlide */
export const SpriteCB_LogoCircleSlide: SpriteCallback = (sprite, rt) => {
  let data = sprite.data;

      if (sprite.x == data[0] && sprite.y == data[1])
      {
          sprite.callback = SpriteCallbackDummy;
      }
      else
      {
          if (data[4] == data[6])
          {
              sprite.x += data[2];
              data[4] = 0;
          }
          else
          {
              data[4]++;
          }

          if (data[5] == data[7])
          {
              sprite.y += data[3];
              data[5] = 0;
          }
          else
          {
              data[5]++;
          }
      }
};

/** Source: battle_transition_frontier.c → SpriteCB_LogoCircleSpiral */
export const SpriteCB_LogoCircleSpiral: SpriteCallback = (sprite, rt) => {
  sprite.x2 = (Sin2(sprite.data[2]) * sprite.data[4]) >> 12;  
      sprite.y2 = (Cos2(sprite.data[2]) * sprite.data[4]) >> 12;  

      sprite.data[2] = (sprite.data[2] + sprite.data[3]) % 360;

      if (sprite.data[4] != sprite.data[5])
          sprite.data[4] += sprite.data[6];
      else
          sprite.callback = SpriteCallbackDummy;
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesMeet */
export const Task_FrontierCirclesMeet: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesMeet_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesCross */
export const Task_FrontierCirclesCross: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesCross_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesAsymmetricSpiral */
export const Task_FrontierCirclesAsymmetricSpiral: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesAsymmetricSpiral_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesSymmetricSpiral */
export const Task_FrontierCirclesSymmetricSpiral: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesSymmetricSpiral_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesMeetInSeq */
export const Task_FrontierCirclesMeetInSeq: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesMeetInSeq_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesCrossInSeq */
export const Task_FrontierCirclesCrossInSeq: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesCrossInSeq_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesAsymmetricSpiralInSeq */
export const Task_FrontierCirclesAsymmetricSpiralInSeq: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesAsymmetricSpiralInSeq_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition_frontier.c → Task_FrontierCirclesSymmetricSpiralInSeq */
export const Task_FrontierCirclesSymmetricSpiralInSeq: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierCirclesSymmetricSpiralInSeq_Funcs[task.data[0]](_gt(rt, taskId)));
};
