// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_gfx_sfx_util.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const SPECIES_NONE = 0;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_gfx_sfx_util.c → SpriteCB_WaitForBattlerBallReleaseAnim */
export const SpriteCB_WaitForBattlerBallReleaseAnim: SpriteCallback = (sprite, rt) => {
  let spriteId = sprite.data[1];

      if (!_gs(rt, spriteId).affineAnimEnded)
          return;
      if (_gs(rt, spriteId).invisible)
          return;

      if (_gs(rt, spriteId).animPaused)
      {
          _gs(rt, spriteId).animPaused = 0;
      }
      else
      {
          if (_gs(rt, spriteId).animEnded)
              sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: battle_gfx_sfx_util.c → SpriteCB_TrainerSlideIn */
export const SpriteCB_TrainerSlideIn: SpriteCallback = (sprite, rt) => {
  if (!(gIntroSlideFlags & 1))
      {
          sprite.x2 += sprite.data[0];
          if (sprite.x2 == 0)
          {
              if (sprite.y2 != 0)
                  sprite.callback = SpriteCB_TrainerSlideVertical;
              else
                  sprite.callback = SpriteCallbackDummy;
          }
      }
};

/** Source: battle_gfx_sfx_util.c → SpriteCB_TrainerSlideVertical */
export const SpriteCB_TrainerSlideVertical: SpriteCallback = (sprite, rt) => {
  sprite.y2 -= 2;
      if (sprite.y2 == 0)
          sprite.callback = SpriteCallbackDummy;
};

/** Source: battle_gfx_sfx_util.c → SpriteCB_EnemyShadow */
export const SpriteCB_EnemyShadow: SpriteCallback = (sprite, rt) => {
  let invisible = false;
      let battler = shadowSprite.data[0];
      let battlerSprite: any = _gs(rt, gBattlerSpriteIds[battler]);

      if (!battlerSprite.inUse || !IsBattlerSpritePresent(battler))
      {
          shadowSprite.callback = SpriteCB_SetInvisible;
          return;
      }
      if (gAnimScriptActive || battlerSprite.invisible)
          invisible = true;
      else if (gBattleSpritesDataPtr.battlerData[battler].transformSpecies != SPECIES_NONE
               && gEnemyMonElevation[gBattleSpritesDataPtr.battlerData[battler].transformSpecies] == 0)
          invisible = true;

      if (gBattleSpritesDataPtr.battlerData[battler].behindSubstitute)
          invisible = true;

      shadowSprite.x = battlerSprite.x;
      shadowSprite.x2 = battlerSprite.x2;
      shadowSprite.invisible = invisible;
};

/** Source: battle_gfx_sfx_util.c → SpriteCB_SetInvisible */
export const SpriteCB_SetInvisible: SpriteCallback = (sprite, rt) => {
  sprite.invisible = true;
};

/** Source: battle_gfx_sfx_util.c → Task_ClearBitWhenBattleTableAnimDone */
export const Task_ClearBitWhenBattleTableAnimDone: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  gAnimScriptCallback();
      if (!gAnimScriptActive)
      {
          gBattleSpritesDataPtr.healthBoxesData[task.data[0]].animFromTableActive = 0;
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_gfx_sfx_util.c → Task_ClearBitWhenSpecialAnimDone */
export const Task_ClearBitWhenSpecialAnimDone: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  gAnimScriptCallback();
      if (!gAnimScriptActive)
      {
          gBattleSpritesDataPtr.healthBoxesData[task.data[0]].specialAnimActive = 0;
          rt.DestroyTask(taskId);
      }
};
