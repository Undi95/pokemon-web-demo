// AUTO-GENERATED by scripts/transpile-callbacks.mjs (Step 7 — movementtype)
// Source : MovementType_X_Step* state machines (= event_object_movement.c)
// Generated : 2026-05-08
// DO NOT EDIT — re-run transpiler to refresh.
//
// 24 functions extracted by extract-decomp-functions.mjs.
// 24 ok, 0 failed.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime } from '../../decomp-runtime';

// ─── Function exports ──────────────────────────────────────────────────────

/** u8 MovementType_WalkSequenceUpRightLeftDown_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceUpRightLeftDown_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gUpRightLeftDownDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gUpRightLeftDownDirections, ((gUpRightLeftDownDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceRightLeftDownUp_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceRightLeftDownUp_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gRightLeftDownUpDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gRightLeftDownUpDirections, ((gRightLeftDownUpDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceDownUpRightLeft_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceDownUpRightLeft_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gDownUpRightLeftDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gDownUpRightLeftDirections, ((gDownUpRightLeftDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceLeftDownUpRight_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceLeftDownUpRight_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gLeftDownUpRightDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gLeftDownUpRightDirections, ((gLeftDownUpRightDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceUpLeftRightDown_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceUpLeftRightDown_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gUpLeftRightDownDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gUpLeftRightDownDirections, ((gUpLeftRightDownDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceLeftRightDownUp_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceLeftRightDownUp_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gLeftRightDownUpDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gLeftRightDownUpDirections, ((gLeftRightDownUpDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceDownUpLeftRight_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceDownUpLeftRight_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gStandardDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gStandardDirections, ((gStandardDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceRightDownUpLeft_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceRightDownUpLeft_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gRightDownUpLeftDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gRightDownUpLeftDirections, ((gRightDownUpLeftDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceLeftUpDownRight_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceLeftUpDownRight_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gLeftUpDownRightDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gLeftUpDownRightDirections, ((gLeftUpDownRightDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceUpDownRightLeft_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceUpDownRightLeft_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gUpDownRightLeftDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gUpDownRightLeftDirections, ((gUpDownRightLeftDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceRightLeftUpDown_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceRightLeftUpDown_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gRightLeftUpDownDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gRightLeftUpDownDirections, ((gRightLeftUpDownDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceDownRightLeftUp_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceDownRightLeftUp_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gDownRightLeftUpDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gDownRightLeftUpDirections, ((gDownRightLeftUpDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceRightUpDownLeft_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceRightUpDownLeft_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gRightUpDownLeftDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gRightUpDownLeftDirections, ((gRightUpDownLeftDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceUpDownLeftRight_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceUpDownLeftRight_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gUpDownLeftRightDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gUpDownLeftRightDirections, ((gUpDownLeftRightDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceLeftRightUpDown_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceLeftRightUpDown_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gLeftRightUpDownDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gLeftRightUpDownDirections, ((gLeftRightUpDownDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 1 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 2;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceDownLeftRightUp_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceDownLeftRightUp_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gDownLeftRightUpDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gDownLeftRightUpDirections, ((gDownLeftRightUpDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceUpLeftDownRight_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceUpLeftDownRight_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gUpLeftDownRightDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gUpLeftDownRightDirections, ((gUpLeftDownRightDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceDownRightUpLeft_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceDownRightUpLeft_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gDownRightUpLeftDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gDownRightUpLeftDirections, ((gDownRightUpLeftDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceLeftDownRightUp_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceLeftDownRightUp_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gLeftDownRightUpDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gLeftDownRightUpDirections, ((gLeftDownRightUpDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceRightUpLeftDown_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceRightUpLeftDown_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gRightUpLeftDownDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gRightUpLeftDownDirections, ((gRightUpLeftDownDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceUpRightDownLeft_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceUpRightDownLeft_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gUpRightDownLeftDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gUpRightDownLeftDirections, ((gUpRightDownLeftDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceDownLeftUpRight_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceDownLeftUpRight_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gDownLeftUpRightDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gDownLeftUpRightDirections, ((gDownLeftUpRightDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.y == objectEvent.currentCoords.y)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceLeftUpRightDown_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceLeftUpRightDown_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gLeftUpRightDownDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gLeftUpRightDownDirections, ((gLeftUpRightDownDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}

/** u8 MovementType_WalkSequenceRightDownLeftUp_Step1(struct ObjectEvent *objectEvent, struct Sprite *sprite)
 *  Source : src/event_object_movement.c */
export function MovementType_WalkSequenceRightDownLeftUp_Step1(npc: any, sprite: any, rt: DecompRuntime): any {
  let directions: number[] = new Array(((gRightDownLeftUpDirections)?.length ?? 32)).fill(0);
      memcpy(directions, gRightDownLeftUpDirections, ((gRightDownLeftUpDirections)?.length ?? 32));
      if (objectEvent.directionSequenceIndex == 2 && objectEvent.initialCoords.x == objectEvent.currentCoords.x)
          objectEvent.directionSequenceIndex = 3;

      return MoveNextDirectionInSequence(objectEvent, sprite, directions);
}
