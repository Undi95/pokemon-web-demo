// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/decoration.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  JOY_HELD,
  JOY_NEW,
  StringExpandPlaceholders,
  gStringVar4,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const DECOR_ITEMS_MENU_PLACE = 0;
const DECOR_ITEMS_MENU_PUT_AWAY = 1;
const DIR_EAST = 4;
const DIR_NORTH = 2;
const DIR_SOUTH = 1;
const DIR_WEST = 3;
const DPAD_ANY = 240;
const DPAD_DOWN = 128;
const DPAD_LEFT = 32;
const DPAD_RIGHT = 16;
const DPAD_UP = 64;
const WINDOW_DECORATION_CATEGORIES = 1;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const MAPSEC_SECRET_BASE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: decoration.c → Task_ShowDecorationItemsWindow */
export const Task_ShowDecorationItemsWindow: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  AddDecorationWindow(WINDOW_DECORATION_CATEGORIES);
      ShowDecorationItemsWindow(taskId);
};

/** Source: decoration.c → Task_PlaceDecoration */
export const Task_PlaceDecoration: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[2])
      {
          case 0:
              if (!rt.gPaletteFade.active)
              {
                  SetInitialPositions(taskId);
                  task.data[2] = 1;
              }
              break;
          case 1:
              rt.gPaletteFade.bufferTransferDisabled = true;
              ConfigureCameraObjectForPlacingDecoration(sPlaceDecorationGraphicsDataBuffer, gCurDecorationItems[gCurDecorationIndex]);
              SetUpDecorationShape(taskId);
              SetUpPlacingDecorationPlayerAvatar(taskId, sPlaceDecorationGraphicsDataBuffer);
              FadeInFromBlack();
              rt.gPaletteFade.bufferTransferDisabled = false;
              task.data[2] = 2;
              break;
          case 2:
              if (IsWeatherNotFadingIn() == true)
              {
                  task.data[12] = DECOR_ITEMS_MENU_PLACE;
                  ContinueDecorating(taskId);
              }
              break;
      }
};

/** Source: decoration.c → Task_InitDecorationItemsWindow */
export const Task_InitDecorationItemsWindow: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      switch (data[2])
      {
      case 0:
          HideSecretBaseDecorationSprites();
          data[2]++;
          break;
      case 1:
          ScriptContext_SetupScript(SecretBase_EventScript_InitDecorations);
          data[2]++;
          break;
      case 2:
          LockPlayerFieldControls();
          data[2]++;
          break;
      case 3:
          if (IsWeatherNotFadingIn() == true)
              task.func = (t) => HandleDecorationItemsMenuInput(t, rt);
          break;
      }
};

/** Source: decoration.c → Task_SelectLocation */
export const Task_SelectLocation: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      if (!_gs(rt, sDecor_CameraSpriteObjectIdx1).data[4])
      {
          if (data[10] == A_BUTTON)
          {
              sPlacePutAwayYesNoFunctions[data[12]].yesFunc(taskId);
              return;
          }

          if (data[10] == B_BUTTON)
          {
              sPlacePutAwayYesNoFunctions[data[12]].noFunc(taskId);
              return;
          }

          if ((JOY_HELD(DPAD_ANY)) == DPAD_UP)
          {
              sDecorationLastDirectionMoved = DIR_SOUTH;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[2] =  0;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[3] = -2;
              data[1]--;
          }

          if ((JOY_HELD(DPAD_ANY)) == DPAD_DOWN)
          {
              sDecorationLastDirectionMoved = DIR_NORTH;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[2] =  0;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[3] =  2;
              data[1]++;
          }

          if ((JOY_HELD(DPAD_ANY)) == DPAD_LEFT)
          {
              sDecorationLastDirectionMoved = DIR_WEST;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[2] = -2;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[3] =  0;
              data[0]--;
          }

          if ((JOY_HELD(DPAD_ANY)) == DPAD_RIGHT)
          {
              sDecorationLastDirectionMoved = DIR_EAST;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[2] =  2;
              _gs(rt, sDecor_CameraSpriteObjectIdx1).data[3] =  0;
              data[0]++;
          }

          if (!IsHoldingDirection() || !ApplyCursorMovement_IsInvalid(taskId))
              ResetCursorMovement();
      }

      if (sDecorationLastDirectionMoved)
      {
          _gs(rt, sDecor_CameraSpriteObjectIdx1).data[4]++;
          _gs(rt, sDecor_CameraSpriteObjectIdx1).data[4] &= 7;
      }

      if (!data[10])
      {
          if (JOY_NEW(A_BUTTON))
              data[10] = A_BUTTON;

          if (JOY_NEW(B_BUTTON))
              data[10] = B_BUTTON;
      }
};

/** Source: decoration.c → Task_PutAwayDecoration */
export const Task_PutAwayDecoration: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[2])
      {
      case 0:
          ClearRearrangementNonSprites();
          task.data[2] = 1;
          break;
      case 1:
          if (!rt.gPaletteFade.active)
          {
              DrawWholeMapView();
              ScriptContext_SetupScript(SecretBase_EventScript_PutAwayDecoration);
              ClearDialogWindowAndFrame(0, true);
              task.data[2] = 2;
          }
          break;
      case 2:
          LockPlayerFieldControls();
          IdentifyOwnedDecorationsCurrentlyInUseInternal(taskId);
          FadeInFromBlack();
          task.data[2] = 3;
          break;
      case 3:
          if (IsWeatherNotFadingIn() == true)
          {
              StringExpandPlaceholders(gStringVar4, gText_DecorationReturnedToPC);
              DisplayItemMessageOnField(taskId, gStringVar4, ContinuePuttingAwayDecorationsPrompt);
              if (gMapHeader.regionMapSectionId == MAPSEC_SECRET_BASE)
                  TryPutSecretBaseVisitOnAir();
          }
          break;
      }
};

/** Source: decoration.c → Task_ContinuePuttingAwayDecorations */
export const Task_ContinuePuttingAwayDecorations: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let data = 0;

      data = task.data;
      switch (data[2])
      {
      case 0:
          if (!rt.gPaletteFade.active)
          {
              SetInitialPositions(taskId);
              data[2] = 1;
              data[6] = 1;
              data[5] = 1;
          }
          break;
      case 1:
          SetUpPuttingAwayDecorationPlayerAvatar();
          FadeInFromBlack();
          data[2] = 2;
          break;
      case 2:
          if (IsWeatherNotFadingIn() == true)
          {
              data[12] = DECOR_ITEMS_MENU_PUT_AWAY;
              ContinuePuttingAwayDecorations(taskId);
          }
          break;
      }
};

/** Source: decoration.c → Task_StopPuttingAwayDecorations */
export const Task_StopPuttingAwayDecorations: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[2])
      {
      case 0:
          if (!rt.gPaletteFade.active)
          {
              WarpToInitialPosition(taskId);
              task.data[2] = 1;
          }
          break;
      case 1:
          FreePlayerSpritePalette();
          gFieldCallback = FieldCB_StopPuttingAwayDecorations;
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToField) */;
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: decoration.c → Task_ReinitializeDecorationMenuHandler */
export const Task_ReinitializeDecorationMenuHandler: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      switch (data[2])
      {
      case 0:
          HideSecretBaseDecorationSprites();
          data[2]++;
          break;
      case 1:
          ScriptContext_SetupScript(SecretBase_EventScript_InitDecorations);
          data[2]++;
          break;
      case 2:
          LockPlayerFieldControls();
          data[2]++;
          break;
      case 3:
          if (IsWeatherNotFadingIn() == true)
              task.func = (t) => HandleDecorationActionsMenuInput(t, rt);
          break;
      }
};
