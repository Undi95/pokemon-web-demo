// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/mauville_old_man.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  RunTextPrintersAndIsPrinter0Active,
  gSaveBlock1Ptr,
  gStringVar4,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const BARD_STATE_GET_WORD = 2;
const BARD_STATE_HANDLE_WORD = 3;
const BARD_STATE_INIT = 0;
const BARD_STATE_PAUSE = 5;
const BARD_STATE_WAIT_BGM = 1;
const BARD_STATE_WAIT_WORD = 4;
const CHAR_BARD_WORD_DELIMIT = 55;
const CHAR_NEWLINE = 254;
const CHAR_SPACE = 0;
const EOS = 255;
const EXT_CTRL_CODE_BEGIN = 252;
const MENU_B_PRESSED = -1;
const MENU_NOTHING_CHOSEN = -2;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const WORD_TO_PITCH_TABLE_INDEX: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: mauville_old_man.c → Task_BardSong */
export const Task_BardSong: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      BardSing(task, gBardSong);

      switch (task.data[0])
      {
      case BARD_STATE_INIT:
          PrepareSongText();
          DrawSongTextWindow(gStringVar4);
          task.data[1] = 0;
          task.data[2] = 0;
          task.data[3] = 0;
          task.data[4] = 0;
          FadeOutBGMTemporarily(4);
          task.data[0] = BARD_STATE_WAIT_BGM;
          break;
      case BARD_STATE_WAIT_BGM:
          if (IsBGMPausedOrStopped())
              task.data[0] = BARD_STATE_GET_WORD;
          break;
      case BARD_STATE_GET_WORD:
      {
          let bard: any = gSaveBlock1Ptr.oldMan.bard;
          let str = gStringVar4[task.data[3]];
          let wordLen = 0;

           
          while (str != CHAR_SPACE
              && str != CHAR_NEWLINE
              && str != EXT_CTRL_CODE_BEGIN
              && str != EOS)
          {
              str++;
              wordLen++;
          }

           
          if (!task.data[5])
              sUnusedPitchTableIndex = WORD_TO_PITCH_TABLE_INDEX(bard.songLyrics[task.data[4]]);
          else
              sUnusedPitchTableIndex = WORD_TO_PITCH_TABLE_INDEX(bard.newSongLyrics[task.data[4]]);

          gBardSong.length /= wordLen;
          if (gBardSong.length <= 0)
              gBardSong.length = 1;

          task.data[4]++;

          if (task.data[2] == 0)
          {
              task.data[0] = BARD_STATE_HANDLE_WORD;
              task.data[1] = 0;
          }
          else
          {
              task.data[0] = BARD_STATE_PAUSE;
              task.data[1] = 0;
          }
      }
          break;
      case BARD_STATE_PAUSE:
           
          if (task.data[2] == 0)
              task.data[0] = BARD_STATE_HANDLE_WORD;
          else
              task.data[2]--;
          break;
      case BARD_STATE_HANDLE_WORD:
          if (gStringVar4[task.data[3]] == EOS)
          {
               
              FadeInBGM(6);
              m4aMPlayFadeOutTemporarily(gMPlayInfo_SE2, 2);
              ScriptContext_Enable();
              rt.DestroyTask(taskId);
          }
          else if (gStringVar4[task.data[3]] == CHAR_SPACE)
          {
               
              EnableTextPrinters();
              task.data[3]++;
              task.data[0] = BARD_STATE_GET_WORD;
              task.data[2] = 0;
          }
          else if (gStringVar4[task.data[3]] == CHAR_NEWLINE)
          {
               
              task.data[3]++;
              task.data[0] = BARD_STATE_GET_WORD;
              task.data[2] = 0;
          }
          else if (gStringVar4[task.data[3]] == EXT_CTRL_CODE_BEGIN)
          {
               
               
               
              task.data[3] += 2;   
              task.data[0] = BARD_STATE_GET_WORD;
              task.data[2] = 8;
          }
          else if (gStringVar4[task.data[3]] == CHAR_BARD_WORD_DELIMIT)
          {
               
              gStringVar4[task.data[3]] = CHAR_SPACE;
              EnableTextPrinters();
              task.data[3]++;
              task.data[2] = 0;
          }
          else
          {
               
              switch (task.data[1])
              {
              case 0:
                  EnableTextPrinters();
                  task.data[1]++;
                  break;
              case 1:
                  task.data[1]++;
                  break;
              case 2:
                  task.data[3]++;
                  task.data[1] = 0;
                  task.data[2] = gBardSong.length;
                  task.data[0] = BARD_STATE_WAIT_WORD;
                  break;
              }
          }
          break;
      case BARD_STATE_WAIT_WORD:
           
           
          task.data[2]--;
          if (task.data[2] == 0)
              task.data[0] = BARD_STATE_HANDLE_WORD;
          break;
      }
      RunTextPrintersAndIsPrinter0Active();
};

/** Source: mauville_old_man.c → Task_StoryListMenu */
export const Task_StoryListMenu: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      let selection = 0;

      switch (task.data[0])
      {
      case 0:
          PrintStoryList();
          task.data[0]++;
          break;
      case 1:
          selection = Menu_ProcessInput();
          if (selection == MENU_NOTHING_CHOSEN)
              break;
          if (selection == MENU_B_PRESSED || selection == GetFreeStorySlot())
          {
              gSpecialVar_Result = 0;
          }
          else
          {
              gSpecialVar_Result = 1;
              sSelectedStory = selection;
          }
          ClearToTransparentAndRemoveWindow(sStorytellerWindowId);
          rt.DestroyTask(taskId);
          ScriptContext_Enable();
          break;
      }
};
