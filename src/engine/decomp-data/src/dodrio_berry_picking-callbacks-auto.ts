// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/dodrio_berry_picking.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
import {
  AddWindow,
  CopyWindowToVram,
  JOY_NEW,
  RemoveWindow,
  UpdatePaletteFade,
} from '../../system/decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const COPYWIN_FULL = 3;
const COPYWIN_MAP = 1;
const FONT_NORMAL = 1;
const GFXFUNC_ERASE_MSG = 6;
const NUM_CLOUDS = 2;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: dodrio_berry_picking.c → SpriteCB_Dodrio */
export const SpriteCB_Dodrio: SpriteCallback = (sprite, rt) => {
  switch (sprite.data[0])
      {
      case 0:
          break;
      case 1:
          DoDodrioMissedAnim(sprite);
          break;
      case 2:
          DoDodrioIntroAnim(sprite);
          break;
      }
};

/** Source: dodrio_berry_picking.c (failed: empty bodyC) */
export const SpriteCB_Status: SpriteCallback = (_sprite, _rt) => { /* TODO empty bodyC */ };

/** Source: dodrio_berry_picking.c → SpriteCB_Cloud */
export const SpriteCB_Cloud: SpriteCallback = (sprite, rt) => {
  let i = 0;
      const moveDelays: any = [30, 20];

      if (sprite.data[10] != true)
      {
          for (i = 0; i < NUM_CLOUDS; i++)
          {
              if (++sCloudSpriteIds[i][1] > moveDelays[i])
              {
                  sprite.x--;
                  sCloudSpriteIds[i][1] = 0;
              }
          }
      }
};

/** Source: dodrio_berry_picking.c → Task_StartDodrioGame */
export const Task_StartDodrioGame: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i, numPlayers;

      switch (sGame.startState)
      {
      case 0:
          rt.SetVBlankCallback(VBlankCB);
          CreateTask_(Task_CommunicateMonInfo, 4);
          sGame.startState++;
          break;
      case 1:
          if (!FuncIsActiveTask(Task_CommunicateMonInfo))
          {
              InitGameGfx(sGame.gfx);
              sGame.startState++;
          }
          break;
      case 2:
          if (!IsGfxFuncActive())
          {
              Rfu_SetLinkStandbyCallback();
              sGame.startState++;
          }
          break;
      case 3:
          if (IsLinkTaskFinished())
          {
              if (gReceivedRemoteLinkPlayers)
              {
                  LoadWirelessStatusIndicatorSpriteGfx();
                  CreateWirelessStatusIndicatorSprite(0, 0);
              }
              sGame.startState++;
          }
          break;
      case 4:
          numPlayers = sGame.numPlayers;
          LoadDodrioGfx();
          for (i = 0; i < numPlayers; i++)
              CreateDodrioSprite(sGame.monInfo[sGame.posToPlayerId[i]], i, sGame.posToPlayerId[i], sGame.numPlayers);

          SetAllDodrioInvisibility(false, sGame.numPlayers);
          sGame.startState++;
          break;
      case 5:
          LoadBerryGfx();
          CreateBerrySprites();
          CreateCloudSprites();
          CreateStatusBarSprites();
          sGame.startState++;
          break;
      case 6:
          BlendPalettes(PALETTES_ALL, 0x10, 0x00);
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 16, 0, "0");
          rt.SetVBlankCallback(VBlankCB);
          sGame.startState++;
          break;
      case 7:
          UpdatePaletteFade();
          if (!rt.gPaletteFade.active)
              sGame.startState++;
          break;
      default:
          rt.DestroyTask(taskId);
          CreateDodrioGameTask(Task_NewGameIntro);
          break;
      }
};

/** Source: dodrio_berry_picking.c → Task_DodrioGame_Leader */
export const Task_DodrioGame_Leader: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  RecvLinkData_Leader();
      sLeaderFuncs[sGame.funcId]();
      if (!sExitingGame)
          UpdateGame_Leader();

      SendLinkData_Leader();
};

/** Source: dodrio_berry_picking.c → Task_DodrioGame_Member */
export const Task_DodrioGame_Member: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  RecvLinkData_Member();
      sMemberFuncs[sGame.funcId]();
      if (!sExitingGame)
          UpdateGame_Member();

      SendLinkData_Member();
};

/** Source: dodrio_berry_picking.c → Task_NewGameIntro */
export const Task_NewGameIntro: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (sGame.state)
      {
      case 0:
          if (SlideTreeBordersOut() == true)
              sGame.state++;
          break;
      case 1:
          InitStatusBarPos();
          sGame.state++;
          break;
      case 2:
          if (DoStatusBarIntro() == true)
              sGame.state++;
          break;
      default:
          if (sGame.isLeader)
              CreateDodrioGameTask(Task_DodrioGame_Leader);
          else
              CreateDodrioGameTask(Task_DodrioGame_Member);

          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: dodrio_berry_picking.c → Task_CommunicateMonInfo */
export const Task_CommunicateMonInfo: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      let i = 0;

      switch (data[0])
      {
      case 0:
          if (SendBlock(0, sGame.monInfo[sGame.multiplayerId].isShiny, 0))
          {
              sGame.playersReceived = 0;
              data[0]++;
          }
          break;
      case 1:
          if (IsLinkTaskFinished())
              data[0]++;
          break;
      case 2:
          if (AllLinkBlocksReceived())
          {
              for (i = 0; i < sGame.numPlayers; i++)
              {
                  sGame.monInfo[i] = gBlockRecvBuffer[i];
                  sGame.playersReceived = sGame.numPlayers;
              }
          }
          if (sGame.playersReceived >= sGame.numPlayers)
          {
              rt.DestroyTask(taskId);
              SetGfxFuncById(GFXFUNC_ERASE_MSG);
              sGame.state++;
          }
          break;
      }
};

/** Source: dodrio_berry_picking.c → Task_ShowDodrioBerryPickingRecords */
export const Task_ShowDodrioBerryPickingRecords: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let window: any = null;
      let i, width, widthCurr;
      const data = task.data;

      switch (data[0])
      {
      case 0:
          window = sWindowTemplates_Records;
          width = GetStringWidth(FONT_NORMAL, gText_BerryPickingRecords, 0);
          for (i = 0; i < ((sRecordsTexts)?.length ?? 0); i++)
          {
              widthCurr = GetStringWidth(FONT_NORMAL, sRecordsTexts[i], 0) + 50;
              if (widthCurr > width)
                  width = widthCurr;
          }width = Math.trunc((width + 7) / 8);
          if (width & 1)
              width++;window.tilemapLeft = Math.trunc((30 - width) / 2);
          window.width = width;
          data[1] = AddWindow(window);
          PrintRecordsText(data[1], width);
          CopyWindowToVram(data[1], COPYWIN_FULL);
          data[0]++;
          break;
      case 1:
          if (!IsDma3ManagerBusyWithBgCopy())
              data[0]++;
          break;
      case 2:
          if (JOY_NEW(A_BUTTON | B_BUTTON))
          {
              rbox_fill_rectangle(data[1]);
              CopyWindowToVram(data[1], COPYWIN_MAP);
              data[0]++;
          }
          break;
      case 3:
          if (!IsDma3ManagerBusyWithBgCopy())
          {
              RemoveWindow(data[1]);
              rt.DestroyTask(taskId);
              ScriptContext_Enable();
          }
          break;
      }
};

/** Source: dodrio_berry_picking.c → Task_TryRunGfxFunc */
export const Task_TryRunGfxFunc: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!sGfx.finished)
          GetGfxFunc()();
};

/** Source: dodrio_berry_picking.c → CB2_DodrioGame */
export const CB2_DodrioGame: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
