// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/use_pokeblock.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  RunTextPrinters,
  UpdatePaletteFade,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const CONDITION_ICON_SELECTED = 0;
const CONDITION_ICON_UNSELECTED = 1;
const PARTY_SIZE = 6;
const TAG_CONDITION_BALL = 101;
const TAG_CONDITION_CANCEL = 102;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: use_pokeblock.c → SpriteCB_UpDown */
export const SpriteCB_UpDown: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0] < 6)
          sprite.y2 -= 2;
      else if (sprite.data[0] < 12)
          sprite.y2 += 2;

      if (++sprite.data[0] > 60)
      {
          rt.DestroySprite(sprite.spriteId);
          sInfo.numEnhancements--;
      }
};

/** Source: use_pokeblock.c → SpriteCB_MonPic */
export const SpriteCB_MonPic: SpriteCallback = (sprite, rt) => {
  sprite.x = sMenu.curMonXOffset + 38;
};

/** Source: use_pokeblock.c → SpriteCB_SelectionIconPokeball */
export const SpriteCB_SelectionIconPokeball: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0] == sMenu.info.curSelection)
          rt.StartSpriteAnim(sprite.spriteId, CONDITION_ICON_SELECTED);
      else
          rt.StartSpriteAnim(sprite.spriteId, CONDITION_ICON_UNSELECTED);
};

/** Source: use_pokeblock.c → SpriteCB_SelectionIconCancel */
export const SpriteCB_SelectionIconCancel: SpriteCallback = (sprite, rt) => {
  if (sMenu.info.curSelection == sMenu.info.numSelections - 1)
          rt.gba.oam[sprite.oamIndex].paletteBank = IndexOfSpritePaletteTag(TAG_CONDITION_BALL);
      else
          rt.gba.oam[sprite.oamIndex].paletteBank = IndexOfSpritePaletteTag(TAG_CONDITION_CANCEL);
};

/** Source: use_pokeblock.c → SpriteCB_Condition */
export const SpriteCB_Condition: SpriteCallback = (sprite, rt) => {
  let prevX = sprite.x;

       
      sprite.x += sprite.data[0];

       
      if ((prevX <= sprite.data[1] && sprite.x >= sprite.data[1])
       || (prevX >= sprite.data[1] && sprite.x <= sprite.data[1]))
      {
           
          sprite.x = sprite.data[1];
          sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: use_pokeblock.c → CB2_ReturnAndChooseMonToGivePokeblock */
export const CB2_ReturnAndChooseMonToGivePokeblock: CB2Callback = (rt) => {
  sMenu = AllocZeroed(((sMenu)?.length ?? 32));
      sInfo = sMenu.info;
      sInfo.pokeblock = sPokeblock;
      sInfo.exitCallback = sExitCallback;
      gPokeblockMonId = GetSelectionIdFromPartyId(gPokeblockMonId);
      sInfo.monInTopHalf = (gPokeblockMonId <= PARTY_SIZE / 2) ? false : true;
      SetUsePokeblockCallback(LoadUsePokeblockMenu);
      rt.SetMainCallback2(CB2_ReturnToUsePokeblockMenu);
};

/** Source: use_pokeblock.c → CB2_ReturnToUsePokeblockMenu */
export const CB2_ReturnToUsePokeblockMenu: CB2Callback = (rt) => {
  sInfo.callback();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
      if (sInfo.callback == ShowUsePokeblockMenu)
      {
          sInfo.mainState = 0;
          rt.SetMainCallback2(CB2_ShowUsePokeblockMenuForResults);
      }
};

/** Source: use_pokeblock.c → CB2_ShowUsePokeblockMenuForResults */
export const CB2_ShowUsePokeblockMenuForResults: CB2Callback = (rt) => {
  ShowUsePokeblockMenuForResults();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** Source: use_pokeblock.c → CB2_UsePokeblockMenu */
export const CB2_UsePokeblockMenu: CB2Callback = (rt) => {
  sInfo.callback();
      AnimateSprites();
      BuildOamBuffer();
      RunTextPrinters();
      UpdatePaletteFade();
};
