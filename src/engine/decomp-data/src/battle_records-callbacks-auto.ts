// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_records.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
import {
  CopyBgTilemapBufferToVram,
  DeactivateAllTextPrinters,
  FreeAllWindowBuffers,
  InitBgsFromTemplates,
  InitWindows,
  JOY_NEW,
  LoadPalette,
  ResetBgsAndClearDma3BusyFlags,
  ShowBg,
  gMain,
  m4aSongNumStart,
} from '../../system/decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const SE_SELECT = 5;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_records.c → Task_TrainerHillWaitForPaletteFade */
export const Task_TrainerHillWaitForPaletteFade: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
          task.func = (t) => Task_CloseTrainerHillRecordsOnButton(t, rt);
};

/** Source: battle_records.c → Task_CloseTrainerHillRecordsOnButton */
export const Task_CloseTrainerHillRecordsOnButton: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      if (JOY_NEW(A_BUTTON) || JOY_NEW(B_BUTTON))
      {
          PlaySE(SE_SELECT);
          task.func = Task_BeginPaletteFade;
      }
};

/** Source: battle_records.c → Task_BeginPaletteFade */
export const Task_BeginPaletteFade: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0, 0x10, "RGB_BLACK");
      task.func = (t) => Task_ExitTrainerHillRecords(t, rt);
};

/** Source: battle_records.c → Task_ExitTrainerHillRecords */
export const Task_ExitTrainerHillRecords: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
          Free(sTilemapBuffer);
          RemoveTrainerHillRecordsWindow(0);
          FreeAllWindowBuffers();
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_records.c → CB2_ShowTrainerHillRecords */
export const CB2_ShowTrainerHillRecords: CB2Callback = (rt) => {
  switch (gMain.state)
      {
      case 0:
          rt.SetVBlankCallback(VBlankCB);
          ClearVramOamPlttRegs();
          gMain.state++;
          break;
      case 1:
          ClearTasksAndGraphicalStructs();
          gMain.state++;
          break;
      case 2:
          sTilemapBuffer = AllocZeroed(BG_SCREEN_SIZE);
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(0, sTrainerHillRecordsBgTemplates, ((sTrainerHillRecordsBgTemplates)?.length ?? 0));
          SetBgTilemapBuffer(3, sTilemapBuffer);
          ResetBgCoordinates();
          gMain.state++;
          break;
      case 3:
          LoadTrainerHillRecordsWindowGfx(3);
          LoadPalette(GetTextWindowPalette(0), BG_PLTT_ID(15), PLTT_SIZE_4BPP);
          gMain.state++;
          break;
      case 4:
          if (IsDma3ManagerBusyWithBgCopy() != true)
          {
              ShowBg(0);
              ShowBg(3);
              CopyBgTilemapBufferToVram(3);
              gMain.state++;
          }
          break;
      case 5:
          InitWindows(sTrainerHillRecordsWindowTemplates);
          DeactivateAllTextPrinters();
          gMain.state++;
          break;
      case 6:
          rt.BeginNormalPaletteFade("PALETTES_ALL", 0, 0x10, 0, "RGB_BLACK");
          gMain.state++;
          break;
      case 7:
          SetDispcntReg();
          rt.SetVBlankCallback(VBlankCB);
          PrintOnTrainerHillRecordsWindow();
          rt.CreateTask((t) => Task_TrainerHillWaitForPaletteFade(t, rt), 8);
          /* TODO scene transition: SetMainCallback2(MainCB2_TrainerHillRecords) */;
          gMain.state = 0;
          break;
      }
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
