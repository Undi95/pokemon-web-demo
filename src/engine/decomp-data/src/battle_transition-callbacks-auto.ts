// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_transition.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  UpdatePaletteFade,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const BG_VRAM = 100663296;
const FADE_TARGET = 4096;
const FLDEFF_POKEBALL_TRAIL = 45;
const MUGSHOT_CHAMPION = 4;
const MUGSHOT_DRAKE = 3;
const MUGSHOT_GLACIA = 2;
const MUGSHOT_PHOEBE = 1;
const MUGSHOT_SIDNEY = 0;
const NUM_WHITE_BARS = 8;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const REG_BG0CNT: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const SET_TILE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_transition.c → SpriteCB_FldEffPokeballTrail */
export const SpriteCB_FldEffPokeballTrail: SpriteCallback = (sprite, rt) => {
  let speeds: number[] = new Array(((sPokeballsTrail_Speeds)?.length ?? 0)).fill(0);
      memcpy(speeds, sPokeballsTrail_Speeds, ((sPokeballsTrail_Speeds)?.length ?? 32));

      if (sprite.data[5] != 0)
      {
          sprite.data[5]--;
      }
      else
      {
          if (sprite.x >= 0 && sprite.x <= DISPLAY_WIDTH)
          {
               
              let posX = sprite.x >> 3;
              let posY = sprite.y >> 3;

               
              if (posX != sprite.data[2])
              {
                  let _var = 0;
                  let ptr = 0;

                  sprite.data[2] = posX;
                  _var = ((REG_BG0CNT >> 8) & 0x1) << 11;
                  ptr = (BG_VRAM + _var);

                  SET_TILE(ptr, posY - 2, posX, 1);
                  SET_TILE(ptr, posY - 1, posX, 1);
                  SET_TILE(ptr, posY - 0, posX, 1);
                  SET_TILE(ptr, posY + 1, posX, 1);
              }
          }
          sprite.x += speeds[sprite.data[0]];
          if (sprite.x < -15 || sprite.x > DISPLAY_WIDTH + 15)
              FieldEffectStop(sprite, FLDEFF_POKEBALL_TRAIL);
      }
};

/** Source: battle_transition.c → SpriteCB_MugshotTrainerPic */
export const SpriteCB_MugshotTrainerPic: SpriteCallback = (sprite, rt) => {
  while (sMugshotTrainerPicFuncs[sprite.data[0]](sprite));
};

/** Source: battle_transition.c → SpriteCB_WhiteBarFade */
export const SpriteCB_WhiteBarFade: SpriteCallback = (sprite, rt) => {
  if (sprite.data[5])
      {
          sprite.data[5]--;
          if (sprite.data[6])
              sTransitionData.VBlank_DMA = 1;
      }
      else
      {
          let i = 0;
          let ptr1 = gScanlineEffectRegBuffers[0][sprite.y];
          let ptr2 = gScanlineEffectRegBuffers[0][sprite.y + DISPLAY_HEIGHT];
          for (i = 0; i < DISPLAY_HEIGHT / NUM_WHITE_BARS; i++)
          {
              ptr1[i] = sprite.data[0] >> 8;
              ptr2[i] = sprite.x;
          }
          if (sprite.x == 0 && sprite.data[0] == FADE_TARGET)
              sprite.data[1] = true;

          sprite.x -= 16;
          sprite.data[0] += FADE_TARGET / 32;

          if (sprite.x < 0)
              sprite.x = 0;
          if (sprite.data[0] > FADE_TARGET)
              sprite.data[0] = FADE_TARGET;

          if (sprite.data[6])
              sTransitionData.VBlank_DMA = 1;

          if (sprite.data[1])
          {
               
               
              if (!sprite.data[6] || (sTransitionData.counter >= NUM_WHITE_BARS - 1 && sprite.data[2]++ > 7))
              {
                  sTransitionData.counter++;
                  rt.DestroySprite(sprite.spriteId);
              }
          }
      }
};

/** Source: battle_transition.c → Task_BattleTransition */
export const Task_BattleTransition: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sTaskHandlers[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Intro */
export const Task_Intro: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (task.data[0] == 0)
      {
          task.data[0]++;
          CreateIntroTask(0, 0, 3, 2, 2);
      }
      else if (IsIntroTaskDone())
      {
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_transition.c → Task_Blur */
export const Task_Blur: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sBlur_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Swirl */
export const Task_Swirl: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sSwirl_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Shuffle */
export const Task_Shuffle: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sShuffle_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_BigPokeball */
export const Task_BigPokeball: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sBigPokeball_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Aqua */
export const Task_Aqua: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sAqua_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Magma */
export const Task_Magma: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sMagma_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Regice */
export const Task_Regice: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sRegice_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Registeel */
export const Task_Registeel: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sRegisteel_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Regirock */
export const Task_Regirock: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sRegirock_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Kyogre */
export const Task_Kyogre: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sKyogre_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_PokeballsTrail */
export const Task_PokeballsTrail: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sPokeballsTrail_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_ClockwiseWipe */
export const Task_ClockwiseWipe: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sClockwiseWipe_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Ripple */
export const Task_Ripple: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sRipple_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Wave */
export const Task_Wave: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sWave_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Sidney */
export const Task_Sidney: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.data[15] = MUGSHOT_SIDNEY;
      DoMugshotTransition(taskId);
};

/** Source: battle_transition.c → Task_Phoebe */
export const Task_Phoebe: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.data[15] = MUGSHOT_PHOEBE;
      DoMugshotTransition(taskId);
};

/** Source: battle_transition.c → Task_Glacia */
export const Task_Glacia: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.data[15] = MUGSHOT_GLACIA;
      DoMugshotTransition(taskId);
};

/** Source: battle_transition.c → Task_Drake */
export const Task_Drake: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.data[15] = MUGSHOT_DRAKE;
      DoMugshotTransition(taskId);
};

/** Source: battle_transition.c → Task_Champion */
export const Task_Champion: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.data[15] = MUGSHOT_CHAMPION;
      DoMugshotTransition(taskId);
};

/** Source: battle_transition.c → Task_Slice */
export const Task_Slice: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sSlice_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_ShredSplit */
export const Task_ShredSplit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sShredSplit_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Blackhole */
export const Task_Blackhole: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sBlackhole_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_BlackholePulsate */
export const Task_BlackholePulsate: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sBlackholePulsate_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_RectangularSpiral */
export const Task_RectangularSpiral: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sRectangularSpiral_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Groudon */
export const Task_Groudon: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sGroudon_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_Rayquaza */
export const Task_Rayquaza: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sRayquaza_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_WhiteBarsFade */
export const Task_WhiteBarsFade: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sWhiteBarsFade_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_GridSquares */
export const Task_GridSquares: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sGridSquares_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_AngledWipes */
export const Task_AngledWipes: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sAngledWipes_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_BattleTransition_Intro */
export const Task_BattleTransition_Intro: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sTransitionIntroFuncs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_FrontierLogoWiggle */
export const Task_FrontierLogoWiggle: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierLogoWiggle_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_FrontierLogoWave */
export const Task_FrontierLogoWave: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierLogoWave_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_FrontierSquares */
export const Task_FrontierSquares: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierSquares_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_FrontierSquaresSpiral */
export const Task_FrontierSquaresSpiral: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierSquaresSpiral_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_FrontierSquaresScroll */
export const Task_FrontierSquaresScroll: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sFrontierSquaresScroll_Funcs[task.data[0]](_gt(rt, taskId)));
};

/** Source: battle_transition.c → Task_ScrollBg */
export const Task_ScrollBg: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!(task.data[2] ^= 1))
      {
          rt.SetGpuReg(REG_OFFSET_BG0VOFS, gBattle_BG0_X);
          rt.SetGpuReg(REG_OFFSET_BG0HOFS, gBattle_BG0_Y);
          gBattle_BG0_X += task.data[0];
          gBattle_BG0_Y += task.data[1];
      }
};

/** Source: battle_transition.c → CB2_TestBattleTransition */
export const CB2_TestBattleTransition: CB2Callback = (rt) => {
  switch (sTestingTransitionState)
      {
      case 0:
          LaunchBattleTransitionTask(sTestingTransitionId);
          sTestingTransitionState++;
          break;
      case 1:
          if (IsBattleTransitionDone())
          {
              sTestingTransitionState = 0;
              /* TODO scene transition: SetMainCallback2(CB2_ReturnToField) */;
          }
          break;
      }

      RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};
