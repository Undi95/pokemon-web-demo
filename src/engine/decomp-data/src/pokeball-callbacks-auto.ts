// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/pokeball.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
import {
  gMPlayInfo_BGM,
  gMain,
  m4aMPlayAllStop,
  m4aSongNumStart,
} from '../../system/decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const B_POSITION_OPPONENT_LEFT = 1;
const B_POSITION_OPPONENT_RIGHT = 3;
const B_POSITION_PLAYER_LEFT = 0;
const B_POSITION_PLAYER_RIGHT = 2;
const B_SIDE_OPPONENT = 1;
const B_SIDE_PLAYER = 0;
const BATTLE_TYPE_LINK = 2;
const BATTLE_TYPE_MULTI = 64;
const BATTLER_AFFINE_EMERGE = 1;
const BATTLER_AFFINE_NORMAL = 0;
const BATTLER_AFFINE_RETURN = 2;
const BATTLER_COORD_X = 0;
const BATTLER_COORD_X_2 = 2;
const BATTLER_COORD_Y = 1;
const BATTLER_COORD_Y_PIC_OFFSET = 3;
const CRY_MODE_DOUBLES = 1;
const CRY_MODE_NORMAL = 0;
const CRY_MODE_WEAK = 11;
const CRY_MODE_WEAK_DOUBLES = 12;
const MON_DATA_POKEBALL = 38;
const MON_DATA_SPECIES = 11;
const MUS_EVOLVED = 371;
const POKEBALL_OPPONENT_SENDOUT = 254;
const POKEBALL_PLAYER_SENDOUT = 255;
const SE_BALL = 23;
const SE_BALL_BOUNCE_1 = 56;
const SE_BALL_BOUNCE_2 = 57;
const SE_BALL_BOUNCE_3 = 58;
const SE_BALL_BOUNCE_4 = 59;
const SE_BALL_THROW = 61;
const SE_BALL_TRADE = 60;
const SPECIES_EGG = 412;
const TRACKS_ALL = 65535;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const HIBYTE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: pokeball.c → SpriteCB_BallThrow */
export const SpriteCB_BallThrow: SpriteCallback = (sprite, rt) => {
  if (TranslateAnimHorizontalArc(sprite))
      {
          let ballId = 0;
          let taskId = rt.gba.oam[sprite.oamIndex].affineParam;
          let opponentBattler = task.data[4];
          let noOfShakes = task.data[2];

          rt.StartSpriteAnim(sprite.spriteId, 1);
          sprite.affineAnimPaused = true;
          sprite.x += sprite.x2;
          sprite.y += sprite.y2;
          sprite.x2 = 0;
          sprite.y2 = 0;
          sprite.data[5] = 0;
          ballId = ItemIdToBallId(GetBattlerPokeballItemId(opponentBattler));
          AnimateBallOpenParticles(sprite.x, sprite.y - 5, 1, 28, ballId);
          sprite.data[0] = LaunchBallFadeMonTask(false, opponentBattler, 14, ballId);
          sprite.data[6] = opponentBattler;
          sprite.data[7] = noOfShakes;
          rt.DestroyTask(taskId);
          sprite.callback = SpriteCB_BallThrow_ReachMon;
      }
};

/** Source: pokeball.c → SpriteCB_BallThrow_ReachMon */
export const SpriteCB_BallThrow_ReachMon: SpriteCallback = (sprite, rt) => {
  sprite.callback = SpriteCB_BallThrow_StartShrinkMon;
};

/** Source: pokeball.c → SpriteCB_BallThrow_StartShrinkMon */
export const SpriteCB_BallThrow_StartShrinkMon: SpriteCallback = (sprite, rt) => {
  if (++sprite.data[5] == 10)
      {
          sprite.data[5] = 0;
          sprite.callback = SpriteCB_BallThrow_ShrinkMon;
          rt.StartSpriteAffineAnim(gBattlerSpriteIds[sprite.data[6]], BATTLER_AFFINE_RETURN);
          AnimateSprite(_gs(rt, gBattlerSpriteIds[sprite.data[6]]));
          _gs(rt, gBattlerSpriteIds[sprite.data[6]]).data[1] = 0;
      }
};

/** Source: pokeball.c → SpriteCB_BallThrow_ShrinkMon */
export const SpriteCB_BallThrow_ShrinkMon: SpriteCallback = (sprite, rt) => {
  sprite.data[5]++;
      if (sprite.data[5] == 11)
          PlaySE(SE_BALL_TRADE);

      if (_gs(rt, gBattlerSpriteIds[sprite.data[6]]).affineAnimEnded)
      {
          rt.StartSpriteAnim(sprite.spriteId, 2);
          _gs(rt, gBattlerSpriteIds[sprite.data[6]]).invisible = true;
          sprite.data[5] = 0;
          sprite.callback = SpriteCB_BallThrow_Close;
      }
      else
      {
          _gs(rt, gBattlerSpriteIds[sprite.data[6]]).data[1] += 0x60;
          _gs(rt, gBattlerSpriteIds[sprite.data[6]]).y2 = -_gs(rt, gBattlerSpriteIds[sprite.data[6]]).data[1] >> 8;
      }
};

/** Source: pokeball.c → SpriteCB_BallThrow_Close */
export const SpriteCB_BallThrow_Close: SpriteCallback = (sprite, rt) => {
  if (sprite.animEnded)
      {
          sprite.data[5]++;
          if (sprite.data[5] == 1)
          {
              sprite.data[3] = 0;
              sprite.data[4] = 32;
              sprite.data[5] = 0;
              sprite.y += Cos(0, 32);
              sprite.y2 = -Cos(0, sprite.data[4]);
              sprite.callback = SpriteCB_BallThrow_FallToGround;
          }
      }
};

/** Source: pokeball.c → SpriteCB_BallThrow_FallToGround */
export const SpriteCB_BallThrow_FallToGround: SpriteCallback = (sprite, rt) => {
  let r5 = false;

      switch (sprite.data[3] & 0xFF)
      {
      case 0:
          sprite.y2 = -Cos(sprite.data[5], sprite.data[4]);
          sprite.data[5] += 4 + (sprite.data[3] >> 8);
          if (sprite.data[5] >= 64)
          {
              sprite.data[4] -= 10;
              sprite.data[3] += 0x101;
              if (sprite.data[3] >> 8 == 4)
                  r5 = true;
              switch (sprite.data[3] >> 8)
              {
              case 1:
                  PlaySE(SE_BALL_BOUNCE_1);
                  break;
              case 2:
                  PlaySE(SE_BALL_BOUNCE_2);
                  break;
              case 3:
                  PlaySE(SE_BALL_BOUNCE_3);
                  break;
              default:
                  PlaySE(SE_BALL_BOUNCE_4);
                  break;
              }
          }
          break;
      case 1:
          sprite.y2 = -Cos(sprite.data[5], sprite.data[4]);
          sprite.data[5] -= 4 + (sprite.data[3] >> 8);
          if (sprite.data[5] <= 0)
          {
              sprite.data[5] = 0;
              sprite.data[3] &= 0xFF00;
          }
          break;
      }
      if (r5)
      {
          sprite.data[3] = 0;
          sprite.y += Cos(64, 32);
          sprite.y2 = 0;
          if (sprite.data[7] == 0)
          {
              sprite.callback = SpriteCB_ReleaseMonFromBall;
          }
          else
          {
              sprite.callback = SpriteCB_BallThrow_StartShakes;
              sprite.data[4] = 1;
              sprite.data[5] = 0;
          }
      }
};

/** Source: pokeball.c → SpriteCB_BallThrow_StartShakes */
export const SpriteCB_BallThrow_StartShakes: SpriteCallback = (sprite, rt) => {
  sprite.data[3]++;
      if (sprite.data[3] == 31)
      {
          sprite.data[3] = 0;
          sprite.affineAnimPaused = true;
          rt.StartSpriteAffineAnim(sprite.spriteId, 1);
          sprite.callback = SpriteCB_BallThrow_Shake;
          PlaySE(SE_BALL);
      }
};

/** Source: pokeball.c → SpriteCB_BallThrow_Shake */
export const SpriteCB_BallThrow_Shake: SpriteCallback = (sprite, rt) => {
  switch (sprite.data[3] & 0xFF)
      {
      case 0:
      case 2:
          sprite.x2 += sprite.data[4];
          sprite.data[5] += sprite.data[4];
          sprite.affineAnimPaused = false;
          if (sprite.data[5] > 3 || sprite.data[5] < -3)
          {
              sprite.data[3]++;
              sprite.data[5] = 0;
          }
          break;
      case 1:
          sprite.data[5]++;
          if (sprite.data[5] == 1)
          {
              sprite.data[5] = 0;
              sprite.data[4] = -sprite.data[4];
              sprite.data[3]++;
              sprite.affineAnimPaused = false;
              if (sprite.data[4] < 0)
                  ChangeSpriteAffineAnim(sprite, 2);
              else
                  ChangeSpriteAffineAnim(sprite, 1);
          }
          else
          {
              sprite.affineAnimPaused = true;
          }
          break;
      case 3:
          sprite.data[3] += 0x100;
          if (sprite.data[3] >> 8 == sprite.data[7])
          {
              sprite.callback = SpriteCB_ReleaseMonFromBall;
          }
          else
          {
              if (sprite.data[7] == 4 && sprite.data[3] >> 8 == 3)
              {
                  sprite.callback = SpriteCB_BallThrow_StartCaptureMon;
                  sprite.affineAnimPaused = true;
              }
              else
              {
                  sprite.data[3]++;
                  sprite.affineAnimPaused = true;
              }
          }
          break;
      case 4:
      default:
          sprite.data[5]++;
          if (sprite.data[5] == 31)
          {
              sprite.data[5] = 0;
              sprite.data[3] &= 0xFF00;
              rt.StartSpriteAffineAnim(sprite.spriteId, 3);
              if (sprite.data[4] < 0)
                  rt.StartSpriteAffineAnim(sprite.spriteId, 2);
              else
                  rt.StartSpriteAffineAnim(sprite.spriteId, 1);

              PlaySE(SE_BALL);
          }
          break;
      }
};

/** Source: pokeball.c → SpriteCB_ReleaseMonFromBall */
export const SpriteCB_ReleaseMonFromBall: SpriteCallback = (sprite, rt) => {
  let battler = sprite.data[6];
      let ballId = 0;

      rt.StartSpriteAnim(sprite.spriteId, 1);
      ballId = ItemIdToBallId(GetBattlerPokeballItemId(battler));
      AnimateBallOpenParticles(sprite.x, sprite.y - 5, 1, 28, ballId);
      sprite.data[0] = LaunchBallFadeMonTask(true, sprite.data[6], 14, ballId);
      sprite.callback = HandleBallAnimEnd;

      if (gMain.inBattle)
      {
          let mon: any = null;
          let species = 0;
          let pan = 0;
          let wantedCryCase = 0;
          let taskId = 0;

          if (GetBattlerSide(battler) != B_SIDE_PLAYER)
          {
              mon = gEnemyParty[gBattlerPartyIndexes[battler]];
              pan = 25;
          }
          else
          {
              mon = gPlayerParty[gBattlerPartyIndexes[battler]];
              pan = -25;
          }

          species = GetMonData(mon, MON_DATA_SPECIES);
          if ((battler == GetBattlerAtPosition(B_POSITION_PLAYER_LEFT) || battler == GetBattlerAtPosition(B_POSITION_OPPONENT_LEFT))
           && IsDoubleBattle() && gBattleSpritesDataPtr.animationData.introAnimActive)
          {
              if (gBattleTypeFlags & BATTLE_TYPE_MULTI && gBattleTypeFlags & BATTLE_TYPE_LINK)
              {
                  if (IsBGMPlaying())
                      m4aMPlayStop(gMPlayInfo_BGM);
              }
              else
              {
                  m4aMPlayVolumeControl(gMPlayInfo_BGM, TRACKS_ALL, 128);
              }
          }

          if (!IsDoubleBattle() || !gBattleSpritesDataPtr.animationData.introAnimActive)
              wantedCryCase = 0;
          else if (battler == GetBattlerAtPosition(B_POSITION_PLAYER_LEFT) || battler == GetBattlerAtPosition(B_POSITION_OPPONENT_LEFT))
              wantedCryCase = 1;
          else
              wantedCryCase = 2;

          gBattleSpritesDataPtr.healthBoxesData[battler].waitForCry = true;

          taskId = rt.CreateTask((t) => Task_PlayCryWhenReleasedFromBall(t, rt), 3);
          task.data[0] = species;
          task.data[1] = pan;
          task.data[2] = wantedCryCase;
          task.data[3] = battler;
          task.data[4] = gBattlerSpriteIds[sprite.data[6]];
          task.data[5] = (mon) >> 16;
          task.data[6] = (mon);
          task.data[15] = 0;
      }

      rt.StartSpriteAffineAnim(gBattlerSpriteIds[sprite.data[6]], BATTLER_AFFINE_EMERGE);

      if (GetBattlerSide(sprite.data[6]) == B_SIDE_OPPONENT)
          rt.setSpriteCallback(gBattlerSpriteIds[sprite.data[6]], SpriteCB_OpponentMonFromBall);
      else
          rt.setSpriteCallback(gBattlerSpriteIds[sprite.data[6]], SpriteCB_PlayerMonFromBall);

      AnimateSprite(_gs(rt, gBattlerSpriteIds[sprite.data[6]]));
      _gs(rt, gBattlerSpriteIds[sprite.data[6]]).data[1] = 0x1000;
};

/** Source: pokeball.c → SpriteCB_BallThrow_StartCaptureMon */
export const SpriteCB_BallThrow_StartCaptureMon: SpriteCallback = (sprite, rt) => {
  sprite.animPaused = true;
      sprite.callback = SpriteCB_BallThrow_CaptureMon;
      sprite.data[3] = 0;
      sprite.data[4] = 0;
      sprite.data[5] = 0;
};

/** Source: pokeball.c → SpriteCB_BallThrow_CaptureMon */
export const SpriteCB_BallThrow_CaptureMon: SpriteCallback = (sprite, rt) => {
  let battler = sprite.data[6];

      sprite.data[4]++;
      if (sprite.data[4] == 40)
      {
          return;
      }
      else if (sprite.data[4] == 95)
      {
          gDoingBattleAnim = false;
          m4aMPlayAllStop();
          m4aSongNumStart(MUS_EVOLVED);
      }
      else if (sprite.data[4] == 315)
      {
          FreeOamMatrix(rt.gba.oam[_gs(rt, gBattlerSpriteIds[sprite.data[6]]).oamIndex].matrixNum);
          rt.DestroySprite(gBattlerSpriteIds[sprite.data[6]]);
          DestroySpriteAndFreeResources(sprite);
          if (gMain.inBattle)
              gBattleSpritesDataPtr.healthBoxesData[battler].ballAnimActive = false;
      }
};

/** Source: pokeball.c → SpriteCB_PlayerMonSendOut_1 */
export const SpriteCB_PlayerMonSendOut_1: SpriteCallback = (sprite, rt) => {
  sprite.data[0] = 25;
      sprite.data[2] = GetBattlerSpriteCoord(sprite.data[6], BATTLER_COORD_X_2);
      sprite.data[4] = GetBattlerSpriteCoord(sprite.data[6], BATTLER_COORD_Y_PIC_OFFSET) + 24;
      sprite.data[5] = -30;
      rt.gba.oam[sprite.oamIndex].affineParam = sprite.data[6];
      InitAnimArcTranslation(sprite);
      sprite.callback = SpriteCB_PlayerMonSendOut_2;
};

/** Source: pokeball.c → SpriteCB_PlayerMonSendOut_2 */
export const SpriteCB_PlayerMonSendOut_2: SpriteCallback = (sprite, rt) => {
  let r6 = 0;
      let r7 = 0;

      if (HIBYTE(sprite.data[7]) >= 35 && HIBYTE(sprite.data[7]) < 80)
      {
          let r4 = 0;

          if ((rt.gba.oam[sprite.oamIndex].affineParam & 0xFF00) == 0)
          {
              r6 = sprite.data[1] & 1;
              r7 = sprite.data[2] & 1;
              sprite.data[1] = ((sprite.data[1] / 3) & ~1) | r6;
              sprite.data[2] = ((sprite.data[2] / 3) & ~1) | r7;
              rt.StartSpriteAffineAnim(sprite.spriteId, 4);
          }
          r4 = sprite.data[0];
          AnimTranslateLinear(sprite);
          sprite.data[7] += sprite.data[6] / 3;
          sprite.y2 += Sin(HIBYTE(sprite.data[7]), sprite.data[5]);
          rt.gba.oam[sprite.oamIndex].affineParam += 0x100;
          if ((rt.gba.oam[sprite.oamIndex].affineParam >> 8) % 3 != 0)
              sprite.data[0] = r4;
          else
              sprite.data[0] = r4 - 1;
          if (HIBYTE(sprite.data[7]) >= 80)
          {
              r6 = sprite.data[1] & 1;
              r7 = sprite.data[2] & 1;
              sprite.data[1] = ((sprite.data[1] * 3) & ~1) | r6;
              sprite.data[2] = ((sprite.data[2] * 3) & ~1) | r7;
          }
      }
      else
      {
          if (TranslateAnimHorizontalArc(sprite))
          {
              sprite.x += sprite.x2;
              sprite.y += sprite.y2;
              sprite.y2 = 0;
              sprite.x2 = 0;
              sprite.data[6] = rt.gba.oam[sprite.oamIndex].affineParam & 0xFF;
              sprite.data[0] = 0;

              if (IsDoubleBattle() && gBattleSpritesDataPtr.animationData.introAnimActive
               && sprite.data[6] == GetBattlerAtPosition(B_POSITION_PLAYER_RIGHT))
                  sprite.callback = SpriteCB_ReleaseMon2FromBall;
              else
                  sprite.callback = SpriteCB_ReleaseMonFromBall;

              rt.StartSpriteAffineAnim(sprite.spriteId, 0);
          }
      }
};

/** Source: pokeball.c → SpriteCB_ReleaseMon2FromBall */
export const SpriteCB_ReleaseMon2FromBall: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0]++ > 24)
      {
          sprite.data[0] = 0;
          sprite.callback = SpriteCB_ReleaseMonFromBall;
      }
};

/** Source: pokeball.c → SpriteCB_OpponentMonSendOut */
export const SpriteCB_OpponentMonSendOut: SpriteCallback = (sprite, rt) => {
  sprite.data[0]++;
      if (sprite.data[0] > 15)
      {
          sprite.data[0] = 0;
          if (IsDoubleBattle() && gBattleSpritesDataPtr.animationData.introAnimActive
           && sprite.data[6] == GetBattlerAtPosition(B_POSITION_OPPONENT_RIGHT))
              sprite.callback = SpriteCB_ReleaseMon2FromBall;
          else
              sprite.callback = SpriteCB_ReleaseMonFromBall;
      }
};

/** Source: pokeball.c → SpriteCB_PokeballReleaseMon */
export const SpriteCB_PokeballReleaseMon: SpriteCallback = (sprite, rt) => {
  if (sprite.data[1] == 0)
      {
          let subpriority = 0;
          let spriteId = sprite.data[0];
          let monPalNum = sprite.data[2];
          let selectedPalettes = sprite.data[3] | (sprite.data[4] << 16);

          if (sprite.subpriority != 0)
              subpriority = sprite.subpriority - 1;
          else
              subpriority = 0;

          rt.StartSpriteAnim(sprite.spriteId, 1);
          AnimateBallOpenParticlesForPokeball(sprite.x, sprite.y - 5, rt.gba.oam[sprite.oamIndex].priority, subpriority);
           
          sprite.data[1] = LaunchBallFadeMonTaskForPokeball(true, monPalNum, selectedPalettes);
          sprite.callback = SpriteCB_ReleasedMonFlyOut;
          _gs(rt, spriteId).invisible = false;
          rt.StartSpriteAffineAnim(spriteId, BATTLER_AFFINE_EMERGE);
          AnimateSprite(_gs(rt, spriteId));
          _gs(rt, spriteId).data[1] = 0x1000;
          sprite.data[7] = 0;
      }
      else
      {
          sprite.data[1]--;
      }
};

/** Source: pokeball.c → SpriteCB_ReleasedMonFlyOut */
export const SpriteCB_ReleasedMonFlyOut: SpriteCallback = (sprite, rt) => {
  let emergeAnimFinished = false;
      let atFinalPosition = false;
      let monSpriteId = sprite.data[0];
      let x, y;

      if (sprite.animEnded)
          sprite.invisible = true;

      if (_gs(rt, monSpriteId).affineAnimEnded)
      {
          rt.StartSpriteAffineAnim(monSpriteId, BATTLER_AFFINE_NORMAL);
          emergeAnimFinished = true;
      }x = Math.trunc((sprite.data[5] - sprite.x) * sprite.data[7] / 128) + sprite.x;
  y = Math.trunc((sprite.data[6] - sprite.y) * sprite.data[7] / 128) + sprite.y;
      _gs(rt, monSpriteId).x = x;
      _gs(rt, monSpriteId).y = y;

      if (sprite.data[7] < 128)
      {
          let sine = -(gSineTable(sprite.data[7]) / 8);

          sprite.data[7] += 4;
          _gs(rt, monSpriteId).x2 = sine;
          _gs(rt, monSpriteId).y2 = sine;
      }
      else
      {
          _gs(rt, monSpriteId).x = sprite.data[5];
          _gs(rt, monSpriteId).y = sprite.data[6];
          _gs(rt, monSpriteId).x2 = 0;
          _gs(rt, monSpriteId).y2 = 0;
          atFinalPosition = true;
      }
      if (sprite.animEnded && emergeAnimFinished && atFinalPosition)
      {
          if (_gs(rt, monSpriteId).data[7] == SPECIES_EGG)
              DoMonFrontSpriteAnimation(_gs(rt, monSpriteId), _gs(rt, monSpriteId).data[7], true, 0);
          else
              DoMonFrontSpriteAnimation(_gs(rt, monSpriteId), _gs(rt, monSpriteId).data[7], false, 0);

          DestroySpriteAndFreeResources(sprite);
      }
};

/** Source: pokeball.c → SpriteCB_TradePokeball */
export const SpriteCB_TradePokeball: SpriteCallback = (sprite, rt) => {
  if (sprite.data[1] == 0)
      {
          let subpriority = 0;
          let monSpriteId = sprite.data[0];
          let monPalNum = sprite.data[2];
          let selectedPalettes = sprite.data[3] | (sprite.data[4] << 16);

          if (sprite.subpriority != 0)
              subpriority = sprite.subpriority - 1;
          else
              subpriority = 0;

          rt.StartSpriteAnim(sprite.spriteId, 1);
          AnimateBallOpenParticlesForPokeball(sprite.x, sprite.y - 5, rt.gba.oam[sprite.oamIndex].priority, subpriority);
           
          sprite.data[1] = LaunchBallFadeMonTaskForPokeball(true, monPalNum, selectedPalettes);
          sprite.callback = SpriteCB_TradePokeballSendOff;
           
           
          _gs(rt, monSpriteId).affineAnimPaused = false;
          rt.StartSpriteAffineAnim(monSpriteId, BATTLER_AFFINE_RETURN);
          AnimateSprite(_gs(rt, monSpriteId));
          _gs(rt, monSpriteId).data[1] = 0;
      }
      else
      {
          sprite.data[1]--;
      }
};

/** Source: pokeball.c → SpriteCB_TradePokeballSendOff */
export const SpriteCB_TradePokeballSendOff: SpriteCallback = (sprite, rt) => {
  let monSpriteId = 0;

      sprite.data[5]++;
      if (sprite.data[5] == 11)
          PlaySE(SE_BALL_TRADE);

      monSpriteId = sprite.data[0];
      if (_gs(rt, monSpriteId).affineAnimEnded)
      {
          rt.StartSpriteAnim(sprite.spriteId, 2);
          _gs(rt, monSpriteId).invisible = true;
          sprite.data[5] = 0;
          sprite.callback = SpriteCB_TradePokeballEnd;
      }
      else
      {
          _gs(rt, monSpriteId).data[1] += 96;
          _gs(rt, monSpriteId).y2 = -_gs(rt, monSpriteId).data[1] >> 8;
      }
};

/** Source: pokeball.c → SpriteCB_TradePokeballEnd */
export const SpriteCB_TradePokeballEnd: SpriteCallback = (sprite, rt) => {
  if (sprite.animEnded)
          sprite.callback = SpriteCallbackDummy;
};

/** Source: pokeball.c → SpriteCB_HealthboxSlideInDelayed */
export const SpriteCB_HealthboxSlideInDelayed: SpriteCallback = (sprite, rt) => {
  sprite.data[1]++;
      if (sprite.data[1] == 20)
      {
          sprite.data[1] = 0;
          sprite.callback = SpriteCB_HealthboxSlideIn;
      }
};

/** Source: pokeball.c → SpriteCB_HealthboxSlideIn */
export const SpriteCB_HealthboxSlideIn: SpriteCallback = (sprite, rt) => {
  sprite.x2 -= sprite.data[0];
      sprite.y2 -= sprite.data[1];
      if (sprite.x2 == 0 && sprite.y2 == 0)
          sprite.callback = SpriteCallbackDummy;
};

/** Source: pokeball.c → SpriteCB_HitAnimHealthoxEffect */
export const SpriteCB_HitAnimHealthoxEffect: SpriteCallback = (sprite, rt) => {
  let r1 = sprite.data[1];

      _gs(rt, r1).y2 = sprite.data[0];
      sprite.data[0] = -sprite.data[0];
      sprite.data[2]++;
      if (sprite.data[2] == 21)
      {
          _gs(rt, r1).x2 = 0;
          _gs(rt, r1).y2 = 0;
          rt.DestroySprite(sprite.spriteId);
      }
};

/** Source: pokeball.c → Task_DoPokeballSendOutAnim */
export const Task_DoPokeballSendOutAnim: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let throwCaseId = 0;
      let battler = 0;
      let itemId, ballId;
      let ballSpriteId = 0;
      let notSendOut = false;

      if (task.data[0] == 0)
      {
          task.data[0]++;
          return;
      }

      throwCaseId = task.data[2];
      battler = task.data[3];

      if (GetBattlerSide(battler) != B_SIDE_PLAYER)
          itemId = GetMonData(gEnemyParty[gBattlerPartyIndexes[battler]], MON_DATA_POKEBALL);
      else
          itemId = GetMonData(gPlayerParty[gBattlerPartyIndexes[battler]], MON_DATA_POKEBALL);

      ballId = ItemIdToBallId(itemId);
      LoadBallGfx(ballId);
      ballSpriteId = CreateSprite(gBallSpriteTemplates[ballId], 32, 80, 29);
      _gs(rt, ballSpriteId).data[0] = 0x80;
      _gs(rt, ballSpriteId).data[1] = 0;
      _gs(rt, ballSpriteId).data[7] = throwCaseId;

      switch (throwCaseId)
      {
      case POKEBALL_PLAYER_SENDOUT:
          gBattlerTarget = battler;
          _gs(rt, ballSpriteId).x = 24;
          _gs(rt, ballSpriteId).y = 68;
          rt.setSpriteCallback(ballSpriteId, SpriteCB_PlayerMonSendOut_1);
          break;
      case POKEBALL_OPPONENT_SENDOUT:
          _gs(rt, ballSpriteId).x = GetBattlerSpriteCoord(battler, BATTLER_COORD_X);
          _gs(rt, ballSpriteId).y = GetBattlerSpriteCoord(battler, BATTLER_COORD_Y) + 24;
          gBattlerTarget = battler;
          _gs(rt, ballSpriteId).data[0] = 0;
          rt.setSpriteCallback(ballSpriteId, SpriteCB_OpponentMonSendOut);
          break;
      default:
          gBattlerTarget = GetBattlerAtPosition(B_POSITION_OPPONENT_LEFT);
          notSendOut = true;
          break;
      }

      _gs(rt, ballSpriteId).data[6] = gBattlerTarget;
      if (!notSendOut)
      {
          rt.DestroyTask(taskId);
          return;
      }

       
      _gs(rt, ballSpriteId).data[0] = 34;
      _gs(rt, ballSpriteId).data[2] = GetBattlerSpriteCoord(gBattlerTarget, BATTLER_COORD_X);
      _gs(rt, ballSpriteId).data[4] = GetBattlerSpriteCoord(gBattlerTarget, BATTLER_COORD_Y) - 16;
      _gs(rt, ballSpriteId).data[5] = -40;
      InitAnimArcTranslation(_gs(rt, ballSpriteId));
      rt.gba.oam[_gs(rt, ballSpriteId).oamIndex].affineParam = taskId;
      task.data[4] = gBattlerTarget;
      task.func = (t) => TaskDummy(t, rt);
      PlaySE(SE_BALL_THROW);
};

/** Source: pokeball.c → Task_PlayCryWhenReleasedFromBall */
export const Task_PlayCryWhenReleasedFromBall: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let wantedCry = task.data[2];
      let pan = task.data[1];
      let species = task.data[0];
      let battler = task.data[3];
      let monSpriteId = task.data[4];
      let mon: any = ((task.data[5] << 16) | (task.data[6]));

      switch (task.data[15])
      {
      case 0:
      default:
          if (_gs(rt, monSpriteId).affineAnimEnded)
              task.data[15] = wantedCry + 1;
          break;
      case 1:
           
          if (ShouldPlayNormalMonCry(mon) == true)
              PlayCry_ByMode(species, pan, CRY_MODE_NORMAL);
          else
              PlayCry_ByMode(species, pan, CRY_MODE_WEAK);
          gBattleSpritesDataPtr.healthBoxesData[battler].waitForCry = false;
          rt.DestroyTask(taskId);
          break;
      case 2:
          StopCryAndClearCrySongs();
          task.data[10] = 3;
          task.data[15] = 20;
          break;
      case 20:
          if (task.data[10] == 0)
          {
               
              if (ShouldPlayNormalMonCry(mon) == true)
                  PlayCry_ReleaseDouble(species, pan, CRY_MODE_DOUBLES);
              else
                  PlayCry_ReleaseDouble(species, pan, CRY_MODE_WEAK_DOUBLES);

              gBattleSpritesDataPtr.healthBoxesData[battler].waitForCry = false;
              rt.DestroyTask(taskId);
          }
          else
          {
              task.data[10]--;
          }
          break;
      case 3:
          task.data[10] = 6;
          task.data[15] = 30;
          break;
      case 30:
          if (task.data[10] != 0)
          {
              task.data[10]--;
              break;
          }
          task.data[15]++;
           
      case 31:
          if (!IsCryPlayingOrClearCrySongs())
          {
              StopCryAndClearCrySongs();
              task.data[10] = 3;
              task.data[15]++;
          }
          break;
      case 32:
          if (task.data[10] != 0)
          {
              task.data[10]--;
              break;
          }
           
          if (ShouldPlayNormalMonCry(mon) == true)
              PlayCry_ReleaseDouble(species, pan, CRY_MODE_NORMAL);
          else
              PlayCry_ReleaseDouble(species, pan, CRY_MODE_WEAK);

          gBattleSpritesDataPtr.healthBoxesData[battler].waitForCry = false;
          rt.DestroyTask(taskId);
          break;
      }
};
