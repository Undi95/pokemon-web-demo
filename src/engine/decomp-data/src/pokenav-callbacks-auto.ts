// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/pokenav.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
import {
  FreeAllSpritePalettes,
  ResetTasks,
  UpdatePaletteFade,
} from '../../system/decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const LT_CONTINUE = 3;
const LT_FINISH = 4;
const LT_INC_AND_CONTINUE = 1;
const LT_INC_AND_PAUSE = 0;
const LT_PAUSE = 2;
const POKENAV_MAIN_MENU = 0;
const POKENAV_MENU_FUNC_EXIT = -1;
const POKENAV_MENU_IDS_START = 100000;
const POKENAV_MODE_FORCE_CALL_READY = 1;
const POKENAV_MODE_NORMAL = 0;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const LOOPED_TASK_DECODE_STATE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: pokenav.c → Task_RunLoopedTask */
export const Task_RunLoopedTask: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let loopedTask = GetWordTaskArg(taskId, 1);
      let state = task.data[0];
      let exitLoop = false;

      while (!exitLoop)
      {
          let action = loopedTask(state);
          switch (action)
          {
          case LT_INC_AND_CONTINUE:
              (state)++;
              break;
          case LT_INC_AND_PAUSE:
              (state)++;
              return;
          case LT_FINISH:
              rt.DestroyTask(taskId);
              return;
           
          default:
              state = LOOPED_TASK_DECODE_STATE(action);
              break;
          case LT_CONTINUE:
              break;
          case LT_PAUSE:
              return;
          }
      }
};

/** Source: pokenav.c → Task_RunLoopedTask_LinkMode */
export const Task_RunLoopedTask_LinkMode: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let task: any = null;
      let state = 0;
      let action = 0;

      if (Overworld_IsRecvQueueAtMax())
          return;

      task = GetWordTaskArg(taskId, 1);
      state = task.data[0];
      action = task(state);
      switch (action)
      {
      case LT_INC_AND_PAUSE:
      case LT_INC_AND_CONTINUE:
          (state)++;
          break;
      case LT_FINISH:
          rt.DestroyTask(taskId);
          break;
       
      default:
          state = LOOPED_TASK_DECODE_STATE(action);
          break;
      case LT_PAUSE:
      case LT_CONTINUE:
          break;
      }
};

/** Source: pokenav.c → Task_Pokenav */
export const Task_Pokenav: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let menuId = 0;
      const data = task.data;

      switch (data[0])
      {
      case 0:
          InitPokenavMainMenu();
          data[0] = 1;
          break;
      case 1:
           
          if (PokenavMainMenuLoopedTaskIsActive())
              break;
          SetActivePokenavMenu(POKENAV_MAIN_MENU);
          data[0] = 4;
          break;
      case 2:
          if (IsActiveMenuLoopTaskActive())
              break;
          data[0] = 3;
      case 3:
          menuId = GetCurrentMenuCB();
          if (menuId == POKENAV_MENU_FUNC_EXIT)
          {
              ShutdownPokenav();
              data[0] = 5;
          }
          else if (menuId >= POKENAV_MENU_IDS_START)
          {
              PokenavMenuCallbacks[gPokenavResources.currentMenuIndex].free2();
              PokenavMenuCallbacks[gPokenavResources.currentMenuIndex].free1();
              if (SetActivePokenavMenu(menuId))
              {
                  data[0] = 4;
              }
              else
              {
                  ShutdownPokenav();
                  data[0] = 5;
              }
          }
          else if (menuId != 0)
          {
              RunMainMenuLoopedTask(menuId);
              if (IsActiveMenuLoopTaskActive())
                  data[0] = 2;
          }
          break;
      case 4:
          if (!IsActiveMenuLoopTaskActive_())
              data[0] = 3;
          break;
      case 5:
          if (!WaitForPokenavShutdownFade())
          {
              let calledFromScript = (gPokenavResources.mode != POKENAV_MODE_NORMAL);

              FreeMenuHandlerSubstruct1();
              FreePokenavResources();
              if (calledFromScript)
                  /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
              else
                  /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldWithOpenMenu) */;
          }
          break;
      }
};

/** Source: pokenav.c → CB2_InitPokeNav */
export const CB2_InitPokeNav: CB2Callback = (rt) => {
  gPokenavResources = Alloc(((gPokenavResources)?.length ?? 32));
      if (gPokenavResources == null)
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldWithOpenMenu) */;
      }
      else
      {
          InitPokenavResources(gPokenavResources);
          ResetTasks();
          rt.SetVBlankCallback(VBlankCB);
          rt.CreateTask((t) => Task_Pokenav(t, rt), 0);
          rt.SetMainCallback2(CB2_Pokenav);
          rt.SetVBlankCallback(VBlankCB);
      }
};

/** Source: pokenav.c → CB2_InitPokenavForTutorial */
export const CB2_InitPokenavForTutorial: CB2Callback = (rt) => {
  UpdatePaletteFade();
      if (rt.gPaletteFade.active)
          return;

      gPokenavResources = Alloc(((gPokenavResources)?.length ?? 32));
      if (gPokenavResources == null)
      {
          /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldContinueScriptPlayMapMusic) */;
      }
      else
      {
          InitPokenavResources(gPokenavResources);
          gPokenavResources.mode = POKENAV_MODE_FORCE_CALL_READY;
          ResetTasks();
          rt.ResetSpriteData();
          FreeAllSpritePalettes();
          rt.SetVBlankCallback(VBlankCB);
          rt.CreateTask((t) => Task_Pokenav(t, rt), 0);
          rt.SetMainCallback2(CB2_Pokenav);
          rt.SetVBlankCallback(VBlankCB);
      }
};

/** Source: pokenav.c → CB2_Pokenav */
export const CB2_Pokenav: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      UpdatePaletteFade();
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
