// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/minigame_countdown.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
import {
  m4aSongNumStart,
} from '../../system/decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const ANIM_ONE = 2;
const ANIM_START_LEFT = 3;
const ANIM_START_MID = 4;
const ANIM_START_RIGHT = 5;
const ANIM_THREE = 0;
const ANIM_TWO = 1;
const FUNC_FREE = 1;
const FUNC_RUN = 3;
const FUNC_START = 2;
const LINKCMD_COUNTDOWN = 32767;
const RFU_PACKET_SIZE = 6;
const SE_BALL_BOUNCE_1 = 56;
const SE_BALL_BOUNCE_2 = 57;
const SE_PIN = 21;
const STATE_END = 9;
const STATE_RUN = 3;
const STATE_START = 2;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: minigame_countdown.c → SpriteCB_StaticCountdown */
export const SpriteCB_StaticCountdown: SpriteCallback = (sprite, rt) => {
  let data = _gt(rt, sprite.data[3]).data;

      if (data[11] % data[5] != 0)
          return;
      if (data[11] == data[10])
          return;

      data[10] = data[11];
      switch (sprite.data[4])
      {
      case ANIM_THREE:
          sprite.invisible = false;
      case ANIM_TWO:
      case ANIM_ONE:
           
          PlaySE(SE_BALL_BOUNCE_1);
          rt.StartSpriteAnim(sprite.spriteId, sprite.data[4]);
          break;
      case ANIM_START_LEFT:
           
          PlaySE(SE_PIN);
          rt.StartSpriteAnim(sprite.spriteId, sprite.data[4]);
          _gs(rt, tSpriteIds(1)).invisible = false;
          _gs(rt, tSpriteIds(2)).invisible = false;
          break;
      case ANIM_START_LEFT + 1:  
           
          sprite.invisible = true;
          _gs(rt, tSpriteIds(1)).invisible = true;
          _gs(rt, tSpriteIds(2)).invisible = true;
          data[0] = STATE_END;
          return;
      }
      sprite.data[4]++;
};

/** Source: minigame_countdown.c → SpriteCB_Start */
export const SpriteCB_Start: SpriteCallback = (sprite, rt) => {
  let y = 0;
      let data = sprite.data;

      switch (data[0])
      {
      case 0:
          data[4] = 64;
          data[5] = sprite.y2 << 4;
          data[0]++;
      case 1:
          data[5] += data[4];
          data[4]++;
          sprite.y2 = data[5] >> 4;
          if (sprite.y2 >= 0)
          {
              PlaySE(SE_BALL_BOUNCE_2);
              sprite.y2 = 0;
              data[0]++;
          }
          break;
      case 2:
          data[1] += 12;
          if (data[1] >= 128)
          {
              PlaySE(SE_BALL_BOUNCE_2);
              data[1] = 0;
              data[0]++;
          }
          y = gSineTable(data[1]);
          sprite.y2 = -(y >> 4);
          break;
      case 3:
          data[1] += 16;
          if (data[1] >= 128)
          {
              PlaySE(SE_BALL_BOUNCE_2);
              data[1] = 0;
              data[0]++;
          }
          sprite.y2 = -(gSineTable(data[1]) >> 5);
          break;
      case 4:
          if (++data[1] > 40)
              sprite.callback = SpriteCallbackDummy;
          break;
      }
};

/** Source: minigame_countdown.c → Task_StaticCountdown */
export const Task_StaticCountdown: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
       
      case STATE_START:
          sStaticCountdownFuncs[data[1]][FUNC_START](taskId);
          data[0] = STATE_RUN;
          break;
      case STATE_RUN:
          sStaticCountdownFuncs[data[1]][FUNC_RUN](taskId);
          break;
      case STATE_END:
          sStaticCountdownFuncs[data[1]][FUNC_FREE](taskId);
          rt.DestroyTask(taskId);
          break;
      }
};

/** Source: minigame_countdown.c → Task_StaticCountdown_Init */
export const Task_StaticCountdown_Init: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      data[2] = 0;
      data[3] = 0;
      data[4] = 0;
      data[5] = 60;
      data[6] = 0;
      data[6] = 0;
      data[8] = 3;
      data[4] = 120;
      data[5] = 88;
      StaticCountdown_CreateSprites(taskId, data);

      rt.StartSpriteAnim(tSpriteIds(1), ANIM_START_MID);
      _gs(rt, tSpriteIds(1)).x2 = -32;

      rt.StartSpriteAnim(tSpriteIds(2), ANIM_START_RIGHT);
      _gs(rt, tSpriteIds(2)).x2 = 32;
};

/** Source: minigame_countdown.c → Task_StaticCountdown_Free */
export const Task_StaticCountdown_Free: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;
      const data = task.data;

      for (i = 0; i < data[8]; i++)
          rt.DestroySprite(tSpriteIds(i));
      FreeSpriteTilesByTag(sSpriteSheet_321Start_Static[data[3]].tag);
      FreeSpritePaletteByTag(sSpritePalette_321Start_Static[data[4]].tag);
};

/** Source: minigame_countdown.c → Task_StaticCountdown_Start */
export const Task_StaticCountdown_Start: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      PlaySE(SE_BALL_BOUNCE_1);
      rt.setSpriteCallback(tSpriteIds(0), SpriteCB_StaticCountdown);
      _gs(rt, tSpriteIds(0)).invisible = false;
      task.data[0] = STATE_RUN;
};

/** Source: minigame_countdown.c → Task_StaticCountdown_Run */
export const Task_StaticCountdown_Run: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let packet: number[] = new Array(RFU_PACKET_SIZE).fill(0);
      const data = task.data;

      if (gReceivedRemoteLinkPlayers)
      {
           
          if (gRecvCmds[0][1] == LINKCMD_COUNTDOWN)
              data[11] = gRecvCmds[0][2];

          if (GetMultiplayerId() == 0)
          {
               
               
              data[12]++;
              memset(packet, 0, ((packet)?.length ?? 32));
              packet[0] = LINKCMD_COUNTDOWN;
              packet[1] = data[12];
              Rfu_SendPacket(packet);
          }
      }
      else
      {
           
          data[11]++;
      }
};

/** Source: minigame_countdown.c → Task_MinigameCountdown */
export const Task_MinigameCountdown: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 0:
          Load321StartGfx(data[2], data[3]);
          data[7] = CreateNumberSprite(data[2], data[3], data[4], data[5], data[6]);
          CreateStartSprite(data[2], data[3], data[4], data[5], data[6], data[8], data[9]);
          data[0]++;
          break;
      case 1:
          if (!RunMinigameCountdownDigitsAnim(data[7]))
          {
              InitStartGraphic(data[7], data[8], data[9]);
              FreeSpriteOamMatrix(_gs(rt, data[7]));
              rt.DestroySprite(data[7]);
              data[0]++;
          }
          break;
      case 2:
          if (!IsStartGraphicAnimRunning(data[8]))
          {
              rt.DestroySprite(data[8]);
              rt.DestroySprite(data[9]);
              FreeSpriteTilesByTag(data[2]);
              FreeSpritePaletteByTag(data[3]);
              rt.DestroyTask(taskId);
          }
          break;
      }
};
