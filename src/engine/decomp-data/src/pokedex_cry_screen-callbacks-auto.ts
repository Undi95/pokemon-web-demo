// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/pokedex_cry_screen.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const MIN_NEEDLE_POS = 32;
const NEEDLE_MOVE_INCREMENT = 5;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: pokedex_cry_screen.c → SpriteCB_CryMeterNeedle */
export const SpriteCB_CryMeterNeedle: SpriteCallback = (sprite, rt) => {
  let i = 0;
      let peakAmplitude = 0;
      let x = 0;
      let y = 0;
      let affine: any = null;
      let matrix: any = null;
      let amplitude = 0;

      rt.gba.oam[_gs(rt, sCryMeterNeedle.spriteId).oamIndex].affineMode = ST_OAM_AFFINE_NORMAL;
      rt.gba.oam[_gs(rt, sCryMeterNeedle.spriteId).oamIndex].affineParam = 0;

       
       
      switch (sDexCryScreen.cryState)
      {
      case 0:
          sCryMeterNeedle.targetRotation = MIN_NEEDLE_POS;
          if (sCryMeterNeedle.rotation > 0)
          {
              if (sCryMeterNeedle.moveIncrement != 1)
                  sCryMeterNeedle.moveIncrement--;
          }
          else
          {
              sCryMeterNeedle.moveIncrement = NEEDLE_MOVE_INCREMENT;
          }
          break;
      case 2:
          peakAmplitude = 0;
          for (i = 0; i < ((sDexCryScreen.cryWaveformBuffer)?.length ?? 0); i++)
          {
              if (peakAmplitude < sDexCryScreen.cryWaveformBuffer[i])
                  peakAmplitude = sDexCryScreen.cryWaveformBuffer[i];
          }
          SetCryMeterNeedleTarget(peakAmplitude * 208 / 256);
          break;
      case 6:
           
          amplitude = sDexCryScreen.cryWaveformBuffer[10];
          SetCryMeterNeedleTarget(amplitude * 208 / 256);
          break;
      }

      if (sCryMeterNeedle.rotation == sCryMeterNeedle.targetRotation)
      {
           
      }
      else if (sCryMeterNeedle.rotation < sCryMeterNeedle.targetRotation)
      {
           
          sCryMeterNeedle.rotation += sCryMeterNeedle.moveIncrement;
          if (sCryMeterNeedle.rotation > sCryMeterNeedle.targetRotation)
          {
              sCryMeterNeedle.rotation = sCryMeterNeedle.targetRotation;
              sCryMeterNeedle.targetRotation = 0;
          }
      }
      else
      {
           
          sCryMeterNeedle.rotation -= sCryMeterNeedle.moveIncrement;
          if (sCryMeterNeedle.rotation < sCryMeterNeedle.targetRotation)
          {
              sCryMeterNeedle.rotation = sCryMeterNeedle.targetRotation;
              sCryMeterNeedle.targetRotation = 0;
          }
      }

      affine.xScale = 256;
      affine.yScale = 256;
      affine.rotation = sCryMeterNeedle.rotation * 256;
      ObjAffineSet(affine, matrix, 1, 2);
      SetOamMatrix(rt.gba, 0, matrix.a, matrix.b, matrix.c, matrix.d);
      x = gSineTable(((sCryMeterNeedle.rotation + 0x7) & 0xFF));
      y = gSineTable(((sCryMeterNeedle.rotation + 0x7) & 0xFF) + 64);sprite.x2 = Math.trunc(x * 24 / 256);
  sprite.y2 = Math.trunc(y * 24 / 256);
};
