// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/pokeblock.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  FreeAllSpritePalettes,
  FreeAllWindowBuffers,
  InitSpriteAffineAnim,
  JOY_NEW,
  UpdatePaletteFade,
  m4aSongNumStart,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const LIST_CANCEL = -2;
const LIST_NOTHING_CHOSEN = -1;
const MAX_MENU_ITEMS = 9;
const MENU_B_PRESSED = -1;
const MENU_NOTHING_CHOSEN = -2;
const PBLOCK_CASE_FEEDER = 2;
const PBLOCK_CASE_GIVE = 3;
const SE_SELECT = 5;
const SELECT_BUTTON = 4;
const TILE_HIGHLIGHT_BLUE = 4101;
const TILE_HIGHLIGHT_NONE = 5;
const TILE_HIGHLIGHT_RED = 8197;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: pokeblock.c → SpriteCB_ShakePokeblockCase */
export const SpriteCB_ShakePokeblockCase: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0] > 1)
          sprite.data[0] = 0;

      switch (sprite.data[0])
      {
      case 0:
          rt.gba.oam[sprite.oamIndex].affineMode = ST_OAM_AFFINE_NORMAL;
          sprite.affineAnimsTableName = sAffineAnims_PokeblockCaseShake;
          InitSpriteAffineAnim(sprite);
          sprite.data[0] = 1;
          sprite.data[1] = 0;
          break;
      case 1:
          if (++sprite.data[1] > 11)
          {
              rt.gba.oam[sprite.oamIndex].affineMode = ST_OAM_AFFINE_OFF;
              sprite.data[0] = 0;
              sprite.data[1] = 0;
              FreeOamMatrix(rt.gba.oam[sprite.oamIndex].affineParamIndex);
              sprite.callback = SpriteCallbackDummy;
          }
          break;
      }
};

/** Source: pokeblock.c → Task_FreeDataAndExitPokeblockCase */
export const Task_FreeDataAndExitPokeblockCase: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (!rt.gPaletteFade.active)
      {
          if (sPokeblockMenu.caseId == PBLOCK_CASE_FEEDER || sPokeblockMenu.caseId == PBLOCK_CASE_GIVE)
              gFieldCallback = FieldCB_ContinueScriptHandleMusic;

          DestroyListMenuTask(data[0], sSavedPokeblockData.scrollOffset, sSavedPokeblockData.selectedRow);
          DestroyScrollArrows();
          rt.ResetSpriteData();
          FreeAllSpritePalettes();

          if (sPokeblockMenu.callbackOnUse != null)
              /* TODO scene transition: SetMainCallback2(sPokeblockMenu.callbackOnUse) */;
          else
              /* TODO scene transition: SetMainCallback2(sSavedPokeblockData.callback) */;

          FreeAllWindowBuffers();
          Free(sPokeblockMenu);
          rt.DestroyTask(taskId);
      }
};

/** Source: pokeblock.c → Task_HandlePokeblockMenuInput */
export const Task_HandlePokeblockMenuInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (!rt.gPaletteFade.active && MenuHelpers_ShouldWaitForLinkRecv() != true)
      {
          if (JOY_NEW(SELECT_BUTTON))
          {
              ListMenuGetScrollAndRow(data[0], sSavedPokeblockData.scrollOffset, sSavedPokeblockData.selectedRow);
              if (sSavedPokeblockData.scrollOffset + sSavedPokeblockData.selectedRow != sPokeblockMenu.itemsNo - 1)
              {
                   
                  PlaySE(SE_SELECT);
                  DrawPokeblockMenuHighlight(sSavedPokeblockData.selectedRow, TILE_HIGHLIGHT_RED);
                  data[2] = sSavedPokeblockData.scrollOffset + sSavedPokeblockData.selectedRow;
                  sPokeblockMenu.isSwapping = true;
                  task.func = (t) => Task_HandlePokeblocksSwapInput(t, rt);
              }
          }
          else
          {
              let oldPosition = sSavedPokeblockData.selectedRow;
              let input = ListMenu_ProcessInput(data[0]);
              ListMenuGetScrollAndRow(data[0], sSavedPokeblockData.scrollOffset, sSavedPokeblockData.selectedRow);

              if (oldPosition != sSavedPokeblockData.selectedRow)
              {
                   
                  DrawPokeblockMenuHighlight(oldPosition, TILE_HIGHLIGHT_NONE);
                  DrawPokeblockMenuHighlight(sSavedPokeblockData.selectedRow, TILE_HIGHLIGHT_BLUE);
              }

              switch (input)
              {
              case LIST_NOTHING_CHOSEN:
                  break;
              case LIST_CANCEL:
                  PlaySE(SE_SELECT);
                  gSpecialVar_Result = 0xFFFF;
                  gSpecialVar_ItemId = 0;
                  FadePaletteAndSetTaskToClosePokeblockCase(taskId);
                  break;
              default:
                   
                  PlaySE(SE_SELECT);
                  gSpecialVar_ItemId = input;
                  ShowPokeblockActionsWindow(taskId);
                  break;
              }
          }
      }
};

/** Source: pokeblock.c → Task_HandlePokeblocksSwapInput */
export const Task_HandlePokeblocksSwapInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (MenuHelpers_ShouldWaitForLinkRecv() == true)
          return;

      if (JOY_NEW(SELECT_BUTTON))
      {
           
          PlaySE(SE_SELECT);
          ListMenuGetScrollAndRow(data[0], sSavedPokeblockData.scrollOffset, sSavedPokeblockData.selectedRow);
          UpdatePokeblockSwapMenu(taskId, false);
      }
      else
      {
          let i = sSavedPokeblockData.scrollOffset;
          let row = sSavedPokeblockData.selectedRow;
          let input = ListMenu_ProcessInput(data[0]);
          ListMenuGetScrollAndRow(data[0], sSavedPokeblockData.scrollOffset, sSavedPokeblockData.selectedRow);

          if (i != sSavedPokeblockData.scrollOffset || row != sSavedPokeblockData.selectedRow)
          {
              for (i = 0; i < MAX_MENU_ITEMS; i++)
              {
                  row = i + sSavedPokeblockData.scrollOffset;
                  if (row == data[2])
                      DrawPokeblockMenuHighlight(i, TILE_HIGHLIGHT_RED);
                  else
                      DrawPokeblockMenuHighlight(i, TILE_HIGHLIGHT_NONE);
              }
          }

          SetSwapLineSpritesInvisibility(sPokeblockMenu.swapLineSpriteIds, ((sPokeblockMenu.swapLineSpriteIds)?.length ?? 0), false);
          UpdateSwapLineSpritesPos(sPokeblockMenu.swapLineSpriteIds, ((sPokeblockMenu.swapLineSpriteIds)?.length ?? 0), 128, (sSavedPokeblockData.selectedRow * 16) + 8);

          switch (input)
          {
          case LIST_NOTHING_CHOSEN:
              break;
          case LIST_CANCEL:
              PlaySE(SE_SELECT);
              if (JOY_NEW(A_BUTTON))  
                  UpdatePokeblockSwapMenu(taskId, false);
              else
                  UpdatePokeblockSwapMenu(taskId, true);  
              break;
          default:
               
              PlaySE(SE_SELECT);
              UpdatePokeblockSwapMenu(taskId, false);
              break;
          }
      }
};

/** Source: pokeblock.c → Task_HandlePokeblockActionsInput */
export const Task_HandlePokeblockActionsInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let itemId = 0;

      if (MenuHelpers_ShouldWaitForLinkRecv() == true)
          return;

      itemId = Menu_ProcessInputNoWrap();
      if (itemId == MENU_NOTHING_CHOSEN)
      {
          return;
      }
      else if (itemId == MENU_B_PRESSED)
      {
          PlaySE(SE_SELECT);
          PokeblockAction_Cancel(taskId);
      }
      else
      {
          PlaySE(SE_SELECT);
          sPokeblockMenuActions[sPokeblockMenu.pokeblockActionIds[itemId]].func.void_u8(taskId);
      }
};

/** Source: pokeblock.c → CB2_PokeblockMenu */
export const CB2_PokeblockMenu: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      DoScheduledBgTilemapCopiesToVram();
      UpdatePaletteFade();
};

/** Source: pokeblock.c → CB2_InitPokeblockMenu */
export const CB2_InitPokeblockMenu: CB2Callback = (rt) => {
  while (1)
      {
          if (MenuHelpers_ShouldWaitForLinkRecv() == true)
              break;
          if (InitPokeblockMenu() == true)
              break;
          if (MenuHelpers_IsLinkActive() == true)
              break;
      }
};
