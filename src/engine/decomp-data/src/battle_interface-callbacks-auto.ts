// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_interface.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const B_SIDE_PLAYER = 0;
const BLDCNT_TGT2_ALL = 16128;
const PARTY_SIZE = 6;
const SE_BALL_TRAY_BALL = 115;
const SE_BALL_TRAY_EXIT = 116;
const SOUND_PAN_ATTACKER = -64;
const SOUND_PAN_TARGET = 63;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_interface.c → SpriteCB_HealthBar */
export const SpriteCB_HealthBar: SpriteCallback = (sprite, rt) => {
  let healthboxSpriteId = sprite.hBar_HealthBoxSpriteId;

      switch (sprite.hBar_Data6)
      {
      case 0:
          sprite.x = _gs(rt, healthboxSpriteId).x + 16;
          sprite.y = _gs(rt, healthboxSpriteId).y;
          break;
      case 1:
          sprite.x = _gs(rt, healthboxSpriteId).x + 16;
          sprite.y = _gs(rt, healthboxSpriteId).y;
          break;
      case 2:
      default:
          sprite.x = _gs(rt, healthboxSpriteId).x + 8;
          sprite.y = _gs(rt, healthboxSpriteId).y;
          break;
      }

      sprite.x2 = _gs(rt, healthboxSpriteId).x2;
      sprite.y2 = _gs(rt, healthboxSpriteId).y2;
};

/** Source: battle_interface.c → SpriteCB_HealthBoxOther */
export const SpriteCB_HealthBoxOther: SpriteCallback = (sprite, rt) => {
  let healthboxMainSpriteId = sprite.hOther_HealthBoxSpriteId;

      sprite.x = _gs(rt, healthboxMainSpriteId).x + 64;
      sprite.y = _gs(rt, healthboxMainSpriteId).y;

      sprite.x2 = _gs(rt, healthboxMainSpriteId).x2;
      sprite.y2 = _gs(rt, healthboxMainSpriteId).y2;
};

/** Source: battle_interface.c → SpriteCB_StatusSummaryBar_Enter */
export const SpriteCB_StatusSummaryBar_Enter: SpriteCallback = (sprite, rt) => {
  if (sprite.x2 != 0)
          sprite.x2 += sprite.data[0];
};

/** Source: battle_interface.c → SpriteCB_StatusSummaryBar_Exit */
export const SpriteCB_StatusSummaryBar_Exit: SpriteCallback = (sprite, rt) => {
  sprite.data[1] += 32;
      if (sprite.data[0] > 0)
          sprite.x2 += sprite.data[1] >> 4;
      else
          sprite.x2 -= sprite.data[1] >> 4;
      sprite.data[1] &= 0xF;
};

/** Source: battle_interface.c → SpriteCB_StatusSummaryBalls_Enter */
export const SpriteCB_StatusSummaryBalls_Enter: SpriteCallback = (sprite, rt) => {
  let var1 = 0;
      let var2 = 0;
      let pan = 0;

      if (sprite.data[1] > 0)
      {
          sprite.data[1]--;
          return;
      }

      var1 = sprite.data[2];
      var2 = sprite.data[3];
      var2 += 56;
      sprite.data[3] = var2 & 0xFFF0;

      if (var1 != 0)
      {
          sprite.x2 += var2 >> 4;
          if (sprite.x2 > 0)
              sprite.x2 = 0;
      }
      else
      {
          sprite.x2 -= var2 >> 4;
          if (sprite.x2 < 0)
              sprite.x2 = 0;
      }

      if (sprite.x2 == 0)
      {
          pan = SOUND_PAN_TARGET;
          if (var1 != 0)
              pan = SOUND_PAN_ATTACKER;

          if (sprite.data[7] != 0)
              PlaySE2WithPanning(SE_BALL_TRAY_EXIT, pan);
          else
              PlaySE1WithPanning(SE_BALL_TRAY_BALL, pan);

          sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: battle_interface.c → SpriteCB_StatusSummaryBalls_Exit */
export const SpriteCB_StatusSummaryBalls_Exit: SpriteCallback = (sprite, rt) => {
  let var1 = 0;
      let var2 = 0;

      if (sprite.data[1] > 0)
      {
          sprite.data[1]--;
          return;
      }
      var1 = sprite.data[2];
      var2 = sprite.data[3];
      var2 += 56;
      sprite.data[3] = var2 & 0xFFF0;
      if (var1 != 0)
          sprite.x2 += var2 >> 4;
      else
          sprite.x2 -= var2 >> 4;
      if (sprite.x2 + sprite.x > 248
       || sprite.x2 + sprite.x < -8)
      {
          sprite.invisible = true;
          sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: battle_interface.c → SpriteCB_StatusSummaryBalls_OnSwitchout */
export const SpriteCB_StatusSummaryBalls_OnSwitchout: SpriteCallback = (sprite, rt) => {
  let barSpriteId = sprite.data[0];

      sprite.x2 = _gs(rt, barSpriteId).x2;
      sprite.y2 = _gs(rt, barSpriteId).y2;
};

/** Source: battle_interface.c → Task_HidePartyStatusSummary */
export const Task_HidePartyStatusSummary: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let ballIconSpriteIds: number[] = new Array(PARTY_SIZE).fill(0);
      let isBattleStart = 0;
      let summaryBarSpriteId = 0;
      let battler = 0;
      let i = 0;

      isBattleStart = task.data[10];
      summaryBarSpriteId = task.data[1];
      battler = task.data[0];

      for (i = 0; i < PARTY_SIZE; i++)
          ballIconSpriteIds[i] = task.tBallIconSpriteId(i);

      rt.SetGpuReg(REG_OFFSET_BLDCNT, BLDCNT_TGT2_ALL | BLDCNT_EFFECT_BLEND);
      rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(16, 0));

      task.data[15] = 16;

      for (i = 0; i < PARTY_SIZE; i++)
          rt.gba.oam[_gs(rt, ballIconSpriteIds[i]).oamIndex].objMode = ST_OAM_OBJ_BLEND;

      rt.gba.oam[_gs(rt, summaryBarSpriteId).oamIndex].objMode = ST_OAM_OBJ_BLEND;

      if (isBattleStart)
      {
          for (i = 0; i < PARTY_SIZE; i++)
          {
              if (GetBattlerSide(battler) != B_SIDE_PLAYER)
              {
                  _gs(rt, ballIconSpriteIds[PARTY_SIZE - 1 - i]).data[1] = 7 * i;
                  _gs(rt, ballIconSpriteIds[PARTY_SIZE - 1 - i]).data[3] = 0;
                  _gs(rt, ballIconSpriteIds[PARTY_SIZE - 1 - i]).data[4] = 0;
                  rt.setSpriteCallback(ballIconSpriteIds[PARTY_SIZE - 1 - i], SpriteCB_StatusSummaryBalls_Exit);
              }
              else
              {
                  _gs(rt, ballIconSpriteIds[i]).data[1] = 7 * i;
                  _gs(rt, ballIconSpriteIds[i]).data[3] = 0;
                  _gs(rt, ballIconSpriteIds[i]).data[4] = 0;
                  rt.setSpriteCallback(ballIconSpriteIds[i], SpriteCB_StatusSummaryBalls_Exit);
              }
          }
          _gs(rt, summaryBarSpriteId).data[0] /= 2;
          _gs(rt, summaryBarSpriteId).data[1] = 0;
          rt.setSpriteCallback(summaryBarSpriteId, SpriteCB_StatusSummaryBar_Exit);
          SetSubspriteTables(_gs(rt, summaryBarSpriteId), sStatusSummaryBar_SubspriteTable_Exit);
          task.func = (t) => Task_HidePartyStatusSummary_BattleStart_1(t, rt);
      }
      else
      {
          task.func = (t) => Task_HidePartyStatusSummary_DuringBattle(t, rt);
      }
};

/** Source: battle_interface.c → Task_HidePartyStatusSummary_BattleStart_1 */
export const Task_HidePartyStatusSummary_BattleStart_1: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if ((task.data[11]++ % 2) == 0)
      {
          if (--task.data[15] < 0)
              return;

          rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(task.data[15], 16 - task.data[15]));
      }
      if (task.data[15] == 0)
          task.func = (t) => Task_HidePartyStatusSummary_BattleStart_2(t, rt);
};

/** Source: battle_interface.c → Task_HidePartyStatusSummary_BattleStart_2 */
export const Task_HidePartyStatusSummary_BattleStart_2: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let ballIconSpriteIds: number[] = new Array(PARTY_SIZE).fill(0);
      let i = 0;

      let battler = task.data[0];
      if (--task.data[15] == -1)
      {
          let summaryBarSpriteId = task.data[1];

          for (i = 0; i < PARTY_SIZE; i++)
              ballIconSpriteIds[i] = task.tBallIconSpriteId(i);

          gBattleSpritesDataPtr.animationData.field_9_x1C--;
          if (gBattleSpritesDataPtr.animationData.field_9_x1C == 0)
          {
              rt.DestroySprite(summaryBarSpriteId);
              rt.DestroySprite(ballIconSpriteIds[0]);
          }
          else
          {
              FreeSpriteOamMatrix(_gs(rt, summaryBarSpriteId));
              rt.DestroySprite(summaryBarSpriteId);
              FreeSpriteOamMatrix(_gs(rt, ballIconSpriteIds[0]));
              rt.DestroySprite(ballIconSpriteIds[0]);
          }

          for (i = 1; i < PARTY_SIZE; i++)
              rt.DestroySprite(ballIconSpriteIds[i]);
      }
      else if (task.data[15] == -3)
      {
          gBattleSpritesDataPtr.healthBoxesData[battler].partyStatusSummaryShown = 0;
          rt.SetGpuReg(REG_OFFSET_BLDCNT, 0);
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, 0);
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_interface.c → Task_HidePartyStatusSummary_DuringBattle */
export const Task_HidePartyStatusSummary_DuringBattle: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let ballIconSpriteIds: number[] = new Array(PARTY_SIZE).fill(0);
      let i = 0;
      let battler = task.data[0];

      if (--task.data[15] >= 0)
      {
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, BLDALPHA_BLEND(task.data[15], 16 - task.data[15]));
      }
      else if (task.data[15] == -1)
      {
          let summaryBarSpriteId = task.data[1];

          for (i = 0; i < PARTY_SIZE; i++)
              ballIconSpriteIds[i] = task.tBallIconSpriteId(i);

          rt.DestroySprite(summaryBarSpriteId);
          rt.DestroySprite(ballIconSpriteIds[0]);

          for (i = 1; i < PARTY_SIZE; i++)
              rt.DestroySprite(ballIconSpriteIds[i]);
      }
      else if (task.data[15] == -3)
      {
          gBattleSpritesDataPtr.healthBoxesData[battler].partyStatusSummaryShown = 0;
          rt.SetGpuReg(REG_OFFSET_BLDCNT, 0);
          rt.SetGpuReg(REG_OFFSET_BLDALPHA, 0);
          rt.DestroyTask(taskId);
      }
};
