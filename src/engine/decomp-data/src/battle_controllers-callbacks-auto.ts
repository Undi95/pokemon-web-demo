// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_controllers.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const B_COMM_CONTROLLER_IS_DONE = 2;
const B_COMM_TO_CONTROLLER = 0;
const B_COMM_TO_ENGINE = 1;
const BATTLE_TYPE_BATTLE_TOWER = 256;
const BATTLE_TYPE_IS_MASTER = 4;
const BATTLE_TYPE_MULTI = 64;
const LINK_BUFF_ABSENT_BATTLER_FLAGS = 6;
const LINK_BUFF_ACTIVE_BATTLER = 1;
const LINK_BUFF_ATTACKER = 2;
const LINK_BUFF_DATA = 8;
const LINK_BUFF_EFFECT_BATTLER = 7;
const LINK_BUFF_SIZE_HI = 5;
const LINK_BUFF_SIZE_LO = 4;
const LINK_BUFF_TARGET = 3;
const SENDTASK_STATE_BEGIN_SEND_BLOCK = 3;
const SENDTASK_STATE_COUNT_PLAYERS = 2;
const SENDTASK_STATE_FINISH_SEND_BLOCK = 4;
const SENDTASK_STATE_INITIAL_DELAY = 1;
const SENDTASK_STATE_INITIALIZE = 0;
const SENDTASK_STATE_UNUSED_STATE = 5;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const BYTE_TO_RECEIVE: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const BYTE_TO_SEND: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const IS_BATTLE_CONTROLLER_ACTIVE_ON_LOCAL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const MARK_BATTLE_CONTROLLER_IDLE_FOR_PLAYER: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_controllers.c → Task_HandleSendLinkBuffersData */
export const Task_HandleSendLinkBuffersData: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let numPlayers = 0;
      let blockSize = 0;
          gLinkBattleSendBuffer[task.data[15] + offset]

      switch (task.data[11])
      {
      case SENDTASK_STATE_INITIALIZE:
          task.data[10] = 100;
          task.data[11]++;
          break;
      case SENDTASK_STATE_INITIAL_DELAY:
          task.data[10]--;
          if (task.data[10] == 0)
              task.data[11]++;
          break;
      case SENDTASK_STATE_COUNT_PLAYERS:
          if (gWirelessCommType)
          {
              task.data[11]++;
          }
          else
          {
              if (gBattleTypeFlags & BATTLE_TYPE_BATTLE_TOWER)
                  numPlayers = 2;
              else
                  numPlayers = (gBattleTypeFlags & BATTLE_TYPE_MULTI) ? 4 : 2;

              if (GetLinkPlayerCount_2() >= numPlayers)
              {
                  if (IsLinkMaster())
                  {
                      CheckShouldAdvanceLinkState();
                      task.data[11]++;
                  }
                  else
                  {
                      task.data[11]++;
                  }
              }
          }
          break;
      case SENDTASK_STATE_BEGIN_SEND_BLOCK:
          if (task.data[15] != task.data[14])
          {
              if (task.data[13] == 0)
              {
                  if (task.data[15] >  task.data[14]
                   && task.data[15] == task.data[12])
                  {
                      task.data[12] = 0;
                      task.data[15]    = 0;
                  }
                  blockSize = (BYTE_TO_SEND(LINK_BUFF_SIZE_LO) | (BYTE_TO_SEND(LINK_BUFF_SIZE_HI) << 8)) + LINK_BUFF_DATA;
                  SendBlock(BitmaskAllOtherLinkPlayers(), BYTE_TO_SEND(0), blockSize);
                  task.data[11]++;
              }
              else
              {
                  task.data[13]--;
                  break;
              }
          }
          break;
      case SENDTASK_STATE_FINISH_SEND_BLOCK:
          if (IsLinkTaskFinished())
          {
              blockSize = BYTE_TO_SEND(LINK_BUFF_SIZE_LO) | (BYTE_TO_SEND(LINK_BUFF_SIZE_HI) << 8);
              task.data[13] = 1;
              task.data[15]  = task.data[15] + blockSize + LINK_BUFF_DATA;
              task.data[11] = SENDTASK_STATE_BEGIN_SEND_BLOCK;
          }
          break;
      case SENDTASK_STATE_UNUSED_STATE:
          if (--task.data[13] == 0)
          {
              task.data[13] = 1;
              task.data[11] = SENDTASK_STATE_BEGIN_SEND_BLOCK;
          }
          break;
      }

};

/** Source: battle_controllers.c → Task_HandleCopyReceivedLinkBuffersData */
export const Task_HandleCopyReceivedLinkBuffersData: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let blockSize = 0;
      let battler = 0;
      let playerId = 0;
          gLinkBattleRecvBuffer[task.data[15] + offset]

      if (task.data[15] != task.data[14])
      {
          if (task.data[15] >  task.data[14]
           && task.data[15] == task.data[12])
          {
              task.data[12] = 0;
              task.data[15]    = 0;
          }
          battler = BYTE_TO_RECEIVE(LINK_BUFF_ACTIVE_BATTLER);
          blockSize = BYTE_TO_RECEIVE(LINK_BUFF_SIZE_LO) | (BYTE_TO_RECEIVE(LINK_BUFF_SIZE_HI) << 8);

          switch (BYTE_TO_RECEIVE(0))
          {
          case B_COMM_TO_CONTROLLER:
              if (IS_BATTLE_CONTROLLER_ACTIVE_ON_LOCAL(battler))
                  return;

              memcpy(gBattleBufferA[battler], BYTE_TO_RECEIVE(LINK_BUFF_DATA), blockSize);
              MarkBattlerReceivedLinkData(battler);

              if (!(gBattleTypeFlags & BATTLE_TYPE_IS_MASTER))
              {
                  gBattlerAttacker    = BYTE_TO_RECEIVE(LINK_BUFF_ATTACKER);
                  gBattlerTarget      = BYTE_TO_RECEIVE(LINK_BUFF_TARGET);
                  gAbsentBattlerFlags = BYTE_TO_RECEIVE(LINK_BUFF_ABSENT_BATTLER_FLAGS);
                  gEffectBattler      = BYTE_TO_RECEIVE(LINK_BUFF_EFFECT_BATTLER);
              }
              break;
          case B_COMM_TO_ENGINE:
              memcpy(gBattleBufferB[battler], gLinkBattleRecvBuffer[task.data[15] + LINK_BUFF_DATA], blockSize);
              break;
          case B_COMM_CONTROLLER_IS_DONE:
              playerId = BYTE_TO_RECEIVE(LINK_BUFF_DATA);
              MARK_BATTLE_CONTROLLER_IDLE_FOR_PLAYER(battler, playerId);
              break;
          }

          task.data[15] = task.data[15] + blockSize + LINK_BUFF_DATA;
      }

};
