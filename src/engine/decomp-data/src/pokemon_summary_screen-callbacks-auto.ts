// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/pokemon_summary_screen.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  ClearWindowTilemap,
  JOY_NEW,
  PutWindowTilemap,
  m4aSongNumStart,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const AILMENT_NONE = 0;
const B_BUTTON = 2;
const DPAD_DOWN = 128;
const DPAD_LEFT = 32;
const DPAD_RIGHT = 16;
const DPAD_UP = 64;
const MAX_MON_MOVES = 4;
const MENU_L_PRESSED = 1;
const MENU_R_PRESSED = 2;
const MOVE_NONE = 0;
const PSS_LABEL_WINDOW_MOVES_APPEAL_JAM = 15;
const PSS_LABEL_WINDOW_MOVES_POWER_ACC = 14;
const PSS_LABEL_WINDOW_POKEMON_SKILLS_STATUS = 13;
const PSS_LABEL_WINDOW_PORTRAIT_SPECIES = 19;
const PSS_PAGE_BATTLE_MOVES = 2;
const PSS_PAGE_CONTEST_MOVES = 3;
const PSS_PAGE_INFO = 0;
const PSS_PAGE_SKILLS = 1;
const SE_FAILURE = 32;
const SE_SELECT = 5;
const SPRITE_ARR_ID_BALL = 1;
const SPRITE_ARR_ID_MON = 0;
const SPRITE_ARR_ID_MOVE_SELECTOR1 = 4;
const SPRITE_ARR_ID_STATUS = 2;
const SPRITE_NONE = 255;
const SUMMARY_MODE_SELECT_MOVE = 3;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: pokemon_summary_screen.c → SpriteCB_Pokemon */
export const SpriteCB_Pokemon: SpriteCallback = (sprite, rt) => {
  let summary: any = sMonSummaryScreen.summary;

      if (!rt.gPaletteFade.active && sprite.data[2] != 1)
      {
          sprite.data[1] = IsMonSpriteNotFlipped(sprite.data[0]);
          PlayMonCry();
          PokemonSummaryDoMonAnimation(sprite, sprite.data[0], summary.isEgg);
      }
};

/** Source: pokemon_summary_screen.c → SpriteCB_MoveSelector */
export const SpriteCB_MoveSelector: SpriteCallback = (sprite, rt) => {
  if (sprite.animNum > 3 && sprite.animNum < 7)
      {
          sprite.data[1] = (sprite.data[1] + 1) & 0x1;
          if (sprite.data[1] > 24)
              sprite.invisible = true;
          else
              sprite.invisible = false;
      }
      else
      {
          sprite.data[1] = 0;
          sprite.invisible = false;
      }

      if (sprite.data[0] == SPRITE_ARR_ID_MOVE_SELECTOR1)
          sprite.y2 = sMonSummaryScreen.firstMoveIndex * 16;
      else
          sprite.y2 = sMonSummaryScreen.secondMoveIndex * 16;
};

/** Source: pokemon_summary_screen.c → Task_HandleInput */
export const Task_HandleInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (MenuHelpers_ShouldWaitForLinkRecv() != true && !rt.gPaletteFade.active)
      {
          if (JOY_NEW(DPAD_UP))
          {
              ChangeSummaryPokemon(taskId, -1);
          }
          else if (JOY_NEW(DPAD_DOWN))
          {
              ChangeSummaryPokemon(taskId, 1);
          }
          else if ((JOY_NEW(DPAD_LEFT)) || GetLRKeysPressed() == MENU_L_PRESSED)
          {
              ChangePage(taskId, -1);
          }
          else if ((JOY_NEW(DPAD_RIGHT)) || GetLRKeysPressed() == MENU_R_PRESSED)
          {
              ChangePage(taskId, 1);
          }
          else if (JOY_NEW(A_BUTTON))
          {
              if (sMonSummaryScreen.currPageIndex != PSS_PAGE_SKILLS)
              {
                  if (sMonSummaryScreen.currPageIndex == PSS_PAGE_INFO)
                  {
                      StopPokemonAnimations();
                      PlaySE(SE_SELECT);
                      BeginCloseSummaryScreen(taskId);
                  }
                  else  
                  {
                      PlaySE(SE_SELECT);
                      SwitchToMoveSelection(taskId);
                  }
              }
          }
          else if (JOY_NEW(B_BUTTON))
          {
              StopPokemonAnimations();
              PlaySE(SE_SELECT);
              BeginCloseSummaryScreen(taskId);
          }
      }
};

/** Source: pokemon_summary_screen.c → Task_ChangeSummaryMon */
export const Task_ChangeSummaryMon: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 0:
          StopCryAndClearCrySongs();
          break;
      case 1:
          SummaryScreen_DestroyAnimDelayTask();
          rt.DestroySprite(sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_MON]);
          break;
      case 2:
          rt.DestroySprite(sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_BALL]);
          break;
      case 3:
          CopyMonToSummaryStruct(sMonSummaryScreen.currentMon);
          sMonSummaryScreen.switchCounter = 0;
          break;
      case 4:
          if (ExtractMonDataToSummaryStruct(sMonSummaryScreen.currentMon) == false)
              return;
          break;
      case 5:
          RemoveAndCreateMonMarkingsSprite(sMonSummaryScreen.currentMon);
          break;
      case 6:
          CreateCaughtBallSprite(sMonSummaryScreen.currentMon);
          break;
      case 7:
          if (sMonSummaryScreen.summary.ailment != AILMENT_NONE)
              PositionStatusSlidingWindow(10, -2);
          DrawPokerusCuredSymbol(sMonSummaryScreen.currentMon);
          data[1] = 0;
          break;
      case 8:
          sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_MON] = LoadMonGfxAndSprite(sMonSummaryScreen.currentMon, data[1]);
          if (sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_MON] == SPRITE_NONE)
              return;
          _gs(rt, sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_MON]).data[2] = 1;
          TryDrawExperienceProgressBar();
          data[1] = 0;
          break;
      case 9:
          SetTypeIcons();
          break;
      case 10:
          PrintMonInfo();
          break;
      case 11:
          PrintPageSpecificText(sMonSummaryScreen.currPageIndex);
          LimitEggSummaryPageDisplay();
          break;
      case 12:
          _gs(rt, sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_MON]).data[2] = 0;
          break;
      default:
          if (!MenuHelpers_ShouldWaitForLinkRecv() && !FuncIsActiveTask(Task_SlideStatusWindow))
          {
              data[0] = 0;
              task.func = (t) => Task_HandleInput(t, rt);
          }
          return;
      }
      data[0]++;
};

/** Source: pokemon_summary_screen.c → Task_HandleInput_MoveSelect */
export const Task_HandleInput_MoveSelect: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (MenuHelpers_ShouldWaitForLinkRecv() != true)
      {
          if (JOY_NEW(DPAD_UP))
          {
              data[0] = 4;
              ChangeSelectedMove(data, -1, sMonSummaryScreen.firstMoveIndex);
          }
          else if (JOY_NEW(DPAD_DOWN))
          {
              data[0] = 4;
              ChangeSelectedMove(data, 1, sMonSummaryScreen.firstMoveIndex);
          }
          else if (JOY_NEW(A_BUTTON))
          {
              if (sMonSummaryScreen.lockMovesFlag == true
               || (sMonSummaryScreen.newMove == MOVE_NONE && sMonSummaryScreen.firstMoveIndex == MAX_MON_MOVES))
              {
                  PlaySE(SE_SELECT);
                  CloseMoveSelectMode(taskId);
              }
              else if (HasMoreThanOneMove() == true)
              {
                  PlaySE(SE_SELECT);
                  SwitchToMovePositionSwitchMode(taskId);
              }
              else
              {
                  PlaySE(SE_FAILURE);
              }
          }
          else if (JOY_NEW(B_BUTTON))
          {
              PlaySE(SE_SELECT);
              CloseMoveSelectMode(taskId);
          }
      }
};

/** Source: pokemon_summary_screen.c → Task_HandleInput_MovePositionSwitch */
export const Task_HandleInput_MovePositionSwitch: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (MenuHelpers_ShouldWaitForLinkRecv() != true)
      {
          if (JOY_NEW(DPAD_UP))
          {
              data[0] = 3;
              ChangeSelectedMove(data[0], -1, sMonSummaryScreen.secondMoveIndex);
          }
          else if (JOY_NEW(DPAD_DOWN))
          {
              data[0] = 3;
              ChangeSelectedMove(data[0], 1, sMonSummaryScreen.secondMoveIndex);
          }
          else if (JOY_NEW(A_BUTTON))
          {
              if (sMonSummaryScreen.firstMoveIndex == sMonSummaryScreen.secondMoveIndex)
                  ExitMovePositionSwitchMode(taskId, false);
              else
                  ExitMovePositionSwitchMode(taskId, true);
          }
          else if (JOY_NEW(B_BUTTON))
          {
              ExitMovePositionSwitchMode(taskId, false);
          }
      }
};

/** Source: pokemon_summary_screen.c → Task_SetHandleReplaceMoveInput */
export const Task_SetHandleReplaceMoveInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  SetNewMoveTypeIcon();
      CreateMoveSelectorSprites(SPRITE_ARR_ID_MOVE_SELECTOR1);
      task.func = (t) => Task_HandleReplaceMoveInput(t, rt);
};

/** Source: pokemon_summary_screen.c → Task_HandleReplaceMoveInput */
export const Task_HandleReplaceMoveInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (MenuHelpers_ShouldWaitForLinkRecv() != true)
      {
          if (rt.gPaletteFade.active != true)
          {
              if (JOY_NEW(DPAD_UP))
              {
                  data[0] = 4;
                  ChangeSelectedMove(data, -1, sMonSummaryScreen.firstMoveIndex);
              }
              else if (JOY_NEW(DPAD_DOWN))
              {
                  data[0] = 4;
                  ChangeSelectedMove(data, 1, sMonSummaryScreen.firstMoveIndex);
              }
              else if (JOY_NEW(DPAD_LEFT) || GetLRKeysPressed() == MENU_L_PRESSED)
              {
                  ChangePage(taskId, -1);
              }
              else if (JOY_NEW(DPAD_RIGHT) || GetLRKeysPressed() == MENU_R_PRESSED)
              {
                  ChangePage(taskId, 1);
              }
              else if (JOY_NEW(A_BUTTON))
              {
                  if (CanReplaceMove() == true)
                  {
                      StopPokemonAnimations();
                      PlaySE(SE_SELECT);
                      sMoveSlotToReplace = sMonSummaryScreen.firstMoveIndex;
                      gSpecialVar_0x8005 = sMoveSlotToReplace;
                      BeginCloseSummaryScreen(taskId);
                  }
                  else
                  {
                      PlaySE(SE_FAILURE);
                      ShowCantForgetHMsWindow(taskId);
                  }
              }
              else if (JOY_NEW(B_BUTTON))
              {
                  StopPokemonAnimations();
                  PlaySE(SE_SELECT);
                  sMoveSlotToReplace = MAX_MON_MOVES;
                  gSpecialVar_0x8005 = MAX_MON_MOVES;
                  BeginCloseSummaryScreen(taskId);
              }
          }
      }
};

/** Source: pokemon_summary_screen.c → Task_HandleInputCantForgetHMsMoves */
export const Task_HandleInputCantForgetHMsMoves: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      let move = 0;
      if (FuncIsActiveTask(Task_SlidePowerAccWindow) != 1)
      {
          if (JOY_NEW(DPAD_UP))
          {
              data[1] = 1;
              data[0] = 4;
              ChangeSelectedMove(data[0], -1, sMonSummaryScreen.firstMoveIndex);
              data[1] = 0;
              task.func = (t) => Task_HandleReplaceMoveInput(t, rt);
          }
          else if (JOY_NEW(DPAD_DOWN))
          {
              data[1] = 1;
              data[0] = 4;
              ChangeSelectedMove(data[0], 1, sMonSummaryScreen.firstMoveIndex);
              data[1] = 0;
              task.func = (t) => Task_HandleReplaceMoveInput(t, rt);
          }
          else if (JOY_NEW(DPAD_LEFT) || GetLRKeysPressed() == MENU_L_PRESSED)
          {
              if (sMonSummaryScreen.currPageIndex != PSS_PAGE_BATTLE_MOVES)
              {
                  ClearWindowTilemap(PSS_LABEL_WINDOW_PORTRAIT_SPECIES);
                  if (!_gs(rt, sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_STATUS]).invisible)
                      ClearWindowTilemap(PSS_LABEL_WINDOW_POKEMON_SKILLS_STATUS);
                  move = sMonSummaryScreen.summary.moves[sMonSummaryScreen.firstMoveIndex];
                  task.func = (t) => Task_HandleReplaceMoveInput(t, rt);
                  ChangePage(taskId, -1);
                  PositionPowerAccSlidingWindow(9, -2);
                  PositionAppealJamSlidingWindow(9, -2, move);
              }
          }
          else if (JOY_NEW(DPAD_RIGHT) || GetLRKeysPressed() == MENU_R_PRESSED)
          {
              if (sMonSummaryScreen.currPageIndex != PSS_PAGE_CONTEST_MOVES)
              {
                  ClearWindowTilemap(PSS_LABEL_WINDOW_PORTRAIT_SPECIES);
                  if (!_gs(rt, sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_STATUS]).invisible)
                      ClearWindowTilemap(PSS_LABEL_WINDOW_POKEMON_SKILLS_STATUS);
                  move = sMonSummaryScreen.summary.moves[sMonSummaryScreen.firstMoveIndex];
                  task.func = (t) => Task_HandleReplaceMoveInput(t, rt);
                  ChangePage(taskId, 1);
                  PositionPowerAccSlidingWindow(9, -2);
                  PositionAppealJamSlidingWindow(9, -2, move);
              }
          }
          else if (JOY_NEW(A_BUTTON | B_BUTTON))
          {
              ClearWindowTilemap(PSS_LABEL_WINDOW_PORTRAIT_SPECIES);
              if (!_gs(rt, sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_STATUS]).invisible)
                  ClearWindowTilemap(PSS_LABEL_WINDOW_POKEMON_SKILLS_STATUS);
              move = sMonSummaryScreen.summary.moves[sMonSummaryScreen.firstMoveIndex];
              PrintMoveDetails(move);
              ScheduleBgCopyTilemapToVram(0);
              PositionPowerAccSlidingWindow(9, -3);
              PositionAppealJamSlidingWindow(9, -3, move);
              task.func = (t) => Task_HandleReplaceMoveInput(t, rt);
          }
      }
};

/** Source: pokemon_summary_screen.c → Task_SlidePowerAccWindow */
export const Task_SlidePowerAccWindow: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      data[1] += data[0];
      if (data[1] < 0)
      {
          data[1] = 0;
      }
      else if (data[1] > sPowerAccSlidingWindow.width)
      {
          data[1] = sPowerAccSlidingWindow.width;
      }
      CopyNColumnsToTilemap(sPowerAccSlidingWindow, sMonSummaryScreen.bgTilemapBuffers[PSS_PAGE_BATTLE_MOVES][0], data[1], true);
      if (data[1] <= 0 || data[1] >= sPowerAccSlidingWindow.width)
      {
          if (data[0] < 0)
          {
              if (sMonSummaryScreen.currPageIndex == PSS_PAGE_BATTLE_MOVES)
                  PutWindowTilemap(PSS_LABEL_WINDOW_MOVES_POWER_ACC);
          }
          else
          {
              if (!_gs(rt, sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_STATUS]).invisible)
                  PutWindowTilemap(PSS_LABEL_WINDOW_POKEMON_SKILLS_STATUS);
              PutWindowTilemap(PSS_LABEL_WINDOW_PORTRAIT_SPECIES);
          }
          ScheduleBgCopyTilemapToVram(0);
          rt.DestroyTask(taskId);
      }
      ScheduleBgCopyTilemapToVram(1);
      ScheduleBgCopyTilemapToVram(2);
};

/** Source: pokemon_summary_screen.c → Task_SlideAppealJamWindow */
export const Task_SlideAppealJamWindow: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      data[1] += data[0];
      if (data[1] < 0)
      {
          data[1] = 0;
      }
      else if (data[1] > sAppealJamSlidingWindow.width)
      {
          data[1] = sAppealJamSlidingWindow.width;
      }
      CopyNColumnsToTilemap(sAppealJamSlidingWindow, sMonSummaryScreen.bgTilemapBuffers[PSS_PAGE_CONTEST_MOVES][0], data[1], true);
      if (data[1] <= 0 || data[1] >= sAppealJamSlidingWindow.width)
      {
          if (data[0] < 0)
          {
              if (sMonSummaryScreen.currPageIndex == PSS_PAGE_CONTEST_MOVES && FuncIsActiveTask(PssScrollRight) == 0)
                  PutWindowTilemap(PSS_LABEL_WINDOW_MOVES_APPEAL_JAM);
              DrawContestMoveHearts(data[2]);
          }
          else
          {
              if (!_gs(rt, sMonSummaryScreen.spriteIds[SPRITE_ARR_ID_STATUS]).invisible)
              {
                  PutWindowTilemap(PSS_LABEL_WINDOW_POKEMON_SKILLS_STATUS);
              }
              PutWindowTilemap(PSS_LABEL_WINDOW_PORTRAIT_SPECIES);
          }
          ScheduleBgCopyTilemapToVram(0);
          rt.DestroyTask(taskId);
      }
      ScheduleBgCopyTilemapToVram(1);
      ScheduleBgCopyTilemapToVram(2);
};

/** Source: pokemon_summary_screen.c → Task_SlideStatusWindow */
export const Task_SlideStatusWindow: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      data[1] += data[0];
      if (data[1] < 0)
          data[1] = 0;
      else if (data[1] > sStatusSlidingWindow1.width)
          data[1] = sStatusSlidingWindow1.width;
      CopyNColumnsToTilemap(sStatusSlidingWindow1, sMonSummaryScreen.bgTilemapBuffers[PSS_PAGE_INFO][0], data[1], false);
      CopyNColumnsToTilemap(sStatusSlidingWindow2, sMonSummaryScreen.bgTilemapBuffers[PSS_PAGE_INFO][0], data[1], false);
      ScheduleBgCopyTilemapToVram(3);
      if (data[1] <= 0 || data[1] >= sStatusSlidingWindow1.width)
      {
          if (data[0] < 0)
          {
              CreateSetStatusSprite();
              PutWindowTilemap(PSS_LABEL_WINDOW_POKEMON_SKILLS_STATUS);
              ScheduleBgCopyTilemapToVram(0);
          }
          rt.DestroyTask(taskId);
      }
};

/** Source: pokemon_summary_screen.c → Task_PrintInfoPage */
export const Task_PrintInfoPage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      switch (data[0])
      {
      case 1:
          PrintMonOTName();
          break;
      case 2:
          PrintMonOTID();
          break;
      case 3:
          PrintMonAbilityName();
          break;
      case 4:
          PrintMonAbilityDescription();
          break;
      case 5:
          BufferMonTrainerMemo();
          break;
      case 6:
          PrintMonTrainerMemo();
          break;
      case 7:
          rt.DestroyTask(taskId);
          return;
      }
      data[0]++;
};

/** Source: pokemon_summary_screen.c → Task_PrintSkillsPage */
export const Task_PrintSkillsPage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 1:
          PrintHeldItemName();
          break;
      case 2:
          PrintRibbonCount();
          break;
      case 3:
          BufferLeftColumnStats();
          break;
      case 4:
          PrintLeftColumnStats();
          break;
      case 5:
          BufferRightColumnStats();
          break;
      case 6:
          PrintRightColumnStats();
          break;
      case 7:
          PrintExpPointsNextLevel();
          break;
      case 8:
          rt.DestroyTask(taskId);
          return;
      }
      data[0]++;
};

/** Source: pokemon_summary_screen.c → Task_PrintBattleMoves */
export const Task_PrintBattleMoves: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 1:
          PrintMoveNameAndPP(0);
          break;
      case 2:
          PrintMoveNameAndPP(1);
          break;
      case 3:
          PrintMoveNameAndPP(2);
          break;
      case 4:
          PrintMoveNameAndPP(3);
          break;
      case 5:
          if (sMonSummaryScreen.mode == SUMMARY_MODE_SELECT_MOVE)
              PrintNewMoveDetailsOrCancelText();
          break;
      case 6:
          if (sMonSummaryScreen.mode == SUMMARY_MODE_SELECT_MOVE)
          {
              if (sMonSummaryScreen.firstMoveIndex == MAX_MON_MOVES)
                  data[1] = sMonSummaryScreen.newMove;
              else
                  data[1] = sMonSummaryScreen.summary.moves[sMonSummaryScreen.firstMoveIndex];
          }
          break;
      case 7:
          if (sMonSummaryScreen.mode == SUMMARY_MODE_SELECT_MOVE)
          {
              if (sMonSummaryScreen.newMove != MOVE_NONE || sMonSummaryScreen.firstMoveIndex != MAX_MON_MOVES)
                  PrintMoveDetails(data[1]);
          }
          break;
      case 8:
          rt.DestroyTask(taskId);
          return;
      }
      data[0]++;
};

/** Source: pokemon_summary_screen.c → Task_PrintContestMoves */
export const Task_PrintContestMoves: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 1:
          PrintMoveNameAndPP(0);
          break;
      case 2:
          PrintMoveNameAndPP(1);
          break;
      case 3:
          PrintMoveNameAndPP(2);
          break;
      case 4:
          PrintMoveNameAndPP(3);
          break;
      case 5:
          if (sMonSummaryScreen.mode == SUMMARY_MODE_SELECT_MOVE)
              PrintNewMoveDetailsOrCancelText();
          break;
      case 6:
          if (sMonSummaryScreen.mode == SUMMARY_MODE_SELECT_MOVE)
          {
              if (sMonSummaryScreen.newMove != MOVE_NONE || sMonSummaryScreen.firstMoveIndex != MAX_MON_MOVES)
                  PrintContestMoveDescription(sMonSummaryScreen.firstMoveIndex);
          }
          break;
      case 7:
          rt.DestroyTask(taskId);
          return;
      }
      data[0]++;
};

/** Source: pokemon_summary_screen.c → CB2_InitSummaryScreen */
export const CB2_InitSummaryScreen: CB2Callback = (rt) => {
  while (MenuHelpers_ShouldWaitForLinkRecv() != true && LoadGraphics() != true && MenuHelpers_IsLinkActive() != true);
};
