// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/field_effect.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  InitSpriteAffineAnim,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const CRY_PRIORITY_NORMAL = 10;
const CRY_VOLUME_RS = 125;
const FLDEFF_ASH_LAUNCH = 50;
const FLDEFF_ASH_PUFF = 49;
const FLDEFF_FLY_IN = 32;
const FLDEFF_MOVE_DEOXYS_ROCK = 66;
const FLDEFF_NPCFLY_OUT = 30;
const FLDEFF_USE_FLY = 31;
const MAX_SPRITES = 64;
const PARTY_SIZE = 6;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: field_effect.c → SpriteCB_PokeballGlowEffect */
export const SpriteCB_PokeballGlowEffect: SpriteCallback = (sprite, rt) => {
  sPokeballGlowEffectFuncs[sprite.data[0]](sprite);
};

/** Source: field_effect.c → SpriteCB_PokeballGlow */
export const SpriteCB_PokeballGlow: SpriteCallback = (sprite, rt) => {
  if (_gs(rt, sprite.data[0]).data[0] > 4)
      {
          FieldEffectFreeGraphicsResources(sprite);
      }
};

/** Source: field_effect.c → SpriteCB_PokecenterMonitor */
export const SpriteCB_PokecenterMonitor: SpriteCallback = (sprite, rt) => {
  if (sprite.data[0] != 0)
      {
          sprite.data[0] = 0;
          sprite.invisible = false;
          rt.StartSpriteAnim(sprite.spriteId, 1);
      }
      if (sprite.animEnded)
      {
          FieldEffectFreeGraphicsResources(sprite);
      }
};

/** Source: field_effect.c → SpriteCB_HallOfFameMonitor */
export const SpriteCB_HallOfFameMonitor: SpriteCallback = (sprite, rt) => {
  if (_gt(rt, sprite.data[0]).data[15])
      {
          if (sprite.data[1] == 0 || (--sprite.data[1]) == 0)
          {
              sprite.data[1] = 16;
              sprite.invisible ^= 1;
          }
          sprite.data[2]++;
      }
      if (sprite.data[2] > 127)
      {
          FieldEffectFreeGraphicsResources(sprite);
      }
};

/** Source: field_effect.c → SpriteCB_AshLaunch */
export const SpriteCB_AshLaunch: SpriteCallback = (sprite, rt) => {
  if (sprite.animEnded)
          FieldEffectStop(sprite, FLDEFF_ASH_LAUNCH);
};

/** Source: field_effect.c → SpriteCB_AshPuff */
export const SpriteCB_AshPuff: SpriteCallback = (sprite, rt) => {
  if (sprite.animEnded)
          FieldEffectStop(sprite, FLDEFF_ASH_PUFF);
};

/** Source: field_effect.c → SpriteCB_FieldMoveMonSlideOnscreen */
export const SpriteCB_FieldMoveMonSlideOnscreen: SpriteCallback = (sprite, rt) => {
  if ((sprite.x -= 20) <= DISPLAY_WIDTH / 2)
      {sprite.x = Math.trunc(DISPLAY_WIDTH / 2);
          sprite.data[1] = 30;
          sprite.callback = SpriteCB_FieldMoveMonWaitAfterCry;
          if (sprite.data[6])
              PlayCry_NormalNoDucking(sprite.data[0], 0, CRY_VOLUME_RS, CRY_PRIORITY_NORMAL);
          else
              PlayCry_Normal(sprite.data[0], 0);
      }
};

/** Source: field_effect.c → SpriteCB_FieldMoveMonWaitAfterCry */
export const SpriteCB_FieldMoveMonWaitAfterCry: SpriteCallback = (sprite, rt) => {
  if ((--sprite.data[1]) == 0)
          sprite.callback = SpriteCB_FieldMoveMonSlideOffscreen;
};

/** Source: field_effect.c → SpriteCB_FieldMoveMonSlideOffscreen */
export const SpriteCB_FieldMoveMonSlideOffscreen: SpriteCallback = (sprite, rt) => {
  if (sprite.x < -64)
          sprite.data[7] = true;
      else
          sprite.x -= 20;
};

/** Source: field_effect.c → SpriteCB_NPCFlyOut */
export const SpriteCB_NPCFlyOut: SpriteCallback = (sprite, rt) => {
  let npcSprite: any = null;

      sprite.x2 = Cos(sprite.data[2], 0x8c);
      sprite.y2 = Sin(sprite.data[2], 0x48);
      sprite.data[2] = (sprite.data[2] + 4) & 0xff;
      if (sprite.data[0])
      {
          npcSprite = _gs(rt, sprite.data[1]);
          npcSprite.coordOffsetEnabled = false;
          npcSprite.x = sprite.x + sprite.x2;
          npcSprite.y = sprite.y + sprite.y2 - 8;
          npcSprite.x2 = 0;
          npcSprite.y2 = 0;
      }

      if (sprite.data[2] >= 0x80)
          FieldEffectStop(sprite, FLDEFF_NPCFLY_OUT);
};

/** Source: field_effect.c → SpriteCB_FlyBirdLeaveBall */
export const SpriteCB_FlyBirdLeaveBall: SpriteCallback = (sprite, rt) => {
  if (sprite.data[7] == false)
      {
          if (sprite.data[0] == 0)
          {
              rt.gba.oam[sprite.oamIndex].affineMode = ST_OAM_AFFINE_DOUBLE;
              sprite.affineAnimsTableName = sAffineAnims_FlyBird;
              InitSpriteAffineAnim(sprite);
              rt.StartSpriteAffineAnim(sprite.spriteId, 0);
              sprite.x = 0x76;
              sprite.y = -0x30;
              sprite.data[0]++;
              sprite.data[1] = 0x40;
              sprite.data[2] = 0x100;
          }
          sprite.data[1] += (sprite.data[2] >> 8);
          sprite.x2 = Cos(sprite.data[1], 0x78);
          sprite.y2 = Sin(sprite.data[1], 0x78);
          if (sprite.data[2] < 0x800)
          {
              sprite.data[2] += 0x60;
          }
          if (sprite.data[1] > 0x81)
          {
              sprite.data[7]++;
              rt.gba.oam[sprite.oamIndex].affineMode = ST_OAM_AFFINE_OFF;
              FreeOamMatrix(rt.gba.oam[sprite.oamIndex].affineParamIndex);
              (() => { const _ctcv = CalcCenterToCornerVec(0, 0, 0); sprite.centerToCornerVecX = _ctcv.centerToCornerVecX; sprite.centerToCornerVecY = _ctcv.centerToCornerVecY; })();
          }
      }
};

/** Source: field_effect.c → SpriteCB_FlyBirdSwoopDown */
export const SpriteCB_FlyBirdSwoopDown: SpriteCallback = (sprite, rt) => {
  sprite.x2 = Cos(sprite.data[2], 0x8c);
      sprite.y2 = Sin(sprite.data[2], 0x48);
      sprite.data[2] = (sprite.data[2] + 4) & 0xff;
      if (sprite.data[6] != MAX_SPRITES)
      {
          let sprite1: any = _gs(rt, sprite.data[6]);
          sprite1.coordOffsetEnabled = false;
          sprite1.x = sprite.x + sprite.x2;
          sprite1.y = sprite.y + sprite.y2 - 8;
          sprite1.x2 = 0;
          sprite1.y2 = 0;
      }
      if (sprite.data[2] >= 0x80)
      {
          sprite.data[7] = true;
      }
};

/** Source: field_effect.c → SpriteCB_FlyBirdReturnToBall */
export const SpriteCB_FlyBirdReturnToBall: SpriteCallback = (sprite, rt) => {
  if (sprite.data[7] == false)
      {
          if (sprite.data[0] == 0)
          {
              rt.gba.oam[sprite.oamIndex].affineMode = ST_OAM_AFFINE_DOUBLE;
              sprite.affineAnimsTableName = sAffineAnims_FlyBird;
              InitSpriteAffineAnim(sprite);
              rt.StartSpriteAffineAnim(sprite.spriteId, 1);
              sprite.x = 0x5e;
              sprite.y = -0x20;
              sprite.data[0]++;
              sprite.data[1] = 0xf0;
              sprite.data[2] = 0x800;
              sprite.data[4] = 0x80;
          }
          sprite.data[1] += sprite.data[2] >> 8;
          sprite.data[3] += sprite.data[2] >> 8;
          sprite.data[1] &= 0xff;
          sprite.x2 = Cos(sprite.data[1], 0x20);
          sprite.y2 = Sin(sprite.data[1], 0x78);
          if (sprite.data[2] > 0x100)
          {
              sprite.data[2] -= sprite.data[4];
          }
          if (sprite.data[4] < 0x100)
          {
              sprite.data[4] += 24;
          }
          if (sprite.data[2] < 0x100)
          {
              sprite.data[2] = 0x100;
          }
          if (sprite.data[3] >= 60)
          {
              sprite.data[7]++;
              rt.gba.oam[sprite.oamIndex].affineMode = ST_OAM_AFFINE_OFF;
              FreeOamMatrix(rt.gba.oam[sprite.oamIndex].affineParamIndex);
              sprite.invisible = true;
          }
      }
};

/** Source: field_effect.c → SpriteCB_DeoxysRockFragment */
export const SpriteCB_DeoxysRockFragment: SpriteCallback = (sprite, rt) => {
  switch (sprite.data[0])
      {
      case 0:
          sprite.x -= 16;
          sprite.y -= 12;
          break;
      case 1:
          sprite.x += 16;
          sprite.y -= 12;
          break;
      case 2:
          sprite.x -= 16;
          sprite.y += 12;
          break;
      case 3:
          sprite.x += 16;
          sprite.y += 12;
          break;
      }
      if (sprite.x < -4 || sprite.x > DISPLAY_WIDTH + 4 || sprite.y < -4 || sprite.y > DISPLAY_HEIGHT + 4)
          rt.DestroySprite(sprite.spriteId);
};

/** Source: field_effect.c → Task_PokecenterHeal */
export const Task_PokecenterHeal: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      /* task already param */
      sPokecenterHealEffectFuncs[task.data[0]](task);
};

/** Source: field_effect.c → Task_HallOfFameRecord */
export const Task_HallOfFameRecord: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      /* task already param */
      sHallOfFameRecordEffectFuncs[task.data[0]](task);
};

/** Source: field_effect.c → Task_UseFly */
export const Task_UseFly: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      /* task already param */
      if (!task.data[0])
      {
          if (!IsWeatherNotFadingIn())
              return;

          gFieldEffectArguments[0] = GetCursorSelectionMonId();
          if (gFieldEffectArguments[0] > PARTY_SIZE - 1)
              gFieldEffectArguments[0] = 0;

          FieldEffectStart(FLDEFF_USE_FLY);
          task.data[0]++;
      }
      if (!FieldEffectActiveListContains(FLDEFF_USE_FLY))
      {
          Overworld_ResetStateAfterFly();
          WarpIntoMap();
          /* TODO scene transition: SetMainCallback2(CB2_LoadMap) */;
          gFieldCallback = FieldCallback_FlyIntoMap;
          rt.DestroyTask(taskId);
      }
};

/** Source: field_effect.c → Task_FlyIntoMap */
export const Task_FlyIntoMap: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      /* task already param */
      if (task.data[0] == 0)
      {
          if (rt.gPaletteFade.active)
          {
              return;
          }
          FieldEffectStart(FLDEFF_FLY_IN);
          task.data[0]++;
      }
      if (!FieldEffectActiveListContains(FLDEFF_FLY_IN))
      {
          UnlockPlayerFieldControls();
          UnfreezeObjectEvents();
          rt.DestroyTask(taskId);
      }
};

/** Source: field_effect.c → Task_FallWarpFieldEffect */
export const Task_FallWarpFieldEffect: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      /* task already param */
      while (sFallWarpFieldEffectFuncs[task.data[0]](task));
};

/** Source: field_effect.c → Task_EscalatorWarpOut */
export const Task_EscalatorWarpOut: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      /* task already param */
      while (sEscalatorWarpOutFieldEffectFuncs[task.data[0]](task));
};

/** Source: field_effect.c → Task_EscalatorWarpIn */
export const Task_EscalatorWarpIn: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  /* task already param */
      /* task already param */
      while (sEscalatorWarpInFieldEffectFuncs[task.data[0]](task));
};

/** Source: field_effect.c → Task_UseWaterfall */
export const Task_UseWaterfall: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sWaterfallFieldEffectFuncs[task.data[0]](_gt(rt, taskId), gObjectEvents[gPlayerAvatar.objectEventId]));
};

/** Source: field_effect.c → Task_UseDive */
export const Task_UseDive: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sDiveFieldEffectFuncs[task.data[0]](_gt(rt, taskId)));
};

/** Source: field_effect.c → Task_LavaridgeGymB1FWarp */
export const Task_LavaridgeGymB1FWarp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sLavaridgeGymB1FWarpEffectFuncs[task.data[0]](_gt(rt, taskId), gObjectEvents[gPlayerAvatar.objectEventId], _gs(rt, gPlayerAvatar.spriteId)));
};

/** Source: field_effect.c → Task_LavaridgeGymB1FWarpExit */
export const Task_LavaridgeGymB1FWarpExit: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while (sLavaridgeGymB1FWarpExitEffectFuncs[task.data[0]](_gt(rt, taskId), gObjectEvents[gPlayerAvatar.objectEventId], _gs(rt, gPlayerAvatar.spriteId)));
};

/** Source: field_effect.c → Task_LavaridgeGym1FWarp */
export const Task_LavaridgeGym1FWarp: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  while(sLavaridgeGym1FWarpEffectFuncs[task.data[0]](_gt(rt, taskId), gObjectEvents[gPlayerAvatar.objectEventId], _gs(rt, gPlayerAvatar.spriteId)));
};

/** Source: field_effect.c → Task_EscapeRopeWarpOut */
export const Task_EscapeRopeWarpOut: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sEscapeRopeWarpOutEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_EscapeRopeWarpIn */
export const Task_EscapeRopeWarpIn: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sEscapeRopeWarpInEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_TeleportWarpOut */
export const Task_TeleportWarpOut: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sTeleportWarpOutFieldEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_TeleportWarpIn */
export const Task_TeleportWarpIn: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sTeleportWarpInFieldEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_FieldMoveShowMonOutdoors */
export const Task_FieldMoveShowMonOutdoors: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sFieldMoveShowMonOutdoorsEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_FieldMoveShowMonIndoors */
export const Task_FieldMoveShowMonIndoors: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sFieldMoveShowMonIndoorsEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_SurfFieldEffect */
export const Task_SurfFieldEffect: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sSurfFieldEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_FlyOut */
export const Task_FlyOut: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sFlyOutFieldEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_FlyIn */
export const Task_FlyIn: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  sFlyInFieldEffectFuncs[task.data[0]](_gt(rt, taskId));
};

/** Source: field_effect.c → Task_DeoxysRockCameraShake */
export const Task_DeoxysRockCameraShake: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      if (data[7])
      {
          if (++data[6] > 20)
          {
              data[6] = 0;
              if (data[5] != 0)
                  data[5]--;
          }
      }
      else
      {
          data[5] = 4;
      }

      if (++data[0] > 1)
      {
          data[0] = 0;

          if (++data[1] & 1)
              SetCameraPanning(0, -data[5]);
          else
              SetCameraPanning(0, data[5]);
      }
      UpdateCameraPanning();
      if (data[5] == 0)
          rt.DestroyTask(taskId);
};

/** Source: field_effect.c → Task_DestroyDeoxysRock */
export const Task_DestroyDeoxysRock: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      InstallCameraPanAheadCallback();
      SetCameraPanningCallback(0);
      sDestroyDeoxysRockEffectFuncs[data[0]](data, taskId);
};

/** Source: field_effect.c → Task_MoveDeoxysRock */
export const Task_MoveDeoxysRock: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      let sprite: any = _gs(rt, data[1]);
      switch (data[0])
      {
          case 0:
              data[4] = sprite.x << 4;
              data[5] = sprite.y << 4;
              data[6] = ((data[8]) === 0 ? 0 : ((data[2] * 16 - data[4]) / (data[8])) | 0);
              data[7] = ((data[8]) === 0 ? 0 : ((data[3] * 16 - data[5]) / (data[8])) | 0);
              data[0]++;
               
          case 1:
              if (data[8] != 0)
              {
                  data[8]--;
                  data[4] += data[6];
                  data[5] += data[7];
                  sprite.x = data[4] >> 4;
                  sprite.y = data[5] >> 4;
              }
              else
              {
                  let object: any = gObjectEvents[data[9]];
                  sprite.x = data[2];
                  sprite.y = data[3];
                  ShiftStillObjectEventCoords(object);
                  object.triggerGroundEffectsOnStop = true;
                  FieldEffectActiveListRemove(FLDEFF_MOVE_DEOXYS_ROCK);
                  rt.DestroyTask(taskId);
              }
              break;
      }
};
