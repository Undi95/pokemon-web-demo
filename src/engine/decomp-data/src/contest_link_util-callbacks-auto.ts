// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/contest_link_util.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const CONTEST_CATEGORY_BEAUTY = 1;
const CONTEST_CATEGORY_COOL = 0;
const CONTEST_CATEGORY_CUTE = 2;
const CONTEST_CATEGORY_SMART = 3;
const CONTEST_CATEGORY_TOUGH = 4;
const CONTESTANT_COUNT = 4;
const FLAG_SYS_GAME_CLEAR = 2148;
const MON_DATA_BEAUTY_RIBBON = 51;
const MON_DATA_COOL_RIBBON = 50;
const MON_DATA_CUTE_RIBBON = 52;
const MON_DATA_SMART_RIBBON = 53;
const MON_DATA_TOUGH_RIBBON = 54;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: contest_link_util.c → Task_LinkContest_StartCommunicationEm */
export const Task_LinkContest_StartCommunicationEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let gameCleared = 0;

      switch (task.data[9])
      {
      case CONTEST_CATEGORY_COOL:
          gHighestRibbonRank = GetMonData(gPlayerParty[gContestMonPartyIndex], MON_DATA_COOL_RIBBON);
          break;
      case CONTEST_CATEGORY_BEAUTY:
          gHighestRibbonRank = GetMonData(gPlayerParty[gContestMonPartyIndex], MON_DATA_BEAUTY_RIBBON);
          break;
      case CONTEST_CATEGORY_CUTE:
          gHighestRibbonRank = GetMonData(gPlayerParty[gContestMonPartyIndex], MON_DATA_CUTE_RIBBON);
          break;
      case CONTEST_CATEGORY_SMART:
          gHighestRibbonRank = GetMonData(gPlayerParty[gContestMonPartyIndex], MON_DATA_SMART_RIBBON);
          break;
      case CONTEST_CATEGORY_TOUGH:
      default:
          gHighestRibbonRank = GetMonData(gPlayerParty[gContestMonPartyIndex], MON_DATA_TOUGH_RIBBON);
          break;
      }

      gContestMons[gContestPlayerMonIndex].highestRank = gHighestRibbonRank;
      gameCleared = FlagGet(FLAG_SYS_GAME_CLEAR) > 0;
      gContestMons[gContestPlayerMonIndex].gameCleared = gameCleared;
      SetTaskFuncWithFollowupFunc(taskId, Task_LinkContest_CommunicateMonsEm, Task_LinkContest_StartCommunicateRngEm);
};

/** Source: contest_link_util.c → Task_LinkContest_StartCommunicateRngEm */
export const Task_LinkContest_StartCommunicateRngEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  SetTaskFuncWithFollowupFunc(taskId, Task_LinkContest_CommunicateRngEm, Task_LinkContest_StartCommunicateLeaderIdsEm);
};

/** Source: contest_link_util.c → Task_LinkContest_StartCommunicateLeaderIdsEm */
export const Task_LinkContest_StartCommunicateLeaderIdsEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  SetTaskFuncWithFollowupFunc(taskId, Task_LinkContest_CommunicateLeaderIdsEm, Task_LinkContest_StartCommunicateCategoryEm);
};

/** Source: contest_link_util.c → Task_LinkContest_StartCommunicateCategoryEm */
export const Task_LinkContest_StartCommunicateCategoryEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  SetTaskFuncWithFollowupFunc(taskId, Task_LinkContest_CommunicateCategoryEm, Task_LinkContest_SetUpContestEm);
};

/** Source: contest_link_util.c → Task_LinkContest_SetUpContestEm */
export const Task_LinkContest_SetUpContestEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;
      let rank = 0;
      let gameCleared = 0;
      let categories: number[] = new Array(CONTESTANT_COUNT).fill(0);
      let leaderIds: number[] = new Array(CONTESTANT_COUNT).fill(0);

      memset(categories, 0, ((categories)?.length ?? 32));
      memset(leaderIds, 0, ((leaderIds)?.length ?? 32));

      for (i = 0; i < gNumLinkContestPlayers; i++)
          categories[i] = task.data[i + 1];

       
      for (i = 0; i < gNumLinkContestPlayers && categories[0] == categories[i]; i++)
          ;

      if (i == gNumLinkContestPlayers)
          gSpecialVar_0x8004 = false;  
      else
          gSpecialVar_0x8004 = true;  

      for (i = 0; i < gNumLinkContestPlayers; i++)
          leaderIds[i] = task.data[i + 5];

       
      if (gNumLinkContestPlayers != CONTESTANT_COUNT && GetMultiplayerId() == 0)
      {
          rank = gContestMons[0].highestRank;
          for (i = 1; i < gNumLinkContestPlayers; i++)
          {
              if (rank < gContestMons[i].highestRank)
                  rank = gContestMons[i].highestRank;
          }

          if (rank)
              rank--;

          gameCleared = true;
          for (i = 0; i < gNumLinkContestPlayers; i++)
          {
              if (!gContestMons[i].gameCleared)
              {
                  gameCleared = false;
                  break;
              }
          }

          SetLinkAIContestants(categories[0], rank, gameCleared);
      }

       
      gContestLinkLeaderIndex = LinkContest_GetLeaderIndex(leaderIds);

      if (gNumLinkContestPlayers < CONTESTANT_COUNT)
          SetTaskFuncWithFollowupFunc(taskId, Task_LinkContest_CommunicateAIMonsEm, Task_LinkContest_CalculateRound1Em);
      else
          task.func = (t) => Task_LinkContest_CalculateRound1Em(t, rt);
};

/** Source: contest_link_util.c → Task_LinkContest_CalculateRound1Em */
export const Task_LinkContest_CalculateRound1Em: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  CalculateRound1Points(gSpecialVar_ContestCategory);
      SetTaskFuncWithFollowupFunc(taskId, Task_LinkContest_CommunicateRound1Points, Task_LinkContest_CalculateTurnOrderEm);
};

/** Source: contest_link_util.c → Task_LinkContest_CalculateTurnOrderEm */
export const Task_LinkContest_CalculateTurnOrderEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  SortContestants(false);
      SetTaskFuncWithFollowupFunc(taskId, Task_LinkContest_CommunicateTurnOrder, Task_LinkContest_FinalizeConnection);
};

/** Source: contest_link_util.c → Task_LinkContest_CommunicateMonsEm */
export const Task_LinkContest_CommunicateMonsEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      default:
          task.data[0] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      case 0:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestMons[gContestPlayerMonIndex], 0) == 1)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              for (i = 0; i < gNumLinkContestPlayers; i++)
              {
                  memcpy(gContestMons[i], gBlockRecvBuffer[i], 0);
                  StripPlayerAndMonNamesForLinkContest(gContestMons[i], gLinkPlayers[i].language);
              }

              task.data[0]++;
          }
          break;
      }
};

/** Source: contest_link_util.c → Task_LinkContest_CommunicateRngEm */
export const Task_LinkContest_CommunicateRngEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      default:
          task.data[0] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      case 0:
          if (GetMultiplayerId() == 0)
          {
               
              if (!IsLinkTaskFinished())
                  return;

              if (LinkContest_SendBlock(gRngValue, ((gRngValue)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          else
          {
               
              task.data[0]++;
          }
          break;
      case 1:
           
          if (LinkContest_GetBlockReceived(0))
          {
              memcpy(gRngValue, gBlockRecvBuffer[0], ((gRngValue)?.length ?? 32));
              memcpy(gContestRngValue, gBlockRecvBuffer[0], ((gContestRngValue)?.length ?? 32));
              task.data[0]++;
          }
          break;
      }
};

/** Source: contest_link_util.c → Task_LinkContest_CommunicateLeaderIdsEm */
export const Task_LinkContest_CommunicateLeaderIdsEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;
      let data: number[] = new Array(CONTESTANT_COUNT).fill(0);
      let leaderId = 0;

      if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      default:
          task.data[0] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      case 0:
          if (IsLinkTaskFinished())
          {
              leaderId = 0x6E;
              if (LinkContest_SendBlock(leaderId, ((leaderId)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              for (i = 0; i < gNumLinkContestPlayers; i++)
              {
                  data[i] = gBlockRecvBuffer[i][0];
                  task.data[i + 5] = data[i];
              }

              task.data[0]++;
          }
          break;
      }
};

/** Source: contest_link_util.c → Task_LinkContest_CommunicateCategoryEm */
export const Task_LinkContest_CommunicateCategoryEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;
      let data: number[] = new Array(CONTESTANT_COUNT).fill(0);
      let category = 0;

      if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      default:
          task.data[0] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      case 0:
          if (IsLinkTaskFinished())
          {
              category = task.data[9];
              if (LinkContest_SendBlock(category, ((category)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              for (i = 0; i < gNumLinkContestPlayers; i++)
              {
                  data[i] = gBlockRecvBuffer[i][0];
                  task.data[i + 1] = data[i];
              }

              task.data[0]++;
          }
          break;
      }
};

/** Source: contest_link_util.c → Task_LinkContest_CommunicateAIMonsEm */
export const Task_LinkContest_CommunicateAIMonsEm: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      default:
          task.data[0] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      case 0:
          if (GetMultiplayerId() == 0)
          {
              if (!IsLinkTaskFinished())
                  return;

              if (LinkContest_SendBlock(gContestMons[gNumLinkContestPlayers], (CONTESTANT_COUNT - gNumLinkContestPlayers) * 0) == 1)
                  task.data[0]++;
          }
          else
          {
              task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceived(0))
          {
              memcpy(gContestMons[gNumLinkContestPlayers], gBlockRecvBuffer[0], (CONTESTANT_COUNT - gNumLinkContestPlayers) * 0);
              for (i = gNumLinkContestPlayers; i < CONTESTANT_COUNT; i++)
                  StripPlayerAndMonNamesForLinkContest(gContestMons[i], gLinkPlayers[0].language);

              task.data[0]++;
          }
          break;
      }
};
