// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/item_use.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  JOY_NEW,
  StringExpandPlaceholders,
  gStringVar4,
  m4aSongNumStart,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const B_BUTTON = 2;
const DIR_NONE = 0;
const FONT_NORMAL = 1;
const LOCALID_PLAYER = 255;
const PBLOCK_CASE_FIELD = 0;
const PYRAMID_LOCATION_NONE = 0;
const SE_GLASS_FLUTE = 117;
const SE_ITEMFINDER = 72;
const SE_REPEL = 47;
const SE_USE_ITEM = 1;
const VAR_REPEL_STEP_COUNT = 16417;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: item_use.c → Task_CallItemUseOnFieldCallback */
export const Task_CallItemUseOnFieldCallback: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (IsWeatherNotFadingIn() == 1)
          sItemUseOnFieldCB(taskId);
};

/** Source: item_use.c → Task_CloseCantUseKeyItemMessage */
export const Task_CloseCantUseKeyItemMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  ClearDialogWindowAndFrame(0, true);
      rt.DestroyTask(taskId);
      ScriptUnfreezeObjectEvents();
      UnlockPlayerFieldControls();
};

/** Source: item_use.c → Task_UseItemfinder */
export const Task_UseItemfinder: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let playerDir = 0;
      let playerDirToItem = 0;
      let i = 0;
      const data = task.data;
      if (data[3] == 0)
      {
          if (data[4] == 4)
          {
              playerDirToItem = GetDirectionToHiddenItem(data[0], data[1]);
              if (playerDirToItem != DIR_NONE)
              {
                  PlayerFaceHiddenItem(sClockwiseDirections[playerDirToItem - 1]);
                  task.func = (t) => Task_HiddenItemNearby(t, rt);
              }
              else
              {
                   
                  playerDir = GetPlayerFacingDirection();
                  for (i = 0; i < ((sClockwiseDirections)?.length ?? 0); i++)
                  {
                      if (playerDir == sClockwiseDirections[i])
                          data[5] = (i + 1) & 3;
                  }
                  task.func = (t) => Task_StandingOnHiddenItem(t, rt);
                  data[3] = 0;
                  data[2] = 0;
              }
              return;
          }
          PlaySE(SE_ITEMFINDER);
          data[4]++;
      }
      data[3] = (data[3] + 1) & 0x1;
};

/** Source: item_use.c → Task_CloseItemfinderMessage */
export const Task_CloseItemfinderMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  ClearDialogWindowAndFrame(0, true);
      ScriptUnfreezeObjectEvents();
      UnlockPlayerFieldControls();
      rt.DestroyTask(taskId);
};

/** Source: item_use.c → Task_HiddenItemNearby */
export const Task_HiddenItemNearby: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (ObjectEventCheckHeldMovementStatus(gObjectEvents[GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0)]) == true)
          DisplayItemMessageOnField(taskId, gText_ItemFinderNearby, Task_CloseItemfinderMessage);
};

/** Source: item_use.c → Task_StandingOnHiddenItem */
export const Task_StandingOnHiddenItem: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (ObjectEventCheckHeldMovementStatus(gObjectEvents[GetObjectEventIdByLocalIdAndMap(LOCALID_PLAYER, 0, 0)]) == true
      || data[2] == false)
      {
           
          PlayerFaceHiddenItem(sClockwiseDirections[data[5]]);
          data[2] = true;
          data[5] = (data[5] + 1) & 3;
          data[3]++;

          if (data[3] == 4)
              DisplayItemMessageOnField(taskId, gText_ItemFinderOnTop, Task_CloseItemfinderMessage);
      }
};

/** Source: item_use.c → Task_OpenRegisteredPokeblockCase */
export const Task_OpenRegisteredPokeblockCase: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          CleanupOverworldWindowsAndTilemaps();
          OpenPokeblockCase(PBLOCK_CASE_FIELD, CB2_ReturnToField);
          rt.DestroyTask(taskId);
      }
};

/** Source: item_use.c → Task_ShowTMHMContainedMessage */
export const Task_ShowTMHMContainedMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (JOY_NEW(A_BUTTON | B_BUTTON))
      {
          StringCopy(gStringVar1, gMoveNames[ItemIdToBattleMoveId(gSpecialVar_ItemId)]);
          StringExpandPlaceholders(gStringVar4, gText_TMHMContainedVar1);
          DisplayItemMessage(taskId, FONT_NORMAL, gStringVar4, UseTMHMYesNo);
      }
};

/** Source: item_use.c → Task_StartUseRepel */
export const Task_StartUseRepel: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (++data[8] > 7)
      {
          data[8] = 0;
          PlaySE(SE_REPEL);
          task.func = (t) => Task_UseRepel(t, rt);
      }
};

/** Source: item_use.c → Task_UseRepel */
export const Task_UseRepel: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!IsSEPlaying())
      {
          VarSet(VAR_REPEL_STEP_COUNT, GetItemHoldEffectParam(gSpecialVar_ItemId));
          RemoveUsedItem();
          if (CurrentBattlePyramidLocation() == PYRAMID_LOCATION_NONE)
              DisplayItemMessage(taskId, FONT_NORMAL, gStringVar4, CloseItemMessage);
          else
              DisplayItemMessageInBattlePyramid(taskId, gStringVar4, Task_CloseBattlePyramidBagMessage);
      }
};

/** Source: item_use.c → Task_UsedBlackWhiteFlute */
export const Task_UsedBlackWhiteFlute: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if(++task.data[8] > 7)
      {
          PlaySE(SE_GLASS_FLUTE);
          if (CurrentBattlePyramidLocation() == PYRAMID_LOCATION_NONE)
              DisplayItemMessage(taskId, FONT_NORMAL, gStringVar4, CloseItemMessage);
          else
              DisplayItemMessageInBattlePyramid(taskId, gStringVar4, Task_CloseBattlePyramidBagMessage);
      }
};

/** Source: item_use.c → Task_UseDigEscapeRopeOnField */
export const Task_UseDigEscapeRopeOnField: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  ResetInitialPlayerAvatarState();
      StartEscapeRopeFieldEffect();
      rt.DestroyTask(taskId);
};

/** Source: item_use.c → Task_CloseStatIncreaseMessage */
export const Task_CloseStatIncreaseMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (JOY_NEW(A_BUTTON | B_BUTTON))
      {
          if (CurrentBattlePyramidLocation() == PYRAMID_LOCATION_NONE)
              Task_FadeAndCloseBagMenu(taskId);
          else
              CloseBattlePyramidBag(taskId);
      }
};

/** Source: item_use.c → Task_UseStatIncreaseItem */
export const Task_UseStatIncreaseItem: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if(++task.data[8] > 7)
      {
          PlaySE(SE_USE_ITEM);
          RemoveBagItem(gSpecialVar_ItemId, 1);
          if (CurrentBattlePyramidLocation() == PYRAMID_LOCATION_NONE)
              DisplayItemMessage(taskId, FONT_NORMAL, UseStatIncreaseItem(gSpecialVar_ItemId), Task_CloseStatIncreaseMessage);
          else
              DisplayItemMessageInBattlePyramid(taskId, UseStatIncreaseItem(gSpecialVar_ItemId), Task_CloseStatIncreaseMessage);
      }
};

/** Source: item_use.c → CB2_CheckMail */
export const CB2_CheckMail: CB2Callback = (rt) => {
  let mail: any = null;
      mail.itemId = gSpecialVar_ItemId;
      ReadMail(mail, CB2_ReturnToBagMenuPocket, false);
};

/** Source: item_use.c → CB2_OpenPokeblockFromBag */
export const CB2_OpenPokeblockFromBag: CB2Callback = (rt) => {
  OpenPokeblockCase(PBLOCK_CASE_FIELD, CB2_ReturnToBagMenuPocket);
};
