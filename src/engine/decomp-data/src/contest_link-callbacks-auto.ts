// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/contest_link.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
// Constants resolved from decomp #defines / enums / TS data modules :
const BLOCK_REQ_SIZE_100 = 2;
const CONTESTANT_COUNT = 4;
const LINK_CONTEST_FLAG_HAS_RS_PLAYER = 4;
const LINK_CONTEST_FLAG_IS_LINK = 1;
const LINK_CONTEST_FLAG_IS_WIRELESS = 2;
const VERSION_RUBY = 2;

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: contest_link.c → Task_LinkContest_Init */
export const Task_LinkContest_Init: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      for (i = 0; i < CONTESTANT_COUNT; i++)
          gBlockRecvBuffer[i][0] = 0xFF;

      task.data[0] = 0;
      task.func = (t) => Task_LinkContest_StartInitFlags(t, rt);
};

/** Source: contest_link.c → Task_LinkContest_StartInitFlags */
export const Task_LinkContest_StartInitFlags: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  task.func = (t) => Task_LinkContest_InitFlags(t, rt);
};

/** Source: contest_link.c → Task_LinkContest_InitFlags */
export const Task_LinkContest_InitFlags: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      if (!gReceivedRemoteLinkPlayers)
          return;

      gContestPlayerMonIndex = GetMultiplayerId();
      gNumLinkContestPlayers = GetLinkPlayerCount();
      gLinkContestFlags = LINK_CONTEST_FLAG_IS_LINK;
      if (gWirelessCommType == 1)
          gLinkContestFlags = LINK_CONTEST_FLAG_IS_LINK | LINK_CONTEST_FLAG_IS_WIRELESS;

       
      for (i = 0; i < gNumLinkContestPlayers && (gLinkPlayers[i].version & 0xFF) - 1 > VERSION_RUBY - 1; i++)
          ;

      if (i < gNumLinkContestPlayers)
          gLinkContestFlags |= LINK_CONTEST_FLAG_HAS_RS_PLAYER;

      SwitchTaskToFollowupFunc(taskId);
};

/** Source: contest_link.c → Task_LinkContest_CommunicateMonsRS */
export const Task_LinkContest_CommunicateMonsRS: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      case 0:
           
          if (GetMultiplayerId() == 0)
          {
              if (IsLinkTaskFinished())
              {
                  memcpy(gBlockSendBuffer, gContestMons[gContestPlayerMonIndex], 0);
                  task.data[0] = 10;
              }
          }
          else
          {
              memcpy(gBlockSendBuffer, gContestMons[gContestPlayerMonIndex], 0);
              task.data[0] = 1;
          }
          break;
      case 1:
           
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              for (i = 0; i < gNumLinkContestPlayers; i++)
              {
                  memcpy(gContestMons[i], gBlockRecvBuffer[i], 0);
                  StripPlayerAndMonNamesForLinkContest(gContestMons[i], gLinkPlayers[i].language);
              }

              task.data[0]++;
          }
          break;
      case 10:
           
          if (++task.data[11] > 300)
          {
              SendBlockRequest(BLOCK_REQ_SIZE_100);
              task.data[0] = 1;
          }
          break;
      default:
          task.data[0] = 0;
          task.data[11] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateRngRS */
export const Task_LinkContest_CommunicateRngRS: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[0])
      {
      case 0:
          if (GetMultiplayerId() == 0)
          {
              if (IsLinkTaskFinished() && LinkContest_SendBlock(gRngValue, ((gRngValue)?.length ?? 32)) == true)
                  task.data[0]++;
          }
          else
          {
              task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceived(0))
          {
              memcpy(gRngValue, gBlockRecvBuffer[0], ((gRngValue)?.length ?? 32));
              memcpy(gContestRngValue, gBlockRecvBuffer[0], ((gContestRngValue)?.length ?? 32));
              task.data[0]++;
          }
          break;
      default:
          task.data[0] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateCategoryRS */
export const Task_LinkContest_CommunicateCategoryRS: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      case 0:
          gBlockSendBuffer[0] = task.data[9];
          if (GetMultiplayerId() == 0)
          {
              if (IsLinkTaskFinished())
                  task.data[0] = 10;
          }
          else
          {
              task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              for (i = 0; i < gNumLinkContestPlayers; i++)
                  task.tCategories(i) = gBlockRecvBuffer[i][0];

              task.data[0]++;
          }
          break;
      case 10:
          if (++task.data[11] > 10)
          {
              SendBlockRequest(BLOCK_REQ_SIZE_100);
              task.data[0] = 1;
          }
          break;
      default:
          task.data[0] = 0;
          task.data[11] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateMonIdxs */
export const Task_LinkContest_CommunicateMonIdxs: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[0])
      {
      case 0:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestPlayerMonIndex, ((gContestPlayerMonIndex)?.length ?? 32)) == true)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
              task.data[0]++;
          break;
      default:
          task.data[0] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateMoveSelections */
export const Task_LinkContest_CommunicateMoveSelections: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      switch (task.data[0])
      {
      case 0:
          if (IsLinkTaskFinished())
          {
               
              if (LinkContest_SendBlock(eContestantStatus[gContestPlayerMonIndex].currMove, ((eContestantStatus[gContestPlayerMonIndex].currMove)?.length ?? 32)) == true)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
               
              for (i = 0; i < gNumLinkContestPlayers; i++)
                  eContestantStatus[i].currMove = gBlockRecvBuffer[i][0];

              task.data[0]++;
          }
          break;
      default:
          task.data[0] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateFinalStandings */
export const Task_LinkContest_CommunicateFinalStandings: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[0])
      {
      case 0:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestMonTotalPoints, ((gContestMonTotalPoints)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestMonTotalPoints, gBlockRecvBuffer[gContestLinkLeaderIndex], ((gContestMonTotalPoints)?.length ?? 32));
              task.data[0]++;
          }
          break;
      case 2:
      case 5:
      case 8:
      case 11:
          if (task.data[1]++ > 10)
          {
              task.data[1] = 0;
              task.data[0]++;
          }
          break;
      case 3:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestMonAppealPointTotals, ((gContestMonAppealPointTotals)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 4:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestMonAppealPointTotals, gBlockRecvBuffer[gContestLinkLeaderIndex], ((gContestMonAppealPointTotals)?.length ?? 32));
              task.data[0]++;
          }
          break;
      case 6:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestMonRound2Points, ((gContestMonRound2Points)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 7:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestMonRound2Points, gBlockRecvBuffer[gContestLinkLeaderIndex], ((gContestMonRound2Points)?.length ?? 32));
              task.data[0]++;
          }
          break;
      case 9:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestFinalStandings, ((gContestFinalStandings)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 10:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestFinalStandings, gBlockRecvBuffer[gContestLinkLeaderIndex], ((gContestFinalStandings)?.length ?? 32));
              task.data[0]++;
          }
          break;
      default:
          task.data[0] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateAppealsState */
export const Task_LinkContest_CommunicateAppealsState: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  switch (task.data[0])
      {
      case 0:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(eContestantStatus, CONTESTANT_COUNT * 0) == 1)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(eContestantStatus, gBlockRecvBuffer[gContestLinkLeaderIndex], CONTESTANT_COUNT * 0);
              task.data[0]++;
          }
          break;
      case 2:
      case 5:
      case 8:
      case 11:
          if (task.data[1]++ > 10)
          {
              task.data[1] = 0;
              task.data[0]++;
          }
          break;
      case 3:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestResources.appealResults, 0) == 1)
                  task.data[0]++;
          }
          break;
      case 4:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestResources.appealResults, gBlockRecvBuffer[gContestLinkLeaderIndex], 0);
              task.data[0]++;
          }
          break;
      case 6:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestResources.excitement, 0) == 1)
                  task.data[0]++;
          }
          break;
      case 7:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestResources.excitement, gBlockRecvBuffer[gContestLinkLeaderIndex], 0);
              task.data[0]++;
          }
          break;
      case 9:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestantTurnOrder, ((gContestantTurnOrder)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 10:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestantTurnOrder, gBlockRecvBuffer[gContestLinkLeaderIndex], ((gContestantTurnOrder)?.length ?? 32));
              task.data[0]++;
          }
          break;
      default:
          task.data[0] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateLeaderIdsRS */
export const Task_LinkContest_CommunicateLeaderIdsRS: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  let i = 0;

      if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      case 0:
          gBlockSendBuffer[0] = 0x6E;
          if (GetMultiplayerId() == 0)
          {
              if (IsLinkTaskFinished())
                  task.data[0] = 10;
          }
          else
          {
              task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              for (i = 0; i < CONTESTANT_COUNT; i++)
                  task.tLeaderIds(i) = gBlockRecvBuffer[i][0];

              task.data[0]++;
          }
          break;
      case 10:
          if (++task.data[11] > 10)
          {
              SendBlockRequest(BLOCK_REQ_SIZE_100);
              task.data[0] = 1;
          }
          break;
      default:
          task.data[0] = 0;
          task.data[11] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateRound1Points */
export const Task_LinkContest_CommunicateRound1Points: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      case 0:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestMonRound1Points, ((gContestMonRound1Points)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestMonRound1Points, gBlockRecvBuffer[gContestLinkLeaderIndex], ((gContestMonRound1Points)?.length ?? 32));
              task.data[0]++;
          }
          break;
      default:
          task.data[0] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};

/** Source: contest_link.c → Task_LinkContest_CommunicateTurnOrder */
export const Task_LinkContest_CommunicateTurnOrder: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!LinkContest_TryLinkStandby(task.data[12]))
          return;

      switch (task.data[0])
      {
      case 0:
          if (IsLinkTaskFinished())
          {
              if (LinkContest_SendBlock(gContestantTurnOrder, ((gContestantTurnOrder)?.length ?? 32)) == 1)
                  task.data[0]++;
          }
          break;
      case 1:
          if (LinkContest_GetBlockReceivedFromAllPlayers())
          {
              memcpy(gContestantTurnOrder, gBlockRecvBuffer[gContestLinkLeaderIndex], ((gContestantTurnOrder)?.length ?? 32));
              task.data[0]++;
          }
          break;
      default:
          task.data[0] = 0;
          task.data[12] = 0;
          SwitchTaskToFollowupFunc(taskId);
          break;
      }
};
