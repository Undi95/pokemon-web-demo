// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/field_special_scene.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
import {
  JOY_NEW,
  gSaveBlock1Ptr,
  m4aSongNumStart,
} from '../../system/decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const BOX1_X_OFFSET = 3;
const BOX1_Y_OFFSET = 3;
const BOX2_X_OFFSET = 0;
const BOX2_Y_OFFSET = -3;
const BOX3_X_OFFSET = -3;
const BOX3_Y_OFFSET = 0;
const EXECUTE_MOVEMENT = 2;
const EXIT_PORTHOLE = 3;
const FLAG_DONT_TRANSITION_MUSIC = 16385;
const FLAG_HIDE_MAP_NAME_POPUP = 16384;
const IDLE_CHECK = 1;
const INIT_PORTHOLE = 0;
const LOCALID_PLAYER = 255;
const MAP_OFFSET = 7;
const SE_TRUCK_DOOR = 52;
const SE_TRUCK_MOVE = 49;
const SE_TRUCK_STOP = 50;
const SE_TRUCK_UNLOAD = 51;
const SS_TIDAL_DEPART_SLATEPORT = 2;
const SS_TIDAL_EXIT_CURRENTS_LEFT = 10;
const SS_TIDAL_EXIT_CURRENTS_RIGHT = 9;
const VAR_SS_TIDAL_STATE = 16564;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const LOCALID_TRUCK_BOX_BOTTOM_L: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const LOCALID_TRUCK_BOX_BOTTOM_R: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic
const LOCALID_TRUCK_BOX_TOP: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: field_special_scene.c → Task_Truck1 */
export const Task_Truck1: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      let cameraXpan = 0, cameraYpan = 0;
      let yBox1, yBox2, yBox3;

      yBox1 = GetTruckBoxYMovement(data[1] + 30) * 4;
      SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_TOP,      gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX1_X_OFFSET - cameraXpan, BOX1_Y_OFFSET + yBox1);
      yBox2 = GetTruckBoxYMovement(data[1]) * 2;
      SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_L, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX2_X_OFFSET - cameraXpan, BOX2_Y_OFFSET + yBox2);
      yBox3 = GetTruckBoxYMovement(data[1]) * 4;
      SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_R, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX3_X_OFFSET - cameraXpan, BOX3_Y_OFFSET + yBox3);

       
      if (++data[1] == 30000)
          data[1] = 0;

      cameraYpan = GetTruckCameraBobbingY(data[1]);
      SetCameraPanning(cameraXpan, cameraYpan);
};

/** Source: field_special_scene.c → Task_Truck2 */
export const Task_Truck2: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      let cameraYpan, cameraXpan;
      let yBox1, yBox2, yBox3;

      data[0]++;
      data[2]++;

      if (data[0] > 5)
      {
          data[0] = 0;
          data[1]++;
      }
      if (data[1] == ((sTruckCamera_HorizontalTable)?.length ?? 0))
      {
           
          rt.DestroyTask(taskId);
      }
      else
      {
          if (sTruckCamera_HorizontalTable[data[1]] == 2)
              task.func = (t) => Task_Truck3(t, rt);

          cameraXpan = sTruckCamera_HorizontalTable[data[1]];
          cameraYpan = GetTruckCameraBobbingY(data[2]);
          SetCameraPanning(cameraXpan, cameraYpan);
          yBox1 = GetTruckBoxYMovement(data[2] + 30) * 4;
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_TOP,      gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX1_X_OFFSET - cameraXpan, BOX1_Y_OFFSET + yBox1);
          yBox2 = GetTruckBoxYMovement(data[2]) * 2;
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_L, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX2_X_OFFSET - cameraXpan, BOX2_Y_OFFSET + yBox2);
          yBox3 = GetTruckBoxYMovement(data[2]) * 4;
          SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_R, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX3_X_OFFSET - cameraXpan, BOX3_Y_OFFSET + yBox3);
      }
};

/** Source: field_special_scene.c → Task_Truck3 */
export const Task_Truck3: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
     let cameraXpan, cameraYpan;

     data[0]++;

     if (data[0] > 5)
     {
         data[0] = 0;
         data[1]++;
     }

     if (data[1] == ((sTruckCamera_HorizontalTable)?.length ?? 0))
     {
         rt.DestroyTask(taskId);
     }
     else
     {
         cameraXpan = sTruckCamera_HorizontalTable[data[1]];
         cameraYpan = 0;
         SetCameraPanning(cameraXpan, cameraYpan);
         SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_TOP,      gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX1_X_OFFSET - cameraXpan, BOX1_Y_OFFSET + cameraYpan);
         SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_L, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX2_X_OFFSET - cameraXpan, BOX2_Y_OFFSET + cameraYpan);
         SetObjectEventSpritePosByLocalIdAndMap(LOCALID_TRUCK_BOX_BOTTOM_R, gSaveBlock1Ptr.location.mapNum, gSaveBlock1Ptr.location.mapGroup, BOX3_X_OFFSET - cameraXpan, BOX3_Y_OFFSET + cameraYpan);
     }
};

/** Source: field_special_scene.c → Task_HandleTruckSequence */
export const Task_HandleTruckSequence: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      switch (data[0])
      {
      case 0:
          data[1]++;
          if (data[1] == 90)
          {
              SetCameraPanningCallback(null);
              data[1] = 0;
              data[2] = rt.CreateTask((t) => Task_Truck1(t, rt), 0xA);
              data[0] = 1;
              PlaySE(SE_TRUCK_MOVE);
          }
          break;
      case 1:
          data[1]++;
          if (data[1] == 150)
          {
              FadeInFromBlack();
              data[1] = 0;
              data[0] = 2;
          }
          break;
      case 2:
          data[1]++;
          if (!rt.gPaletteFade.active && data[1] > 300)
          {
              data[1] = 0;
              rt.DestroyTask(data[2]);
              data[3] = rt.CreateTask((t) => Task_Truck2(t, rt), 0xA);
              data[0] = 3;
              PlaySE(SE_TRUCK_STOP);
          }
          break;
      case 3:
          if (!_gt(rt, data[3]).isActive)
          {
               
              InstallCameraPanAheadCallback();
              data[1] = 0;
              data[0] = 4;
          }
          break;
      case 4:
          data[1]++;
          if (data[1] == 90)
          {
              PlaySE(SE_TRUCK_UNLOAD);
              data[1] = 0;
              data[0] = 5;
          }
          break;
      case 5:
          data[1]++;
          if (data[1] == 120)
          {
              MapGridSetMetatileIdAt(4 + MAP_OFFSET, 1 + MAP_OFFSET, METATILE_InsideOfTruck_ExitLight_Top);
              MapGridSetMetatileIdAt(4 + MAP_OFFSET, 2 + MAP_OFFSET, METATILE_InsideOfTruck_ExitLight_Mid);
              MapGridSetMetatileIdAt(4 + MAP_OFFSET, 3 + MAP_OFFSET, METATILE_InsideOfTruck_ExitLight_Bottom);
              DrawWholeMapView();
              PlaySE(SE_TRUCK_DOOR);
              rt.DestroyTask(taskId);
              UnlockPlayerFieldControls();
          }
          break;
      }
};

/** Source: field_special_scene.c → Task_HandlePorthole */
export const Task_HandlePorthole: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      let cruiseState = GetVarPointer(VAR_SS_TIDAL_STATE);
      let location: any = gSaveBlock1Ptr.location;

      switch (data[0])
      {
      case INIT_PORTHOLE:  
          if (!rt.gPaletteFade.active)
          {
              data[1] = 0;
              data[0] = EXECUTE_MOVEMENT;  
          }
          break;
      case IDLE_CHECK:
          if (JOY_NEW(A_BUTTON))
              data[1] = 1;
          if (!ScriptMovement_IsObjectMovementFinished(LOCALID_PLAYER, location.mapNum, location.mapGroup))
              return;
          if (CountSSTidalStep(1) == true)
          {
              if (cruiseState == SS_TIDAL_DEPART_SLATEPORT)
                  cruiseState = SS_TIDAL_EXIT_CURRENTS_RIGHT;
              else
                  cruiseState = SS_TIDAL_EXIT_CURRENTS_LEFT;
              data[0] = EXIT_PORTHOLE;
              return;
          }
          data[0] = EXECUTE_MOVEMENT;
           
      case EXECUTE_MOVEMENT:
          if (data[1])
          {
              data[0] = EXIT_PORTHOLE;
              return;
          }

          if (cruiseState == SS_TIDAL_DEPART_SLATEPORT)
          {
              ScriptMovement_StartObjectMovementScript(LOCALID_PLAYER, location.mapNum, location.mapGroup, sSSTidalSailEastMovementScript);
              data[0] = IDLE_CHECK;
          }
          else
          {
              ScriptMovement_StartObjectMovementScript(LOCALID_PLAYER, location.mapNum, location.mapGroup, sSSTidalSailWestMovementScript);
              data[0] = IDLE_CHECK;
          }
          break;
      case EXIT_PORTHOLE:
          FlagClear(FLAG_DONT_TRANSITION_MUSIC);
          FlagClear(FLAG_HIDE_MAP_NAME_POPUP);
          SetWarpDestinationToDynamicWarp(0);
          DoDiveWarp();
          rt.DestroyTask(taskId);
          break;
      }
};
