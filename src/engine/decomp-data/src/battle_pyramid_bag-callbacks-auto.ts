// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/battle_pyramid_bag.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../decomp-helpers';
import {
  ClearWindowTilemap,
  FillWindowPixelBuffer,
  FreeAllSpritePalettes,
  FreeAllWindowBuffers,
  JOY_NEW,
  StringExpandPlaceholders,
  UpdatePaletteFade,
  gSaveBlock2Ptr,
  gStringVar4,
  m4aSongNumStart,
} from '../../decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const A_BUTTON = 1;
const ANIM_BAG_STILL = 0;
const B_BUTTON = 2;
const COLORID_DARK_GRAY = 0;
const COLORID_LIGHT_GRAY = 1;
const CURSOR_INVISIBLE = 1;
const ITEM_NONE = 0;
const LIST_CANCEL = -2;
const LIST_NOTHING_CHOSEN = -1;
const LISTFIELD_CURSORKIND = 16;
const PYRAMIDBAG_LOC_CHOOSE_TOSS = 3;
const PYRAMIDBAG_LOC_FIELD = 0;
const PYRAMIDBAG_LOC_PARTY = 2;
const PYRAMIDBAG_LOC_PREV = 4;
const SE_SELECT = 5;
const SELECT_BUTTON = 4;
const WIN_INFO = 1;
const WIN_TOSS_NUM = 3;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: battle_pyramid_bag.c → SpriteCB_BagWaitForShake */
export const SpriteCB_BagWaitForShake: SpriteCallback = (sprite, rt) => {
  if (sprite.affineAnimEnded)
      {
          rt.StartSpriteAffineAnim(sprite.spriteId, ANIM_BAG_STILL);
          sprite.callback = SpriteCallbackDummy;
      }
};

/** Source: battle_pyramid_bag.c → Task_ChooseItemsToTossFromPyramidBag */
export const Task_ChooseItemsToTossFromPyramidBag: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (!rt.gPaletteFade.active)
      {
          CleanupOverworldWindowsAndTilemaps();
          gFieldCallback2 = CB2_FadeFromPartyMenu;
          GoToBattlePyramidBagMenu(PYRAMIDBAG_LOC_CHOOSE_TOSS, CB2_ReturnToField);
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_pyramid_bag.c → Task_ClosePyramidBag */
export const Task_ClosePyramidBag: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      if (!rt.gPaletteFade.active)
      {
          DestroyListMenuTask(data[0], gPyramidBagMenuState.scrollPosition, gPyramidBagMenuState.cursorPosition);

           
           
          if (gPyramidBagMenu.newScreenCallback != null)
              /* TODO scene transition: SetMainCallback2(gPyramidBagMenu.newScreenCallback) */;
          else
              /* TODO scene transition: SetMainCallback2(gPyramidBagMenuState.exitCallback) */;
          RemoveScrollArrow();
          rt.ResetSpriteData();
          FreeAllSpritePalettes();
          FreeAllWindowBuffers();
          Free(gPyramidBagMenu);
          rt.DestroyTask(taskId);
      }
};

/** Source: battle_pyramid_bag.c → Task_HandlePyramidBagInput */
export const Task_HandlePyramidBagInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      if (MenuHelpers_ShouldWaitForLinkRecv() == true || rt.gPaletteFade.active)
          return;

      if (JOY_NEW(SELECT_BUTTON))
      {
          if (gPyramidBagMenuState.location != PYRAMIDBAG_LOC_PARTY)
          {
              ListMenuGetScrollAndRow(data[0], gPyramidBagMenuState.scrollPosition, gPyramidBagMenuState.cursorPosition);
              if (gPyramidBagMenuState.scrollPosition + gPyramidBagMenuState.cursorPosition != gPyramidBagMenu.listMenuCount - 1)
              {
                  PlaySE(SE_SELECT);
                  Task_BeginItemSwap(taskId);
              }
          }
      }
      else
      {
          let listId = ListMenu_ProcessInput(data[0]);
          ListMenuGetScrollAndRow(data[0], gPyramidBagMenuState.scrollPosition, gPyramidBagMenuState.cursorPosition);
          switch (listId)
          {
          case LIST_NOTHING_CHOSEN:
              break;
          case LIST_CANCEL:
              PlaySE(SE_SELECT);
              gSpecialVar_ItemId = ITEM_NONE;
              CloseBattlePyramidBag(taskId);
              break;
          default:
              PlaySE(SE_SELECT);
              gSpecialVar_ItemId = gSaveBlock2Ptr.frontier.pyramidBag.itemId[gSaveBlock2Ptr.frontier.lvlMode][listId];
              data[1] = listId;
              data[2] = gSaveBlock2Ptr.frontier.pyramidBag.quantity[gSaveBlock2Ptr.frontier.lvlMode][listId];
              if (gPyramidBagMenuState.location == PYRAMIDBAG_LOC_PARTY)
                  TryCloseBagToGiveItem(taskId);
              else
                  OpenContextMenu(taskId);
              break;
          }
      }
};

/** Source: battle_pyramid_bag.c → Task_ChooseHowManyToToss */
export const Task_ChooseHowManyToToss: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      if (AdjustQuantityAccordingToDPadInput(data[8], data[2]) == true)
      {
          UpdateNumToToss(data[8]);
      }
      else if (JOY_NEW(A_BUTTON))
      {
           
          PlaySE(SE_SELECT);
          ClearStdWindowAndFrameToTransparent(WIN_TOSS_NUM, false);
          ClearWindowTilemap(WIN_TOSS_NUM);
          ScheduleBgCopyTilemapToVram(1);
          AskConfirmToss(taskId);
      }
      else if (JOY_NEW(B_BUTTON))
      {
           
          PlaySE(SE_SELECT);
          ClearStdWindowAndFrameToTransparent(WIN_TOSS_NUM, false);
          ClearWindowTilemap(WIN_TOSS_NUM);
          ScheduleBgCopyTilemapToVram(1);
          DontTossItem(taskId);
      }
};

/** Source: battle_pyramid_bag.c → Task_TossItem */
export const Task_TossItem: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      let scrollOffset = gPyramidBagMenuState.scrollPosition;
      let selectedRow = gPyramidBagMenuState.cursorPosition;

      if (JOY_NEW(A_BUTTON | B_BUTTON))
      {
          PlaySE(SE_SELECT);
          RemovePyramidBagItem(gSpecialVar_ItemId, data[8]);
          DestroyListMenuTask(data[0], scrollOffset, selectedRow);
          UpdatePyramidBagList();
          UpdatePyramidBagCursorPos();
          SetBagItemsListTemplate();
          data[0] = ListMenuInit(gMultiuseListMenuTemplate, scrollOffset, selectedRow);
          ScheduleBgCopyTilemapToVram(0);
          SetTaskToMainPyramidBagInputHandler(taskId);
      }
};

/** Source: battle_pyramid_bag.c → Task_WaitCloseErrorMessage */
export const Task_WaitCloseErrorMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  if (JOY_NEW(A_BUTTON))
      {
          PlaySE(SE_SELECT);
          Task_CloseBattlePyramidBagMessage(taskId);
      }
};

/** Source: battle_pyramid_bag.c → Task_CloseBattlePyramidBagMessage */
export const Task_CloseBattlePyramidBagMessage: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      CloseBattlePyramidBagTextWindow();
      PrintItemDescription(data[1]);
      PrintSelectorArrow(data[0], COLORID_DARK_GRAY);
      SetTaskToMainPyramidBagInputHandler(taskId);
};

/** Source: battle_pyramid_bag.c → Task_BeginItemSwap */
export const Task_BeginItemSwap: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;

      data[1] = gPyramidBagMenuState.scrollPosition + gPyramidBagMenuState.cursorPosition;
      gPyramidBagMenu.toSwapPos = data[1];
      ListMenuSetTemplateField(data[0], LISTFIELD_CURSORKIND, CURSOR_INVISIBLE);
      CopyItemName(gSaveBlock2Ptr.frontier.pyramidBag.itemId[gSaveBlock2Ptr.frontier.lvlMode][data[1]], gStringVar1);
      StringExpandPlaceholders(gStringVar4, gText_MoveVar1Where);
      FillWindowPixelBuffer(WIN_INFO, PIXEL_FILL(0));
      PyramidBagPrint(WIN_INFO, gStringVar4, 3, 0, 0, 1, 0, COLORID_DARK_GRAY);
      PrintSelectorArrow(data[0], COLORID_LIGHT_GRAY);
      UpdateSwapLinePos(data[1]);
      task.func = (t) => Task_ItemSwapHandleInput(t, rt);
};

/** Source: battle_pyramid_bag.c → Task_ItemSwapHandleInput */
export const Task_ItemSwapHandleInput: TaskCallback = (task, rt) => {
  const taskId = task.taskId;
  const data = task.data;
      if (MenuHelpers_ShouldWaitForLinkRecv() != true)
      {
          if (JOY_NEW(SELECT_BUTTON))
          {
              PlaySE(SE_SELECT);
              ListMenuGetScrollAndRow(data[0], gPyramidBagMenuState.scrollPosition, gPyramidBagMenuState.cursorPosition);
              PerformItemSwap(taskId);
          }
          else
          {
              let id = ListMenu_ProcessInput(data[0]);
              ListMenuGetScrollAndRow(data[0], gPyramidBagMenuState.scrollPosition, gPyramidBagMenuState.cursorPosition);
              SetSwapLineInvisibility(false);
              UpdateSwapLinePos(gPyramidBagMenuState.cursorPosition);
              switch (id)
              {
              case LIST_NOTHING_CHOSEN:
                  break;
              case LIST_CANCEL:
                  PlaySE(SE_SELECT);
                  if (JOY_NEW(A_BUTTON))
                      PerformItemSwap(taskId);
                  else
                      CancelItemSwap(taskId);
                  break;
              default:
                  PlaySE(SE_SELECT);
                  PerformItemSwap(taskId);
                  break;
              }
          }
      }
};

/** Source: battle_pyramid_bag.c → CB2_PyramidBagMenuFromStartMenu */
export const CB2_PyramidBagMenuFromStartMenu: CB2Callback = (rt) => {
  GoToBattlePyramidBagMenu(PYRAMIDBAG_LOC_FIELD, CB2_ReturnToFieldWithOpenMenu);
};

/** Source: battle_pyramid_bag.c → CB2_ReturnToPyramidBagMenu */
export const CB2_ReturnToPyramidBagMenu: CB2Callback = (rt) => {
  GoToBattlePyramidBagMenu(PYRAMIDBAG_LOC_PREV, gPyramidBagMenuState.exitCallback);
};

/** Source: battle_pyramid_bag.c → CB2_PyramidBag */
export const CB2_PyramidBag: CB2Callback = (rt) => {
  RunTasks();
      AnimateSprites();
      BuildOamBuffer();
      DoScheduledBgTilemapCopiesToVram();
      UpdatePaletteFade();
};

/** Source: battle_pyramid_bag.c → CB2_LoadPyramidBagMenu */
export const CB2_LoadPyramidBagMenu: CB2Callback = (rt) => {
  while (MenuHelpers_ShouldWaitForLinkRecv() != true
          && LoadPyramidBagMenu() != true
          && MenuHelpers_IsLinkActive() != true);
};
