// AUTO-GENERATED by scripts/transpile-callbacks.mjs
// Source : src/region_map.c (via SPRITE_CALLBACKS / TASKS / SPRITE_CBS / CB2S)
// Generated : 2026-04-29
// DO NOT EDIT — re-run `node scripts/transpile-callbacks.mjs` to refresh.
//
// Symbol substitution rules in script header.
// Manual fixes may be needed for /* TODO */ markers and undefined constants below.

/* eslint-disable */
// @ts-nocheck

import type { DecompRuntime, DecompSprite, DecompTask } from '../../system/decomp-runtime';
import {
  Sin, Cos, Q_8_8_TO_INT, SetOamMatrix, CalcCenterToCornerVec,
  ST_OAM_AFFINE_OFF, ST_OAM_AFFINE_NORMAL, ST_OAM_AFFINE_DOUBLE, ST_OAM_AFFINE_ERASE,
  ST_OAM_OBJ_NORMAL, ST_OAM_OBJ_BLEND, ST_OAM_OBJ_WINDOW,
  ST_OAM_4BPP, ST_OAM_8BPP,
  RGB, RGB_BLACK, RGB_WHITE, RGB_WHITEALPHA,
  PLTT_SIZEOF, PLTT_SIZE_4BPP, PLTT_SIZE_8BPP,
  OBJ_PLTT_ID_FADED, BG_PLTT_ID_FADED,
  BLDALPHA_BLEND, WIN_RANGE, GET_TRUE_SPRITE_INDEX, ANIM_SPRITES_START,
  gSineTable, PaletteBuffer, FreeAllSpritePalettes,
} from '../../system/decomp-helpers';
import {
  DeactivateAllTextPrinters,
  FillWindowPixelBuffer,
  FreeAllSpritePalettes,
  InitBgsFromTemplates,
  InitWindows,
  LZ77UnCompVram,
  LoadPalette,
  PutWindowTilemap,
  ResetBgsAndClearDma3BusyFlags,
  ResetPaletteFade,
  ShowBg,
  gMain,
} from '../../system/decomp-globals';
// Constants resolved from decomp #defines / enums / TS data modules :
const CHAR_SPACE = 0;
const FONT_NORMAL = 1;
const MAP_NAME_LENGTH = 16;
const TAG_CURSOR = 1;
const TAG_PLAYER_ICON = 0;
const WIN_FLY_TO_WHERE = 2;
// Unresolved constants (auto-stub at 0; replace with real values when needed) :
const _UNDEFINED = 0;
const PIXEL_FILL: any = _UNDEFINED; // TODO : not found in decomp /include or auto/src — possibly dynamic

export type SpriteCallback = (sprite: DecompSprite, rt: DecompRuntime) => void;
export type TaskCallback = (task: DecompTask, rt: DecompRuntime) => void;
export type CB2Callback = (rt: DecompRuntime) => void;

const _emptySprite: any = { data: new Array(16).fill(0), invisible: false, x: 0, y: 0, x2: 0, y2: 0, oamIndex: 0, spriteId: -1 };
const _emptyTask: any = { data: new Array(16).fill(0), func: null, taskId: -1 };
function _gs(rt: DecompRuntime, id: number): DecompSprite { return (rt.gSprites.get(id) as DecompSprite) ?? _emptySprite; }
function _gt(rt: DecompRuntime, id: number): DecompTask { return (rt.gTasks.get(id) as DecompTask) ?? _emptyTask; }
function _palView(buf: PaletteBuffer, base: number): ArrayLike<number> {
  return new Proxy({ length: 512 }, { get(t, k) { if (k === 'length') return 512; const i = Number(k); return Number.isFinite(i) ? buf.get(base + i) : undefined; } }) as ArrayLike<number>;
}

/** Source: region_map.c → SpriteCB_CursorMapFull */
export const SpriteCB_CursorMapFull: SpriteCallback = (sprite, rt) => {
  if (sRegionMap.cursorMovementFrameCounter != 0)
      {
          sprite.x += 2 * sRegionMap.cursorDeltaX;
          sprite.y += 2 * sRegionMap.cursorDeltaY;
          sRegionMap.cursorMovementFrameCounter--;
      }
};

/** Source: region_map.c (failed: empty bodyC) */
export const SpriteCB_CursorMapZoomed: SpriteCallback = (_sprite, _rt) => { /* TODO empty bodyC */ };

/** Source: region_map.c → SpriteCB_PlayerIconMapZoomed */
export const SpriteCB_PlayerIconMapZoomed: SpriteCallback = (sprite, rt) => {
  sprite.x2 = -2 * sRegionMap.scrollX;
      sprite.y2 = -2 * sRegionMap.scrollY;
      sprite.data[0] = sprite.y + sprite.y2 + sprite.centerToCornerVecY;
      sprite.data[1] = sprite.x + sprite.x2 + sprite.centerToCornerVecX;
      if (sprite.data[0] < -8 || sprite.data[0] > DISPLAY_HEIGHT + 8 || sprite.data[1] < -8 || sprite.data[1] > DISPLAY_WIDTH + 8)
          sprite.data[2] = false;
      else
          sprite.data[2] = true;

      if (sprite.data[2] == true)
          SpriteCB_PlayerIcon(sprite);
      else
          sprite.invisible = true;
};

/** Source: region_map.c → SpriteCB_PlayerIconMapFull */
export const SpriteCB_PlayerIconMapFull: SpriteCallback = (sprite, rt) => {
  SpriteCB_PlayerIcon(sprite);
};

/** Source: region_map.c → SpriteCB_PlayerIcon */
export const SpriteCB_PlayerIcon: SpriteCallback = (sprite, rt) => {
  if (sRegionMap.blinkPlayerIcon)
      {
          if (++sprite.data[7] > 16)
          {
              sprite.data[7] = 0;
              sprite.invisible = sprite.invisible ? false : true;
          }
      }
      else
      {
          sprite.invisible = false;
      }
};

/** Source: region_map.c → SpriteCB_FlyDestIcon */
export const SpriteCB_FlyDestIcon: SpriteCallback = (sprite, rt) => {
  if (sFlyMap.regionMap.mapSecId == sprite.data[0])
      {
          if (++sprite.data[1] > 16)
          {
              sprite.data[1] = 0;
              sprite.invisible = sprite.invisible ? false : true;
          }
      }
      else
      {
          sprite.data[1] = 16;
          sprite.invisible = false;
      }
};

/** Source: region_map.c → CB2_OpenFlyMap */
export const CB2_OpenFlyMap: CB2Callback = (rt) => {
  switch (gMain.state)
      {
      case 0:
          rt.SetVBlankCallback(VBlankCB);
          rt.SetGpuReg(REG_OFFSET_DISPCNT, 0);
          rt.SetGpuReg(REG_OFFSET_BG0HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG0VOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG1HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG1VOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG2VOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG2HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG3HOFS, 0);
          rt.SetGpuReg(REG_OFFSET_BG3VOFS, 0);
          sFlyMap = Alloc(((sFlyMap)?.length ?? 32));
          if (sFlyMap == null)
          {
              /* TODO scene transition: SetMainCallback2(CB2_ReturnToFieldWithOpenMenu) */;
          }
          else
          {
              ResetPaletteFade();
              rt.ResetSpriteData();
              FreeSpriteTileRanges();
              FreeAllSpritePalettes();
              gMain.state++;
          }
          break;
      case 1:
          ResetBgsAndClearDma3BusyFlags(0);
          InitBgsFromTemplates(1, sFlyMapBgTemplates, ((sFlyMapBgTemplates)?.length ?? 0));
          gMain.state++;
          break;
      case 2:
          InitWindows(sFlyMapWindowTemplates);
          DeactivateAllTextPrinters();
          gMain.state++;
          break;
      case 3:
          LoadUserWindowBorderGfx(0, 0x65, BG_PLTT_ID(13));
          ClearScheduledBgCopiesToVram();
          gMain.state++;
          break;
      case 4:
          InitRegionMap(sFlyMap.regionMap, false);
          CreateRegionMapCursor(TAG_CURSOR, TAG_CURSOR);
          CreateRegionMapPlayerIcon(TAG_PLAYER_ICON, TAG_PLAYER_ICON);
          sFlyMap.mapSecId = sFlyMap.regionMap.mapSecId;
          StringFill(sFlyMap.nameBuffer, CHAR_SPACE, MAP_NAME_LENGTH);
          sDrawFlyDestTextWindow = true;
          DrawFlyDestTextWindow();
          gMain.state++;
          break;
      case 5:
          LZ77UnCompVram(sRegionMapFrameGfxLZ, BG_CHAR_ADDR(3));
          gMain.state++;
          break;
      case 6:
          LZ77UnCompVram(sRegionMapFrameTilemapLZ, BG_SCREEN_ADDR(30));
          gMain.state++;
          break;
      case 7:
          LoadPalette(sRegionMapFramePal, BG_PLTT_ID(1), ((sRegionMapFramePal)?.length ?? 32));
          PutWindowTilemap(WIN_FLY_TO_WHERE);
          FillWindowPixelBuffer(WIN_FLY_TO_WHERE, PIXEL_FILL(0));
          AddTextPrinterParameterized(WIN_FLY_TO_WHERE, FONT_NORMAL, gText_FlyToWhere, 0, 1, 0, null);
          ScheduleBgCopyTilemapToVram(0);
          gMain.state++;
          break;
      case 8:
          LoadFlyDestIcons();
          gMain.state++;
          break;
      case 9:
          BlendPalettes(PALETTES_ALL, 16, 0);
          rt.SetVBlankCallback(VBlankCB);
          gMain.state++;
          break;
      case 10:
          rt.SetGpuReg(REG_OFFSET_BLDCNT, 0);
          SetGpuRegBits(REG_OFFSET_DISPCNT, DISPCNT_OBJ_1D_MAP | DISPCNT_OBJ_ON);
          ShowBg(0);
          ShowBg(1);
          ShowBg(2);
          SetFlyMapCallback(CB_FadeInFlyMap);
          rt.SetMainCallback2(CB2_FlyMap);
          gMain.state++;
          break;
      }
};

/** Source: region_map.c → CB2_FlyMap */
export const CB2_FlyMap: CB2Callback = (rt) => {
  sFlyMap.callback();
      AnimateSprites();
      BuildOamBuffer();
      DoScheduledBgTilemapCopiesToVram();
};

/** ⚠️ Generic patch (post-transpile-patches.mjs) — VBlankCB no-op.
 *  Notre runtime call TransferPlttBuffer auto si vblankCallback non-null,
 *  donc le scene-side VBlankCB est essentiellement un marqueur "transfer ON". */
const VBlankCB: () => void = () => { /* no-op */ };
