// AUTO-GENERATED from src/mauville_old_man.c by extract-decomp-task-machines.mjs
// Do not edit manually — re-run `npm run extract:task-machines` to refresh.
//
// Generated: 2026-04-27
// Stats: 2 Task_, 0 CB2_, 0 SpriteCB_

export const TASKS = {
  "Task_BardSong": {
    callsTo: ["BardSing","DestroyTask","DrawSongTextWindow","EnableTextPrinters","FadeInBGM","FadeOutBGMTemporarily","IsBGMPausedOrStopped","PrepareSongText","RunTextPrintersAndIsPrinter0Active","ScriptContext_Enable","WORD_TO_PITCH_TABLE_INDEX","m4aMPlayFadeOutTemporarily"],
    terminalMarkers: ["DestroyTask"],
    lineCount: 118,
    bodyC: "struct Task *task = &gTasks[taskId];\n\n    BardSing(task, &gBardSong);\n\n    switch (task->tState)\n    {\n    case BARD_STATE_INIT:\n        PrepareSongText();\n        DrawSongTextWindow(gStringVar4);\n        task->tWordState = 0;\n        task->tDelay = 0;\n        task->tCharIndex = 0;\n        task->tLyricsIndex = 0;\n        FadeOutBGMTemporarily(4);\n        task->tState = BARD_STATE_WAIT_BGM;\n        break;\n    case BARD_STATE_WAIT_BGM:\n        if (IsBGMPausedOrStopped())\n            task->tState = BARD_STATE_GET_WORD;\n        break;\n    case BARD_STATE_GET_WORD:\n    {\n        struct MauvilleManBard *bard = &gSaveBlock1Ptr->oldMan.bard;\n        u8 *str = &gStringVar4[task->tCharIndex];\n        u16 wordLen = 0;\n\n         \n        while (*str != CHAR_SPACE\n            && *str != CHAR_NEWLINE\n            && *str != EXT_CTRL_CODE_BEGIN\n            && *str != EOS)\n        {\n            str++;\n            wordLen++;\n        }\n\n         \n        if (!task->tUseNewSongLyrics)\n            sUnusedPitchTableIndex = WORD_TO_PITCH_TABLE_INDEX(bard->songLyrics[task->tLyricsIndex]);\n        else\n            sUnusedPitchTableIndex = WORD_TO_PITCH_TABLE_INDEX(bard->newSongLyrics[task->tLyricsIndex]);\n\n        gBardSong.length /= wordLen;\n        if (gBardSong.length <= 0)\n            gBardSong.length = 1;\n\n        task->tLyricsIndex++;\n\n        if (task->tDelay == 0)\n        {\n            task->tState = BARD_STATE_HANDLE_WORD;\n            task->tWordState = 0;\n        }\n        else\n        {\n            task->tState = BARD_STATE_PAUSE;\n            task->tWordState = 0;\n        }\n    }\n        break;\n    case BARD_STATE_PAUSE:\n         \n        if (task->tDelay == 0)\n            task->tState = BARD_STATE_HANDLE_WORD;\n        else\n            task->tDelay--;\n        break;\n    case BARD_STATE_HANDLE_WORD:\n        if (gStringVar4[task->tCharIndex] == EOS)\n        {\n             \n            FadeInBGM(6);\n            m4aMPlayFadeOutTemporarily(&gMPlayInfo_SE2, 2);\n            ScriptContext_Enable();\n            DestroyTask(taskId);\n        }\n        else if (gStringVar4[task->tCharIndex] == CHAR_SPACE)\n        {\n             \n            EnableTextPrinters();\n            task->tCharIndex++;\n            task->tState = BARD_STATE_GET_WORD;\n            task->tDelay = 0;\n        }\n        else if (gStringVar4[task->tCharIndex] == CHAR_NEWLINE)\n        {\n             \n            task->tCharIndex++;\n            task->tState = BARD_STATE_GET_WORD;\n            task->tDelay = 0;\n        }\n        else if (gStringVar4[task->tCharIndex] == EXT_CTRL_CODE_BEGIN)\n        {\n             \n             \n             \n            task->tCharIndex += 2;   \n            task->tState = BARD_STATE_GET_WORD;\n            task->tDelay = 8;\n        }\n        else if (gStringVar4[task->tCharIndex] == CHAR_BARD_WORD_DELIMIT)\n        {\n             \n            gStringVar4[task->tCharIndex] = CHAR_SPACE;\n            EnableTextPrinters();\n            task->tCharIndex++;\n            task->tDelay = 0;\n        }\n        else\n        {\n             \n            switch (task->tWordState)\n            {\n            case 0:\n                EnableTextPrinters();\n                task->tWordState++;\n                break;\n            case 1:\n                task->tWordState++;\n                break;\n            case 2:\n                task->tCharIndex++;\n                task->tWordState = 0;\n                task->tDelay = gBardSong.length;\n                task->tState = BARD_STATE_WAIT_WORD;\n                break;\n            }\n        }\n        break;\n    case BARD_STATE_WAIT_WORD:\n         \n         \n        task->tDelay--;\n        if (task->tDelay == 0)\n            task->tState = BARD_STATE_HANDLE_WORD;\n        break;\n    }\n    RunTextPrintersAndIsPrinter0Active();",
  },
  "Task_StoryListMenu": {
    callsTo: ["ClearToTransparentAndRemoveWindow","DestroyTask","GetFreeStorySlot","Menu_ProcessInput","PrintStoryList","ScriptContext_Enable"],
    terminalMarkers: ["DestroyTask"],
    lineCount: 26,
    bodyC: "struct Task *task = &gTasks[taskId];\n    s32 selection;\n\n    switch (task->data[0])\n    {\n    case 0:\n        PrintStoryList();\n        task->data[0]++;\n        break;\n    case 1:\n        selection = Menu_ProcessInput();\n        if (selection == MENU_NOTHING_CHOSEN)\n            break;\n        if (selection == MENU_B_PRESSED || selection == GetFreeStorySlot())\n        {\n            gSpecialVar_Result = 0;\n        }\n        else\n        {\n            gSpecialVar_Result = 1;\n            sSelectedStory = selection;\n        }\n        ClearToTransparentAndRemoveWindow(sStorytellerWindowId);\n        DestroyTask(taskId);\n        ScriptContext_Enable();\n        break;\n    }",
  },
} as const;
