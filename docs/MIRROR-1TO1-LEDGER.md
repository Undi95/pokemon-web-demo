# 🪞 MIRROR 1:1 LEDGER — notre `src/game/` ↔ décomp `pokeemeraude/src/`

> Directive user 2026-06-05 : reconstruire le jeu **post-camion** en miroir 1:1 STRICT de la
> décomp — mêmes noms de fichiers, mêmes includes, mêmes noms de fonctions, zéro duplication.
> Manuel, par Claude seul. Ce ledger = LE backlog + suivi. Plan complet : mémoire `mirror-port-1to1`.
>
> **Convention** : `decomp/src/X.c` → `src/game/X.ts` ; `decomp/include/X.h` → `src/game/include/X.ts`
> (re-export API + macros/constantes/types). Un `#include "X.h"` = un `import … from '@game/include/X'`.
> Noms de fonctions/globals EXACTS décomp. Couche matériel (gba/m4a/dma/regs/OAM/window framework)
> = fournie par `src/engine/{system,gba}`, exposée via `src/game/include/gba/*.ts`.
>
> Statuts : ☐ TODO · ◑ WIP · ☑ PORTÉ (tsc 0) · ✅ VÉRIFIÉ (runtime/A-B). Inventaire décomp = **310 `.c`**.

---

## 📍 ÉTAT ACTUEL / REPRISE (2026-06-05)
**Modules miroir FAITS + vérifiés runtime 1:1** (`src/game/`) :
- `random.ts`, `trig.ts`, `math_util.ts`, `util.ts`(pures) — feuilles pures (+ `include/` chacun).
- `metatile_behavior.ts` — 140 prédicats, **audité ligne-à-ligne, 6 vrais bugs gameplay fixés** (collision/eau/ponts). `field/metatile-behavior(.ts/-helpers.ts)` ré-exportent.
- `event_data.ts` (+ `include/`) — **migration STRUCTURELLE** : save block flags/vars → `number[]` bit-packé/indexé (layout décomp) ; `script-vars.ts` = bridge nom→id. **+ helpers cross-module flags/vars PORTÉS** (ClearTempFieldEventData/ClearDailyFlags · Mystery Event+Gift (6) · ClearMysteryGiftFlags/Vars · Reset RTC (3), event_data.c:39-162) ; National Dex (c:55-80) différé (couplage `gSaveBlock2.pokedex`). Vérifié end-to-end (CanResetRTC/IsMysteryGiftEnabled round-trip flag+var, boot 0 err, saveSelfTest).
- `pokemon.ts` (+ `include/`) — **AMORCE (port incrémental, 2026-06-05)** : helpers PURS portés avant la migration de stockage — `GetNatureFromPersonality`, `ModifyStatByNature` (+ `gNatureStatTable` transcrite 1:1 ; **signature 1:1 1-based restaurée** vs la version 0-based aplatie de party-storage), macro `GET_UNOWN_LETTER`, `GetGenderFromSpeciesAndPersonality` (version *number* 1:1 ; species-runtime re-exporte ; version *string* différée=migration species), **shiny** (`IsShinyOtIdPersonality` + macro `GET_SHINY_VALUE`) + nouveau miroir **`include/global.ts`** (HIHALF/LOHALF de global.h). **Dups consolidées** : party-storage/flavor-compat/mail-data/species-runtime/battle-main-functions(IsMonShiny)/decomp-bridge(HIHALF/LOHALF/GET_SHINY_VALUE/GET_UNOWN_LETTER)/pokemon-legacy(isShinyFromOtIdPersonality)/damage-calc+ai-script-commands(`gStatStageRatios`). **Helpers purs pokemon.c désormais ÉPUISÉS** (nature/gender/unown/shiny/stat-by-nature/stat-stage-ratios faits ; le reste — `GetLevelFromMonExp`, `CalculateMonStats`, EVs, checksums — touche le struct mon → migration de STOCKAGE). Vérifié (tsc 0, harness voie L 0-err, **test positif shiny : party OW `isShiny`===calcul manuel `allMatch=true`** sur Treecko/Jirachi/Chansey ; cycles fonction-seulement bénins, boot OK).
- `include/constants/flags.ts`(**1482**)+`vars.ts`(**293**) — ids RÉSOLUS (`scripts/gen-event-data-ids.mjs`). **🐛 Fix générateur (2026-06-05)** : le regex de capture de nom `[A-Z0-9_]` (majuscules only) tronquait les noms hex `FLAG_UNUSED_0x91F`→`FLAG_UNUSED_0` (expr invalide → jamais résolu) ; or `DAILY_FLAGS_START`/`FLAGS_COUNT` en dépendent → **DAILY_FLAGS_START(2336)/NUM_DAILY_FLAGS(64)/FLAGS_COUNT(2400) manquaient**. Corrigé → `[A-Za-z0-9_]` (1er char reste `[A-Z_]`). +393 flags / +35 vars.

- `string_util.ts` (+ `include/`) — **◑ SURFACE SANS-ÉTAT 100% FAITE (2026-06-05, 2 passes)** : fns simples buffer (Length/Copy/Append/CopyN/Compare/Fill/CopyPadded) + Convert{Int,UInt}ToDecimal/HexStringN **+ 2e passe** StringBraille · StringCopyN_Multibyte · StringLength_Multibyte · WriteColorChangeControlCode · IsString[N]Japanese · GetExtCtrlCodeLength · SkipExtCtrlCode(static) · StringCompareWithoutExtCtrlCodes · ConvertInternationalString · StripExtCtrlCodes — **toutes portées 1:1 sur `Uint8Array`** (sémantique pointeur `u8*` via `subarray`), **vérifiées headless 10/10 valeurs exactes** (lenMB=3, braille=[252,6,6,1,65,255], ciJpn=[252,21,65,66,252,22,255], gecl=[2,4,3,1,1,0]…). **🐛 vrai bug 1:1 trouvé+corrigé** : `WriteColorChangeControlCode` local d'`easy-chat-render` OMETTAIT le `*dest = EOS` final (string_util.c:625) → StringAppend suivant cassé → consolidé vers le miroir (qui écrit le EOS). **✅ STAGE 0 du chantier TEXTE 1:1 FAIT + VÉRIFIÉ HEADLESS (2026-06-05)** : le nœud À ÉTAT est porté byte-level — buffers EWRAM `gStringVar1-4` (Uint8Array) + `sUnknownStringVar` ; `StringExpandPlaceholders` (récursif, c:335) + 14× `ExpandPlaceholder_*` + table `sExpandPlaceholderFuncs` + `GetExpandedPlaceholder` (c:499) ; NOUVEAU `src/game/strings.ts` (+`include/`) = miroir PARTIEL strings.c (12 `gText_ExpandedPlaceholder_*` FR + bridge transitoire `InitTextData`/`EncodePlayerNameFR`). **NON-BREAKING** (rien de live ne l'appelle ; coexiste avec la voie ASCII gba-text-system jusqu'au flip `__USE_DECOMP_TEXT__`). Vérif **18/18 headless** (placeholders/récursion/ext-ctrl 0-3 args/accents/genre). Détail = `docs/TEXT-DATA-1TO1-MIGRATION-PLAN.md` §Stage 0. **RESTE = Stages 1-5** (extraction token→byte des 174 data, renderer glyphe byte-level, flip ~57 callers, save playerName, retrait flag) = **flag + A/B AVEC user**.

**Dé-duplications faites** : gBitTable (5 copies→1), Sin2/Cos2 (wallclock→trig), metatile (3 fichiers→1), flags/vars (Record name-keyed → id-indexé), **CanResetRTC (decomp-globals name-keyed CASSÉ + gba-menu-system stub → délégués miroir) + IsMysteryGiftEnabled (gba-menu-system stub → miroir)**, **nature/stat/gender/shiny (`GetNatureFromPersonality`, `ModifyStatByNature`+`gNatureStatTable`, `GetUnownLetterByPersonality`/`GET_UNOWN_LETTER`, `GetGenderFromSpeciesAndPersonality`(number), `IsShinyOtIdPersonality`/`GET_SHINY_VALUE`, `HIHALF`/`LOHALF` → miroir `pokemon.ts`+`include/global.ts`)**, **WriteColorChangeControlCode (easy-chat-render impl locale [bug EOS] + decomp-bridge no-op stub → miroir `string_util.ts`)**.

**Méthode rodée (par module)** : lire `.c`+`.h` décomp → créer `src/game/X.ts`+`include/X.ts` 1:1 (mêmes noms fns/imports) → consolider l'existant (ancien=re-export, 0 dup) → **auditer ligne-à-ligne vs décomp** (diff d'ensembles + tri runtime des faux positifs) → corriger bugs 1:1 → **tsc 0 + vérif runtime**. Met à jour CE ledger à chaque module.

**Règles** : tables de DONNÉES = transcrire/extraire (jamais recalculer, le fixed-point tronque) · vérifier chaque « bug » avant de le claim · `import()` dynamique en eval = instance ≠ runtime (utiliser `window.__*` / globals runtime) · migrations structurelles (pokemon/strings) = stockage 1:1 + bridge transitoire (cf. event_data).

**PROCHAINS PAS** (au choix, suivre le plan) :
0. ✅ FAIT (2026-06-05) : **string_util surface SANS-ÉTAT 100%** (2e passe, headless 10/10, 1 bug 1:1 EOS corrigé). **⚠️ AUTONOME LEAF = ÉPUISÉ** : il ne reste QUE 2 gros leviers, tous deux **transverses + A/B-sensibles → à faire AVEC user** : (a) **nœud gStringVar1-4** (string_util) = StringExpandPlaceholders byte-level + bridge **~57 callers** JS-string↔Uint8Array (état runtime = JS-string aujourd'hui, texte PARTOUT) ; (b) **migration stockage pokemon** (PokemonInstance→BoxMon, party affiché partout). Les deux = pattern « stockage 1:1 + bridge transitoire » (cf. event_data). **→ DÉCISION USER (2026-06-05) : attaquer le nœud gStringVar, option « RÉ-ENCODAGE DATA 1:1 » (la plus fidèle).** Plan stagé complet = **`docs/TEXT-DATA-1TO1-MIGRATION-PLAN.md`** (5 stages, flag `__USE_DECOMP_TEXT__` style voie V/L). **Découverte racine** : 3 mondes texte, 2 représentations de placeholders — nos data OW = tokens ASCII `{STR_VAR_1}` (174 fichiers) vs bytes `0xFD` décomp ; renderer OW = ASCII (`String.fromCharCode`) vs glyphe-par-index-charmap décomp. → migration touche extraction + 174 data + gStringVar + renderer glyphe + 57 callers + save(playerName). **✅ Stage 0 FAIT + vérifié headless 18/18 (2026-06-05)** : cœur byte-level dans le miroir (gStringVar1-4 Uint8Array + sUnknownStringVar + `StringExpandPlaceholders`/`GetExpandedPlaceholder`/14× `ExpandPlaceholder_*` + NOUVEAU `src/game/strings.ts` placeholders FR + bridge `InitTextData`/`EncodePlayerNameFR`). tsc 0, dédup clean (seule dup = `StringExpandPlaceholders` ×2 = TRANSITOIRE assumée du flag). **✅ FLIP DIRECT COMPLET — tsc 0 (2026-06-06, décision user : pas de flag, pas de transitoire, « si ça casse ça casse »).** Le flag `__USE_DECOMP_TEXT__` a été RETIRÉ (text.ts garde `encodeOwTextSource`/`encodeOwText`/`decodeOwBytes` = notre PRÉPROC, l'analogue de `_("…")`).
**✅ CŒUR MESSAGE OW MIGRÉ BYTE** : `gStringVar1-4` + `StringExpandPlaceholders` (gba-text-system) = le miroir byte (source UNIQUE, JS-string retirées) ; `setStringVar4`/`AddTextPrinterForMessage`/`AddTextPrinterParameterized3` byte ; `getText` → `Uint8Array` (encode via `encodeOwText` au 1er accès, cache, source `_textsByLabel` reste lisible) ; `ShowFieldMessage(Uint8Array)` + `field-message-box` byte ; callers migrés (`script-opcodes-message`, `start-menu` [×6, littéraux→`encodeOwText`], `trainer-battle-flow`, `specials-registry`, `multichoice-data` [fallback décodé]). `getOwCharmap`/`decodeOwBytes` ajoutés (plomberie). **tsc : 483 usages → cascade réelle = 41 erreurs restantes sur 3 fichiers** : `bag-menu.ts`(15), `bedroom-pc.ts`(22), `bag-screen.ts`(4) — modèles texte STRING (`StringCopy('',x)→string`, `_gsv.gStringVarN=str`) à réécrire byte. ✅ **PAS de cascade** : les accesseurs de noms (`GetItemName`/`getMoveName`/`gSpeciesNames`…) RESTENT string-source ; on les encode AU POINT D'ENTRÉE dans le pipeline byte via `encodeOwText` (= notre préproc, comme les littéraux start-menu) → les 3 menus sont FINISSABLES sans toucher leurs consommateurs partagés. Travail = réécrire le modèle texte string de ces menus (`StringCopy('',x)→string`, `_gsv.gStringVarN=str`, `BagMenu_Print(str)`) en byte (`StringCopy(gStringVar2, encodeOwText(name))`, `BagMenu_Print(bytes)`). **✅ tsc 0 — 3 menus migrés byte** (bag-screen/bag-menu/bedroom-pc : modèle string→byte ; `setStringVar` byte ; `_printDescription`/`BagMenu_Print`/`_itemStoragePrintWindowMessage`/`_showSticky` byte ; noms encodés AU SITE via `encodeOwText` ; fns de mesure `GetStringWidth`/`RightAlign`/`CenterAlign` + `AddTextPrinterParameterized3` élargies `string|Uint8Array`). **Dup `StringExpandPlaceholders` RÉSOLUE** (1 seule def = miroir). user A/B = texte OW marche (dialogues confirmés ✅ ; Vite transpile sans type-check donc tournait même avant tsc 0). ⚠️ Valider en jeu : sac / PC chambre / dépôt objets / écrans à `{STR_VAR}`. **✅ texte OW 1:1 strict À 100% (2026-06-06)** : (a) **`list_menu` byte FAIT** — `ListMenuItem.name`/`ListMenuPrint`/`_spawnMultichoiceMenu`/`getMultichoiceList` = `string|Uint8Array` ; bag (`GetItemNameFromPocket` via buffer FRAIS par item = 1:1 `StringCopy(name[i], gStringVar4)`, évite le piège ref partagée gStringVar4) + multichoice produisent byte ; **les 2 ponts `decodeOwBytes` RETIRÉS**. user A/B = sac / PC chambre / dépôt / dialogues OK, boot 0 erreur. **DETTE RESTANTE (non-bloquante)** : Stage 1 data extraction byte natif (notre préproc `encodeOwText` au load = déjà fonctionnel → cosmétique) ; save playerName `u8[]` (Stage 4). **Bonus** : `?debug` → `playerName='DEBUG'` (boot-mode ; byte-level 1:1 sans fallback → un nom vide rendait `{PLAYER}` vide). `decodeOwBytes` reste (utilisé par GetCurrentFieldMessageText debug). **🐛 FIX 2026-06-06 (bug user : panneau « MAISON DE » sans nom)** : `strings.ts` a sa PROPRE charmap `_textCharmap` (init par `InitTextData`) DISTINCTE de `getOwCharmap` (gba-text-system) ; `InitTextData` n'était jamais appelé au boot → `_textCharmap` null → `EncodePlayerNameFR`/placeholders FR rendaient des ESPACES (byte 0). Fix = appeler `InitTextData(charmap)` dans `gba-text-system.loadFontData` après chargement charmap. Vérifié runtime : `{PLAYER}`→DEBUG, `{RIVAL}`→FLORA. ⚠️ LEÇON : 2 charmaps (text.ts/encodeOwText via getOwCharmap OK + strings.ts/encodeFR via _textCharmap) → toute fn d'encodage runtime doit avoir sa charmap branchée au boot. cf. plan doc.
**▶ RELOCALISATION RENDERER TEXTE engine→miroir (user 2026-06-06 : « oui lance ») — EN COURS PAR VAGUES.**
- **✅ VAGUE 1 = `text_window.c`→`src/game/text_window.ts` FAITE + vérifiée runtime (2026-06-06).** Détail dans la table Phase A (ligne text_window.c). Pattern de bascule rodé : SOURCE miroir + signatures 1:1 + HW importé d'engine + consolidation 3-fichiers→1 + re-export hub transitoire + global-scope préservé via `export *`. tsc 0, dialog box vérifiée.
- **☐ VAGUE 2+ = `text.c`→`src/game/text.ts` (le CŒUR, 1904 l. décomp).** `src/game/text.ts` existe déjà (wrapper `encodeOwText`) → l'ÉTENDRE. Source actuelle (noms NON-1:1) = `engine/ui/gba-text-printer.ts`(998 : `runTextPrinter`=RenderText do/while, `addTextPrinter`, `encodeStringForFont`, glyphes/blit) + `gba-text-system.ts`(724 : `AddTextPrinterParameterized*`/`RunTextPrinters`/`GetStringWidth`/`GetFontAttribute`/`DeactivateAllTextPrinters`). **ÉCART STRUCTUREL mesuré** : `RenderText` décomp = `switch(state)` + `currentChar` pointeur + `DecompressGlyph_*`/`CopyGlyphToWindow`/`gCurGlyph` par char + `gFonts`/`FontFunc_*`/`RenderFont` ; l'engine = do/while + index + glyphes pré-calc, PAS de gFonts/FontFunc. Le rendu actuel est **A/B-validé sur ROM** → contrat *baseline-vérifiée-puis-raffiner* : sous-vagues = (2a) constantes/enums/structs/globals 1:1 (FONT_*/RENDER_STATE_*/FONTATTR_*/gTextPrinters/gFonts/gCurGlyph) ; (2b) fns logiques périphériques (GetStringWidth/GetFontAttribute/GetMenuCursorDimensionByFont/Deactivate/IsActive/Save+RestoreTextColors) ; (2c) cœur (AddTextPrinter/RunTextPrinters/RenderFont+FontFunc_*/RenderText state-machine) ; (2d) nettoyage + bascule importeurs. **HW reste engine** (DecompressGlyph/blit/DMA/window framework). Noms de fns 1:1 décomp partout.
Convention (rappel) : `text.c`→`src/game/text.ts` (+ `text.h`→`include/`), noms 1:1 (`RenderText`/`RunTextPrinters`/`AddTextPrinter*`/`GetStringWidth`…). Source actuelle (noms NON-1:1) = `engine/ui/gba-text-printer.ts`(998) + `gba-text-system.ts`(724) + `gba-window-system.ts`(946) = **2668 l. / 41 importeurs**. **⚠️ le HW (DecompressGlyph/CopyGlyphToWindow/blit/DMA/window framework) RESTE fourni par engine** (règle ledger : pas un target 1:1 ligne-à-ligne) → relocaliser surtout la **LOGIQUE** (state machine RenderText, AddTextPrinter, mesures de largeur, EXT_CTRL handling, RENDER_STATE_*, TextPrinter struct) ; le HW exposé via `include/gba/*` ou re-export. Convention : `src/game/X.ts` = SOURCE, l'ancien `engine/ui/*.ts` = re-export transitoire, puis basculer les 41 importeurs, puis supprimer. Procéder par vagues tsc-guidées (Vite transpile sans type-check → le jeu boot même tsc rouge, mais viser tsc 0 par vague). **Aussi en dette** : save `playerName`→`u8[]` (Stage 4, couplé save).
1. ✅ FAIT (2026-06-05) : helpers cross-module event_data (Temp/Daily/Mystery/RTC). RESTE = **National Dex** (event_data.c:55-80) à porter AVEC le module pokedex (gSaveBlock2.pokedex + DEX_MODE_NATIONAL + ResetPokedexScrollPositions).
2. 🧭 **INSIGHT 2026-06-05 : feuilles PURES ÉPUISÉES.** Sondage des candidats field (field_poison/door/tasks, party_menu) → presque tout le reste field/combat/menus couple le **modèle pokemon** (gPlayerParty BoxMon vs gSaveBlock1Ptr.playerParty PokemonInstance = split V/L) OU le **HW** (tilemap/sprite/window). Ex `field_poison.c` (7 fns) bloqué : `GetMonData(gPlayerParty)` + `GetAilmentFromStatus` (dans party_menu.c, UI).
3. **→ PROCHAIN DÉBLOCAGE = migration modèle pokemon** (gPlayerParty=BoxMon canonique + bridge PokemonInstance). Débloque field_poison/party_menu/summary/daycare/evolution/fusion combat V/L. **GROS + RISQUÉ (party affiché partout + save) → À FAIRE AVEC L'USER pour l'A/B, pas en autonomie.** Pattern = stockage 1:1 + bridge (cf. event_data). **✅ CONFIRMÉ 2026-06-05 (exploration post-purs)** : `GetAilmentFromStatus`/`_ailmentFromStatus` (party-screen), `field_poison`, `GetLevelFromMonExp`, `updateHealthboxStatus` (battle-healthbox) — TOUS bloqués par le **status STRING `'PSN'`/`currentHp` de PokemonInstance** vs `u32`/BoxMon décomp (porter les fns 1:1 u32 = code mort sans caller). **Périmètre PUR autonome ÉPUISÉ.** Les tables/calculs de combat (type/nature/stat/exp) sont déjà 1:1+centralisés (pas de dup).
4. ◑ **ENTAMÉ (2026-06-05)** : `src/game/pokemon.ts` amorcé (nature/stat/unown purs + 4 dups consolidées, vérifié). Continuer les fns pures restantes (`GetGenderFromSpeciesAndPersonality` [3 copies dont 1 string → lié migration species/data], `GetUnownLetterByPersonality` impl, `GetLevelFromMonExp/BoxMonExp`, `GetGMaxTargetSpecies`…) PUIS la migration de stockage `PokemonInstance→BoxMon` AVEC user (A/B).
   🟢 **MIGRATION MODÈLE POKÉMON LANCÉE (user a CHOISI, 2026-06-06)** — unifier `gPlayerParty` sur `Pokemon` (BoxMon) 1:1, retirer le split V/L. **✅ PHASE 0 (audit)** : le modèle cible EXISTE + est 1:1-COMPLET (`interface Pokemon` battle/party-storage.ts:151 + `GetMonData`/`SetMonData` couvrent TOUS les MON_DATA_* + `CalculateMonStats`) → zéro modèle à construire ; pont AVANT `pokemonInstanceToPokemon()` existe (party-storage:515, copie V→L) ; `PokemonInstance` (legacy) = **219 usages / 27 fichiers**. **✅ PHASE 1 (adaptateur inverse) + vérifiée** : `pokemonToPokemonInstance(mon)` (engine/pokemon/pokemon.ts, import `type Pokemon` SEUL = zéro cycle) — round-trip 17 champs cœur OK. **✅ PHASE 2-ENABLER (vue LIVE) + vérifiée** : `makePokemonInstanceView(mon): PokemonInstance` (Proxy : get=dérive, set top-level→écrit `mon.*` backing ; status enum↔STATUS1) — read+write propagés vérifiés runtime. Les 2 = ADDITIFS, tsc 0, zéro risque boot, rien commité. **RESTE PHASE 2 = LE CÂBLAGE (switch source de vérité, BRICK-RISK, A/B user)** : (1) peupler gPlayerParty depuis block1.playerParty au load/boot OW ; (2) `block1.playerParty = gPlayerParty.map(makePokemonInstanceView)` ; (3) router création vers gPlayerParty ; (4) save sérialise Pokemon + migration ; (5) combat L lit gPlayerParty direct (retire copie V→L). Puis P3 (flip appelants adaptateur→GetMonData par sous-système) + P4 (retirer PokemonInstance + battle-flow voie V = un modèle/une voie). cf. mémoire `mirror-port-1to1` (bloc MIGRATION POKÉMON).
5. National Dex (event_data.c:55-80, avec module pokedex). · Cosmétique : imports MB_* du miroir metatile → ENUM_MB_0.

**VÉRIF EN JEU** : reload preview (`pokemon-web-demo`, port 5173) **via `?debug`** (= OW direct, PAS l'intro). Combat L = `&` puis `'`. Keymap : **flèches**=directions, **w**=A, **x**=B. tsc depuis `D:\Projet 1\pokemon-web-demo` (`npx tsc --noEmit`). Dédup-check : `node scripts/check-duplicate-helpers.mjs`. **JAMAIS commit/push** (sauf demande). **PAS d'agent** (sauf devtools post-vague si l'user le redemande).

---

## 🔌 COUCHE MATÉRIEL / CONSOLE — PAS un target 1:1 (fournie par l'émulation `engine/{system,gba}`)
Ces `.c` décrivent le HW GBA que notre runtime émule (divergence légitime JS≠HW). On EXPOSE l'API qu'ils
offrent (via `include/gba/*.ts`), on ne les « 1:1 »e pas ligne-à-ligne.
`main.c` (runtime/IntrTable/ReadKeys) · `m4a.c`/`m4a_tables.c`/`sound.c` · `agb_flash*.c` · `siirtc.c` ·
`dma3_manager.c` · `gpu_regs.c` · `io_reg.c` · `bg.c`/`blit.c`/`window.c`/`sprite.c` (framework — partiel,
la logique de placement OAM/window EST portée côté game) · `malloc.c` · `mini_printf.c`/`libisagbprn.c` ·
`librfu*.c`/`AgbRfu_LinkManager.c`/`multiboot.c` · `decompress.c` (LZ77 — wrap émulation).

## 🚫 HORS-SCOPE — exclu (pré-camion + sous-systèmes différés)
**Pré-camion** : `intro.c` · `intro_credits_graphics.c` · `title_screen.c` · `main_menu.c` · `credits.c` ·
`rom_header_gf.c`. (Birch/boot = séparé, intact.)
**Link/sans-fil/échange** : `link*.c` · `cable_*.c` · `union_room*.c` · `record_mixing.c` ·
`recorded_battle.c` · `trade.c` · `mystery_*.c`/`wonder_news.c` · `berry_fix_*.c`.
**Battle Frontier** : `battle_dome/factory/factory_screen/palace/pike/pyramid/pyramid_bag/tower/tent/arena.c` ·
`frontier_*.c` · `apprentice.c` · `trainer_hill.c`.
**Contest** : `contest*.c` · `use_pokeblock.c`/`pokeblock*.c` (contest-side).
**PokéNav** : `pokenav*.c` · `match_call.c`.
**Mini-jeux/divers** : `roulette.c` · `slot_machine.c` · `berry_blender.c`/`berry_crush.c`/`berry_powder.c` ·
`dodrio_berry_picking.c` · `pokemon_jump.c` · `ereader_*.c` · `bard_music.c` · `dewford_trend.c` ·
`mauville_old_man.c` · `lilycove_lady.c` · `faraway_island.c` · `mirage_tower.c` · `rayquaza_scene.c` ·
`hall_of_fame.c`/`hof_pc.c` · `roamer.c`(→F) · `trainer_card.c` · `diploma.c` · `walda_phrase.c` ·
`pokemon_storage_system.c`(PC — gros, différé) · `decoration*.c` · `secret_base.c` · `slot/lottery_corner.c` ·
`image_processing_effects.c` · `confetti_util.c` · `minigame_countdown.c` · `digit_obj_util.c`.

---

## ✅ IN-SCOPE — le vrai jeu post-camion (cibles 1:1)

### Phase A — feuilles / fondation
| décomp .c | src/game/ | statut | notes |
|---|---|---|---|
| random.c | random.ts | ✅ | PROOF-OF-CONVENTION fait : `src/game/random.ts`+`include/random.ts` créés ; `system/random.ts`=re-export transitoire (source unique, 0 dup) ; `SeedRngAndSetTrainerId`/`GetGeneratedTrainerIdLower` (main.c) dé-mixées + flaggées. tsc 0 + boot OK + RNG vérifié runtime. |
| util.c | util.ts | ◑ | **Pures FAITES + vérifiées 1:1** : gBitTable, sCrc16Table(256, **prouvée via CRC bit-à-bit==table**), CountTrailingZeroBits, CalcCRC16(WithTable), CalcByteArraySum, Store/LoadWord. **gBitTable consolidé 100%** (battle-controllers re-exporte le miroir ; battle-action-selection:116 + battle-main-functions:276 importent `gBitTable as _gBitTable` du miroir — vérifié 2026-06-05, 0 copie locale restante). **HW-couplées TODO** (couche sprite/palette/affine) : CreateInvisibleSpriteWithCallback, DoBgAffineSet, CopySpriteTiles, BlendPalette. tsc 0. |
| trig.c | trig.ts | ✅ | `src/game/trig.ts`+`include/trig.ts`. gSineTable=réexpose G_SINE_TABLE ; gSineDegreeTable transcrit (180) + **vérifié runtime 1:1** (anchors + diff). decomp-helpers Sin/Cos/gSineTable redirigés (0 dup). NB : `& 0xFF` ancien = ÉQUIVALENT (table périodique), pas un bug. tsc 0. |
| math_util.c | math_util.ts | ✅ | `src/game/math_util.ts`+`include/`. 9 fns fixed-point, trunc-vers-zéro (Math.trunc), s16 via `<<16>>16`, s64 via BigInt. **Vérifié runtime 1:1** (discriminant Div16(-1,3)=-85≠floor). Ancien `decomp-data/math_util-data` = métadonnées seules (pas d'impl). tsc 0. |
| string_util.c | string_util.ts | ◑ STAGE 0 FAIT | **Toutes les fns SANS ÉTAT portées + vérifiées 1:1 (2026-06-05, 2 passes).** _1ère passe_ : StringCopy(_Nickname/_PlayerName) · StringGet_Nickname · StringAppend(N) · StringCopyN · StringLength · StringCompare(N) · IsStringLengthAtLeast · StringFill · StringCopyPadded · StringFillWithTerminator · Convert{Int,UInt}ToDecimalStringN / ConvertIntToHexStringN. _2e passe_ : **StringBraille · StringCopyN_Multibyte · StringLength_Multibyte · WriteColorChangeControlCode · IsString[N]Japanese · GetExtCtrlCodeLength · SkipExtCtrlCode(static) · StringCompareWithoutExtCtrlCodes · ConvertInternationalString · StripExtCtrlCodes**. **Sémantique pointeur `u8*` = vue `Uint8Array.subarray`**. `sExtCtrlCodeLengths` = init désigné 1:1 (string_util.c:659, indices 0..RESUME_MUSIC). 🐛 **vrai bug 1:1** : `WriteColorChangeControlCode` local d'easy-chat OMETTAIT `*dest=EOS` (c:625) → consolidé vers miroir (écrit EOS) ; decomp-bridge no-op stub → re-export miroir. **Vérif headless 10/10 valeurs exactes** (lenMB=3 · copyNMB=[65,249,5,255] · wccc=[252,1,4,255]/[252,3,7,255]/[252,2,9,255] · gecl=[2,4,3,1,1,0] · strip=[65,66,255] · cmpEq=0/cmpAB=-1 · braille=[252,6,6,1,65,255] · ciJpn=[252,21,65,66,252,22,255]). tsc 0, dédup-check clean, boot 0 erreur console. **✅ STAGE 0 byte-level FAIT + VÉRIFIÉ HEADLESS 18/18 (2026-06-05)** : gStringVar1-4/sUnknownStringVar (Uint8Array) + `StringExpandPlaceholders` (récursif, c:335) + `GetExpandedPlaceholder` (c:499) + 14× `ExpandPlaceholder_*` + NOUVEAU `strings.ts` (12 placeholders FR + bridge `InitTextData`/`EncodePlayerNameFR`). NON-BREAKING (coexiste voie ASCII gba-text-system jusqu'au flag `__USE_DECOMP_TEXT__`). Fallthrough ext-ctrl décomp rendu par compteur `nArgs` (`noFallthroughCasesInSwitch`). **RESTE = Stages 1-5** (extraction 174 data token→byte, renderer glyphe byte-level, flip ~57 callers, save playerName, retrait flag) = **flag + A/B AVEC user**. cf. `docs/TEXT-DATA-1TO1-MIGRATION-PLAN.md`. |
| text.c | text.ts | ✅ logique (HW=engine) | **✅ VAGUE 2b-1 RELOCALISATION RENDERER — attributs de font (2026-06-06)** : `FONT_*` (10) + `FONTATTR_*` (8) + `TEXT_SKIP_DRAW` + `FontInfo` + `sFontInfos` (text.c:119-221) + `GetFontAttribute` (text.c:1645) + `sMenuCursorDimensions` (text.c:223-235) + `GetMenuCursorDimensionByFont` (text.c:1678) **déplacés engine→`src/game/text.ts`** (1:1) ; `include/text.ts` re-exporte (surface text.h) ; `gba-text-system` importe en tête (usage interne : default params fontId + GetFontAttribute dans AddTextPrinterParameterized3/4) + ré-exporte (consommateurs). **tsc 0 · dédup 0 fn dup · boot sain (cycle `gba-text-system↔text.ts` runtime-safe, comme l'archi existante) · texte rendu vérifié runtime** (ShowFieldMessage → bordure + texte OK). **✅ VAGUE 2b-2 GetStringWidth FAITE + vérifiée (2026-06-06)** : port 1:1 COMPLET (text.c:1328 state-machine byte-level — multi-ligne **MAX**, EXT_CTRL skip par code, placeholders gStringVar1-3/dynamic, EXTRA_SYMBOL `0x100|sym`, KEYPAD_ICON) + `GetGlyphWidth_{Small,Normal,Short,Narrow,SmallNarrow}` + `GetFontWidthFunc` + `sGlyphWidthFuncs` + `GetKeypadIconWidth` + `GetStringRightAlign/CenterAlignXOffset`, déplacés engine→miroir ; width-tables HW exposées via `getFontGlyphWidths` (engine) ; gba-text-system ré-exporte ; constantes EXT_CTRL via `characters-data` (valeurs décomp). **Vérif HEADLESS : 19/19 libellés FONT_NORMAL = largeurs IDENTIQUES (allMatch, 0 régression layout) ; multi-ligne `AAA\nBBBBBBBB` 66 (somme = BUG engine) → 48 (MAX = 1:1 correct).** tsc 0, dédup 0 fn dup, boot 0 err. 🔴 **DETTE 1:1 trouvée** : `gba-text-printer` a les `EXT_CTRL_CODE_*` DÉCALÉS +1 (PAUSE 0x09 vs 0x08 décomp ; idem PAUSE_UNTIL_PRESS/WAIT_SE/PLAY_BGM/ESCAPE) ; `characters-data` (utilisé ici) = valeurs décomp CORRECTES ; les codes des labels (COLOR/HIGHLIGHT/SHADOW/FONT/CLEAR/SKIP/CLEAR_TO/MIN_LETTER_SPACING) sont identiques dans les 2 → mesures OK. À corriger globalement (encodeur+runtime) dans une vague dédiée. **🔬 ANALYSE 2c FAITE (2026-06-06)** : RenderText décomp (text.c:934-1224) lu intégralement (switch(state) HANDLE_CHAR/WAIT/CLEAR/SCROLL_START/SCROLL/WAIT_SE/PAUSE + sous-switch currChar) + FontFunc_* + RenderFont (boucle sur RENDER_REPEAT) + CopyGlyphToWindow. Modèle décomp = **RenderText 1 char/appel + RENDER_REPEAT** (RenderFont reboucle) vs engine `runTextPrinter` do/while. Plan : porter RenderText+RenderFont+FontFunc_* dans le miroir, `runTextPrinter` délègue, HW (blit/glyphData/down-arrow) reste engine. `_getTextInputState()` exposé (gba-text-printer) = prep. **🔴🔴 BLOC PRÉ-REQUIS 2c-prep découvert** : `gba-text-printer` a les `EXT_CTRL_CODE_*` DÉCALÉS +1 (PAUSE 0x09…) et `runTextPrinter`+l'encodeur sont cohérents entre eux dessus ; `GetStringWidth` (déjà porté) utilise `characters-data` (valeurs correctes 0x08). Porter RenderText dans `text.ts` mettrait 2 jeux `EXT_CTRL_CODE_*` contradictoires dans le même fichier → **il FAUT d'abord unifier** les `EXT_CTRL_CODE_*` sur les valeurs décomp (corriger gba-text-printer **+ `encodeStringForFont`** ensemble, A/B dialogues/pauses) AVANT de porter RenderText. = **2c-prep** ✅ **FAIT + vérifié runtime (2026-06-06)** : `gba-text-printer` importe les `EXT_CTRL_CODE_*` depuis `characters-data` (source unique, valeurs décomp) au lieu de ses 14 defs locales (dont 5 décalées +1 : PAUSE 0x09→0x08, PAUSE_UNTIL_PRESS/WAIT_SE/PLAY_BGM/ESCAPE) → encodeur (`encodeStringForFont`) + runtime (`runTextPrinter`) + mesure (`GetExtCtrlCodeLength`) cohérents 1:1 ; hardcode `main-menu-impl:977` (0x09 → `import EXT_CTRL_CODE_PAUSE`). tsc 0, dédup 0 dup, 0 err console. **Non-régression** : OW dialogue rend + **combat boot + « Un MEDHYENA sauvage apparaît!▼ » + down-arrow ▼** (= `\p`/`PAUSE_UNTIL_PRESS` unifié 10→9 : battle-message encode 9, runTextPrinter décode 9). Corrige aussi une incohérence LATENTE (`{PAUSE N}` OW encodé 9 mais `GetExtCtrlCodeLength` lisait length 1 au lieu de 2). **→ blocage 2c levé (1 seul jeu EXT_CTRL).** Reste avant RenderText : —. Ensuite **2c** ✅ **FAIT + vérifié runtime (2026-06-06)** : `RenderText` (state-machine `switch(state)` 1:1 text.c:934) + `renderHandleChar` (pacing + consommation chars, glyphe via `blitGlyphToWindow` HW engine) + `RenderFont` (boucle `RENDER_REPEAT`, text.c:352) portés dans `src/game/text.ts` ; `runTextPrinter` (gba-text-printer) **délègue** `return RenderText(printer)`. Cycle gba-text-printer↔text.ts runtime-safe (usages dans fonctions). Constantes EXT_CTRL via characters-data (post-2c-prep). **tsc 0, dédup 0 dup (délégation, pas de corps dupliqué), boot sain (cycle init OK), 0 err console.** Rendu NON-RÉGRESSIF vérifié : OW dialogue (« Le moteur RenderText tourne ») + **combat « Un MEDHYENA sauvage apparaît!▼ » avec down-arrow ▼** rendent identiques. **DETTE résiduelle vers 1:1 pur (raffinements)** : (a) états WAIT/CLEAR/SCROLL_START/SCROLL pilotés par la boucle de scène (early-return UPDATE = modèle engine) au lieu d'être centralisés dans RenderText (TextPrinterWait/Scroll/DownArrow non encore portés) ; (b) per-char garde le `do/while` (control codes instantanés) au lieu du modèle décomp strict 1-char+RenderFont-loop (où un control coûte 1 tick de delay) — `RenderFont` est porté mais `RunTextPrinters` appelle encore `runTextPrinter`→`RenderText` (pas `RenderFont`) ; (c) `DecompressGlyph`/`CopyGlyphToWindow`/`gCurGlyph` non portés (glyphe via blit engine direct). **⚠️ A/B user demandé** : pacing typewriter, scroll de page `\l`, bobbing du ▼. ✅ **VAGUE 2e FAITE + vérifiée (2026-06-06)** : couche de GESTION relocalisée dans `src/game/text.ts` — `gTextPrinters` (registre, `const` muté in-place) + `ActivePrinter` + `_dropPrintersForWindow` + `AddTextPrinterParameterized`/`3`/`4` + `_addTextPrinterParameterizedCore` + `AddTextPrinterForMessage`/`WithCallback` + `IsTextPrinterActive` + `_debugGetTextPrinters` + `ClearTextPrinters` + `DeactivateAllTextPrinters` ; `gba-text-system` ré-exporte + expose le font-state HW (`getFontGlyphData`/`getFontGlyphWidths`/`resolveFontForMirror`/`getDownArrowPixels`/`getDarkDownArrowPixels`/`getPlayerTextSpeedDelay`). **`RunTextPrinters` (driving scroll/down-arrow/input) reste en engine** (importe `gTextPrinters` du miroir) — sa centralisation 1:1 dans `RenderText` = raffinement A/B restant. tsc 0, dédup 0 dup, boot sain (cycle gba-text-system↔text.ts runtime-safe), 0 err, OW dialogue + combat « Un MEDHYENA…!▼ » NON-RÉGRESSIFS. **→ logique text.c ~98% dans le miroir.** ✅ **VAGUE 2 RAFFINEMENT 1 FAIT + vérifié runtime (2026-06-06)** : driving CENTRALISÉ dans `RenderText` — états `WAIT`/`CLEAR`/`SCROLL_START`/`SCROLL`/`WAIT_SE` portés 1:1 (text.c:1167-1214) + helpers `TextPrinterWait`/`TextPrinterWaitWithDownArrow`/`TextPrinterWaitAutoMode`/`TextPrinterClearDownArrow` (text.c:850-899/838) + `sWindowVerticalScrollSpeeds` (text.c:76) + `getPlayerTextSpeed` (1:1 menu.c:474, exposé engine, honore `forceMidTextSpeed`) ; `RunTextPrinters` réduit à la **boucle 1:1 décomp** (text.c:319 : `RenderFont` + `finished`, driving retiré) ; instant-loop `_addTextPrinterParameterizedCore` → `RenderFont` (text.c:303-308) ; `PAUSE_UNTIL_PRESS`→`RENDER_STATE_WAIT` (1:1, plus l'alias CLEAR) ; `PAUSE`→`RENDER_UPDATE` toujours (1:1, plus REPEAT same-frame) ; champ `autoScrollDelay` ajouté au printer ; HW de driving (`scrollWindow`/`fillWindowPixelBuffer`/`fillWindowPixelRect`/`textPrinterDrawDownArrow`) + `PlaySE`/`IsSEPlaying`/`SE_SELECT` importés du HW engine/globals (cycle text.ts↔decomp-globals runtime-safe). Imports morts nettoyés dans gba-text-system. **Vérifié runtime via vraie field-message 3 pages** (`ShowFieldMessage` bytes charmap réelle) : page1→`\p`=CLEAR (▼ bobbing, `downArrowYPosIdx`↑ chaque frame) → A(=W) → clear fenêtre + reset curseur (y 3→1) → page2 typewriter (cc↑) → `\l`=SCROLL_START (▼) → A → SCROLL (scrollDistance, ~8 frames) → page3 ; screenshots page1(▼)/page3 OK, cc final 77 exact (23+1+26+1+26), dialogue refermé propre (`hidden:true`), 0 err console. tsc 0, dédup 0 dup. ✅ **RAFFINEMENTS 2 + 3 FAITS + vérifiés runtime (2026-06-06) → text.c LOGIQUE ~100% dans le miroir.** **(2) Modèle glyphe** : `gCurGlyph` (struct `TextGlyph`) + `DecompressGlyph_Small/Normal/Short/Narrow/SmallNarrow` (dispatch sur `printer.fontId` = subStruct.fontId ; lisent la glyph data GLOBALE `getFontGlyphData(FONT_X)` → plus de cache glyphData/glyphWidths/resolveFont par-printer) + `CopyGlyphToWindow` + `ClearTextSpan` portés 1:1 (text.c:596/649/1683+) ; **EXTRA_SYMBOL fold** dans le chemin normal (glyphId `0x100|sym`, plus de cas séparé) ; avance curseur 1:1 (minLetterSpacing / japanese / latin = width-only) ; **`MIN_LETTER_SPACING` corrigé** (→ `minLetterSpacing`, pas `letterSpacing`) ; champs printer `fontId`/`japanese`/`minLetterSpacing` ajoutés. HW = `blitGlyphToWindow` engine (latfont pré-décodé = frontière). Rendu OW **pixel-identique** (screenshots glyphes/chiffres/casse/`\n`), combat boote clean ; `letterSpacing=0` chez TOUS les appelants live → l'avance latine width-only = **zéro-impact observable**. **(3) Registre** : `sTextPrinters` (1:1 text.c:39, indexé par windowId, `.active` sur le struct `TextPrinter`) remplace `gTextPrinters: ActivePrinter[]` (wrapper retiré, `_dropPrintersForWindow` retiré) ; tableau **CREUX** (windowId MONOTONE non-borné = window-system engine ≠ pool fixe `[WINDOWS_MAX]` HW) ; `AddTextPrinter*` → `sTextPrinters[windowId]=printer` + `.active` ; `RunTextPrinters` = boucle 1:1 sur slots actifs (text.c:319-343, `active=false` à FINISH) ; `IsTextPrinterActive(id)=sTextPrinters[id].active` ; `DeactivateAllTextPrinters` 1:1 (active=false, ne vide pas) ; devtools `dev.printers` adapté. Vérifié runtime (printer placé au slot windowId, `active` true→false à FINISH, IsTextPrinterActive transitions, message rendu+fini cc exact). tsc 0, dédup 0 dup, 0 err console. ✅ **STRUCT IMBRIQUÉE FAITE + vérifiée runtime (2026-06-06)** : `TextPrinter` = `printerTemplate` (1:1 text.h:64) + `subStruct` (1:1 text.h:53, objet nommé — PAS le blob `subStructFields[7]` casté) + champs top-level (`active`/`state`/`textSpeed`/`delayCounter`/`scrollDistance`/`minLetterSpacing`/`japanese`) + extensions marquées (`encodedString`/`window`/`instantPath`/`pauseCounter`/`downArrow*`/`onCharRendered`) ; tous les accès `printer.X` → `printer.printerTemplate.X` | `printer.subStruct.X` (rename sed word-boundary, tsc-vérifié ; dispatch glyphe sur `subStruct.fontId` = font COURANT) ; test 5 points (accents / `\n` / `\l` scroll / `{PLAYER}`→DEBUG / close-A) **NON-RÉGRESSIF**. **→ text.c = renderer + state-machine + driving + mesures + gestion + glyphe + registre + STRUCT = 100% 1:1 STRUCTUREL DANS LE MIROIR.** Restent SEULES des **FRONTIÈRES engine/scope (PAS de la dette portable)** : (a) JP glyph path = nécessite `gFontXJapaneseGlyphs` ABSENTS d'un build FR (le décomp lui-même gate derrière `isJapanese` ; signature `(glyphId, isJapanese)` 1:1 préservée) ; (b) `sTextPrinters` array creux indexé-windowId vs `[WINDOWS_MAX]` fixe = forcé par le windowId MONOTONE de `window.c` (couche HW engine) ; (c) HW blit/scroll/window framework/font load = engine PAR DESIGN (frontière déclarée dans la directive). Aucune dette text.c portable restante. [ancien plan] **2c** RenderText (modèle 1-char/RenderFont-loop, délégation, A/B user). RESTE AUSSI : RENDER_STATE_*/RENDER_* + struct TextPrinter (2b-3). [Ancien plan] **2c** RenderText state-machine (engine `runTextPrinter` do/while → `switch(state)` 1:1, A/B user) + AddTextPrinter/RunTextPrinters/RenderFont/FontFunc_* ; **2d** bascule importeurs. HW glyphe (DecompressGlyph/CopyGlyphToWindow/blit) reste engine. <br>**`src/game/text.ts` (+`include/`) créé (2026-06-05, chantier TEXTE Stage 1/2)** : flag `__USE_DECOMP_TEXT__` (`isDecompTextEnabled`, défaut OFF) + `encodeOwTextSource` = **cœur STAGE 1** (encodeur source→bytes charmap 1:1 : `{STR_VAR_1}`→`[0xFD,2]` etc. + reste délégué à `encodeStringForFont`, 0 dup ; calqué sur `encodeTemplate`). **Vérifié headless 10/10 par ÉQUIVALENCE-BYTES** (voie byte STAGE 0 === voie ASCII actuelle). Le RENDERER (RenderText/RunTextPrinters/AddTextPrinter/glyphes) est DÉJÀ porté en `engine/ui/gba-text-printer.ts` (`runTextPrinter`=RenderText, consomme bytes charmap — prouvé par le combat byte-level) → **Stage 2 essentiellement fait** ; relocalisation 1:1 dans le miroir = plus tard. RESTE (flag+A/B user) : intégrer `encodeOwTextSource` à l'extraction (régénérer 174 data en bytes) + brancher gStringVar byte live. cf. `docs/TEXT-DATA-1TO1-MIGRATION-PLAN.md`. |
| text_window.c | text_window.ts | ✅ | **VAGUE 1 RELOCALISATION RENDERER FAITE + vérifiée runtime (2026-06-06).** `src/game/text_window.ts` (+`include/`) = SOURCE 1:1 COMPLÈTE des 13 fns de text_window.c (`GetWindowFrameTilesPal`/`LoadMessageBoxGfx`/`LoadWindowGfx`/`LoadUserWindowBorderGfx(_)`/`DrawTextBorderOuter` + **3 ajoutées 1:1** : `DrawTextBorderInner`/`rbox_fill_rectangle`/`LoadUserWindowBorderGfxOnBg` + `GetTextWindowPalette`/`GetOverworldTextboxPalettePtr`) + `preloadTextWindowFrames` (pont asset). **Signatures remises 1:1 décomp** (`windowId`-first, `bg=GetWindowAttribute(windowId,WINDOW_BG)` ; l'engine divergeait en `bg`-first). HW importé d'engine (`LoadBgTiles`/`FillBgTilemapBufferRect`/`GetWindowAttribute`/`writePalette` inline tant que `LoadPalette` pas exposé). **Consolidation 3-fichiers→1** : retiré `LoadMessageBoxGfx` de `gba-window-system` + `GetTextWindowPalette/Overworld` de `decomp-bridge` (= defs dupliquées supprimées) ; `gba-text-window.ts` = re-export pur (hub pour ses 16 importeurs) ; `decomp-globals` `export * from '../../game/text_window'` (réexpose au global-scope, cycle runtime-safe comme gba-window-system↔decomp-globals existant) ; `decomp-bridge` re-exporte les 3 fns depuis le hub ; 4 importeurs LoadMessageBoxGfx (bag-menu/bedroom-pc/start-menu/field-message-box) basculés sur le hub ; `list-menu:1660` corrigé (passe windowId, pas le bg résolu). **tsc 0 · dédup 0 fn dup · boot sain · OW + transition de map OK · dialog box LoadMessageBoxGfx vérifié runtime (bordure vert/cyan + « SALUT le monde » via ShowFieldMessage).** DETTE héritée non-régressée : `sTextWindowPalettes` pas préchargé→`GetTextWindowPalette`=null ; `LoadPalette` inline. **✅ **VAGUE 2d FAITE + vérifiée (2026-06-06)** : 17 importeurs de `gba-text-window` basculés → `src/game/text_window` directement ; `decomp-bridge` re-exporte du miroir ; **`gba-text-window.ts` SUPPRIMÉ** (hub transitoire éliminé) ; 0 import résiduel ; tsc 0, boot + dialogue OW vérifiés (LoadMessageBoxGfx/LoadUserWindowBorderGfx résolvent du miroir), 0 err console. (Restent des mentions `gba-text-window` en COMMENTAIRES historiques = cosmétique.) **→ text_window 100% relocalisé dans le miroir.** [obsolète] RESTE bascule finale** : ~20 importeurs `gba-text-window`/`decomp-bridge`→`src/game/text_window` direct, puis supprimer `gba-text-window.ts`. |
| task.c | task.ts | ☐ | gTasks (existant system) |
| sprite.c | sprite.ts | ☐ | OAM engine (existant system/sprite) — partiel HW |
| palette.c | palette.ts | ☐ | (existant system/palette) |
| menu.c | menu.ts | ◑ (par vagues) | **✅ VAGUE 1 (text-speed/run) FAITE + vérifiée (2026-06-06)** : `src/game/menu.ts`+`include/` créés ; `sTextSpeedFrameDelays` (menu.c:77) + `GetPlayerTextSpeed` (474) + `GetPlayerTextSpeedDelay` (481) + `RunTextPrintersAndIsPrinter0Active` (163) portés 1:1, relocalisés depuis `gba-text-system` (+ **consolidé la 3e copie de `GetPlayerTextSpeed`/`Delay` qui vivait dans `gba-menu-system`** → import miroir + re-export ; signature `(speed?)` non-1:1 retirée). `gba-text-system` + `gba-menu-system` ré-exportent du miroir (consommateurs/expose global intacts) ; `text.ts` importe `GetPlayerTextSpeed` (RenderText SCROLL) + `GetPlayerTextSpeedDelay` (P-core/ForMessage) du miroir. Cycle `text↔menu` runtime-safe (1:1 : text.c appelle GetPlayerTextSpeed de menu.c). **tsc 0, dédup 0 dup (GetPlayerTextSpeed 3→1), boot OK (DEBUG), dialogue OW typé à la bonne vitesse + 2 lignes rendues + fini, 0 err console.** ✅ **VAGUE 2 (chaîne AddTextPrinter) FAITE + vérifiée (2026-06-06)** : `AddTextPrinter` (text.c:271, **cœur partagé**) posé dans text.ts (prend un `AddTextPrinterTemplate` {str,windowId,fontId,x,y,letterSpacing,lineSpacing,colors} + speed + callback) ; `AddTextPrinterParameterized2/3/4` (menu.c:169/1917/1938) + `AddTextPrinterForMessage`/`_2`/`WithCustomSpeed`/`WithCallbackForMessage` (menu.c:191+) RELOCALISÉS text.ts→`menu.ts` (remplissent le template + AddTextPrinter) ; **fusion non-1:1 `_addTextPrinterParameterizedCore` RETIRÉE** ; `AddTextPrinterParameterized` (text.c:251) reste text.ts (1:1 : template via GetFontAttribute + AddTextPrinter) ; gba-text-system ré-exporte P2/P3/P4/ForMessage* du miroir menu.ts (importeurs intacts) ; le sentinel `speed<0`=option joueur (field-message-box) gardé dans P3/P4 (extension documentée). Cycle menu↔text↔gba-text-system runtime-safe (1:1 : menu.c remplit+AddTextPrinter de text.c ; text.c appelle GetPlayerTextSpeed de menu.c). tsc 0, dédup 0 dup, **5 points re-vérifiés runtime** ({PLAYER}→DEBUG + accents été/à/ç/ï/ê + `\n` + `\l` scroll + close-A, screenshots), 0 err console. ✅ **VAGUE 3 (window frames) FAITE + vérifiée runtime (2026-06-06)** : toute la famille bordure-de-fenêtre relocalisée `gba-window-system`→`menu.ts` 1:1 — `DrawDialogueFrame`/`DrawStdWindowFrame`/`ClearDialogWindowAndFrame`/`ClearStdWindowAndFrame` + `WindowFunc_DrawStandardFrame`/`DrawDialogueFrame`/`ClearStdWindowAndFrame`/`ClearDialogWindowAndFrame` + custom-tile `DrawDialogFrameWithCustomTileAndPalette`/`DrawStdFrameWithCustomTileAndPalette` (+WindowFunc_ + statics UNUSED `DrawDialogFrameWithCustomTile`/`DrawStdFrameWithCustomTile`) + transparent `ClearStdWindowAndFrameToTransparent`/`ClearDialogWindowAndFrameToTransparent` (+WindowFunc_*NullPalette/Transparent) + `SetStandardWindowBorderStyle` + `LoadMessageBoxAndBorderGfx`/`LoadMessageBoxAndFrameGfx` + `InitStandardTextBoxWindows`/`InitTextBoxGfxAndPrinters`/`FreeAllOverworldWindowBuffers` + `Menu_LoadStdPal`/`Menu_LoadStdPalAt` (+UNUSED `Menu_GetStdPal`/`Color`) + données (`gStandardMenuPalette` via assetCache + `preloadStdMenuPalette`, `sStandardTextBox_WindowTemplates`, `sYesNo_WindowTemplates`, statics `sTileNum`/`sPaletteNum`/`sStartMenuWindowId`/`sMapNamePopupWindowId`) + consts locaux 1:1 (`DLG_WINDOW_BASE_TILE_NUM=0x200`, `STD=0x214`, palettes 15/14, `PIXEL_FILL`/`BG_TILE_V_FLIP`/`BG_COORD_SET`/`WINDOW_NONE`/`DUMMY_WIN_TEMPLATE`). **🔑 base de tuiles remise à la VALEUR DÉCOMP `0x200`/`0x214`** (était `0xFC` côté engine) — le textbox window (baseBlock 0x194, 27×4=108 tiles) finit pile à 0x200 → bordure contiguë 1:1, 32KB/BG l'héberge ; load (LoadMessageBoxGfx) + draw partagent 0x200 = auto-cohérent. **HW pur AJOUTÉ à engine sous noms décomp** : `CallWindowFunction` (window.c:525, lit gWindows privé) + `COPYWIN_NONE/MAP/GFX/FULL`. **Hub** : `gba-window-system` ré-exporte les fns frame + `DLG_WINDOW_*` du miroir (les ~15 importeurs bag/party/start-menu/bedroom-pc/wallclock/region-map/money-box/pokedex/starter-choose/script-opcodes INTACTS). **Bonus 1:1** : l'`ClearStdWindowAndFrame`/`ClearDialog*` engine déviaient (fill 0 au lieu de PIXEL_FILL(1), copyToVram ignoré, palette 0) → corrigés 1:1 (CallWindowFunction + FillWindowPixelBuffer(PIXEL_FILL(1)) + ClearWindowTilemap + CopyWindowToVram). Cycle `menu↔gba-window-system` runtime-safe (usages dans fonctions). **tsc 0, dédup 0 fn dup, 0 err console ; vérifié runtime à l'écran** (screenshots) : cadre dialog OW se rend PARFAITEMENT à base 0x200 (zéro régression vs 0xFC) · accents `éàçùêîô` · `\n` 2 lignes · `\l` down-arrow ▼ · `{PLAYER}`→DEBUG · close `HideFieldMessageBox` propre (aucun résidu de cadre = chemin Clear relocalisé OK). ✅ **VAGUE 4 (système Menu curseur + YesNo) FAITE + logique vérifiée runtime (2026-06-06)** : le **`sMenu` 1:1** (struct menu.c:36) + `InitMenu`(static)/`InitMenuNormal`/`InitMenuInUpperLeftCorner`/`InitMenuInUpperLeftCornerNormal` + `RedrawMenuCursor` + `Menu_MoveCursor`/`Menu_MoveCursorNoWrapAround` + `Menu_GetCursorPos` + `Menu_ProcessInput`/`Menu_ProcessInputNoWrap`/`Menu_ProcessInputNoWrapClearOnChoose`/`ProcessMenuInput_other`/`Menu_ProcessInputNoWrapAround_other` + YesNo (`CreateYesNoMenu`/`CreateYesNoMenuAtPos`(static)/`CreateYesNoMenuInTopLeft`(UNUSED)/`DisplayYesNoMenuDefaultYes`/`DisplayYesNoMenuWithDefault`/`EraseYesNoWindow`/`GetYesNoWindowId`) relocalisés `gba-menu-system`→`menu.ts`. **🔑 REMONTÉ de la version SIMPLIFIÉE engine** (`menuCursorPos`/`menuNumItems`/`menuActive`, cursor char, pas de struct) **au `sMenu` décomp complet** (min/maxCursorPos, **wrap-around** vs clamp, `APressMuted`, son uniquement-si-bouge). Curseur 1:1 `gText_SelectorArrow3='▶'` via `RedrawMenuCursor`=`FillWindowPixelRect(PIXEL_FILL(1))`+`AddTextPrinterParameterized` (taille via `GetMenuCursorDimensionByFont`). Input via `JOY_NEW`/`JOY_REPEAT` (decomp-globals), constantes `A_BUTTON`/`B_BUTTON`/`DPAD_*` (io_reg-data), `MENU_NOTHING_CHOSEN`/`MENU_B_PRESSED` (menu-data), `SE_SELECT` (songs-data). **Hub** : `gba-menu-system` ré-exporte les 17 fns du miroir (importeurs starter-choose/script-opcodes*/start-menu INTACTS) ; logique simplifiée (`_processMenuInput`/`drawMenuCursor`/`clearMenuCursor` + state) SUPPRIMÉE, imports nettoyés. Dépendance **menu→gba-menu-system = sens unique** (pas un cycle ; menu n'importe pas gba-menu-system). **tsc 0, dédup 0 fn dup** (menu fns single-source), **boot 0 err** (cycle decomp-globals↔gba-menu-system↔menu runtime-safe). **Vérifié runtime DÉTERMINISTE (CreateYesNoMenu + pilotage `gMain.newKeys`)** : cursor 0→1 (DOWN), clamp 1 (DOWN, NoWrap), 1→0 (UP), clamp 0 (UP), **A → ret 0 (=OUI) + EraseYesNoWindow** (boîte retirée, screenshot OW propre = cycle create→nav→select→erase complet). **Rendu de CADRE = chemin `DrawStdFrameWithCustomTileAndPalette` déjà validé V3 + identique à start-menu/bag/party.** 🐛 **RÉGRESSION trouvée+corrigée (user A/B)** : j'avais init `sYesNoWindowId = 0` (litéral décomp `u8` menu.c:68), mais le start-menu (PAS encore mirroré) gate la création du YesNo sur `GetYesNoWindowId() < 0` (start-menu.ts:856/901) → `0 < 0` faux → **boîte SAUVER jamais créée**. FIX = restaurer le **sentinel ENGINE `-1`** (init + reset dans `EraseYesNoWindow`), marqué glue transitoire (à retirer quand start_menu.c sera porté avec gating par task-state). **✅ VISUEL OUI/NON CONFIRMÉ in-game (START→SAUVER, screenshot user)** : cadre std (0x214) + curseur **▶ sur OUI** + texte OUI/NON + panneau infos-save + dialogue « Voulez-vous sauvegarder la partie? » rendent **1:1 correctement** (= aussi une validation visuelle du cadre std V3 + du curseur menu V4). ⚠️ (En OW brut hors contexte, le YesNo s'affiche en boîte noire car le gfx bordure std 0x214 + palette 14 ne sont chargés que par un contexte appelant `LoadMessageBoxAndBorderGfx`/`Menu_LoadStdPal` — PAS un bug.) ✅ **VAGUE 4-bis (MenuAction printers + grid cursor) FAITE + vérifiée (2026-06-06)** : ADDITIF (aucun équivalent engine) — `struct MenuAction` + `PrintMenuActionTextsAtPos`/`PrintMenuActionTexts`/`PrintMenuTable`/`PrintMenuActionTextsInUpperLeftCorner` + `PrintMenuActionGridText`/`PrintMenuActionGrid`/`PrintMenuGridTable` + grid cursor (`InitMenuGrid`/`MoveMenuGridCursor`/`ChangeMenuGridCursorPosition`[wrap]/`ChangeGridMenuCursorPosition`[clamp]/`Menu_ProcessGridInput`/`InitMenuActionGrid`) + `SetWindowTemplateFields`/`AddWindowParameterized`. tsc 0, dédup 0 fn dup. (Pas de caller post-camion encore → logique 1:1-transcrite, vérif runtime quand un grid menu sera porté.) UNUSED différés : `PrintMenuActionTextsWithSpacing` (dépend `AddTextPrinterParameterized5` non porté) + variants `*AtTop`/`_TopLeft`/`*NoSpacing`/`*Repeat`. 🐛🔴 **RÉGRESSION BOOT trouvée+corrigée (user « c'est bon »)** : importer `menu_helpers` (pour `GetLRKeysPressed`, 1:1 grid input) DANS le `menu.ts` foundational (tiré par les hubs gba-window/menu/text-system) forçait l'éval de la chaîne `menu_helpers→mail-data→save/pokemon` PENDANT l'init foundational → **cassait l'ordre d'init ESM (boot sans canvas, 0 erreur console/serveur — échec silencieux ; le vrai blocage était masqué par des erreurs esbuild STALE dans le log serveur)**. FIX = `GetLRKeysPressed` en **import DIFFÉRÉ** `void import('./menu_helpers').then(m=>_GetLRKeysPressed=m.GetLRKeysPressed)` (éval menu_helpers APRÈS la phase sync du boot, comme bag-menu/mail ; grid input runtime ≫ après résolution ; L/R inactif avant). Constantes `MENU_L/R_PRESSED` importées de `menu_helpers-data` (léger). Aussi : `menu_helpers` importe `OPTIONS_BUTTON_MODE_LR` de `global-data` (pas gba-menu-system) = 1 arête de cycle cassée. Boot vérifié propre (canvas 1, DEBUG, 0 err), YesNo non-régressif. **LEÇON : ne pas tirer un module « lourd/à chaîne profonde » (menu_helpers→mail-data) dans un module FOUNDATIONAL (menu.ts, ré-exporté par les hubs) via import statique — utiliser un import différé ; et se méfier des erreurs esbuild STALE dans le log serveur quand on diagnostique un boot cassé (vérifier l'état ACTUEL du fichier, pas le log).** **RESTE V4-bis-2 (déféré)** : `DisplayItemMessageOnField` (task-couplé), `sMenuInfoIcons` (move-info + blit), start-menu/map-popup window-mgmt (= start_menu.c), HofPCTopBar_* (niche endgame), Decompress*/TempTileData (HW+heap+task). Plus tard : consolider `STD/DLG_WINDOW_*` locaux start-menu/bedroom-pc→miroir ; câbler `preloadStdMenuPalette` au boot OW. |
| menu_helpers.c | menu_helpers.ts | ◑ (cœur autonome) | **✅ Cœur autonome porté + vérifié (2026-06-06)** : `src/game/menu_helpers.ts`+`include/` = 9 fns logiques 1:1 — `SetCursorWithinListBounds`/`SetCursorScrollWithinListBounds` (relocalisées d'`engine/ui/menu-helpers.ts` **SUPPRIMÉ** ; pattern `ListPos` pour `u16*`), `SetItemListPerPageCount` (retourne `{pageItems,totalItems}` = 2 out-params), `GetLRKeysPressed`/`GetLRKeysPressedAndHeld` (`gMain.newKeys`/`newAndRepeatedKeys` + `optionsButtonMode`), `AdjustQuantityAccordingToDPadInput` (`IntRef` pour `s16*`, JOY_REPEAT DPAD + PlaySE SE_SELECT), `IsHoldingItemAllowed`/`IsWritingMailAllowed`/`MenuHelpers_IsLinkActive`/`MenuHelpers_ShouldWaitForLinkRecv` (link/union = single-player FR → false, frontière hors-scope). Consts `MENU_L/R_PRESSED` (menu_helpers.h) + bits DPAD/L/R (1:1 io_reg.h, locaux → futur `include/gba/io_reg.ts`). **Dédup** : `MenuHelpers_IsLinkActive` consolidé (locaux bag-menu+mail retirés → import miroir). bag-menu importe du miroir. **tsc 0, dédup 0 dup fn, boot OK (DEBUG), helpers purs vérifiés headless 1:1** (SetItemListPerPageCount 2/2, SetCursorWithinListBounds 2/2, SetCursorScrollWithinListBounds = la boucle réévalue le `cursor` MUTÉ → 1 itér., 1:1 exact). **RESTE menu_helpers.c (HW/task-couplé = frontière engine)** : `ResetVramOamAndBgCntRegs`/`ResetAllBgsCoordinates`/`SetVBlankHBlankCallbacksToNull` (regs HW) ; `DisplayMessageAndContinueTask`/`RunTextPrintersRetIsActive` (texte+task) ; `DoYesNoFuncWithChoice`/`CreateYesNoMenuWithCallbacks` (YesNo+task) ; swap-line sprites (Load/Create/Destroy/SetInvisibility/UpdatePos). |
| list_menu.c | list_menu.ts | ☐ | |

### Phase B — modèle de données
| décomp .c | src/game/ | statut | notes |
|---|---|---|---|
| pokemon.c | pokemon.ts | ◑ AMORCE | **Port INCRÉMENTAL commencé (2026-06-05)** : helpers PURS portés (`GetNatureFromPersonality` · `ModifyStatByNature` + `gNatureStatTable` transcrite · macro `GET_UNOWN_LETTER`/`GET_SHINY_VALUE` · `GetGenderFromSpeciesAndPersonality` number · `IsShinyOtIdPersonality` + miroir `include/global.ts` HIHALF/LOHALF · table `gStatStageRatios`) — tsc 0 + harness voie L + test positif shiny vérifiés. **Purs ÉPUISÉS** ; reste = migration STOCKAGE (mon). 🐛 **signature `ModifyStatByNature` restaurée 1:1** (1-based : STAT_ATK=1…STAT_SPDEF=5 + `table[statIndex-1]`, vs la version 0-based aplatie de party-storage → délégation `+1` préservant le résultat). Aussi vanilla (sans BUGFIX) : retVal u16 tronqué avant /100. 4 dups consolidées (party-storage/flavor-compat/mail-data). **RESTE = migration STOCKAGE** (PokemonInstance→BoxMon, `gPlayerParty` canonique → résout split V/L) = GROS, à faire AVEC user (A/B). Autres fns pures à porter ensuite : `GetUnownLetterByPersonality` (impl pokemon_icon.c), `GetLevelFromMonExp/BoxMonExp`. NB : version *string* de `GetGenderFromSpeciesAndPersonality` (pokemon/pokemon.ts) reste = couplée migration species. |
| data.c | data.ts | ☐ | gSpeciesInfo/gBattleMoves bridges (data auto en decomp-data) |
| daycare.c | daycare.ts | ☐ | |
| pokemon_size_record.c | pokemon_size_record.ts | ☐ | |
| mail_data.c | mail_data.ts | ☐ | |

### Phase C — field / overworld
| décomp .c | src/game/ | statut | notes |
|---|---|---|---|
| overworld.c | overworld.ts | ☐ | CB2_Overworld, warp, map load |
| fieldmap.c | fieldmap.ts | ☐ | |
| field_camera.c | field_camera.ts | ☐ | (existant field/field-camera) |
| field_player_avatar.c | field_player_avatar.ts | ☐ | (existant field/player-avatar) |
| event_object_movement.c | event_object_movement.ts | ☐ | GROS long-tail mouvement PNJ |
| metatile_behavior.c | metatile_behavior.ts | ✅ | **`src/game/metatile_behavior.ts` créé (140 prédicats, relocalisé par déplacement, import MB_* fixé) + `field/metatile-behavior.ts` re-exporte (reexportSameFn=true) + boot OK + IsTallGrass→[2] vérifié runtime. tsc 0.** Omis 1:1-justifié = 4 `Unref_*` (UNUSED dead-code). ✅ **HELPERS DÉDUPLIQUÉS** : `field/metatile-behavior-helpers.ts` re-exporte les prédicats du miroir + garde ses 5 fns uniques movement/bike (IsMetatileDirectionallyImpassable/ShouldJumpLedge/IsRunningDisallowed* + lookup tables). Vérifié boot + `impassable_door_east`. <br>🐛 **4 BUGS 1:1 TROUVÉS+CORRIGÉS+VÉRIFIÉS** (le miroir les a fait surface en comparant 2 impls divergentes aux 256 bytes vs le décomp) : `IsEastBlocked`/`IsWestBlocked` manquaient `MB_SECRET_BASE_BREAKABLE_DOOR` ; `IsReflective` manquait `MB_UNUSED_SOOTOPOLIS_DEEP_WATER_2` (et avait des MB en trop) ; `IsShallowFlowingWater` avait `MB_NO_SURFACING` (FAUX) + manquait `MB_SHOAL_CAVE_ENTRANCE`. = vrais bugs gameplay (collision portes secret-base, reflets, eau). <br>🐛 **Bug constantes trouvé** : `metatile-behavior-constants` est INCOMPLET (manque MB_UNUSED_SOOTOPOLIS_DEEP_WATER_2…) → le miroir source désormais `MBC` depuis `ENUM_MB_0` (metatile_behaviors-data, enum auto-extrait complet). **AUDIT COMPLET 140 prédicats FAIT** (diff ensembles-MB décomp vs miroir + tri runtime des artefacts) → **2 bugs de plus trouvés+fixés+vérifiés** : `IsBridgeOverWater` (avait MED_EDGE_1/2 hors-décomp, manquait UNUSED_BRIDGE+BIKE_BRIDGE_OVER_BARRIER) ; `IsBridgeOverWaterNoEdge` (faisait 3 égalités au lieu du RANGE `>=OCEAN && <=POND_HIGH`, manquait OCEAN). `IsUnableToEmerge` = vanilla-correct (MB_WATER_DOOR sous #ifdef BUGFIX, documenté). **= 6 bugs metatile 1:1 fixés ce tour.** ✅ valeurs MB_* de `metatile-behavior-constants` = 0 écart (saines ; 103 MB_UNUSED_* manquantes prises via MBC=ENUM_MB_0). `GetBridgeType` (seule fn non-bool) VÉRIFIÉE = **1:1 identique** au décomp (faux positif de l'audit : ranges). **→ MODULE 100% AUDITÉ 1:1, TERMINÉ** (140 prédicats + GetBridgeType). Reste cosmétique only : basculer les imports nommés du miroir sur ENUM_MB_0 (valeurs déjà bonnes). | `field/metatile-behavior.ts` = **136/140 prédicats fidèles** (spot-check `IsTallGrass = mb===MB_TALL_GRASS` 1:1) ; `field/metatile-behavior-helpers.ts` = **25 prédicats DUPLIQUÉS** (tous dans le main, 0 unique) = pure toile. **PLAN consolidation (next, mécanique)** : (1) relocaliser le main → `src/game/metatile_behavior.ts` (import MB_* depuis `../engine/system/metatile-behavior-constants`) ; (2) réconcilier 4 noms décomp absents du main (IsArrowWarp + 3 « Unused » — apparaissent dans le .c mais pas comme défs standard, à vérifier) ; (3) `helpers` + ancien `main` → re-export du miroir (élimine les 25 dups) ; (4) tsc + spot-check. Aucun prérequis, aucune divergence structurelle. |
| field_control_avatar.c | field_control_avatar.ts | ☐ | (existant field/) |
| wild_encounter.c | wild_encounter.ts | ☐ | (existant field/, 1:1 vérifié) |
| field_poison.c | field_poison.ts | ☐ | |
| field_effect.c / _helpers.c | field_effect(.helpers).ts | ☐ | |
| field_message_box.c | field_message_box.ts | ☐ | |
| field_tasks.c | field_tasks.ts | ☐ | |
| field_weather.c / _effect.c | field_weather(.effect).ts | ☐ | |
| field_screen_effect.c | field_screen_effect.ts | ☐ | |
| script.c | script.ts | ☐ | VM scripts (existant script/script-runtime = bridge JSON, à reconsidérer) |
| scrcmd.c | scrcmd.ts | ☐ | opcodes script (existant script/) |
| script_movement.c | script_movement.ts | ☐ | |
| script_pokemon_util.c | script_pokemon_util.ts | ☐ | |
| script_menu.c | script_menu.ts | ☐ | |
| event_data.c | event_data.ts | ✅ FAIT | **MAJ 2026-06-05 (2e passe) : helpers cross-module flags/vars PORTÉS** (ClearTempFieldEventData/ClearDailyFlags · Mystery Event+Gift · ClearMysteryGift{Flags,Vars} · Reset RTC) — 1:1 strict par-id, ⚠️ piège mémoire géré : `memset(vars,…,NUM_TEMP_VARS*2 BYTES)` décomp = `NUM_TEMP_VARS` ÉLÉMENTS sur notre `number[]` u16. National Dex (c:55-80) seul différé (pokedex). Dups consolidées vers le miroir : CanResetRTC (×2 dont 1 name-keyed cassé) + IsMysteryGiftEnabled. Vérifié end-to-end (round-trip flag+var). <br>**MIGRATION 1:1 COMPLÈTE + VÉRIFIÉE (2026-06-05).** Save block `flags`/`vars` → **`number[]`** bit-packé/indexé (layout décomp) ; `src/game/event_data.ts` (FlagSet/Clear/Get + VarGet/Set + GetVar/FlagPointer + gSpecialVars + InitEventData, par ID) ; `script-vars.ts` = **bridge nom→id** (via `include/constants/flags.ts`/`vars.ts` résolus, fallback legacy anti-régression) ; gSpecialVar.Result/LastTalked/Facing → special vars par id. Accesseurs fixés (dev-audit-tools, object-events VarGet). **Vérifié runtime** : boot OK, var round-trip (runtime `__gVars`), flag round-trip intra-bridge (set/get/clear), special vars (gSpecialVar.Result≡VarGet VAR_RESULT), **171 flags set lus correctement par nom**, **`saveSelfTest_sb1: true`** (round-trip save number[]↔JSON). tsc 0. ⚠️ pokedex/trainer-card comptent des FLAG_*_SEEN/_CAUGHT (= non-1:1 pré-existant, pokemon pas des flags → vrai 1:1 = bitsets gSaveBlock2.pokedex ; 0 régression = restaient 0). À PORTER : helpers cross-module event_data.c:39-162 (National Dex/Mystery/ClearTemp). <br>_Histo prérequis :_ `scripts/gen-event-data-ids.mjs` résout la chaîne de #define de flags.h/vars.h → `src/game/include/constants/flags.ts` (1089) + `vars.ts` (258), VÉRIFIÉ (FLAG_TEMP_1=1, VAR_RESULT=0x800D, FLAG_BADGE01_GET=2151… matchent le décomp ; tsc 0). → la migration event_data peut se faire : save block→number[], event_data.ts par-id, bridge nom→id via ces tables, ~7 accesseurs. (Détail plan ci-dessous.) <br>_Histo : TENTÉ→REVERTÉ car_ une table résolue `FLAG_*`/`VAR_*` → id NUMÉRIQUE. Notre extraction (`decomp-data/include/constants/flags-data.ts`) garde les ids comme **EXPRESSIONS NON RÉSOLUES** (`"(TEMP_FLAGS_START + 0x1)"`) ; `resolveDecompConstant` ne résout PAS les `FLAG_*`/`VAR_TEMP_*` (seulement quelques special vars : VAR_RESULT=0x800D, VAR_FACING=0x800C ✓). Sans cette table, le bridge nom→id no-op les flags → scripts cassés. **UNBLOCK = construire un résolveur d'ids** (évaluer les chaînes d'expr de flags.h/vars.h : chaque section START = END précédent +1, FLAG_X = START + offset). = tâche dédiée (extraction déterministe), candidate délégation devtools/extraction. C'est exactement pourquoi l'ancien code stocke par NOM (évite les ids). save-blocks/script-vars = INCHANGÉS (name-keyed, marche). <br>**PLAN une fois débloqué (1 passe)** : <br>• **Stockage = `number[]` PAS Uint8Array** (save = `JSON.stringify`, save-sectors.ts:109 → Uint8Array casse le round-trip, number[] OK). <br>• `save-blocks.ts:1100/1102` : `flags: number[]` (taille `FLAGS_COUNT>>3`) + `vars: number[]` (256), init `.fill(0)`. <br>• Constantes décomp : VARS_START=0x4000, VARS_COUNT=256 (0x40FF), SPECIAL_VARS 0x8000-0x8015 (22, séparés/non-sauvés via gSpecialVars[]), SPECIAL_FLAGS_START=0x4000 (sSpecialFlags 128=16o), FLAGS_COUNT=DAILY_FLAGS_END+1. <br>• `src/game/event_data.ts` : Get/Flag/VarPointer + FlagSet/Clear/Get + VarGet/Set (par ID, bit-ops sur number[]) + gSpecialVar_* + InitEventData/ClearTemp/National Dex/etc. <br>• `script-vars.ts` → **bridge** : FlagSet(name)→id=resolveDecompConstant(name)→mirror FlagSet(id) ; idem Var. Garder gSpecialVar obj/Compare/ResetScriptVars/debug. <br>• **~7 accesseurs directs à fixer** : pokedex-screen:45 + trainer-card-screen:361 (`Object.keys(flags)` → compter les bits set) ; object-events:200/4775 (`vars[name]` → VarGet) ; dev-scope:320/329 (debug). <br>• **Vérif** : boot + set/get flag + set/get var + 1 script + save round-trip (`__saveSectorsSelfTest`). ⚠️ Ancien save casse (format change) = OK. |
| map_name_popup.c | map_name_popup.ts | ☐ | |
| trainer_see.c | trainer_see.ts | ☐ | PNJ détection |
| event_object_lock.c | event_object_lock.ts | ☐ | |
| field_specials.c | field_specials.ts | ☐ | |
| field_door.c | field_door.ts | ☐ | |
| fldeff_*.c | fldeff_*.ts | ☐ | cut/dig/flash/rocksmash/strength/teleport/sweetscent/softboiled |
| rotating_gate.c | rotating_gate.ts | ☐ | |
| bike.c | bike.ts | ☐ | |
| tileset_anims.c / tilesets.c | tileset_anims.ts / tilesets.ts | ☐ | |

### Phase D — combat (beaucoup déjà porté en `engine/battle/`, à RELOCALISER + dé-spiderweb)
| décomp .c | src/game/ | statut | notes |
|---|---|---|---|
| battle_main.c | battle_main.ts | ☐ | consolide battle-main-functions/battle-cb2/battle-init |
| battle_util.c | battle_util.ts | ☐ | consolide atk-canceler/disobedience/ability-battle-effects/end-turn… |
| battle_util2.c | battle_util2.ts | ☐ | |
| battle_script_commands.c | battle_script_commands.ts | ☐ | 12k lignes (existant) |
| battle_controllers.c | battle_controllers.ts | ☐ | |
| battle_controller_player.c | battle_controller_player.ts | ☐ | (existant, anims 1:1 récentes) |
| battle_controller_opponent.c | battle_controller_opponent.ts | ☐ | (existant) |
| battle_interface.c | battle_interface.ts | ☐ | consolide healthbox/healthbox-l/hp-bar (résout le bug nom-reshow) |
| battle_bg.c | battle_bg.ts | ☐ | |
| battle_intro.c | battle_intro.ts | ☐ | |
| battle_message.c | battle_message.ts | ☐ | consolide battle-string-decoder/message |
| battle_setup.c | battle_setup.ts | ☐ | consolide battle-setup-helpers/trainer-battle-flow |
| battle_gfx_sfx_util.c | battle_gfx_sfx_util.ts | ☐ | |
| pokeball.c | pokeball.ts | ☐ | capture + slide healthbox |
| battle_anim*.c | battle_anim*.ts | ☐ | gros (anims de moves) — A/B visuel |
| battle_transition.c | battle_transition.ts | ☐ | |
| reshow_battle_screen.c | reshow_battle_screen.ts | ☐ | (existant, 1:1) |
| battle_ai_script_commands.c | battle_ai_script_commands.ts | ☐ | (existant battle/ai/) |
| battle_ai_switch_items.c | battle_ai_switch_items.ts | ☐ | |
| battle_tv.c / battle_records.c | (différé) | — | |

### Phase E — menus / sous-écrans
| décomp .c | src/game/ | statut | notes |
|---|---|---|---|
| party_menu.c | party_menu.ts | ☐ | (existant ui/party-screen, 1:1 structurel) |
| item.c | item.ts | ☐ | (existant bag/bag, AddBagItem rollback 1:1) |
| item_menu.c | item_menu.ts | ☐ | (existant bag/bag-menu) |
| item_use.c | item_use.ts | ☐ | |
| item_icon.c / item_menu_icons.c | item_icon.ts / item_menu_icons.ts | ☐ | |
| shop.c | shop.ts | ☐ | |
| pokemon_summary_screen.c | pokemon_summary_screen.ts | ☐ | (existant ui/summary-screen) |
| pokedex.c | pokedex.ts | ☐ | (existant, backbone 1:1) |
| start_menu.c | start_menu.ts | ☐ | |
| option_menu.c | option_menu.ts | ☐ | |
| naming_screen.c | naming_screen.ts | ☐ | |
| money.c | money.ts | ☐ | (existant, 1:1) |
| coins.c | coins.ts | ☐ | (existant, 1:1) |
| mail.c | mail.ts | ☐ | |
| region_map.c | region_map.ts | ☐ | |
| mon_markings.c | mon_markings.ts | ☐ | |
| move_relearner.c | move_relearner.ts | ☐ | |

### Phase F — save / divers
| décomp .c | src/game/ | statut | notes |
|---|---|---|---|
| save.c | save.ts | ☐ | (existant save/, secteurs 1:1) |
| load_save.c | load_save.ts | ☐ | |
| save_location.c | save_location.ts | ☐ | |
| rtc.c | rtc.ts | ☐ | ⚠️ cycle d'init (deadlock boot connu) à résoudre proprement dans le miroir |
| clock.c | clock.ts | ☐ | |
| time_events.c | time_events.ts | ☐ | |
| play_time.c | play_time.ts | ☐ | |
| heal_location.c | heal_location.ts | ☐ | white-out (#15) |
| evolution_scene.c | evolution_scene.ts | ☐ | |
| evolution_graphics.c | evolution_graphics.ts | ☐ | |
| egg_hatch.c | egg_hatch.ts | ☐ | |
| new_game.c | new_game.ts | ☐ | post-camion partie (pré=exclu) |
| post_battle_event_funcs.c | post_battle_event_funcs.ts | ☐ | |
| birch_pc.c | birch_pc.ts | ☐ | rating |
| gym_leader_rematch.c | gym_leader_rematch.ts | ☐ | |

---

### Phase G — autres in-scope (niche / à classer / sprites mon)
| décomp .c | src/game/ | statut | notes |
|---|---|---|---|
| pokemon_animation.c | pokemon_animation.ts | ☐ | anims du sprite mon (intro/combat) |
| pokemon_icon.c | pokemon_icon.ts | ☐ | icônes mon (menus/party) |
| trainer_pokemon_sprites.c | trainer_pokemon_sprites.ts | ☐ | sprites dresseur/mon (combat/intro) |
| berry.c | berry.ts | ☐ | baies : pousse/cueillette (field) |
| berry_tag_screen.c | berry_tag_screen.ts | ☐ | écran tag baie |
| safari_zone.c | safari_zone.ts | ☐ | mode Safari (field+combat) |
| coord_event_weather.c | coord_event_weather.ts | ☐ | météo par coord (field) |
| braille_puzzles.c | braille_puzzles.ts | ☐ | énigmes braille (field) |
| field_special_scene.c | field_special_scene.ts | ☐ | scènes spéciales (camion/bateau…) — **inclut l'arrivée camion = début du vrai jeu** |
| give_gift_ribbon_to_party.c | give_gift_ribbon_to_party.ts | ☐ | rubans cadeaux |
| player_pc.c | player_pc.ts | ☐ | PC de la maison (menu) |
| menu_specialized.c | menu_specialized.ts | ☐ | menus spécialisés (helpers UI) |
| digit_obj_util.c | digit_obj_util.ts | ☐ | sprites de chiffres (compteurs UI) |
| mon_markings.c | mon_markings.ts | ☐ | marquages mon (déjà listé E) |
| save_failed_screen.c | save_failed_screen.ts | ☐ | écran échec sauvegarde |
| reload_save.c | reload_save.ts | ☐ | rechargement save |
| reset_rtc_screen.c | reset_rtc_screen.ts | ☐ | reset RTC (niche) |
| field_region_map.c | field_region_map.ts | ☐ | mini carte région (field) |
| script_pokemon_util.c | script_pokemon_util.ts | ☐ | utils mon pour scripts (déjà listé C) |

> Couverture in-scope : tables A→G. Les `.c` NON listés ici = couche matériel ou hors-scope
> (voir sections du haut). Au moindre doute en portant, vérifier la catégorie avant.

## ⚙️ RÈGLES APPRISES (à appliquer partout)
- **Tables de DONNÉES : TRANSCRIRE les littéraux de la décomp, JAMAIS recalculer.** Le fixed-point décomp `(s16)(x * N)` TRONQUE vers zéro, ≠ `Math.round`. Vérifié sur `gSineDegreeTable[84,88,92,96]` : un calcul `round()` aurait donné 4 valeurs FAUSSES. Les littéraux décomp SONT la spec (et rendent le miroir diffable). Réutiliser la donnée déjà extraite dans `decomp-data/` quand elle existe (vérifiable), sinon transcrire.
- **Vérifier chaque « bug » trouvé avant de le claim** (cf. `& 0xFF` cru bug → en fait équivalent par périodicité de la table). Ne jamais inscrire un faux fix.
- **Cycle header↔impl bénin** (n'échange que des fonctions/consts, 0 exec top-level) = OK ; cycle qui tire de l'exec top-level (save/rtc) = deadlock → à éviter.

## 🔧 ÉTAT DEVTOOLS / COUVERTURE (2026-06-05, agent)
- **`coverage:1to1` / `audit:1to1` / `lint:1to1` / `pair:1to1` walkent déjà `src/`** (parent de engine/+game/) → le miroir est **compté porté automatiquement** dès création (citations `1:1 décomp X.c:N`). Confirmé : random 4/4, trig 4/4, math_util 9/9, **util 10/11** (DoBgAffineSet HW différé), zéro double-comptage.
- **`check-duplicate-helpers.mjs`** corrigé (scanne maintenant `src/engine`+`src/game`) → détecte les dups engine↔miroir pendant la migration. **Lancer `node scripts/check-duplicate-helpers.mjs` après chaque vague** pour traquer les dups à consolider.
- ⚠️ **Dette pré-existante (hors-vague, à nettoyer un jour)** : les tables `MAPPING` (`audit-1to1-decomp.mjs`) et `PORT_MAPPING` UNCITED (`audit-decomp-1to1-coverage.mjs`) ont plein de chemins `src/engine/*.ts` périmés (fichiers déplacés en sous-dossiers lors de refactors). N'affecte PAS le compte de couverture cité.
- Consolidation faite suite au flag : `wallclock.ts` Sin2/Cos2 (ex-`Math.round(sin/cos)` NON-1:1) → importe le miroir `trig` (1:1, table tronquée). tsc 0.

## 📌 NOTES DE MIGRATION
- **Anti-casse** : le pré-camion (boot/Birch/title) reste sur `src/engine/`/scenes existantes → intact.
  Pour chaque module migré, l'ancien `engine/.../*.ts` devient un **re-export transitoire** depuis
  `src/game/X.ts` (zéro dup de logique) PUIS on bascule les importeurs PUIS on supprime l'ancien.
- **Globals décomp** (`gPlayerParty`, `gMain`, `gSaveBlock1Ptr`…) : déjà sur globalThis via
  `engine/system/decomp-globals` → le miroir les lit/écrit là (pont ES-module↔« RAM »).
- **Le split V/L PokemonInstance↔BoxMon** se résout en Phase B (un seul `pokemon.ts` BoxMon 1:1).
- **devtools** : après chaque vague, déléguer à un agent la réparation des zones devtools obsolètes (seule délégation autorisée).
