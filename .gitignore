# Build outputs
node_modules/
dist/
.vite/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# Pokemon ROM (commercial binary, not redistributable)
public/decomp/em/rom.gba
public/decomp/**/rom.gba
public/decomp/**/*.gba

# byte-VM : bytecode compilé (node scripts/compile-scripts.cjs). COMMITÉ (plus ignoré) pour
# que le clone du repo boote SANS le décomp — cf partage à un pote (2026-07-12). Régénérable
# à la demande ; re-gitignorer après le partage si le churn de diffs gêne.

# External reference docs (PDFs/random)
*.pdf

# Reference videos (user's local files for visual comparison, not redistributable)
*.mp4

# Dev memos / audit notes (= local working docs, not part of the public repo).
# README.md reste tracké comme intro publique du projet.
AUDIT_*.md
DEV_LOG.md
ROADMAP.md
*_NOTES.md
*_SCRATCH.md

# ─────────────────────────────────────────────────────────────────────────────
# Outillage & documentation INTERNES — gardés en local, hors du repo public.
# (les fichiers restent sur le disque ; seuls exclus du suivi Git)
# ─────────────────────────────────────────────────────────────────────────────

# Doc de travail interne (plans FILE-OPUS, audits, héritage Fable5, checklists…)
docs/
audit-reports/
CLAUDE.md
.claude/

# Scripts d'AUDIT / PROBE / ANALYSE 1:1 internes (vérification de fidélité au
# décomp). NON nécessaires au build ni à la génération d'assets : les pipelines
# extract-* / gen-* / compile-* / render-* / transpile-callbacks restent trackés,
# ainsi que les scripts référencés par package.json.
scripts/audit-*.cjs
scripts/audit-*.mjs
scripts/probe-*.mjs
scripts/check-*.mjs
scripts/validate-*
scripts/analyze-*
scripts/sweep-*
scripts/qualify-*
scripts/classify-*
scripts/gfx-verify-*
scripts/list-*
scripts/cartograph-1to1.cjs
scripts/find-import-cycle.cjs
scripts/transpile-c.cjs
scripts/decomp-index.cjs
scripts/build-ts-symbol-index.cjs
scripts/build-helper-bridge-manifest.mjs
scripts/map-battle-dupes.cjs
scripts/shop-callgraph-completeness.cjs
scripts/reroute-from-bridge.cjs
scripts/wf-spine-decycle-map.js
scripts/dump-mist-pal.cjs
scripts/apply-pokemon-c-qualifications.cjs
scripts/m4a-oracle/
scripts/m4a/analyze-wav.mjs
scripts/m4a/spectrum-analyze.mjs
scripts/m4a/M4A_DECOMP_SPEC.md

# (obsolète : audit-reports/ est désormais ignoré en entier ci-dessus)
audit-coverage.json
